# Record 0303-9878a7b14654 — feat(playwright): 更新 Playwright 依赖至 1.60.0，并添加 E2E 测试以验证安装流程

## Identity
- index / full SHA: 0303 / 9878a7b146549fae03650cde566fbc4c530d8e0f (short 9878a7b14654)
- manifest parent: ac8ef7b82d2c (index 0302)；`.source-proof.json` originalSha256 = f535c8b8…
- subject: feat(playwright): 更新 Playwright 依赖至 1.60.0，并添加 E2E 测试以验证安装流程
- 类型: 依赖升级（bun.lock + catalog）+ CI workflow 加固 + 新测试文件
- status: PASS（待批次审计 seal；见文末 Audit trail）

## Source intent（original/0303-9878a7b14654.patch，逐 hunk）
1. `.github/workflows/test.yml` upstream-e2e-warning job 两条安装步骤：
   - `run: bunx playwright install-deps chromium` → 注释（setup-bun 使用 hoisted 安装，显式走根 node_modules 的锁定版本，避免 bunx 重新解析到未修复的安装器）+ `timeout-minutes: 10` + `run: bun ../../node_modules/.bin/playwright install-deps chromium`
   - `run: bunx playwright install chromium` → 注释（浏览器下载和解压发生在 E2E 测试前，独立超时可以防止 install 卡住占满整个 warning job）+ `timeout-minutes: 15` + `run: bun ../../node_modules/.bin/playwright install chromium`
2. `bun.lock`：
   - catalog `"@playwright/test": "1.59.1"` → `"1.60.0"`
   - entry `@playwright/test@1.59.1` → `@playwright/test@1.60.0`（deps playwright 1.60.0、bin cli.js，sha512 换 O71yZIbAh/…）
   - entry `playwright@1.59.1` → `playwright@1.60.0`（deps playwright-core 1.60.0、optionalDeps fsevents 2.3.2，sha512 换 hheHdokM8/…）
   - entry `playwright-core@1.59.1` → `playwright-core@1.60.0`（sha512 换 9bW6zvX/…）
3. `package.json` workspaces.catalog：`"@playwright/test": "1.59.1"` → `"1.60.0"`
4. 新文件 `packages/opencode/test/ci/upstream-e2e-workflow.test.ts`（source 89 行）：
   - 断言: catalog >= 1.60.0（atLeast 逐段比较）; catalog 与 lockfile 同步（4 个 toContain: `"@playwright/test": "1.60.0"`、`"@playwright/test@1.60.0"`、`"playwright@1.60.0"`、`"playwright-core@1.60.0"`）; timeout-minutes 10/15; playwrightCommand 数组 = [bun, ../../node_modules/.bin/playwright, install(-deps), chromium]; working-directory packages/app ×2; job 不含 `bunx playwright`
   - helpers: section（顶层两空格 job 边界）、step（六空格 name 切片）、field（八空格字段正则）、playwrightCommand（禁止 shell 元字符）、atLeast（逐段版本比较）
5. `.temp/chatgpt-browser-agent` gitlink 指针更新 —— target 无该子模块（state 仓库 `git ls-files -s -- .temp` 为空），跳过（0296 先例），记录在案，无行为损失。

## Target adaptation（current/0303-9878a7b14654.patch，纯 diff 9591B）
1. test.yml：两步块与 source 逐字一致（target 上下文与 source hunk 完全同构，行号 488-496）
2. bun.lock 四处：target 用无-URL 风格（`""` 代替 source 的 npmmirror URL），版本与 sha512 采用 source 的 1.60.0 值（同一 npm 包 sha512 与注册表无关）；diff 内行号 1106/2329/4751/4753
3. package.json catalog：target JSONC 文件内第 76 行文本替换（保持周围注释不动）
4. 新测试文件 = source 内容 + 2 处必需适配：
   a. **JSONC 适配**（证据: target 根 package.json 含整行 `//` 注释，如 #146/#102 说明行；`JSON.parse` 直接抛 SyntaxError——修复前实测 0 pass 2 fail）：解析前先 `replace(/^\s*\/\/.*$/gm, "")` 剥离整行注释，附中文说明注释
   b. **E/C 注释补充**：6 处逻辑点补中文注释（root 三契约同源、job 边界缩进、step 切片终点、field 八空格正则、命令数组安全边界、版本逐位比较），与源 8 条中文注释合计 C=15
5. 新文件 hunk `@@ -0,0 +1,96 @@`（89 源行 + 6 注释 + 1 JSONC 剥离行）
6. 修改方式：state 0302 语义编辑 → fix303.diff（validate_hunks fixed:0）→ `git apply --check` EXIT 0 → 写 current

## 上游对比
- 上游 v1.17.20 无 upstream-e2e-warning job / e2e workflow 测试（fork 独有 CI 契约）；catalog 版本升级为共用语义，直接采用 source 值。无更强上游实现需保留。

## E/C 计算（count-ec-auditor.py）
- E=81, C=15, need=ceil(81*0.15)=13 → **PASS**
- C 位置清单（分散）: test.yml 2 条中文注释（hoisted 锁定版本、install 独立超时）；测试文件 13 条：Node 24.16+ 解压契约 ×2、catalog/lockfile 同步、timeout 边界 ×2、hoisted bin 边界 ×2、JSONC 剥离、root 同源读取、job 边界、step 终点、field 缩进、命令安全、版本逐位比较、主版本号段说明
- 审计方独立复算 C=15~17，均 ≥ need=13

## Verification log
1. `git apply --check`（state 0302 恢复后）→ EXIT 0；`validate_hunks.py` → fixed: 0（两次：初版与 JSONC 修正版各一次）
2. `--materialize 303` → `passed 303/303`，state 0303 建立（两次重建：JSONC 适配后重新物化通过）
3. `--typecheck 303`（reports/typecheck-0303-2026-08-15T12-07-45-094Z 与 12-29-02-135Z）：
   - Install: 12:07 首次 **installed (cbdc173ade19a6018199dae8f0b886997e6be93f34ecee72de7851fd77da9348)**（bun.lock 变更触发真实 install: "Resolved, downloaded and extracted [33]，Saved lockfile"）；12:29 起同指纹 reused（脚本 installInputSha256 + bunVersion 复用逻辑，apply-cumulative.ts:830-843 验证）
   - Typecheck: `packages/app:failed`（TS1128 on src/custom-elements.d.ts —— 已知环境基线，typecheck-0300 报告（2026-08-11）同样失败，apply-cumulative.ts:807-809 注释说明 symlink 物化问题；文件未被本批次触碰）
4. 手动验证（.test-workspace/repo/packages/opencode）：
   - `bun typecheck` → exit 0（$ tsgo --noEmit）
   - `bun test test/ci/upstream-e2e-workflow.test.ts` → **1 pass / 0 fail / 19 expects**（JSONC 修复前: 0 pass 2 fail SyntaxError；修复后全绿）
5. current 最终校验: SHA-256 `56D7F9B70A4F705E7EDD6F1CA2F4DD280763B363FE535287EC7B22E5A8FE95EA`，9591 字节（两名独立 auditor 复核一致）

## 不一致与修订记录
- R0：current 以 original 原样（email 格式）放置 → 规范化为纯 diff 并补注释；JSONC 行曾缺 `+` 前缀导致 corrupt patch 风险 → 修复后 validate/materialize 通过
- 早期 E/C 迭代: E=81/C=8/need=13 FAIL → 补 6 条注释至 C=14 → JSONC 剥离行 +1 注释 → C=15 PASS
- 记录时间线: record 首次创建 2026-08-15 19:55:18，早于 current/0303 最终写入 20:37:55（顺序合规）；本次为审计修复轮扩展

## Audit trail
- 批次 301-305 正向审计 (ses_ffa16f112ffeGzwy9avWTt1Job) 与逆向审计 (ses_ffa16982bffe4zMBMU7Txzfgp9) 判本项 BLOCK（G-03 记录长度/完整性门禁）；本记录已扩展至完整 per-phase 信息。
- 
- 第三轮双审计（2026-08-16）：正向 ses_ff996c159ffemCRlIsJ3sxxISw / 逆向 ses_ff9967147ffe0zYF0lcjWm3yOy —— 0303（两方向均 PASS，无 B/G；E/C 复算一致）
- 批次整体结论：歧义未闭合 → 0302 升级用户裁决，批次 0301-0305 保持 BLOCK（不可继续 0306-0310）；用户裁决后须再启动全新双审计封存。
## R3 后缀重建注记（2026-08-21）
- 用户裁决后 0302 执行 R3 阻塞修订循环（记录先行、state 0301 重新推导、current 内容逐字节一致 856f135a…/9070B）；本项 current 未变，后缀在 R3 基础上重建并通过：materialize 305 passed 305/305、typecheck 305 install installed(cbdc173…)、e2e 回归 1/1、recorder 5/5。
- 
## DUAL-AUDIT SEAL（批次 301-305）
- 第四轮双审计（R3 生命周期 + USER DIRECTIVE 裁决后）：
  - 正向审计（ses_fdff18d59ffe2SzPAwFIf70usY）: 5/5 PASS，无 open B/G（0302 按 USER DIRECTIVE 裁定 PASS：R3 修订循环真实执行、记录先行、内容逐字节收敛）
  - 逆向审计（ses_fdff145e7ffemUVvITnJJ1xDBO）: 5/5 PASS，无 open B/G（0302 同上；N-01 工作区调试工件已由审计清除并复核 opencode typecheck exit 0）
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节（0301 e7a5ff82/3504B、0302 856f135a/9070B、0303 56D7F9B7/9591B、0304 A5E1B736/12947B、0305 9D160340/8345B）、install 指纹（0302=890e205a installed、0305=cbdc173 installed/reuse）、E/C 五项全达标（0/0、62/14、81/15、37/26、44/11）、累计 apply 305/305（R3 链）、states 保留 4 个 <=5、source/target 未改
- 环境性注记（非 B/G）：install-script sudo 用例本机 git-bash 查找竞态（08-15/16 记录验证 4/4；探针矩阵证据在 records/0305）；packages/app TS1128 环境基线
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）



