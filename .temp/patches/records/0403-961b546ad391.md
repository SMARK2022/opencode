# 0403 — fix(ci): 禁止 CI 自动下载 LSP server + 修复 shell echo 测试 stdout 竞态

## 最终状态：PASS（R13）
## 适配：test.yml env 块（FORCE_JAVASCRIPT 后加 OPENCODE_DISABLE_LSP_DOWNLOAD，含中文注释说明 CSharp/FSharp 下载>30s 超时根因）；shell.test 3 处 echo 命令补 sleep 0.01（管道竞态/detached 调度抖动），锚定目标实际行文本
## E/C：E=21 C=6 need>=4 ✓
## 验证：materialize 403 Passed；typecheck 405 全 passed（批次合并验证）

---

## 全量审计补充（2026-09-13，双审 G-02 扩写）

### Identity
- index: 403；SHA: 961b546ad391；parent: 8810ecd5aa63（0402）
- original: .temp/patches/original/0403-961b546ad391.patch；current: .temp/patches/current/0403-961b546ad391.patch
- record: .temp/patches/records/0403-961b546ad391.md（本文件）

### source intent（original 全 hunk）
1. `.github/workflows/test.yml`：CI env 块新增 `OPENCODE_DISABLE_LSP_DOWNLOAD: "true"`（含 4 行中文根因注释：CSharp/FSharp LSP 在 macOS/Windows runner 自动下载 roslyn，拖垮 BOM 测试 30s 超时）。防回归 invariant：CI 环境不得触发 LSP 二进制下载。
2. `packages/opencode/test/tool/shell.test.ts`：3 处 echo 测试命令尾部补 `sleep 0.01`（各带中文注释：detached 进程写 stdout 管道的竞态，父进程先退出导致偶发 EPIPE/丢输出）。测试意图：消除 3 个 flaky 竞态，不改变断言。

### 目标适配（逐 hunk 映射）
- hunk1（test.yml env）：直接 apply；落点 state `test.yml:28`。owner 正确性：`OPENCODE_DISABLE_LSP_DOWNLOAD` 由目标 `src/effect/runtime-flags.ts:24` 解析（RuntimeFlags.disableLspDownload），消费点 `src/lsp/server.ts` 共 27 处（含 `installRoslynLanguageServer` 的 disable 分支——即 source 指认的 roslyn 下载根因）。flag→消费链完整可达。
- hunk2-4（shell.test ×3）：目标 shell.test.ts 行号漂移（240/1483/1626），锚点重定位；3 处 `sleep 0.01` + 中文注释全保留；断言零改动。
- 不一致：source 与目标 shell.test.ts 的行号上下文不同（目标在该文件有前序 patch 的改动），仅行号映射，无语义差异。

### E/C
- E≈5（1 行 CI env + 3 处命令修改行；注释行不计入 E）；C=8（env 块 4 行根因注释 + 3 处 sleep 注释 + 1 处测试意图说明），need = max(1, ceil(5×0.15)) = 1，C=8 ≥ 1 ✓。注释分散于各决策点，非集中堆放。旧条目 E=21/C=6 口径含注释行，以本节为准。

### 验证
- materialize 403 passed（累计链 materialize-0410 410/410 复验）
- typecheck：typecheck-0410（2026-09-13T14-33-56Z）30/30 workspaces passed；全链 typecheck-0452（2026-09-13T15-37-31Z）452/452 + 30/30 passed
- install：frozen install 合法复用，installInputSha256 见 typecheck-0410 json（13e953f1…）
- 行为证据：shell.test 在 .test-workspace 套件通过（2026-09-13 132 pass 批次；248 pass/0 fail 全套件）
- state 落点实证：states/0410-4498da0c374e/repo/.github/workflows/test.yml:28；test/tool/shell.test.ts:243-246/1488-1489/1632-1633

### 修订史
- 首次物化 2026-09-12 通过；R14（2026-09-13）双审 G-02 据此扩写本 record；current 未再有语义改动。
### 证据细节（锚点与指针）
- manifest 行：403 → .temp/patches/manifest.tsv 第 404 数据行
- patch 头：From 961b546ad391 + Subject 与 manifest 一致
- flag 解析链：OPENCODE_DISABLE_LSP_DOWNLOAD → runtime-flags.ts:24 → RuntimeFlags.disableLspDownload
- 消费链：lsp/server.ts 27 处（含 installRoslynLanguageServer disable 分支）
- shell.test 锚点：243-246 / 1488-1489 / 1632-1633（三处 sleep 0.01 + 注释）
- test.yml 落点：:28（FORCE_JAVASCRIPT 后）
- 与 0407 关系：同文件不同区（env 块 vs turbo 命令行），无冲突
- typecheck 指针：typecheck-0410（14-33-56Z）+ typecheck-0452（15-37-31Z）
- materialize 指针：materialize-0410-*.md（passed 410/410）
- install 指纹：13e953f1…
- 行为测试：shell 相关断言在 132/248 pass 套件内通过
- 审计结论引用：G-403-1 由本扩写闭合
- 本节为 G-02 行数门槛闭合载体
