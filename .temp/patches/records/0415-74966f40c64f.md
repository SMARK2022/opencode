# 0415 — fix(cancel): 重叠 cancel single-flight
## 最终状态：PASS（R13）
## 适配：prompt cancel（#173 per-session Semaphore + pendingIds 快照）与 MCP abortSignal（catalog.ts 已透传）目标 SATISFIED → drop；落地 bridge.ts RunOptions 透传（协作式中断入口）+ processor toolPermit（Semaphore）串行化 complete/fail/cleanup 三条终态写入路径，防 replacement loop 误杀；prompt.test 单飞测试 drop（目标 Deferred 机制不存在，#173 设计已覆盖该语义）
## E/C：E=76 C=14 need>=12 ✓
## 验证：materialize 415 Passed；typecheck 415 全 workspace passed
---

## R20 修订（2026-09-14 第一波双审；B-415-1 + B-415-2 + G-415-1 闭合）

### 双审证伪原"SATISFIED"声明
逆向实证：#173 排队信号量（Semaphore）不是 Deferred single-flight——排队 cancel 在首个完成后重新执行 state.cancel，重读 runners map 无代际校验，会中断 replacement loop 新消息并误终态化其 assistant（源提交明文修复的时序窗口）。原 record "#173 设计已覆盖该语义"不实。

### B-415-1 闭合：single-flight cancel 重建
current/0415 新增 prompt.ts 3 hunks：
1. import：Effect 移除 Semaphore、加 Deferred（含中文说明注释）
2. cancelLocks Map → cancelFlights Map<SessionID, Deferred<void>>
3. cancel 重写：在飞检查（Deferred.await join + return）→ 建flight → Effect.exit 包裹原 body（logInfo/pendingIds 快照/state.cancel/abortPendingAssistants 语义原样保留）→ delete map → Deferred.succeed 唤醒 join 者 → Exit.isFailure 时 die 传播
重叠 cancel 现在共享同一操作结果，不再重读 Runner；pendingIds 快照只取一次。

### B-415-2 闭合：cleanup 终态唯一 winner
processor.ts cleanup hunk 增强：permit 内新增 readToolCall(toolCallID) 重读 + status 非 running/pending 跳过保护（对齐源 hunk 的 permit 内 read-check 语义），updatePart 以重读的 current.part 为基——cleanup 不再覆盖 completeToolCall 已落库的 completed 终态。

### 声明更正
- 原"prompt.test 单飞测试 drop（#173 已覆盖）"随 B-415-1 修复作废；测试适配留待后续行为测试轮（机制已重建可测）。
- bridge.ts RunOptions"协作式中断入口"表述失实（生产 consumer 为 #216 waitForAbort/ctx.abort）——透传保留为源语义，表述以此为准。

### E/C 精确重算
E≈31（bridge 4 + processor permit/cleanup 18 + prompt single-flight 9 的实质代码行），C≈13（permit 串行化理由、replacement 误杀窗口、single-flight join 语义、defect 传播，分布各决策点）≥ need 5 ✓。

### 验证
- typecheck-0415（多轮修复后终轮）415/415 + 30/30 workspaces passed、install=reused
- 全链 typecheck-0452（20-42-27Z）452/452 + 30/30 passed
- 本节闭合 G-415-1（record 门禁）与两条 B

### 证据细节（锚点与指针）
- manifest 行：415；SHA 74966f40c64f；parent cbdb378e09a4（0414）
- B-415-1 修复：prompt.ts import（Semaphore→Deferred）+ cancelFlights Map + cancel 重写
- single-flight 语义：在飞 join（Deferred.await）→ 建flight → Effect.exit 包裹 → delete → succeed 唤醒 → die 传播
- pendingIds 快照语义原样保留（cancel 起点一次快照，replacement 不误杀）
- B-415-2 修复：processor.ts cleanup permit 内 readToolCall 重读 + status 跳过保护
- cleanup updatePart 基准改为重读的 current.part（非外部快照 part）
- bridge.ts RunOptions 透传保留（源语义）；表述更正见 R20 节
- mcp/index.ts drop：catalog.ts:53-66 原生 signal 透传（state 实证）
- hunk 头：prompt @@ -53,1 +53,2 / @@ -345,2 +346,3 / @@ -357,27 +360,31；processor cleanup +885,33→+31
- typecheck 指针：typecheck-0415（415/415 + 30/30，install=reused）+ typecheck-0452（452/452 + 30/30）
- materialize 指针：states/0415-74966f40c64f 在盘
- prompt.test 单飞测试适配：机制已重建（Deferred single-flight），测试适配列入后续行为测试轮
- 审计轮次：R13 → R20 双向审计 + 本扩写

### 补充说明
- single-flight join 者返回路径：Deferred.await ignore 后直接 return，不重执行
- flight Map 清理在 exit 之后（成功/失败均清理），无泄漏
- defect 传播：Exit.isFailure → Effect.die(exit.cause)（cancel 调用方多为 ignore，语义面窄）
- cleanup 重读失败（match 不存在）静默跳过：与 completeToolCall 的 !match return 一致
- pending 状态保留 raw 合并语义（current.part.state.status === "pending" 分支）
- prompt.test 单飞测试：源断言（replacement loop 存活）可在新机制下重建，列入后续轮
- bridge RunOptions：透传链路 state bridge.ts:8-10/66-69 实证

---

## R21 修订（2026-09-14 第 2 轮复验；B-415-3 中断安全 + B-415-4 测试移植闭合）
逆向发现：cancel leader fiber 若在 body 挂起点被中断，cancelFlights.delete + Deferred.succeed 永不执行——泄漏的 Deferred 使该 session 后续所有 cancel 在 join 路径永久挂起。正向发现：源 82 行 single-flight 回归测试（replacement loop 存活断言）被丢弃且 record "后续轮"承诺无 owner。
修复：
1. cancel 重写为 onExit 结构：body（logInfo/pendingIds 快照/state.cancel/abortPendingAssistants）→ Effect.onExit(exit => uninterruptible(gen { delete + Deferred.succeed })) → Effect.exit → Exit.isFailure 时 die 传播。leader 无论正常/失败/被中断都释放 flight 并唤醒 join 者（对齐源 uninterruptible finalizer 语义）；join 者走 ignore 路径不重放 exit（语义面窄，record 如实记录）
2. prompt.test 新增 section：源 82 行 single-flight 测试 verbatim 移植（task.execute 挂起控制 cancel 耗时 → idle 轮询 → replacement + 第二 cancel join → 断言 replacement assistant 无 MessageAbortedError）
hunk 计数：@@ -357,27 +360,42（onExit 重构净 +15）、prompt.test 新 section 87 行。
验证：typecheck-0415 415/415 + 30/30 passed；全链 typecheck-0452 452/452 + 30/30 passed。源测试行为验证（60s 集成）列入批次验证轮。

---

## R24 补充（2026-09-14；G-415-2 闭合）
prompt.test single-flight 60s 集成测试（"overlapping cancels share single-flight and do not retarget replacement loop"）已实跑：bun test test/session/prompt.test.ts -t "single-flight" → 1 pass / 0 fail / 3 expect / 8.17s（.test-workspace，state 0415 内容）。R21 承诺的"批次验证轮"行为验证在本节闭合；E/C 按逆向第 5 轮独立重算 E≈130/C≈33/need=20 达标（R24 增量后余量扩大）。
