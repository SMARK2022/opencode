# Record 0415-74966f40c64f

## Identity
- SHA: 74966f40c64f
- Parent: cbdb378e09a4 (0414)
- Subject: fix(cancel): 重叠 cancel 共享 single-flight 操作，修复 replacement loop 被误杀
- Status: PASS（批次已闭合；详见 0415 批次结语与双审计结论）

## Source intent
source 0415 共 5 文件（435 行）：
1. bridge.ts：Shape.promise 加 `options?: Effect.RunOptions` 并透传 Effect.runPromise——非 Bash 工具经 { signal } 协作式中断（Bash 豁免）。
2. mcp/index.ts convertMcpTool：execute 接收 ToolExecutionOptions 透传 signal 给 client.callTool。
3. processor.ts：import 加 Semaphore；toolPermit 串行化 completeToolCall/failToolCall/cleanup 三路终态写入（read-check-write 原子、唯一 winner）；cleanup 增加 status 重读保护（非 running/pending 跳过，不覆盖 stream event 已落盘的终态）。
4. prompt.ts：cancel 顶部注释措辞 + `Deferred.done → succeed`（exit 作为值存入供 joiner 重放；done 会把失败 Exit 送入 Deferred 失败通道使 Exit.isFailure 分支不可达）。
5. prompt.test.ts：新增 overlapping cancel 测试（task 工具永不 resume → 第一个 cancel 的 Fiber.interrupt 等 2s settle → 提交 replacement + 第二个 cancel join 同一 Deferred → 断言 replacement loop 成功且最后 assistant 无 MessageAbortedError）。

## Target adaptation
- bridge.ts hunk A：目标 bridge.ts 同路径同结构 → 直接适配（Shape 接口 + make() 实现 + #415 注释）。**这是 0412 tools.ts 4 个 TS2554 的消解点**。
- mcp/index.ts hunk B：目标无 convertMcpTool（mcp/catalog.ts:62 已传 signal——上游更强）→ Drop + record。
- processor.ts hunk C：目标 processor 结构相同（0412 已适配 interruptedToolMetadata/failToolCall/cleanup）→ 完全适配（Semaphore import L5；toolPermit 在 aborted 后；completeToolCall/failToolCall 整体包 permit；cleanup 包 permit + status 重读保护）。注意 source 中 failToolCall 的非 abort metadata 为 undefined——目标保留 #209 演进（非 abort 保留原 metadata，0412 已记录）。
- prompt.ts hunk D：0412 已直接采用 succeed 最终语义 → succeed 无待改；注释更新为 0415 版措辞（Deferred.make 无异步 I/O 边界说明）。
- prompt.test.ts hunk E：重锚到目标 "cancel finalizes subtask tool state"（L2524）之前；helpers（useServerConfig/providerCfg/sessions.create/seed/llm.push/reply/hang/user/addSubtask/succeedVoid/awaitWithTimeout/pollWithTimeout/registry.named/Effect.addFinalizer/MessageV2.filterCompactedEffect/Fiber）在目标全部可用，测试体 verbatim。

## Verification（完成）
- materialize 415：passed 415/415
- typecheck 415（批次末）：415/415，install reused 1165df52，30/30 workspaces 全绿，**0412 遗留的 4 个 tools.ts TS2554 消失**（bridge Shape.promise options 生效）
- bun test test/session/prompt.test.ts -t "overlapping cancels"：1 pass / 0 fail（7.87s，真实 2s settle 窗口内第二个 cancel join 同一 Deferred；replacement loop 未被打断且最后 assistant 无 MessageAbortedError）
- bun test test/session/prompt.test.ts -t "cancel"：12 pass / 4 skip / **1 fail** = "cancel aborts an in-flight shell auto review"（#216，非本批次新增）
- **该失败经三方对照确认为 pre-existing**：在 0405 基线 state、0414 state、0415 state 上均以相同错误（"shell auto review never started"，reviewing 状态从未出现，poll 5s 超时）失败——批次开始前即存在（Windows 环境下 reviewer 子会话/auto-review 启动问题），非 0411-0415 引入，归 N 级（环境性 pre-existing），record 记录，不阻塞本批次

## E/C
- count-ec-auditor.py：E=107 / C=41 / need=17 → PASS（注释集中于 single-flight 原子性与 Deferred 语义、终态唯一 winner、cleanup 重读保护、AbortSignal 透传、测试挂起机制与替换 loop 竞态说明）

## 不一致记录
1. mcp/index.ts convertMcpTool hunk：Drop——目标 mcp/catalog.ts:62 已传 signal（上游更强），MCP 协作中断已覆盖
2. prompt.ts hunk D（done→succeed）：0412 已直接采用 succeed 最终语义（typecheck 必过），本项仅更新注释措辞
3. processor failToolCall 非 abort metadata：保留目标 #209 演进（原 metadata）vs source undefined（0412 已记录）

## 最终行为
- current/0415 覆盖 4 文件（bridge.ts/processor.ts/prompt.ts/prompt.test.ts）+ mcp hunk Drop，15739 字节；五个 current（0411-0415）累计 apply 至 415
- EffectBridge.Shape.promise 接收 RunOptions（AbortSignal）→ 非 Bash 工具协作式中断生效（0412 的 tools.ts 4 处 signal 透传 typecheck 落地）
- ToolPart 终态写入串行化：completeToolCall/failToolCall/cleanup 共用 toolPermit（唯一 winner），cleanup 重读后非 running/pending 跳过——late 结果不覆盖已落盘终态
- 重叠 cancel single-flight：同一 Session 的第二个 cancel join 首个 Deferred 并重放同一 Exit，不重新执行 state.cancel，replacement loop 不再被误杀

## 验证细节
- processor.ts 目标现状锚点：effect import L5（{Cause, Deferred, Effect, Exit, Layer, Context, Option, Scope, Schema} + Semaphore）；aborted L206；completeToolCall L285；failToolCall L328；cleanup loop L879
- 测试插入锚点：目标 prompt.test.ts L2523 "cancel finalizes subtask tool state" 之前（source 行号 1855 与目标差异 669 行）；helpers 检查：seed(L450 const)/addSubtask(L531)/succeedVoid(L426)/llm.hang/reply().text/pollWithTimeout/awaitWithTimeout 齐备
- overlapping cancel 测试机制：task.execute 覆盖为 Effect.callback 永不 resume → taskReady Deferred 同步信号（避免 sleep 竞态，符合 test/AGENTS.md 反模式指引）→ 第一个 cancel fork → poll session idle（Runner.cancel idleIfCurrent 后 Fiber.interrupt 仍进行）→ 提交 replacement + 第二个 cancel → 断言 Exit.isSuccess(fiber2) 且最后 assistant 无 MessageAbortedError
- 批次链验证：0411 的 3 个中间态错误（formatLongExecutionNotice/interruptedToolMetadata 双参/elapsed_ms）在 0412 消解；0412 的 4 个 tools.ts TS2554 在本项（bridge）消解 → 批次末 typecheck 415 30/30 全绿证明整链类型一致
- 全部行为测试：overlapping cancel 1/1；goal continuation 2/2；httpapi-goal 10/10；goal 25/25；goal-migration 1/1；message-v2 50/50；processor-effect 20/20；policy 12/12；ripgrep 7/7；glob 4/4；grep 13/13；layout 8/8

## Verdict: BATCH 0411-0415 CLOSED
- R2 双审计（正向 + 逆向，修复后全新独立运行，均完成完整五项审计）一致 APPROVE：0411-0415 各 5/5 PASS，无未关闭 B/G。
- 0411 的 R1 B-01（tools.ts 五个包装点 notice 装饰）与 B-02（progress start 保留）在两个 auditor 的 materialized state 直接读取中均验证通过；0412 tools.ts 段已重生成（pre=0411 修复版，无 hunk 重复）。
- 链验证：materialize 415 passed 415/415（0405 前缀 apply 406-415）；typecheck 415 30/30 workspaces 全绿（install reused 1165df52，bun 1.3.14）；中间态错误链（0411×3 → 0412 消解；0412/0413/0414×4 TS2554 → 0415 消解）闭合。
- 行为测试全量：overlapping cancel 1/1、goal continuation 2/2、httpapi-goal 10/10、goal 25/25、goal-migration 1/1、message-v2 50/50、processor-effect 20/20、policy 12/12、ripgrep 7/7、glob 4/4、grep 13/13（另有 pre-existing 5000ms flake 单测）、layout 8/8、TUI spacing 5/5。
- 审计 N 级均已记录（0412 N-a/N-b、0413 N-1..5、0414 chrome 差异、0415 pre-existing auto-review 失败等），不阻塞。
- 后续批次从 0416 开始。
