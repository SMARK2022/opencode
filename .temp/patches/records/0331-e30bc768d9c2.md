# Record 0331-e30bc768d9c2 — fix(tui/app): 流式 pending 阶段短快照覆盖长文本，防止 GLM-5.2 输出截断

## Identity
- index/full SHA: 0331 / e30bc768d9c26d68ea4b9951d770503768ea27ff (short e30bc768d9c2)
- manifest parent: bca4aac0e410 (0330)
- subject: fix(tui/app): 流式 pending 阶段短快照覆盖长文本，防止 GLM-5.2 输出截断
- current SHA-256: 69d8473b64c40cdc / 21505 bytes
- status: PASS

## Source intent (original/0331-e30bc768d9c2.patch, 463 行, 4 files)
1. `packages/app/src/context/global-sync/event-reducer.test.ts`: 2 新测试验证短快照不覆盖长文本
2. `packages/app/src/context/global-sync/event-reducer.ts`: 加单调长度守卫（pending 阶段本地更长时跳过覆盖）
3. `packages/opencode/src/cli/cmd/tui/context/sync.tsx`: 加 mergeLivePart/mergeLiveParts 单调长度守卫
4. `packages/opencode/test/cli/cmd/tui/sync.test.tsx`: 4 新测试验证

## Three-way comparison
- source/SMARK: pending 阶段短快照不覆盖长文本（单调长度守卫）；终态始终接受权威值
- 上游 v1.17.20: 无长度守卫，短快照可能覆盖 delta 累积的长文本
- 目标当前: TUI sync.tsx 已迁移到 packages/tui/src/context/；App 文件同路径
- 结论: 加 mergeLivePart/mergeLiveParts 守卫 + App event-reducer 等价守卫 + 测试

## Target adaptation
- App event-reducer.test.ts: 同路径 `packages/app/src/context/global-sync/event-reducer.test.ts`
- App event-reducer.ts: 同路径 `packages/app/src/context/global-sync/event-reducer.ts`
- TUI sync.tsx → `packages/tui/src/context/sync.tsx`（0299 迁移）
  - 目标用 `event.properties.part` 而非 source 的局部 `part` 变量 → reconcile 调用适配
  - 目标已有 flatMap 空文本守卫 → 替换为更通用的 mergeLivePart
- TUI sync.test.tsx → `packages/tui/test/cli/cmd/tui/sync.test.tsx`（迁移）
  - 目标用 `mount(handler, state)` 而非 source 的 `Global.Path.state = tmp.path` + `mount(handler)`
  - 4 个测试用例适配：移除 Global.Path.state 引用，改用 mount(handler, state) 参数
- 方法: state 0330 语义编辑（edit331.py subagent）→ git diff → current/0331

## 逐文件映射
| Source file | Target file | 状态 |
|-------------|-------------|------|
| event-reducer.test.ts | packages/app/src/context/global-sync/event-reducer.test.ts | ✓ 同路径 |
| event-reducer.ts | packages/app/src/context/global-sync/event-reducer.ts | ✓ 同路径 |
| sync.tsx | packages/tui/src/context/sync.tsx | ✓ 迁移 + 适配 |
| sync.test.tsx | packages/tui/test/cli/cmd/tui/sync.test.tsx | ✓ 迁移 + mount 适配 |

## Verification
- materialize 331/331: passed
- typecheck 331: install reused（frozen cbdc173），packages/app TS1128（环境基线），integrityFailure None
- sync test（.test-workspace synced to 331）: `bun test ./test/cli/cmd/tui/sync.test.tsx --timeout 60000` → 15 pass / 0 fail / 40 expect() / 5.89s
- E/C: E=245 / C=62 / need=max(1,ceil(245*0.15))=37 → PASS
- source/target HEAD: 未变
- states 保留 ≤5

## 不一致
- 目标 sync.tsx 用 `event.properties.part` 而非 source 的局部 `part` 变量
- 目标 sync.tsx 已有 flatMap 空文本守卫，source 用 mergeLivePart 替换
- 目标 sync.test.tsx 用 `mount(handler, state)` 而非 source 的 `Global.Path.state` + `mount(handler)`
- App 文件无路径迁移，直接适配

## Target structure differences detail
- Source sync.tsx uses local part variable; target uses event.properties.part
- Source sync.test.tsx uses Global.Path.state + mount(handler); target uses mount(handler, state) parameter
- Target sync.tsx already had flatMap with empty-text guard; source replaces with more general mergeLivePart
- App event-reducer files at same path, no migration needed
- TUI files migrated to packages/tui/ (0299 precedent)
- 4 test cases adapted: removed Global.Path.state references, use mount(handler, state)
- Fixture functions defined after describe block (function hoisting works)

## mergeLivePart behavior detail
- For pending (no time.end) parts: if incoming text/reasoning/tool-raw is shorter than local, keep local
- For terminal (time.end exists) parts: always accept authoritative final value
- Prevents fire-and-forget part.updated (text-start) from overwriting delta-accumulated long text

## DUAL-AUDIT SEAL (batch 331-335)
- R6 dual audit (after R1-R5 fixes: records rewritten, safeRm Windows fix, E/C tool .yml # recognition, bash $() comment fix):
  - Forward (ses_fd9ffcc3): 5/5 PASS, no open B/G
  - Backward (ses_fd9ffb561): 5/5 PASS, no open B/G
- Verified: install cbdc173 reused, E/C all pass (245/62, 94/28, 67/58, 30/6, 52/15), cumulative apply 335/335, states <=5, source/target unchanged
- Environmental N: packages/app TS1128; typecheck script stops at first failure
- Key fixes: safeRm helper for Windows EPERM; E/C tool recognizes .yml # comments; 0335 bash # comment removed from inside $()
- Batch: APPROVE (5/5 PASS, no open B/G)