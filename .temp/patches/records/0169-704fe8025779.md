# Patch 0169 — 704fe8025779

## Identity
- Index: **169**
- Full SHA: `704fe80257797401210454dfded5f3f161a6ca6b`
- SHA12: `704fe8025779`
- Parent: `2c720138d339` (#168) — **hard serial dependency**
- Subject: feat(codex): Codex TUI HTTP fingerprint + unconditional configured User-Agent
- Authored: 2026-05-18T23:20:35+08:00
- Current path: `.temp/patches/current/0169-704fe8025779.patch`
- Current bytes: **13145**
- Current SHA-256: `8d060f8075aef2a849530b262b1ed77fb9fae947dd11c97a1b4170371508444e`
- Original path: `.temp/patches/original/0169-704fe8025779.patch`
- Original bytes: **12717**
- Record updated: 2026-07-25T08:23:58.182313+00:00
- Batch: **166–170**
- Tip base: `states/0168-2c720138d339`
- Note: retained states may prune `0169-*` after materialize 170 (workflow-only #170); tip `0170` still contains #169 code deltas

## Source intent
Two coupled behaviors:

### A. Codex plugin fingerprint
Centralize fixed headers and use on all OpenAI Codex OAuth / device-auth / chat / API paths:
- `CODEX_USER_AGENT` — fixed `codex-tui/0.130.0 …` string (exported)
- `CODEX_HEADERS` — JSON Content-Type + `originator: codex-tui` + User-Agent (exported)
- `CODEX_FORM_HEADERS` — form-urlencoded + same originator/UA
Call sites: exchange/refresh token form headers; OAuth loader `headers`; custom fetch force on Codex responses URL; deviceauth usercode/token; `chat.headers` for `providerID === "openai"` via `Object.assign(..., CODEX_HEADERS)`
Removed: `InstallationVersion` / `os` for these UA paths

### B. Provider configured UA always wins
#168 introduced fetch guard:
```ts
if (!currentUserAgent || currentUserAgent.startsWith(configuredUserAgent))
  headers.set("User-Agent", configuredUserAgent)
```
#169 removes the guard → always `headers.set("User-Agent", configuredUserAgent)`.
Why: AI SDK / middleware often appends suffix (`… ai-sdk/openai/…`), failing `startsWith`, so custom UA never reaches the wire. Integration test uses OpenAI-compatible/alias provider with `User-Agent: codex_cli_rs/0.2333`.

## Three-way comparison

### Tip@168 strengths to keep
- Path is **`packages/opencode/src/plugin/openai/codex.ts`** (not `plugin/codex.ts`)
- `NetworkProxy.fetch(..., { purpose: "plugin"|"provider" })`
- Optional `codexApiEndpoint` / `experimentalWebSockets` + `OpenAIWebSocketPool`
- `session-id` (kebab) not source `session_id`
- Title-agent websocket header smuggling
- #168 `requestUserAgent` / `requestHeaders` pipeline

### SMARK applied on tip
| Area | Adaptation |
|---|---|
| Constants | Export CODEX_USER_AGENT / CODEX_HEADERS; internal FORM headers |
| Token/deviceauth | Replace bare form/JSON UA with shared constants; keep purpose |
| OAuth loader | `headers: CODEX_HEADERS` next to `apiKey` |
| Custom fetch | After rewrite to codex endpoint, force CODEX_HEADERS when `url.href === new URL(codexApiEndpoint).href` (supports test overrides) |
| chat.headers | `Object.assign(CODEX_HEADERS)` then keep tip `session-id` + title header |
| provider fetch | Unconditional configured UA set |
| tests | codex unit tests + Effect alias UA wire test; `createEventResponse` used **directly** (no double `new Response` wrap — typecheck fix) |

## Current hunk map
1. `packages/opencode/src/plugin/openai/codex.ts`
2. `packages/opencode/src/provider/provider.ts`
3. `packages/opencode/test/plugin/codex.test.ts`
4. `packages/opencode/test/session/llm.test.ts`

## Independent E/C
- **E:** **117**
- **C:** **21**
- Required: **18**
- Gate: **PASS**
- C sites: fixed fingerprint why; force headers on endpoint rewrite; chat.headers override; unconditional UA set vs #168 startsWith; loader/chat same source; test intents

## Materialize / typecheck / tests
- materialize through 169 then 170 OK
- typecheck 170: **30/30** packages (`typecheck-0170-2026-07-25T07-56-46-317Z.md`); install reused `bfd9217687…`
- Focused tests:
  - `bun test test/plugin/codex.test.ts -t "Codex TUI|User-Agent"` → **2 pass**
  - `bun test test/session/llm.test.ts -t "configured provider|OpenAI alias"` → **3 pass**
- Revision history: first loader `headers: CODEX_HEADERS` replace failed due to indent mismatch (12 vs 10 spaces before `async fetch`); fixed and rematerialized; double-Response wrap caused TS2345 — fixed to `createEventResponse(...)` direct

## Final tip behavior after #169
Codex OAuth/deviceauth/chat/API use fixed codex-tui fingerprint; tip websocket/session-id/title semantics preserved; configured provider UA always reaches the wire even when SDK appends suffixes.

## Dual independent audit (batch 166–170)
- FORWARD: **PASS** (all five; no open B/G) — task after record expansion re-audit
- REVERSE: **PASS** (all five; no open B/G) — independent reverse re-audit
- Prior cycle: BLOCK only on record size G for 167–170; records expanded ≥50 lines & ≥3000 chars; behavior had no open B
- Materialize: `materialize-0170-2026-07-25T07-56-36-492Z`
- Typecheck: `typecheck-0170-2026-07-25T07-56-46-317Z` (30/30; install `bfd9217687…`)
- Sealed: 2026-07-25T08:32:23.632271+00:00
- Batch verdict: **PASS** — may advance to 171–175
