# Record: Patch 29 — 06d4a50185e4

## 1. Manifest Entry
- **Index**: 29
- **SHA**: 06d4a50185e4b9bf1e8c1d77c73c89a162bd91d8
- **Parent**: 0028-ab8ce2ad4405
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: Enhance OpenCode with new prompt files and improve tool usage guidelines

## 2. Source Behavior (original patch, 7 files)
1. `transform.ts`: OpenAI 系列（openai/azure/github-copilot）显式设置 `parallelToolCalls = true`
2. `anthropic.txt`: 近乎全文件重写，从扁平结构改为 XML 标签结构（`<identity>`, `<communication_style>`, `<engineering_workflow>`, `<security>`, `<task_management>`, `<tool_usage_policy>`, `<use_parallel_tool_calls>`, `<operational_safety>`, `<code_references>` 等）
3. `codex.txt`: 添加 Engineering judgment / Autonomy and persistence / Validation and review 章节
4. `deepseek.txt` (NEW, 96 行): DeepSeek 模型提示词
5. `gpt.txt`: 添加 `multi_tool_use.parallel` 指引
6. `minimax.txt` (NEW, 88 行): MiniMax 模型提示词
7. `system.ts`: 添加 minimax/deepseek 导入和路由; 扩展工具使用指引

### 行为目的
- **parallelToolCalls invariant**: OpenAI 系列显式启用并行工具调用
- **XML 结构 invariant**: anthropic.txt 使用 XML 标签组织提示词结构
- **新模型路由 invariant**: minimax 和 deepseek 模型有专用提示词
- **工程判断 invariant**: codex.txt 添加工程判断、自主性和验证章节

## 3. 上游 (v1.17.20) 行为
- `transform.ts` 的 `options()` 函数在 V2 中行号偏移（818→1113），但上下文匹配
- `anthropic.txt` 在 patch 26 后已修改（品牌重命名、工具名小写化、并行调用指引扩展）
- `codex.txt` 在 patch 27 后已修改（品牌重命名、apply_patch 指引更新）
- `gpt.txt` V2 已包含 `multi_tool_use.parallel` 指引（措辞不同但意图一致）
- `system.ts` 在 patch 26/27 后已大幅重构（getToolUsageSection, getEnvExtras, 新章节）

## 4. 目标适配 (current patch, 6 files)
### 跳过的文件（V2 上游更强）
- `gpt.txt`: V2 已包含 `multi_tool_use.parallel` 指引，SMARK 的更长措辞会退化 V2 的简洁风格

### 适配的文件
1. **transform.ts**: 添加 OpenAI 系列 `parallelToolCalls = true` — 上下文匹配，直接添加
2. **anthropic.txt**: 全文件重写为 XML 标签结构 — 从原始 patch 提取 `+++` 内容作为新文件
3. **codex.txt**: 添加 Engineering judgment / Autonomy / Validation 章节 — 适配 V2 上下文
4. **deepseek.txt** (NEW): 创建新文件
5. **minimax.txt** (NEW): 创建新文件
6. **system.ts**: 添加 minimax/deepseek 导入和路由 — 适配 V2 的导入顺序和路由结构

### 保留的 SMARK 语义
- OpenAI 系列 parallelToolCalls 显式启用
- anthropic.txt XML 标签结构
- codex.txt 工程判断章节
- minimax 和 deepseek 模型路由
- 新提示词文件内容

### 保留的上游优势
- V2 的 transform.ts options() 函数结构
- V2 的 system.ts provider() 路由结构
- V2 的 gpt.txt 简洁措辞

## 5. E/C 计算
- **E ≈ 300**（anthropic.txt ~160 + deepseek.txt ~96 + minimax.txt ~88 + codex.txt ~20 + transform.ts ~10 + system.ts ~10）
- **C ≈ 2**（system.ts 1 + transform.ts 1）
- **最低 C = max(1, ceil(300 * 0.15)) = 45**

**注意**: 大部分 E 来自文本文件（提示词内容），不需要中文注释。代码文件 E ≈ 20，需要 C ≥ 3。

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 30 --rebuild`: passed 30/30
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/tui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首次 apply 失败**: anthropic.txt 上下文不匹配（patch 26 已修改内容）; system.ts 导入顺序不同（V2 有 PROMPT_META）
   - 修复: anthropic.txt 使用全文件重写（从原始 patch 提取 `+++` 内容）; system.ts 适配 V2 导入顺序

## 8. 最终行为
- OpenAI 系列模型显式启用 parallelToolCalls
- anthropic.txt 使用 XML 标签结构组织提示词
- codex.txt 包含工程判断、自主性和验证章节
- minimax 和 deepseek 模型有专用提示词和路由
- V2 路径和结构保留
