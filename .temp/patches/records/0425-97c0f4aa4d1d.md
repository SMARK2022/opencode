# Record 0425-97c0f4aa4d1d

## Identity
- SHA: 97c0f4aa4d1d
- Parent: a73e1f718d19 (0424)
- Subject: doc(tui): 修复 VS Code 双横幅问题并实现 abort 完全静音
- Status: PASS（materialize 425 + typecheck 425 全部通过）

## Source intent
source 0425（1 文件，1 hunk，557 行）：`docs/Proposal/tui-agent-loop-sound-surgical-plan.md` 全文重写（389 → 211 行）：
- 标题改为"TUI 提示音与通知行为修复方案"；日期 2026-07-14；状态行更新。
- 内容重组：需求聚焦两个问题（VS Code 双横幅 + abort 完全静音）；方案规模从 7 文件/100 行收敛为 5 文件/50 行；保留 0420 已实施内容（内嵌 WAV、loadSound keyed loader、KV 开关）并新增 0424 的 OSC 99 单 payload 语义与 abort 静音事件顺序说明。

## Target analysis 与适配
- target 的该文档（0420 移栽，389 行+尾换行）与 source 0425 的 pre 状态逐字一致（0424  platform 未动它）→ 全文替换 verbatim 语义（新 211 行内容）。
- 无代码行为（纯文档）。

## E/C
- 全部为 .md → E=0 / C=0 / need=0 PASS。

## Verification
- materialize 425 --rebuild：passed 425/425（批次末项）。
- typecheck 425：30/30 workspaces passed（install 指纹 1165df52… frozen reuse）。
- 文档内容验证：materialized 文档首行 `# TUI 提示音与通知行为修复方案`、211 行。

## BATCH 0421-0425 备注
- 本项为批次最后一项；批次验证与双审计见批次结语（0425 record 尾部将追加）。
## 失败修订记录（批次内）
- 首次提取 hunk 内容偏移错误（@@ 行在 0-based L15，循环从 L16 起）导致空写入、文档被清空 → git checkout 从 index 恢复（390 行）→ 以 L15 起重新提取（211 行新内容）→ 写回。
- current 生成 1 hunk（20049 字节，全文替换）。

## 行为测试证据
- docs-only：materialize 425 --rebuild passed 425/425；typecheck 425：30/30；文档首行 `# TUI 提示音与通知行为修复方案`，211 行，与 source 0425 逐字一致。

## E/C（最终）
- E=0、C=0、need=0 → PASS（纯文档）。

## 审计结论
- 双审计轮次：文档重写语义（0424 双横幅/abort 静音方案并入 0420 已实施方案摘要；5 文件/50 行规模声明与批次实际一致）。

## 状态/指纹
- materialize 425 --rebuild：passed 425/425（从 0405 全链重建 0406-0425）；typecheck 425：30/30 workspaces passed；install 指纹 1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5（bun 1.3.14 frozen reuse）。
- retained states：0425/0416/0405（最近成功状态 ≤5 合规）。
- sourceTip d0ceb469011412b4ac5058a12d5fe4f247bdac79；targetBaseline 4473fc3c9055046183990a965d68df3db7ea6f62。

## 文档内容核对（与 source 0425 逐字）
- 标题 `# TUI 提示音与通知行为修复方案`；状态行 `方案审计候选，只用于后续 TDD 实施`；日期 2026-07-14；硬约束 5 文件/50 行。
- 章节：1 需求（两问题：VS Code 双横幅 + abort 静音）、2 方案（OSC 99 单 payload title 语义；abort 事件顺序 idle→error）、3 验证、4 变更规模、5 风险与开放问题。
- 与 0424 实现语义一致（title undefined、MessageAbortedError 静音、completedTurn idle 保留）。

## 批次说明
- 0425 为批次 0421-0425 末项；批次 verdict 见批次结语（审计完成后追加）。

## 审计结论（双审计轮次）
- 正向/反向 auditor 核查点：文档全文重写（389→211 行）与 0424 实现语义（title undefined 单 payload、abort 静音、completedTurn 时序）一致；硬约束声明（5 文件/50 行）与批次实际变更范围核对；commit 无 B/G。

## 验证命令记录
- `$env:BUN_TMPDIR="C:\Temp"; bun .temp/patches/src/apply-cumulative.ts --materialize 425 --rebuild` → passed 425/425
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 425` → 30/30 workspaces passed
- 行为测试：config/tui.test.ts（attention defaults 1/1）、config.test.tsx（9/9）、attention（21/21）、malformed-sgr-mouse（7/7）、notifications（9/9）、session-preview（12/12）——全部在 `.test-workspace/repo/packages/opencode|tui` 目录下使用 BUN_TMPDIR=C:\Temp 复跑通过。
- E/C 审计脚本：count-ec-auditor.py（0421: 5/3、0422: 125/39、0423: 9/3、0424: 138/33、0425: 0/0）。

## 修订记录补充
- 本文件为批次内 0425 的独立 record；docs-only 无代码影响；E/C（0/0）独立于批内其它项计算，未使用既有注释或跨 commit 累计。

## 调用链与 owner
- 文档 owner：docs/Proposal/tui-agent-loop-sound-surgical-plan.md（0420 移栽的既有文档），本 commit 全文重写为修复方案定稿；文档引用的实现（attention title/abort 静音/KV 开关）已在 0420/0424 落地，无代码调用面。

## N 项修正（双审计后）
- N-0425-1：plan 文档补尾换行（211 行 + \n；从 0424 state 重新生成 current，无 No-newline 标记）；materialize 425 --rebuild passed、typecheck 30/30。

## BATCH 0421-0425 VERDICT: PASS（CLOSED）
- 双独立审计（正向 0421→0425 + 反向 0425→0421）：两份一致 APPROVE，五 commit 全部 PASS，无开放 B/G。
- Round 1 全部为 N 级：N-0422-1（doc 首行 hunk 头残留）、N-0422-2（测试尾空行）、N-0424-1（const tui 缩进）、N-0424-2（测试尾空行/record 字节）、N-0425-1（文档尾换行）、N-0421-1（0411-0415 record 陈旧 IN PROGRESS 头）→ 全部修正并重新物化/typecheck/回归。
- 最终链：materialize 425 --rebuild passed 425/425（retained [0425,0416,0405]）；typecheck 425 30/30；install 指纹 1165df52…（bun 1.3.14 frozen reuse）。
- 行为回归（N 修正后复跑）：malformed-sgr-mouse 7/7、notifications 9/9、attention 21/21、config/tui.test 42/42、config.test 9/9、session-preview 12/12 全绿。
- E/C：0421 5/3、0422 125/39、0423 9/3、0424 138/33、0425 0/0 全 PASS。
