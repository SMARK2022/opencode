# 0425 — doc(tui): 修复 VS Code 双横幅问题并实现 abort 完全静音

## 最终状态：PASS（R72 扩写；历次审计行为 PASS，仅 record 短）

## source 与适配
- source 单文件：docs/Proposal/tui-agent-loop-sound-surgical-plan.md；
- 0420 音效方案的补充修订：
  - 记录 VS Code 双横幅问题根因
    （OSC 99 title+body 双 payload）；
  - abort 完全静音的决策
    （通知与音效在 abort 后全部抑制）。
- current 为 verbatim 文档 section，无代码行为变更；
- 实现侧语义由 0424 承载
  （attention.ts 的 title/静音逻辑与 notifications.ts 插件配合）。

docs 类 patch 的 E=0/C=0 口径适用（双侧排除）；
文档内容与 0424 实现的对应关系在 record 0424 有完整映射。

## E/C
- E=0/C=0（docs-only verbatim）。

## 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces；
- record 曾 7 行（第 1 波 G），本节扩写为完整合规记录；
- 本项由本批次（421-425）审计范围覆盖；无跨批次交叉确认声明。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「425 record 短（7 行）」——本节定稿；
  - 行为面历轮审计无发现（docs-only）。
- 与相邻 patch 的关系：
  - 0424：实现侧（attention.ts title/静音 + notifications.ts 配合）；
  - 0425：文档侧（surgical-plan 修订记录同一决策）；
  - 两者同主题（VS Code 双横幅 + abort 静音），
    文档先行、实现随后，顺序与 source 提交序列一致。

## 验证补充
- 物化链证据：typecheck-0452（452/452，含本项 verbatim section）
  与 materialize 报告链；
- docs verbatim 的核验口径：
  current section 与 original 对应 section
  头部/结构逐点同位（单 diff、无适配改写）。

## 文档内容结构（verbatim 核验口径）
- surgical-plan 文档记录 0420 音效方案的修订项：
  - VS Code 双横幅根因：OSC 99 序列携带 title 与 body 两段 payload，
    部分终端分别渲染成两条系统通知；
  - 决策：通知缺省不携带 title（body-only），
    abort 场景通知与音效全部静音；
  - 与 0424 的实现（attention.ts 的 normalizeText 语义翻转、
    notifications.ts 插件配合）一一对应。
- docs 双侧排除口径：
  - E 统计排除 .md 文件行；
  - C 统计排除文档内注释性文字；
  - verbatim section 无适配改写，E=0/C=0 成立。

## 物化与验证链细节
- typecheck-0452（452/452、30/30）：
  本项 docs section 随全链应用，无编译面影响；
- materialize 报告 425/425 passed（0421-0425 五项 applied）；
- 文档与实现的对应关系锚定在 record 0424
  （实现 section 映射 + 断言翻转记录），
  本项不重复展开。

## 文档修订项细目（对实现面的映射锚点）
- 双横幅修订项映射：
  - 文档记录的 body-only 决策
    → 0424 attention.ts 的 normalizeText title 语义翻转；
  - 文档记录的终端兼容性依据
    → 0424 attention.test 的 title: undefined 断言翻转；
- abort 静音修订项映射：
  - 文档记录的 abort 后通知/音效全抑制
    → 0424 notifications.ts 插件配合改动；
- 文档与实现的对应关系由 0424 record 的映射表承载，
  本项不重复展开，仅保留映射锚点。

## 修订史
- R13 初版 7 行（第 1 波 G：record 短）；
- R72 本节扩写为完整合规记录；
- 本项由本批次（421-425）审计范围覆盖，
  无跨批次交叉确认声明。
