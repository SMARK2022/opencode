# 0246-9711fd18599a record — feat(grep): 优化 grep 过滤器的输出格式

## Manifest
- index 246, sha `9711fd18599aff8424ecea57bbbd88d66762edf1`, parent `06c953137c9e` (0245)
- subject: feat(grep): 优化 grep 过滤器的输出格式，确保在 TUI 和滚动回显中紧凑显示，避免暴露参数名称

## Original 身份
- original/0246-9711fd18599a.patch: 10683 字节, 4 entries（run/tool.ts、tui routes/session/index.tsx、entry.body.test.ts、scrollback.surface.test.ts）

## Source 行为（逐 hunk）
1. **run/tool.ts**：新增 `grepPatterns/grepFilter/grepResult/grepDetails` 4 个 helper——inline 行不显示 `include=`/`exclude=` 字段名；exclude 用 `!glob` 语义（`!` 前缀），include 最多显示 2 项、exclude 显示 1 项 + `+N` 压缩；`grepResult` 处理 matches/truncated/timedOut 组合（`64+ matches, timed out`）；`runGrep` 的 description 改用 grepDetails；`scrollGrepStart` 同款。
2. **tui routes/session/index.tsx**：同款 3 个 helper（无类型注解版），Grep 组件加 `filters/result` createMemo，替换 `<Show when={metadata.matches}>` 块。
3. **entry.body.test.ts**：新增测试 `summarizes grep filters and incomplete results without exposing parameter names`——断言 description = `in node_modules · *.js · !**/.cache/** +2 · 64+ matches, timed out`，且不含 `include=`/`exclude=`/`timeout`。
4. **scrollback.surface.test.ts**：新增测试 `renders grep filters compactly in scrollback headers`——滚动回显头含 `✱ Grep "function" in node_modules · *.js · !**/.cache/** +2`，不含参数名。

## 目标适配（Round 1）
- **路径迁移**：`packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx`（TUI 已迁移，0245 起）。
- **run/tool.ts**：hunk 原样可 apply（offset 2），未改动。
- **TUI 组件适配**：目标 Grep 用 `stringValue/numberValue` helpers、`ToolProps = { input: Record<string, unknown>; metadata: ... }`（无泛型）、Read 尾部无空行直接接 Grep。适配：
  - helper 插入在 `function Grep(props: ToolProps)` 前（目标无 `function Read(...)` 尾空行，直接接）。
  - Grep 组件：`filters/result` createMemo（目标已 import createMemo）；保留目标 `stringValue(props.input.pattern)` 等写法；`<Show when={numberValue(props.metadata.matches)}>` 块替换为 `<Show when={filters()}>· {filters()} </Show>` + `<Show when={result()}>({result()})</Show>`。
  - `grepResult` 签名 `{ matches?: unknown; truncated?: unknown; timedOut?: unknown }` 与目标 `Record<string, unknown>` metadata 结构兼容。
- **测试**：两个测试文件在目标存在（`packages/opencode/test/cli/run/`），hunk 原样可 apply（offset 63/204），未改动。

## 生成方式
- gen-246.py：复制 245 状态 repo（含 .git）→ `git apply --exclude=tui/routes/session/index.tsx` 应用 run/tool.ts + 测试 hunks → 手动编辑目标 TUI 文件 → `git diff` 生成。
- current/0246-9711fd18599a.patch: 10748 字节, 4 entries, SHA-256: b790a9c62bedd0b77b8ce9a992136001600019a71ccbd702890f1365f32ad1a0

## E/C
- E = 108（grepPatterns 22 + grepFilter 14 + grepResult 17 + grepDetails 11 + runGrep 4 + scrollGrepStart 9 + TUI helpers 42 + Grep 组件 6 + 测试 53 中有效行）——具体以 count-ec.py 输出为准
- C = 8（run/tool.ts 4 条 + TUI 3 条 + Grep memo 1 条：#246 前缀，解释 !glob 语义、+N 压缩、timed out 组合、memo 渲染）
- 需 count-ec.py 确认

## 验证
- apply-cumulative --typecheck 246: passed 246/246
- entry.body.test.ts "summarizes grep filters": 1 pass (4 expect)
- scrollback.surface.test.ts "renders grep filters compactly": 1 pass (4 expect)

## 状态
- materialized: states/0246-9711fd18599a

## E/C 最终（count-ec.py）
- E=121, C=19, need=19, PASS（注释：#246 前缀 12 条——grepPatterns 规整、grepFilter 空输入、grepResult 超时/截断、grepDetails 分段过滤、runGrep 共用摘要、scrollGrepStart 空段退回、TUI filters/result memo、测试负向断言）

## 验证（最终）
- apply-cumulative --materialize 250: passed 250/250（累计）
- entry.body + scrollback + stats + reviewer 51 pass 0 fail；tui transport 8 pass
- opencode/tui typecheck 通过
