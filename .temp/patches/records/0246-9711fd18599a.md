# Record 0246-9711fd18599a

## Status
PASS

## 修改内容
feat(grep): 优化 grep 过滤器的输出格式，确保在 TUI 和滚动回显中紧凑显示，避免暴露参数名称。

### 回退发现与重建（2026-09-06）
- record 旧版记载 current 为 10748B 适配版（b790a9c6…），但现存 current/0246 = original 原样（适配版丢失）。
- 本轮按旧版适配思路完整重建并重新验证。

### Source 逐 hunk 行为
1. run/tool.ts：新增 grepPatterns/grepFilter/grepResult/grepDetails 4 helper——inline 行不显示 include=/exclude= 字段名；exclude 用 `!glob` 语义；include 最多 2 项、exclude 1 项 + `+N` 压缩；grepResult 处理 matches/truncated/timedOut（`64+ matches, timed out`）；runGrep/scrollGrepStart 改走 grepDetails。
2. TUI session/index.tsx：同款 3 helper + Grep 组件 filters/result createMemo，替换 `<Show when={metadata.matches}>` 块。
3. entry.body.test.ts：`toolInlineInfo` 摘要测试（description 全文锁定 + 三条参数名负向断言）。
4. scrollback.surface.test.ts：滚动回显 start 阶段头格式测试（`✱ Grep "function" in node_modules · *.js · !**/.cache/** +2`）。

### 路径迁移与目标适配
- `packages/opencode/src/cli/cmd/run/tool.ts`：pre-image 逐字吻合，hunk 原样（行号 offset 已同步）。
- `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` 目标不存在 → retarget `packages/tui/src/routes/session/index.tsx` Grep 组件（L3033-3044）：helper 插在函数前；Grep 保留目标 stringValue/numberValue 写法（complete={stringValue(props.input.pattern)} 等），memo 值过滤段/结果段；grepResult 参数类型 `{ matches?: unknown; ... }` 与 Record<string, unknown> 兼容。
- 两个测试文件（packages/opencode/test/cli/run/）原样可 apply。

### 0244 依赖说明
- metadata 字段 matches/truncated/timedOut 与 0244 重写后的 grep.ts 输出（grep.ts:214-218）完全匹配；input include/exclude 为 PatternList（string | string[]）与 grepPatterns 规整逻辑匹配。

## E/C
- E = 143（plus - commentPlus + minus，含 2 个测试文件新增行）
- C = 22（#246 标注：tool.ts 9 行、TUI 8 行、entry.body.test 4 行、scrollback.test 1 行——覆盖 !glob 幂等、空 filter 契约、单复数/截断语义、timed out 后置、memo 分离、负向断言口径等真实 invariant）
- need = ceil(143 * 0.15) = 22 → 余 0 PASS

## Source
- original/0246-9711fd18599a.patch: 239 行 4 文件。

## Current
- current/0246-9711fd18599a.patch: 12541 bytes, 6 hunk / 4 文件
- SHA-256: 48eb111c52b56f86280a04c729c9e19983206d3d3e7477a79c6f77178165f225

## 安装
- materialize 246 --rebuild: passed 246/246
- Install: reused frozen fingerprint

## 测试
- typecheck 246: 全部 workspace passed
- `bun test test/cli/run/entry.body.test.ts test/cli/run/scrollback.surface.test.ts`（packages/opencode）: 32 pass / 0 fail

## 修订历史
1. 初版：write 6 节 current（tool 3 hunk 原样行号、TUI 重写、测试原样）。
2. hunk sync 脚本 $Matches 覆盖 bug 产生 6 处残片头（`,6 ,48` 等）→ 重建头脚本（按体实测重写全部 7 头）修复；此后 sync 脚本改为组值快照版。
3. C 从 10 → 19 → 21 → 22 逐轮补齐（edit 原子失败 2 次后拆分调用；JSX {/* */} 注释不计 C 口径改用 // 注释）。
4. 最终：materialize OK、typecheck OK、oc 32/0、C=22=need → PASS。

## 三方比较
- Source：run 与 TUI 两侧统一 grep 摘要语义（紧凑、无参数名、超时/截断元数据）。
- Upstream v1.17.20：TUI 已迁 packages/tui 且 Grep 用 stringValue/numberValue 风格；run 侧 tool.ts 未变。
- Target：run 侧原样 + TUI 侧按目标组件风格重写同语义；测试双路径（completed inline + start 滚动回显）保留。无行为丢失、无重复 owner（run/TUI 是两条输出路径，共享语义各自实现，与 source 一致）。

## 最终行为
- grep 工具 inline 描述：`in <path> · <include> · <!exclude +N> · <N+ matches, timed out>`，不暴露参数名。
- 滚动回显 start 头：`✱ Grep "<pattern>" in <path> · <filters>`；completed 后追加结果段。
- 超时/截断元数据以紧凑后缀呈现（`+`、`, timed out`）。
- 2 个回归测试锁定 completed 与 start 两条渲染路径。

## 批次双审结论（2026-09-06，245-249）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：N-05 record hunk 计数笔误（实际 7 hunk 节）；C 复算口径保守低报（复算 ≥35），门禁余量只增不减。