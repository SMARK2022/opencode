# Record 0375-948a36cf24d5

## Source patch

- Source: `feat(docs): 添加多个文档以增强项目的结构和清晰度，包括代理技能、问题跟踪和决策记录` (SMARK2022, 2026-07-05)
- Original: `.temp/patches/original/0375-948a36cf24d5.patch` (539 lines, 9 files)
- Replay index: 0375, sha 948a36cf24d5

## Port decision summary

This item is a documentation + submodule-pointer commit: it adds the「Agent skills」section to
`AGENTS.md`, introduces the engineering-skills doc set (`docs/adr/README.md` + ADR-0001,
`docs/agents/{domain,issue-tracker,triage-labels}.md`), reworks the `docs/prompts.md` workflow
chain, and advances the `thirdparty/chatgpt-browser-agent` submodule pointer. No code files are
touched, so the whole item is E/C-excluded except the gitlink, which is excluded as git-generated
content (mode 160000) — E=0/C=0 by policy.

## File-by-file port

1. `AGENTS.md` (EXISTS — adapt):
   - Insert the upstream「Agent skills」block (❋ Issue tracker / ❋ Triage labels / ❋ Domain docs)
     after the "Commits and PR Titles" section and immediately before `## Style Guide`.
   - The upstream hunk carried an extra context line (`- Prefer automation: …`) that does not
     exist in this series' AGENTS.md; the insertion was re-anchored on the `## Style Guide` header
     only. Content of the three subsections is verbatim, including the `SMARK2022/opencode` (origin)
     remote, the 5 canonical lifecycle labels coexisting with `.opencode/agent/triage.md`, and the
     single-context CONTEXT.md + docs/adr layout references.
2. `CONTEXT.md` (EXISTS — SKIPPED):
   - The upstream commit creates CONTEXT.md as a NEW file (a domain glossary, blob c3204f0…). In
     this replay the file already exists at the target baseline with different, load-bearing
     content ("# OpenCode Session Runtime" — System Context/Session History/Context Epoch
     vocabulary that earlier v2-era items and the AGENTS.md V2 Session Core rules reference).
     Replacing it would orphan those references and destroy baseline-owned documentation. The item
     therefore SKIPS the CONTEXT.md replacement; the docs that reference it (`docs/agents/domain.md`
     instructs readers to "proceed silently" if a referenced file doesn't exist or differs) remain
     valid as guidance. Rendered decision: preserve the local CONTEXT.md, note the deviation.
3. `docs/adr/README.md` (NEW): created verbatim — ADR trigger criteria (load-bearing / non-obvious /
   rejectable), the format template, zero-padded numbering with supersede-never-renumber rule, and
   the index table with [0001].
4. `docs/adr/0001-triage-labels-and-team-assignment-coexist.md` (NEW): created verbatim —
   documents that the `.opencode/agent/triage.md` team-assignment agent (assignee axis) and the
   mattpocock `triage` skill's 5 lifecycle labels (label axis) coexist on orthogonal axes; the
   assignee agent never adds labels, the skill leaves assignment untouched. Accepted 2026-07-05.
5. `docs/agents/domain.md` (NEW): created verbatim — single-context layout (one CONTEXT.md +
   docs/adr/ at root), the read-before-exploring order (CONTEXT.md glossary, ADRs, package-local
   AGENTS.md, specs/effect patterns), silent-proceed rule, glossary-vocabulary discipline and ADR
   conflict flagging, and monorepo orientation via the Module map.
6. `docs/agents/issue-tracker.md` (NEW): created verbatim — SMARK fork `SMARK2022/opencode` as
   `origin` (never open SMARK issues upstream), gh CLI conventions, PR-as-triage toggle (no),
   wayfinder map/child/blocking/frontier/claim/resolve operations, and the coexistence statement
   with the team-assignment agent.
7. `docs/agents/triage-labels.md` (NEW): created verbatim — the 5-label role→label mapping table,
   label creation instructions, and the orthogonal-axes table against the team-assignment agent.
8. `docs/prompts.md` (EXISTS — update, on top of item 0371's section move):
   - Intro: the archive note becomes a workflow-chain description (① 只调研 → ② 方案审计与放行 →
     ③ TDD 手术级实现 → ④ 创建 commit; chain-runnable or per-scenario copy).
   - In the moved「只调研」section: insert `-------------` + `## 方案审计与放行` heading before the
     "在完整设计方案之后…" paragraph; insert the normalized「完整进行相应的审计与检查…不得进行任何的
     审计范围收缩与下降。」line after the hard subagent-check paragraph; after the closing
     `-------------` add the audit-loop rules: 阻塞性意见 definition (behavior-level defects only,
     style/labeling preferences excluded), 最多迭代 3 轮 force-convergence with the
     「开放问题」escalation and N-failure「未完成独立复核」escalation, and the blue-print release
     sentence「subagent 无阻塞性意见后放行，进入 TDD 手术级实现。」.
   - TDD section: 「做一次独立复核」→「做独立复核」(drop 一次, aligning the hard-gate wording with
     the audit-loop rule); append the review-loop paragraph after the 6-point checklist (same
     3-round iteration, no scope shrinking, escalation rules) before「根据复核结果继续修正…」.
   - Deviations from the upstream text: the upstream file contained a raw fragment line (`在完整`)
     and an unnormalized duplicate audit sentence; this series' file (post-0371) is the cleaned
     shape, so only the normalized sentence is inserted and the section streamlines to the same
     final chain semantics without the fragment.
9. `thirdparty/chatgpt-browser-agent` (GITLINK update):
   - Pointer chain verified consistent: 0338 created the pointer at `d78e57cba1`, 0345 advanced to
     `f4de5136bf` (present in the 0370 state's index), 0375 advances to `8144b3de709d` — every hop
     is a single `Subproject commit` line transition.
   - Applied with `git update-index --cacheinfo 160000,8144b3de709d…,thirdparty/chatgpt-browser-agent`
     in the scratch repo so the generated diff carries the standard gitlink hunk (index line with
     `160000`, `-/+ Subproject commit` lines), matching the 0345 precedent. `git apply --index`
     accepts the gitlink hunk (kills any doubt about empty submodule checkout state — 0345 already
     proved the mechanism in this exact series).
   - The gitlink lines are excluded from E per the auditor (mode 160000 index line switches into
     gitlink-skip mode for the hunk body).

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix375.diff` (22,475 bytes, 343 lines; 8 files: AGENTS.md,
  5 new docs, docs/prompts.md, gitlink)
- Current patch: `.temp/patches/current/0375-948a36cf24d5.patch`
- Materialized state: `0375-948a36cf24d5`

## E/C gate

`count-ec-auditor.py`: E=0, C=0, need=0 → PASS (all modified files are `.md` or gitlink-excluded).

## Materialize

`--materialize 375` → `passed 375/375`, state published. The `git apply` pass created the new
`docs/adr/` and `docs/agents/` directories implicitly and updated the gitlink in the index.

## Batch-end typecheck (375) — findings

Command: `bun .temp/patches/src/apply-cumulative.ts --typecheck 375`.

- Pre-existing environmental blocker resolved: the shared test workspace held
  `packages/app/src/custom-elements.d.ts` as a materialized regular file (checked out while that
  repo's `core.symlinks` was false) → `tsgo -b` failed TS1128. Fixed by setting
  `core.symlinks=true` in the test-workspace repo, deleting the artifact and re-checking out the
  tracked symlink (LinkType=SymbolicLink verified). With that, packages/app and the whole
  alphabetical prefix (app, cli, client, codemode, console/*, core, desktop, effect-*,
  enterprise, http-*, llm) pass.
- PRE-EXISTING (not caused by this batch) packages/opencode failure:
  `test/tool/shell.test.ts:1809/1830/1851` — TS2353: `description` is not a member of the shell
  input type `{ command, timeout?, workdir?, compress_output? }`. These three call sites were
  introduced by an earlier replayed item (≤0370, the "timeout empty-output diagnostic" tests —
  confirmed via `git log -S "Timeout no output test"` in the scratch history showing they arrived
  with the 0370 state snapshot, not with 0371-0375) and were never surfaced because every prior
  batch-end check aborted at packages/app first. No file in 0371-0375 touches shell.ts or
  shell.test.ts.
- Direct verification of this batch's changes: with the three stale `description:` arguments
  removed locally in the test workspace (behavior-neutral), `bun typecheck` passes in BOTH
  `packages/opencode` (covers task.ts nudge, goal.test.ts resume test, task.test.ts nudge tests)
  and `packages/tui` (covers 0372 voice wiring and 0373 Resume condition). The local edit was
  reverted afterward; the workspace is back to the canonical staged state.
- Conclusion: 0371-0375 are type-clean; the batch-end script reports red solely on the
  pre-existing shell.test.ts error from an earlier item, which should be fixed by the owning batch.

## Notes / deviations from source

- CONTEXT.md creation skipped (baseline-owned file with different purpose — see above).
- AGENTS.md insertion re-anchored (missing `Prefer automation` context line in this series).
- prompts.md transformations normalized (the upstream file's stray `在完整` fragment and duplicated
  audit sentence do not exist in the local file; the final workflow-chain semantics match).
- Everything else ported verbatim.

## G-01 修复（Round 1 逆向审计 BLOCK 修正）
**问题**：物料化 state 中 `packages/opencode/test/tool/shell.test.ts` 的 3 处 `run({...})` 调用含 `description:` 字段（0350 引入的 "timeout empty-output diagnostic" 测试），但目标 shell `run` 输入 schema（`packages/opencode/src/tool/shell/prompt.ts:16-26`）无 `description` 字段 → TS2353 真实类型错误。此前每批 typecheck 都因 packages/app TS1128 首错停止而掩盖了此错误，本批逆向审计员将其标记为 G-01（真实类型错误，非 test-workspace artifact）。
**修复**：在 current/0375 末尾追加 shell.test.ts hunk，删除 3 处 `description:` 参数行（纯删除，E=0 不受影响——纯删除行不计入 E；`// #350`/`#126` 等注释不变）。行为中性：description 仅测试诊断文本，不在目标 shell 输入 schema 中，删除不影响任何断言。
**验证**：
- `--typecheck 375`：**全部 30 个 workspace PASS**（含 packages/app——test-workspace 以 core.symlinks=true 物化 symlink 后 TS1128 消失；canonical state repo 仍为 core.symlinks=false，tree entry 相同）
- G-02 关闭：packages/tui 现已在 typecheck 报告中执行并通过
- G-03 关闭：packages/opencode 现在真正 PASS，记录 0373/0374 的 PASS 声明与报告一致

## DUAL-AUDIT SEAL (batch 0371-0375)
- R2 forward auditor (ses_fc9d94a3): 5/5 PASS, batch APPROVE, no open B/G
- R2 backward auditor (ses_fc9d9391): 5/5 PASS, batch APPROVE, no open B/G
- R1->R2 revision loop closed: G-01 (shell.test.ts 3x TS2353 description from 0350, masked by app TS1128) fixed in 0375 via 3 pure deletions; G-02 (packages/tui skipped) closed — all 30 workspaces PASS in typecheck-0375; G-03 (records 0373/0374 PASS vs report FAILED) closed — report now truly green
- N items (non-blocking): 0375 docs trailing newline cosmetic; prompts.md layout-only; 0372 binding shapes file-native; CONTEXT.md kept (baseline load-bearing glossary); 0374 loop: dropped (TaskPromptOps has no member)
- Install: 7c95150d reused, bun 1.3.14; integrityFailure None; typecheck 375: 30/30 workspaces PASS
- Tests: prompt-voice-input 20/20, goal 20/20, task nudge 4/4, session-preview 12/12
- Verdict: BATCH 0371-0375 CLOSED
