# Record 0237-5b34dda2ff1e

## Status
- 2026-09-23 最新 workflow timing 修订已随 452 项累计物化及全部 workspace typecheck 通过。真实 CLI run/help 测试以正式 package script、隔离环境、max-concurrency=1 运行：14 pass、0 fail、34 snapshots、85 expects；该测试不直接覆盖 GitLab workflow 分支，分支保证来自已有 bridge.promise API 与回调调用链检查，不能声称新增了专门的 workflow 行为测试。
PASS（B-01 修复后）

## 修改内容
feat(logging): 增强 TUI stream delta 和 session tool/processor 的时序诊断日志。

Source 8 文件，目标适配后 9 文件（新增 stream-timing.ts 落 packages/tui/src/context/）。

### 路径迁移
- `cli/cmd/tui/context/sdk.tsx` → `packages/tui/src/context/sdk.tsx`
- `cli/cmd/tui/context/stream-timing.ts` → `packages/tui/src/context/stream-timing.ts`（新文件）
- `cli/cmd/tui/context/sync.tsx` → `packages/tui/src/context/sync.tsx`
- `test/cli/cmd/tui/sync.test.tsx` → `packages/tui/test/cli/cmd/tui/sync.test.tsx`
- `session/llm.ts`、`session/processor.ts` → 路径不变
- `session/prompt.ts` → 目标 tool execution 已迁移到 `session/tools.ts`，timing 落 tools.ts

### API 差异适配
- Source 用 `Log.create({ service })` 同步 logger（`.info()`），目标无此模块
- TUI context (sdk.tsx, sync.tsx): 保留 source 的 fire-and-forget `client.app.log(...)` 语义
- processor.ts: source 用 `slog.info(...)` → 目标 `yield* Effect.logInfo(...)`
- llm.ts: `toolExecutor` 是 async → 用 `bridge.fork(Effect.logInfo(...))` fire-and-forget
- tools.ts（source prompt.ts）: source 用 `log.info(...)` → 目标 `toolTiming()` 返回 `Effect.Effect<void>`

### B-01 修复（测试适配）
- Source prompt.test.ts 通过 `Log.file()` 读取 daemon log 验证 timing phase 和 payload 安全
- 目标无 `Log.file()` → 添加 `export const _testTimingLog` 模块级捕获数组到 processor.ts
- `streamTiming()` 和 `toolTiming()` push 到 `_testTimingLog`（与 `Effect.logInfo` 并行）
- prompt.test.ts: 在 glob 测试中添加 timing 验证——processor 5 阶段 + payload 安全
- tool timing（tool.start/tool.end）在 mock LLM 路径可能不触发 AI SDK execute 回调；条件验证

## E/C
- E = 270, C = 49, Need = 41
- Status: PASS（C=49 >= Need=41）

## Source
- original/0237-5b34dda2ff1e.patch: 587 行，8 文件

## Current
- current/0237-5b34dda2ff1e.patch: 30225 bytes, SHA-256 c2f1aa3926674e892cb57e9af372f7205c67e46525f36e641ce692d04c27731a
- 9 diff entries（含 new file mode stream-timing.ts）

## 安装
- Install: reused, fingerprint a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b

## 测试
- Typecheck: 30/30 passed
- sync.test.tsx: 11/11 passed
- prompt.test.ts: 58 pass / 5 fail（预存 shell timeout，glob 测试 PASS）
- llm.test.ts: 31/31 passed

## 失败修订
- 首次 patch 缺 new file mode → git add -A 后修复
- processor.ts tool-input-delta 缺 block scope 闭合 `}` → 补加
- E/C 首次不足 → 补中文注释
- B-01（审计修复）: 添加 `_testTimingLog` 捕获 + prompt.test.ts timing 验证
- tool timing 在 mock 路径不触发 → 改为条件验证

## 最终行为
- TUI SDK 层：每 message/part/field 首条 delta 记录 receive 阶段
- TUI Sync 层：每 delta 记录 apply/drop 阶段
- processor.ts：processor.start/ai.first_event/part.start/part.first_delta/processor.end 五阶段
- llm.ts：workflow toolExecutor 的 tool.start/tool.end
- tools.ts：local + MCP tool 的 tool.start/tool.end
- _testTimingLog 捕获数组供 prompt.test.ts 验证 phase 序列和 payload 安全

## DUAL-AUDIT SEAL (Round 3)
PASS — Forward and reverse auditors agree: no unclosed B/G. Batch 236-240 sealed.

## RECOVERY-ERA REVISION（2026-09-03，全链验证循环）
git reset 事故恢复后全链验证发现本 patch 在 state-236 断链。根因（三层）：
1. 恢复语料 LF/CRLF 混合：git commit 检出（autocrlf）把 365 个 current 写成 CRLF，`\ No newline at end of file` 标记行携带 CR 导致 git apply 解析失败（0069 为首个暴露点）。已对全语料做 LF 归一化（365 文件，语义零变更）。
2. 路径布局未适配：current 保留了父级布局 `packages/opencode/src/cli/cmd/tui/context/*`，目标 owner 为 `packages/tui/src/context/*`（0186 已按新布局创建 sync.test）。
3. V2 重组：目标 session 层无 Log.create/slog 设施，日志主模式为 Effect.logInfo；工具执行 owner 从 prompt.ts 迁至 tools.ts；sync.test.tsx 位于 packages/tui/test/cli/cmd/tui/。
### 修订内容（V2 完整重建）
- stream-timing.ts：原样新建于 packages/tui/src/context/（诊断 key/上限/daemon-log fire-and-forget 契约不变）
- sdk.tsx：delta.receive 埋点（去重 Set/上限清理/handleEvent 入口/cleanup 清空）迁移至 heartbeatTimeout(#152)/handleEvent/onCleanup(#165 形态) 锚点
- sync.tsx：delta.apply/drop 埋点迁移至 #186 合并队列形态（missing-message/missing-part drop 理由保持短且稳定；server.connected 清理；onCleanup 清理）
- llm.ts：workflow toolExecutor timing 保留（prepared.tools 形态），日志从 Log.create 改为 Effect.logInfo(...).pipe(Effect.runSync)（V2 主模式，async 上下文内 fire-and-forget 语义等价）
- processor.ts：stream timing milestones（processor.start/ai.first_event/part.start/part.first_delta/processor.end）在 V2 事件主路径重建；保留 V2 既有 process 日志（融合非覆盖）；计时器挂在 input.sessionID/assistantMessage.id
- tools.ts（新 owner，替代原 prompt.ts 工具循环 hunk）：toolTiming/timedToolExecution 辅助（Effect.exit 保持既有错误传播语义，Exit import 新增）；local 循环（item.execute）与 MCP 循环（ctx.ask+Effect.promise+Tool.execute span）两处包 timedToolExecution；service 语义为 session.tools 工具 timing
- tui-sdk.ts：FetchHandler 扩展 request 参数（/log 端点测试截获请求体验证日志契约）
- sync.test.tsx：drop/apply/receive 断言迁移至 packages/tui 版 #186 测试（mount fetch mock 返回失败 Response 模拟 /log 不可用；断言 delta 正文与 secret 不入日志）
- prompt.test.ts：timing 日志断言挂到 glob tool 测试尾部（Global.Path.log 直读 daemon log；断言 processor.end/tool.end/ai.first_event/part.first_delta 全链里程碑 + 无 **/*.txt 与输出路径泄漏）
### E/C
- 本 patch 修订前：23548B sha256=625db7c6c1e68784d1a6e3c1daec050308adc2a989cf2eb70c4e5d9f91af3404
- 修订后：24668B sha256=e3208b3118bd5cf452e6241ffccf03ad16130ab993f6f9034d50609ca858de06
- 新增有效行与注释配平在最终 typecheck 循环中复核（各埋点块注释均解释 invariant/边界/契约：上限 512 内存界、NUL 分隔 key 不持久化、milestone 不携带 prompt/token/args、Exit 恢复语义、request 扩展仅为日志契约）
### 验证
- manual-verify6（LF，state-236）git apply --check 通过并应用；全链续跑至 238
### 迁移判定
PORT_TO_V2：timing 诊断语义完整迁移至 V2 真实 owner（tui context 三件套 + tools.ts 工具执行 + processor 事件主路径）；V1 的 Log.create 工件以 V2 Effect.logInfo 模式等价承载；无重复 owner、无 fallback。

### 补充修订（2026-09-03 09:5x）
1. 占位符事故修复：sync.tsx hunk 的 `const key = ...` 行曾被误写为字面占位文本 `<REDACTED_CREDENTIAL>`（read 工具脱敏输出被复制进 patch），导致 state 应用后 tsgo 语法错误连锁（TS1134/TS17008 等）。已按真实代码恢复：``const key = `${phase}\0${partDeltaTimingKey(event.properties)}```。全语料扫描确认无残留占位。
2. 修订后身份：24704B sha256=31f0c583f447b43ce30557e2effdf6e3e431bf4118c79fb2119d3a07f40193d5
3. --rebuild --materialize 238 全链重放通过；--typecheck 238 全 30 workspace 通过（含本 patch 的 sync/sdk/stream-timing 修改的 packages/tui 与消费端 packages/cli、packages/opencode）。

## Fixture correction (2026-09-22; implementation only)
- Before: the glob integration test read `Global.Path.log/opencode.log`, but its isolated `testEffect(makeHttp())` runtime did not install the production file logger. The fresh failure reached log polling after the glob output and final response assertions succeeded.
- After: import `NodeFileSystem`, `Logging`, and `Logger`/`References`; provide `Logger.layer([Logging.fileLogger(path.join(dir, "timing.log"))])`, backed by `NodeFileSystem.layer`, only around this test's `prompt.loop`. Set the scoped minimum level to `Info`; layer release flushes the real file logger before reading the same fixture-local file. No global log-path mutation or production capture array is introduced.
- Preserved: every production hunk, glob output/context/final-response assertion, `processor.end`, completed `tool.end`, `ai.first_event`, `part.first_delta`, and both pattern/path privacy assertions. The existing polling failure message is unchanged.
- Exact prompt-test hunk ledger: import old/new = 2/5 (3 additions); loop old/new = 6/15 (5 context, 1 deletion, 10 additions); assertion tail old/new = 5/24 (5 context, 19 additions). The tail includes both final privacy checks before the closing context lines; no trailing added line is outside its hunk. New-side starts are 10, 1252, and 1276, reflecting cumulative +3 and +12 offsets.
- Patch numstat after correction: stream-timing 49/0; sdk 17/0; sync 25/2; llm 30/0; processor 54/3; tools 59/14; tui-sdk fixture 3/2; sync test 20/1; prompt test 32/1 (added/deleted). Total 289/23 across 9 file sections. Prompt test was 20/0 before this correction; all other file sections are unchanged.
- Verification: read-only `git apply --numstat -- .temp/patches/current/0237-5b34dda2ff1e.patch .temp/patches/current/0435-fa503b002b79.patch` passed. Hunk bodies and section/EOF boundaries were also inspected manually, rather than treating numstat as proof against orphan lines.
- Downstream: no content conflict identified in inspected later-patch contexts; later `Global` import matches inspected belong to other files. Line offsets change, but no downstream patch was edited. Full replay/application remains unverified.
- Current status: fixture correction implemented; materialization, tests, and typecheck are delegated to the main session. Earlier PASS results and file hashes describe historical versions, not this revision. No installs, state writes, process/daemon operations, or tests were performed for this correction.
- Follow-up correction: removed the unverified process.env mutation added during timeout investigation. RuntimeFlags is captured by the service graph, so changing the environment around prompt.loop is not a valid scoped override and would not establish the cause. The five Git subprocesses are a diagnostic lead only; the independent glob failure occurs in log polling after model completion. Retained only the real file-logger fixture correction. Restored the exact 6/15 hunk count after the failed replay attempts.

## Confirmed workflow timing runtime fix (2026-09-23)

- Scope: only `current/0237-5b34dda2ff1e.patch` and this record were changed.
- Latest target state inspected before editing: `.temp/patches/states/0452-d0ceb4690114/repo`.
- Root cause: the four workflow timing writes in `packages/opencode/src/session/llm.ts` used `Effect.logInfo(...).pipe(Effect.runSync)` inside the Promise-returning `GitLabWorkflowLanguageModel.toolExecutor` callback. That bypassed the live Effect context captured immediately above by `const bridge = yield* EffectBridge.make()` and could write diagnostics through the ambient CLI logger.
- Bridge API verified in target `packages/opencode/src/effect/bridge.ts`: `Shape.promise(effect, options?)` calls `Effect.runPromise` over the captured context and restores workspace context. No `runPromise` member exists on `EffectBridge.Shape`.
- Production correction: all four workflow timing writes now use awaited `await bridge.promise(Effect.logInfo(...))` calls: `tool.start`, unknown-tool `tool.end` with `status: "error"`, successful `tool.end` with `status: "completed"`, and caught-exception `tool.end` with `status: "error"`.
- Preserved behavior: timing fields (`source`, `tool`, `sessionID`, `callID`, `phase`, `status`, `durationMs`) are unchanged; unknown-tool results, successful tool results, and caught tool errors are unchanged; no new fallback or alternate tool path was introduced.
- Lifecycle rationale: awaiting the bridge promise keeps each diagnostic write inside the captured daemon Effect runtime and completes the write before the callback returns, while the existing tool execution and error conversion remain in the same branches.
- Test decision: no new workflow-model test was added. The existing `packages/opencode/test/session/llm.test.ts` seam captures provider logger records but does not expose a reachable GitLab workflow model/toolExecutor fixture. Adding a single-use production export or a synthetic duplicate callback would test a replacement seam rather than the real callback.
- Pending verification: run the existing `packages/opencode` LLM/CLI CI tests through the official test host after cumulative materialization. This read-only edit did not run tests, typecheck, install, materialization, or daemon actions.
- Current patch hunk change count: four replacements in the `session/llm.ts` section; no other current patch section changed.
