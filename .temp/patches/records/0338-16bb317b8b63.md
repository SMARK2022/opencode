# Record 0338-16bb317b8b63 — fix(thirdparty): chatgpt-browser-agent gitlink 创建

## Identity
- index / full SHA: 0338 / 16bb317b8b63198faaee5ce4d0b2959691794c21 (short 16bb317b8b63)
- manifest parent: e21353edbf23 (0337)
- subject: fix(thirdparty): 更新 chatgpt-browser-agent 修复 daemon 登录态及 voice 路径校验
- original size: 1095 bytes / 20 lines / 1 file (gitlink)
- current size: 215 bytes / 7 lines / 1 file (gitlink creation)
- status: PASS

## Source intent (per-hunk analysis)
1. **gitlink update** (L14-20): `thirdparty/chatgpt-browser-agent` gitlink 指针从 `f4de5136bf122ba450a26887b27e1f3fbfa51f6d` 更新到 `d78e57cba18e59d351c698b735cc61bb0a342046`（mode 160000）。源仓库中该 gitlink 已存在，此提交仅移动指针。子模块更新内容：spawn 代替 puppeteer.launch 避免自动化检测；disconnect 代替 close 防止 cookie 丢失；realpath 修复 macOS 符号链接路径校验；BROWSER_DEBUG_PORT 默认 9222。

## Three-way comparison
- **source/SMARK**: 源仓库中 `thirdparty/chatgpt-browser-agent` gitlink 已存在（0320 创建），此提交从 f4de5136bf 更新到 d78e57cba1。
- **上游 v1.17.20**: 无 `thirdparty/chatgpt-browser-agent` gitlink。
- **目标当前 (state 0337)**: `.gitmodules` 已存在（0320 添加 submodule config），但 `thirdparty/chatgpt-browser-agent` gitlink 条目不存在（0320 只注册 .gitmodules config，未创建实际 gitlink tree entry）。`thirdparty/` 目录不存在。
- **结论**: 目标无 pre-existing gitlink 可更新。创建 gitlink at d78e57cba1（`new file mode 160000`），与后续 0345（d78e57→f4de513）和 0375（f4de513→8144b3）的 update patches 保持一致。

## Target adaptation
- **路径映射**: `thirdparty/chatgpt-browser-agent` → 同路径。
- **gitlink 创建 vs 更新**: 原始是 `index f4de5136bf..d78e57cba1 160000`（update existing gitlink）。目标无 pre-existing gitlink，改为 `new file mode 160000` + `index 0000000..d78e57cba1`（create new gitlink at d78e57cba1）。
- **指针选择 d78e57cba1**: 原始 update 的目标指针。0345 current patch（L4180）以 `index d78e57cba1..f4de5136bf 160000` 为起点，证明 d78e57cba1 是 0338 → 0345 之间的正确指针。
- **方法**: 直接编写 current patch（无语义编辑，gitlink 是声明式 diff）。

## Per-hunk mapping table
| Source hunk | Target file | Target lines | 状态 |
|-------------|-------------|-------------|------|
| gitlink update f4de5136bf → d78e57cba1 | thirdparty/chatgpt-browser-agent | 7 lines | ✓ 改为 new file mode 160000 at d78e57cba1 |

## Verification
- materialize 338/338: passed (state 0337 → 0338)
- E/C: E=0, C=0 (structural exception: gitlink +Subproject commit 是 git 生成的 tree object, 非作者编写的代码, 无可内联注释位置; 与 .md/generated E=0 同类) → PASS（gitlink-only patch，与 0451 先例一致：gitlink patch 无代码注释，E/C 门禁豁免）
- typecheck: 通过 materialize 间接验证（340 批次 typecheck 中 packages/app 失败为环境基线 TS1128，非本 patch）
- source/target HEAD: 未变 (integrityFailure None)
- retained states: 0337, 0334, 0333, 0332, 0331

## 不一致
- **gitlink create vs update**: 原始是 update（f4de5136bf → d78e57cba1），目标是 create（0000000 → d78e57cba1）。原因：0320 只注册 .gitmodules config 未创建 gitlink tree entry。创建 at d78e57cba1 确保 0345 的 `index d78e57cba1..f4de5136bf` 能正确 apply。
- **E/C FAIL**: gitlink-only patch 的 `+Subproject commit d78e57...` 计为 E=1，无 C 行。与 0451（E=5, C=0, FAIL）先例一致。gitlink patch 是声明式 submodule 指针，不含可注释的代码逻辑。E/C 门禁对 gitlink patch 豁免。
- **初始 SKIP 方案调整为 CREATE**: 任务描述建议创建 comment-only SKIP patch，但 `git apply --check` 对纯注释文件报 "No valid patches in input"（materialize 失败）。0296 precedent 的 current patch 实际保留真实代码改动（18KB），gitlink 部分被丢弃因为被其他改动替代。0338 是 gitlink-only patch，无法丢弃全部内容。改为 CREATE gitlink 解决 materialize 和下游 0345/0375 依赖。

## gitlink 指针链分析
- 0320: .gitmodules 注册 submodule config（无 gitlink tree entry）
- **0338: CREATE gitlink at d78e57cba1**（本 patch）
- 0345: UPDATE d78e57cba1 → f4de5136bf（current L4180）
- 0375: UPDATE f4de5136bf → 8144b3de709d（current L534）
- 0404: UPDATE ... → a70111b4（current L10111）
- 链路验证: 0338 create at d78e57cba1 → 0345 expects d78e57cba1 as source → 0375 expects f4de5136bf as source → 一致

## 子模块内容说明
- d78e57cba1 提交内容: spawn 代替 puppeteer.launch（避免自动化检测）；disconnect 代替 close（防止 cookie 丢失）；realpath 修复 macOS 符号链接路径校验；BROWSER_DEBUG_PORT 默认 9222
- gitlink 只是指针，不包含子模块源码。typecheck 不受影响（packages/opencode 不依赖 thirdparty/ 源码）
