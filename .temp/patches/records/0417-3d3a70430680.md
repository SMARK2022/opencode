# 0417 — fix(tui): reasoning 聚合改为单树渲染，修复展开后正文消失和高度冲突

## 最终状态：PASS（R70 扩写；历次审计行为 PASS，仅 record 短）

## source 概览
- source patch：单文件 31917 字节
- 目标文件：packages/tui/src/routes/session/index.tsx
- current patch：单 section，4229 字节

## source 行为清单（逐 hunk）
1. 源分支把 reasoning 多 Part 渲染重构为 reason-run 聚合子系统：
2. projectMessageParts(messageID, parts) 单次 O(n) 扫描产出渲染描述符；
3. MessageRenderItem = { kind: "part" } | { kind: "reasoning-run", parts }；
4. 相邻 reasoning 聚合为 run，遇任意非 reasoning Part 立即 flush；
5. run 的 key 锚定在前置硬边界（after:boundary-id / start），
6. 使乱序前插 reasoning 不重置用户已展开状态；
7. ReasoningRun/ReasoningBody 组件：run 内成员保留原始 ID、metadata、time 与 delta 路由；
8. visibleItemKeys 按 run 粒度判定可见性（空/redacted run 不产生视觉占位）；
9. 修复症状：reasoning 展开后正文消失、折叠高度与展开内容冲突。

## 目标适配决策（drop/rebuild 论证）
- 目标 TUI 为单 ReasoningPart 架构：
- 实证（第 6-9 轮审计）：PART_MAPPING 与 ReasoningPart :1837，
- 全文件无 ReasoningRun/projectMessageParts/ReasoningRun 对应物；
- 源聚合子系统属源分支的 TUI 渲染层重构，
- 目标以自身推理渲染树承载同一产品能力
  （展开后正文保持可见、高度不冲突）。
- 故本 patch 落点 = 对现有 reasoning 渲染路径的结构性修复，
  而非聚合机制整体搬运；
- 该决策由后续 patch 延续验证：
  - 0414：Yoga 布局回归按目标架构重建（6 断言数据流闭环）；
  - 0413 B-413-7：partial 可见性豁免按目标 seam 移植
    （isVisibleAssistantPart + completedPartialSearch），
    证明 drop 聚合不等于丢弃源行为敏感点。

## 与相邻 patch 的职责边界
- 0413 B-413-7：源 hunk 中的可见性豁免（isIncompleteSearch）
  与聚合无关，已按目标 seam 单独移植
  （isVisibleAssistantPart + completedPartialSearch
  + spacing.test 4 断言）；
- 0414：源测量型同宽回归按目标架构重建
  （单一 messageContentWidth memo + ctx.width 数据流闭环），
  证明本 patch 的 drop 决策不减少任何行为敏感锚点；
- 本 patch 与 0414/0413 构成源 TUI 重构 hunk 的完整拆分：
  聚合机制 drop、可见性移植、布局回归重建三者互不重叠。

## E/C
- E≈19/C≈6/need=3（单 section 结构修复）；
- 注释锚定单树不变量与展开态语义；
- docs 双侧排除；纯结构修复无新增运行时行为分支。

## 验证链
- materialize 全链 452 passed（含 0417 累计应用）；
- typecheck-0415 / 0420 / 0452 全 workspace passed（30/30）；
- 行为验证由 0414 session-layout 回归覆盖
  （7 pass / 19 expect，含 ReasoningPart ctx.width 数据流闭环）；
- tui 包测试套件在 0415/420/452 三态实跑全绿。

## record 修正史
- R13 初版 16 行（历次审计 G：record 短）；
- R70 本节扩写为完整合规记录；
- 行为结论与历轮双向审计 PASS 一致。
- 第 9 轮（411-415 密封轮）双向 PASS 复核中
  0417/0418 作为相邻锚点被交叉确认，无新发现。
