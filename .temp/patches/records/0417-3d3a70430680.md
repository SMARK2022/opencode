# Record 0417-3d3a70430680

## Identity
- SHA: 3d3a70430680a4f939b3372773513ef5cf8b56d2
- Parent: a0a7a4e19d12 (0416)
- Subject: fix(tui): reasoning 单树化——折叠 preview 与全文合并为一棵常驻树
- Status: PASS（materialize + typecheck + 批次回归全部通过）

## Source intent
source 0417（639 行，2 文件）：
1. `src/cli/cmd/tui/routes/session/index.tsx` 7 hunks：删除 createThrottledSignal import；REASONING_SEPARATOR；ReasoningRun 顶部删除 bodyMounted/previews/hidden memo；**双树收敛为单常驻树**（折叠仅 maxHeight/overflow 切换，For member key=part.id）；footer 简化；reasoningRowsTotal 含分隔符行；ReasoningBody 重写。
2. `test/cli/cmd/tui/session-message-render.test.tsx`：断言改 + 5 个新测试（first expanded frame / streaming survive toggles / resize preference / selection guard / abort-error coexistence）。

## Target analysis 与适配（关键决策）
- 目标无 ReasoningRun 聚合（0413 剥离）；目标 ReasoningPart 为 per-part singleton（0415 状态 L1838-1948）：
  - `createSignal(false)` 默认折叠（hide）；5 行预览块（slice(0,5)）+ `▼ 展开查看完整推理` hint；全文树 `!inMinimal() || expanded()` 挂载（Switch：!isDone→streaming / isDone→keyed remount）；#137 completedKey；50ms createThrottledSignal。
- source 修复的病灶（双树切换空窗/height 冲突）在目标不存在（互斥 Show + 同步 markdown 渲染）；但 **source 单树常驻思想**是有效语义（toggle 不卸载 markdown 子树，保留滚动/高亮/测量状态）→ 目标承载为**单树化**：
  - 折叠态：maxHeight=5 + overflow=hidden + content=前 5 行（previewText 轻量渲染）
  - 展开态：maxHeight/overflow 放开 + content=summary().body（标题在 header）
  - streaming=!isDone()；#137 keyed remount 保留重定义：流式期 key 恒定 "streaming"（不 remount），完成态 key=width+content（remount 一次防粘滞）
  - hint 保留（>5 行才显示）
- 测试 hunk：目标无 session-message-render harness（0414 同因）→ SKIP + record；渲染稳定性由单树结构承载（行为验证见 0418 hint 语义 + 后续人工/回归）。
- 目标 hint 行在单树化中同步更新为 `collapsed && ... > 5`（0417 引入 collapsed 变量；0418 在该基础上改流式无条件提示）。

## Verification
- materialize 417/418/419/420：passed（含恢复后的重建链）
- typecheck 420：全部 workspace PASS
- E/C：E=38、C=6（注释：流式不 remount/collapsed 判定/折叠裁剪不卸载/展开溢出/完成态 key）、need=6 ✓
- 关键修复过程（本 record 记录）：addheads/addcomments/fixhunkcounts 曾破坏 0417/0418 current（hunk body 丢失）→ 从 0416 state + D:\Temp\opencode\idx-0418post.tsx 重建：0417 = diff(0416, idx-0418post−0418变化)；0418 = diff(0417 state, idx-0418post+0417注释)。重建后 materialize 全链重新验证通过。
## Hunk mapping
| 文件 | 变更 | 说明 |
|---|---|---|
| packages/tui/src/routes/session/index.tsx | 单 hunk（@`-1888,60 +1888,51`）| 双树（preview Show + full Switch）替换为单常驻树 IIFE：previewText 5 行 slice；reasoningTreeKey = isDone ? `width+content` : "streaming"；collapsed = inMinimal && !expanded；maxHeight 5/overflow hidden；content 折叠=previewText 展开=summary().body；hint `collapsed && lines>5` |
| test/cli/cmd/tui/session-message-render.test.tsx | SKIP | 目标无该渲染 harness（全库核实；packages/tui/test/cli/tui/thinking.test.ts 只测 reasoningSummary 纯函数）|

## E/C（独立计算）
- script 计数：E=38、C=6、need=6 PASS。
- auditor 语义重计（`{/* #417：单树常驻…… */}` 块为真注释）：C≥8、PASS。
- 代表注释位置：IIFE 内 `// 流式期间不 remount…`、`// 折叠/展开共用一棵常驻 markdown 树…`、`// 折叠态固定 5 行高度：maxHeight/overflow 裁剪而非卸载子树…`、`// 展开态恢复可见溢出…`；JSX 内 `{/* #417 … */}` 块（4 行）。

## Install / typecheck 证据
- install 指纹 `1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5`（bun 1.3.14 frozen reuse；auditor 独立重算一致）。
- typecheck-0420-17:35:17：30/30 workspaces passed（packages/opencode、packages/tui 均含）。

## 审计结论（双审计）
- 正向 auditor：无 B；N-3（无渲染 harness）、N-5（目标 50ms throttle/per-part 保留=最小适配）记档。
- 反向 auditor：构造性重建证明 current/0417 恰好是单树化变更（t417=tree(0416)+current/0417）；G-01 record 门禁 → 修复轮本 record 扩写达标。
- 本项 verdict：PASS（修复轮无 B/G 残留）。

## 失败修订记录（恢复事件）
- addheads/addcomments/fixhunkcounts 脚本事故清空 current/0417 body → 重建：diff(0416 state index.tsx, idx-0418post.tsx − 0418 变化行) 生成 0417 current；materialize 417 重建验证通过（17:28:03 passed 417）。
- 0417 的 4 条 IIFE 内 `//` 注释在重建时补齐；hunk 头行数经 fixhunkcounts 修正（+1888,47 → +1888,51）。
