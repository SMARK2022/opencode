# Record 0411-7bd1e1560289

## Identity
- SHA: 7bd1e1560289
- Parent: 4498da0c374e (0410)
- Subject: feat(goal): 增加 continueOnError 策略控制终止错误后的 GOAL 续跑
- Status: PASS（批次已闭合；详见 0415 批次结语与双审计结论）（目标适配重写中）

## Source intent
source 0411 共 14 文件（3873 行）：
1. 新增 `docs/goal-error-continuation-control-plan.md`（587 行设计文档）：为持久化 GOAL 增加默认关闭的 `continueOnError: boolean`；eligible 错误 allowlist 固定为 APIError + UnknownError；prompt loop 内层 outcome 扩为 "continue"|"break"|"terminal-error"；一次性 marker 仅存 { assistantID }。
2. 新增 opendcode-style migration（migration.sql: `ALTER TABLE session_goal ADD continue_on_error integer DEFAULT false NOT NULL` + Drizzle snapshot.json）。
3. TUI dialog-goal.tsx：setGoal 返回解析后的 Goal、toggleContinueOnError 选项（✓/○/⋯ Saving）、reconcile 到 sync store。
4. TUI sync.tsx：SessionGoalInfo 加 continueOnError + goal.reconcile provider。
5. HTTP 层：GoalSetPayload 加 continueOnError；handler 透传。
6. goal.sql.ts：SessionGoalTable 加 continue_on_error(boolean 默认 false)。
7. goal.ts：Goal Struct + continueOnError；SetInput；fromRow；update/insert 分支。
8. prompt.ts：interruptedToolState 传 elapsed；local/MCP adapter/Task/direct shell 长耗时 notice（formatLongExecutionNotice + user_abort elapsed_ms；source 分支夹带）；goal continuation 重构（normalCompletion/errorCompletion + marker + outcome terminal-error + continueOnError guard）。
9. 测试：httpapi-goal 4 用例；goal 5 用例；prompt 2 用例；新增 goal-migration 兼容测试。

## Target adaptation strategy（0411 为目标重写）
- 路径映射：TUI → packages/tui/src/{component/dialog-goal.tsx, context/sync.tsx}；goal.sql.ts → packages/core/src/session/goal.sql.ts；Goal Struct → packages/schema/src/session-goal.ts；migration → core TS migration 体系（新增 20260727000000_add_goal_continue_on_error.ts + schema.gen.ts 同步 + migration.gen.ts 注册）。
- prompt.ts 夹带的 local/MCP adapter notice：**目标包装点位于 packages/opencode/src/session/tools.ts（source 分支在 prompt.ts resolveTools，目标同 owner 在 tools.ts 的 tool() 包装）**——首次适配误判为"目标无该结构"而跳过了 user_abort/long-completed 装饰；R2 审计（B-01）证伪该判断后，已在 tools.ts 五个包装点（local execute + MCP list/listTemplates/read/工具循环）补齐装饰（adapterStartedAt + aborted→user_abort Notice + 长成功→completed Notice，Bash 豁免），并补 import。
- **B-02（R2 审计）**：progress metadata 更新重置 time.start 的修复（source hunk 显式保留 running 原 start）同样迁移到 tools.ts ctx.metadata()。
- Task/direct shell/interruptedToolState 有落点 → 迁移。local/MCP 的 generic-long-notice 语义与 0412 的 processor/message-v2 适配共同构成完整链（工具终态装饰 + 错误 marker 回放 + shell policy）。
- interruptedToolState：目标已有同一函数（#173/#216 注释），适配 elapsed 参数传递。
- 测试适配：httpapi-goal 的 request 为 3 参数（url, directory, init）——source 2 参数 + x-opencode-directory header 需改写；404 缩进 hunk 目标已收敛跳过；goal/prompt 测试 EOF 追加 verbatim；goal-migration 测试按 core migrate-review-agent.test.ts 模式重写（SqliteClient + applyOnly）。
- prompt.ts continuation 重构保留目标已有逻辑（isOrphanedInterruptedTool、orphan 日志、maxGoalTurns pause）。
- 注释前缀统一用 #411（与目标 #364 风格一致）。

## Verification（完成）
- materialize 411：passed 411/411（复用 0405 前缀重放 406-411；state 0411 因语义化编辑污染经用户授权删除并全量重建）
- typecheck 411：单 index 报 3 个错误，全部为 0412 顺序依赖（formatLongExecutionNotice 未导出、interruptedToolMetadata 单参、elapsed_ms 不在 ExecutionNotice）——0412 应用后消失（最终由批次末 typecheck 415 验证）
- 该顺序依赖在 source 分支同样存在（0411/0412 为相邻 commit，0411 的 prompt.ts 引用 0412 的导出）

## 修订历史
- R1：current/0411 从 zero 重写（首版 apply 失败：TUI 路径迁移 + #364 注释前缀 + 6/8 空格缩进 + goal.sql.ts 在 core 包 + migration 体系差异）
- R2：httpapi-goal 测试 parsed 类型断言（目标 responseJson 返回 Json 类型，TS18047/TS2339；对齐文件 L152 既有 as 断言模式）
- R3（批次双审计 BLOCK 修复）：
  - B-01：tools.ts 五个工具包装点补齐 user_abort/long-completed notice 装饰（记录 adapter 入口时间；aborted 且非 Bash 先追加 user_abort Notice 再 completeToolCall；否则长成功追加 completed Notice）；目标包装点结构为上游 #214/#237 既有（tools.ts 而非 source 的 prompt.ts resolveTools），语义等价的 owner 迁移
  - B-02：tools.ts ctx.metadata() 的 `time: { start: Date.now() }` → `{ start: match.state.status === "running" ? match.state.time.start : Date.now() }`（progress 更新保留原始 start，end−start 为完整执行时间，避免污染 elapsed_ms marker 与 Task 长成功 Notice）
  - 0412 current 的 tools.ts 段重新生成（pre 变为 0411 后版本；post 不变，仅 AbortSignal + ShellID import）
- E/C（最终）：count-ec-auditor.py E=428 / C=109 / need=65 → PASS（B-01/B-02 修复后重测）

## 行为测试（批次末 0415 状态实测）
- bun test test/server/httpapi-goal.test.ts：10 pass / 0 fail（含 4 个 continueOnError 用例）
- bun test test/session/goal.test.ts：25 pass / 0 fail（含 5 个 continueOnError 用例）
- bun test test/session/prompt.test.ts -t "goal error continuation"：2 pass / 0 fail（默认 false 不续跑 + 开启后 1 次续跑到 maxGoalTurns pause）
- core bun test test/session-goal-migration.test.ts：1 pass / 0 fail（旧行迁移后 continue_on_error=0 且原字段不变）

## 最终行为
- 创建/更新/删除 goal 时可选透传 continueOnError（HTTP payload + domain SetInput + DB boolean 列默认 false）
- prompt loop：terminal-error outcome + 一次性 marker（assistantID）→ 下一 iteration 顶部消费并以精确 allowlist（APIError/UnknownError）复验 → 进入现有 GOAL continuation 入口（goalTurns/maxGoalTurns/active/主 session/非 decide/step 限制）
- 默认 false：升级后旧 GOAL 行为不变；错误路径不注入 continuation；错误内容不进入模型上下文
- TUI：Manage Goal 菜单新增 “Continue after errors” toggle（✓/○/⋯ Saving），POST 成功后 reconcile 到 sync store 保持菜单打开
- migration：旧库 ALTER TABLE 增量加列（默认 0）+ schema.gen.ts fresh 建表同步 + migration.gen.ts 注册；兼容测试覆盖旧行保留
- 0411 还夹带 elapsed/notice 改造（interruptedToolState 传 elapsed、Task/direct shell 长成功 notice、tools.ts 本地/MCP 包装装饰、progress start 保留）——这些引用的 formatLongExecutionNotice 与 interruptedToolMetadata 双参签名由 0412 提供（同一批次链路）