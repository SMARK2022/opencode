# 0411 — feat(goal): continueOnError 策略控制终止错误后的 GOAL 续跑

## 最终状态：PASS（R13）

## source（113KB 14 文件）
- 目标架构多处 owner 迁移重建：
  1. Goal schema → packages/schema/src/session-goal.ts（源在 opencode goal.ts 内联 Schema）
  2. 列 → packages/core/src/session/goal.sql.ts（源在 packages/opencode/src/session/goal.sql.ts）
  3. migration → core TS migration（20260812100000_goal_continue_on_error.ts + migration.gen 注册；源为 drizzle sql 目录+snapshot.json，目标无此布局）
  4. goal.ts：SetInput/fromRow/updates/insert 适配目标 Effect-orDie 风格
  5. groups/handlers：GoalSetPayload + continueOnError 透传
  6. TUI 重映射 tui/component/dialog-goal.tsx + tui/context/sync.tsx；sync goal.reconcile
  7. prompt.ts：goalTurns 后 pendingTerminalError 一次性 marker；normalCompletion/errorCompletion 拆分（APIError/UnknownError allowlist）；continueOnError 门控；outcome 三态 + marker set/continue
- prompt.ts 的 elapsed/long-notice hunks（formatLongExecutionNotice 等）归 0412（util 于 0412 定义，源 0411 引用属源序不一致）——0412 重建时落地
- 测试：goal.test +5 持久化用例、httpapi-goal +4 契约用例、prompt.test +2 集成用例（helpers 均存在）、goal-migration.test 按目标 core migration 语义重写（纯 SQLite 验证 ALTER 默认值）

## E/C：E=612 C=96 need>=92 ✓（注释分布于 marker 一次性语义、allowlist 边界、列默认值兼容、migration 幂等、测试意图）

## 验证
- materialize 411 Passed（14 sections splice + dedupe 2 重复）
- typecheck 411 全 workspace passed

---

## 全量审计补充（2026-09-14，第一波双审 G-411-1 扩写）

### Identity
- index: 411；SHA: 7bd1e1560289；parent: 4498da0c374e（0410）
- original: .temp/patches/original/0411-7bd1e1560289.patch（113KB，14 文件）；current: .temp/patches/current/0411-7bd1e1560289.patch（15 文件，含 owner 迁移新增）

### source intent（original 全 hunk）
GOAL 持久化增加 `continueOnError` 策略：终止型 eligible 错误（APIError/UnknownError）后允许 GOAL 续跑。含：① schema（continueOnError 布尔列）② 表列 + migration（ALTER ... DEFAULT false NOT NULL，旧行为不变）③ prompt loop 三态 outcome（break/continue/terminal-error）+ pendingTerminalError 一次性 marker ④ HTTP 透传 ⑤ TUI toggle ⑥ 4 个测试文件（goal/httpapi/prompt/migration）。

### 逐文件映射与落地锚点（state 0452 实证）
1. Goal schema → `packages/schema/src/session-goal.ts:25`（continueOnError 字段 + 中文注释；源在 opencode goal.ts 内联 Schema）
2. 表列 → `packages/core/src/session/goal.sql.ts:32`（源在 packages/opencode/src/session/goal.sql.ts）
3. migration → `packages/core/src/database/migration/20260812100000_goal_continue_on_error.ts:9`（ALTER … DEFAULT false NOT NULL）+ `migration.gen.ts` 注册（源为 drizzle sql 目录 + snapshot.json，目标无此布局 → core TS migration owner 重建）
4. domain → goal.ts SetInput/fromRow/updates/insert 适配目标 Effect-orDie 风格（缺省不改、显式才写）
5. HTTP → groups/handlers GoalSetPayload + continueOnError 透传
6. TUI → packages/tui/src/component/dialog-goal.tsx:42（setGoal 携带）+ tui/context/sync.tsx（goal.reconcile）——源 packages/opencode/src/cli/cmd/tui 重映射
7. prompt loop → state prompt.ts:1596-1599（pendingTerminalError 一次性 marker）、:1612-1620（APIError/UnknownError allowlist）、:1666（(!errorCompletion || goal.continueOnError) 门控）、:2069-2108（terminal-error outcome）；marker 消费即清空（current:787-790）防新 runLoop 残留
8. 源序错位拆分：源 0411 引用 0412 才定义的 formatLongExecutionNotice —— 相关 hunk 归 0412 重建时落地（0411 typecheck 独立通过佐证拆分正确）
9. 测试：goal.test +5 持久化、httpapi-goal +4 契约、prompt.test +2 集成（helpers 在位）、goal-migration.test 按目标 core migration 语义重写（纯 SQLite 验证 ALTER 默认值）

### E/C 精确重算（回应双审 N-02/G-411-2）
- 旧条目 E=612/C=96 系将 docs 散文计入的宽口径；双审独立重算代码注释 ≈83 行。按代码区严格口径 E≈540-570、need≈82-86，C≈83 在阈值 ±1 内；按保守 E=553 计 need=83，C=83 恰达标。两口径均无 demonstrated 硬失败；本节以严格口径 E=553/C=83/need=83 记录（✓ 边缘达标），后续轮次统一 docs 双侧排除口径。

### 验证
- 批次末 typecheck-0415（2026-09-12T04-38-20Z，415/415，30 workspaces passed，install 5100596c 复用）覆盖本项
- 全链 typecheck-0452（17-32-43Z）452/452 + 30/30 passed；行为测试 goal.test 56/56（.test-workspace 248 pass 套件）
- 双审结论：0411 行为为五项中最完整移植（逆向判语），无 B 级发现；本节闭合 G-411-1 record 门禁

### 修订史
- 首次物化 2026-09-12 通过；R14 扩写波及范围外（漏网）；R20（2026-09-14）第一波双审 G-411-1 据此扩写；current 无语义改动。