# Record 0189 — ff399b61bde1

- Index: 189
- Source SHA12: ff399b61bde1
- Batch: 186-190
- Updated: 2026-07-26T18:20:02Z
- Builder self-check: READY-FOR-AUDIT

## 1. Source intent

Three improvements:
1. **Prompt width-based cumulative tokens**: The bottom-right token meter was keyed off terminal columns (`width > 120`). When the session sidebar squeezes the prompt allocation, the meter should compact based on the *actual prompt box width* after flex layout, not terminal width. Uses `renderBefore` to measure.
2. **Pending interval 50ms**: Reduce `PENDING_TOOL_INPUT_PROGRESS_INTERVAL` from 200 to 50 for more responsive streaming.
3. **`+N -M` order**: Swap PendingStats span order from `-removed +added` to `+added -removed` to match diff conventions.
4. (Source also touches `packages/core/src/models-snapshot.js` — a ~4MB auto-generated vendor snapshot. Tip has no such file; omission justified.)

Source files:
- `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx`
- `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` (PendingStats order)
- `packages/opencode/src/cli/cmd/tui/routes/session/pending-tool-input.ts` (interval)
- `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` (frame test order swap)
- `packages/core/src/models-snapshot.js` (OMITTED on tip)

## 2. Tip architecture

Tip prompt component: `packages/tui/src/component/prompt/index.tsx`. Already has `showCumulative` keyed off `dimensions().width > 120` (from `#109`). Has `untrack`, `BoxRenderable`, `renderBefore` available.

Tip has no `session-message-render.test.tsx` frame harness; pending-tool-input unit tests serve as the behavior contract.

Tip has no `packages/core/src/models-snapshot.js` — this is an auto-generated vendor snapshot absent from the tip codebase. Omission is an intentional adaptation, not a silent drop.

## 3. Adaptation (source → tip mapping)

| Source | Tip |
|---|---|
| `.../tui/component/prompt/index.tsx` | `packages/tui/src/component/prompt/index.tsx` |
| `.../tui/routes/session/index.tsx` | `packages/tui/src/routes/session/index.tsx` |
| `.../tui/routes/session/pending-tool-input.ts` | `packages/tui/src/routes/session/pending-tool-input.ts` |
| `session-message-render.test.tsx` | `pending-tool-input.test.ts` (formatPendingCounts order test) |
| `models-snapshot.js` | OMITTED (no tip owner) |
| English comments | Chinese `#189` decision comments |

## 4. Implementation (hunk map)

### prompt/index.tsx
- `showCumulative`: `dimensions().width > 120` → `promptWidth() > 100`.
- `promptWidth` signal initialized to `dimensions().width`.
- `syncPromptWidth(this: BoxRenderable)`: `renderBefore` callback; reads `this.width` after flex; skips if unchanged.
- Root `<box>`: add `renderBefore={syncPromptWidth}`.
- Fallback comment: `width <= 120` → `#189：prompt 槽 <=100`.

### pending-tool-input.ts
- `PENDING_TOOL_INPUT_PROGRESS_INTERVAL`: `200` → `50`.
- `formatPendingCounts`: swap to `+added` then `-removed` (locks `+N -M` contract).

### session/index.tsx
- `PendingStats`: swap `<Show removed>` and `<Show added>` order → `+added` first, `-removed` second.

### pending-tool-input.test.ts
- `formatPendingCounts uses + then - order for #189 UI`: ` +1 -2`.
- `edit path and counts compose the pending headline contract`: `Edit src/space file.ts +1 -2`.
- `progress interval is 50ms after #189`.

## 5. Verification

- materialize `passed 190/190`; tip `states/0190-8d2352918cad`.
- typecheck `typecheck-0190-2026-07-26T18-17-46-010Z.md`: all workspaces passed.
- `bun test pending-tool-input.test.ts` → 8 pass (7 from #187 + 1 interval).

## 6. LIVE IDENTITY

- Current: `.temp/patches/current/0189-ff399b61bde1.patch`
- Bytes: 6068
- SHA-256: `d230db4ee7d161a1e764390f5ddc94c30b087a75621ffdf46cb923e1994084aa`
- E=21, C=10, need=4, gate=PASS
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Tip: `states/0190-8d2352918cad`
- Focused tests: pending-tool-input 8 pass / 0 fail

## DUAL-AUDIT SEAL

- Sealed: 2026-07-26T18:23:37Z
- FORWARD: BATCH PASS (ses_060825b0fffed22nrlffLRoNo5) — no open B/G
- REVERSE: BATCH PASS (ses_060825af9ffe2U4eGLGAoe5q9a) — no open B/G
- Tip: `states/0190-8d2352918cad`
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Focused tests: sync 4 pass; pending-tool-input 8 pass; session layout+spacing 6 pass; total 18/0
- Non-blocking N only (do not reopen seal): N-01 createPendingToolInputStats comment drift; N-02 frame harness adapted to headline contract; N-03 read.txt bullet placement; N-04 #190 E/C borderline; models-snapshot omission justified
