# Record 0315-ebeaf3561ec5 — feat(tests): 使用短会话超时和增加shell测试超时

## Identity
- index/full SHA: 0315 / ebeaf3561ec52ff13d16005fa111b9717187c3e0 (short ebeaf3561ec5)
- manifest parent: cf597333da81（0314 和 0315 之间的 excluded merge commit；cumulative-apply 前驱 = 0314-9b0b83e23bc0）
- subject: feat(tests): 使用短会话超时和增加shell测试超时
- current SHA-256: 81d94b009aa13db6 / 2190 bytes
- status: PASS

## Source intent (original/0315-ebeaf3561ec5.patch, 158 行)
### Hunk 1: `packages/opencode/test/session/prompt.test.ts` (L70-76)
- 新增: `const shortSessionTimeout = process.platform === "win32" ? 15_000 : 3_000`
- 行为目的: Windows CI 冷启动 session+server 链路远比 Linux 慢；给 git 相关 session 测试更宽的超时窗口

### Hunk 2-13: `packages/opencode/test/session/prompt.test.ts` (L1549-2989)
- 12 处 `{ git: true },\n  3_000,` → `{ git: true },\n  shortSessionTimeout,`
- 行为目的: 将 12 个 it.instance 测试的超时从固定 3s 改为平台相关（win32 15s/else 3s）

### Hunk 14: `packages/opencode/test/tool/repo_clone.test.ts` (L208-211)
- 新增: `60_000,` 超时到测试用例
- 行为目的: repo_clone 测试需要更长超时（克隆+checkout 操作）
- **NO-OP（target 无 repo_clone 测试）**

### Hunk 15: `packages/opencode/test/tool/shell.test.ts` (L172-181)
- 新增: `each` 函数加 `timeout?: number` 参数
- 修改: `it.live(...)` 调用加 `timeout` 传递
- 行为目的: shell 集成测试在 Windows 上 spawn 开销大；可按用例传入更宽的超时

### Hunk 16: `packages/opencode/test/tool/shell.test.ts` (L210-211)
- 新增: `each("basic", ...)` 调用加 `60_000` 超时
- 行为目的: shell basic 测试在 Windows CI 上需要 60s 超时（spawn + echo + 断言链路）

## Three-way comparison
- source/SMARK: session 测试用平台相关超时（win32 15s/else 3s）；shell basic 测试加 60s 超时
- 上游 v1.17.20: session 测试固定 3s 超时；shell 测试无显式超时（bun 默认 5s）
- 目标当前: prompt.test.ts 有 2 处 `3_000` 超时（source 有 12 处，目标经 0308/0310 重构后测试用例减少）；shell.test.ts each 函数无 timeout 参数
- 结论: 加 shortSessionTimeout 常量 + 替换目标 2 处 `3_000`；each 加 timeout 参数 + basic 加 60s

## Target adaptation
- prompt.test.ts: L78 后加 `shortSessionTimeout` 常量（win32 15_000/else 3_000）+ 2 行中文注释
  - L2152 `3_000,` → `shortSessionTimeout,`（cancel semantics test，上下文 `expect((yield* status.get(chat.id)).type).toBe("idle")` 后）
  - L2361 `3_000,` → `shortSessionTimeout,`（queue semantics test，上下文 `{ config: cfg },` 后）
  - L1109 `addStepFinish(chat.id, old.assistant.id, 12_000, 3_000)` 不改（函数调用参数，非测试超时，source 也不改）
  - source 有 12 处但目标经 0308/0310 重构后仅 2 处匹配 `{ git: true },\n  3_000,` 模式
- shell.test.ts: L156 each 函数加 `timeout?: number` 参数 + 2 行中文注释 + `it.live(..., timeout)` 传递
  - L224 `each("basic")` 加 `60_000,` 超时
- repo_clone.test.ts: NO-OP（target 无 repo_clone 测试，0309/0310/0311 先例）
- 方法: state 0314 语义编辑（edit315.py patch_window）→ git diff → current/0315

## 逐 hunk 映射
| Source hunk | Source file | Target file | Target line | 状态 |
|-------------|-------------|-------------|-------------|------|
| Hunk 1 (const) | prompt.test.ts L70 | prompt.test.ts L78 | L81 新增 | ✓ 适配 |
| Hunk 2-13 (12× 3_000) | prompt.test.ts L1549-2989 | prompt.test.ts L2152/L2361 | 2 处替换 | ✓ 适配（12→2） |
| Hunk 14 (repo_clone) | repo_clone.test.ts L208 | — | — | NO-OP |
| Hunk 15 (each timeout) | shell.test.ts L172 | shell.test.ts L156 | L156-166 修改 | ✓ 适配 |
| Hunk 16 (basic 60_000) | shell.test.ts L210 | shell.test.ts L213 | L224 新增 | ✓ 适配 |

## Verification
- materialize 315/315: passed（批次末累计 apply 全部通过，从 state 0314 继续）
- typecheck 315（batch-end）: install reused（frozen cbdc173），packages/app TS1128（环境基线），integrityFailure None，source/target HEAD 未变，passed 315/315
- shell basic test（.test-workspace synced to 315）: `bun test ./test/tool/shell.test.ts -t "basic" --timeout 60000` → 4 pass / 0 fail / 8 expect() calls / 16.07s（4 shells × echo test，60s 超时）
- E/C: E=6 / C=4 / need=max(1,ceil(6*0.15))=1 → PASS
  - 注释位置: prompt.test.ts L80-81（Windows CI 冷启动超时原因）、shell.test.ts L159-160（shell spawn 开销超时原因）
- source/target HEAD: 未变（integrityFailure None）
- states 保留 ≤5

## 不一致
- source 有 12 处 `3_000`→`shortSessionTimeout`，目标仅 2 处（0308/0310 重构减少测试用例）——已适配目标实际存在的 2 处，无法替换不存在的模式
- L1109 `addStepFinish(..., 12_000, 3_000)` 不改: source 不改此处（函数参数非测试超时），目标同样不改
- repo_clone.test.ts 60_000 超时: NO-OP（target 无 repo_clone 测试，glob 确认无 repo_clone* 文件）
- manifest parent 为 cf597333（excluded merge between 0314 and 0315），cumulative-apply 前驱为 0314-9b0b83e23bc0

## DUAL-AUDIT SEAL（批次 311-315）
- R1 双审计（记录扩展后：四份记录从 <50L/3000C 扩展至 ≥55L/3787C，0315 manifest parent 更正为 cf597333）：
  - 正向审计（ses_fdefde82fffeDVRJftO2F6brVb）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdefdcbf6ffeU0neKZIAxyCnKO）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（9/2、12/6、13/3、19/8、6/4）、累计 apply 315/315、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：packages/app TS1128 环境基线（custom-elements.d.ts 符号链接物化文本）；truncation cleanup 测试 7天旧文件时间戳依赖；typecheck script packages/app 首失败阻断后续 workspace（tsgo -b 模式覆盖 project references）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
