# Record 0305-3f959c5549fd — feat(tests): 更新 Playwright 安装和测试流程，优化 Windows CI 兼容性

## Identity
- index / full SHA: 0305 / 3f959c5549fd6fbd2425dde4efcef6f846963a2b (short 3f959c5549fd)
- manifest parent: f325360dd994 (index 0304)；`.source-proof.json` originalSha256 = 1875344a…
- subject: feat(tests): 更新 Playwright 安装和测试流程，优化 Windows CI 兼容性
- 类型: CI workflow + 测试基础设施（含新 fixture 语义、preload 增强）
- status: PASS（待批次审计 seal；见文末 Audit trail）

## Source intent（original/0305-3f959c5549fd.patch，逐 hunk）
1. `.github/workflows/test.yml` upstream-e2e-warning job：
   - install-deps 步骤: 注释改为"锁定 CLI"版本 + 新增注释（Windows 上 Bun 执行 .bin shim 会重映射 bin metadata；Node 直接执行 CLI 不触发该边界）+ `run: node ../../node_modules/playwright/cli.js install-deps chromium`
   - install-browsers 步骤: 保留原注释 + 新增注释（继续走锁定的 Node CLI，确保安装和测试阶段使用同一个 Playwright 入口）+ `run: node ../../node_modules/playwright/cli.js install chromium`
   - runE2E 步骤: 新增 `working-directory: packages/app` + 注释（不通过 package script 解析 playwright bin，避免 Windows Bun 再次触发 .bin metadata remap）+ `run: node ../../node_modules/playwright/cli.js test`；保留 id/continue-on-error/env（CI、PLAYWRIGHT_JUNIT_OUTPUT）/timeout-minutes:30
2. `packages/opencode/test/ci/upstream-e2e-workflow.test.ts`（source 在 0303 基础上修改）：
   - 新增 `const runE2E = step(job, "Run upstream app e2e tests")`
   - 两个 playwrightCommand 数组 `"bun"/"../../node_modules/.bin/playwright"` → `"node"/"../../node_modules/playwright/cli.js"`
   - 新增 `expect(playwrightCommand(runE2E)).toEqual(["node", "../../node_modules/playwright/cli.js", "test"])`
   - 注释更新（安装和测试命令必须停留在 packages/app 的依赖边界内，并显式使用 hoisted 后的根 CLI；Windows Bun .bin shim 重映射 bin metadata 边界）
   - 新增 runE2E working-directory 断言 + envField(runE2E, "PLAYWRIGHT_JUNIT_OUTPUT") = e2e/junit-${{ matrix.os_label }}.xml（注释: E2E 入口改为直接执行 CLI 后仍必须保留 JUnit 输出位置，后续汇总步骤依赖此约定）+ `expect(job).not.toContain(".bin/playwright")`
   - 新 helper `envField`（十空格缩进 env 字段正则）
3. `packages/opencode/test/fixture/plugin-deps.ts` 新文件：`markConfigDependenciesInstalled(dir)` = mkdir node_modules + 写 package-lock.json（根 packages deps: @opencode-ai/plugin 0.0.0）——Config 依赖安装的最小认可标记，避免 Windows CI 真实跑 Arborist
4. `packages/opencode/test/installation/install-script.test.ts`：
   - createReleaseAsset 在 tar 后立即校验: `runCommand(archiveCommand(asset,payload), dir)` → code!=0 抛（含输出）→ `!existsSync(asset)` 抛；注释（release asset 是 fake curl 的唯一输入；必须在这里验证 tar 成功，否则 Windows CI 会延迟到 cp cannot stat，掩盖真正的归档失败原因）
   - 新 helper `archiveCommand`：非 win32 = 原 `["tar","-czf",asset,"-C",payload,"opencode"]`；win32 = `[BASH, "-c", "exec env PATH=<bashPath()> tar -czf <bashPathForFile(asset)> -C <bashPathForFile(payload)> opencode"]`（注释: GitHub Windows runner 的安装脚本实际由 Git Bash 执行；归档也走同一 POSIX PATH，让 tar 产物和后续 fake curl/cp 使用一致的 /c/... 路径语义）
5. `packages/opencode/test/preload.ts`：import `markConfigDependenciesInstalled`；XDG env 设置后调用 `await markConfigDependenciesInstalled(path.join(process.env["XDG_CONFIG_HOME"], "opencode"))`（注释: 全局配置目录本轮测试不需要真实安装 @opencode-ai/plugin；只预置依赖完成标记，保留"没有全局 opencode.json/opencode.jsonc"的配置语义，同时避免 Windows full run 等待 npm reify）
6. `packages/opencode/test/provider/provider.test.ts`：移除本地 markPluginDependenciesReady 改用共享 helper；persist 用例两处 + enabled/disabled 用例补标记
7. `packages/opencode/test/server/httpapi-provider.test.ts`：四个 writeProvider* 函数补依赖标记（markLocalPluginDependencies 包装）
8. `packages/opencode/test/tool/repo_clone.test.ts`：waitForContent 轮询 + isolateRepoCache 清缓存

## Target adaptation（current/0305-3f959c5549fd.patch，纯 diff 8345B）
1. test.yml：三处步骤按 source 原样（含保留字段），target 基线与 source pre-image 同构故逐字应用
2. e2e workflow 测试：source 全部断言与 helper 应用在 0303/0304 产出的目标文件上（JSONC 剥离行与已补注释保留），atLeast 注释版本保持 0304 状态
3. install-script.test.ts：createReleaseAsset 校验 + archiveCommand（source 原样；win32 分支复用本文件既有 bashCommand/bashPath/quoteForSh/bashPathForFile 约定）
4. preload.ts：改用 **target 既有 helper** `markPluginDependenciesReady`（`test/fixture/plugin.ts`，契约与 source 的 markConfigDependenciesInstalled 逐字节一致: 同一 mkdir node_modules + package-lock.json 根 deps 写法）——不新建 plugin-deps.ts（避免重复 owner，记录为无操作 + 理由）
5. 无操作项（记录 + 证据）：
   - plugin-deps.ts：既有 fixture/plugin.ts 等义承载（上文）
   - provider.test.ts：target 已 import 自 fixture/plugin；persist 用例 2002/2003 双标记、enabled/disabled 用例 2059 标记均已存在
   - httpapi-provider.test.ts：writeProviderAuthPlugin/writeProviderAuthValidationPlugin/writeFunctionOptionsPlugin/writeProviderModelsMutationPlugin 四处标记已存在（行 109/144/186/218）
   - repo_clone.test.ts：target 无 repo_clone 工具（src/tool/ 无此文件）与测试，waitForContent/isolateRepoCache 无所有者
6. 修改方式：state 0304 语义编辑（test.yml 3 窗口、e2e 4 窗口、install-script 1 窗口、preload 2 窗口）→ fix305.diff（validate_hunks fixed:0）→ `git apply --check` EXIT 0 → 写 current/0305

## 上游对比
- upstream-e2e-warning job/e2e 测试/preload 标记均为 fork 独有（上游无对应能力，直接按 source 语义承载）；test.yml runE2E 步骤的 id/continue-on-error/env/timeout-minutes 保留字段来自 target 基线（source 同样保留），无更强上游实现。

## E/C 计算（count-ec-auditor.py）
- E=44, C=11, need=ceil(44*0.15)=7 → **PASS**（审计方独立复算 C=11~15，均达标）
- C 位置清单（分散）: e2e 测试 4 组中文注释（Node CLI 安装/测试同入口、.bin shim Windows 边界、JUnit 输出约定、runE2E 命令直达 CLI）；test.yml 2 条新注释（锁定 CLI、同入口）；install-script 2 组（tar 失败提前暴露、Git Bash 同 POSIX PATH）；preload 2 行（预置标记避免 npm reify）；envField 说明

## Verification log
1. `git apply --check`（state 0304 干净树）→ EXIT 0；`validate_hunks.py` → fixed: 0
2. `--materialize 305` → `passed 305/305`，state 0305 建立
3. `--typecheck 305`（reports/typecheck-0305… 与 latest）：Install `reused (cbdc173...)`（303 起同一指纹，无依赖变化）；Typecheck `packages/app:failed`（TS1128 已知环境基线）；Source/Target repository unchanged: true
4. 行为验证（.test-workspace/repo）：
   - packages/opencode `bun test test/ci/upstream-e2e-workflow.test.ts test/installation/install-script.test.ts test/tool/external-directory.test.ts` → **13 pass / 0 fail / 56 expects**（含 node cli.js 命令断言、runE2E/envField JUnit 断言、archiveCommand 归档路径）
   - packages/opencode `bun test test/provider/provider.test.ts test/server/httpapi-provider.test.ts` → **112 pass / 1 skip / 0 fail**（preload 全局标记无回归；provider/httpapi 既有标记路径复验）
   - packages/opencode `bun test test/fixture/fixture.test.ts test/config/config.test.ts` → **99 pass / 2 skip / 0 fail**
   - packages/opencode `bun typecheck` → exit 0（$ tsgo --noEmit）
5. current 最终校验: SHA-256 `9D1603400F2FE43C29E8AB896A617A61C686F1536D8C361453FF11CDF38E9CD4`，8345 字节（两名独立 auditor 复核一致）

## 不一致与修订记录
- R0→R1：install-script 窗口初版缺 `}` 与 `// #167` 之间的空行导致 AssertionError → 补空行后重跑成功；test.yml/e2e 窗口曾先行应用后因脚本中途失败需 checkout 还原再整跑（state 只读纪律）
- preload 标记使用既有 helper 名称而非 source 新名——行为契约逐字节一致，属于 owner 合流而非语义损失（记录无操作理由）
- 记录时间线: record 首次创建 2026-08-15 22:40:14，早于 current/0305 最终写入 22:42:01（顺序合规）；本次为审计修复轮扩展

## Audit trail
- 批次 301-305 正向审计 (ses_ffa16f112ffeGzwy9avWTt1Job) 与逆向审计 (ses_ffa16982bffe4zMBMU7Txzfgp9) 判本项 BLOCK（G-03 记录长度/完整性门禁）；本记录已扩展至完整 per-phase 信息。
- 
- 第三轮双审计（2026-08-16）：正向 ses_ff996c159ffemCRlIsJ3sxxISw / 逆向 ses_ff9967147ffe0zYF0lcjWm3yOy —— 0305（两方向均 PASS，无 B/G；E/C 复算一致）
- 批次整体结论：歧义未闭合 → 0302 升级用户裁决，批次 0301-0305 保持 BLOCK（不可继续 0306-0310）；用户裁决后须再启动全新双审计封存。

## R3 后缀重建与 sudo 用例环境性分析（2026-08-21）
- 按用户裁决（不删除、不例外、需修改），0302 执行阻塞修订循环（R3）；本项（0303-0305）current 未变，但后缀 state 在 R3 基础上重建：materialize 305 → passed 305/305（state 0305 reuse 合法：累计哈希未变）；typecheck 305 → install `installed (cbdc173…)`（依赖态回 0305 正确）、packages/app 环境基线；e2e workflow 测试 1/1 pass；recorder 5/5 pass（packages/tui）；install-script 3/4 pass——**sudo 用例在本机（2026-08-21）出现 git-bash 环境性竞态**
- sudo 用例分析（探针矩阵，证据保存在本次会话）：`exec env 'PATH=<POSIX>' /bin/bash ...` 在 Bun.spawn 下偶发 `exec: env: not found`（127）；同形状命令跨进程随机成败（sudo-env 探针 s6 失败 s7 成功、seq4 成功 seq5 失败），独立脚本与部分 bun-test 探针全部成功；隔离逐字复刻（sudo-faithful2，helper 与工作区文件逐字节一致）成功 code=1 且输出正确拒绝文案；**相同代码在本批次记录验证日（08-15/16）4/4 通过**（records 0304/0305 验证段）。结论：非批次代码缺陷（命令构造与 source 逐字一致，探针证明形状可用），系 D:\Program Files\Git git-bash 经 Bun spawn 时对外部命令（env/head）查找的瞬态竞态（环境层），判定为环境性 N；后续批次在新环境复核时注意该用例
- 调试工件（__debug_sudo/__envprobe/__sudobisect*/__sudosenv/__sudoprobe/__sudoseq.test.ts）已从 test workspace 清除
- 
## DUAL-AUDIT SEAL（批次 301-305）
- 第四轮双审计（R3 生命周期 + USER DIRECTIVE 裁决后）：
  - 正向审计（ses_fdff18d59ffe2SzPAwFIf70usY）: 5/5 PASS，无 open B/G（0302 按 USER DIRECTIVE 裁定 PASS：R3 修订循环真实执行、记录先行、内容逐字节收敛）
  - 逆向审计（ses_fdff145e7ffemUVvITnJJ1xDBO）: 5/5 PASS，无 open B/G（0302 同上；N-01 工作区调试工件已由审计清除并复核 opencode typecheck exit 0）
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节（0301 e7a5ff82/3504B、0302 856f135a/9070B、0303 56D7F9B7/9591B、0304 A5E1B736/12947B、0305 9D160340/8345B）、install 指纹（0302=890e205a installed、0305=cbdc173 installed/reuse）、E/C 五项全达标（0/0、62/14、81/15、37/26、44/11）、累计 apply 305/305（R3 链）、states 保留 4 个 <=5、source/target 未改
- 环境性注记（非 B/G）：install-script sudo 用例本机 git-bash 查找竞态（08-15/16 记录验证 4/4；探针矩阵证据在 records/0305）；packages/app TS1128 环境基线
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）


