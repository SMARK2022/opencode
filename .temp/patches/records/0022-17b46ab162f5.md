# Record: Patch 22 — 17b46ab162f5

## 1. Manifest Entry

- **Index**: 22
- **SHA**: 17b46ab162f5c6c6f31a99addc2c8e66dbe59b70
- **Parent**: 0021-b94a79527811
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加 compact 格式化持续时间的功能，优化时间显示

## 2. Source Behavior (original patch)

### 修改文件
- `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx`
  - 添加 `agentLoopStartTime: number | undefined` 到 store 类型和初始化
  - 添加 `createEffect(on(() => status().type, (currentType, prevType) => {...}))` 跟踪 busy 状态开始时间
  - **SMARK 清除逻辑**: 仅在进入 idle 时清除（`currentType === "idle" && prevType !== "idle"`），retry/error 期间保留计时器
  - 添加 `elapsedSeconds` 信号 + `setInterval` 每秒更新 + `onCleanup` 清除
  - 修改 footer JSX：使用 `<box gap={2}>` 包装，添加 `<Show>` 显示已用时间
  - **SMARK footer 颜色**: 使用嵌套 `<span style={{ fg: theme.text }}>` 显示持续时间，外层 `theme.textMuted`

- `packages/opencode/src/util/format.ts`
  - 新增 `formatDurationCompact` 函数

### 行为目的
- **agentLoopStartTime invariant**: 进入 busy 状态时记录 `Date.now()`，仅在进入 idle 时清除
- **prevType 检测**: 使用 `on` 的第二个参数 `prevType` 检测状态转换，确保 retry/error 期间计时器持续运行
- **elapsedSeconds invariant**: 每秒更新 `Math.floor((Date.now() - start) / 1000)`，`start` 为空时归零
- **定时器清理**: `onCleanup(() => clearInterval(interval))` 防止内存泄漏
- **显示规则**: 仅在 `agentLoopStartTime && elapsedSeconds() > 0` 时显示已用时间
- **compact 格式**: 紧凑显示避免占用过多 footer 空间

## 3. 上游 (v1.17.20) 行为

- prompt/index.tsx 的 store 不包含 `agentLoopStartTime` 字段
- footer 只有 esc 按钮显示，无已用时间显示
- format.ts 只有 `formatDuration` 函数（完整格式），无 `formatDurationCompact`
- TUI 路径从 `packages/opencode/src/cli/cmd/tui/` 迁移到 `packages/tui/src/`

## 4. 目标适配 (current patch)

### 路径迁移
- `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx` → `packages/tui/src/component/prompt/index.tsx`
- `packages/opencode/src/cli/cmd/tui/util/format.ts` → `packages/tui/src/util/format.ts`

### V2 适配
- 保留 V2 的 store 结构和 `setStore` API
- 保留 V2 的 `createEffect(on(...))` 模式，使用 `prevType` 参数检测状态转换
- 保留 V2 的 `createSignal` + `onCleanup` 模式
- 保留 V2 的 JSX 结构（`<box>`, `<Show>`, `<text>`, `<span>`）

### 保留的 SMARK 语义
- `agentLoopStartTime` 跟踪逻辑完整保留
- **清除逻辑匹配 SMARK**: `currentType === "idle" && prevType !== "idle"` — 仅在进入 idle 时清除，retry/error 期间保留
- `elapsedSeconds` 信号 + 定时器 + 清理完整保留
- footer 显示逻辑完整保留，包括嵌套 `<span>` 颜色
- `formatDurationCompact` 函数完整保留

### 保留的上游优势
- V2 路径结构和导入规范
- V2 的 store 和 signal API
- V2 的 JSX 组件结构

## 5. E/C 计算

### 有效修改行 E
- prompt/index.tsx:
  - 导入变更: 1 行
  - store 类型: 1 行
  - store 初始化: 1 行
  - timer effect (含 prevType 逻辑): 12 行
  - elapsed signal + effect: 7 行
  - footer JSX: 13 行
  - 小计: ~35 行
- format.ts:
  - `formatDurationCompact` 函数 + 注释: 20 行
  - 小计: ~20 行
- **总 E ≈ 45**

### 合格中文注释 C
1. `// 代理循环计时器：进入 busy 状态时记录开始时间，仅在进入 idle 时清除` — invariant 说明
2. `// 使用 prevType 检测状态转换，确保 retry/error 期间计时器持续运行` — 边界处理
3. `// 每秒更新已用秒数信号，onCleanup 清除定时器避免内存泄漏` — 清理责任
4. `{/* 代理循环已用时间，仅在运行且 >0 时显示 */}` — 显示规则
5. `// 紧凑持续时间格式化，用于 footer 显示代理循环已用时间` — 函数用途
6. `// 60秒以内显示 Ns，3600秒以内显示 NmNs，86400秒以内显示 NhNmNs，超过一天显示 NdNh` — 格式规则
7. `// 分钟级别：显示分+剩余秒` — 分支说明
8. `// 小时级别：无剩余秒时省略秒部分，保持显示简洁` — 显示策略
9. `// 天级别：只显示天+小时，避免过长` — 显示策略

- **总 C = 9**
- **最低 C = max(1, ceil(45 * 0.15)) = 7**
- **9 ≥ 7，通过**

### 注释位置
- timer effect: 注释 1-2 在 `createEffect(on(...))` 之前
- elapsed signal: 注释 3 在 `createSignal` 之前
- footer JSX: 注释 4 在 `<Show>` 之前
- formatDurationCompact: 注释 5-6 在函数之前，注释 7-9 在各分支内部

## 6. 验证

### Apply
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 25 --rebuild`
- 结果: passed 25/25

### Typecheck
- packages/core: passed
- packages/opencode: passed
- packages/tui: passed

### Install 指纹
- `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史

1. **首轮审计 BLOCK (G-02)**: 中文注释比例为 0%
   - 修复: 添加 9 条中文注释

2. **patch 格式错误**: hunk header 行数未更新
   - 修复: 从 state 21 重新生成 patch

3. **二轮审计 BLOCK (B-01)**: 计时器清除逻辑偏离 SMARK——使用 `type !== "busy"` 清除（包括 retry），而 SMARK 使用 `currentType === "idle" && prevType !== "idle"` 仅在进入 idle 时清除
   - 修复: 改为 SMARK 的 `prevType` 逻辑，仅在进入 idle 时清除，retry/error 期间保留计时器
   - 同时修复 footer 颜色: 使用嵌套 `<span style={{ fg: theme.text }}>` 匹配 SMARK

## 8. 最终行为

- `agentLoopStartTime` 在进入 busy 时记录开始时间，仅在进入 idle 时清除
- retry/error 期间计时器持续运行，用户可见已用时间
- `elapsedSeconds` 每秒更新，`onCleanup` 清除定时器
- footer 显示已用时间，使用嵌套 `<span>` 控制颜色（外层 muted，持续时间 text）
- `formatDurationCompact` 提供紧凑持续时间格式
- V2 路径和导入规范保留
- 中文注释比例满足 15% 门禁
