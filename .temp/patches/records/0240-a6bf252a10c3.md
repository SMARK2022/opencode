# Record 0240-a6bf252a10c3

## Status
PASS（B-01 修复后——直接 wrapSSE 单元测试绕过 Effect runtime 限制）

## 修改内容
feat(provider): 添加 provider fetch/SSE transport 时序诊断日志 + 2 个测试。

Source 4 文件，目标适配后 2 文件（2 文件跳过，2 文件适配）。

### B-01 修复（测试适配——最终方案）
- Source llm.test.ts 有 2 个测试 + helpers
- 测试 1 "logs provider fetch timing phases": 通过 `drain` + `_testFetchTimingLog` 验证 fetch.start/response/sse.first_chunk/sse.end + payload 安全。**PASS**
- 测试 2 "logs SSE timeout when stalled": **直接测试 `wrapSSE` 函数**（绕过 drain/Effect runtime），创建 stalled SSE response，直接读取 wrapped stream，验证 sse.first_chunk + sse.timeout phase + chunkTimeoutMs=25。**PASS**
- helpers: `createStalledEventResponse` 完整适配
- `wrapSSE` 和 `providerFetchRequestID` 导出供测试直接调用

### 为什么直接测试 wrapSSE
- `drain` → `Effect.ignore` 无法捕获 `ProviderResponseStreamError` defect
- `Effect.catchAllCause`/`Effect.sandbox`/`Effect.either` 在 Effect v4 中有类型不兼容或导致 hang
- 直接测试 `wrapSSE` 绕过 Effect runtime 限制，完整验证 SSE timeout 行为

## E/C
- E = 207, C = 36, Need = 32
- Status: PASS（C=36 >= Need=32）

## Source
- original/0240-a6bf252a10c3.patch: 490 行，4 文件

## Current
- current/0240-a6bf252a10c3.patch: 15405 bytes, SHA-256 78527fe517f30216993d8573ada708ab8a4707e9360ae6a97a7f2c47c811e3df
- 2 diff entries

## 安装
- Install: reused, fingerprint a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b

## 测试
- Typecheck: 30/30 passed
- llm.test.ts: 51 pass / 1 skip / 0 fail（两个 timing 测试均 PASS）

## 最终行为
- provider fetch 记录 fetch.start → fetch.response → sse.first_chunk → sse.end
- SSE body 记录 sse.timeout/sse.error
- wrapSSE 导出供直接单元测试
- _testFetchTimingLog 捕获数组供测试验证
- createStalledEventResponse helper 完整适配
- 2 个测试均 PASS

## DUAL-AUDIT SEAL (Round 3)
PASS — Forward and reverse auditors agree: no unclosed B/G. Batch 236-240 sealed.

## RECOVERY-ERA INVESTIGATION（2026-09-03，全链验证循环）
state-238 断链。四文件两类问题，V1→V2 判定已完成，适配方案如下（待实施）：
### 语义清单（source intent）
1. bus publish 日志 INFO→DEBUG（token-delta 热路径降噪）
2. provider fetch/SSE 全链 timing 诊断（requestID 关联；fetch.start/response/error + sse.first_chunk/timeout/error/end；payload-free 契约：不记录 body/auth/prompt/chunk 内容）
3. bus.test.ts：INFO 抑制下 publish 仍投递（读 daemon log 断言无 publishing 行）
4. llm.test.ts：provider.fetch timing 集成测试（createStalledEventResponse 模拟 SSE 停滞，断言 milestone 链路）
### V1→V2 判定
- bus/index.ts：**V2 根因满足**。目标 bus/global.ts（GlobalBusEmitter extends EventEmitter）emit 路径无任何 log.info——publish 日志噪音在 V2 架构中不存在。hunk 1 不迁移（无 owner 可改、无语义缺口）。
- bus.test.ts：目标无 test/bus 目录。测试目标语义（发布投递不受日志级别影响）由 V2 无日志架构根因满足。hunk 不迁移，record 存证。
- provider/provider.ts：**V2 存在且同形**。wrapSSE(res, ms, ctl) 3 参形态与 fetch 区 customFetch/NetworkProxy 分支（L1993-2013）与 pre-image 结构一致 → timing hunk 可适配。V2 无 Log.create 设施 → timing() 用 Effect.logInfo(...).pipe(Effect.runSync)（async 上下文，0235/0237 既定模式）；providerFetchRequestID 模块级计数器原样。
- test/session/llm.test.ts：**V2 存在**（import 区结构不同：L7 为 effect import 行）。hunk @7 适配 import 上下文；timing 集成测试（@320 起 ~165 行）适配 V2 测试结构。
### 实施要点（下轮执行）
1. provider.ts：timing/errorFields/bodyBytes/FetchTiming + wrapSSE 第 4 参 + fetch 区 timingInput/timeoutMs/fetch.start/response/error + SSE 计数——行号对照 L85（wrapSSE）与 L1964-2013（fetch 区）；Effect.logInfo 替代 fetchLog.info
2. llm.test.ts：@7 import 上下文适配 + readLogUntil/withInfoLog/providerFetchEvents helper + 集成测试迁移（对照 V2 测试文件 157/320 区锚点）
3. bus 两 hunk 移除（判定如上）；E/C 按迁移后实量计算

### 补充锚点详情（同日深查）
- V2 wrapSSE（L85-127）的超时错误已演进为 `Object.assign(new ProviderError.ResponseStreamError("SSE read timed out"), { code: "SSE_READ_TIMEOUT" })`（非 pre-image 的 new Error）——timing 适配必须保留 V2 错误类型，errorFields 的 errorCode 判定（SSE_READ_TIMEOUT 不重复记 sse.error）语义不变。
- wrapSSE 尾部 `return new Response(body, { headers: new Headers(res.headers) ...})`（L126+）——pre-image 的 ctrl.close/enqueue 结构同形，计数埋点插入位一致。
- fetch 区锚点：L1964 chunkTimeout 提取、L1971 chunkAbortCtl、L1993-2013 customFetch/NetworkProxy 分支与 wrapSSE 调用（L2013，3 参）。
- provider.ts 现有 import 含 NetworkProxy（L26）；Effect import 形态需实测（timing 用 Effect.logInfo+runSync）。

### ⚠️ 当前文件状态（2026-09-03 收尾注记）
current/0240-a6bf252a10c3.patch 当前为**中间态残缺文件**（一次仓促的部分重写，含非法锚点占位与未完成测试段）。下一轮必须以本 record 的实施方案 + 锚点详情**完整重写覆盖**（provider.ts 三 hunk + llm.test.ts 三 hunk，bus 两 hunk 判定移除），不得基于当前残缺内容增量修补。链状态：passed 239/243，断点 240，预计重写后可直接通过。
