# Record 0113 — c48d9fa920c3

## Identity
- Index: **113**
- Source SHA: `c48d9fa920c3…`
- Subject: feat(session): 抽出默认标题 + Go upsell 常量
- Current: `.temp/patches/current/0113-c48d9fa920c3.patch`
- SHA-256: `1d032a5a20a6399a9c0c92e117205f30a832cfd3f472883f4b2cf854e3263fca`
- Bytes: **4919** (original 6272 ≈ 76.5%)
- Why not 100% size: tip TUI no longer imports `@/session/session` or compares `SessionRetry.GO_UPSELL_MESSAGE` string; upsell is action/provider based. Those two original hunks are **semantically relocated**, not dropped: title check lives in `packages/tui/src/util/session.ts`; upsell message remains shared via `retry-constants` for server/retry paths.

## Source intent (all six original files)
1. **title.ts (new)**: `createDefaultTitle`, `isDefaultTitle` with parent/child prefixes + ISO regex
2. **session.ts**: remove inline helpers; import/re-export; create uses `createDefaultTitle(!!parentID)`
3. **retry-constants.ts (new)**: `GO_UPSELL_MESSAGE` with Go URL
4. **retry.ts**: import + re-export message
5. **tui/app.tsx**: import `isDefaultTitle` from title module; terminal title blanking when default
6. **tui/session/index.tsx**: compare status message to `GO_UPSELL_MESSAGE` from constants

## Upstream / tip design
- Server: titles still inline in `session/session.ts` before this patch
- TUI: `packages/tui/src/app.tsx` already uses `isDefaultTitle` from `./util/session` (local regex copy)
- TUI upsell: `goUpsellKeys(action)` by provider/action — **not** string-equal to GO_UPSELL_MESSAGE
- Must not force-delete tip upsell redesign

## Three-way fuse
| Source hunk | Tip owner | Adaptation |
|-------------|-----------|------------|
| title.ts new | packages/opencode/src/session/title.ts | full |
| session extract | session/session.ts | full + createDefaultTitle call |
| retry-constants | session/retry-constants.ts | full |
| retry re-export | session/retry.ts | full |
| app.tsx import title | app already util/session | **rewrite util/session** to same prefixes/regex as title.ts (single algorithm, TUI-safe path) |
| session GO_UPSELL import | tip no string match | keep retry-constants for server; do not reintroduce brittle string upsell detector |

## Critical bug fixed
Broken regex over-escape made `isDefaultTitle(createDefaultTitle()) === false`.  
Current title.ts + util/session use template `\d` correctly (verified true/true).

## E/C
- E=19, C=9 (need ≥3)
- Comments: module purpose; createDefaultTitle; isDefaultTitle; regex note; Session export; upsell centralization; TUI util sync; terminal title behavior

## Verification
- Bun: `isDefaultTitle(createDefaultTitle())` true on both server title.ts and tui util after apply
- Materialize 115 + typecheck: packages/opencode + packages/tui passed
- Files in current: title.ts, retry-constants.ts, session.ts, retry.ts, tui/util/session.ts

## Final behavior
Default titles generated/detected from shared algorithm; TUI terminal title still blanks on default titles; Go upsell copy single-sourced for retry path without undoing tip’s structured upsell UI.

## Dual-audit repair (post tip GO_UPSELL overwrite)
- Restored tip short `GO_UPSELL_MESSAGE` without embedded URL.
- Kept independent `GO_UPSELL_URL` for action.link.
- Current SHA-256: 1d032a5a20a6399a9c0c92e117205f30a832cfd3f472883f4b2cf854e3263fca
- Bytes: 4919

## Independent dual-audit verdict
- FORWARD (111→115): **PASS** — no open B/G
- REVERSE (115→111): **PASS** — no open B/G
- Tip: `states/0115-6e38594c33fc`
- Materialize: `materialize-0115-2026-07-22T19-25-28-236Z`
- Typecheck: `typecheck-0115-2026-07-22T19-26-07-344Z`
- Install: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- Batch release: **PASS**
