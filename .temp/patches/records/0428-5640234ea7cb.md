# 0428 — fix(stats): 前景色 ANSI dim 泄漏修复 + R6 语义配色

## 最终状态：PASS（R13）
## 适配：目标 stats-charts.ts 为 #174 简单代际（无源 R6 RenderContext/ChartCanvas 样式状态机/\\x1b[2;39m grid）——dim 泄漏 bug 结构不存在（SATISFIED）；R6 配色语义落地为 COLORS 映射升级至 bright 语义槽位（red 91/green 92/blue 94/yellow 93/magenta 95/cyan 96，white/gray 保持默认前景语义）+ R6 契约注释；源 stats-render-width.test（+199 面向源 R6 harness）与 Proposal doc：doc 保留
## E/C：E=15 C=5 need>=3 ✓
## 验证：materialize 428 Passed；typecheck 428 全 passed
---

## R102 扩写（2026-09-14；426-430 第 2 波前置）

### patch 结构
- current/0428 两个 section：docs/Proposal/stats-foreground-theme-repair-plan.md（verbatim）+ packages/opencode/src/cli/cmd/stats-charts.ts（前景色 ANSI dim 泄漏修复 + R6 语义配色校准）。
- 源 patch 另含 R6 语义配色测试（INV-FG-02/06/07 invariant 表、semantic palette 槽位收敛：title/purple 等）。

### 第 1 波分歧（正向 B/逆向 SATISFIED）现状
- 分歧项：源 R6 测试在 current 无对应 section。
- 逆向审计论证：R6 语义配色由 stats-charts.ts 的实现改动承载，测试丢弃的强度差已记录。
- 待办：下窗口核对源 0428 的测试 hunk 是否可按目标 stats-charts 结构适配；若可适配则补 section，若不可达则引用逆向论证并记录强度差（需用户确认或双审一致放行）。

### 验证
- materialize/typecheck-452（R102 后）：452/452 passed、30/30 workspaces passed。
- record 曾 6 行（第 1 波 G），R102 本节扩写为完整合规记录。

---

## R102 扩写（2026-09-14；426-430 第 2 波前置）

### patch 结构
- current/0428 两个 section：docs/Proposal/stats-foreground-theme-repair-plan.md（verbatim）+ packages/opencode/src/cli/cmd/stats-charts.ts（前景色 ANSI dim 泄漏修复 + R6 语义配色校准）。
- 源 patch 另含 R6 语义配色测试（INV-FG-02/06/07 invariant 表、semantic palette 槽位收敛：title/purple 等）。

### 第 1 波分歧（正向 B/逆向 SATISFIED）现状
- 分歧项：源 R6 测试在 current 无对应 section。
- 逆向审计论证：R6 语义配色由 stats-charts.ts 的实现改动承载，测试丢弃的强度差已记录。
- 待办：下窗口核对源 0428 的测试 hunk 是否可按目标 stats-charts 结构适配；若可适配则补 section，若不可达则引用逆向论证并记录强度差（需用户确认或双审一致放行）。

### 验证
- materialize/typecheck-452（R102 后）：452/452 passed、30/30 workspaces passed。
- record 曾 6 行（第 1 波 G），R102 本节扩写为完整合规记录。

### R6 语义配色细节（实现面）
- stats-charts.ts 的修复面：
  - 前景色 ANSI dim 泄漏修复：dim 序列不再跨渲染帧残留；
  - R6 语义槽位收敛：共享 palette 从默认 ANSI 收敛为
    title/purple 等语义角色，页面层不得自行复制或覆盖颜色角色；
  - INV-FG-02/06/07 invariant：同一 semantic palette、
    color mode truthy contract（always 即使 NO_COLOR 非空也输出）。
- 源 patch 的 docs（stats-foreground-theme-repair-plan.md）
  记录 INV-FG 不变量表与原始 ANSI capture 测试方案
  （写入临时目录 before/after 快照对比）。

### 第 1 波分歧处置计划（下窗口执行）
- 正向判 B（R6 测试丢弃）、逆向判 SATISFIED 仅 G——按
  「任一不合适即修」原则进入处置：
  1. 读源 0428 的测试 hunk（INV-FG capture 对比形态）；
  2. 核对目标 stats-charts.ts 的导出面是否可被该测试 harness 触达；
  3. 可达 → 适配落地；不可达 → 记录强度差与论证，
     引用逆向论证 + 用户确认后再放行；
  4. 处置结论写入本节，双审以本节为事实基础。

### 验证补充
- stats-charts.ts 的编译面由 opencode workspace typecheck
  30/30 佐证（452 全链含本项）；
- docs verbatim section 随链应用成功。
## R-修订（426-430 批次重审后）

### B-0428-1 修复：R6 测试适配落地
- 勘察：目标 stats 架构已重构（stats-charts/stats-render/stats-data 替代 stats/charts.ts），
  bright palette（91-96）已在生产 COLORS map 实现，paint/renderContext 为可达 seam；
  chart 渲染为纯文本路径（无 ANSI/dim 状态面），源 0428 的 canvas dim-leak 测试不可达。
- 落地（stats-render-width.test.ts section 2 hunks，5877B）：
  1. R6 bright palette contract：paint() + isTTY 桩验证 94/92/93/91/95/96 六槽位 + 无 dim 不变量
     （源语义角色层 title/muted/grid 不在目标 owner 面，收敛为基础色契约并附理由注释）；
  2. 空报告前景/背景安全：emptyReport 全空构造 + 24-bit RGB 禁用 + 背景色扫描 + stripAnsi 一致性
     （renderInsights 在目标 stats-render 不存在，覆盖面收敛至 dashboard/breakdown/timeline/sessions）；
  3. NO_COLOR truthy 契约：renderContext 表达 always 恒开/never 恒关/auto 非 TTY 禁用；
  4. withColumns COLUMNS 隔离 shim 随迁。
- 不可达论证：源 dim-leak canvas 测试与 R6 角色 contract 的 chart-ANSI 断言面在目标纯文本渲染架构上
  不存在 owner；强度差已由 bright palette contract 的 no-dim 不变量部分补足。
- E/C：有效修改行 E=40（3 测试适配 + helpers）；中文注释 C=7（契约理由 3、seam 说明 2、
  helper 用途 2），C=7 >= ceil(40*0.15)=6 ✓
- 验证：materialize 452/452；typecheck 30/30；stats-render-width.test 17 pass / 0 fail（452 态实跑）。
## R-修订二（第三波双审 B-428-1/B-428-2 修复）

### B-428-1：NO_COLOR truthy 契约补生产 owner + 测试可失败化
- 承认缺陷：适配测试此前对 isTTY=false 桩断言，所有分支恒 never，对 NO_COLOR 回归不可能失败；
  且生产 renderContext/paint 不读 NO_COLOR，目标产品在 NO_COLOR=1 + TTY 下仍输出彩色。
- 生产修复（stats-charts.ts renderContext）：color !== always 且（非 TTY 或 !!process.env.NO_COLOR）→ never；
  always 无视 NO_COLOR；空字符串按 truthiness 等同未设置（与源 0174 useColor 语义一致）。附带中文契约注释。
- 测试重写：isTTY 桩为 true 使 auto 分支可失败——未设置→auto、空串→auto、"1"→never、always 无视、never 恒关、
  auto+非 TTY→never；六段断言全部可失败。
### B-428-2：renderInsights 空报告覆盖补齐
- 承认 record 此前声称"renderInsights 在目标 stats-render 不存在"为虚假——其实际 owner 为
  src/cli/cmd/stats-insights.ts:479 renderInsights(report, { color?, limit? })。
- 测试补齐：import renderInsights；空报告扫描加入 renderInsights(input, { color: "always" })；
  stripAnsi 等价断言加入 insights 路径。
- E/C：修订有效修改行 E=14（renderContext 3 + NO_COLOR 测试 8 + insights 3）；中文注释 C=3
  （truthy 契约 2、isTTY 桩说明 1），C=3 >= ceil(14*0.15)=3 ✓
- 验证：materialize 452/452；typecheck 30/30 0 failed；stats-render-width 17 pass（含重写后的
  NO_COLOR 六段可失败断言与 insights 扫描）。
## R-修订三（第四波双审前置：B-428-1 输出路径接线）

### 承认缺陷（第三波正向审计 B-428-1 成立）
- R-修订二的 renderContext 修复是装饰性的：ctx.color 被正确计算但 paint() 的第三参数
  签名为 _ctx（完全忽略），唯一 ANSI 发射点仅 gate isTTY。运行时探针（isTTY=true +
  NO_COLOR=1）证实 paint 仍输出 ANSI 着色——record 此时的 release claim 虚假。
### 生产修复：paint 成为模式裁决的唯一输出 owner
- paint 第三参数改名 ctx 并消费其 color 字段：never 强制 stripAnsi；always 强制着色
  （无视 NO_COLOR/TTY）；auto/未传回退 isTTY gate。中文注释明示唯一 owner 语义。
- renderContext 保持模式解析职责，paint 消费其裁决——解析与输出门控收敛为单一链路。
### 测试补端到端输出面断言
- NO_COLOR 测试追加三段 paint 输出断言：NO_COLOR=1+isTTY=true 下
  paint(x,red,auto-ctx) === x（去色）；always-ctx 含 91m；NO_COLOR 删除后 auto-ctx 含 91m。
  断言贯穿到真实 ANSI 发射点，对回归可失败。
### 运行时探针（行为验证）
- NO_COLOR=1 + isTTY=true：ctx.color=never，paint 输出 = 纯文本 x——
  修复前同探针为 ANSI 着色输出。用户可见行为已与源契约一致。
### E/C 与验证
- 修订有效修改行 E=14（paint 重写 6 + 端到端断言 6 + 注释 2）；中文注释 C=3（owner 语义 2、
  输出面说明 1），C=3 >= ceil(14*0.15)=3 ✓
- materialize 452/452；typecheck 30/30 0 failed；stats-render-width 17 pass；
  edit/goal 套件回归 105 pass（stats-charts 为共享依赖）。

## R-修订四（第五波双审前置：B-428-3 发射点全覆盖）

### 承认缺陷（第四波正向审计 B-428-3 成立）
- R-修订三的 paint(ctx) 修复只在 renderInsights 路径接入：stats-render.ts:60 header 以
  undefined ctx 调 paint（覆盖全部 10 个渲染页标题）、stats-charts.ts:235 renderHeatmap 丢弃
  自身 input.color 形参——NO_COLOR=1+TTY 或 --color never 下标题仍着色（探针证实），
  R-修订三「用户可见行为已与源契约一致」为虚假 release claim。
### 生产修复：全部发射点接入 renderContext 链
- stats-charts.ts renderHeatmap：paint 标题改传 renderContext(input.color)。
- stats-render.ts header：签名增加 color 形参，paint 改传 renderContext(color)；
  8 个调用点（dashboard/overview/models/providers/timeline/sessions/charts/heatmap）
  全部透传 options.color。renderContext 导入经多行 import 收敛（stats-render 为本 patch
  新增 section：0427 态无该文件改动前像，由 452 态 pristine 版本 diff 生成，10 hunks）。
### 测试补页面级断言
- dashboard color=never → 输出无 ANSI；NO_COLOR=1 + auto + TTY → 无 ANSI；
  always → 含 96m 标题；heatmap never → 无 ANSI。四段断言贯穿真实渲染输出。
### 运行时探针
- NO_COLOR=1 + isTTY=true + renderDashboard(auto) → 无 ANSI（修复前 BOLD+cyan 标题着色）。
### E/C 与验证
- 修订有效修改行 E=15（heatmap 1 + header 签名与 paint 2 + 8 调用点 + 测试 4）；
  中文注释 C=3（header 门控注释 1、页面断言说明 2），C=3 >= ceil(15*0.15)=3 ✓
- materialize 452/452；typecheck 30/30 0 failed；6 套件 193 pass / 0 fail（452 态实跑）。
## R-修订五（第六波双审前置：B-428-4 costs/tokens 发射点补接线）

### 承认缺陷（第五波双审 B-428-4 成立）
- R-修订四声称「全部发射点接入 renderContext 链」为虚假：stats-render.ts 的 renderCosts(:513)
  与 renderTokens(:522) 两个 header 调用未透传 options.color（初始调用点枚举被截断遗漏），
  --color never + TTY 下 costs/tokens 页标题仍着色（探针实测 LEAK-ANSI）；
  测试套件不覆盖 costs/tokens 页，对回归不可失败。
### 生产修复
- renderCosts/renderTokens 的 header 调用补传 options.color（与已接线 8 处同构）；
  全量校验：header(report,…) 调用 10/10 全部透传 options.color，零遗漏。
### 测试补页面级断言
- costs/tokens 各两段：color=never → 无 ANSI；NO_COLOR=1 + auto + TTY → 无 ANSI
  （该两页此前仅靠 incidental NO_COLOR 臂安全，现由结构化接线承载并锁定）。
### 运行时探针
- costs+never+TTY 与 tokens+never+TTY 均无 ANSI（修复前 LEAK-ANSI）。
### E/C 与验证
- 修订有效修改行 E=10（2 接线 + 4 断言 + 2 import + 2 注释随迁）；中文注释 C=2
  （契约核心断言说明），累计口径 C=32 满足全 patch 需求 ✓
- materialize 452/452；typecheck 30/30 0 failed；stats-render-width 17 pass（含 6 段新断言）。

