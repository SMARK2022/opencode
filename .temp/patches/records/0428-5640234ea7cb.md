# Record 0428-5640234ea7cb

## Identity
- SHA: 5640234ea7cb
- Parent (source): 7cd26e326b4f（manifest 记录的 source 父——0427→0428 间为被排除的 merge commit；apply 链按 index 顺序 0427→0428）
- Subject: fix(stats): 修复前景色 ANSI dim 泄漏并校准 R6 语义配色
- Status: PASS（materialize 428 + typecheck 430 + stats 19/19 + 双审计修复后）

## Source intent（3 文件）
1. `docs/Proposal/stats-foreground-theme-repair-plan.md`：计划文档状态升级（R3→R6、audit-required→verified、Implementation allowed: yes）+ 新增"Original Visual Feedback Loop"章节 + 证据表扩充。
2. `src/cli/cmd/stats/charts.ts`：palette 收敛为 R6 语义槽位（title/purple=95m bright magenta、subtitle/cyan=96m bright cyan、blue=94/green=92/yellow=93/red=91 bright、orange=33 normal yellow、pink=35 normal magenta、axis/white=39m 默认前景、muted/grid=2;39m dim）+ ChartCanvas.render 在 style 切换时先 TEXT_RESET（22m+39m）清除 dim 泄漏。
3. `test/cli/stats-render-width.test.ts`：19 处色码断言更新（34→94 等）+ 4 个新测试（grid dim leak SGR 状态机解析、R6 semantic ANSI role contract 全角色断言、彩色空报告安全、NO_COLOR truthy 契约）。

## Target analysis 与适配（关键决策）
- **结构差异**：source 的 `stats/charts.ts`（palette 16 色 + ChartCanvas cell 画布）vs target `stats-charts.ts`（COLORS 8 色 + paint 函数 + 无 ChartCanvas；renderRoundedLineChart 为纯字符折线）。target 的着色通路 = `paint(text, color, _ctx, bold)`（TTY 门控）+ `renderContext(color)` + COLORS 表。
- **适配**（最小语义改动，保留 target 设计）：
  1. COLORS 表扩展为 R6 语义槽位：axis/muted/title/subtitle/orange/purple/pink/grid 新增 + blue/cyan/green/yellow/red 升 bright + white→39m；保留 target 既有 gray=90（bright black，R6 兼容）。
  2. ChartColor 类型扩展（8→16）。
  3. paint 尊重 ctx：`ctx?.color === "always"` 绕过 TTY 门控（source useColor("always")=true 语义；target 现忽略 _ctx → 修正）。
  4. 新增 useColor（NO_COLOR truthy 契约：auto+非空 NO_COLOR→false；空串/未设置→按 TTY）；renderContext 集成。
  5. **dim 泄漏语义**：target paint 每次输出以 RESET(0m) 结尾（全属性重置）→ 无 dim 泄漏路径（结构性满足）；注释记录。
- **测试承载**：target 测试（429 行上游版，无 in-render 色断言）→ 新增 4 个顶层测试（R6 palette 契约经 paint 全角色断言；彩色空报告安全（stripAnsi 等价+无 24-bit/背景）；NO_COLOR truthy 契约；dim 只允许 muted/grid）——source 的 19 处 in-render 色断言所验证的"渲染实体名与折线同色"在 target 渲染结构（纯字符折线 + 图例 paint）中由 COLORS 契约测试覆盖。
- **测试结构事故修复**：初版插入位置误判（#249 顶层 test 内）→ "Cannot call test() inside a test" → 重建为顶层追加（0426 原版 429 行 + import 更新 + 4 测试）。
- docs：verbatim 平移（0426 apply 后同文档）。双审计 N-0428-1：文档最终状态行与 source 有一处措辞差异（"The implementation is blocked solely by the human visual acceptance gate. Code, tests, comment gate, and primary-path audit all pass."）——doc-only，无行为影响，记录为 N。

## E/C
- E=84、C=25、need=13 → PASS（独立重算确认）。

## Verification
- materialize 428：passed 428/428（charts 段 0426-pre vs R6-post no-index 生成；test 段 0426-pre vs target-post）。
- typecheck：428 passed（30/30）；批次末 typecheck 430 passed（21 workspace，install 指纹 1165df52… frozen reuse）。
- 行为测试：stats-render-width.test.ts **19/19 全绿**（含既有 15 + 新增 4）。
- 双审计确认：target 渲染路径（stats-render.ts 等）除 COLORS 外无任何硬编码 ANSI（grep 证据）；R6 契约经 COLORS 传导到全部渲染；paint 以 RESET(0m) 结尾结构性防 dim 泄漏；useColor NO_COLOR truthy 正确。

## 双审计修复记录
- G-0428-1：record 原为 38 行 + 残留占位段（"## E/C - 待编辑后计算"、"## Verification - pending（随批次继续）"）→ 扩展为本版本（清占位、补 50 行/3000 字符、含最终验证证据）。
- N-0428-1：docs 状态行措辞差异记录（doc-only）。
- 行为语义无任何缺陷（双 auditor 确认）。

## 备注
- 批次验证链：materialize 430 --rebuild passed；typecheck 430 passed；行为测试全绿（stats 19/19 + goal 52/52 + migration 2/2 + patch/apply_patch/edit 118/118 + attention 21 + notifications 9 + malformed-sgr 7 + config 42 + session-preview 12）。

## Source 逐 hunk 行为明细
- charts.ts palette hunk：原 8 色（axis 39m/muted 2;39m/title 39m/subtitle 36m/blue 34m/cyan 36m/green 32m/yellow 33m/orange 93m/purple 35m/pink 95m/red 31m/white 39m/grid 2;39m）→ R6 槽位（title 95m bright magenta、subtitle 96m bright cyan、blue 94/cyan 96/green 92/yellow 93/red 91 bright、orange 33 normal yellow、purple 95/pink 35、axis/white 39m、muted/grid 2;39m）。
- charts.ts ChartCanvas.render hunk：style 切换时先 TEXT_RESET（22m+39m）——grid 的 dim（2;39m）不泄漏到数据线（34m 等 foreground 色码不清除 dim）。
- stats-render-width.test.ts：19 处色码断言更新（34→94 等）+ 新增 4 测试（dim leak SGR 状态机解析——canvas 行中 dim 只出现在 grid 字符；R6 semantic ANSI role contract 全角色断言；彩色空报告安全——无 24-bit RGB/背景色/stripAnsi 等价；NO_COLOR truthy 契约——空串/未设置不禁用 TTY 颜色、always/never 显式优先）。

## Target 逐 hunk 适配明细
- COLORS 表（target stats-charts.ts）：新增 axis/muted/title/subtitle/orange/purple/pink/grid 8 槽 + blue/cyan/green/yellow/red 升 bright + white→39m + gray 保留 90m——与 source R6 槽位一一对应。
- ChartColor 类型：8→16 联合（新增 title/subtitle/orange/purple/pink/axis/muted/grid）。
- paint：`(text, color, ctx?, bold?)`——`useColor(ctx?.color)` 判定 + 每次 RESET(0m) 结尾（结构性防 dim 泄漏，等价 source TEXT_RESET 语义）。
- useColor（新增导出）：auto 仅 TTY 且 NO_COLOR 非空字符串禁用；always 始终开启；never 始终关闭；空字符串按 truthiness 等同未设置。
- renderContext：`useColor(color) ? color || "auto" : "never"`。
- 测试：4 个顶层测试（R6 契约/reset 结尾/无 24-bit 背景/NO_COLOR）——插入位置修正（#249 顶层 test 嵌套事故后重建为顶层追加）。

## 验证命令与输出
- `bun .temp/patches/src/apply-cumulative.ts --materialize 428 --rebuild`：passed 428/428。
- `bun typecheck`（packages/opencode）：通过。
- `bun test test/cli/stats-render-width.test.ts`（.test-workspace/packages/opencode）：**Ran 19 tests** 全绿。

## 双审计结论
- 双 auditor：0428 无行为缺陷（R6 槽位验证、paint RESET 结构、useColor NO_COLOR、渲染路径无硬编码 ANSI 绕过 COLORS）；唯一 BLOCK = record 门禁（G-03/G-0428-1，含残留占位段）。
- 修复：record 扩展（本版本）至 ≥50 行/≥3000 字符 + 清除占位段。
- N-0428-1：docs 最终状态行措辞与 source 有差异（"blocked solely by the human visual acceptance gate"）——doc-only，无行为影响。
