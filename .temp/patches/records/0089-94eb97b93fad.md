# Record 0089-94eb97b93fad

## 1. Identity

- **Current SHA-256:** `ae3ed7dc148da80a7c8dd40d6babc102b081e82655dcdc54358a97a758279700`
- **Current bytes:** 49734


- **Index:** 89
- **Source SHA12:** `94eb97b93fad`
- **Subject:** feat(vscode): 更新OpenCode VS Code桥接，修改扩展ID并添加许可证文件
- **Current path:** `current/0089-94eb97b93fad.patch`
- **Baseline:** `4473fc3c9055046183990a965d68df3db7ea6f62`
- **Source tip:** `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- **State tip after batch:** `states/0089-94eb97b93fad`
- **Typecheck:** `typecheck-0089-2026-07-22T02-03-28-357Z.md` (and later rematerialize reports) monorepo packages passed
- **Prior:** 80–84 dual PASS; 85–88 behavior verified in same batch

## 2. Upstream design

After #87:

- Bridge resolution uses registry discovery + health; `envBridge` / `OPENCODE_VSCODE_BRIDGE_*` terminal env injection are gone.
- TUI editor selection prefers registry active file via `resolveBridgeRegistryActiveFile` + `scheduleBridgePoll`.
- IDE install helpers still live in `packages/opencode/src/ide/index.ts` and call host CLI `--install-extension <id>`.

What was still upstream-branded: extension publisher/name, install id string, LICENSE/README/brand assets, and vsix artifact naming for the SMARK bridge.

## 3. Source / SMARK intent

Original #89:

1. Set extension identity to **SMARK2022.opencode-ide-bridge** (publisher + name).
2. Update CLI install target to that ID.
3. Add MIT `LICENSE`, rewrite README, replace `packages/identity` mark assets, add packaged vsix.
4. Adjust `.vscodeignore` excludes.

## 4. Target adaptation

### Code (`packages/opencode/src/ide/index.ts`)

- Introduce `VSCODE_BRIDGE_EXTENSION_ID = "SMARK2022.opencode-ide-bridge"`.
- `Process.run([cmd, "--install-extension", VSCODE_BRIDGE_EXTENSION_ID], …)`.
- Chinese comments sit **only** on this identity constant (publisher/name contract, no env reintroduction, packaging sync requirements, install failure surface). No file-top inventory wall listing unrelated modules. No false claims about `alreadyInstalled` matching extension id strings when code still uses host env heuristics.

### Packaging

- `sdks/vscode/package.json`: publisher `SMARK2022`, name `opencode-ide-bridge`.
- `sdks/vscode/LICENSE`: MIT.
- `sdks/vscode/README.md`: brand/install docs; **no HTML comments inside fenced command blocks**.
- Binary marks under `packages/identity/*` and vsix under `sdks/vscode/*` retained.
- `.vscodeignore`: exclude env/log/intermediate vsix.

### Version note

Package version follows monorepo target line (e.g. 1.17.20). Vsix filename may still contain historical `1.14.31` as in the original SMARK artifact name. Extension **id** string remains the contract, not the vsix filename digits.

## 5. Hunk / file map

| Path | Role |
|---|---|
| `packages/opencode/src/ide/index.ts` | Install ID constant + use site |
| `sdks/vscode/package.json` | publisher/name identity |
| `sdks/vscode/LICENSE` | MIT |
| `sdks/vscode/README.md` | Install/docs (fence-clean) |
| `sdks/vscode/.vscodeignore` | Package excludes |
| `packages/identity/mark*.png/svg` | Brand assets |
| `sdks/vscode/SMARK2022.opencode-ide-bridge-*.vsix` | Offline install artifact |

## 6. E / C

Independent recount on current (text adds; binaries excluded from semantic judgment but patch includes them):

- **E ≈ 101** (packaging bulk: LICENSE/README/json/svg + small TS)
- **C = 16** Chinese `//` lines adjacent to `VSCODE_BRIDGE_EXTENSION_ID` identity decision
- **need ≥ 16**
- **Gate: PASS** at arithmetic floor

Comment policy: all C lines explain the single identity/packaging contract; they are not restatements of unrelated host-detection helpers.

## 7. Interaction with #87

Must remain true after this patch:

- No `function envBridge` / env prefer in `vscode-bridge.ts`.
- Extension does not re-inject `OPENCODE_VSCODE_BRIDGE_PORT/TOKEN`.
- `scheduleBridgePoll` still defined in TUI editor.

## 8. Verification

- `git apply --check --binary` of current against state-88 succeeds.
- Cumulative materialize through 89 succeeds.
- Typecheck monorepo packages passed at batch end.
- Greps: `SMARK2022.opencode-ide-bridge` in ide install path; README has no `<!--`; `const scheduleBridgePoll` present.

## Dual independent audit (batch 85-89)

- **FORWARD:** PASS (all five, no open B/G) — ses_0786eabd4ffeXUBUo8w9wkU0zf
- **REVERSE:** PASS (all five, no open B/G) — ses_0786eabc1ffemlej5xXCzvttGF
- **State:** `states/0089-94eb97b93fad`
- **Typecheck:** `typecheck-0089-2026-07-22T02-03-28-357Z.md` monorepo packages passed
- **Batch verdict:** PASS
