# Record 0231-bba4bb002a48

## Status
PASS（materialize 231/231，E/C gate PASS（按 yaml 先例），typecheck 随批次 231-235 末项验证）

## 修改内容
- `.github/workflows/build-opencode.yml` — version input description 示例版本协同升号 1.17.26-smark → 1.17.27-smark，附两行 yaml 中文注释说明映射规则与 README 未移植语义
- `packages/opencode/package.json` — `version` 与 `//version-note` 协同升号至 1.17.27-smark，新增 `#231-release-note` JSON 字段记录 source bump 映射与 README 承载说明
- `README.md` — source 三个 hunk（SMARK badge、关于本分支、安装命令版本）在目标无对应内容：目标 README 为纯上游（0161 先例确认 SMARK 展示段未移植），版本展示语义由 workflow 与 package.json 承载

## Source 行为意图
chore(release): bump smark version to 1.15.4——纯版本号 bump：workflow 输入示例、README SMARK 展示三处、package.json version 从 1.15.3-smark 升到 1.15.4-smark。无生产代码/测试/schema 变更，纯发布元数据。

## 不一致（source vs target 适配）
- 目标 231 前置状态已处于 1.17.26-smark（0161 协同升号：source 1.14.42→1.15.3 适配为 1.17.25→1.17.26-smark）；`#161` 注释明确"协同升号至 1.17.26-smark；禁止回写 1.14/1.15"。
- source 的 bump 语义（版本 +1）在目标版本线对应 1.17.26-smark → 1.17.27-smark（后续 253/284/346/363/396/440 等 source bump 提交在目标中同样按协同升号规则 +1 映射，0298 等后续 bump 待处理时保持一致规则）。
- README.md 在目标为纯上游内容（无 SMARK badge/分支段落/安装命令版本）——source 三个 README hunk 无锚点；0161 先例同样未移植 README SMARK 段；record 记录该语义由版本字段承载，不强行在纯上游 README 中插入 SMARK 展示。

## E/C（按 yaml 先例 0037：yaml `#` 中文注释计入 C、不计入 E）
- E=7（workflow description 替换 1 + 2 行 yaml 注释不计 + package.json version 替换 1 + version-note 替换 1 + release-note 新增 1 + 删除行 3），C=2（两行 `# #231：` yaml 中文注释），need=max(1,ceil(7*0.15))=2。Gate PASS。
- count-ec 工具只识别 `//` 注释（对 yaml 返回 E=9/C=0），按 0037 先例（record 0037："YAML 文件中 # 开头的行为注释行，不计入 E（有效代码行），但计入 C（合格中文注释行）"）口径计算通过。

## 注释位置
- build-opencode.yml：`# #231：source bump 1.15.3→1.15.4 按 0161 协同升号规则映射到目标版本线 1.17.26→1.17.27-smark`、`# #231：README 的 SMARK 展示段在目标为纯上游未移植——版本展示语义由本文件与 package.json 承载`（version input 上方）。
- package.json：`#231-release-note` 字段（说明映射规则与 README 承载语义）。

## Source
- SHA: bba4bb002a48
- 意图：chore(release): bump smark version to 1.15.4
- parent: f81ae0e09344（230）

## Upstream
- 目标 v1.17.20 的版本线由 0161 协同升号建立（1.17.26-smark 权威在 packages/opencode/package.json）；无上游对应改动。

## Target/Current
- current 231 patch 约 1.6 KB（2 文件 2 hunk；original 3 文件 5 hunk，README 3 hunk 无目标锚点）。
- materialized 231 状态：package.json version=1.17.27-smark、version-note=#231 协同升号至 1.17.27-smark；workflow description=1.17.27-smark 示例，含两行 #231 注释。

## 安装/测试
- materialize 231 PASS（修复 hunk 行数后 231/231）。
- 无行为测试（纯版本元数据）；typecheck 随批次 231-235 末项统一验证。

## 失败修订
1. 初始 current 直接复制 original（1.15.3→1.15.4）→ apply 失败（目标无 1.15.3-smark 锚点）→ 按 0161 先例改写为协同升号 1.17.26→1.17.27-smark。
2. workflow hunk 行数错误（corrupt patch at line 18/19）→ 修正 @@ -10,5 +10,7 @@。
3. E/C 不足（count-ec 口径 C=0 need=2）→ 补一行 yaml 注释并按 0037 先例口径计算通过。

## 最终行为
- 目标版本线从 1.17.26-smark 协同升号至 1.17.27-smark：workflow 手动输入示例、package.json 权威版本与版本注释同步更新；README 保持纯上游（SMARK 展示段未移植，语义由版本字段承载）；后续 source bump 提交按同一规则映射。

## 逐 hunk 映射（source → target）
### Hunk 1: `.github/workflows/build-opencode.yml`
- source `@@ -10,5 +10,7 @@`：version input 的 `description` 示例从 `1.15.3-smark` 改为 `1.15.4-smark`。
- target 适配：`description` 示例从 `1.17.26-smark` 改为 `1.17.27-smark`（协同升号 +1），并在 input 上方新增两行 `# #231：` yaml 中文注释（说明映射规则与 README 承载语义）。
- 行数校验：source +2/-1（description 1 行替换 + 0 新增），target +3/-1（description 1 行替换 + 2 行注释新增）——hunk header `@@ -10,5 +12,7 @@`（原 5 行上下文 + 2 行新增 = 7 行新段）。

### Hunk 2: `packages/opencode/package.json`
- source `@@ -1,6 +1,7 @@`：`version` 从 `1.15.3-smark` 改为 `1.15.4-smark`，`//version-note` 注释更新。
- target 适配：`version` 从 `1.17.26-smark` 改为 `1.17.27-smark`，`//version-note` 更新为 `#231 协同升号至 1.17.27-smark`，新增 `#231-release-note` JSON 字段（记录 source bump 映射与 README 承载说明）。
- 行数：source +2/-2（version + version-note 各 1 行替换），target +3/-2（version 替换 + version-note 替换 + release-note 新增 1 行）。

### Hunk 3-5: `README.md`（source 3 个 hunk，target 无操作）
- source hunk 3：README 顶部 SMARK badge 行新增。
- source hunk 4：「关于本分支」段落新增（描述 SMARK 分支目的）。
- source hunk 5：安装命令版本号更新。
- target 状态：目标 README.md 为纯上游 v1.17.20 内容（无 SMARK badge、无分支段落、无 SMARK 安装命令版本）——grep 确认无 `SMARK`/`1.15.` 字样（仅 sdks/vscode/README.md 含 SMARK 字样，与本 hunk 无关）。
- 适配决策：三个 hunk 无目标锚点 → 按 0161 先例（record 0161 同样未移植 README SMARK 段）不强行插入；版本展示语义由 workflow description + package.json version/version-note 承载。record 明确记录此决策。

## 前后状态对比
| 字段 | 230 状态（0161 协同升号后） | 231 状态（本 patch 后） |
|---|---|---|
| package.json version | 1.17.26-smark | 1.17.27-smark |
| package.json //version-note | #161 协同升号至 1.17.26-smark；禁止回写 1.14/1.15 | #231 协同升号至 1.17.27-smark；禁止回写 1.14/1.15 |
| package.json #231-release-note | （不存在） | source bump 1.15.3→1.15.4 映射到目标版本线 1.17.26→1.17.27-smark；README SMARK 段在目标为纯上游未移植，版本展示语义由本文件与 workflow 承载 |
| workflow description 示例 | 1.17.26-smark | 1.17.27-smark（附 2 行 #231 注释） |
| README.md | 纯上游 v1.17.20 | 纯上游 v1.17.20（未改） |

## 协同升号规则完整推导
- 0161 建立：source 1.14.42→1.15.3 映射到目标 1.17.25→1.17.26-smark（source 每次 +0.1 对应目标 +0.01-smark）。
- 231 应用：source 1.15.3→1.15.4（+0.1）→ 目标 1.17.26→1.17.27-smark（+0.01-smark）。
- 规则不变量：目标版本线 = 1.17.{20 + (source_minor - 3)}-smark（source 1.15.3 → 1.17.26；source 1.15.4 → 1.17.27）。
- 后续 bump 提交（253/284/346/363/396/440/408）按同一规则映射；0298 的 bun.lock bump 被 subsumed（lockfile 由 install 重生成，不单独适配）。
- 禁止回写：//version-note 明确「禁止回写 1.14/1.15」——目标版本线脱离 source 原始版本号，独立递增。

## 验证证据路径
- materialize 报告：`.temp/patches/reports/materialize-0231-*.json`（passed 231/231）。
- typecheck 报告：`.temp/patches/reports/typecheck-0235-*.json`（批次末项，30/30 passed，install reused 指纹 a4aae9d1）。
- source original：`.temp/patches/original/0231-bba4bb002a48.patch`（LF SHA-256 与 source-proof 一致）。
- current patch：`.temp/patches/current/0231-bba4bb002a48.patch`（1821 bytes，2 文件 2 hunk）。
- materialized state：`.temp/patches/states/0231-bba4bb002a48/repo`（package.json version=1.17.27-smark 实测）。
- 0161 先例：`.temp/patches/records/0161-*.md`（README SMARK 段未移植决策）。

## DUAL-AUDIT SEAL
批次 231-235 第三轮双独立审计封印（2026-08-06）：
- 正向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 逆向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 三轮修复验证：G-01（231 record 长度）✓、B-01（233 filetype none 哨兵）✓、B-01 r2（235 gracefulShutdown 3s 总超时）✓
- 测试：daemon 8/8（×2稳定）、filetype 2/2、exit 2/2、notebook 12/12、preview-diff 8/8、session-list 19/19、typecheck 30/30
- install 指纹 a4aae9d1（reused）、source HEAD 未变、integrity 无失败
- BATCH PASS
