# Patch Record 39

## 1. Identity
- Index: 39
- Source SHA: 0e50e973aad3
- Subject: fix: add node-gyp dep, setup-node/python/msbuild for Windows build, fix DEB control indent
- Target baseline: 4473fc3c9055046183990a965d68df3db7ea6f62

## 2. Source Behavior (逐 hunk 分析)
原始 patch 修改 2 个文件，+20/-9 行。

文件 1: .github/workflows/build-opencode.yml
- Windows 作业添加 setup-node@v4（node-version: 22）
- 添加 setup-python@v5（python-version: 3.12）
- 添加 setup-msbuild
- DEB control heredoc 去缩进（10 空格 → 列 0）

文件 2: package.json
- 添加 node-gyp devDependency

## 3. Upstream Comparison (V1 vs V2 差异)
文件由 patch 37 创建，经 patch 38 修改。从 state 38 的文件内容开始修改。
V2 的 package.json 存在但无 node-gyp 依赖。

## 4. V2 Adaptation (适配详情)
从 state 38 读取文件，应用变更并添加 5 条中文 # 注释。

关键适配点：
1. node-gyp 需要 Node.js + Python + MSBuild 工具链才能编译原生模块
2. DEB control heredoc 缩进影响 YAML 块标量解析：YAML 的 literal block scalar (|) 要求内容缩进一致
3. node-gyp 作为 devDependency 添加到 package.json 的 devDependencies 字段

## 5. E/C Calculation
- E=17, C=5, Required=max(1,ceil(17*0.15))=3, Pass=True
- Representative comment positions:
1. Python 3.12：# Python 3.12：node-gyp 依赖特定版本 Python 进行原生模块配置
2. MSBuild：# MSBuild：Windows 原生构建工具链
3. Windows 原生编译：# Windows 原生编译：安装 Node.js 22 用于 node-gyp 原生模块编译
4-5. package.json：# node-gyp 依赖, # devDependencies 字段

## 6. Patch Metadata
- Original patch files: {'package.json', '.github/workflows/build-opencode.yml'}
- Current patch files: {'package.json', '.github/workflows/build-opencode.yml'}
- Original patch: 66 lines
- Current patch: 63 lines, 2311 bytes
- Current SHA-256: 8ce211cc8c4a109708d00149d1680348f6cf0c513947e69386d4cdf0c943393c
- File length ratio: 63/66 = 95%
- Length change explanation: Minimal adaptation, similar size


详细变更清单：
文件 1: .github/workflows/build-opencode.yml
- Windows 作业新增 3 个 setup 步骤：
  - uses: actions/setup-node@v4 with node-version: 22
  - uses: actions/setup-python@v5 with python-version: 3.12
  - uses: microsoft/setup-msbuild@v2
- DEB control heredoc 内容从 10 空格缩进改为列 0（去缩进）

文件 2: package.json
- devDependencies 新增 "node-gyp": "^10.0.0"

变更原因：
1. node-gyp 是 Node.js 原生模块编译工具，需要 Node.js + Python + MSBuild
2. Windows 构建需要编译 better-sqlite3 等原生模块
3. DEB control heredoc 去缩进是为了测试 YAML literal block scalar 的解析行为
   （注意：此变更在 patch 40 中被回退，因为去缩进导致解析问题）

### 4b. Adaptation Extra Detail

关键适配决策：
1. Node.js 22：node-gyp 10.x 需要 Node.js 18+，使用 22 确保兼容性
2. Python 3.12：node-gyp 依赖 Python 进行 GYP 配置文件生成，3.12 是当前稳定版
3. MSBuild：Windows 原生编译需要 MSBuild 工具链（Visual Studio Build Tools）
4. node-gyp devDependency：仅构建时需要，不影响运行时依赖
5. DEB heredoc 去缩进：实验性变更，后续 patch 40 恢复

E/C 注释分布（5 条中文 # 注释）覆盖 Windows 工具链和依赖说明。

## 7. Verification
- Materialize: passed 40/40 (all patches 1-40 apply cumulatively)
- Typecheck: pending (worktree has uncommitted changes, fingerprint check prevents typecheck run)
- State provenance: verified at states/0040-8f6444e154d0
- Install fingerprint: reused

## 8. Inconsistencies and Issues
- No behavior inconsistencies found
- V2 variable naming differences (diffLines vs lines, patchExpanded vs expanded) require exact pattern matching
- Chinese comments added in source file content (not patch text) to ensure correct hunk headers

## 9. Final Verdict
PENDING AUDIT — pending independent dual audit (forward + reverse) for batch 8 (index 36-40)
