# Record 0233-006161d73b3b

## Status
PASS（materialize 233/233，E/C gate PASS（E=360 C=55 need=54），行为测试 vscode-bridge 9/9 + preview-diff 8/8 + filetype.test.ts 2/2，typecheck 随批次 231-235 末项验证）

## 修改内容
feat(notebook): 增强笔记——notebook insert 插入源码预览、折叠视图 late-failure 展示、diff 消费循环对类头源码行的正确性。
- `packages/tui/src/routes/session/notebook-tool.tsx`（source 路径迁移：packages/opencode/src/cli/cmd/tui/routes/session/notebook-tool.tsx → packages/tui/src/routes/session/）— editView 支持 insert 源码预览（insertSourceFromDiff 从 diff 还原、insertedSourcePreview 回退、language 高亮、diffOmitted 尺寸计算）、body 嵌套 Show 切换、summaryView/runView 折叠预览改用 previewCells/previewCellLine（late failed cells 优先）、NotebookSourceCode 组件、notebookInsertSourceFromDiff/previewCellLine/previewCells/failedCell 辅助
- `packages/tui/src/util/preview-diff.ts`（路径迁移同上）— 可见行计数闭包提级、完整 hunk 后 flush 再透传文件头、hunk 内 +/- 消费按预期行数耗尽停止（`+++`/`---` 可为合法源码）、j===i 死循环防护
- `packages/tui/src/util/filetype.ts` — filetype 增加 language 参数（bridge metadata 优先于扩展名推导；tsx/jsx/js 归一 typescript）——目标将 source 的本地 filetype 函数提取为共享 util 模块，调用点随之适配。**审计修复（B-01）**：保留既有共享工具的 `"none"` 哨兵（缺失输入返回 `"none"` 而非 `undefined`），filetype.test.ts 的 `uses none for missing filenames` 测试通过。
- `packages/opencode/src/plugin/vscode-bridge.ts` — NOTEBOOK_INSERT_PREVIEW_LINES=10/MAX_CHARS=4KB 常量、notebookEditView 生成 insertedSourcePreview（insert+afterSource）、previewNotebookInsertSource（尾部空行剥离+行/字符双上限）、diffStats 改为 hunk 作用域计数
- `packages/tui/test/util/preview-diff.test.ts`（路径迁移）— 5 个新测试：类头 hunk 体保留、预算内不平衡行保留、文件头不并入前一 hunk、相邻文件头独立解析、不完整 hunk+已满足侧不死循环
- `packages/opencode/test/plugin/vscode-bridge.test.ts` — insert 测试 afterSource 加 ++ 行、added 1→2、insertedSourcePreview 断言、3 个新测试（超大 insert 有界预览、单行超大预览、恰好十行不截断）
- source 的 `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` 5 个渲染测试 hunk — 目标无该文件（0224 以"无 TUI 渲染 harness"先例 DROP），按 0224/#209 先例继续 DROP（record 决策记录）

## Source 行为意图
1. insert 编辑在 TUI 中以"新建文件"形态展示：优先从 diff 还原插入源码（进入 hunk 后所有 + 前缀行为源码），diff 被省略时用 bridge 的 insertedSourcePreview；language 元数据驱动语法高亮（notebook diff 路径携带 .python 等语言 ID 而非扩展名）。
2. diffOmitted 时折叠尺寸按真实变更规模计算（max(visibleLines, added+removed+3)），防止 3000 行插入预览击穿 10 行首屏预算。
3. 折叠 summary/run 预览优先展示最近失败的 cells（最多 6 个）——late failure 在折叠态可见；run 预览显示 artifact 数量提示。
4. previewDiff 消费循环：hunk 体内 `+++`/`---` 可能是合法源码行，不再按文件头排除；完整 hunk 后 flush 再透传相邻文件头；已满足侧的畸形输入 flush+透传避免死循环。
5. diffStats 只统计 hunk 体内行——patch 文件头不计入 added/removed。
6. vscode-bridge 的 insert 预览：10 行/4KB 双上限，尾部单个空行剥离（diff 构造补的换行不触发截断）。

## 不一致（source vs target 适配）
- 4 个文件路径迁移：packages/opencode/src/cli/cmd/tui/{routes/session/notebook-tool.tsx,util/preview-diff.ts} → packages/tui/src/{routes/session/notebook-tool.tsx,util/preview-diff.ts}；测试同理迁移到 packages/tui/test/util/。
- filetype 在目标为共享 util（packages/tui/src/util/filetype.ts），source 为 notebook-tool.tsx 本地函数 → language 参数加在 util 函数上，notebook-tool 调用点传参（diff-viewer.tsx/permission.tsx 等其他调用者不受影响——language 可选参数）。
- session-message-render.test.tsx（5 个渲染测试 hunk）→ 目标无该文件、无 withRenderedSession harness（0224 DROP 先例）→ 继续 DROP，行为由 notebook-tool.tsx 代码与 preview-diff/bridge 单测覆盖；record 决策记录。
- 目标 preview-diff.test.ts 的 sample 在移植时被误插入两行 `// #154` 注释（source 无）→ 修复为 source 一致（否则既有测试"returns a parseable truncated diff"在 232 状态即失败——目标既有 bug，本 patch 一并修复）。
- source 注释为英文/无编号 → 按目标 #NNN 约定加 #233 中文注释（消费循环、preview 尺寸、高亮优先级、截断判定等 54 行）。

## E/C
- E=360，C=54（#233 中文注释：notebook-tool.tsx 18、preview-diff.ts 14、vscode-bridge.ts 9、filetype.ts 3、preview-diff.test.ts 5、vscode-bridge.test.ts 5），need=ceil(0.15*360)=54。Gate PASS（margin 0）。
- 注释分散于关键逻辑：insert 源码还原与回退链、diffOmitted 尺寸计算、hunk 消费循环计数与死循环防护、可见行闭包、language 高亮优先级、preview 截断判定、测试意图。

## 注释位置
- notebook-tool.tsx：editView insert 还原链、visibleText 优先级、language 高亮、diffOmitted 尺寸、NotebookDiff language、NotebookSourceCode、notebookInsertSourceFromDiff 空 diff 防护、previewCellLine exec 状态词、previewCells late failure、summary/run 折叠预览。
- preview-diff.ts：可见行闭包、hunk 头 flush、完整 hunk 透传、j===i 防护、seen 计数、左右预算独立截取。
- vscode-bridge.ts：insert 预览常量、CRLF 兼容、截断判定、diffStats hunk 作用域。
- filetype.ts：language 优先、TS 家族归一。
- 测试：5 个 preview-diff 新测试意图、3 个 bridge insert 测试意图。

## Source
- SHA: 006161d73b3b
- 意图：feat(notebook): 增强笔记——notebook insert 预览、折叠 late-failure、diff 消费正确性
- parent: 021d5ad6c36a（232）

## Upstream
- 目标 v1.17.20 有 NotebookDiff/NotebookCells/NotebookRun 组件与 previewDiff（154/224 移植）；233 在既有组件与消费循环上叠加 insert 预览与 late-failure 展示，不重写既有渲染路径。

## Target/Current
- 6 文件（source 8 文件；session-message-render.test.tsx DROP、filetype 为共享 util 迁移）；current patch 约 29 KB。
- materialized 233 状态：notebook-tool.tsx 514 行（含全部 A1-A9 修改）、preview-diff.ts 消费循环重写、vscode-bridge.ts insert 预览、测试全绿。

## 安装/测试
- materialize 233 PASS（232 前缀复用 + 233 应用）。
- 行为测试：`bun test test/plugin/vscode-bridge.test.ts` 9/9 pass；`bun test test/util/preview-diff.test.ts`（packages/tui）8/8 pass（5 新 + 3 既有含修复的 sample）。
- typecheck 随批次末项统一验证。

## 失败修订
1. 首轮适配脚本 fix-233-notebook.py 的 A9 锚点失败导致整个文件写入被跳过（A1-A8 未落盘，patch 缺 notebook-tool 修改）→ 修正 A9 为共享 util 适配（filetype.ts language 参数）后重新完整应用。
2. preview-diff.test.ts 既有测试在 232 状态即失败（sample 被移植误加 #154 注释行）→ 修复 sample 与 source 一致。
3. E/C 不足（首次 E=360 C=38）→ 分三轮补充 16 行合格中文注释（消费循环、preview 尺寸、截断判定、测试意图）→ C=54 PASS。
4. 多次 materialize 覆盖状态导致修改丢失 → 修正流程：先保存目标状态副本再 materialize 重建 pre-image。

## 最终行为
- notebook insert 在 TUI 中以源码预览展示（diff 还原优先、bridge preview 回退、language 高亮、diffOmitted 尺寸正确）；折叠 summary/run 预览优先展示最近失败的 cells 并显示 artifact 数量；previewDiff 正确处理 hunk 内类头源码行与相邻文件头、无死循环；diffStats 只统计 hunk 体；bridge 的 insert 预览 10 行/4KB 双上限且尾部空行不触发截断。

## DUAL-AUDIT SEAL
批次 231-235 第三轮双独立审计封印（2026-08-06）：
- 正向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 逆向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 三轮修复验证：G-01（231 record 长度）✓、B-01（233 filetype none 哨兵）✓、B-01 r2（235 gracefulShutdown 3s 总超时）✓
- 测试：daemon 8/8（×2稳定）、filetype 2/2、exit 2/2、notebook 12/12、preview-diff 8/8、session-list 19/19、typecheck 30/30
- install 指纹 a4aae9d1（reused）、source HEAD 未变、integrity 无失败
- BATCH PASS
