# Record: 0293-d7f6ce2fcf0a

## 基本信息
- index: 0293
- sha12: d7f6ce2fcf0a
- subject: feat(shell): 增强ShellTool以支持隐藏诊断信息并优化输出处理
- source date: Thu, 11 Jun 2026 00:24:26 +0800

## Source 行为重建
原始 patch（19KB，400 行）修改 5 个文件：
1. **TUI session/index.tsx**：添加 Renderable import；AutoReviewLine 添加 id/onMouseUp/selectable；新增 autoReviewLineID/autoReviewLineTarget 函数；openAutoReviewFromToolChrome 改用 renderable 命中检测
2. **bash-compress.ts**：renderDiagnosticAppendix 签名变更（移除 visibleOutput）；移除 normalizeVisibleText/contextAlreadyVisible；简化 priorityContexts
3. **shell.ts**：tail 返回 hidden 字段；diag→hiddenDiag（收集被驱逐块）；路径解析使用 Shell.ps(shell)；exit notice 不再因 appendix 被抑制；diagnosticSnapshot 在 metadata 中重用
4. **session-message-render.test.tsx**：新增可见诊断测试（目标不存在，跳过）
5. **shell.test.ts**：更新测试名称/断言，新增可见诊断测试

## 三方比较
- **TUI**：路径迁移 packages/opencode/src/cli/cmd/tui/ → packages/tui/src/；目标已有 #292 注释和 openAutoReviewSession 函数
- **bash-compress.ts**：目标 normalizePowerShellOutput 在文档注释和 renderDiagnosticAppendix 之间，需适配
- **shell.ts**：目标有 #282/#289 注释需适配；AppFileSystem→FSUtil
- **session-message-render.test.tsx**：目标不存在，跳过
- **shell.test.ts**：需移除 description 属性

## 适配内容
- **TUI**：路径迁移；适配 #292 上下文；添加 Renderable import + autoReviewLineID/autoReviewLineTarget + box id/onMouseUp + text selectable
- **bash-compress.ts**：适配 normalizePowerShellOutput 位置；移除 visibleOutput/normalizeVisibleText/contextAlreadyVisible；简化 priorityContexts
- **shell.ts**：AppFileSystem→FSUtil；适配 #282/#289 注释；tail 返回 hidden；hiddenDiag 收集；exit notice 不被 appendix 抑制
- **shell.test.ts**：移除 description；更新测试名称/断言
- **跳过 session-message-render.test.tsx**：目标不存在

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（19214 bytes）
- 编辑后 SHA-256: 351eafda0d12d8bd78f2ddc7c9f733e30dcd77c02f527189cebb6adb6f18e772
- 编辑后字节: 18859
- materialize 293: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| TUI Renderable import + AutoReviewLine | packages/tui/src/routes/session/index.tsx | 路径迁移 + #292 上下文适配 |
| TUI autoReviewLineID/autoReviewLineTarget | 同上 | 新增函数 |
| bash-compress.ts renderDiagnosticAppendix | 适配 normalizePowerShellOutput 位置 | 签名变更 |
| shell.ts tail/diag/hiddenDiag | 适配 FSUtil + #282/#289 注释 | 隐藏诊断逻辑 |
| session-message-render.test.tsx | 删除 | 目标不存在 |
| shell.test.ts | 适配行号 + 移除 description | 测试更新 |

## E/C 计算
- E (有效修改行): ~80（TUI 20 + bash-compress 30 + shell 25 + test 5）
- C (中文注释行): 12
  - TUI: autoReviewLineID 说明(2) + autoReviewLineTarget 说明(2) + #293 标记(1)
  - bash-compress: #293 renderDiagnosticAppendix 说明(2) + #293 priorityContexts 说明(2)
  - shell.ts: #293 hiddenDiag 说明(2) + #293 exit notice 说明(1)
- 满足 E > 0 时 C >= max(1, ceil(80 * 0.15)) = 12，12 >= 12 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)

## 不一致
- TUI 文件路径迁移
- session-message-render.test.tsx 不存在
- AppFileSystem→FSUtil
- shell.test.ts description 已移除

## 物化验证
- 待验证

## Source 行为重建（详细）

### Hunk 1: TUI Renderable import (@@ -32,6)
- 引入 `type Renderable` 供 `autoReviewLineTarget` 参数使用；type-only import 无运行时副作用

### Hunk 2: TUI AutoReviewLine box 属性 (@@ -2141,21)
- mouseup 从内层 `<text>` 提升到外层 `<box>`，text `selectable={false}`；防止 OpenTUI 把普通点击解释为文本选择导致事件被消费而不导航
- 新增 `id={autoReviewLineID(props.review)}` 供 renderable 查找

### Hunk 3: TUI autoReviewLineID / autoReviewLineTarget (新增函数)
- `autoReviewLineID`：`auto-review-${review.reviewID}`，仅用于 TUI renderable 查找，不持久化
- `autoReviewLineTarget`：从 target 向上遍历 parent 链 + `findDescendantById` 定位 review row renderable

### Hunk 4: TUI openAutoReviewFromToolChrome 命中检测 (@@ -2179,10)
- 废弃 `parentY + 1` 布局猜测；改用 `autoReviewLineTarget` 查找真实 review row
- char frame 0-based y → renderable screenY 1-based 归一化：`evt.y === row.screenY - 1`
- 旧逻辑依赖标题行在 parentY、review 行在 parentY+1 的假设；BlockTool 相邻行可能是 body/expand，导致误导航

### Hunk 5: bash-compress RenderAppendixOptions 移除 visibleOutput (@@ -199,7)
- `renderDiagnosticAppendix` 不再做可见性去重，移除 `visibleOutput: string` 字段
- 诊断摘录只来自隐藏输出 snapshot，不需要和 visible output 二次比对

### Hunk 6: bash-compress 移除 normalizeVisibleText + contextAlreadyVisible (@@ -616,16 / @@ -2046,28)
- 移除 `normalizeVisibleText`（空白归一化，用于可见性判断）和 `contextAlreadyVisible`（检查诊断上下文是否已在可见输出中）两个函数
- stripAnsi 注释更新：从"判断诊断行是否已出现在可见窗口中"改为"供压缩和诊断渲染使用；诊断候选只来自隐藏输出"
- 可见性去重逻辑完全移除，诊断摘录唯一来源是 hiddenDiag snapshot

### Hunk 7: bash-compress priorityContexts 简化 (@@ -2095,7 / @@ -2114,28)
- 三个 for+filter 循环 → `const priorityContexts = [...firstErrorContexts, ...fatalContexts, ...recentErrorContexts]`
- 所有 snapshot contexts 直接进入去重+限流管线，不再做可见性过滤

### Hunk 8: shell.ts tail 返回 hidden (@@ -320,11 / @@ -345,9)
- 返回类型 `{ text, cut }` → `{ text, cut, hidden }`
- `hidden = visible ? source.slice(0, source.length - visible.length) : source`
- tail 只展示末尾窗口；hidden 是同一 source 中被窗口丢弃的前缀

### Hunk 9: shell.ts PowerShell drive-relative 路径 (@@ -735,7)
- `C:../file` 在 PowerShell 下是 drive-relative（相对于该 drive 的 shell cwd），Node 按进程级 per-drive cwd 解析会得到错误路径
- 新逻辑：PS shell 下移除 `^[A-Za-z]:(?![\\/])` 前缀；绝对路径 `C:/file`、`C:\\file` 不受影响
- 权限扫描以可见命令 cwd 为唯一信任边界

### Hunk 10: shell.ts diag→hiddenDiag (@@ -835,7 / @@ -874,7 / @@ -883,6)
- 重命名 `diag` → `hiddenDiag`；push 时机从「每个进入 chunk」改为「内存窗口驱逐 chunk + tail 截断」
- 旧：`diag.push(chunk)` 在 onChunk 中对每个 chunk 调用（包含最终可见的 chunk）
- 新：`hiddenDiag.push(item.text)` 仅在内存窗口驱逐时调用；`hiddenDiag.push(end.hidden)` 在 tail 截断时调用
- 不变量：hiddenDiag 只包含最终不可见文本，不包含任何出现在模型可见输出中的内容

### Hunk 11: shell.ts diagnosticSnapshot 生命周期后移 (@@ -982,8 / @@ -992,10)
- 旧顺序：`diag.end()` + `snapshot()` → compression → tail
- 新顺序：compression → tail → `hiddenDiag.push(end.hidden)` → `hiddenDiag.end()` + `snapshot()`
- snapshot 在所有 hidden 文本（内存窗口驱逐 + tail 截断）收集完毕后生成

### Hunk 12: shell.ts exit notice 与 appendix 独立 (@@ -1012,29)
- 旧条件 `code !== 0 && !appendix` → 新条件 `code !== 0`；exit notice 和 diagnostic appendix 互相独立，永不互相抑制
- 变量重命名：`appendix` → `diagnosticAppendix`；`visibleOutput` 参数从 `renderDiagnosticAppendix` 调用中移除

### Hunk 13: shell.test.ts 测试更新 (@@ -1907,7 / @@ -1925,7)
- 测试重命名：`"omits exit notice when diagnostics already explain non-empty failure"` → `"adds hidden diagnostics without suppressing non-empty failure exit notice"`
- 断言反转：`not.toContain('reason="exit"')` → `toContain('<opencode_notice ... reason="exit" exit_code="9" />')`
- 新增测试 `"keeps visible diagnostics out of appendix without suppressing exit notice"`：验证可见 fatal 行不生成 `<bash_high_signal_excerpt>` 但 exit notice 仍存在

### Hunk 14: session-message-render.test.tsx
- 目标不存在（TUI 测试文件未迁移），跳过

## 三方比较（详细）

### TUI session/index.tsx
- 路径 `packages/opencode/src/cli/cmd/tui/routes/session/` → `packages/tui/src/routes/session/`（#287 迁移）
- target 已有 #292 注释和 `openAutoReviewSession` 函数；AutoReviewLine 上下文行号偏移
- Renderable import、autoReviewLineID/Target、openAutoReviewFromToolChrome 均可直接适配，无结构冲突

### bash-compress.ts
- target 在 stripAnsi 和 renderDiagnosticAppendix 之间新增 `normalizePowerShellOutput`（#289），hunk 行号需适配
- visibleOutput 移除、normalizeVisibleText/contextAlreadyVisible 移除、priorityContexts 简化均无结构冲突
- target 的 `RenderAppendixOptions` 和 `BashDiagnosticCollector` 结构与 source 一致

### shell.ts
- `AppFileSystem` → `FSUtil`（#282 重命名）；target 已有 #282/#289 注释需保留并添加 #293
- tail hidden 字段、diag→hiddenDiag、snapshot 生命周期、exit notice 条件变更均无结构冲突
- target 的 `normalizePowerShellOutput` 调用位置与 source 一致

### shell.test.ts
- target 已移除 `description` 属性（#283），需从 source hunk 中移除
- 测试名称/断言更新、新增测试均无结构冲突
- target 的 `Truncate.MAX_LINES` 引用与 source 一致

### session-message-render.test.tsx
- 目标不存在，跳过

## 物化验证（详细）
- patch apply：current patch 成功 apply 到 0292 状态 repo，无冲突
- SHA-256：编辑后 351eafda0d12d8bd78f2ddc7c9f733e30dcd77c02f527189cebb6adb6f18e772（18859 bytes）
- typecheck：待批次验证
- 测试运行：待批次验证（shell.test.ts 两个 it.live 测试各 15s 超时）
- install 指纹：08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4（reused，无依赖变更）

## 审计历史

### 第一轮双审计
- 审计 A：PASS（第一轮双审计通过）
- 审计 B：PASS（第一轮双审计通过）
- 仲裁结论：待仲裁

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 291-295，第一轮）
- 正向审计 (ses_011d96b20): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_011d9572e): 5/5 PASS, 无 B/G (N-01~N-02 非阻塞)
- materialize 295: passed 295/295
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)
- E/C: 291=16/8, 292=60/10, 293=80/12, 294=2/1, 295=5/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#215 closeSink finalizer, #289 normalizePowerShellOutput）保留为基线


## R8 修复记录（2026-08-14）
- 背景：0293（hidden diagnostics 重构）shell.ts 9 hunk + test hunk 与 0289/0290 后的 shell.ts 上下文偏移不匹配（多 hunk 偏移量不一致：tail 区 +6、后段 +1）。
- 修复：在 state 292 上语义化应用 0293 全部变更（tail 加 source/hidden、PowerShell drive-relative 路径加固、diag→hiddenDiag 改名、onChunk 不再 push 可见 chunk、hiddenDiag.push(end.hidden)、snapshot 后移、appendix 只从 hidden 生成、exitNotice 不再依赖 appendix），git diff 权威生成 shell.ts/test 段替换。
- test 段：改 "omits exit notice when diagnostics..." 为 "adds hidden diagnostics..."（行为反转）+ 新增 "keeps visible diagnostics out of appendix"。适配 target：run() 选项无 description 字段（source 有）。
- 修复记录：hunk1 需把 #283 注释作为 context；10 个 shell hunk 行号按锚点定位。
- 最终 E/C：E=74, C=37, need=12, PASS
- 验证：materialize 293-300 passed；opencode typecheck exit 0；tui typecheck exit 0；hidden diagnostics 1/1、exit notice 6/6、bash-compress 8/8、truncation 20/20

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `dfac52bdbe35ba2f68ef96e0bfac09e38dbe1cc3c810dc2ce30f8a8ed2083469` / bytes: 19520
- E/C 最终：E=73, C=37, need=11, PASS
- 修复 B-01（双向审计一致命中）：shell.ts resolvePath 的 PowerShell drive-relative 正则 `(?![\/])` 丢失反斜杠成员（source 为 `(?![\\/])`），导致 win32 跨盘符时 `C:\file` 被剥成 `\file` 并经 path.resolve 重定位到错误盘符（权限扫描/Workdir 解析风险）。已恢复 source 排除类 `[\\/]`。
- 验证：materialize 300 后 shell.ts 正则包含 `\\` 成员；exit notice 6/6、hidden/visible diagnostics 1/1、bash-compress 8/8、truncation 20/20、message-v2 38/38、abort 12/12 全 pass；opencode/tui typecheck exit 0。
