# Record 0387-a68cf8a647f9

## Source patch

- Source: `feat(goal): 工具支持 get 模式 + MAX_OBJECTIVE_CHARS 提升至 6400 + 计费错误日志` (SMARK2022, 2026-07-06)
- Original: `.temp/patches/original/0387-a68cf8a647f9.patch` (15,082 bytes, 242 lines, 8 files)
- Replay index: 0387, sha a68cf8a647f931a3e48de1141907ff66376362e9

## Source intent

1. goal 工具 status 参数改为 optional：无参数时返回当前 goal 完整信息
   （objective/status/tokensUsed/tokenBudget/remainingTokens/timeUsedSeconds），
   有参数时保持原 update 行为——合并 Codex get_goal + update_goal 为单工具。
   goal.txt 描述同步重写为 get/update 双模式说明，并新增一条
   "When marking a budgeted goal achieved with status complete, report the final
   token usage from the tool result to the user."
2. MAX_OBJECTIVE_CHARS 从 4000 提升至 6400，"比 Codex 的 4000 字符更宽松，支持
   更复杂的 goal 描述"；goal.ts:112 错误消息已是模板字面量自动传播新值。
3. prompt.ts accountUsage 从 `Effect.ignore` 改为 `Effect.catchDefect` +
   slog.error 记录：accountUsage 返回 `Effect<void>`（error=never），ignore
   不捕获 defect，DB 运行时错误被静默丢失，日志便于诊断 token 计费丢失。
4. 相关测试/注释同步 4000→6400（httpapi-goal、session goal.test、
   dialog-prompt.tsx minHeight 注释、dialog-goal.tsx description 截断注释）。

## State analysis (state 0386)

八个文件在 state-0386 全部存在，迁移情况与偏差：

1. `packages/tui/src/component/dialog-goal.tsx`：0386 移植时 description 注释已改为
   无字数版（"避免超长 objective"），因此源码第一 hunk（4000→6400 注释）在此文件
   无对应位置；但该文件行 12 有目标独有镜像常量
   `const MAX_OBJECTIVE_CHARS = 4000`（客户端预检上限镜像服务端值）——服务端提升后
   客户端预检若不同步会误拦 4001-6400 字节 objective，必须一并提升。
2. `packages/tui/src/ui/dialog-prompt.tsx`：注释存在但目标为 maxHeight=9（源码是
   15），且源码注释第三行 "maxHeight=15 在 24 行终端中..." 在目标不存在——该文件
   只改 "4000 字符"→"6400 字符"，不动 maxHeight 数值。
3. `packages/opencode/src/session/goal.ts`：MAX_OBJECTIVE_CHARS 401 处为
   `#364：goal objective 最大字符数，与 Codex 的 4000 字符限制对齐` 注释，行 93/95
   错误消息用模板字面量 `${MAX_OBJECTIVE_CHARS}`——改常量即自动传播。
4. `prompt.ts`：accountUsage 调用点（行 1890）与源码上下文一致，但目标文件全部
   使用 `Effect.logInfo/logError` 风格，**没有 `slog`**——源码的
   `slog.error(...)` 必须适配为 `Effect.logError(...)`。
5. `tool/goal.ts`：目标已演进为 `Tool.define<typeof Parameters, Record<string, never>, never>`
   泛型签名（源码为无泛型 `Tool.define(...)`），导入顺序也不同（effect 在前、
   Tool 其次、DESCRIPTION 第三、SessionGoal 最后）；get 分支的返回值形状
   `{ title, metadata, output }` 与 update 分支一致，满足 ExecuteResult。
6. `goal.txt` 内容与源码预补丁版本逐字一致。
7. `test/server/httpapi-goal.test.ts`（0380 创建）与 `test/session/goal.test.ts`
   的 4000/4001 断言与源码一致，但目标带 `#364` 前缀注释。

## Port decision

全语义移植（8 文件），三处目标适配：

1. `MAX_OBJECTIVE_CHARS` 提升本体、goal.txt 重写、updateToolAutoReview 无关——
   逐字移植，包括源码的 4 行解释注释与 goal.txt 的 get/update 双模式描述。
2. prompt.ts：catchDefect + `Effect.logError("goal accountUsage defect", {
   defect: String(defect) })`——错误消息保留源码文案，日志 API 适配目标
   `Effect.log*` 约定（源码 slog 在目标不复存在）。
3. tool/goal.ts：get 模式完整移植，含 option 判定 `Option.isNone`、remaining
   计算（`Math.max(0, tokenBudget - tokensUsed)`，tokenBudget==null 时 remaining
   null→"unbounded"）、JSON.stringify 响应结构（goal{objective,status}、
   tokensUsed/tokenBudget/remainingTokens/timeUsedSeconds）；annotate description
   逐字；`import { Effect, Option, Schema } from "effect"` 补 Option。
4. TUI 两文件：dialog-goal.tsx 镜像常量 4000→6400（附 0387 同步说明注释，目标
   独有行）；dialog-prompt.tsx 注释 4000→6400（保留 maxHeight=9 目标现实）。
5. 两测试文件：4001→6401、4000→6400 断言与标题/注释同步。

## E/C gate

`count-ec-auditor.py`: E=57, C=18, need=9 → PASS。C 覆盖：MAX_OBJECTIVE_CHARS
"更宽松" 注释、get/update 双模式 3 行注释、annotate 一致性注释、get 模式
"镜像 Codex get_goal" 注释、catchDefect 2 行诊断注释、镜像常量同步注释、
httpapi/session 测试的越界拒绝注释。

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix387.diff` (13,458 bytes, 8 files, 79+/29-)
- Current patch: `.temp/patches/current/0387-a68cf8a647f9.patch`
- Materialized state: `.temp/patches/states/0387-a68cf8a647f9`

## Materialize

`--materialize 387` → `passed 387/387`，state `0387-a68cf8a647f9` 发布。0386
marker 提交后编辑 8 文件，materialize 零冲突；发布后 8 文件 SHA256 与 scratch
全部一致。

## Verification

- Materialize passed 387/387；发布 state 与 scratch marker 逐文件核对一致。
- Batch-end `--typecheck 390` 覆盖本提交新增代码：
  `Option.isNone` 导入、`goal.tokenBudget`/`tokensUsed`/`timeUsedSeconds` 字段、
  `Schema.optional(...).annotate` 链、catchDefect 回调类型、测试 repeat(6401)
  断言类型，30/30 workspaces PASS（含 packages/opencode、packages/tui）。
- 复核 `goalSvc.set(ctx.sessionID, { status: params.status })` 在 status optional
  分支后的类型收窄：get 分支早 return 后 update 分支 `params.status` 收窄为
  "complete" | "blocked"，`title: `Goal ${params.status}`` 模板无 undefined。

## Notes / deviations from source

- `slog.error` → `Effect.logError` 是必须适配（目标无 slog 命名空间）；语义
  （defect 落日志、不静默吞掉）不变。
- dialog-goal.tsx 镜像常量提升是本提交在目标上的**新增范围**：源码同一位置没有
  该常量（目标 0363 移植引入镜像），不提升会导致客户端预检与服务端不一致。
- dialog-prompt.tsx 仅注释数值变化：目标 maxHeight=9（前序适配值）与源码 15 的
  差异保留，注释更新只反映 4000→6400 的字符量。
- tool/goal.ts `Tool.define<typeof Parameters, ...>` 泛型签名保留——源码的
  define 调用形态不同，但 get-mode 分支的类型形状与 execute 返回对齐无需改签名。
- 其余内容（阈值、nudge、response 结构、测试断言数值）源码逐字。