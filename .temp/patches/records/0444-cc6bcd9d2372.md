# 0444-cc6bcd9d2372 fix(app): 合并单调进度快照

## Identity
- index / full SHA: 0444 / cc6bcd9d23728e4e74ea5e61c66336c0faf1b1bc（manifest parent = 0443 6d90ba6c7afa）
- subject: fix(app): 合并单调进度快照
- original: 14991 bytes / 300 行 / 5 文件
- current: 与 original 逐字节一致（未适配）
- status: 预研完成——4/5 低风险，hunk5 需决策（Record 先于适配编辑建立——合规时序）

## Source Intent
1. **event-reducer.ts**：import mergePartSnapshot；case 增加 message.part.progress fallthrough；tool Part 经 mergePartSnapshot 后再 reconcile——消费 0443 产物的客户端合并语义。
2. **part-merge.ts 新建 50 行**：progressVersion 归一化（缺失/NaN/负→v0）；mergePartSnapshot（无本地→接受；非 tool/不同 ID→incoming；incoming terminal 直接胜；已有 terminal 挡 running；bash+running 严格递增）；mergePartSnapshots page 合并。
3. **event-reducer.test.ts**：shellPart fixture + reducer 级单调测试（SSE 与 HTTP 两种 transport 到达顺序结果一致；terminal 权威）。
4. **part-merge.test.ts 新建**：legacy 首次接受、非法 version 归一、terminal 双到序支配、HTTP page 按 ID 复用单 Part 合同。
5. **app/src/context/sync.tsx**：loadMessages HTTP page 的 parts 经 mergePartSnapshots 共享合并后再写 store。

## 0440 state 锚点状态
| 文件 | 状态 |
|---|---|
| event-reducer.test.ts | ✅ textPart L28（漂移-6）、store.part 断言 L465（+41）、baseState/push/loadLsp helper 全在 → 可应用 |
| event-reducer.ts | ✅ SKIP_PARTS L19/L283、case message.part.updated L262、reconcile L314 与源基线逐字一致（仅行漂移）→ 可应用 |
| part-merge.ts / part-merge.test.ts | ✅ 新文件无冲突；@opencode-ai/sdk/v2/client 导出存在 |
| app/src/context/sync.tsx | ❌ 仅 98 行，无 reconcile/next.part/filtered/setStore("part")——HTTP page 合并点在 0440 app 架构中无对应落点 |

## 适配策略
1. event-reducer.ts / event-reducer.test.ts / part-merge.ts / part-merge.test.ts 四处按 original 近净应用（行漂移由 git apply 容差处理）。
2. hunk5（app/src/context/sync.tsx）：0440 的 app 架构中 part store 唯一写入方是 event-reducer.ts，HTTP page 路径没有独立 sync.tsx 合并点。处置选项：
   a. 找到 0440 中 HTTP page parts 写 store 的实际位置（event-reducer 的 loadMessages 路径或 app/store 路由）作为 mergePartSnapshots 的落点；
   b. 若 0440 架构中 HTTP page 加载不与 live progress 竞争（progress 只走 SSE/reducer），则 hunk5 语义已被结构消除——按 superseded 记录并在测试中验证。
   预研倾向先验证 b：grep app 中 part store 的全部写入点，确认 HTTP page 路径是否经过 event-reducer。

## 与 0443 的依赖
- part-merge/mergePartSnapshot 消费的 progress 事件由 0443 产出（客户端为字符串类型无硬编译依赖，但语义链依赖 0443 先行）。
- 必须在 0443 落位后 materialize。

## E/C 预估
part-merge.ts 新建 50 行 + reducer 修改；适配后以 count-ec-auditor 复算为准。

## 验证计划
- materialize 444。
- 行为测试：event-reducer.test + part-merge.test 全套。

## Status
预研完成，适配进行中（本 record 建立于适配编辑之前——合规时序）

## part-merge.ts 合并规则详表
- 无本地 snapshot → 接受 incoming。
- 非 tool part 或不同 messageID → incoming 直接替换。
- incoming terminal → 直接胜出。
- 本地 terminal + incoming running → 本地保留（terminal 权威）。
- 同为 running（bash+running）→ 严格递增 version 才接受。

## 与 0443 的客户端消费链
- 0443 的 publishPartProgress 走 Bus（message.part.progress），app 端 event-reducer 的 case fallthrough 消费之。
- HTTP page（loadMessages）与 live SSE 两条到达路径在 reducer 汇合后由 mergePartSnapshot 保证单调。
- part-merge.test 的四种到达顺序用例锁定该不变量。

## 测试矩阵明细
- reducer 单调：SSE 与 HTTP 到达顺序一致（terminal 权威）。
- part-merge：legacy 接受、v0 归一、terminal 双到序、page 按 ID 复用。
- 全部用例为纯 reducer 级，无 I/O，运行成本极低。

## hunk5 的验证计划
- 在 event-reducer 单调测试通过后，用 HTTP page 加载路径的集成测试（若存在）验证 page 合并；若 0440 无该路径测试宿主，record 记录为结构性消除。
- part store 写入点唯一性已由 grep 确认（event-reducer.ts 为唯一 part store 写入方）。

## Round-3 闭合验证（最终）

- **current/0444**：12911B、5→4 文件（sync.tsx hunk5 superseded——state app 架构中 part store 唯一写入方为 event-reducer.ts，grep 全 src 证实 setStore("part") 仅一处；HTTP page 合并点不存在，版本合同已由 event-reducer 的 progress fallthrough + mergePartSnapshot 统一承担），LF 无 CR。
- materialize 444：passed 444/444（在完整 0443 patch 之后累计应用）。
- typecheck 444：全包 passed；install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad。
- E/C：additions=195、blank=16、import-only=5、注释=31 → E=143、C=31 ≥ ceil(143×0.15)=22（21.7%）PASS。注释分布：part-merge 归一化/无候选接受/ID 边界/terminal 支配、event-reducer fallthrough/merge owner、测试断言处。
- source 4 hunks 映射：H1 shellPart fixture+monotonic 测试 ✓ 原样落位；H2 import+fallthrough+tool merge ✓ 原样；H3/H4 part-merge 新文件 ✓ 原样；H5 sync.tsx 合并 superseded（证据入档）。
- 与 0443 的关系：app 文件在 0443 前后一致（0443 不触 app/*），442 基线提取的 diff 在 443 后等价 apply ✓ 已由 materialize 444 实证。

## Round-4 修订（2026-09-16：owner 移植落盘）
- **事实核清**：server-session.ts = TARGET BASELINE 既有文件（git cat-file 4473fc3c 证实，含 reconcile/
  applyMessagePage）。Round-3 的"event-reducer 唯一 part-store 写入方"结论 = 在被 retention 清理删除中的
  state 上 grep 所得的伪象（字母序局部删除），已废弃。审计的 owner 裁定正确。
- **移植落盘**（source H5 语义在真实 owner 完成，替代 superseded 判定）：
  - 新增 section packages/app/src/context/server-session.ts 两 hunk：
    1. @@ -17,1 +17,2 import mergePartSnapshots from "./global-sync/part-merge"（0444 同 patch 新建模块，
       Part 类型同源 @opencode-ai/sdk/v2/client）。
    2. @@ -519,3 +519,4 replaceParts 的 fetched 计算先过 mergePartSnapshots(data.part[item.id], page parts)，
       中文注释说明防 page-load 回退 invariant。replaceParts 唯一调用方 = applyMessagePage(:584) = HTTP page
       合并点 ✓ 与 source sync.tsx loadMessages 的 page 合并语义同构。
  - E/C：E=2（import 行 + fetched 改写行）、C=1（防回退注释）≥ max(1, ceil(2×0.15))=1 ✓。
- **验证**：materialize 444 passed 444/444（修复补尾换行后）；typecheck 444 全部 workspace 通过（0 failed）。
  修订轮次：首版缺尾换行 → normalized corrupt at :281 → 补 \n 后通过。
- **待闭合项（下窗口）**：server-session.test.ts 集成测试（page 携带 stale running tool part 不回退 live
  progressVersion）——fixture 需新增 toolPart helper（SDK v2 ToolPart running state 形状待对照 typecheck），
  复用"preserves same-ID live updates over the initial page"的 pending/response/apply 模式。
- **provenance 注意**：本轮 typecheck 报告 source mirror HEAD 于运行中被外部移动（4a2c9862→2bb2e110，
  非本会话操作）；target 指纹 df89e331 前后一致 ✓。批次双审前须复检 source 稳定。

## Round-4 测试闭合（2026-09-16）
- **集成测试落盘**：current/0444 追加 server-session.test.ts section（3 hunk）：
  1. @@ -3,1 +3,1 import type 增加 ToolPart。
  2. @@ -59,3 +59,18 新增 toolPart fixture（ToolStateRunning + metadata.progressVersion）。
  3. @@ -446,3 +446,19 新增测试"does not regress a running tool part when a stale initial page arrives"：
     live running part v3 先入 store，initial page 携带同 ID stale running v1，断言 store 保留 v3
     （mergePartSnapshot running 严格递增 → local 胜）；中文注释锁定单调合同。
  - 修订轮次：v1 测试体漏 + 前缀（全为 ctx）→ corrupt；v2 两个空行写成裸空行 + header 20≠实际 19 +
    尾部多余空行 → corrupt at :328；fix-r319 三处齐修 → passed。
- **验证**：materialize 444 passed；typecheck 444 全 workspace 通过；bun test src/context/server-session.test.ts
  （packages/app，.test-workspace）= **69 pass / 0 fail**（含新测试）；materialize 452 全链 passed 452/452。
- **最终 E/C**：E=203（Round-3 的 143 + server-session 生产 2 + 测试 import 1 + helper 14 + 测试体 15 +
  空行归整），C=34（Round-3 的 31 + 防回退注释 1 + 测试单调合同注释 1 + part-merge/迁移既有注释复核）
  ≥ ceil(203×0.15)=31 ✓ PASS。注释分布：part-merge 归一化/接受/边界/支配、reducer fallthrough、
  server-session 防回退、测试单调合同处。
- 0444 全部审计 B 项闭合：owner 移植（生产+测试）完成，source H5 语义在真实 owner server-session.ts 承载。

## Round-5 修订（2026-09-16：事件路径合并，逆向审计 B-01 闭合）
- 逆向审计 reachable 证据：UI 的 data.part 经 directory-sync Proxy 路由到 server-session store；其
  message.part.updated 分支（baseline :846-:903）无条件 reconcile 覆盖 = 后到 stale durable 快照回退
  live running 进度（正是 source 修复的缺陷）；0443 的 50ms progress 通道在 app 侧无消费者。
- 修复（evtSection，@@ -846,1 +846,3 + @@ -894,3 +894,5）：
  1. case "message.part.progress" fallthrough 至 updated 分支（同一 Part 快照双通道合并写入，注释锁定）；
  2. part.updated/progress 写入前 mergePartSnapshot(parts[found], part)——与 replaceParts page 路径共用
     单一单调合同（import 扩展为 { mergePartSnapshot, mergePartSnapshots }）。
- 测试：hunk3 重建为组合 hunk（@@ -448,1 +448,28，唯一锚点无空行 ctx）：page stale 测试 + 新增 event-path
  stale 测试（v3 live 后到 v1 updated → store 保留 v3）。
- 修订轮次：evtSection 尾部空行 corrupt :290 → 移除；hunkA header +846,4 实为 3 → 修正；hunk3 空行 ctx
  遇 git apply 严格解析 + 有限搜索窗口 → 改唯一锚点。
- 验证：materialize 444 passed；typecheck 444 全 workspace；bun test server-session.test.ts = **70 pass/
  0 fail**；materialize 452 全链 passed。
- 最终 E/C：E≈233、C=37 ≥ ceil(233×0.15)=35 ✓（新增注释 3：fallthrough 双通道、防回退、事件路径单调合同）。
