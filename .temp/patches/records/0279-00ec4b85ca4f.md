# Record 0279-00ec4b85ca4f

## Patch Identity
- Index: 0279
- SHA12: 00ec4b85ca4f
- Subject: fix(tui): 降低上下文面板与选择框渲染开销
- Current SHA-256: 9e9469217afc3d1a8f0fab25ac3533958a0722e70cef95c09a2fc8f5439f6e47
- Current bytes: 34594
- Files: context-usage.tsx + dialog-model.tsx + dialog-select.tsx + context-usage.test.ts + dialog-select.test.tsx（新增）

## Source Intent
Source patch 的完整行为目的见 original/0279-00ec4b85ca4f.patch。

## Adaptation Details
- 276: image.test.ts 添加 tokenBudget 测试；read.test.ts 适配 LayerNode 模式（Image.node 代替 Image.defaultLayer），添加有效 JPEG base64 和损坏图片测试
- 277: image.ts 添加 tokenBudget 支持（保留 photon，添加 targetMaxBase64Bytes 辅助函数）；read.ts 收敛图片处理到 Image.Service（替换 processImageWithTokenBudget），添加 Image.Service 到 Tool.define context type；registry.ts 添加 Image.node 到 deps
- 278: opencode.jsonc 适配 references 块上下文（hunk header 从 -2 改为 -14）；session-ses_1837.md 新文件
- 279: context-usage.tsx 添加 contextUsageRefreshKey 函数（轻量刷新信号）；dialog-model.tsx 添加 currentModelKey memo 和字符串 key
- 280: subagent-permissions.ts 重写 deriveSubagentSessionPermission（PermissionV1 类型，parentAgentCeiling/forwardParentSessionRule/childSpecificCeilings/subagentDenyOverrides 辅助函数）；reviewer/service.ts 简化 reviewerEnabled

## E/C Calculation
- E=28 C=5 need=5 PASS

## Verification
- materialize 280: passed 280/280
- opencode typecheck: exit 0
- tui typecheck: exit 0
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: reused


## Source Behavior Analysis
源 patch 降低 /context 面板渲染开销：
1. context-usage.tsx: 新增 contextUsageRefreshKey 函数（轻量刷新信号）
2. dialog-model.tsx: 添加 currentModelKey memo，key 从对象改为字符串
3. dialog-select.tsx: 添加 currentKey prop 支持
4. 测试文件

## Upstream Comparison
目标路径从 packages/opencode/src/cli/cmd/tui/ 迁移到 packages/tui/src/。
目标 dialog-select.tsx 结构大幅演进（807 行 vs 源约 500 行）。
目标 context-usage.tsx 使用相对导入而非 @tui/ 别名。

## Target Adaptation
1. context-usage.tsx: 添加 contextUsageRefreshKey 函数（简化版，返回 join 的字符串数组）
2. dialog-model.tsx: 添加 currentModelKey memo 和字符串 key

## Hunk Mapping
| Source File | Target File | Adaptation |
|---|---|---|
| context-usage.tsx | tui/src/routes/session/context-usage.tsx | contextUsageRefreshKey 函数 + inputBreakdown 全字段 key |
| dialog-model.tsx | tui/src/component/dialog-model.tsx | currentModelKey memo + 字符串 key |
| dialog-select.tsx | tui/src/ui/dialog-select.tsx | identity key 热路径（primitiveKey/optionIdentityKey/currentIdentityKey/flatRows/rowsByOption） |
| context-usage.test.ts | tui/test/context-usage.test.ts | refresh key 测试 + toJSON 守卫 + inputBreakdown 变更测试 |
| dialog-select.test.tsx | tui/test/cli/tui/dialog-select.test.tsx | 3 个用例适配 target harness（TestTuiContexts + testRender） |

## Comment Positions
1. #279：/context 面板的低成本刷新信号（context-usage.tsx）
2. #279：避免流式增量触发大对象 JSON roundtrip（context-usage.tsx）
3. #279：模型选项使用稳定的 provider/model identity key（dialog-model.tsx）
4. #279：避免 DialogSelect 为高亮当前模型反复做对象深比较（dialog-model.tsx）
5. #279：使用字符串 key 代替对象，减少深比较（dialog-model.tsx）

## Failure Revision History
- 第一次 typecheck：dialog-model.tsx TS1127（模板字符串被 Python 脚本转义损坏）
- 修复：手动修复模板字符串

## Final Behavior
/context 面板使用轻量 refresh key 避免流式增量触发大对象 JSON roundtrip；DialogModel 使用字符串 key 减少深比较。

## 第二轮修复（第一轮双审计 BLOCK 反馈）

### B-01/B-02: contextUsageRefreshKey 死代码+简化——已修复
- 完整实现 contextUsageRefreshKey（使用 refreshJson 序列化内容而非 .length）
- 接入 ContextUsagePanel：buildInput + inputSource + inputKey + untrack(buildInput) 替换 inputRaw/setInput

### B-03: currentModelKey 未传递——已修复
- 添加 currentKey={currentModelKey()} 到 DialogSelect 调用

### B-04: dialog-select.tsx 优化丢弃——已修复
- 添加 currentKey prop、key field、DialogSelectRow 类型
- 添加 primitiveKey/optionIdentityKey/currentIdentityKey 辅助函数
- 添加 flatRows/rowsByOption/currentIndex memo
- 替换 isDeepEqual 为 identity key 比较

### B-05: provider options 缺少 key——已修复
- 添加 key: \/\ 到 provider options

### B-06: 测试丢弃——已修复
- 添加 contextUsageRefreshKey 测试到 context-usage.test.ts

### E/C 修复后
- E=135 C=24 need=21 PASS

### 验证
- materialize 280: passed 280/280
- opencode typecheck exit 0, tui typecheck exit 0

## 第三轮修复（第二轮双审计 BLOCK 反馈）

### B-01: refreshJson 使用完整 JSON.stringify——已修复
- 替换为按字段提取的 key 函数：partRefreshKey/messageRefreshKey/providerRefreshKey/configRefreshKey/agentRefreshKey/tokenRefreshKey
- 使用 .length 和 refreshJsonLength 代替完整序列化
- 保留 refreshJson 仅用于小对象（vcs、attachments）

### B-03: dialog-select isDeepEqual 热路径未替换——已修复
- current memo 使用 identity key 代替 isDeepEqual
- onMouseOver/onMouseDown 使用 rowsByOption().get(option)?.index 代替 findIndex + isDeepEqual
- scroll-boundary 使用 store.selected === 0 代替 isDeepEqual

### B-02: 测试——部分修复
- context-usage.test.ts 添加了 refresh key 测试（验证不同 raw 内容产生不同 key）
- dialog-select.test.tsx 未创建（目标测试 harness 结构不同，需大量适配）

### E/C 修复后
- E=212 C=32 need=32 PASS

### 验证
- materialize 280: passed 280/280
- tui typecheck exit 0, opencode typecheck exit 0

## 第四轮修复（第三轮双审计正向 BLOCK 反馈）

### B-01: 延迟滚动效果使用过时 isDeepEqual——已修复
- 替换 on([() => store.filter, () => props.current], ...) 为 on([() => store.filter, () => currentIndex()], ...)
- 移除 setTimeout 内的旧 filter/current 捕获，改用 store.filter.length 和 currentIndex() 实时读取
- 添加中文注释说明滚动目标必须在 callback 执行时重新解析

### E/C 修复后
- E=219 C=33 need=33 PASS

### 验证
- materialize 280: passed 280/280
- tui typecheck exit 0, opencode typecheck exit 0

## 第五轮修复（第四轮双审计正向 BLOCK 反馈）

### B-01: 旧 isDeepEqual current sync effect 未移除——已修复
- 移除 createEffect(on(() => props.current, (current) => { ... isDeepEqual ... })) 旧效果
- 新的 currentIndex() memo + createEffect(on(() => currentIndex(), ...)) 已替代其功能
- 消除 isDeepEqual 热路径的重复执行

### 验证
- materialize 280: passed 280/280
- tui typecheck exit 0, opencode typecheck exit 0

## 第六轮修复（批次 276-280 重审 B-02/B-03）

### B-03: refresh key 对 inputBreakdown 只跟踪 JSON 长度——已修复
- 现象：messageRefreshKey 用 `refreshJsonLength(m.inputBreakdown)`（仅长度），step-start/step-finish 完全省略 inputBreakdown；/context 面板分类条直接消费 inputBreakdown 数值（context-usage.ts util 1261 行），数值变化但 JSON 长度不变时 key 不变 → 面板显示陈旧分类。
- 修复：新增 `inputBreakdownRefreshKey` 全字段函数（system/instructions/skills/tools/messages.*/attachments，逐字段 join），messageRefreshKey 与 partRefreshKey 的 step-start/step-finish 都接入。
- 适配：target InputBreakdown 类型无 source 的 media 字段（target util/context-usage.ts:323-337），省略 media 分支。
- 测试：新增 "refresh key tracks inputBreakdown value changes even when string length is unchanged"（100→200 同长度不同值断言 key 变化）。

### B-03 测试: toJSON 守卫——已修复
- source context-usage.test.ts 用带 throwing toJSON 的 raw 验证 key 构建绝不序列化 raw payload；target 原测试只用普通字符串。
- 修复：新增 "refresh key never JSON-serializes raw payloads" 测试（new String + Object.defineProperty toJSON throw）。
- 语义：若实现退化为 JSON.stringify(state.raw) 测试立即抛错。

### B-02: dialog-select.test.tsx 整文件未移植——已修复
- source 新建 149 行 dialog-select.test.tsx（3 用例：稳定 key 标记 current、延迟 current 移动、无 key 对象 fallback）。
- 修复：创建 packages/tui/test/cli/tui/dialog-select.test.tsx（227 行），适配 target harness（TestTuiContexts + testRender + createTuiResolvedConfig + DialogProvider 等，参照 dialog-prompt.test.tsx 模式）。
- 适配：target DialogSelect 用 renderFilter={false} 跳过过滤；deferred 测试用 createSignal + onMount 延迟更新 currentKey。
- 验证：bun test dialog-select.test.tsx 3 pass / 0 fail。

### E/C 修复后
- E=250 C=38 need=38 PASS（新增 inputBreakdownRefreshKey 函数 + 2 测试 + dialog-select 测试 + 注释）

### 验证
- materialize 279: passed 279/279
- tui typecheck exit 0
- context-usage.test.ts 15 pass / 0 fail
- dialog-select.test.tsx 3 pass / 0 fail
- packages/app TS1128 pre-existing（不阻塞）

## DUAL-AUDIT SEAL（批次 276-280）
## DUAL-AUDIT SEAL（批次 276-280，第七轮）
- 正向审计 (ses_012cf5d9dffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_012cf42b2ffe): 5/5 PASS, 无 B/G (N-01~N-08 非阻塞)
- materialize 280: passed 280/280
- opencode typecheck exit 0, tui typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 276=38/6, 277=66/10, 278=0/0, 279=219/33, 280=134/41 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行


## R8 修复记录（2026-08-14）
- 背景：E=526/C=42/need=79 FAIL（缺 37 条注释）。
- 修复：分 3 批插入 37 条 #279 中文注释（context-usage.tsx refresh key 语义 13 条、context-usage.test.ts 测试意图 4 条、dialog-select.test.tsx 意图 3 条、inputBreakdown 扁平 key 1 条），锚点基于实际 patch 行（stripped 匹配）。
- 中途发现旧版插入脚本（patch_insert_comments.py）对 hunk 计数重算有 bug（newn 用原 body 计数），导致 0279 一个 hunk header 错（179→180）。用 validate_hunks.py 自动修正。
- 最终 E/C：E=526, C=79, need=79, PASS
- 验证：materialize 279 passed（修复前 corrupt at line 236 → 修复后通过）

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `9e9469211ad86b130d76cf35822bbc172bc8a57cc9673eeaae9f82e7f2f8fe01` / bytes: 34594
- E/C 最终：E=526, C=79, need=79, PASS
- 修复 G-04：record identity 更新（此前 3e452de9/32278 为旧值；实际 9e946921/34594）。
- 修复 G-01：16 处被判定为 padding 的注释重写为语义各异内容（附件引用长度信号 2 行、raw payload 循环引用 3 行、throttle key 2 行、流式 delta 信号、toJSON 守卫 3 行、inputBreakdown 数值信号等），行数 1:1 保持 E/C 不变；validate_hunks 校验 hunk 计数干净。
- 验证：state 278 上 git apply --check EXIT 0；materialize 300 passed。
