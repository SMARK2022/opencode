# Record 0357-863b3787d3a9

## Identity
- manifest parent: 222c2031c61e (0356)
- subject: fix(config): remove unnecessary mcp config items
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0357-863b3787d3a9` (passed 357/357)
- current patch: `.temp/patches/current/0357-863b3787d3a9.patch` (1626 bytes)

## Source intent
Remove the local `mcp.chatgpt` server config from the repository's own opencode
configuration. The block pointed at a machine-specific absolute `node.exe` path
and a submodule-local MCP server (`thirdparty/chatgpt-browser-agent/mcp-server.js`)
with `timeout: 690000` and `enabled: true`. The commit subject is
"移除不必要的 mcp 配置项" — the chatgpt MCP registration is no longer needed,
so the whole block (including its inline comments) is deleted. Only
`provider {}`, `permission {}` and the `tools { github-triage/pr-search: false }`
entries remain. This is opencode's own config in the repo (`customize-opencode`
territory — a config-file edit, not application code).

## Source patch anatomy
Single hunk on `.opencode/opencode.jsonc`: deletes the 17-line `mcp` block
(lines 5-21 of the source file region starting at `"mcp": {`). Pure deletion
patch — zero added lines.

## Target mapping (state 0356)
| Source file | Target file | Status |
|---|---|---|
| `.opencode/opencode.jsonc` | `.opencode/opencode.jsonc` (EXISTS, 36 lines) | Adapted |

## Target-adaptation decisions
1. **Diverged block content**: The target's mcp block carries documentation the
   source version lacks — the `#278` comment pair above `"mcp"` (path/env
   rationale), the inline `// submodule 从 .temp 迁移到 thirdparty 后...` comment
   inside `command`, and the `#278` pair above `"enabled"` (timeout staircase
   rationale: daemon start < CLI call < response). The source patch removed only
   the block itself; the curated patch removes the entire section including its
   leading doc comments, since every removed line documents the removed config
   item and none documents anything that survives. Keeping the comments would
   leave dangling documentation for a deleted key.
2. **Deletion shape**: Removed target lines 15-31 (1-based): 5-6 comment lines +
   the 12-line block. Result: `"permission": {},` is followed directly by
   `"tools": {`.
3. **Line endings**: File is CRLF in materialized worktrees; the patch is stored
   LF-normalized (the apply tool normalizes before `git apply --index`), so hunk
   content matches the LF index blobs regardless.

## Edit script (semantic summary)
Remove every line from `  // #278：ChatGPT MCP 本地配置——...` (first doc comment
line after `"permission": {},`) through the block's closing `  },` immediately
before `  "tools": {`. No other key is touched; trailing comma of the deleted
`mcp` entry goes with the block.

## E/C audit
`python count-ec-auditor.py` → **E=0, C=0, need=0, PASS** (pure deletion; the
auditor's pass rule "E==0 and C==0" holds — no added code or comments to gate).

## Materialization
- Preflight: `git apply --check --index` against a pristine 0356-state copy →
  exit 0.
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 357`
  (BUN_TMPDIR=C:\Temp) → "passed 357/357", state at
  `states/0357-863b3787d3a9`.
- Post-apply verification: `.opencode/opencode.jsonc` exists and contains zero
  occurrences of `mcp`, `chatgpt`, `#278`; the file ends with the tools block
  and trailing comma retained from the target baseline.

## Line accounting
- `.opencode/opencode.jsonc`: +0 / -17 (source patch counted -16 lines because
  its block lacked the extra `#278` comments present in the target).

## Risks / notes
- Config is repo-local; no schema or runtime defaults change. The removal does
  not affect the `.temp/patches` pipeline (excluded from repo fingerprints only
  by `.temp/` ignore rules — noted that `.gitignore` was previously modified to
  ignore `.temp`).
- E/C gate is satisfied by the deletion-only rule; the record itself is the
  substantive artifact for this item (data-less diff).

## Per-item diff stats
- fix0357.diff bytes: 1272 (single deletion hunk, one file).
- Patch file: original commit header + curated diff; LF-normalized storage.