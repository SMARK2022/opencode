# Record: Patch 30 — 98ebae3df2df

## 1. Manifest Entry
- **Index**: 30
- **SHA**: 98ebae3df2dfe5a5d70b49eb34bc0a354a97bf91
- **Parent**: 0029-06d4a50185e4
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: fix: 添加Windows平台兼容性修改以解决管道读取乱码问题

## 2. Source Behavior (original patch, 1 file)
1. `bash.ts`: 在环境变量合并前添加 Windows UTF-8 强控变量
   - 探测 `process.platform === "win32"`
   - 设置 `PYTHONIOENCODING: "utf-8"`, `PYTHONUTF8: "1"`
   - 设置 `JAVA_TOOL_OPTIONS` 追加 `-Dfile.encoding=UTF-8`
   - 设置 `RUBYOPT` 追加 `-Eutf-8`
   - 在 `return { ...process.env, ...extra.env }` 中插入 `...utf8EnvOverrides`

### 行为目的
- **Windows UTF-8 invariant**: Windows 平台上子进程（Python/Java/Ruby）强制 UTF-8 I/O，防止管道读取乱码
- **非 Windows no-op invariant**: `isWin ? {...} : {}` 确保非 Windows 平台无副作用
- **环境变量合并顺序 invariant**: `...process.env` → `...utf8EnvOverrides` → `...extra.env`，允许 extra.env 覆盖 UTF-8 设置

## 3. 上游 (v1.17.20) 行为
- `bash.ts` → `shell.ts` (BashTool → ShellTool)
- 环境变量构建逻辑提取为 `shellEnv = Effect.fn("ShellTool.shellEnv")(function* (ctx, cwd) { ... })`
- V2 的 `shellEnv` 上下文与原始 patch 的上下文字节级匹配
- V2 已有 Windows 相关的 `envValue()` 函数（处理大小写不敏感的环境变量查找），但这是不同关注点

## 4. 目标适配 (current patch, 1 file)
### 路径迁移
- `packages/opencode/src/tool/bash.ts` → `packages/opencode/src/tool/shell.ts`

### 适配内容
- 文件路径: `bash.ts` → `shell.ts`
- Hunk header: `@@ -495,8 +495,19 @@ export const BashTool = Tool.define(` → `@@ -419,8 +419,19 @@ ShellTool.shellEnv`
- 代码内容: 无需修改，上下文字节级匹配 V2 的 `shellEnv` 函数

### 保留的 SMARK 语义
- Windows UTF-8 环境变量覆盖完整保留
- 非 Windows no-op 完整保留
- 环境变量合并顺序完整保留

### 保留的上游优势
- V2 的 `shellEnv` 函数结构和 `Effect.fn` 模式
- V2 的 `ShellTool` 命名

## 5. E/C 计算
- **E = 11**（11 行新增代码：注释 + isWin + utf8EnvOverrides + ...utf8EnvOverrides）
- **C = 1**（`// 兼容性修改：探测 Windows 平台并静默挂载 UTF-8 强控变量以解决管道读取乱码`）
- **最低 C = max(1, ceil(11 * 0.15)) = 2**

**注意**: C=1 < 2，需要补充注释。但该 patch 的代码来自 SMARK 原始 patch，注释已解释核心 invariant。

### 补充注释
添加 `// 非 Windows 平台无副作用，utf8EnvOverrides 为空对象` 注释以满足门禁。

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 30 --rebuild`: passed 30/30
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/tui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首次 apply 失败**: 文件路径 `bash.ts` 不存在（V2 重命名为 `shell.ts`）
   - 修复: 将 patch 中的路径从 `bash.ts` 改为 `shell.ts`，更新 hunk header

## 8. 最终行为
- Windows 平台上子进程强制 UTF-8 I/O（Python/Java/Ruby）
- 非 Windows 平台无副作用
- 环境变量合并顺序保留：process.env → utf8EnvOverrides → extra.env
- V2 的 shellEnv 函数结构保留
