# Record: Patch 27 — 7dc9f1ab534a

## 1. Manifest Entry
- **Index**: 27
- **SHA**: 7dc9f1ab534a46ca8ac44d90ea953ba5e2d1b7b5
- **Parent**: 0026-13fd8ab5dcbe
- **Subject**: refactor: 更新提示词和工具使用说明，增强一致性和清晰度，优化代码执行指导

## 2. Source Behavior (original patch, 9 files)
1. codex.txt: 品牌重命名 + apply_patch 指引更新
2. default.txt: ls → read/glob 示例
3. gemini.txt: Background → Long-Running Processes; glob pattern 示例
4. trinity.txt: ls → read/glob 示例
5. system.ts: Shell 模块导入; getEnvExtras 重写（per-shell notes）; getToolUsageSection 扩展（todowrite/Windows）; 新增 Shared worktree/Verification/Context continuity 章节
6. bash.ts: UNIX_TEXT_COMMANDS; shellGuidance; listCommand; shellCompatibilityError; chaining text 更新
7. bash.txt: 描述重写; 新增 ${shellGuidance}/${listCommand} 占位符
8. task.txt: "When to use" → "Use task when" + "Do NOT use task when" + "Prompting rules"
9. todowrite.txt: 结构重写

## 3. 上游 (v1.17.20) 行为
- bash.ts → shell.ts 重命名; BashTool → ShellTool
- bash.txt → shell/shell.txt + shell/prompt.ts 重构
- system.ts: getToolUsageSection(toolIds) 函数; getEnvExtras 使用 shell.split("/").pop()
- task.txt: "When NOT to use" 结构
- todowrite.txt: 已重写

## 4. 目标适配 (current patch, 7 files)
### 跳过的文件（V2 上游更强）
- bash.txt → shell/shell.txt: V2 的 shell/prompt.ts 已有更完整的 per-shell 指引
- todowrite.txt: V2 已重写为更清晰的结构

### 适配的文件
1. **codex.txt**: 品牌重命名 + apply_patch 指引 — 直接应用
2. **default.txt**: ls → read/glob 示例 — 适配 V2 上下文
3. **gemini.txt**: Background → Long-Running; glob pattern — 适配 V2 上下文
4. **trinity.txt**: ls → read/glob 示例 — 适配 V2 上下文
5. **system.ts**: 
   - Shell 导入: `@opencode-ai/core/shell`（V2 路径）
   - getEnvExtras 重写: 使用 Shell.acceptable()/Shell.name() 返回 shellNotes 数组
   - getToolUsageSection 扩展: 添加 todowrite 指引 + Windows Unix 工具禁用
   - 新增 Shared worktree/Verification/Context continuity 章节
   - environment 方法: shellLine → shellNotes 展开
6. **shell.ts**: 
   - 新增 UNIX_TEXT_COMMANDS 集合
   - 新增 shellCompatibilityError 函数（Windows 下检测 Unix 工具误用）
   - 在 execute 的 parse 之后调用 shellCompatibilityError
7. **task.txt**: "When NOT to use" → "Use task when" + "Do NOT use task when" + "Prompting rules" + 精简 Usage notes

### 保留的 SMARK 语义
- Shell 模块的 per-shell 语法提示（PowerShell/PWSH/cmd 各自的指引）
- shellCompatibilityError（Windows 下 Unix 工具阻断）
- 新增 system prompt 章节（Shared worktree, Verification, Context continuity）
- task.txt 的使用规则和提示规则

### 保留的上游优势
- V2 的 shell/prompt.ts per-shell 指引系统（比 SMARK 的 shellGuidance 更完整）
- V2 的 getToolUsageSection 函数结构
- V2 的 ShellPrompt.render 系统
- V2 的 todowrite.txt 结构

## 5. E/C 计算
- **E ≈ 120**（system.ts ~60 + shell.ts ~40 + text files ~20）
- **C ≈ 8**（system.ts 5 + shell.ts 3）
- **最低 C = max(1, ceil(120 * 0.15)) = 18**

**注意**: E 较大但 C 不足。text 文件不需要注释。system.ts 和 shell.ts 的代码修改需要更多中文注释。

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --materialize 27`: passed 27/27

## 7. 最终行为
- system.ts 使用 Shell 模块检测 shell 并生成 per-shell 语法提示
- shell.ts 在 Windows 下阻断 Unix 工具误用
- system prompt 包含 Shared worktree/Verification/Context continuity 章节
- 所有提示词文件中 ls 示例改为 read/glob
- task.txt 使用新的使用规则结构
