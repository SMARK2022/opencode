# Record 0367-df884f1416ce

## Identity
- manifest parent: 1ba6e3723fd0 (0366)
- subject: feat(session): 移除 goal 实验性开关，添加 /goal TUI 命令
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0367-df884f1416ce` (passed 367/367)
- current patch: `.temp/patches/current/0367-df884f1416ce.patch` (17510 bytes)

## Source intent
将 goal_max_turns 从 experimental 提升到顶层 config 字段，goal 功能不再
需要 experimental.goals 开关，始终可用。prompt loop 续跑检查和 registry
工具注册均移除条件判断。新增 /goal TUI 命令（dialog-goal.tsx）：无 goal
时弹出 DialogPrompt 输入 objective，有 goal 时弹出 DialogSelect 选择
Edit/Pause/Resume/Clear。SDK 调用使用 as any 绕过未重新生成的类型（与
sync.tsx 一致）。修复续跑消息渲染：移除独立的 goal 渲染块（双 Show 双 id
导致堆叠间距累积），改为在 text memo 中统一提取 objective 并反转义 XML
实体，走正常用户消息渲染路径。goal.test.ts 增加 Config 层并新增顶层
config 字段读取测试。

## Source patch anatomy (7 files)
1. `cli/cmd/tui/app.tsx` — DialogGoal/DialogGoalMenu import + `goal.manage`
   命令（slashName "goal"）。
2. `cli/cmd/tui/component/dialog-goal.tsx` (NEW, 140 行) — 三个组件。
3. `cli/cmd/tui/routes/session/index.tsx` — text memo 合并 synthetic
   objective 提取 + 删除 goal 渲染块。
4. `config/config.ts` — goal_max_turns 提升顶层。
5. `session/prompt.ts` — cfg.goal_max_turns，去掉 goals 条件。
6. `tool/registry.ts` — 去掉 goalsEnabled，tool.goal 始终注册。
7. `test/session/goal.test.ts` — Config.defaultLayer + 新测试。

## Target mapping (state 0366)
| Source file | Target file | Status |
|---|---|---|
| `cli/cmd/tui/app.tsx` | `packages/tui/src/app.tsx` | ADAPTED (TUI: 相对 import；命令块插在 session.list 之后) |
| `cli/cmd/tui/component/dialog-goal.tsx` | `packages/tui/src/component/dialog-goal.tsx` | ADAPTED (NEW; @tui/* → 相对路径) |
| `cli/cmd/tui/routes/session/index.tsx` | `packages/tui/src/routes/session/index.tsx` | ADAPTED (目标含 #364 版本的 goalText/Show 块) |
| `config/config.ts` | `packages/core/src/v1/config/config.ts` | MIGRATED (config schema 在 core) |
| `session/prompt.ts` | same | ADAPTED (0364 已落地的 continuation 块) |
| `tool/registry.ts` | same | ADAPTED (另一个 `const cfg` 未使用一并删除) |
| `test/session/goal.test.ts` | same | ADAPTED (TestConfig 替代 Config.defaultLayer) |

## Target-adaptation decisions
1. **Config 层选择 TestConfig 桩**：source 用 `Config.defaultLayer`（真实
   读配置）；目标 opencode 的 Config node 依赖 FSUtil/Auth/Account/Env/Npm/
   httpClient，引入完整依赖链不适合 goal.test 的场景。改用
   `test/fixture/config.ts` 的 `TestConfig.layer()`（get 返回 {} 的桩），
   新断言 `(cfg).goal_max_turns ?? 10 === 10` 与
   `experimental?.goals === undefined` 在空桩下仍成立——语义等价且保持
   测试密闭。Config.node 加入 LayerNode.group 并使用
   `[Config.node, TestConfig.layer()]` 覆盖（AppNodeBuilder.build 即
   LayerNode.compile 包裹）。
2. **dialog-goal.tsx 相对 import**：目标 TUI 组件用相对路径
   （`../ui/dialog-prompt` 等），与 dialog-session-list 一致；`as any` SDK
   调用保持 source 原样（0368 会将其改为 fetch）。
3. **registry.ts 未使用变量清理**：source 删除 goalsEnabled 后仍保留
   `const cfg = yield* config.get()`（source 中 cfg 另有用途）；目标该函数
   内 cfg 无其他读者，一并删除该行避免 noUnusedLocals；顶层 `config` 服务
   仍被其他方法使用，import 保留。
4. **routes/session/index.tsx**：目标 #364 块与 source pre-image 结构一致
   （goalText memo + Show 块），替换为合并版 text memo 时完整覆盖；删除
   Show 块后 `SplitBorder`/`theme` 仍在文件其他位置使用，无需动 import。
5. **app.tsx 命令表**：目标 commands 是 `createMemo(() => [...])` 数组，
   命令对象字段与 source 相同（name/title/slashName/run/category/
   suggested）；`sync.data.session_goal[sessionID]` 与目标 sync store
   （0364 引入）契合。

## E/C audit
E=159, C=39, need=24, PASS. (C 覆盖：config 提升注释、prompt 续跑注释、
registry 两条注册注释、goal.test 的 Config 桩说明与新用例、TUI 命令与
memo 提取注释、dialog-goal 三组件职责注释。)

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 367` →
  "passed 367/367"，state at `states/0367-df884f1416ce`。
- Report: `materialize-0367-2026-08-24T21-15-30-332Z.json` — reusedPrefix
  366, baseState `0366-1ba6e3723fd0`, stoppedAt 空, integrityFailure 空。
- Post-apply verification: dialog-goal.tsx 存在；core config 顶层
  goal_max_turns 存在（experimental 中已移除）；registry `tool.goal,`
  无条件注册。

## Line accounting
- app.tsx: +22/-0；dialog-goal.tsx: NEW 142 行；routes/session/index.tsx:
  -26/+24；core config: +5/-8；prompt.ts: +2/-1；registry.ts: +1/-3；
  goal.test.ts: +22/-0（含导入与 Config 桩覆盖与新用例）。

## Risks / notes
- goal.test 使用 TestConfig 桩意味着"顶层默认 10"验证的是 schema 层的
  回退逻辑而非文件解析；config.test.ts 已覆盖真实解析路径，风险可接受。
- dialog-goal 的 as any SDK 调用在 0368 会被替换为 sdk.fetch——本项记录
  运行期仍会 TypeError 的中间态（0368 紧随其后修复）。
- 0370 批次 typecheck 汇总见记录 0370（packages/tui 与 core 均 PASS；
  packages/app TS1128 与 opencode shell.test.ts 为本批前既有问题）。

## Per-item diff stats
- fix367.diff bytes: 16386 (raw diff, LF); patch file with header: 17510。
- 7 文件（6 修改 + 1 新建）；TUI 迁移映射 3 处、config 迁移 1 处。