# Record 0304-f325360dd994

## Identity
- index/SHA: 0304 / f325360dd9944436a119d791c00a847c3aac7413 (short f325360dd994)
- subject: feat(log): 增强日志系统，支持缺失目录的自动恢复并添加相关测试
- status: PASS
- current SHA-256: `A5E1B7362CF7CF29CE6ED869370FDA1C577ED362A6D4CB7985C394A9AB50F913`
- current size: 12947 字节

## Source intent (original/0304-f325360dd994.patch, hunk 清单)
1. `packages/core/src/util/log.ts` 重写（SMARK legacy Log.init 模块）：
   - `write`/`stream` 分离：print=true 恢复 stderr 且销毁旧 stream；init 自愈缺失日志目录（mkdir recursive）
   - stream error 吞掉（日志通道不产生 unhandled error）；write 永不 reject（destroyed/closed/!writable → 0；write 回调 err→0）
   - 保留 cleanup/truncate/runID dedup 语义（unmodified）
2. `packages/opencode/test/ci/upstream-e2e-workflow.test.ts`：atLeast 注释改写（只比较主版本号段→逐段比较固定版本号覆盖下限约束）
3. `packages/opencode/test/fixture/fixture.ts`：tmpdir 与 tmpdirScoped 两个 git fixture 增加 `core.autocrlf false`（Windows runner 系统 autocrlf=true 破坏 clone/commit 断言）
4. `packages/opencode/test/installation/install-script.test.ts`：
   - `existsSync` import；`BASH = preferredWindowsGitBash() ?? Bun.which("bash") ?? "bash"`
   - uname/id heredoc `"${"$1"}"` → `"$1"`（**target 早已修复，无操作**）
   - bashCommand/bashPath 注释重写为 Git Bash/WSL 双风味语义
   - bashPathForFile 风味感知：（Git Bash `/c/`，WSL `/mnt/c/`）+ `windowsBashFlavor()`/`preferredWindowsGitBash()` 两个 helper
5. `packages/opencode/test/tool/external-directory.test.ts`：断言改归一化路径（不再写死 F:\ 盘符）——**target #210 测试已用 scoped tmpdir 动态路径实现同等语义，无硬编码盘符可改，无操作**
6. `packages/opencode/test/tool/repo_clone.test.ts`：每用例独立 owner/repo 避免 Global.Path.repos 缓存冲突——**target 无 repo_clone 工具与测试（upstream 无此工具），无 owner，无操作**
7. `packages/opencode/test/util/log.test.ts`：readEventually 轮询 + `list[0]!` 规范化 + 新测试"dev logging recreates a missing log directory"——**target 无 legacy Log 测试；缺失目录语义下移 owner 为 core observability（见下）**

## Target adaptation (current/0304-f325360dd994.patch)
1. `packages/core/src/observability/logging.ts`（上游 Effect Logger owner）：
   - fileLogger 返回 `Effect<Logger>`：打开文件前 `FileSystem.makeDirectory(path.dirname(file), { recursive: true })` 自愈缺失目录；否则 `FileSystem.open("a+")` 的 ENOENT 经 `Layer.orDie` 终止进程
   - import 增加 `Effect, FileSystem`（effect 统一导出）
   - 写失败忽略语义上游已具备（toFile flush 用 effect.ignore）+ 批处理（batchWindow 1000ms）保留
2. `packages/core/test/effect/observability.test.ts`：新增"file logger recreates a missing log directory"镜像 source log.test.ts 新测试意图（Layer.orDie 下不应泄漏 ENOENT）
3. fixture.ts：两处 autocrlf=false（source 原样）
4. install-script.test.ts：source 全部语义 + #292 时期 7 处 "WSL-only" 注释同步为双风味（行为已风味感知，注释需准确）；helpers 按 source 原样追加
5. upstream-e2e-workflow.test.ts：atLeast 注释改写（source 原样，含 0303 引入额外注释保留）
6. 无操作项记录：repo_clone（无工具）、external-directory（#210 已等义）、heredoc（已修复）、log.test.ts readEventually/list[0]（legacy API 专属，无可承载 owner）

## E/C（count-ec-auditor.py）
- E=37, C=26, need=ceil(37*0.15)=6 → **PASS**（最终 current 确认）

## Verification log
1. `git apply --check fix304.diff`（state 0303 干净树）→ EXIT 0；`validate_hunks.py` → fixed: 0
2. `--materialize 304` → `passed 304/304`，state 0304 建立（修订版：首次文本级 hunk 修补产物 corrupt patch 被弃，改从干净 state 0303 重跑 edit304.py 以 git diff 权威生成）
3. `--typecheck 304`：Install `reused (cbdc173...)`（0304 未动依赖，指纹不变正确）；Typecheck `packages/app:failed`（已知环境基线 TS1128，同 0300-0303）
4. 手动行为测试（.test-workspace/repo）：
   - packages/core `bun test test/effect/observability.test.ts` → **6 pass / 0 fail**（含新增 missing-dir 自愈测试；修复前 FileSystem.makeDirectory 非静态方法导致 3 失败，改为 `yield* FileSystem.FileSystem` 实例调用后全绿）
   - packages/opencode `bun test test/installation/install-script.test.ts` → **4 pass / 0 fail**
   - packages/opencode e2e/fixture/external-directory 三文件 → **11 pass / 0 fail**
   - packages/core `bun typecheck` → exit 0；packages/opencode `bun typecheck` → exit 0

## 修订记录（R1）
- R0：fileLogger 用 `FileSystem.makeDirectory(...)` 静态调用 → 运行时 TypeError（Effect 统一导出中 makeDirectory 是服务实例方法）→ 改为 `const fs = yield* FileSystem.FileSystem` + `fs.makeDirectory(...)`；E=37/C=26
- R1：记录转义瑕疵修正（SHA/字节行）；批次审计后扩展本段
- R2（批次复审修复轮）：atLeast 注释缩进从 4 空格修正为 2 空格（source 原样；审计 N-01）；current 重写后重建 305/305 + typecheck 305 + e2e 1/1 复验通过；E/C 不变（37/26）；current 最终 SHA-256 `A5E1B7362CF7CF29CE6ED869370FDA1C577ED362A6D4CB7985C394A9AB50F913` / 12947 字节（13030B→12949B→12947B 修订链：初始文本修补→R0→R2 缩进）

## 不一致与修订记录（审计轮）
- 记录时间线: record 首次创建 2026-08-15 21:29:50，早于 current/0304 最终写入 22:25:41（顺序合规）
- source log.ts 的 print=true→stderr 重初始化语义在 target 无 legacy writer（上游每次 layer 构建重新打开文件，无残留 writer 可泄漏），故无需平移该分支；record 明示
- heredoc `"$1"`、external-directory、repo_clone、log.test.ts 无操作理由逐条记录于 Source intent 第 4/5/6/7 项

## Audit trail
- 批次 301-305 正向审计 (ses_ffa16f112ffeGzwy9avWTt1Job) 与逆向审计 (ses_ffa16982bffe4zMBMU7Txzfgp9) 对 0304 判 PASS（记录 50+ 行/3900+ 字符达标，无 B/G）；仍按批次统一要求扩展本段供 seal 追加。
- 
- 第三轮双审计（2026-08-16）：正向 ses_ff996c159ffemCRlIsJ3sxxISw / 逆向 ses_ff9967147ffe0zYF0lcjWm3yOy —— 0304（两方向均 PASS，无 B/G；R2 缩进修复后 current A5E1B736…/12947B 复核一致）
- 批次整体结论：歧义未闭合 → 0302 升级用户裁决，批次 0301-0305 保持 BLOCK（不可继续 0306-0310）；用户裁决后须再启动全新双审计封存。
## R3 后缀重建注记（2026-08-21）
- 用户裁决后 0302 执行 R3 阻塞修订循环（记录先行、state 0301 重新推导、current 内容逐字节一致 856f135a…/9070B）；本项 current 未变，后缀在 R3 基础上重建并通过：materialize 305 passed 305/305、typecheck 305 install installed(cbdc173…)、e2e 回归 1/1、recorder 5/5。
- 
## DUAL-AUDIT SEAL（批次 301-305）
- 第四轮双审计（R3 生命周期 + USER DIRECTIVE 裁决后）：
  - 正向审计（ses_fdff18d59ffe2SzPAwFIf70usY）: 5/5 PASS，无 open B/G（0302 按 USER DIRECTIVE 裁定 PASS：R3 修订循环真实执行、记录先行、内容逐字节收敛）
  - 逆向审计（ses_fdff145e7ffemUVvITnJJ1xDBO）: 5/5 PASS，无 open B/G（0302 同上；N-01 工作区调试工件已由审计清除并复核 opencode typecheck exit 0）
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节（0301 e7a5ff82/3504B、0302 856f135a/9070B、0303 56D7F9B7/9591B、0304 A5E1B736/12947B、0305 9D160340/8345B）、install 指纹（0302=890e205a installed、0305=cbdc173 installed/reuse）、E/C 五项全达标（0/0、62/14、81/15、37/26、44/11）、累计 apply 305/305（R3 链）、states 保留 4 个 <=5、source/target 未改
- 环境性注记（非 B/G）：install-script sudo 用例本机 git-bash 查找竞态（08-15/16 记录验证 4/4；探针矩阵证据在 records/0305）；packages/app TS1128 环境基线
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）



