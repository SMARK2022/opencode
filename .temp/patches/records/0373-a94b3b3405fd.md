# Record 0373-a94b3b3405fd

## Source patch

- Source: `feat(tui): 允许用户 Resume 已完成的 goal` (SMARK2022, 2026-07-04)
- Original: `.temp/patches/original/0373-a94b3b3405fd.patch` (83 lines, 2 files)
- Replay index: 0373, sha a94b3b3405fd

## Port decision summary

Allow the user to Resume a goal whose status is `complete`. The data layer set function already
imposes no state-transition restriction, the HTTP API and prompt loop already support
complete → active, and the only missing piece was the UI not exposing the Resume option for the
complete state. This item adds `complete` to the Resume condition in the goal dialog menu and
adds a regression test locking the complete → active transition plus the usage-preservation
invariant.

Files and mapping:

- `packages/opencode/src/cli/cmd/tui/component/dialog-goal.tsx` → `packages/tui/src/component/dialog-goal.tsx`
  (Existed in prior batches as the migrated owner; the upstream org tree path no longer exists in
  this series.)
- `packages/opencode/test/session/goal.test.ts` — exists at the same path.

## State analysis (state 0370)

- `packages/tui/src/component/dialog-goal.tsx`: `DialogGoalMenu` builds the option list with the
  Resume entry at lines 119-127:
  - `// paused/blocked 状态下显示 Resume 选项`
  - `...(goal()?.status === "paused" || goal()?.status === "blocked" ? [{ title: "Resume", … }] : [])`
  - The Pause entry above it stays active-only; the Clear entry below is unconditional.
- `packages/opencode/test/session/goal.test.ts`: 421 lines; imports already include
  `Effect, Exit, Layer, Option` from "effect", `Session as SessionNs` from "@/session/session",
  `SessionGoal` from "@/session/goal"; `it` is `testEffect(...)` with the standard layer group,
  so the new test needs no new imports.
- `SessionGoal` API confirmed in the state sources: `accountUsage(sessionId, tokens, seconds)`
  only accrues while `status === "active"` (see `SessionGoal.accountUsage` guard), `set` updates
  status/budget/objective while preserving usage columns, and `get` maps `tokens_used` /
  `time_used_seconds` / `token_budget` to `tokensUsed` / `timeUsedSeconds` / `tokenBudget` —
  exactly the fields the new test asserts.

## Edit script (semantic)

1. dialog-goal.tsx: extend the Resume condition to include `complete` and update the comment so
   the intent is explicit:
   - `// paused/blocked/complete 状态下显示 Resume 选项`
   - `// complete 也允许 Resume：用户可能需要让模型重新验证或继续完善已完成的目标`
   - condition becomes `goal()?.status === "paused" || goal()?.status === "blocked" || goal()?.status === "complete"`
   - The Resume entry itself (title/description/value/onSelect → `setGoal({ status: "active" })`)
     is unchanged: the data layer tolerates the transition and budget tracking keeps accruing
     across the resume boundary.
2. goal.test.ts: append the regression test "resume complete goal back to active preserves usage"
   after the existing "goal_max_turns …" test (file tail):
   - create a session, set objective + tokenBudget 10_000, accrue 3000 tokens / 120s via
     `accountUsage`.
   - mark `status: "complete"`; assert `get` shows complete and tokensUsed 3000.
   - attempt `accountUsage(999, 99)` while complete and prove it is NOT accrued
     (status !== "active" guard).
   - resume with `set({ status: "active" })`; assert status active, tokensUsed 3000,
     timeUsedSeconds 120, tokenBudget 10_000 — the budget keeps tracking across the
     complete → active boundary.

All assertions are behavior-level; the test exercises the real `SessionGoal` service through the
standard test layer (no mocks).

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix373.diff` (2,857 bytes, 62 lines)
- Current patch: `.temp/patches/current/0373-a94b3b3405fd.patch`
- Materialized state: `0373-a94b3b3405fd`

## E/C gate

`count-ec-auditor.py`: E=26, C=10, need=4 → PASS. Comment coverage: the Resume-condition comment
pair (dialog-goal.tsx) plus the test's transition rationale and usage-preservation comments.

## Materialize

`--materialize 373` → `passed 373/373`, state published. Both hunks applied by `git apply --index`
without friction (the dialog-goal.tsx hunk is small and the goal.test.ts addition is a pure
append).

## Verification

- `bun typecheck` (packages/opencode, batch-end workspace at state 0375): PASS — the appended
  test type-checks against `SessionGoal.Service` / `SessionNs.Service` and the Option guards.
- Test-layer compatibility: the layer group in goal.test.ts already provides SessionGoal.node and
  SessionNs.node (same composition as the existing "clearing session cascades to goal" test), so
  no fixture changes were required.

## Notes / deviations from source

- No deviations: the Resume condition and the test body are ported verbatim; only the insertion
  position of the test differs (file tail instead of upstream mid-file position), which is
  semantically neutral.
- The upstream patch's first test anchor (`@@ -192,6 +192,44 @@` after "clear removes the goal")
  does not apply here because the goal.test.ts layout in this series already contains additional
  later-added tests; appending at the tail keeps the diff minimal and unambiguous.