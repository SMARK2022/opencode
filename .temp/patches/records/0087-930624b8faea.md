# Record

- **Current SHA-256:** `8299d42b311219f37ff6df7fbf3533d93b39cfc579916b859d7336c52f9301b7`
- **Current bytes:** 12385
: Patch 930624b — 930624b8faea

## Source Commit
- SHA: 930624b8faea
- Subject: feat(vscode): 更新桥接功能，移除环境变量支持，增强注册表桥接选择逻辑
- Author: SMARK <3222301373@qq.com>
- Date: Mon, 4 May 2026

## Source Behavior (逐 hunk 分析)

### 概述
source patch 87 移除环境变量桥接支持（OPENCODE_VSCODE_BRIDGE_PORT/TOKEN），增强注册表桥接选择逻辑。在 editor.ts 中添加 Bridge Registry 作为主连接方式（优先于 WebSocket 和 Zed），添加 5 秒轮询和清理逻辑。

### 文件变更清单（4 文件）
1. `packages/tui/src/context/editor.ts` — **修改**（V2 路径迁移）：添加 "bridge" 到 source 枚举；添加 bridgePoll 变量；重构 connect() 为三层优先级（① Bridge Registry → ② WebSocket → ③ Zed）；添加 scheduleBridgePoll()；添加 bridgePoll 清理；更新 enabled() 包含桥接注册表检查；添加 resolveBridgeRegistryActiveFile() 和 bridgeRegistryWorkspaceScore() 函数；添加 pathContainsLength() 辅助函数
2. `packages/opencode/src/ide/vscode-bridge.ts` — **修改**：移除 envBridge() 函数和 resolveBridge() 中的调用
3. `packages/opencode/test/ide/vscode-bridge.test.ts` — **修改**：移除 env bridge 测试和相关环境变量清理代码
4. `sdks/vscode/src/extension.ts` — **修改**：移除 OPENCODE_VSCODE_BRIDGE_PORT 和 OPENCODE_VSCODE_BRIDGE_TOKEN 环境变量

### 路径迁移
- `packages/opencode/src/cli/cmd/tui/context/editor.ts` → `packages/tui/src/context/editor.ts`
- 其他文件路径不变

## V2 适配

## V2 适配

### 适配策略
- editor.ts：V2 使用 Schema.Literals 替代 zod enum，createSimpleContext 替代自定义 context。在 connect() 开头添加桥接注册表检查，使用 setSelection() 和 setStore() 适配 V2 API。添加 pathContainsLength() 函数（V2 中不存在）。添加 node:path、node:os、node:fs 导入。
- vscode-bridge.ts：直接移除 envBridge() 函数和调用，V2 代码结构兼容
- test：移除 env bridge 测试和 env var 清理代码
- extension.ts：直接移除环境变量，V2 代码结构兼容

### invariant 和错误语义
- Bridge Registry 优先级：注册表发现 > WebSocket > Zed，确保 VS Code 桥接优先于其他编辑器集成
- bridgePoll：5 秒间隔重新检查注册表，发现新桥接时自动切换
- resolveBridgeRegistryActiveFile：60 秒新鲜度过滤，工作区路径匹配评分
- enabled()：包含桥接注册表检查，确保 UI 正确显示编辑器状态

## 三方比较

| 文件 | Source (SMARK) | 上游 (V2) | 目标适配 |
|---|---|---|---|
| packages/tui/src/context/editor.ts, packages/opencode/src/ide/vscode-bridge.ts, packages/opencode/test/ide/vscode-bridge.test.ts, sdks/vscode/src/extension.ts | 完整 source 行为 | V2 基线 | 在 V2 基线上应用 SMARK 行为 |

## E/C 计算
- E (有效修改行): 69
- C (合格中文注释行): 11
- 最低 C: max(1, ceil(69 * 0.15)) = 11
- 判定: 11 >= 11, **通过**

## 安装与测试
- Materialize: passed 90/90
- Typecheck: all 30 workspaces passed
- Install: reused
- Source repository unchanged: true
- Target repository unchanged: true

## 失败修订
- 初次 apply 失败：editor.ts 路径未迁移（cli/cmd/tui/ → packages/tui/src/）
- 修复：使用 V2 路径生成 diff
- envBridge() 移除不完整：resolveBridge() 中仍有调用
- 修复：使用更灵活的正则表达式移除所有 envBridge 引用
- pathContainsLength 函数在 V2 中不存在
- 修复：添加函数定义
- "env bridge is preferred" 测试未移除
- 修复：移除测试
- enabled() 未更新包含桥接注册表检查
- 修复：添加 resolveBridgeRegistryActiveFile(directory) 到 enabled()

## 最终行为
- 所有 source 行为已适配到 V2 架构
- Typecheck 全部通过
- Source/Target worktree 未被改写

## Verdict: PASS (dual audit confirmed)

## 详细 Hunk 分析

### editor.ts 变更
1. Schema 枚举：添加 "bridge" 到 source 枚举（2 处），允许桥接来源标识
2. bridgePoll 变量：新增轮询定时器，5 秒间隔检查注册表
3. connect() 重构：三层优先级
   - ① Bridge Registry：调用 resolveBridgeRegistryActiveFile(directory)，命中时关闭 WebSocket、设置 selection、启动轮询
   - ② WebSocket：调用 resolveEditorConnection()，命中时打开 editor socket
   - ③ Zed：调用 resolveZedDbPath() 和 resolveZedSelection()
4. scheduleBridgePoll()：5 秒后重新调用 connect()，发现新桥接时自动切换
5. reconnectWithDirectory：清理 bridgePoll
6. onCleanup：清理 bridgePoll
7. enabled()：添加 resolveBridgeRegistryActiveFile(directory) 检查
8. resolveBridgeRegistryActiveFile()：读取 ~/.local/state/opencode/ide/*.json，60 秒新鲜度过滤，工作区路径匹配评分
9. bridgeRegistryWorkspaceScore()：计算工作区路径与当前目录的匹配度
10. pathContainsLength()：路径包含判断，返回 parent 路径长度用于评分

### vscode-bridge.ts 变更
- 移除 envBridge() 函数（15 行）
- 移除 resolveBridge() 中的 envBridge() 调用和健康检查

### test 变更
- 移除 originalPort/originalToken 环境变量声明
- 移除 restoreEnv 调用
- 移除 "env bridge is preferred when healthy" 测试
- 移除环境变量删除

### extension.ts 变更
- 移除 OPENCODE_VSCODE_BRIDGE_PORT 和 OPENCODE_VSCODE_BRIDGE_TOKEN 环境变量

## 代码质量
- 无 any 类型引入
- 无未使用的 helper
- 无死分支
- 风格保持一致：早返回、const 优先、函数式数组方法
- 导入添加：node:path, node:os, node:fs（V2 中 editor.ts 原本不导入这些）

## 不一致与决策
1. V2 使用 Schema.Literals 替代 zod enum，需要不同的方式添加 "bridge"
2. V2 的 connect() 使用 resolveEditorConnection(directory, port, editor.connection)（3 参数），V1 使用 resolveEditorConnection(directory)（1 参数）
3. V2 的 editor.ts 不包含 pathContainsLength 函数，需要新增
4. V2 使用 setSelection() 替代 setStore("selection", ...)


## 5. E / C

- **E = 77**
- **C = 16**
- **need ≥ 12**
- **Gate:** PASS

## Dual independent audit (batch 85-89)

- **FORWARD:** PASS (all five, no open B/G) — ses_0786eabd4ffeXUBUo8w9wkU0zf
- **REVERSE:** PASS (all five, no open B/G) — ses_0786eabc1ffemlej5xXCzvttGF
- **State:** `states/0089-94eb97b93fad`
- **Typecheck:** `typecheck-0089-2026-07-22T02-03-28-357Z.md` monorepo packages passed
- **Batch verdict:** PASS
