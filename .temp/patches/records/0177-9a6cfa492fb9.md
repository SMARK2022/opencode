# Record 0177-9a6cfa492fb9

- Index: 177
- Source SHA12: 9a6cfa492fb9
- Batch: 176-180
- Updated: 2026-07-25T21:09:24Z
- Verdict: **PASS** (dual independent audit sealed)

## 1. Source intent (original)

Original subject roughly: update stats ranking logic and introduce tool-usage data.

Concrete source hunks:

1. **CLI sort surface** in `stats.ts`: `sortChoices` leads with tokens; `--sort` default tokens; `sortMode` gains explicit `cost` branch and defaults to tokens when unset/invalid.
2. **Data plane** in `stats/data.ts`: group/series finalizers and session lists rank by tokens then cost; add `toolSeries` / tool event usage aggregation keyed by context tokens.
3. **Render/insights/charts**: dashboard/sessions prefer tokens; add “Tool context tokens over time” series rendering; tests assert that title.
4. **Non-goal / non-change**: `metricMode` must continue to honor `--metric cost`. A previous bad residual inverted this and is explicitly forbidden.

## 2. Upstream tip design retained

After #176 tip already has:

- Effect `aggregateStats` single write path.
- Rich `ToolUsage` and `toolEvents` substrate (required dependency).
- Multi-command stats CLI with shared `renderOptions`.

#177 therefore optimizes ranking defaults and adds the missing **time series** for tool context on top of that substrate rather than replacing the aggregation architecture.

## 3. First divergence on tip@176

| Check | Pre-#177 tip | Source requirement |
|---|---|---|
| `sortChoices[0]` | `cost` | `tokens` |
| yargs `--sort` default | `cost` | `tokens` |
| `sortMode` fallback | `cost` | `tokens` |
| `metricMode("cost")` | `cost` | `cost` (must stay) |
| `finalizeGroups` comparator | cost-first | tokens-first |
| `toolSeries` on report | absent | present |
| sessions render default sort | cost | tokens |

## 4. Primary-path edits

### `packages/opencode/src/cli/cmd/stats.ts`

- Reorder `sortChoices` to `["tokens","cost","calls","updated"]`.
- Default `--sort` to tokens.
- `sortMode`: accept `cost|tokens|calls|updated`; default return `"tokens"`.
- `metricMode`: restored/kept as `cost → cost`, else tokens. Chinese comment states metric/sort decoupling.

### `packages/opencode/src/cli/cmd/stats-data.ts`

- Replace cost-primary sorts with tokens-primary / cost-secondary (including ascending duals if present).
- Comment on `finalizeGroups`.
- Add `toolSeries: UsageSeries[]` to `StatsReport`.
- While folding `toolEvents`, accumulate per-tool daily points (`tokens.total` carries contextTokens) and `finalizeSeries(toolSeries, sortedDaily)`.

### `packages/opencode/src/cli/cmd/stats-render.ts`

- `options.sort ?? "tokens"` for sessions.
- Export `renderToolSeries` emitting chart title `Tool context tokens over time` via `renderRoundedLineChart`.

## 5. Metrics / E-C

- Current path: `.temp/patches/current/0177-9a6cfa492fb9.patch`
- Bytes: 7756
- SHA-256: `a41e0fe0f1306418f7f6bfb386851ef2c08b519bb71be69a9fa6bab9f96c758f`
- E=29, qualifying C=10, need=5, gate=PASS
- Comment locations: sortChoices, metricMode, finalizeGroups, toolSeries accumulation, renderToolSeries, sessions default.

## 6. Verification evidence

- Cumulative materialize includes #177 (batch end 180/180).
- Typecheck report `typecheck-0180-2026-07-25T21-04-49-902Z.md`; install fingerprint `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f` reused; workspaces passed.
- Manual tip inspection: metricMode returns cost for cost; sortMode defaults tokens.
- No `1016` or unrelated constant damage in this patch.

## 7. Failures fixed in this rebuild loop

1. Auditor B: metricMode cost→tokens — fixed.
2. Auditor B: toolSeries missing — fixed on #176 data model.
3. Auditor G: record &lt;3000 chars — this rewrite.
4. Earlier syntax break from comment eating `finalizeGroups` — not present.

## 8. Residual risk

- Interactive dashboard view list may not auto-embed `renderToolSeries`; charts helper is exported for composition. If a future audit requires dashboard auto-inclusion, wire `renderToolSeries(report)` into `renderCharts`/`renderDashboard` return arrays without changing data semantics.

## 9. Post-BLOCK repair (2026-07-25T21:38:14Z)

- Wired `Tool context tokens over time` into **primary** paths: `renderBreakdown` (by=tool) and `renderCharts`.
- Width test asserts the chart title (not only `ctx=`).
- `metricMode("cost")` remains cost; sort tokens-first retained.

## LIVE IDENTITY (authoritative post-repair)

- Updated: 2026-07-25T21:52:12Z
- Current path: `.temp/patches/current/0177-9a6cfa492fb9.patch`
- Bytes: 11087
- SHA-256: `95aed18a35ed21c9ed1dc2934d32ed8c324462cd1d071786b8dd236378b81f0f`
- Independent E=89, C=17, need=14, gate=PASS
- Batch typecheck report: `typecheck-0180-2026-07-25T21-50-38-944Z.md`
- Install fingerprint: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Tip state: `states/0180-201e910402a2`
- Any earlier SHA/bytes/E/C lines in this record are superseded by this block.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-25T22:01:23Z
- Batch: 176-180
- FORWARD auditor: PASS on 176,177,178,179,180 — no open B/G
- REVERSE auditor: PASS on 180,179,178,177,176 — no open B/G
- Tip: states/0180-201e910402a2
- Typecheck: typecheck-0180-2026-07-25T21-50-38-944Z (all workspaces passed)
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Materialize: materialize-0180-2026-07-25T21-50-16-624Z (reusedPrefix 175, applied 176-180)
- Verdict: **PASS** (independent dual audit)

