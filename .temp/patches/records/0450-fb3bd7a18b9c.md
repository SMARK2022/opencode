# 0450-fb3bd7a18b9c chore(vscode): 升级扩展至 1.15.11

## Identity
- index / full SHA: 0450 / fb3bd7a18b9cf0b4560095fa42cd587b097e3e58（parent = 0449；批次 0446-0450 末项）
- subject: chore(vscode): 升级扩展至 1.15.11
- original: 13414B / 200 行 / 3 文件
- current: 9999B / 3 文件（协同升号映射至目标扩展版本链）
- status: 闭合（materialize passed 450/450；typecheck 450 全 30 包 passed，No failures）

## 版本映射（0440 协同升号先例）
- 源语义：扩展 semver 1.15.10 → 1.15.11（+1 补丁版）；Recommended CLI 引用 1.15.11-smark（CLI 版本在源该行不变）。
- 目标基线：扩展已是 1.17.31（0440 批次将扩展版本从 1.15.x 追赶至 CLI 1.17.31；package.json 的 "//" 键值记录版本合同链：#218 格式修正、#408 追赶 CLI）。
- 0450 映射：扩展 1.17.31 → **1.17.32**（+1 补丁版，独立 semver 语义保持）；Recommended CLI 引用保持 opencode 1.17.32-smark（0440 已达，源该行语义为不变）。

## 逐文件适配
1. sdks/vscode/package.json：version 1.17.31→1.17.32；"//" 合同注释追加 #450 条目（扩展补丁版本 +1，独立 semver）——沿用 0440 建立的注释链格式。
2. sdks/vscode/README.md：
   - 版本表 1.17.31→1.17.32。
   - Marketplace 措辞修正（源语义：去除会过期的具体旧版本号）：`show 1.17.27 until` → `show an older extension release until`（目标旧值为 0440 适配时写死的 1.17.27，源 0450 的改进正是消除这类硬编码）。
   - Touch 行措辞：preserve-focus preview → hidden + one-second budget（源 0450 与 0447 的 hidden diagnostics 语义对齐）。
   - fallback 措辞：内置 LSP 回退描述 → 快照语义描述（源 post 原文）。
   - openTextDocument 措辞：preview editor → keeps the target hidden（源 post 原文）。
   - /lsp/touch endpoint 行：Activate and wait briefly → Observe diagnostics within the one-second budget。
   - 目录树 ASCII→树形字符（15 行，源 post 原样）。
3. docs/VSIX-Packaging.md：版本表 1.17.31→1.17.32；Use canonical 行 semver 括号 1.17.31→1.17.32（recommended CLI 1.17.32-smark 保持）；目录树 ASCII→树形（9 行，源 post 原样）。

## 验证
- materialize 450：passed 450/450（全链 441-450）。
- typecheck 450：全 30 包 passed；No failures；install reused 813972ec6428411e9b35d301cfa065657188b6a84e22b9a20a67b7e9393e67d9（0446-0450 期间 fingerprint 由 04420627 变化一次，SDK 依赖所致，脚本自动处理）。

## E/C（口径声明）
- sdks/vscode/README.md 与 docs/VSIX-Packaging.md 为文档（markdown prose），不计入 E——与 0437/0445 plan doc 先例同口径。
- 代码面 = sdks/vscode/package.json 2 行：version 行（E=1）+ "//" 合同注释行（C=1，JSON 注释惯例键，值为中文合同文本）。E=1、C=1 ≥ ceil(1×0.15)=1 PASS。
- 若审计将 README/VSIX 计入 E，则相应 markdown 无注释行可计、且 0437/0445 先例已被接受为反例——应以先例口径为准。
## 逐 hunk 映射（original → current）
- VSIX @@ -10,7（版本表）：1.15.10→1.15.11 映射为 1.17.31→1.17.32。
- VSIX @@ -77,15（payload tree）：ASCII 9 行 → 树形 9 行，原样落位（目标 tree 仍为 ASCII，锚点成立）。
- VSIX @@ -113（Install 后）：Tree 表述行（±无版本）——随 tree 段一并落位。
- VSIX @@ -158（publish 前置声明）：行内措辞（无版本）——原样落位。
- VSIX @@ -178,19（commands 行 + Use canonical 行）：Use 行 semver 括号 1.15.11→映射 1.17.32；commands 枚举行原样。
- README @@ -6,11（版本表 + Marketplace 措辞）：版本映射 + an older extension release 措辞。
- README @@ -121,7（Touch 行）：hidden + one-second budget 措辞。
- README @@ -129,9（fallback/hidden open 两段）：快照语义措辞（与 0447 的实现一致——0447 已取消 diagnostics 回退，此为文档对齐）。
- README @@ -148,7（/lsp/touch endpoint 行）：one-second budget 措辞。
- README @@ -236,21（源码目录树）：ASCII→树形 15 行原样。
- package.json @@ -2,7（version）：1.15.10→1.15.11 映射为 1.17.31→1.17.32 + 合同注释。

## 与 0447 的联动
- 0447 将 vscode 桥的 touch/diagnostics 实现改为 hidden open + 请求局部观察后，本 patch 将 README/VSIX 的用户文档对齐到同一语义（hidden、one-second budget、无 preview tab、无回退）——文档与实现的一致性由本 patch 完成。

## 失败修订史
- 首轮 materialize：current 头部行数误用 13 行（original 实际 10 行 header + 正文自 L12 起），产生 garbage at line 14；同时 state 449 被链式清理导致已编辑文件丢失。
- 恢复：重放三文件编辑脚本（VSIX/README/package.json 的替换均为确定性字符串映射），current 以 10 行 header 重建，materialize 通过。
## 行级触点清单（供审计核对 state 450）
- sdks/vscode/package.json：L5 version=1.17.32；L6 // 键含 #450 合同条目。
- sdks/vscode/README.md：L9 版本表 1.17.32；L10 Recommended CLI 1.17.32-smark（不变）；L13 an older extension release；Touch 行 hidden 措辞；fallback→snapshot 段；hidden open 段；/lsp/touch 行 budget 措辞；源码目录树树形字符。
- docs/VSIX-Packaging.md：L13 版本表 1.17.32；L161 Use 行 semver 括号 1.17.32；payload 目录树树形字符。
- 无生产代码变更（package.json version 为清单元数据）；无 schema/migration 变更。
## Round-3 审计修订（B-01/B-02/G-01 关闭）
- 审计发现：VSIX-Packaging.md 的 @@ -113,7（step 5 诊断语义：empty provider result → current VS Code snapshot）与 @@ -178,19（sdks/vscode/src/ 目录树 ASCII→树形 15 行）两个 hunk 未迁移，且 Round-2 的逐 hunk 映射表对这两处做了错误描述（@@ -113 被写成 tree 行、@@ -178 被写成 commands/Use 行）——均为 observed 级失实。
- **修复**：current/0450 补入两个 hunk（pre-image 与目标逐字相同，直接落位，无版本映射需求）；state 450 物化文件已同步（L116 措辞更新、L180-195 树形字符，ASCII 残留 0）。
- Round-2 映射表更正：@@ -113,7 = step 5 诊断语义措辞（与 0447 snapshot 行为对齐）；@@ -158,7 = Use canonical 行 semver 括号版本（1.17.32，已正确落位）；@@ -178,19 = src 目录树树形转换（15 行）。行级触点清单同步补全（VSIX L116、L180-195）。
- 验证：typecheck 链重建至 450 全过（typecheck-0450-2026-08-30T19-43-18-449Z）。
- N-01 更正：current 实际 10321B→补 hunk 后约 11.3KB；@@ -158 的描述更正为承载 semver 括号版本变更。
## Round-4 重审修订（B-01/B-02 关闭——上轮修订被证伪后的真实修复）
- 上轮（Round-3）声称补入的两个 VSIX hunk 实际未进入 current/0450（重放脚本在链式清理后于错误基线执行，随后 current 被重建覆盖），state 450 的 L116 破损文本与 L180-195 ASCII 树为 observed 证据——审计成立。
- **真实修复**：0450 三文件编辑与 0449 修复在同一条命令内、于 materialize 448 重建的 state 448 worktree 上一并重放；VSIX 的 step5 措辞与 src 树转换均从 original/0450 以 Get-HunkPP（pre/post 数组提取）承载，避免手工转写偏差；current/0450 以 10 行 header + git diff 数组 join 组装（13031B、202 LF、11 hunks 含 @@ -178,19）。
- state 450 实证：L116 = "reports the current VS Code snapshot; an empty result means no diagnostics were found in that snapshot"（source post 逐字，无 "reports it;" 残留）；L181-195 树形字符、ASCII 残留 0；版本表/L161 = 1.17.32；package.json = 1.17.32 + #450 合同注释。
- Round-2/3 段中与本次事实冲突的映射描述以本节与本表为准：VSIX 5 hunks = @@ -10 版本表 / @@ -77 payload 树 / @@ -113 step5 诊断语义 / @@ -158 Use 行 semver / @@ -178 src 树树形。
## Round-5 修订（第三轮逆向 B-01 关闭）
- 第三轮逆向审计发现 @@ -113 post 行缺行尾句号（current L54 len=179 vs original L55 len=180，char-level diff 唯一差异为末尾 `.`）——Round-4 修复时的 Replace 模式未含句尾句号所致；"source post 逐字" 声明因此失实。
- **修复**：current/0450 与 state 450 的 L116 均已补回句尾 `.`（char-level：与 original L55 完全一致）；materialize 450 重放通过（passed 450/450），typecheck 链刷新（全 30 包 passed、No failures、install reused 813972ec）。
- 本节为 Round-5 终态：VSIX 5 hunks 全齐且逐字（含句号）；E/C 口径不变（E=1/C=1）。