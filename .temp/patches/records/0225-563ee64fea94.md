# Patch 0225 — 563ee64fea94 — fix: 统一使用小写 auto agent 标识符

## 修改内容

### 纯重命名："Auto" → "auto"
- `packages/opencode/src/agent/agent.ts`：
  - `EXPLICIT_ONLY_PRIMARY_AGENT_NAMES` set: `"Auto"` → `"auto"`
  - agents record key: `Auto:` → `auto:`
  - name field: `name: "Auto"` → `name: "auto"`
  - 注释中 "Auto" → "auto"（保持 #201/#203 标记）
- `packages/opencode/src/permission/auto.ts`：
  - `input.metadata.agent === "Auto"` → `=== "auto"`（功能变更）
  - 注释中 "Auto" → "auto"
- `packages/opencode/src/permission/reviewer/service.ts`：
  - `return metadata.agent === "Auto"` → `=== "auto"`（功能变更）
  - 注释中 "Auto" → "auto"
  - 不触碰 UI 显示标题如 "Auto permission review"（非 agent 标识符）
- `packages/opencode/test/agent/agent.test.ts`：
  - `toContain("Auto")` → `toContain("auto")`
  - `svc.get("Auto")` → `svc.get("auto")`
  - `expect(agent).toBe("Auto")` → `"auto"`
  - `default_agent: "Auto"` → `"auto"`
  - 测试名中 "Auto" → "auto"
- `packages/opencode/test/permission/auto.test.ts`：
  - `agent: "Auto"` → `agent: "auto"`（2处）
  - 测试名中 "native Auto" → "native auto"
- `packages/opencode/test/permission/reviewer-prompt.test.ts`：
  - `agent: "Auto"` → `agent: "auto"`（2处）
  - 不触碰 `title: "Auto permission review request"`（测试文本内容，非 agent 名）
- `packages/opencode/test/session/prompt.test.ts`：
  - `agent: "Auto"` → `agent: "auto"`（2处）
  - 不触碰 `title: "Auto compact"`（session 标题，非 agent 名）

### 丢弃的 hunk
- `packages/opencode/test/tool/shell.test.ts`：源 patch 重命名 `agent: "Auto"` 和测试名，但 tip 中该 SSH-private-key 测试已被移除/重构。仅剩一个 #203 注释中的 "Auto" → "auto" 替换。丢弃整个 hunk。

## 不一致与修复
- 无行为逻辑变更——纯机械重命名
- shell.test.ts hunk 丢弃：tip 中对应测试已被移除，重命名目标不存在
- UI 显示标题不触碰：源 patch 未修改这些，它们不是 agent 标识符

## E/C
- E = 20（代码/测试行修改）
- C = 32（#225 中文注释解释重命名原因和丢弃 hunk 理由）
- need = max(1, ceil(20 * 0.15)) = 3
- gate = PASS（C >= need，余量 29）

## 注释位置
- agent.ts：EXPLICIT_ONLY 重命名原因、auto key/name 一致性
- auto.ts：metadata.agent 比较值变更
- service.ts：metadata.agent 比较值变更
- 各测试文件：重命名覆盖说明

## Source
- SHA: 563ee64fea94
- 意图：统一使用小写 "auto" 作为 agent 标识符，保持 EXPLICIT_ONLY ↔ agent-key ↔ metadata.agent 不变量

## 上游
- tip 使用 #201/#203 标记的注释——重命名仅替换 "Auto" → "auto"，保持 #XXX 标记

## 目标
- 7 个文件（3 生产 + 4 测试）
- shell.test.ts hunk 丢弃（测试已移除）

## Current
- SHA-256: 6cef186f791ee15d
- Bytes: 18756

## 安装
- typecheck-0225 通过：30/30 workspaces passed
- install reused: 6caf0c192b005ee2771bebc192462cc82c634b63eda1d115a6fa95da3a14fddb

## 测试
- typecheck: PASS

## 失败修订
- 无（机械重命名首次即通过 typecheck）

## 最终行为
- agent key/name/EXPLICIT_ONLY/metadata.agent 全部统一为小写 "auto"
- auto-routing 的 metadata.agent === "auto" 比较与 agent key 一致
- UI 显示标题不受影响
- shell.test.ts 中的废弃 hunk 丢弃（测试已移除）
