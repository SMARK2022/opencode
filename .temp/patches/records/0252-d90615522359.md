# Record 0252-d90615522359

## Status
PASS

## 修改内容
feat(vscode): 更新笔记本编辑逻辑，确保 language 仅编辑时源保持不变，拒绝空数组作为源参数。

### 回退发现
- current 曾为 original 原样（适配版丢失）。本轮在临时 LF 仓（D:\Temp\opencode\t252b-*）以 reject 模式定位全部失败 hunk，逐一手工作业后 git diff 重新生成完整 current。

### Source 语义（9 文件 24 hunk）
1. **descriptions.ts**：edit 文案明确「language-only 仅在完全省略 oldCode/newCode 时保源；空串=显式清空」。
2. **bridge.ts**：newCode 参数加注释、language describe 补「omit oldCode and newCode entirely for this mode」；notebookEditView 新增 requestedCellId/cellId（insert/type-change 后暴露新句柄）；env save 分支追加通用 edit 门禁（防绕过文件写入边界）。
3. **edit.ts**：入口 rejectEmptySourceArray（拒绝空数组防静默清 cell）；typeChange 判定修复（languageId+kind 双比较，覆盖 code-cell 报 markdown 的混合态）；matchAndReplace LF 归一坐标拼装+整体还原 EOL（修复 CRLF cell 坐标错位损坏）；rejectEmptySourceArray helper；compactEditResult 暴露 updatedCellId。
4. **env.ts**：restart 用 getCommands(false)（Jupyter 隐藏命令在 public 表外）。
5. **source.ts**：字节溢出换页不吞行；单行超 16KB 时行号前缀+UTF-8 有界预览；takeUtf8Prefix helper。
6. **commands.ts/summary.ts**：prompt/提示语文案对齐新契约。
7. **测试**：bridge.test 2 个新测试（save 门禁 V2 Promise-ask 化 + 句柄轮换）；nb-summary.test 8 个新测试（空数组拒绝、language-only 保源、混合态 kind、CRLF cell、单行有界预览、分页不吞行、隐藏命令 restart）+ fixture（commandLists/executedCommands/extensionLookup、UriLike.scheme、eol as number）。

### 目标适配
- 11 hunk 干净应用 + 13 hunk 因早期 SMARK patch 的中文注释/结构微调（#105/#223/#224/#233/#234、break outer、timeout 变量化、envArgs 内联）重切。
- bridge.test 测试 V2 化：`async ask` + throw（替代 Effect.die）；`plugin.tool!` + 模块级 plugin。
- CRLF 宿主由脚本的 apply 前 LF 规范化处理（250 引入）。

## E/C
- E = 259（plus - commentPlus + minus）
- C = 39（#252 中文契约注释：edit.ts 8、bridge.ts 3、env.ts 3、source.ts 5、descriptions 2、commands 2、nb-summary 测试 12、bridge.test 4——覆盖空数组歧义、kind 混合态、CRLF 坐标、句柄轮换、隐藏命令面、分页不吞行、门禁顺序等真实 invariant）
- need = ceil(259 * 0.15) = 39 → 余 0 PASS

## Source
- original/0252-d90615522359.patch: 572 行 9 文件。

## Current
- current/0252-d90615522359.patch: 36715 bytes, 24 hunk / 9 文件
- SHA-256: 42732f53711a18fad550252426e88a1d059076507f2c096885431b23d4aba427

## 安装
- materialize 252 --rebuild: passed 252/252

## 测试
- typecheck 252: 全部 workspace passed
- `bun test --timeout 120000 test/plugin/vscode-bridge.test.ts test/plugin/vscode-notebook-tool-summary.test.ts`（packages/opencode）: 21 pass / 0 fail（含 10 个新测试与既有回归全绿）

## 修订历史
1. reject 模式收集 13 个失败 hunk（临时 LF 仓 t252）。
2. 逐文件手工适配（行级/字节级；read 工具方括号包装误判已由字节码流程取代）。
3. template literal 反引号在 shell 转义中丢失 → 用 [char]96 重构。
4. env.ts 漏改定位（base commit 时序混淆）→ side 目录重做。
5. C 分三轮补齐 5→19→34→39（生产 15 + 测试 24 处分散契约注释）。
6. 最终：materialize OK、typecheck OK、21/0、C=39=need → PASS。

## 三方比较
- Source：notebook 编辑的空数组/混合态/CRLF 三类数据完整性修复 + 句柄轮换 + 权限/分页/隐藏命令边界。
- Upstream v1.17.20：notebook 模块在 sdks/vscode（结构一致），bridge 在 packages/opencode（#223/#224/#233/#234 谱系叠加）。
- Target：全部 24 hunk 语义落地；测试 V2 化（Promise ask）。无行为丢失、无重复 owner、V1/V2 无冲突。

## 最终行为
- 空数组源参数在变更前被拒绝；空串=显式清空；字段省略=language-only 保源（含 kind）。
- CRLF cell 的字符串替换坐标正确且 EOL 保持。
- insert/type-change 后 metadata 暴露新句柄+原请求句柄。
- save 分支服从通用 edit 门禁；restart 检查全量命令面。
- 单行超预算有界预览、分页不吞行、隐藏 Jupyter 命令可 restart。

## 批次双审结论（2026-09-07，250-254）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：current 文件头含 8 行生成期 warning 噪声（git apply 忽略，无损）；source.ts 顶部继承的 #234 注释措辞在有界预览语义下轻微失真（下次触碰时对齐）。