# 0252-d90615522359 record — feat(vscode): 更新笔记本编辑逻辑

## Manifest
- index 252, sha `d90615522359203384f2dd8929ba2e5b725d803d`, parent `a78bedf5a562` (0251)
- subject: feat(vscode): 更新笔记本编辑逻辑，确保语言仅编辑时源保持不变，拒绝空数组作为源参数

## Original 身份
- original/0252-d90615522359.patch: 31437 字节, 9 files 23 hunk（vscode-bridge.ts、vscode-bridge-descriptions.ts、2 测试、sdks/vscode 5 文件）

## Source 行为（逐 hunk）
1. **descriptions.ts**：edit 描述——语言仅编辑只有在 oldCode/newCode 完全省略时保留源；空字符串 = 显式空源。
2. **bridge.ts** h1：editArgs language describe + 注释（语言仅编辑 = 省略源字段，非空值）；h2：notebookEditView cellId = insertedCellId ?? updatedCellId ?? requestedCellId + requestedCellId 不同时保留；h3：env save 操作必须过通用 edit 门禁。
3. **bridge.test.ts**：2 新测试（save 门禁、cellId 句柄）。
4. **notebook-summary.test.ts**：夹具扩展（commandLists public/all 分离、executedCommands、extensionLookup）+ 7 新测试（空数组拒绝、语言仅编辑保源、kind 混合态、CRLF 保留、超长行有界前缀、换页行不提前消费、restart 隐藏命令）+ 类型（eol as number、createUri scheme、UriLike scheme）。
5. **commands.ts**：newCode prompt 文案（空串 = 显式空源；语言仅编辑走 bridge API）。
6. **edit.ts** h1：editNotebook 入口 rejectEmptySourceArray（oldCode/newCode）；h2：typeChange 含 kind 比较 + newKind!；h3：matchAndReplace LF 坐标切片 + EOL 还原；h4：rejectEmptySourceArray helper；h5：compactEditResult 加 updatedCellId。
7. **env.ts**：getCommands(true) → (false)（Jupyter 隐藏命令）。
8. **source.ts** h1：单行超预算——renderedLines>0 整行留页；首行超预算输出行号 + takeUtf8Prefix 有界前缀；h2：takeUtf8Prefix helper。
9. **summary.ts**：next-step 提示去掉默认 offset=1。

## 目标适配（Round 1）
- **sdks/vscode 5 文件**：语义完整移植（commands 文案、env getCommands(false)、summary offset 提示、source 前缀分支 + takeUtf8Prefix、edit 5 hunk）。
  - source.ts：端口已有 `break outer`（#94）→ 新逻辑用 `break outer`；端口已有"仅字节预算设 bytesCut"块 → 替换为 source 的 renderedLines>0 分支 + 首行前缀分支。
  - edit.ts：typeChange 加 kind 比较（newKind 前置计算）；matchAndReplace 用 src（端口已有 LF 归一化 src 变量）切片 + `documentEol` 还原；compactEditResult 加 updatedCellId。
- **bridge.ts**：editArgs language describe + 注释；notebookEditView cellId 句柄；env save 门禁（`if (args.operation === "save") await ask(context, "edit", args)`）。
- **descriptions.ts**：edit 文案。
- **2 测试文件**：语义保留 + 适配：
  - bridge.test.ts：`VscodeBridgePlugin()` → `VscodeBridgePlugin({} as unknown as PluginInput)`（目标签名需参数）；ask 用目标 throw 模式；allowContext 已有。
  - notebook-summary.test.ts：夹具（commandLists/executedCommands/extensionLookup/getCommands(filterInternal)/getExtension/beforeEach reset）+ 7 测试 + eol as number + createUri scheme + UriLike scheme 全部移植（#252 中文注释）。
- **错误修复**：bridge.test 2 处 VscodeBridgePlugin 参数；251 的 compaction.test 用 `MessageV2.CompactionPart` → `SessionV1.CompactionPart`（目标 message-v2.ts 的 CompactionPart 从 core import，非导出——已同步修 251 patch）。

## 生成方式
- gen-252a/b/c/d.py：复制 251 状态 → 逐文件语义替换 → git diff。
- current/0252-d90615522359.patch: 28766 字节, 9 entries, SHA-256: 4725314fb0b4636abb7b2841f2f009dc2ae3fb70b9128b4c012c1d30b5239244

## 验证
- apply-cumulative --materialize 252: passed 252/252
- vscode-bridge.test.ts + notebook-tool-summary.test.ts: 21 pass 0 fail（108 expect）
- opencode typecheck 通过

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0252-d90615522359

## E/C 最终（count-ec.py）
- E=207, C=39, need=32, PASS（#252 注释 39 条：语言仅编辑省略源、typeChange kind、LF 坐标、前缀分支、takeUtf8Prefix、夹具、测试意图）

## 验证（最终）
- materialize 255: passed 255/255（累计）
- vscode-bridge + notebook-summary 21 pass（108 expect）
- opencode typecheck 通过
- typecheck-0255 报告：.temp/patches/reports/typecheck-0255-2026-08-08T15-01-13-533Z.md（install 指纹 c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c；packages/app pre-existing TS1128 失败，0247 已记录 baseline 缺陷；opencode/tui typecheck 手动验证通过）

## DUAL-AUDIT SEAL — 批次 251-255（重审通过）
- 正向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；G-01（typecheck-0255 报告）、G-02（records ≥50 行/≥3000 字符）、G-03（0252 身份行 28766B/4725314f）均已验证修复。
- 逆向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；仅 N-01~N-04 记录级观察（seal 格式、自动 typecheck 覆盖、sdks/vscode node-gyp 环境、lockfile 差异）。
- 两份结果一致，批次 251-255 放行。
