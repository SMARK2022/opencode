# Record 0344-a29ede2abb22

## Identity
- SHA: a29ede2abb221c8bbfebba203e48f1bb5e656863
- Parent: a1a2952e2c03 (0343)
- Subject: fix(tui): 修复 BlockTool preview/body 隐藏区域泄漏首行导致命令重复显示
- Status: PASS

## Source intent

### Hunk 1 — `@@ -2410,10 +2410,12 @@` (index.tsx, BlockTool comment update)
The upstream commit (a29ede2ab) refines the hiding strategy introduced by 0343 (a1a2952e2c). The 0343 approach used `maxHeight=0 + overflow=hidden` to hide preview/body when collapsed/expanded. However, OpenTUI's `updateFromLayout` applies `Math.max(layout.height, 1)` which forces a height of 0 to 1, leaking the first line of the hidden region. For shell tool blocks where both preview and body contain `$ command`, this causes the command to appear twice on screen.

The fix: replace `maxHeight=0 + overflow=hidden` with `visible={false}` (display:none), which removes the node from the layout tree entirely while keeping it mounted (preserving 0343's fix for collapse→expand→collapse blank content). No-preview blocks (TodoWrite/Question) still use `maxHeight=previewLines` clipping.

### Hunk 2 — `@@ -2462,23 +2464,23 @@` (index.tsx, BlockTool JSX prop changes)
Three concrete prop changes:
- **Preview box**: `maxHeight={collapsed() ? undefined : 0}` + `overflow={collapsed() ? undefined : "hidden"}` → `visible={collapsed()}`
- **Body box**: `maxHeight={collapsed() ? (hasPreview() ? 0 : previewLines()) : undefined}` + `overflow={collapsed() ? "hidden" : undefined}` → `visible={!collapsed() || !hasPreview()}` + `maxHeight={!hasPreview() && collapsed() ? previewLines() : undefined}` + `overflow={!hasPreview() && collapsed() ? "hidden" : undefined}`

The body box now has THREE props: `visible` controls display for preview-having blocks, while `maxHeight`/`overflow` only apply to no-preview blocks (the TodoWrite/Question fallback path).

### Test file hunk — `@@ -1338,6 +1338,65 @@` (session-message-render.test.tsx)
Adds two regression tests:
- "non-collapsed shell block shows command once": verifies `$ echo hello` appears exactly once in non-collapsed state (would fail under maxHeight=0)
- "expanded shell block does not leak preview first row": verifies `$ echo test` appears once in expanded state (would fail under maxHeight=0)

## Three-way comparison

| Aspect | SMARK original | Upstream (dev) | Target (0340+0343 state) |
|---|---|---|---|
| Source path | `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` | same | `packages/tui/src/routes/session/index.tsx` (migrated per 0299) |
| Test path | `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` | same | **Does not exist** in target state |
| Pre-patch hiding strategy | `maxHeight=0 + overflow=hidden` (from 0343) | same | `maxHeight=0 + overflow=hidden` (from 0343 adaptation) |
| `collapsed` memo | exists (pre-existing) | exists | Introduced by 0343 adaptation (not in upstream baseline) |

## Target adaptation

### Path mapping
- Source: `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx` (migrated per 0299)
- Test: **SKIPPED** (file does not exist in target state, same as 0343)

### Comment adaptation
The upstream changes three comment lines to reference `visible` instead of `maxHeight=0 + overflow=hidden`. Since the 0343 adaptation introduced the `collapsed` memo and the comment text verbatim from upstream, the 0344 comment changes are a direct 1:1 mapping — the oldString text matches the 0343-adapted code exactly.

### JSX prop adaptation
The 0343 adaptation introduced the preview/body JSX structure using `collapsed()` verbatim from the upstream. The 0344 prop changes (`maxHeight/overflow → visible`) apply directly to this structure — no further adaptation needed beyond the path mapping.

### Test file skip
Same as 0343: the test file `session-message-render.test.tsx` does not exist in the target state. The two new regression tests ("non-collapsed shell block shows command once" and "expanded shell block does not leak preview first row") are skipped.

## Per-hunk mapping table

| Hunk | Upstream file | Target file | Adaptation |
|---|---|---|---|
| 1 (comments) | `index.tsx:2410` | `index.tsx:2729` | Comment text replacement (1:1, matches 0343-adapted code) |
| 2 (preview props) | `index.tsx:2464` | `index.tsx:2789` | `maxHeight/overflow` → `visible` (1:1) |
| 2 (body props) | `index.tsx:2464` | `index.tsx:2801` | `maxHeight/overflow` → `visible + conditional maxHeight/overflow` (1:1) |
| 3 (test 1) | `session-message-render.test.tsx:1338` | **SKIPPED** | Test file not migrated |
| 4 (test 2) | `session-message-render.test.tsx:1370` | **SKIPPED** | Test file not migrated |

## Verification

- **Materialize**: `passed 344/344` — patch applies cleanly to state 0343
- **E/C**: E=6, C=4, need=1, **PASS** — the 3 updated CJK comment lines (visible strategy explanation) cover the 6 executable prop lines (`visible={collapsed()}`, `visible={!collapsed() || !hasPreview()}`, `maxHeight={...}`, `overflow={...}`)
- **Typecheck**: packages/tui passes `tsgo --noEmit` (exit 0)

## 不一致 (target structure differences)

1. **Test file not migrated**: Same as 0343 — `session-message-render.test.tsx` does not exist in the target. The two regression tests for command-duplicate verification are skipped. The source fix (visible prop) is complete.

2. **`collapsed` memo provenance**: In the upstream, `collapsed` was a pre-existing memo (from before 0343). In the target, it was introduced by the 0343 adaptation. Since 0344 modifies code that 0343 added (the preview/body props), and the 0343 adaptation used the upstream's code verbatim, the 0344 changes apply without friction. The `collapsed()` references in the new `visible` props work correctly because the memo was properly introduced by 0343.

3. **No-preview fallback path**: The body box's `maxHeight={!hasPreview() && collapsed() ? previewLines() : undefined}` preserves the no-preview clipping path for TodoWrite/Question blocks. This is identical to the upstream's logic and correctly falls back to the 0340 target's pre-existing `previewLines()` behavior for blocks without a preview.
