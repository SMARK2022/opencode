# Record 0228-a672bf686278

## Status
PASS（materialize 228/228，typecheck 230 全 30 workspace，E/C gate PASS，第三轮双审计通过并封印）

## 修改内容
session list 路径切换：home 目录全局视图 + Windows 路径归一化。
- `packages/opencode/src/session/session.ts` —
  - relatedDirectoryConditions 重构为 comparableDirectoryColumn/comparableDirectoryValue（SQL 侧 rtrim+replace 反斜杠→斜杠、去尾部斜杠、win32 lower；JS 侧同构归一化）
  - 新增 homeDirectoryCondition（精确匹配 Global.Path.home 归一化值）、isHomeDirectory（请求目录==home）
  - listByProject 条件构造：home 分支（includeHomeSwitchScope && isHomeDirectory）为**空体**——home 是全局切换枢纽，不添加任何 project/path 谓词，使 search/start/limit 跨所有 session 应用；else 分支（非 home path 查询）在 conditions.push 的 OR 中 union `...(includeHomeSwitchScope ? [homeDirectoryCondition()] : [])` 臂
  - listByProject 的 `.where(and(...conditions))` 改为 `.where(conditions.length > 0 ? and(...conditions) : undefined)`——home 切换查询可能无 project/path 谓词，undefined 表示无 WHERE
  - 保留 #143 的 projectPath/globalPath/nullPath/useDirectoryFallback 全部既有臂与 listGlobal 的原始 `.where(and(...conditions))`（source 未修改 listGlobal）
- `packages/opencode/test/server/session-list.test.ts` — 新增测试：home 跨 project 可见性、home 保留 search/limit、home 视图外精确 home 会话、directory-only 作用域、Windows 拼写等价（withPlatform+withTestHome+DB 直插历史行）；新增辅助 initGitRepo/tempGitRepo/tempDirectory/withTestHome/withPlatform

## Source 行为意图
1. 归一化规则：反斜杠→正斜杠、去尾部斜杠、win32 统一小写（SQL lower 与 JS toLowerCase 两侧一致）。
2. home 作为全局切换枢纽：请求目录是 home 时（非 project 作用域）不添加 project/path 谓词——workspace/start/search/roots/limit 谓词保持，path/project 边界放宽。
3. 非 home 的 path-scoped 查询 union 精确 home 会话（homeDirectoryCondition）。
4. directory-only 调用保持 legacy 分支的精确目录行为。
5. Windows 拼写等价测试：DB 直插 `C:\Users\Alice` / `D:/Work/Repo` 历史行，win32 分支下与 `c:/users/alice/` 请求等价。

## 不一致（source vs target 适配）
- `@/project/project.sql` 与 `@/project/schema` 在目标不存在 → `ProjectTable` 改 `@opencode-ai/core/project/sql`；`ProjectID` 改 `Project`（@opencode-ai/schema/project）并用 `Project.ID.make(...)`。
- `Database.use((db) => ...)` 静态方法在目标不存在 → `Database.Service.use(({ db }) => ...)`。
- **effect-drizzle 的 `.run()`/`.all()` 返回 Effect**（非同步执行）→ DB 种子块必须 `Effect.gen(function* () { yield* db.insert(...).run(); ... })`；addFinalizer 内 use 结果 `.pipe(Effect.ignore)`（回调错误通道必须为 never）。
- ProjectTable.worktree 列为 AbsolutePath brand → insert 用 `AbsolutePath.make(...)`。
- 测试辅助函数（initGitRepo/tempGitRepo/tempDirectory/withTestHome/withPlatform）构建时丢失 → 从 original 完整恢复，补 `import { $ } from "bun"` 与 `import { InstanceRef } from "@/effect/instance-ref"`。

## 修复过程（审计驱动）
1. 首次构建缺 5 个测试辅助函数定义 → 从 original 恢复。
2. session-list.test.ts 的 Database.use 类型错误 → Database.Service.use + Effect。
3. worktree 字符串不匹配 AbsolutePath brand → AbsolutePath.make 包装。
4. **B1（审计）**：home 分支语义反转——当前实现 push homeDirectoryCondition()（只匹配精确 home 会话），source 意图是空体（全局 hub）→ 改为空体 + listByProject `.where(conditions.length > 0 ? and(...) : undefined)`。
5. **B1（审计）**：else 分支缺 home-union 臂 → 在 conditions.push 的 OR 中、`)!` 之前加入 `...(includeHomeSwitchScope ? [homeDirectoryCondition()] : [])`。
6. **B2/B3（审计）**：Windows 测试 DB 种子从未执行——`Database.Service.use(...)` 无 yield* 且 `.run()` 返回 Effect → 改为 `yield* Database.Service.use(({ db }) => Effect.gen(function* () { yield* ... }))`；addFinalizer 内 use 加 `.pipe(Effect.ignore)`。
7. 修复中曾误改 listGlobal 的 `.where(and(...conditions))` → 恢复语义（source 只修 listByProject；材料化状态中 listGlobal 仍保留一处纯空白缩进差异，无行为影响，审计判定 N 级）。
8. 调试确认：effect-drizzle `.run()`/`.all()` 均为 Effect API（driver.ts: execute: () => Effect），DB 种子必须 yield；aliases 对 path="" 返回空数组使 projectPath=or()=undefined，directory-fallback 臂承担匹配（测试验证通过）。

## E/C
- E=361-365（排除注释/空行），C=55-56（#228 中文注释：session.ts 19 处 + session-list.test.ts 36 处），need=55。Gate PASS。

## 注释位置
- session.ts：目录列/JS 值归一化规则、win32 大小写、home 条件精确匹配、home 全局枢纽语义、祖先链匹配、子目录通配、nullPath 兜底、directoryMatchesPath、.where undefined 语义等。
- session-list.test.ts：home 切换场景意图、Windows 拼写等价、DB 直插历史行、withTestHome/withPlatform 作用域、各测试标题意图等。

## Source
- SHA: a672bf686278
- 意图：feat(session) 增强路径切换逻辑，支持全局路径和目录回退，添加临时 Git 仓库测试。

## Upstream
- 目标 v1.17.20 已有 #143 的 path/related/global OR 逻辑；228 在其上新增 home 展开分支与归一化辅助，保留全部既有臂与 listGlobal 行为。

## Target/Current
- 2 文件；current patch 约 23-24 KB（含适配注释与 DB Effect 修复）。

## 安装/测试
- materialize 228 PASS；typecheck 230 全 30 workspace PASS。
- 测试：`bun test test/server/session-list.test.ts` 19/19 pass（含 5 个新测试与全部既有回归）。

## 失败修订
- 见修复过程 1-8。

## 最终行为
- home 目录请求（非 project 作用域）展开为跨 project 的 home 会话列表（无 project/path 谓词，search/start/limit 全局生效）；非 home path 查询 union 精确 home 会话；Windows 拼写变体（反斜杠/大小写/尾部斜杠）命中同一会话；directory-only 列表保持精确目录作用域；历史无 path 行回退 directory 匹配。

## DUAL-AUDIT SEAL (FINAL, batch 226-230, round 3)
- Sealed: 2026-08-05
- FORWARD: PASS；REVERSE: PASS（audit-226-230-forward/reverse-20260805T233532.md）
- Tip: states/0230-f81ae0e09344
- Typecheck: 30/30；Tests: session-list 19/19（含 5 个新测试）
- E/C：E=324/C=56>=need 49（记录口径 E=365/C=56>=need 55 同样通过）
- N 级：listGlobal .where 残留一处纯空白缩进 hunk（source 未触碰，零行为影响）
- 0228: PASS
