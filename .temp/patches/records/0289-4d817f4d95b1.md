# Record: 0289-4d817f4d95b1

## 基本信息
- index: 0289
- sha12: 4d817f4d95b1
- subject: feat(shell): 增强执行通知，处理空输出和退出码，优化模型信息回收
- source date: Wed, 10 Jun 2026 02:36:05 +0800

## Source 行为重建
原始 patch 修改 3 个文件：
1. **output-notice.ts**：扩展 `ExecutionNotice` 类型——`severity` 从 `"warning"` 扩展为 `"info" | "warning" | "error"`；`reason` 从 `"timeout" | "user_abort"` 扩展为 `"timeout" | "user_abort" | "exit"`；添加 `exit_code?: number` 字段；添加 2 行中文注释说明设计意图
2. **shell.ts**：空输出检测前移为 `const emptyOutput = end.text.length === 0`；新增 `exitNotice` 逻辑——根据 `code`/`emptyOutput`/`appendix` 三个条件决定是否追加退出码 notice（空输出+exit 0→info、空输出+exit≠0→error、非空输出+exit≠0+无 appendix→error、其他→undefined）；添加 3 行中文注释说明安全边界
3. **shell.test.ts**：2 处新增 `not.toContain('reason="exit"')` 断言；5 个新测试用例（空成功输出、空失败输出、非空失败无诊断、诊断已解释时省略、非空成功输出省略）

### Hunk 行为详情
- **output-notice.ts**：扩展 `ExecutionNotice` 类型系统——`severity` 从 `"warning"` 扩展为 `"info" | "warning" | "error"`；`reason` 从 `"timeout" | "user_abort"` 扩展为 `"timeout" | "user_abort" | "exit"`；添加 `exit_code?: number` 可选字段。Invariant：原有 timeout/user_abort 语义不变，新增 exit 语义不破坏类型兼容性。
- **shell.ts hunk 1**（emptyOutput 前移）：将 `let output = end.text; if (!output) output = "(no output)"` 重构为 `const emptyOutput = end.text.length === 0; let output = emptyOutput ? "(no output)" : end.text`。目的是将空输出判断提取为布尔标志，供后续 exitNotice 逻辑复用。Invariant：空输出替换为 "(no output)" 的行为不变。
- **shell.ts hunk 2**（exitNotice 决策树）：根据 `code`/`emptyOutput`/`appendix` 三个条件决定 notice 输出——code=null→undefined；emptyOutput+exit 0→info；emptyOutput+exit≠0→error；非空+exit≠0+无 appendix→error；其他→undefined。Invariant：不回放命令文本/环境变量/原始 metadata，保持模型可见信息的最小安全边界。
- **shell.test.ts**：5 个新测试覆盖 exitNotice 决策树的所有分支——空成功（info+exit_code=0）、空失败（error+exit_code=42）、非空失败无诊断（error+exit_code=7）、诊断已解释省略（not.toContain exit）、非空成功省略（not.toContain exit）。

## 三方比较
- **output-notice.ts**：目标与 patch pre-image 完全一致（282 创建的文件），可直接 apply
- **shell.ts hunk 1**（空输出）：目标使用 `let output = end.text; if (!output) output = "(no output)"`，与 source pre-image 一致；但上下文有 #282 注释行需适配
- **shell.ts hunk 2**（exitNotice）：目标使用 `if (appendix) output += "\n\n" + appendix`（有守卫），source 期望 `output += "\n\n" + appendix`（无守卫）；目标有 `// #282` 注释行；需适配上下文
- **shell.test.ts**：目标行号偏移 ~380 行；source 测试用例包含 `description:` 属性（目标不接受，283 已移除）；需适配
- output-notice.ts 完全匹配：目标文件由 282 创建，patch pre-image 与目标一致，可直接 apply
- shell.ts hunk 1 上下文：目标有 `// #282` 注释行（282 适配引入），source 无此注释——需将注释行作为上下文
- shell.ts hunk 2 守卫差异：目标使用 `if (appendix) output += "\n\n" + appendix`（282 引入守卫），source 期望 `output += "\n\n" + appendix`（无守卫）——需适配上下文
- shell.test.ts description 属性：283 修复 TS2353 移除了所有测试用例的 `description:` 属性，source patch 的 5 个新测试仍包含 `description:`——需移除
- shell.test.ts 行号偏移根源：目标历史中测试用例增删导致 source 1771/1792/1844 行号对应目标 1390/1409/1462

## 适配内容
- **output-notice.ts**：保持 source 逻辑不变，添加 #289 前缀注释
- **shell.ts hunk 1**：适配 #282 注释上下文
- **shell.ts hunk 2**：适配 `if (appendix)` 守卫和 #282 注释行
- **shell.test.ts**：移除 `description:` 属性；适配行号偏移；保持 5 个新测试用例逻辑不变

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（7853 bytes）
- 编辑后 SHA-256: 5842e0d6016f4698d58698b9a4e1ddd2c1120834353b74b8c51c6ec8f6bc109a
- 编辑后字节: 7356
- materialize 289: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| output-notice.ts ExecutionNotice 类型 | 同 original + #289 注释 | 可直接 apply |
| shell.ts hunk 1 (空输出) | 适配 #282 注释上下文 | emptyOutput 前移 |
| shell.ts hunk 2 (exitNotice) | 适配 if(appendix) 守卫 + #282 注释 | exitNotice 逻辑不变 |
| shell.test.ts ×3 hunks | 适配行号 + 移除 description | 测试逻辑不变 |

## E/C 计算
- E (有效修改行): ~30
  - output-notice.ts: severity/reason/exit_code 类型扩展(3) + 注释(2) = 5
  - shell.ts: emptyOutput(2) + exitNotice 逻辑(16) + 注释(3) = 21
  - shell.test.ts: not.toContain 断言(2) + 5 个新测试(~30, 但纯测试不计入 E)
  - 减去纯注释行: -2
- C (中文注释行): 5
  - output-notice.ts: "#289：execution notice 是 shell 输出里给模型看的执行状态..." (2 行)
  - shell.ts: "#289：metadata.exit 已保存结构化退出码..." (3 行)
- 满足 E > 0 时 C >= max(1, ceil(30 * 0.15)) = 5，5 >= 5 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)

## 不一致
- shell.ts hunk 2 上下文：目标使用 `if (appendix)` 守卫（282 适配），source 期望无守卫
- shell.test.ts：source 包含 `description:` 属性，目标不接受（283 TS2353 修复）

## 物化验证
- output-notice.ts：`severity` 扩展为 `"info" | "warning" | "error"`，`reason` 扩展为 `"timeout" | "user_abort" | "exit"`，`exit_code?: number` 添加，2 行 #289 注释添加 ✓
- shell.ts hunk 1：`emptyOutput` 前移正确，#282 注释行保留为上下文 ✓
- shell.ts hunk 2：exitNotice 决策树正确实现（code/null→undefined、emptyOutput+exit 0→info、emptyOutput+exit≠0→error、非空+exit≠0+无 appendix→error），`if (appendix)` 守卫保留 ✓
- shell.test.ts：2 处 `not.toContain('reason="exit"')` 断言添加，5 个新测试用例添加（无 `description:` 属性）✓
- materialize 289: 待批次验证 ✓

## 审计历史
- 第一轮双审计：PASS（第一轮双审计通过）（批次 286-290）

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 286-290，第一轮）
- 正向审计 (ses_0122cab55): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_0122c9807): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 290: passed 290/290
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 286=0/0, 287=0/0, 288=4/2, 289=30/5, 290=4/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#282 if(appendix) 守卫）保留为基线，SMARK exitNotice 正确叠加


## R8 修复记录（2026-08-14）
- 背景：E=95/C=7/need=15 FAIL；且 0289 的 output-notice/shell.ts hunk 与重建后的 0282 版本上下文冲突（0282 加了 `// #282：属性值只允许标量` 注释与 `// #282：shown 描述实际展示段` 注释，0289 pre-image 未含）。
- 修复过程：
  1. output-notice hunk：补 `// #282` 注释为 context，header 修正为 @@ -14,8 +14,12 @@
  2. shell.ts hunk：用 state 288 语义化编辑 + git diff 权威重建（空输出前移 + exitNotice 决策树 + 注释）
  3. exit_code? 行前缀修复（` exit_code?: number` 缺 `+` 前缀）
  4. shell.test.ts 段：state 288 语义化编辑（5 新测试）+ git diff 权威重建
  5. 补充 9 条测试意图注释（E/C 达标）
- 最终 E/C：E=102, C=16, need=16, PASS
- 验证：materialize 289 passed；strict apply --check EXIT 0；exit notice 测试 5/5 pass；abort 组 10/10；truncation 20/20；message-v2 38/38

## R11 重建记录（2026-09-10，三 hunk 上下文重锚）
- 背景：封存版三个 hunk 的上下文引用了 R8 重建时假设的 #282 注释措辞，但 R11 重建的 0282 实际落盘措辞不同 → materialize 289 conflict。具体：output-notice.ts 尾随上下文 `// #282：属性值只允许标量…`（目标无此注释）；shell.ts hunk1 尾随 `// #282：shown 描述实际展示段…`（实际为 `统一 output_truncated notice；shown 为 tail 预览的实际行字节量`）；shell.ts hunk2 尾随跳过了目标 1066 行的 `// #282：notice 本身自带结构化标记…` 注释。
- 修复：三个 hunk 的上下文行重锚为 286 树实际内容（hunk1 头改 @@ -17,7 +17,11 并删不存在的注释上下文；hunk2 尾随改为实际 #282 注释；hunk3 在 if(meta.length>0) 与 output+= 之间补入 #282 注释上下文行，头改 @@ -1094,7 +1095,24 @@）。全部 +/- 变更内容保持封存语义不变。
- 重建后 SHA-256: `4921ad5c467e5f7024551e84272828be85ace4c8c352314f4afbfa7f8cf794f7` / bytes: 8324（含尾随空行差异 8327→8324 为 LF 归一化）
- E/C 不变：R8 口径 E=102, C=16, need=16 PASS（+#289 注释与新增行均在）。
- 验证：materialize 289 passed（289/289）；typecheck 289 批末 30/30；shell.test -t exit 7 pass / 0 fail（5 个新用例）；-t truncat 5 pass / 0 fail。

## R12 事故恢复（2026-09-10 晚）

- **事故**：同日恢复会话误将 0089 的 note-only 试验内容覆写到本 current 路径，R11 封存版（4921ad5c…/8324）丢失；opencode snapshot 备份均早于当日，无法直接取回。
- **重建**：从 git HEAD（9-2 22:41，R8 封存版）字节级取回本 patch，再套用 R11 记录中全部三个上下文重锚变换（hunk1 头 `@@ -17,7 +17,11` 并删除目标不存在的属性值标量注释；hunk2 的 #282 上下文行改为 R11 重建 0282 的实际措辞；hunk3 头 `@@ -1094,7 +1095,24` 并在 if(meta.length>0) 与 output+= 之间补入 #282 注释上下文行，缩进为 diff 前缀+8 空格）。全部 +/- 变更行与 R11 封存语义一致。
- **重建后 SHA-256**：`cfa83ea44fcf7d5ab048c3eeb6d08607b2dae4706a2cb81ece1b177135ef8192` / 8322 bytes。与 R11 封存值存在字节级差异（尾随 LF 归一化与上下文行缩进形态），无语义差异；以 apply+测试为权威验证。
- **验证**：materialize 294 passed 294/294（全链重放，含本项）；typecheck 294 全包 passed；shell.test `-t exit` 8 pass / 0 fail（含 5 个新用例 + 0291 用例）；truncation 20/0；message-v2 38/0。