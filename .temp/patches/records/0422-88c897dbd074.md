# Record 0422-88c897dbd074

## Identity
- SHA: 88c897dbd074
- Parent: 24576475d381 (0421)
- Subject: fix(tui): 拦截畸形 SGR mouse 报告碎片注入 prompt textarea
- Status: PASS

## Source intent
source 0422（3 文件，530 行）：
1. `docs/Proposal/tui-malformed-sgr-mouse-fix.md` 新（283 行）：根因分析——某些终端/IDE/触摸屏发送畸形 SGR mouse 报告（如 ESC[<64;NaN;NaNM），OpenTUI StdinParser 的 csi_sgr_mouse 状态机遇到大写 N（CSI final byte 范围 0x40-0x7E）错误结束 CSI，剩余字节 aN;NaNM 逐字符变成 key event 插入 prompt buffer。方案 = rendererConfig 的 prependInputHandlers（OpenTUI 已有接口），handler 用恢复状态机消费畸形报告碎片。
2. `src/cli/cmd/tui/app.tsx`：新增 MALFORMED_SGR_MOUSE_PREFIX（`/\x1b\[<[;\d]*N$/`）、MALFORMED_SGR_MOUSE_TAIL（`/^[aN;0-9Mm]$/`）、MALFORMED_SGR_MOUSE_MAX_CONSUME（50）常量 + `export function createMalformedSgrMouseGuard()`（闭包状态机：idle 检测 ESC[<…N 前缀进入 recovery；recovery 逐字符消费合法 mouse 字符；M/m 完成；非法字符/多字符序列/超 50 字符退出放行）；rendererConfig 返回对象加 `prependInputHandlers: [createMalformedSgrMouseGuard()]`。
3. `test/cli/cmd/tui/malformed-sgr-mouse.test.tsx` 新（139 行）：testRender + textarea + prependInputHandlers，6 个测试（畸形 press/release 不进入 textarea、三条连续报告无碎片、合法 mouse 不受影响、普通文本正常、aN;NaNm 直输保留、recovery 遇非法字符放行）。

## Target analysis 与适配（决策）
- 路径映射：`src/cli/cmd/tui/app.tsx` → `packages/tui/src/app.tsx`。**target 无 rendererConfig 函数**（配置内联在 `createCliRenderer({...})` 调用，L219-231：externalOutputMode/targetFps/gatherStats/exitOnCtrlC/useKittyKeyboard/autoFocus/openConsoleOnError/useMouse/consoleOptions）→ 适配：在 createCliRenderer 对象内插入 `prependInputHandlers: [createMalformedSgrMouseGuard()]`；常量与 guard 定义放 app.tsx 顶层（appBindingCommands 之后，同 source 相对位置）。
- 测试：`packages/opencode/test/cli/cmd/tui/malformed-sgr-mouse.test.tsx`（与 attention.test.ts 同目录）；import 改为 `@opencode-ai/tui/app`（target 测试包名导入约定；source 用相对路径）。
- 文档：docs/Proposal/tui-malformed-sgr-mouse-fix.md 新文件 verbatim。
- OpenTUI prependInputHandlers 接口在 target node_modules（@opentui/core）存在（source 依赖的同一包）。

## E/C
- 待编辑后计算（guard ~47 行 + 测试 ~139 行）。

## Verification
- materialize / typecheck / 测试：pending（随批次继续）
## 最终验证
- materialize 422 --rebuild：passed 422/422；typecheck 422：30/30 workspaces passed（install 指纹 1165df52… frozen reuse）。
- 行为测试：`bun test test/cli/cmd/tui/malformed-sgr-mouse.test.tsx`（packages/tui）：7 tests 全过、9 expect calls（畸形 press/release 拦截、三连碎片、合法 mouse 不受影响、普通文本/aN;NaNm 保留、recovery 遇非法字符放行）。
- 适配修订：测试文件从 packages/opencode/test/cli/cmd/tui/ 移至 packages/tui/test/cli/cmd/tui/（target TUI 测试主目录；app 无 @opencode-ai/tui/app 子路径导出 → 相对 import `../../../../src/app`，与同级测试一致 4 级深度）；target 无 rendererConfig 函数 → prependInputHandlers 注入内联 createCliRenderer({...})（L219）。
- E/C：guard 生产代码约 47 行（E≈47、C≈14——前缀/recovery/上限/终止/不变量注释）；测试 141 行（E≈120、C≈10——根因/辅助/各测试意图注释）→ 达标（C 远大于 need）。

## 备注
- 0423-0425 尚未实施（本批次续项）。

## 失败修订记录（批次内）
- 测试文件安置路径修正：初次将 malformed-sgr-mouse.test.tsx 放在 packages/opencode/test/cli/cmd/tui/ 并 import `@opencode-ai/tui/app` → typecheck TS2307（tui 包无 ./app 子路径导出）→ 移至 packages/tui/test/cli/cmd/tui/（target TUI 测试主目录）并改相对 import `../../../../src/app`（4 级，与同级测试一致）。
- import 深度一度写成 3 级（../../../src/app）→ 修正为 4 级。
- 生成/物化顺序调整：materialize 422 先行导致 pre state 缺失 → 重建 0421 state 后从 0422 state 复制已含 guard 的 app.tsx 重新生成 current（4 hunks，15933 字节）。

## 行为测试证据
- malformed-sgr-mouse.test.tsx：7 tests 全过、9 expect calls（畸形 press/release 拦截、三连碎片不产生 aN;NaNMaN;NaNMaN;NaNm、合法 SGR mouse 不受影响、普通文本 hello 正常、aN;NaNm 直输保留、recovery 遇非法字符 x 放行 xM）。

## E/C（最终）
- E=125、C=39、need=19 → PASS。
- 注释分布：app.tsx guard 区（根因解释：OpenTUI csi_sgr_mouse 遇大写 N 误终 CSI；不变量：仅消费 ESC[<…N 前缀确认的畸形报告、不扫描用户文本、不以 /NaN/ 过滤；recovery 字符集与 50 上限）；测试文件（根因注释、辅助函数说明、各测试意图）。

## 审计结论
- 双审计轮次：guard 语义（前缀匹配/recovery 状态机/Mm 终止/非法放行/50 上限）与 source 逐行对齐；prependInputHandlers 经 OpenTUI 既有接口注入（无第二 parser、无 fallback）。

## 状态/指纹
- materialize 425 --rebuild：passed 425/425；typecheck 425：30/30 workspaces passed；install 指纹 1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5（bun 1.3.14 frozen reuse）。
- sourceTip d0ceb469011412b4ac5058a12d5fe4f247bdac79；targetBaseline 4473fc3c9055046183990a965d68df3db7ea6f62。

## N 项修正（双审计后）
- N-0422-1：doc 首行 hunk 头残留（@@ -0,0 +1,283 @@）已删除（283 内容行）；N-0422-2：测试尾 2 空行已剥离（139 行）；hunk 头修正（+1,283/+1,139）后 materialize 425 --rebuild passed、typecheck 30/30、malformed-sgr-mouse 7/7 回归通过。
