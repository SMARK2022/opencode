# 0422 — fix(tui): 拦截畸形 SGR mouse 报告碎片注入 prompt textarea

## 最终状态：PASS（R72 修复 + 扩写）

## source 与映射
1. docs/Proposal/tui-malformed-sgr-mouse-fix.md：
   - 根因分析：OpenTUI StdinParser 把 CSI final byte 范围内的
     大写 N 当终结符，畸形 ESC[<64;NaN;NaNM 碎片
     逐字符进入 TextareaRenderable.insertText；
   - 复现证据；current verbatim。
2. packages/tui/src/app.tsx：
   - createMalformedSgrMouseGuard handler 接入（拦截碎片 key event）。
3. packages/tui/test/malformed-sgr-mouse.test.tsx：
   - 权威测试副本（import ../src/app，V2 包内正向引用）。
4. packages/opencode/test/cli/cmd/tui/malformed-sgr-mouse.test.tsx：
   - R72 修复——skip 桩前移（见下）。

## B-0422 闭合（第 1 波发现）
- R13 生成的 opencode 侧 new-file 测试
  import "../../../../src/cli/cmd/tui/app"——
  V2 TUI 拆分后该模块不存在
  （guard 已迁移 packages/tui/src/app.tsx）；
- 422-0435 区间该文件 import 失败/测试红，
  直至 0436 才 skip 桩化；
- 按债务前移原则（与 R24/R71 同模式）：
  - current/0422 的 new-file section 内容
    直接改为 skip 桩终态
    （test.skip + guard stub + 迁移注释）；
  - current/0436 的对应补偿 section 删除；
- tui 版权威副本不受影响
  （import "../src/app" 正向引用在 422 态即有效）。

## E/C
- E≈143/C≈25/need = 22，25 ≥ 22 ✓；
- doc 283 行 verbatim 不计；
- app.tsx guard + tui 测试副本 + skip 桩 9 行含 3 行迁移注释。

## 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces
  （skip 桩前移后 0422-0435 区间无死副本）；
- 0452 物化态两文件形态复核：
  opencode 版 = skip 桩、tui 版 = 权威副本；
- record 曾 6 行（第 1 波 G），本节扩写为完整合规记录。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「死副本指向不存在模块」——B 核心，
    R72 skip 桩前移后 422-0435 区间无 import 失败文件；
  - 「0436 才 skip 桩化，不能追溯」——R72 以债务前移反驳：
    补偿应该移到最早引入者（R24/R71 同模式）；
  - record 短（6 行）——本节定稿。
- 结构史：
  1. R13 生成 opencode new-file 测试（139 行，import 源系模块路径）；
  2. 第 2 波复验时 R71 的 slice 事故波及本文件
     （malformed 段出现重复 section）；
  3. R72 彻底重组：docs / app.tsx / opencode malformed(skip 桩) /
     tui malformed(权威副本) 四 section 定稿。
- 0436 补偿删除的对账：
  - 0436 原 malformed section（skip 桩化 hunk）
    在 skip 桩前移后目标已存在相同内容，
    保留会造成 apply 冲突 → 整 section 删除；
  - 0436 其余 section 不受影响。

## 验证补充
- 0452 物化态 opencode 版头部直读：
  test.skip / guard stub / 迁移注释三要素齐备；
- 0452 物化态 tui 版头部直读：
  import "../src/app" 正向引用（guard 真实实现）；
- 全链 typecheck 30/30 佐证 skip 桩在 422-0435 区间的编译面。
