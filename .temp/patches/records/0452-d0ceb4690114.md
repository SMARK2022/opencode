# 0452-d0ceb4690114 chore(submodule): 固定 OpenTUI 清理版本

## Identity
- index / full SHA: 0452 / d0ceb469011412b4ac5058a12d5fe4f247bdac79（parent = 0451；**manifest 末项 = source tip**）
- subject: chore(submodule): 固定 OpenTUI 清理版本
- original: 866B / 20 行 / 1 文件（thirdparty/opentui gitlink）
- current: 866B 与 original 逐字节一致（原样 apply，零适配）
- status: 闭合（materialize 链 passed 452/452；typecheck 452 全 30 包 passed）

## 语义
- gitlink thirdparty/opentui：15cecf50 → cbe492a538137842961d561c33f55fdb7587b40e（SMARK 分支已推送的 Windows verifier 清理版本）。
- commit message：保持源码发现路径与远端默认分支一致。

## 适配判定：原样（无适配）
- pre-image gitlink（15cecf50）恰为 0451 的 post——链式 apply 直接前进。
- materialize 452 实证：state 452 的 opentui gitlink = cbe492a5（git ls-files -s 实测），与 source tip d0ceb469 完成对齐。
- 无代码、无测试、无文档语义；E=0 → C=0（E=0 时 C=0 满足门禁）。

## 验证
- materialize 链：passed 452/452（**全部 452 项累计闭合，replay base = 0452 本身**）。
- typecheck 452（typecheck-0452-2026-08-30T20-59-00-742Z）：30 包全 passed、No failures。
- state 452 gitlink 实测：chatgpt-browser-agent=11f9c519（0446）、opentui=cbe492a5（0452）——两条 submodule 链均达 source 终态。

## 总体重放确认（供最终审计）
- manifest 452 项全部获得 PASS：本项与 0451 为终止批次的纯 submodule 元数据项，原样落位；前 450 项按批次闭合记录（0001-452 累计 apply、批次末 typecheck 452 全绿、source/target worktree unchanged: true）。
- source tip d0ceb469 即本项 SHA——重放终点与 manifest 尾项精确对齐，重放序列无剩余项。
## 原始 patch 结构与 hunk 映射
- original 20 行：L12-13 为 .gitmodules 无关（本项不触 .gitmodules——0451 已注册）；实际唯一 hunk：diff --git a/thirdparty/opentui（160000）+ index 15cecf50..cbe492a5 + @@ -1 +1 的 -Subproject/+Subproject 两行。
- current 映射：与 original 逐字节一致——pre-image（15cecf50）恰为 0451 的落位结果，链式前进零冲突。

## 语义与终点意义
- cbe492a5 为 SMARK 分支"Windows verifier 清理版本"——commit message 声明其将源码发现路径与远端默认分支（smark/main）对齐。
- 本项 SHA（d0ceb469）即 manifest 的 sourceTip：**重放序列在此终结**，materialize 报告的 Replay base = 0452 自身（全部 452 项已物化）。
- 终态核对：state 452 中两条 submodule 链的 gitlink 终值分别为 chatgpt-browser-agent=11f9c519（0446 终态）与 opentui=cbe492a5（本项）——thirdparty 依赖治理的完整收尾。

## 风险与边界说明
- 与 0451 同为 gitlink 元数据变更：无编译面、无测试面；typecheck 452 的 30 包 passed 与本项无依赖边但证明元数据变更未破坏任何编译输入。
- E=0 → C=0：gitlink 指针行（Subproject commit）为 git 元数据而非代码/文档语义，不计有效修改行。

## 失败修订史
- 无。首轮 materialize 即通过（与 0451 同轮），零适配往返。
## 逐行触点清单（供审计）
- thirdparty/opentui gitlink 修改 4 行：`new file` 不存在（modify）；index 行 `15cecf50fe68f39ad03699a346b90f490983fb73..cbe492a538137842961d561c33f55fdb7587b40e 160000`；`-Subproject commit 15cecf50...`；`+Subproject commit cbe492a5...`。
- hunk 头：`@@ -1 +1 @@`（单行 gitlink 文件）。
- 无 .gitmodules 触碰（0451 已注册）；无代码/测试/文档触碰。

## 验证命令记录（工作目录 = states/0452/repo）
- `git ls-files -s thirdparty` → opentui 条目 cbe492a5 与 chatgpt-browser-agent 条目 11f9c519 并列。
- `git ls-files -s thirdparty/opentui` 单独确认 = `160000 cbe492a5... 0`。
- typecheck 报告：Passed 452 / No failures / install reused 813972ec。

## 终局判定依据
- source tip = d0ceb469（本项）与 manifest.json 的 pinned tip 一致——重放覆盖验证：452/452 passed 且 Replay base 为 0452 自身。
- 本项原样（零适配）落位的事实由 materialize 首轮 passed 直接证明，无适配往返即无适配引入风险。
## 与 0451 的批次内关系与批次收尾
- 本项 pre-image（15cecf50）即 0451 的落位结果——两步构成注册→固化的连续治理动作。
- 本项闭合后：批次末验证（typecheck 452 全绿）、452 项 records 齐备、source worktree unchanged、target worktree unchanged——总体重放的三个收尾条件在本项之后全部可满足。

## 终态 gitlink 汇总表
| submodule | 初始（0451 前） | 0451 后 | 0452 后（终态） |
|---|---|---|---|
| thirdparty/chatgpt-browser-agent | 6d27a892（0451 前既有） | 11f9c519（0446 推进） | 11f9c519（不变） |
| thirdparty/opentui | 不存在 | 15cecf50（0451 注册） | cbe492a5（0452 固化） |