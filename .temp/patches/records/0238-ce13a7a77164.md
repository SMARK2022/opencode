# Record 0238-ce13a7a77164

## Status
PASS

## 修改内容
feat(tokenAccounting): 修复 in-flight upload estimate 双算问题，确保输入只计算一次。

Source 2 文件，目标适配后 2 文件（路径迁移）。

### 路径迁移
- `packages/opencode/src/token/accounting.ts` → `packages/tui/src/util/token-accounting.ts`（实现）
- `packages/opencode/test/cli/tui/token-estimate.test.ts` → `packages/tui/test/util/token-accounting.test.ts`（测试）

### 行为适配
- Source 的 `accounting.ts` 从 `msg.tokens` 累加 `confirmedRequest.input`
- 目标的 `token-accounting.ts` 主要从 `p.tokens`（step-finish parts）累加，仅无 step-finish 时用 `msg.tokens` 兜底
- 双算问题在目标的兜底路径同样存在：latestRequestAssistant 的 msg.tokens 同时进入 confirmedRequest 和 stepInput
- 修复：添加 `latestRequestAssistant`，兜底路径跳过其 msg.tokens.input，totalInput 用 `inFlightInput` 替代 `stepInput`
- inFlightInput 逻辑：stepConfirmed 时为 0；stepSF 存在且 latestAssistant 是 latestRequestAssistant 时只用 pendingToolResultTokens+pendingAttachTokens；否则用 stepInput

### 测试适配
- `stepStart` helper 签名适配：source 用位置参数 `(inputTokens, inputChars?)`，目标用 `(id, inputTokens, inputChars?)`（目标 part 需要 id）
- `stepFinish` 签名适配：source 用位置参数，目标用 `(id, tokens, inputChars?)`
- `assistantMsg` 签名适配：source 用 `(id, parentID, tokens)`，目标用 `(id, parentID, tokens?, completed?)`
- `tokenAccounting` 调用签名适配：source 用 `() => item.parts`，目标用 `(id) => parts[id] ?? []`
- 3 个新测试用例完整保留，断言值不变

## E/C
- E = 62（有效修改行）
- C = 13（合格中文注释）
- Need = ceil(62 * 0.15) = 10
- Status: PASS（C=13 >= Need=10）

## 注释位置
- token-accounting.ts: latestRequestAssistant 定义（上传估算值说明）、兜底路径跳过注释（双算原因+output/cost 保留）、inFlightInput 三行注释（step-finish 已到达/msg.tokens 旧值/只有 tool result 是 pending）
- token-accounting.test.ts: stepStart helper 注释（上传估算快照）、lifecycle test 注释（双算修复验证）、multi-assistant 注释（修复双算仍保留历史步骤）、fallback test 注释（output/cost 保留）、provider input test 注释（confirmed step-finish 权威值）

## Source
- original/0238-ce13a7a77164.patch: 178 行，2 文件

## 上游
- 目标 token-accounting.ts 无双算修复——source 新增修复

## 目标
- packages/tui/src/util/token-accounting.ts: +13/-1 行（latestRequestAssistant + 兜底跳过 + inFlightInput）
- packages/tui/test/util/token-accounting.test.ts: +78 行（stepStart helper + 3 新测试）

## Current
- current/0238-ce13a7a77164.patch: 7637 bytes, SHA-256 a9609f7831aef3c8f1a137d805a2901294d0d8b701a63f75000ca4b295cedbc3
- 2 diff entries

## 安装
- Install: reused, fingerprint a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b

## 测试
- Typecheck: 30/30 passed
- token-accounting.test.ts: 10/10 passed（--timeout 30000，含 3 新测试）

## 失败修订
- 首次 test 插入 anchor 不匹配（目标首个 test 有额外 expect 行）→ 修正 anchor

## 最终行为
- latestRequestAssistant 的 msg.tokens.input 不累入 confirmedRequest（避免双算）
- inFlightInput 在 step-finish 已到达但 msg.completed 未写时，只用 pending tool result tokens（不从前估算值中减去 actual usage）
- output 和 cost 在兜底路径仍保留（无等价 step-finish 重复）
- 多 assistant 归属同一 user request 时，已确认历史步骤保留，仅最新 in-flight assistant 去重

## DUAL-AUDIT SEAL (Round 3)
PASS — Forward and reverse auditors agree: no unclosed B/G. Batch 236-240 sealed.
