# Record 0424-a73e1f718d19

## Identity
- SHA: a73e1f718d19
- Parent: 16c8dd6cf7ba (0423)
- Subject: fix(tui): abort 完全静音并从源头修复 VS Code 双横幅
- Status: PASS

## Source intent
source 0424（4 文件，16 hunks）：
1. `attention.ts`：triggerNotification 不传默认 title——OpenTUI OSC 99 在 title 非空时发送 title+body 两个 payload，某些终端（VS Code）渲染成两个横幅 → `normalizeText(request.title, "", TITLE_LIMIT)` + `title || undefined`。
2. `feature-plugins/system/notifications.ts`：notify 的 sound 参数支持 false（question/permission 只保留视觉通知）；新增 completedTurn（检查 latest assistant 是否 time.completed 且无 error；abort 的 Runner.cancel 先发 idle 后发 error，idle 到达时 assistant 未终态化 → completedTurn false → 保留 active 等 error 消费）；session.status idle 处理器：completedTurn false → return（不消费 active）；subagent 完成静音（不通知）；session.error 直接 active.delete（重复 error 自然忽略）；abort（MessageAbortedError）→ sound:false 完全静音。
3. `attention.test.ts`：3 处断言改 `title: undefined`（focused/hello/hello again）。
4. `notifications.test.ts`：setup 支持 messages 参数；completedAssistant/pendingAssistant helpers；question/permission sound:false；subagent 静音；abort 事件顺序测试；重复 error 去重测试。

## Target analysis 与适配（决策）
- 路径映射：attention.ts → `packages/tui/src/attention.ts`（triggerNotification 区域 1:1）；notifications.ts → `packages/tui/src/feature-plugins/system/notifications.ts`（target 版与 source pre 状态 1:1——notify 签名/active/errored/处理器结构相同）→ hunks 直接适配（target 用 console.debug（已有））。
- attention.test.ts → `packages/opencode/test/cli/cmd/tui/attention.test.ts`（0421 改过 config() volume 0.5；title 断言 3 处同 source；显式 title 用例（L199/204）不改）。
- notifications.test.ts → `packages/tui/test/cli/cmd/tui/notifications.test.ts`（target 既有；结构与 source pre 是否 1:1 待核——若 import/结构差异则适配）。

## E/C
- 待编辑后计算。

## Verification
- materialize / typecheck / 测试：pending（随批次继续）
## 最终验证
- materialize 424 --rebuild：passed 424/424；typecheck 424：30/30 workspaces passed（install 指纹 1165df52… frozen reuse）。
- 行为测试：notifications.test.ts 9 tests 全过（10 expect calls）；attention.test.ts 21 tests 全过（73 expect calls）。
- E/C：attention.ts n1（title 处理 5 行注释）C=5；notifications.ts（sound false 语义/completedTurn 时序/errored 合并/abort 静音/子代理静音注释）C≈12；E（notifications 改造+测试）≈200 → 达标。

## 备注
- 0424 的 notifications.test.ts 逐块手工应用（import 路径差异导致 git apply 失效）：import 加 AssistantMessage/Message、setup 签名+helpers+defaultMessages、state.session.messages 注入、question/permission 常量删除、5 个测试重写 + 3 个新测试（no-op idle、subagent 静音、abort 时序、重复 error 去重）。
- 0425 尚未实施（本批次末项）。

## 失败修订记录（批次内）
- notifications.ts 的 edit0424a 脚本 n4 锚点缩进不符（target 顶格 `const tui`，脚本写 2 空格）→ 修正后分三次执行（attention.ts 先应用、notifications.ts 后应用、测试分三部）。
- notifications.test.ts 无法直接 git apply（import 路径差异：source 用 `@/cli/cmd/tui/...` alias，target 用相对路径）→ 逐块手工应用 10 处变换（import 扩展、setup 签名与 helpers、defaultMessages、state.session.messages、常量删除、5 测试重写 + 3 新测试）。
- 生成 current：4 文件 16 hunks（17375 字节）。

## 行为测试证据
- notifications.test.ts：9 tests 全过、10 expect calls（question/permission 静音、dedupe、done 完成路径、no-op idle、subagent 静音、abort 时序 idle→error→idle、model error 抑制 done、abort 静音+timeout 仍 error、重复 error 去重）。
- attention.test.ts：21 tests 全过、73 expect calls（含 title: undefined 断言 3 处）。

## E/C（最终）
- E=138、C=33、need=21 → PASS。
- 注释分布：attention.ts（OSC 99 双 payload 根因、undefined 单 payload 语义）；notifications.ts（sound false 视觉-only、completedTurn 时序、abort 首个 idle 未终态化、subagent 静音、error 消费 active、abort 静音）；测试（各场景意图）。

## 审计结论
- 双审计轮次：title 语义（默认 title 不再注入）、abort 时序（idle→error 消费链）、active/errored 合并（重复 error 幂等）、subagent/question/permission 静音矩阵与 source 逐条对齐。

## N 项修正（双审计后）
- N-0424-1：`const tui` 恢复第 0 列（source 未改动该行，风格一致）；materialize 425 --rebuild passed、typecheck 30/30、notifications 9/9 + attention 21/21 回归通过。
- N-0424-2：notifications.test.ts 尾空行清理；current 字节更新（17375→18241 前值已随 N 修正变化）。
