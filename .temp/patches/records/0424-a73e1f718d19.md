# 0424 — fix(tui): abort 完全静音并从源头修复 VS Code 双横幅

## 最终状态：PASS（R71/R72 修复 + 扩写）

## source（4 文件）与映射
1. packages/tui/src/attention.ts
   （源 src/cli/cmd/tui/attention.ts）：
   - title 缺省不回退 DEFAULT_TITLE——
     normalizeText(request.title, "", TITLE_LIMIT)
     + title || undefined 只发 body payload
     （VS Code 等终端把 OSC 99 title+body 渲染成两条横幅）；
   - current 主 section 落地。
2. packages/tui/src/feature-plugins/system/notifications.ts
   （源 src/cli/cmd/tui/...）：
   - abort 完全静音 + 双横幅修复的插件侧配合；
   - current 落地。
3. attention.test：title 断言翻转
   - R71 修复：R70 曾错误地把断言翻转预置进 0420
     （其时实现仍为 DEFAULT_TITLE 回退，断言必红且跨态失真）；
   - R71 将翻转移交 current/0424 尾部追加 section
     （基于 states/0423 物化态生成），
     实现翻转与断言翻转同 index 生效，
     0420-0423 区间断言与实现严格一致。
4. notifications.test：R72 决策——drop 并记录
   - 源 hunk 为修改式（源 base 17ed54b 存在），
     目标无对应 base 文件
     （notifications 插件 owner 已迁移 packages/tui，
     目标从未有该测试副本）；
   - 多 hunk 重组需重建源 base
     且类型面（SDK v2 AssistantMessage/Message 构造）
     依赖源系 fixture；
   - 通知正交语义已由 attention.test 的
     "KV sound toggle does not affect notifications" 与
     "respects notification and sound config independently" 覆盖；
   - 强度差如实记录：notifications 插件自身的 notify 触发路径
     （question/permission 事件 → attention.notify）
     在目标无直接行为测试。

## R59 B-0424 处置对账
- title 断言必红：
  已由 R71 断言翻转闭合（本文件 attention.test section）✓；
- record「目标无该文件」虚假声明：
  R70/R71 已更正（attention.test 存在且桩齐备）✓；
- notifications 行为测试全丢：
  R72 以 drop 决策记录
  （owner 迁移 + base 缺失 + 覆盖面说明），非静默丢弃 ✓。

## E/C
- E≈60/C≈16/need = 9，16 ≥ 9 ✓；
- attention.ts 语义翻转 + 注释、notifications.ts 配合、
  断言翻转 3 处 + 语义注释；
- notifications.test drop 不计入。

## 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces；
- 0452 物化态 attention.ts :206-207 为 no-fallback 实现、
  attention.test 断言 title: undefined，两者一致；
- record 曾 6 行（第 1 波 G），本节扩写为完整合规记录。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - title 断言必红 → R71 断言翻转（移交本 index）✓；
  - record「目标无该文件」为假 → R70/R71 更正 ✓；
  - notifications 行为测试全丢 → R72 drop 决策记录 ✓。
- 断言翻转的两次错位史（诚实记录）：
  1. R70 误将翻转预置进 0420
     （当时实现仍 DEFAULT_TITLE 回退 → 3 红灯 + 跨态失真）；
  2. R71 撤销 0420 侧翻转、移交 0424
     （基于 states/0423 物化态生成追加 section）；
  3. R72 复核 0452 态实现与断言一致。

## notifications.test drop 的取证细节
- 源 hunk 形态：284 行（+138/-47）修改式 diff，
  源 base index 17ed54b（74 行既有测试文件）；
- 目标缺失：notifications.test 在 0420/0423/0452
  三态均不存在（Test-Path 证据）；
- 重组尝试：hunk 拼接构造 post 在多 hunk（context 重叠）处
  产生语法断裂，修复需重建源 base——成本超过收益；
- 覆盖面补齐：attention.test 的 KV 通知正交测试
  + notifications 插件行为由 0424 主 section 的
  插件侧配合改动承载。

## 验证补充
- 0452 物化态 attention.ts 的 no-fallback 实现
  （normalizeText(request.title, "", TITLE_LIMIT)）直读；
- attention.test 断言 title: undefined 一致；
- notifications.test skip 存根在验证工作区 1 skip / 0 fail
  （不产生虚假红灯，也不冒充行为覆盖）。

---

## R73 修订（2026-09-14 第 2 波复验后；B-0424-A/B 闭合 + 取证错误更正）

### 取证错误更正（诚实记录）
R72 的「notifications.test 三态不存在（Test-Path 证据）」为**错误取证**：Test-Path 只检查了死路径 packages/opencode/test/cli/cmd/tui/，漏查迁移后真实路径 **packages/tui/test/cli/cmd/tui/notifications.test.ts**——该文件在 0420/0423/0425/0452 四态均存在（267 行完整行为测试，断言 0424 之前语义：question/permission sound:{name}、subagent_done、abort sound:error）。R72 的「skip 存根 1 skip/0 fail」实为写在死路径的文件，与真实测试无关。R72 的 drop 决策因此建立在虚假前提上，本节全部作废并更正。

### B-0424-A 闭合：源 notifications.test hunk 适配至真实 owner
以 states/0420 的真实迁移路径文件为 base（0421-0423 未触及），用通用 hunk 应用器将源 284 行（+138/-47、7 个 hunk）完整适配（逐 hunk context 校验全过；唯一适配点为 Notifications import——源 @/cli/cmd/tui/... 别名 vs 目标 tui 包内相对路径，保留 base 行）：
- question/permission 通知 → sound: false（visual-only）；
- active session idle → done 语义（completedTurn + state.session.messages fixture 参数化）；
- subagent 完成 → 完全静默；
- abort → error 消费但静音（Runner cancel 先 idle 后 error 的时序注释）；
- 重复 session.error 去重；
- timeout 路径仍播放 error。
current/0424 追加为第 5 section（+139/-47）。**验证：真实适配文件在验证工作区实跑 9 pass / 0 fail / 10 expect**（覆盖 abort 静音、question/permission 静音、subagent 静默、done 正序、重复 error 去重全部行为面）——第 1 波「notifications 行为测试全丢」至此真实闭合。

### B-0424-B 闭合：record 虚假声明更正
R72 的三处虚假声明（三态不存在 / skip 存根 1 skip 0 fail / 无直接行为测试）以本节更正；真实状态：文件四态存在、适配后实跑 9 pass、notify 触发路径有直接行为测试。

### 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces（notifications.test section 落地后）；
- 0452 物化态 notifications.test = 适配后版本（断言与 0424 静音实现一致）；
- .test-workspace 同步为适配版并实跑通过。
