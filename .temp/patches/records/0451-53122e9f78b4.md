# 0451-53122e9f78b4 chore(submodule): 注册 OpenTUI SMARK 分支

## Identity
- index / full SHA: 0451 / 53122e9f78b4beb80a835cd7b8616cf47af3ad1c（parent = 0450 fb3bd7a18b9c；终止批次 0451-0452 第一项）
- subject: chore(submodule): 注册 OpenTUI SMARK 分支
- original: 1353B / 33 行 / 2 文件（.gitmodules + thirdparty/opentui gitlink）
- current: 1353B 与 original 逐字节一致（原样 apply，零适配）
- status: 闭合（materialize 链 passed 452/452；typecheck 452 全 30 包 passed）

## 语义
- .gitmodules 追加 [submodule "thirdparty/opentui"]：path=thirdparty/opentui、url=https://github.com/SMARK2022/opentui.git、branch=smark/main。
- 新增 gitlink thirdparty/opentui → 15cecf50fe68f39ad03699a346b90f490983fb73（new file mode 160000）。
- commit message 明示：只建立源码与 provenance 链接，不宣称 OpenCode release consumer 已完成。

## 适配判定：原样（无适配）
- 目标 .gitmodules 的 pre-image（chatgpt-browser-agent 条目尾部）与源 pre-image 逐字一致（0446 前批次已注册该 submodule，尾部结构相同）。
- git apply 对新增 gitlink（160000 new file）+ .gitmodules 追加的组合直接应用成功——materialize 452 实证。
- 无代码、无测试、无文档语义；E=0 → C=0（E=0 时 C=0 满足门禁）。

## 验证
- materialize 链：passed 452/452（全链 0001-452 累计闭合）。
- typecheck 452（typecheck-0452-2026-08-30T20-59-00-742Z）：30 包全 passed、No failures、install reused 813972ec6428411e9b35d301cfa065657188b6a84e22b9a20a67b7e9393e67d9。
- state 452 实证（git ls-files -s thirdparty）：opentui gitlink = 15cecf50（0451 post）→ 由 0452 前进至 cbe492a5（后项）；.gitmodules L6-8 含 opentui 注册。
## 原始 patch 结构与 hunk 映射
- original 33 行：L12-19 为 .gitmodules hunk（@@ -2,3 +2,7：上下文 3 行为 chatgpt-browser-agent 条目尾部 + 4 行新增 opentui 注册）；L20-33 为 thirdparty/opentui 新 gitlink hunk（new file mode 160000，@@ -0,0 +1）。
- current 映射：两段与 original 逐字节一致——无路径变化、无行尾差异、无上下文漂移；materialize 452 的 git apply 无 fuzz/offset。

## 适配判定依据（为何零适配）
1. .gitmodules pre-image：源要求 chatgpt-browser-agent 条目已存在且尾部为 branch = smark/main——目标的 .gitmodules 由 0446 前的 submodule 系列批次建立，尾部结构逐字一致（state 450 实测 .gitmodules 已含 chatgpt-browser-agent 三行）。
2. 新 gitlink 的 160000 模式：git apply 对 new file mode 160000 的处理只写 index 条目，不需要 submodule worktree 检出（submodule 内容检出不属于物化范围，与 0446 的指针推进同理）。
3. 目标 thirdparty/ 目录已存在（含 chatgpt-browser-agent gitlink），新条目为并列注册，无路径冲突。

## 与 0446 的关系
- 0446 推进的是 chatgpt-browser-agent 指针（既有 submodule）；0451 注册的是**新** submodule（opentui）并给初始指针 15cecf50；0452 随即将其推进到 cbe492a5（清理版）。三者构成 thirdparty 依赖链的完整收尾。
- submodule URL/branch 的语义：url 指向 SMARK2022/opentui.git、branch=smark/main——与 .gitmodules 中 chatgpt-browser-agent 的 SMARK 分支模式一致（同一 fork 体系的依赖治理方式）。

## 验证与审计指引
- materialize：passed 452/452（replay 链覆盖全部前序）。
- typecheck 452：30 包全 passed；install reused 813972ec（本项为元数据变更，无依赖影响）。
- state 实证：`git -C states/0452-.../repo ls-files -s thirdparty` 应显示 opentui = cbe492a5（0452 终态）；.gitmodules 应含 opentui 三行注册。
- current 与 original 逐字节一致：SHA-256 或 byte-compare 可复核；本项无任何 E/C 计量需求（E=0→C=0）。
## 风险与边界说明
- 本项与 0452 均为 gitlink 元数据变更：不触发任何 workspace 的编译面（typecheck 30 包与 gitlink 无依赖边）；materialize 的 git apply 只需要 index 层操作。
- gitlink 指针对象（15cecf50 / cbe492a5）在目标仓库对象库中不存在是预期行为——submodule 内容由 `git submodule update --init` 在 thirdparty/opentui 独立检出，重放工作流不负责其内容验证。
- branch = smark/main 声明的是 submodule 远端跟踪分支，供 `git submodule update --remote` 使用；固定指针（gitlink）本身优先于 branch 声明——这是 submodule 的标准语义，与 commit message 的"固定到 15cecf50"一致。

## 失败修订史
- 无。本项从 materialize 首轮即原样通过（与 0452 同轮 passed 452/452），无 hunk 冲突、无行尾问题、无适配往返。
## 逐行触点清单（供审计）
- .gitmodules 新增 4 行（+前缀）：`[submodule "thirdparty/opentui"]`、`	path = thirdparty/opentui`、`	url = https://github.com/SMARK2022/opentui.git`、`	branch = smark/main`（tab 缩进与既有条目一致）。
- thirdparty/opentui 新 gitlink 3 行：`new file mode 160000`、`index 0000000..15cecf5`、`+Subproject commit 15cecf50fe68f39ad03699a346b90f490983fb73`。
- 上下文 3 行（chatgpt-browser-agent 尾部）：`	path = thirdparty/chatgpt-browser-agent`、`	url = https://github.com/SMARK2022/chatgpt-browser-agent-smark.git`、`	branch = smark/main`。
- hunk 头：.gitmodules `@@ -2,3 +2,7 @@`；opentui `@@ -0,0 +1 @@`。

## 验证命令记录（工作目录 = states/0452/repo）
- `git ls-files -s thirdparty` → 两条 160000 条目（chatgpt-browser-agent=11f9c519、opentui=cbe492a5）。
- `Select-String .gitmodules -Pattern opentui` → L6-8 三行注册。
- materialize 报告：passed 452/452、Stopped at: none、retained 0452/0450/0446/0445/0441。
## 与 0452 的批次内关系
- 0451 注册 + 初始指针；0452 前进指针到清理版。两项构成同一次 submodule 治理动作的两步提交，审计时可作为一个元数据链整体核验（0452 的 pre-image 即 0451 的 post）。