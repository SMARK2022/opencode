# Record 0321-a8c62d30a171 — feat(log): 优化日志初始化，确保使用单一日志目录快照

## Identity
- index/full SHA: 0321 / a8c62d30a1716768cd02f1b2d28df7e1fac671e7 (short a8c62d30a171)
- manifest parent: 19508edaac3c (0320)
- subject: feat(log): 优化日志初始化，确保使用单一日志目录快照
- current SHA-256: cf3f661a34f0179a / 813 bytes
- status: PASS (NO-OP with documentation comment)

## Source intent (original/0321-a8c62d30a171.patch, 117 行)
1. `packages/core/src/util/log.ts` (L59-95): 加 initID 防止并发 init() 竞态——async gap 间 Global.Path.log 变化导致多个日志文件；用局部 dir/nextLogpath 变量；if (id !== initID) return 检查
2. `packages/opencode/test/util/log.test.ts` (L111-117): 测试验证并发 init 不产生多个日志文件（mock fs.mkdir 延迟 + Global.Path.log 切换）

## Three-way comparison
- source/SMARK: initID counter 防止并发 init() 竞态；局部变量捕获 Global.Path.log 快照
- 上游 v1.17.20: util/log.ts 有 init() 但无 initID guard；并发 init 可能产生多个日志文件
- 目标当前: util/log.ts 已被 observability/logging.ts 替换（0304 迁移）；无 init() 函数；fileLogger 使用 Effect Logger.toFile
- 结论: source 的 initID guard 针对的竞态条件在目标 Effect 架构中不存在——file 参数在调用时捕获，Logger.toFile 是纯 Effect 无可变共享状态

## Target adaptation (NO-OP)
- source 的 `packages/core/src/util/log.ts` 在目标中不存在→被 `packages/core/src/observability/logging.ts` 替换
- source 的 `packages/opencode/test/util/log.test.ts` 在目标中不存在→无测试文件
- 目标的 fileLogger() 已有等效保护：
  - file 参数在调用时捕获（`file = path.join(Global.Path.log, "opencode.log")`），不依赖 async 后的 Global.Path.log
  - Logger.toFile 是纯 Effect，无可变共享状态（无 stream/logpath 变量可被竞态）
  - loggers() 调用 fileLogger() 一次，在 runtime setup 时，非并发 async 调用
- 添加 English 文档注释到 fileLogger() 解释 NO-OP 原因（E=0/C=0）
- 方法: state 0320 语义编辑（edit321v2.py patch_window）→ git diff → current/0321

## 逐 hunk 映射
| Source hunk | Source file | Target file | 状态 |
|-------------|-------------|-------------|------|
| Hunk 1 (initID guard) | util/log.ts L59-95 | observability/logging.ts L49 | NO-OP（Effect 架构无 init()）+ English 注释 |
| Hunk 2 (test) | test/util/log.test.ts L111-117 | — | NO-OP（无测试文件，无 init() 可测试） |

## Verification
- materialize 321/321: passed
- E/C: E=0 / C=0 / need=0 → PASS（comment-only change in .ts file，English 注释不计入 C）
- source/target HEAD: 未变
- states 保留 ≤5

## 不一致
- source 的 util/log.ts 是命令式日志架构（init/cleanup/createWriteStream/mutable stream+logpath）
- 目标的 observability/logging.ts 是 Effect 架构（fileLogger returns Effect, Logger.toFile, no mutable state）
- initID guard 针对的竞态条件（async gap 间 Global.Path.log 变化）在 Effect 架构中不存在
- 目标 fileLogger() 已通过参数默认值捕获 Global.Path.log（调用时评估，非 async 后读取）
- 无需从零构建——目标已有等效保护，source 的修复不适用

## Source initID guard 详细分析
- initID 是递增计数器，每次 init() 调用时 ++initID
- 局部 dir/nextLogpath 变量在 async gap 前捕获 Global.Path.log 快照
- if (id !== initID) return 检查：如果 async 期间有新 init() 调用，旧 init 放弃 logpath 赋值
- 目标 Effect 架构无此问题：fileLogger 的 file 参数在调用时评估，不依赖 async 后的 Global.Path.log
- Logger.toFile 是纯 Effect，无 stream/logpath 可变状态可被竞态
- loggers() 在 runtime setup 时调用一次，非并发 async 调用

## 测试 NO-OP 分析
- source 测试 mock fs.mkdir 延迟 + Global.Path.log 切换验证 initID guard
- 目标无 init() 函数，无法 mock；fileLogger 是纯 Effect，无 async gap 可利用
- 创建等效测试需要 mock Effect Logger.toFile 的内部状态，复杂度高于 source 的 fs.mkdir mock
- 结论: 测试 NO-OP（无 init() 可测试，无竞态条件可验证）

## DUAL-AUDIT SEAL (batch 321-325)
- R1 dual audit:
  - Forward (ses_fdbcb1204ffe9iLg46Odc8wB2Z): 5/5 PASS, no open B/G
  - Backward (ses_fdbcaf978ffebcDXZdeYRtKY3s): 5/5 PASS, no open B/G
- Verified: manifest/proof hashes, original LF-SHA, current SHA+bytes, install cbdc173 reused, E/C all pass (0/0, 88/24, 268/51, 65/11, 26/6), cumulative apply 325/325, states <=5, source/target unchanged
- Environmental N: packages/app TS1128; voice transcribe uses native spawn (architectural)
- Key verified: MCP SDK 1.29.0 already has windowsHide fix (NO-OP correct); 0323 LayerNodePlatform.path critical fix; 0325 pristine original
- Batch: APPROVE (5/5 PASS, no open B/G)