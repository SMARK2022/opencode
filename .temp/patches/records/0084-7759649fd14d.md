# Record 0084-7759649fd14d

## 1. Identity

- **Typecheck report:** `typecheck-0084-2026-07-22T01-06-33-597Z.md`


- **Index:** 84
- **Source SHA:** `7759649fd14dcb2b5e0fb268dc013e2609f9f209`
- **Subject:** feat(vscode): 增强笔记本请求处理，添加文件锁机制以序列化请求，优化运行逻辑
- **Current path:** `current/0084-7759649fd14d.patch`
- **Current SHA-256:** `50ce0d6cd08096828dfbe50f86faa4630565eb8d40b50d3dcc2d65b097482b95`
- **Current bytes:** 3984
- **Install fingerprint:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Baseline:** `4473fc3c9055046183990a965d68df3db7ea6f62`
- **Source tip:** `d0ceb469011412b4ac5058a12d5fe4f247bdac79`

## 2. Upstream design after #83

Extension HTTP bridge already has:

- `safeLocalRequest` (Origin/non-loopback → 403)
- Bearer-only `authorized` (no query token)
- `GET /manifest` with `token: "<redacted>"`
- `registerBridge` hard-bound to `startBridge` resolve
- Notebook POST routing via four-arg `routeRequest(method, pathname, request, output)` that re-reads the body inside the router

Concurrent POSTs on the same notebook `filePath` can interleave edit/run/source and race document version waits. Client enqueue/heartbeat reliability constants and `enqueueBridgeRequest` were fused into #82 `packages/opencode/src/ide/vscode-bridge.ts`. Run endCellId range signal, type=cell mismatch throw, success-boolean wait, timeout exec text, and URI wait identity were fused into #80 `run.ts`.

## 3. Source / SMARK original #84 scope

1. **Client:** health 2s, heartbeat trust 10s, stale 60s, pid-dead first, `enqueueBridgeRequest` on `callBridge` — **present via #82 fuse**.
2. **Extension:** `fileLocks` Map + `withFileLock` FIFO mutex; POST reads JSON body once under lock; `routeRequest(pathname, body, output)` arity (no second body read).
3. **Run:** type=cell endCellId guard; cellAt refresh; URI wait — **present via #80 fuse**.
4. **Summary:** previous-session success wording for agent-facing status.

## 4. What this current changes (honest file list)

### In this patch

- `sdks/vscode/src/bridge.ts`
  - Introduce `fileLocks` / `withFileLock(key, fn)` (FIFO: prev.then(fn).finally release).
  - POST path: `readJson` once → `withFileLock(filePath, () => routeRequest(pathname, body, output))`.
  - **Entire** pre-image `routeRequest` function replaced via paren/brace-safe rewrite to a **single** `routeRequest(pathname, body, output)` with switch cases only (no residual `method`/`request` body, no second `readJson`).
- `sdks/vscode/src/notebook/summary.ts`
  - `"All code cells report successful execution state (may be from a previous session)."`

### Not in this patch (cumulative present, documented)

- `run.ts` endCellId/wait/timeout — #80
- Client queue/heartbeat — #82
- Bearer-only auth / safeLocal / manifest — #82

## 5. E / C (this current only)

- **E = 22**
- **C = 6**
- **need ≥ 4**
- **Gate:** PASS


## 6. Final behavior in state-84 (cumulative checklist)

| Behavior | Owner | Present |
|---|---:|---|
| withFileLock + single body read | 84 | yes |
| one routeRequest(pathname, body, output) | 84 | yes |
| no residual method/request router | 84 | yes |
| summary previous-session string | 84 | yes |
| endCellId range + type=cell throw | 80 | yes |
| success-boolean wait + timeout text | 80 | yes |
| client enqueue/heartbeat | 82 | yes |
| safeLocal / Bearer-only / manifest redacted | 82 | yes |
| NetworkProxy.fetch primary + passthrough fetchWithRoute | 83 | yes |

## 7. Verification

- Materialize cumulative through 84 succeeds after #83 redesign.
- State greps: single `routeRequest`; no `if (method !== "POST")`; previous-session summary string; `withFileLock(filePath`.
- Monorepo typecheck through 84 must pass; install fingerprint reused when deps unchanged.
- Note: monorepo typecheck does not always include `sdks/vscode`; bridge correctness is validated by structural greps and apply success.

## 8. Why current is shorter than original

Original also carried client reliability + run hunks already applied earlier in this migration. Re-applying them would conflict or no-op. Contract is cumulative behavior + honest ownership, not replaying every original file at the same index.

## Dual independent audit (batch 80-84)

- **FORWARD:** PASS (all five, no open B/G) — ses_078a2a765ffexXTi63g11fBQJa
- **REVERSE:** PASS (all five, no open B/G) — ses_078a2a74effes7VUOaxpmbOGtW
- **State:** `states/0084-7759649fd14d`
- **Typecheck:** `typecheck-0084-2026-07-22T01-06-33-597Z.md` monorepo packages passed
- **Install:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Batch verdict:** PASS
