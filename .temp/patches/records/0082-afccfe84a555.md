# Record 0082 — `afccfe84a555`

**Subject:** feat(vscode): VS Code notebook bridge  
**Index:** 82  
**Parent:** see `manifest.tsv`  
**Current:** `current/0082-afccfe84a555.patch`  
**Original:** `original/0082-afccfe84a555.patch`  
**Current SHA-256:** `7eb097dcb6f03c4dfab977560465bbb9e50db4ebecca3de2ca71e72f6f0c9d6a`  
**Current bytes:** 56682  
**Author:** SMARK (manifest)

## 1. Source intent (from original patch)

1. IDE client discover/call bridge.
2. Plugin tools + descriptions for notebook ops.
3. Extension HTTP bridge + local registry heartbeat.
4. Tests for bridge discovery/call.
5. Register `VscodeBridgePlugin` in V2 `internalPlugins`.

## 2. Upstream / pre-state (post previous index)

Notebook handlers under `sdks/vscode`; no CLI plugin/registry discovery stack yet.

## 3. Three-way adaptation

New files from adapted current; path-aware plugin registration. Import `vscode` in bridge.ts for sessionId/registry. Client reliability constants may be fused early; extension fileLocks owned by #84.

### Files in current

- `packages/opencode/src/ide/vscode-bridge.ts` (new)
- `packages/opencode/src/plugin/vscode-bridge.ts` (new)
- `packages/opencode/src/plugin/vscode-bridge-descriptions.ts` (new)
- `packages/opencode/test/ide/vscode-bridge.test.ts` (new)
- `sdks/vscode/src/bridge-registry.ts` (new)
- `packages/opencode/src/plugin/index.ts`
- `sdks/vscode/src/bridge.ts`
- `sdks/vscode/src/extension.ts`

## 4. Final behavior in state-84

CLI discovers local VS Code bridges and invokes notebook tools over authenticated HTTP; registry writes heartbeat files.

## 5. E / C (this current only)

- **E = 796**
- **C = 146** (unique ≈ 144)
- **need ≥ 120**
- **Gate:** PASS


## 6. Verification

- Cumulative materialize through **84**: `states/0084-7759649fd14d`
- Typecheck report: `reports/typecheck-0084-2026-07-22T00-40-33-686Z.md`
- Install fingerprint: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Prior batch 75–79 dual PASS closed
- No empty current; `original/` untouched; no `current/_*.patch`

## 7. Failures and revisions

- Rebuilt against post-#79 notebook + V2 plugin/provider layout when early currents failed apply.
- Closed known defects: resolve double-export (#80), source residual globalLineNum (#81), vscode import + registry (#82), NetworkProxy call-site rewires (#83), routeRequest arity + fileLocks + endCellId order + URI waiter + summary wording (#84).
- Record rewritten with numeric E/C (no provenance padding spam).

## 8. Verdict

**PASS (pending independent dual audit batch 80–84)**

## Fix note (dual-audit B)

- Restored `safeLocalRequest`: reject non-loopback Host and any Origin → 403 Forbidden before auth.
- `registerBridge` success is bound to `startBridge` resolve; failure rejects (no soft-success).

## Bridge security and manifest (revision)

- `safeLocalRequest`: Origin or non-loopback Host → 403 before auth.
- `registerBridge` success is hard-bound to `startBridge` resolve; failure rejects.
- After auth, `GET /manifest` returns registry identity with `token: "<redacted>"`.
- Startup logs use `token <redacted>` and endpoint dump uses `?token=<redacted>` (no bare token in output channel).
- Chinese comments are decision-adjacent (security, registry hard-fail, queue, heartbeat windows); unique C exceeds ceil(E*0.15).

## Auth revision

- `authorized` is Bearer-only (`void url`); query token path removed.
- Logs redact token; `GET /manifest` returns token `"<redacted>"`.

## Dual independent audit (batch 80-84)

- **FORWARD:** PASS (all five, no open B/G) — ses_078a2a765ffexXTi63g11fBQJa
- **REVERSE:** PASS (all five, no open B/G) — ses_078a2a74effes7VUOaxpmbOGtW
- **State:** `states/0084-7759649fd14d`
- **Typecheck:** `typecheck-0084-2026-07-22T01-06-33-597Z.md` monorepo packages passed
- **Install:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Batch verdict:** PASS
