# Record 0093-3ae1d112f256

## Source intent
original 将 `configure` 从“轻量触发 selectKernel”升级为 **public-API 五态机**：
- `configured`：pre 或 post `getActiveRuntime`/executeCode 探针确认活动核
- `selected`：`selectKernel` 返回 **true**（核可能尚未启动，expected）
- `needs-selection`：返回 **false**（取消）或命令无法自动调用
- `selection-requested`：命令完成但无明确 boolean
- `failed`：扩展缺失或命令抛错

配套：pre/post 成对 probe、Python 扩展 best-effort 激活、visibility/command 诊断、`configureResult` guidance、descriptions 列出五态。

## Upstream / pre-92 tip
`notebookEnv` 已有 multi-op 与 `getActiveRuntime`（info 用）。configure 在 92 仅为 best-effort 触发，无 status 机、不读 selectKernel 返回值。

## Target adaptation
- Owner：`sdks/vscode/src/notebook/env.ts`
- 新增 `ConfigureStatus`/`ConfigureProbe`、`probeConfigureRuntime`（复用 `getActiveRuntime`，不读内部 controller）、`resolveConfigureStatus`、`configureResult`、`isPythonNotebookLike`
- `configureNotebook` 节点：meta → Jupyter 激活 → Python best-effort → showNotebook → **preCheck**（已就绪则直接 configured）→ 命令发现 → selectKernel **捕获 result** → **postProbe** → resolve status
- `!hasSelect` / not-invoked → needs-selection（非 failed）
- `result === false` → needs-selection（禁止假 selected）
- descriptions configure 行与五态对齐

## Why not light-weight substitute
original 的核心升级正是 public probe + boolean 判别；降级为“触发即 selected”会产生 **隐藏成功**（用户取消仍 selected），并与 tool description 五态广告冲突。

## Verification
- tip `states/0094-…` / `0093-…`：`resolveConfigureStatus` 存在；`"configured"` 与 `selection-requested` 可达代码路径存在
- materialize 93/94 通过；typecheck-0094-2026-07-22T06-24-11-135Z 全绿
- E/C 见文末

## Materialize / typecheck provenance
- `bun .temp/patches/src/apply-cumulative.ts --materialize 94 --rebuild` tip `states/0094-81efcbb1d8cb`
- typecheck report `typecheck-0094-2026-07-22T07-25-34-017Z.md`；install fingerprint `67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a`
- `sdks/vscode` 不在 monorepo typecheck 矩阵；行为靠完整函数体 + brace 平衡 + 状态路径 grep 证明


## Call chain
Agent → plugin `vscode_notebook_env` operation=configure → bridge POST `/notebook/env` → `notebookEnv` → `configureNotebook` →
1. notebookMeta
2. Jupyter getExtension/activate or failed
3. Python-like heuristic + optional activate
4. showNotebookDocument (non-fatal)
5. preCheck = probeConfigureRuntime → if configured return
6. getCommands public+all → if selectKernel missing → needs-selection
7. executeCommand(selectKernel) capture raw result
8. postProbe = probeConfigureRuntime
9. resolveConfigureStatus(post, invoked, accepted, rejected, error)
10. configureResult → summary `Status: …` + guidance + data.status

## Status truth table
| pre configured | select result | post configured | status |
|---|---|---|---|
| true | (skipped) | — | configured |
| false | true | true | configured |
| false | true | false | selected |
| false | false | false | needs-selection |
| false | non-bool | false | selection-requested |
| false | throw | — | failed |
| false | not invoked | false | needs-selection |

## Three-way
| | original SMARK | light-weight wrong port | this current |
|---|---|---|---|
| probe | yes | no | yes via getActiveRuntime |
| boolean select | yes | no | yes |
| five statuses | yes | 3 | five |
| descriptions | five | five lying | five aligned |


## selectKernel args (post-r6 fix)
executeCommand(SELECT_KERNEL_CMD, { notebookUri: notebook.uri, skipIfAlreadySelected: true })
绑定 filePath 对应 notebook；已选核跳过重复弹层。与 restart 路径 notebookUri 一致。


## Description completeness
info: “after at least one cell has run” + Python path/version guidance.
restart: “Only restart when strictly necessary… prefer re-running cells”.
configure: five-state status text kept.

## Files / hashes

## Risk accepted
- 无真实 Jupyter UI E2E；以 status 分支与 public API 探针契约表达。
- TUI outputPreview 仍用 4 行 collapse（非 SMARK BlockTool 主面）。

- current: `0093-3ae1d112f256.patch`
- sha256: `d2b1aab791131bae135f974a13058bb888aec706151d18866775f8fa7e393306`
- bytes: 20933
- E=329 C=61 need=50 gate=PASS
- updated: 2026-07-22T06:25:00Z

## Independent dual audit (batch 90-94)
- FORWARD: **PASS** all five, no open B/G — task `ses_07747ffb8ffeoOHdlE46H0FgNE` (r11)
- REVERSE: **PASS** all five, no open B/G — task `ses_07747ff96ffe5KTvNREEFo8UZM` (r11)
- Tip: `states/0094-81efcbb1d8cb`
- Typecheck: `typecheck-0094-2026-07-22T07-25-34-017Z.md` (No failures; install `67995d9429bc…dab9c2a`)
- Batch release: **PASS**
