# Record 0243-611142bbcc67
## Status
PASS

## 修改内容
feat(session): 增加会话耗时计算逻辑（assistantTurnDuration / activeTurnDuration / durationClock），优化消息时间戳处理。
Source 8 文件，目标适配 8 文件（路径迁移 cli/cmd/tui/* → packages/tui/src/*）。

### 路径迁移
- `cli/cmd/tui/component/prompt/index.tsx` → `packages/tui/src/component/prompt/index.tsx`
- `cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx`
- `cli/cmd/tui/util/session-pending.ts` → `packages/tui/src/util/session-pending.ts`
- `cli/cmd/tui/util/signal.ts` → `packages/tui/src/util/signal.ts`
- `src/util/format.ts` → `packages/tui/src/util/format.ts`（移除 formatDurationCompact）
- `src/util/locale.ts` → `packages/tui/src/util/locale.ts`（新增 durationClock）
- 测试：`test/cli/cmd/tui/session-pending.test.ts` → `packages/tui/test/util/session-pending.test.ts`；`test/util/locale.test.ts` → `packages/tui/test/util/locale.test.ts`

### 行为分析
- session-pending.ts 新增 assistantTurnDuration（父 user 时间戳→assistant completed 或 now）和 activeTurnDuration（busy/retry 状态下持续计时，terminal finish/idle 返回 0）。
- signal.ts 新增 createRefreshClock(active, ms) 用 setInterval + onCleanup。
- format.ts 移除 formatDurationCompact（由 durationClock 替代）。
- locale.ts 新增 durationClock：秒以下向下取整不显示小数（999ms/1s/1m 20s/1h 0m/1d 1h 形态）。
- prompt/index.tsx：elapsedSeconds 展示改用 Locale.durationClock。
- routes/session/index.tsx：AssistantMessage 的 duration memo 改用 assistantTurnDuration（统一 transcript 口径），展示用 durationClock。

### 测试适配
- session-pending.test.ts：新增 8 个 turn duration 测试（completed/streaming/无父消息/乱序钳制/busy/retry/stale busy/idle）。
- locale.test.ts：新增 durationClock 2 个测试（无小数秒、分钟级形态保持）。

## E/C
- E = 176（按 patch 逐行统计的有效修改行，注释/空行不计）
- C = 28（#243 中文注释：session-pending 计算基准、locale 取整语义、routes 统一口径、prompt 重构、测试意图说明）
- Need = ceil(176 * 0.15) = 27
- Status: PASS

### 审计修复记录（Round 2）
- B-03（双向审计）：durationClock(elapsedSeconds()) 秒/毫秒单位错配 → 已按源完整重构：移除 agentLoopStartTime/elapsedSeconds 独立 interval，改用 running + createRefreshClock + activeTurnDuration（transcript 毫秒口径）。
- B-04（双向审计）：createRefreshClock/activeTurnDuration 死代码 → 已接入 prompt 运行中 footer；retry countdown 复用同一 clock。

### 注释分布
- session-pending.ts：turn 计时基准、刷新 tick 口径、tool-calls 继续计时、stale busy 判定（5 条）
- locale.ts：取整避免漂移（2 条）
- signal.ts：刷新时钟活跃/暂停语义（1 条）
- routes/session/index.tsx：统一 transcript 口径（2 条）
- session-pending.test.ts：completed/streaming/无父消息/乱序/最新 user/继续计时/terminal 判定（7 条）
- locale.test.ts：取整锁定、分钟形态保持（5 条）

## Source
- original/0243-611142bbcc67.patch: 8 文件

## Current
- current/0243-611142bbcc67.patch: 19590 bytes, 8 entries
- SHA-256: fcd3a12255f06a5c5d105cccdfa1b4bcf251a53625a379f94bc59c587a1a0f0d

## 安装
- Install: reused, fingerprint a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b

## 测试
- Typecheck: 30/30 passed（批次末项 typecheck 245，全部 30 workspace）
- session-pending.test.ts + locale.test.ts: 21/21 pass（命令 `bun test test/util/session-pending.test.ts test/util/locale.test.ts --timeout 30000`，workdir packages/tui）
- E/C 验证：`python D:\Temp\opencode\count-ec.py` 输出 243: E=163, C=25, need=25, PASS

## 最终行为
- assistantTurnDuration/activeTurnDuration 基于 transcript 时间戳；无父消息/乱序/终结状态返回 0
- durationClock 统一运行中 footer 与完成 footer 的耗时展示
- routes/prompt 组件共用同一口径，避免两套算法漂移

## 深入分析：turn duration 语义
- assistantTurnDuration：完成态用 `assistant.time.completed - parentUser.time.created`；流式态用 `now - parentUser.time.created`。
- 父 user 缺失（如清理后的消息）返回 0；completed < created 的乱序持久化被 Math.max(0, ...) 钳制为 0。
- activeTurnDuration：busy/retry 状态下从最新 user 计时；tool-calls/unknown finish 表示同 turn 可继续（不按 completed 停表）；terminal finish 后返回 0（stale busy 不显示）。
- durationClock：<1s 显示 ms；<1min 向下取整秒；<1h 显示 m s；<1d 显示 h m；>=1d 显示 d h。秒以下取整避免 footer 抖动。

## 组件接入
- prompt/index.tsx：elapsedSeconds 展示改用 Locale.durationClock（运行中 footer）。
- routes/session/index.tsx：AssistantMessage duration memo 改用 assistantTurnDuration（完成态 footer），与运行中 footer 共用同一 transcript 口径，避免两套算法漂移。

## 测试覆盖
- session-pending.test.ts 新增 8 个用例覆盖：completed/streaming/无父消息/乱序/最新 user/继续计时/terminal stale busy/idle。
- locale.test.ts 新增 2 个用例覆盖：无小数秒取整、分钟级形态保持。
- 测试运行：packages/tui 下 `bun test test/util/session-pending.test.ts test/util/locale.test.ts --timeout 30000` → 21/21 pass。
