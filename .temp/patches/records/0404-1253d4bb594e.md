# Record 0404-1253d4bb594e

## Identity
- manifest parent: 961b546ad391 (0403)
- subject: chore(docs): 更新移动文档
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0404-1253d4bb594e` (passed 404/404)
- current patch: `.temp/patches/current/0404-1253d4bb594e.patch` (446815 bytes raw, 18 entries: 17 new docs + 1 gitlink)

## Source intent
Documents-only maintenance commit: 17 new Markdown documents plus the
`thirdparty/chatgpt-browser-agent` submodule pointer bump. The docs are a
Claude Code ↔ opencode study corpus (MASTER_REPORT + per-area 对比 docs with
Chinese filenames), two DB-space planning docs, and the repository README for
the study folder. No runtime code changes.

## Source patch anatomy (18 entries)
- docs/db-space-diagnosis.md (279), docs/db-space-optimization-design.md
  (2582).
- docs/opencode_claudecode_study_docs/: MASTER_REPORT.md (1859), README.md
  (70), claudecode-opencode-context-governance-plan.md (1055),
  context-management-scheme (2).md (718), context-management-scheme.md (392),
  copilot_schema_and_desc.md (3038).
- docs/opencode_claudecode_study_docs/: 00_总览与最终建议.md (84) … 08_
  源码检查清单与引用文件列表.md — 9 Chinese-named files whose diff headers
  are C-quoted with octal UTF-8 escapes.
- thirdparty/chatgpt-browser-agent: gitlink index b3e79c5d1846..a70111b47806
  (mode 160000).

## Target mapping (state 0403)
No target files exist — all 17 docs are additions at their canonical paths;
nothing to adapt. Gitlink chain check (per task instructions):
0338 d78e57cba1 → 0345 f4de5136bf → 0375 8144b3de709d (current target index,
verified via `git ls-files -s` in state 0403) → 0404 sets a70111b47806. The
resource is a source-side pointer; the target's submodule checkout is empty
(no .git) exactly like the 0345/0375 precedent — `git apply --index` accepts
the gitlink hunk without validating the target commit object, and the
materializer's simulation verified that.

## Key port decisions
1. parse-only the C-quoted headers: `diff --git "a/...\346\200\273..." 
   "b/..."` splits on the quoted `" "` pair, then octal-decodes bytes as
   UTF-8. First inject run forgot to strip the `b/` prefix from quoted paths
   and created a stray `w0401\b` directory with misnamed docs; corrected run
   rewrote the 17 files at their proper paths (the stray dir remains on disk,
   unstaged, and invisible to git diff; it was excluded from the index after
   `git add -A` via `git restore --staged`).
2. Doc content written byte-verbatim from the original patch post-image
   (LF, newline-terminated); a full-content comparison against the source
   patch reconstruction passed for every one of the 17 files ("all docs
   verbatim OK", including the 9 Chinese-named).
3. E/C: all 17 files are .md (excluded) and the gitlink is excluded as
   git-generated (mode 160000) → E=0, C=0, PASS trivially, matching the
   source's own audit class.
4. Gitlink hunk synthesized in a scratch repo (`git update-index --add
   --cacheinfo 160000,8144b3de709d,…` → commit → `--cacheinfo` to the new sha
   → `git diff --cached`), so the hunk's pre-image is the state-0403 commit
   (8144b3de709d) — the exact index value the materializer will diff against.
   Verified the state 0404 index afterwards: `160000 a70111b47806…`.

## E/C audit
E=0, C=0, need=0, PASS (docs + gitlink only).

## Materialization
- `--materialize 404` → "passed 404/404" on the first run WITH the gitlink;
  the state 0404 index carries the a70111b47806 pointer and 71 staged docs-
  related paths. Rebuilt later as part of the 0401-fix chain; still passed.
- Work copy sync: added the 17 docs (`git add -A`), unstaged the stray `b\`
  tree, and pinned the work-copy gitlink via `git update-index --cacheinfo`
  to a70111b47806; final comparison vs state 0404: 0 name diffs, staged diffs
  equal.

## Line accounting
- 17 docs: 9993 inserted lines (+1 for the gitlink line = 9994 patch lines).
- fix404.diff bytes: 446815 (docs hunk 446813 incl. C-quoted headers + gitlink
  hunk).

## Risks / notes
- The gitlink target commit a70111b47806 does not exist as a local object in
  the target submodule (no checkout initialized); submodule operations that
  dereference it (clone/update) are impossible until a remote provides it —
  same limitation documented in records 0345/0375. The pointer is metadata
  that keeps the fork's manifest faithful to the source chain.
- First-run misparse produced a stray `D:\Temp\opencode\w0401\b` tree; no
  tracked content was affected and the materialized states are unaffected;
  the dir is left for the user to delete at their convenience.

## Verification summary
- E/C gate: PASS (0/0).
- Materialize: passed 404/404 twice; index gitlink verified in the published
  state.
- Docs fidelity: 17/17 byte-identical to the source patch post-image.
- Batch typecheck 405: green (this item carries no typed code).