# Record 0103-74a9ca56ea81

## Source intent
#103 构建/CI 三轴：
1. **build.ts**：解析 `--arch=` 为 `archFilter`；`osFilter` 分支过滤时要求 `!archFilter || item.arch===archFilter`。
2. **workflow**：Linux/macOS 打包路径 `../../../`→`../../`（相对 `dist/.../bin` 落到 `dist/`）；upload-artifact 加 `if-no-files-found: error`；Windows **仅 x64**（`--arch=x64`），去掉 arm64 zip 打包。

## 上游 / tip@102 设计
- tip `build.ts` 已有 `osFilter` / `singleFlag` / baseline 逻辑，**无** archFilter。
- tip workflow 已是 SMARK 中文注释版：三平台 artifact；Windows 打 x64+arm64 zip；tar 使用 `../../../`。
- 上游更强点：setup-node/python/msbuild 链、路径过滤 push 触发——必须保留。

## 三方比较
| 语义 | source | tip | 决策 |
|------|--------|-----|------|
| archFilter | 有 | 无 | 加入 osFilter 链 |
| tar ../../ | 有 | ../../../ | 按 source 修正产物路径 |
| win only x64 | 有 | 双架构 | 对齐 source |
| if-no-files-found | 有 | 无 | 三平台补齐 |
| deb Description 空格 | 微调 | 已有前导空格 | 保留 tip |

First divergence：无 arch 过滤；Windows 默认双架构；产物路径多一层 `../`。

## 目标适配
- build.ts：在 `osFilter` 旁声明 `archFilter`；仅改 osFilter 三元真分支的 filter 谓词。
- workflow：路径修正 + Windows 单架构 + if-no-files-found；保留 tip 的 setup 工具链与中文注释骨架。
- 不迁移 job 到 matrix、不改 trigger。

## 失败修订
- 无（对 tip@102 上下文一次生成成功）。

## 调用链 / owner 验证

### owner
- `packages/opencode/script/build.ts`：CLI 二进制目标选择。
- `.github/workflows/build-opencode.yml`：CI 打包与上传策略。
### 调用链
- CI `bun run script/build.ts --os=win32 --arch=x64` → `archFilter` → `allTargets.filter` → 仅 windows-x64 产物。
- Package steps：`cd dist/.../bin` → `tar/Compress` 到 `../../` → artifact 路径匹配 dist 根。
### 验证点
- tip build.ts 存在 `archFilter`。
- tip workflow Windows 命令含 `--arch=x64`，无 `windows-arm64` 打包。
- Linux/macOS tar 使用 `../../opencode-...`。
- 三平台 upload-artifact 均 `if-no-files-found: error`（#104 前仍为 artifact）。
### 与 #104 边界
- #103 仍用 artifact；#104 才切 release。禁止在 #103 预写 release job。




## 设计取舍（详细）
### 为什么不整体覆盖上游
上游 monorepo 的依赖图、CI 工具链与 SMARK source 提交时已分叉。整体粘贴 source 文件会：
1. 丢掉 tip 已合并的安全/性能注释与 setup 步骤；
2. 破坏与相邻 patch（#100 trusted、#103 arch、#104 release）的累计可应用性。
因此每个 commit 只做**最小语义补丁**：在 tip owner 上叠加 source 的可观察行为。

### 为什么注释算 C
E/C 门禁要求对重要 invariant 用中文解释「为什么」。配置/CI 中的 `# #NNN：...` 与 TS `// #NNN：...` 只要解释真实边界（版本钉死、架构过滤、release 原子性），即计入 C；禁止堆叠翻译 identifier。

### 与批次 101–105 的关系
- #101 先落地 run TOP/BOTTOM 与 interactive notebook ask，避免 #105 文案与 run 解析分叉。
- #102 独立依赖 pin，不依赖 notebook。
- #103/#104 顺序敏感：#104 的 workflow 以 #103 为 pre-image。
- #105 不修改 run.ts（TOP 已由 #101 处理），只强化 edit/resolve/描述。

### 自查清单
- [x] current 非空
- [x] 可从 index-1 状态 apply
- [x] E/C 过门
- [x] record sha256 与 current 文件一致
- [x] typecheck 批次末项通过
- [x] 无 original 修改、无测试删除

## E/C（本 patch 独立，非跨批累计）
- E=10 C=3 need=2 gate=PASS
- 合格中文解释性注释位置：
- L54: # #103：显式 --arch=x64，避免 Windows 默认双架构产物膨胀
- L67: # #103：产物落在 dist/ 下（../../ 相对 bin），仅打包 x64
- L85: // #103：允许 CI 用 --arch=x64 只打 Windows x64，避免默认双架构

## current 身份（必须与磁盘一致）
- path: `current/0103-74a9ca56ea81.patch`
- sha256: `4baba11079559dd3c0ca7068d5c23c3c304e9e705a39f3000f1e41bc755ff49d`
- bytes: 5043
- non-empty: yes

## 物化与 typecheck
- materialize 累计至 105：passed 105/105
- tip state: `states/0105-e8675ce41599`
- typecheck report: `typecheck-0105-2026-07-22T10-19-57-539Z`
- Install fingerprint: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- Source/target repository unchanged: true（该次报告）
- 无测试删除；original 未改。

## 最终行为（可观察）
见上文「目标适配」与 tip grep 结果；本 record 关闭前 current 可独立从上一项状态 apply。


## Independent dual audit (batch 101-105)
- FORWARD: **PASS** all five, no open B/G — task `ses_0769ee162ffeET0gNBckYycMsB`
- REVERSE: **PASS** all five, no open B/G — task `ses_0769ee119ffeIvgL0xG4nw5jJ1`
- Tip: `states/0105-e8675ce41599`
- Typecheck: `typecheck-0105-2026-07-22T10-29-22-204Z` (No failures)
- Install: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- #104 repair verified: package.json version authority + setup-bun path; no DEFAULT_VERSION/input
- Batch release: **PASS**
