# Record 0235-1035b75f6874

## Status
PASS（E=292, C=45, need=44；typecheck 30/30；daemon.test.ts 8/8（3 次稳定）、exit.test.tsx 2/2、notebook 12/12、preview-diff 8/8、filetype 2/2、session-list 19/19；materialize 235 成功）

## 修改内容
feat(daemon): 增强守护进程的启动和退出逻辑，添加信号处理和日志记录。source（v1.15.3→v1.15.4 目标线）对 daemon 生命周期、TUI 信号处理、SSE 计数日志的完整行为重建。

### 文件清单（8 个）
- `packages/opencode/src/cli/tui/worker.ts`（source `cli/cmd/tui/worker.ts`）— daemon 两段空闲退出保护（startup idle + 运行期 idle）、launcher watcher、gracefulShutdown(reason)、信号 reason、结构化日志、stop 超时兜底
- `packages/opencode/src/cli/cmd/tui.ts`（source `daemon.ts` 合并）— spawn env 加 `OPENCODE_DAEMON_LAUNCHER_PID: String(process.pid)`
- `packages/tui/src/context/exit.tsx` — 导出 `ExitSignals = ["SIGINT","SIGTERM","SIGHUP"] as const`
- `packages/tui/src/app.tsx` — SIGHUP 单信号块扩展为 ExitSignals 全量注册（acquireRelease 管理 register/remove 对）
- `packages/opencode/src/permission/reviewer/service.ts` — `defaultLayer` 包 `Layer.suspend(() => ...)`
- `packages/opencode/src/server/routes/instance/httpapi/handlers/global.ts` — SSE 连接/断开日志结构化参数 + request abort 监听（断开计数可靠通知）
- `packages/opencode/test/cli/cmd/tui/exit.test.tsx` — 新测试（2 个：ExitSignals 常量、@opencode-ai/tui 可独立导入）
- `packages/opencode/test/cli/tui/daemon.test.ts` — 3 个新 daemon 生命周期测试（startup idle 退出、launcher 死亡退出、launcher 存活不退出）+ #173 适配

### 目标适配决策
1. **thread.ts 懒加载无操作**：source 把 `import { tui } from "./app"` 改为 handler 内动态导入；目标 `TuiThreadCommand` 已用 `await import("../tui/layer")`（thread.test.ts 锁定 `not.toContain('import("./app")')`）→ 语义已存在，无操作。
2. **exit.tsx 跨包迁移**：source exit.tsx 是完整退出编排（signals+hooks）；目标 exit.tsx 是 8 行极简 context，退出编排在 app.tsx。适配：ExitSignals 常量放 exit.tsx（供测试），信号全量注册放 app.tsx（SIGHUP 块扩展，acquireRelease 管理——等价 source onCleanup 语义）。
3. **日志设施**：source 用 `@opencode-ai/core/util/log`；目标无此模块 → worker.ts 局部 `log = { info: (msg, fields) => console.log(...) }`（stdout 非 print-logs 时被丢弃，语义与 Log.init 一致）。
4. **global.ts abort 监听（关键适配）**：source 的断开通知依赖 PassThrough 版 eventResponse 的 onRequestAbort；目标 0234 是 Effect Stream 版，**Windows 上客户端 abort 不触发 Stream.ensuring**（0173 遗留，实测断开计数通知缺失）→ 适配：`eventResponse(request)` 注入 HttpServerRequest，`disconnect()` 去重函数（`disconnected` 标志），request.source（IncomingMessage）监听 `aborted`/`close` 事件 + Request 分支监听 signal abort；ensuring 也调 disconnect（共享去重）。**这是 235 测试（#173 断开 idle）的必要条5. **gracefulShutdown 清理序列总超时兜底（关键适配）**：目标 `InstanceRuntime.disposeAllInstances()` 与 `Server.stop(true)`（Effect HttpServer closeScopeOnce）在 Windows 顺序测试场景偶发挂起（实测挂 25s+）→ 整个 clearIfOwner 后的清理序列包在 3s 总超时 `Promise.race([清理序列, Bun.sleep(3_000)])` 内——clean-stop 语义不变，不论哪步挂起 daemon 都能完成 exit(0)。**这是 235 测试（startup idle / launcher dies / #173 断开 idle 等触发 gracefulShutdown）的必要条件**。fulShutdown）的必要条件**。
6. **#173 测试适配**：既有测试设 `IDLE=250` 无 STARTUP → 235 后 STARTUP 默认=IDLE=250ms 会在连接前退出 → 显式加 `OPENCODE_DAEMON_STARTUP_IDLE_TIMEOUT_MS: "5000"`（隔离其运行期 idle 语义，不改变断言）。
7. **worker.ts idle 模型**：目标 armIdleTimer（单段）扩展为 source 的两段模型——STARTUP_IDLE_TIMEOUT_MS（默认=IDLE）、LAUNCHER_PID（正整数校验）、LAUNCHER_POLL_MS=1s、isActive() 统一判定、三个 cancel 函数（带日志）、maybeScheduleStartupIdleShutdown（launcher-alive 重排）、startLauncherWatcher（首 client 前判活）；gracefulShutdown 参数化 reason + 三 cancel + 结构化日志。

## Source 行为意图（逐 hunk 重建）
- daemon.ts：spawn env 加 launcher PID——daemon 判活 launcher，首个 SSE client 前的孤儿清理。
- worker.ts：startup idle 覆盖「daemon 已起但 TUI 连上 /global/event 前崩溃」；launcher watcher 1s 轮询（仅首 client 前），launcher 死且无活跃工作 → 立即 shutdown；isActive()=sseClients>0 || SessionActivity>0 阻止退出；fire 时重查 + 日志；gracefulShutdown(reason) 带 hadClient/sseClients/sessionActivity 日志；SIGTERM/unhandledRejection/uncaughtException 带 reason；锁写入日志带 pid/port/externalUrl/launcherPID。
- exit.tsx：ExitSignals 全量注册 handler，onCleanup 注销（防重挂载重复监听）；exit 日志（requested/renderer destroyed/completed）；exitReason helper（undefined→normal、Error→name、其他→typeof）。
- service.ts：Layer.suspend 延迟 Config/Provider 实例化——保留 Session↔Permission 类型导入循环边界。
- global.ts：连接/断开日志带 sseClientCount。
- 测试：3 个 daemon 生命周期测试（startup idle 250ms 退出、launcher 987654321 判死退出、launcher=process.pid 存活 750ms）+ exit.test.tsx（ExitSignals + TUI 模块导入无循环）。

## 上游（v1.17.20）对比
- 目标已有 #152/#173 的 idle 模型（armIdleTimer、IDLE_TIMEOUT_MS 可配置、SSE 计数、SessionActivity）——235 在其上扩展两段保护，不重复实现。
- 目标 exit 编排（ExitProvider + destroyRenderer + acquireRelease SIGHUP）——235 扩展为全信号，保留既有 destroyRenderer 语义。
- 目标 Server.stop(true) 挂起是既有缺陷（closeScopeOnce）——235 暴露（gracefulShutdown 成为 Windows 常规路径），stop 超时兜底修复（写入 235，因为 235 的测试使该路径可到达）。
- 目标 Effect Stream SSE 的断开通知缺失（0173 遗留）——235 通过 abort 监听闭合（source 235 的 PassThrough 版有 onRequestAbort，语义等价）。

## E/C
- E=292（a=319 d=35 blank=16 comment=46），C=45（#235 前缀中文说明注释，len>12 且含中文），need=44 → OK。
- 注释位置：worker.ts（log 助手、锁日志、gracefulShutdown cancel、stop 超时、STARTUP/LAUNCHER 常量、isActive、cancel 三函数、armIdleTimer 分支、startup 重排、watcher、first-client、内部 server、预热、升级）、tui.ts（launcher PID）、exit.tsx（ExitSignals）、app.tsx（信号注册、register/remove 对）、service.ts（suspend）、global.ts（计数日志、abort 监听、disconnect 去重、source 双路径、aborted/close）、daemon.test.ts（startup idle、launcher 判死、launcher 存活、#173 适配）、exit.test.tsx（信号常量、导入无循环）。

## 安装/类型检查
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 235`：passed 235/235；install=reused，installInputSha256=a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b；typechecks 30/30 全部 passed。
- 期间修复：materialize 曾失败（0234 state 被 debug 脚本污染含 gs-debug → patch 上下文不匹配）→ 重新 materialize 234 重建干净基线 + 重建 0235 state + 重放全部修改 → 通过。

## 行为测试（.test-workspace/repo，packages/opencode 与 packages/tui）
| 命令 | 结果 |
|---|---|
| `bun test ./test/cli/tui/daemon.test.ts --timeout 60000` | 8 pass / 0 fail（18 expect）|
| `bun test ./test/cli/cmd/tui/exit.test.tsx` | 2 pass / 0 fail（4 expect）|
| `bun test ./test/plugin/vscode-notebook-tool-summary.test.ts ./test/plugin/vscode-bridge.test.ts` | 12 pass / 0 fail（75 expect）|
| `bun test preview-diff.test.ts`（packages/tui）| 8 pass / 0 fail（27 expect）|
| `bun test ./test/server/session-list.test.ts --timeout 30000` | 19 pass / 0 fail（48 expect）|

## 失败修订记录
1. **daemon.test.ts 3 败（startup idle、launcher dies、#173）**：根因调查——(a) startup idle 测试单独跑通过、顺序跑失败 → gs-debug 定位卡在 `internalServer.stop(true)` → stop-debug 定位卡在 `closeScopeOnce`（Effect HttpServer scope 关闭挂起，Windows 顺序场景）→ `stopWithTimeout`（2s 超时）修复；(b) #173 断开 idle 失败 → 断开通知缺失（Effect Stream ensuring 在 Windows abort 不触发）→ `request.source` 实测是 `IncomingMessage`（非 Request）→ 监听 aborted/close 事件 + disconnect 去重 → 修复。
2. **测试环境 OPENCODE_DAEMON_LAUNCHER_PID 注入**：opencode 会话注入 env（OPENCODE_PID 等）——测试前 `Remove-Item Env:OPENCODE_DAEMON_LAUNCHER_PID` 清理（环境操作，非代码）。
3. **materialize 失败（0234 污染）**：debug 脚本曾改 0234 state → 重新 materialize 234 + 重建 0235 + 全量重放。
4. **E/C 不足**：从 CN=38 补 6 条（w-internal/w-warmup/w-upgrade/g-source/g-events/d-startup）→ CN=44 OK。
5. **regen 需 new-file mode**（exit.test.tsx）——regen-fixed.py 已支持（0234 时修复）。

## 一致性说明
- source original 0235 全部 hunk 已重建：worker.ts（两段 idle + launcher watcher + reason + 日志）、daemon.ts（launcher PID env）、thread.ts（懒加载已存在）、exit.tsx（ExitSignals + app.tsx 信号）、service.ts（Layer.suspend）、global.ts（计数日志）、exit.test.tsx、daemon.test.ts（3 测试）。
- 未删除任何 source 测试或行为；未空化任何 hunk；current patch 非空（20829 bytes）。
- materialized state 0235 可累计 apply（passed 235/235）；states 保留 5 个（0231-0235）。
- #173 测试适配（STARTUP=5000）是目标既有测试的语义隔离（非 source 修改），写入本 record。

## DUAL-AUDIT SEAL
批次 231-235 第三轮双独立审计封印（2026-08-06）：
- 正向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 逆向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 三轮修复验证：G-01（231 record 长度）✓、B-01（233 filetype none 哨兵）✓、B-01 r2（235 gracefulShutdown 3s 总超时）✓
- 测试：daemon 8/8（×2稳定）、filetype 2/2、exit 2/2、notebook 12/12、preview-diff 8/8、session-list 19/19、typecheck 30/30
- install 指纹 a4aae9d1（reused）、source HEAD 未变、integrity 无失败
- BATCH PASS
