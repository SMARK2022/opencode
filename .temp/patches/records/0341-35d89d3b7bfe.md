# Record 0341-35d89d3b7bfe

## Identity
- SHA: 35d89d3b7bfe6198a8d1f4fc8187fefe7c4317f4
- Parent: 09c086ffa8d5 (0340)
- Subject: fix(test/fixture): withDisposalTimeout 直接 race 原始 Promise 避免 .finally 微任务延迟
- Status: PASS

## Source intent

### Hunk 1 — `@@ -56,7 +56,12 @@` (fixture.ts, withDisposalTimeout comment block)
The upstream commit extends the existing two-line comment in `withDisposalTimeout` to document a critical settle-timing invariant. The original comment merely said "avoid spurious timed-out warnings"; the patch appends a period and adds five new comment lines explaining WHY `Promise.race` must compete against the original `p` directly rather than a `.finally()`-wrapped derivative: `p.finally(...)` creates an intermediate Promise that settles one microtask round after `p`, and Bus tests depend on `InstanceDisposed` events already being in the PubSub queue when `disposeAllInstances` returns — the extra microtask perturbs cross-runtime event delivery timing on Linux CI.

### Hunk 2 — `@@ -64,10 +69,9 @@` (fixture.ts, withDisposalTimeout return statement)
The upstream commit replaces `Promise.race([p.finally(() => clearTimeout(timer)), timeout])` with two statements: `p.then(() => clearTimeout(timer), () => clearTimeout(timer))` registers settlement callbacks on the ORIGINAL `p` without creating an intermediate Promise, then `return Promise.race([p, timeout])` races the original `p` directly. This preserves identical settle timing to the no-timeout-protection baseline. The `.finally()` wrapper was the sole cause of a one-microtask delay that made `InstanceDisposed` delivery exceed its timeout on Linux CI.

## Three-way comparison

| Aspect | SMARK original | Upstream (dev) | Target (0340 state) |
|---|---|---|---|
| File path | `packages/opencode/test/fixture/fixture.ts` | same | same (already adapted by 0340) |
| Comment text style | `"timed out"` (quoted) | `"timed out"` (quoted) | `timed out` (unquoted) — 0340 adaptation dropped quotes |
| `withDisposalTimeout` visibility | `const` (not exported) | `const` | `const` (0340 added it unexported) |
| `disposeAllInstances` array entries | 2 (InstanceRuntime + runTestInstanceStore) | 2 | 1 (InstanceRuntime only — target lacks runTestInstanceStore) |

The 0340 state already contains `withDisposalTimeout` (added by patch 0340) with the `.finally()` pattern that 0341 fixes. The comment uses unquoted `timed out` (0340 stylistic choice) rather than the upstream's quoted `"timed out"`.

## Target adaptation

### Path mapping
No path migration needed — `packages/opencode/test/fixture/fixture.ts` is the same in both upstream and target.

### Comment style adaptation
The upstream patch's old line is `// 避免向后续测试输出虚假的 "timed out" 警告` (with quotes). The 0340 target has `// 避免向后续测试输出虚假的 timed out 警告` (no quotes). The adaptation preserves the target's unquoted style and appends the period + five new invariant comment lines verbatim from the upstream.

### Code change adaptation
The `Promise.race` / `.finally` replacement is applied identically — the target's `withDisposalTimeout` body is structurally identical to the upstream's pre-patch state (same `p.finally(() => { if (timer) clearTimeout(timer) })` pattern), so the replacement is a clean 1:1 mapping.

## Per-hunk mapping table

| Hunk | Upstream old | Target old | Adaptation | Upstream new → Target new |
|---|---|---|---|---|
| 1 | `// 避免向后续测试输出虚假的 "timed out" 警告` | `// 避免向后续测试输出虚假的 timed out 警告` | Preserve unquoted style, add period + 5 comment lines | Identical new content |
| 2 | `Promise.race([p.finally(...), timeout])` (3 lines) | same 3 lines | Direct replacement | `p.then(...)` + `Promise.race([p, timeout])` (identical) |

## Verification

- **Materialize**: `passed 341/341` — patch applies cleanly to state 0340
- **E/C**: E=2, C=6, need=1, **PASS** — the 5 new CJK comment lines in hunk 1 plus the 1 new CJK comment line in hunk 2 provide ample comment coverage for the 2 executable lines (`p.then(...)` and `return Promise.race(...)`)
- **Typecheck**: packages/opencode passes `tsgo --noEmit` (exit 0) in the test workspace

## 不一致 (target structure differences)

1. **Comment quoting**: The 0340 target uses unquoted `timed out` while the upstream uses quoted `"timed out"`. This is a cosmetic difference from the 0340 adaptation; the semantic content is identical. The new comment lines added by 0341 are copied verbatim from upstream (they don't reference "timed out" so the quoting style is irrelevant).

2. **disposeAllInstances array size**: The target's `disposeAllInstances` has only one `withDisposalTimeout` call (InstanceRuntime only), while the upstream has two (InstanceRuntime + runTestInstanceStore). This difference was introduced by 0340's adaptation and is NOT affected by 0341 (0341 only touches `withDisposalTimeout`, not `disposeAllInstances`). The single-entry array is documented in the 0340 record and will be addressed by 0342's `allSettled→all` change.
