# Record 0112 — c3c396034afb

## Identity
- Index: **112**
- Source SHA: `c3c396034afb…`
- Subject: feat: 会话列表预览最近用户消息（previewLines）
- Current: `.temp/patches/current/0112-c3c396034afb.patch`
- SHA-256: `ff021f72413c7a4e1ceac7453aeec3a4cbe77acd71f411d62dac3260ed2438e2`
- Bytes: **6869** (original 9213 ≈ 81%; path migrate + tip details already present)
- Depends on: 111

## Source intent (original files)
1. `dialog-select.tsx`
   - `DialogSelectOption.previewLines?: string[]`
   - Virtual height adds sum of previewLines lengths
   - Pass `previewLines={option.previewLines}` into **Option**
   - Option renders `<Show when={previewLines}><For each=… tree prefixes + truncate`
2. `dialog-session-list.tsx`
   - Constants: PREVIEW_LINES=1, MESSAGE_LIMIT=20, SESSION_LIMIT=50
   - Prefetch last user message via `session.messages` for first N sessions
   - Filter synthetic/ignored text parts; join whitespace
   - Attach `previewLines` array on options
   - Soft-catch fetch errors (list must still open)

## Upstream / tip design
- Owners: `packages/tui/src/ui/dialog-select.tsx`, `packages/tui/src/component/dialog-session-list.tsx`
- Tip already has **details** multi-line rendering outside Option (category metadata)
- Height historically: `1 + details.length` per option
- Session list uses Solid resources + debounced search; SDK client available

## Prior shrink / defect (must not repeat)
Height-only patch (count previewLines in rows() but never paint) was dual-audit **BLOCK**.  
Current must include **Option-level render** matching original structure (not height-only).

## Three-way fuse
| Piece | Upstream | SMARK | Result |
|-------|----------|-------|--------|
| details lines | present | n/a | keep |
| previewLines type | absent | add | add |
| height | details only | +preview | +preview |
| Option render | no preview | Option For | Option For + prop pass |
| prefetch | absent | limits + effect | same |
| fold to N lines | single-line array often | word fold for N | word fold (supports #115=2) |

## Concrete current mapping
- Path remap: opencode CLI TUI → packages/tui
- Option signature + render Show/For for previewLines
- Call site `previewLines={option.previewLines}`
- Session list createEffect(on sessions) with unloaded filter + Promise.all
- previewLines reduce by words, max SESSION_LIST_PREVIEW_LINES, width ~72

## E/C
- E=77, C=13 (need ≥12)
- Comments: option field; height; Option contract; Option render; constants; map; unloaded; user msg; soft fail; fold; oversize last line

## Verification
- Dry-run from state 111: pass
- Chain 111–115: pass
- Tip after 115: `props.previewLines` / `previewLines={option.previewLines}` present
- Typecheck batch 115: packages/tui passed

## Final behavior
Opening session list prefetches up to 50 sessions’ last user text; each option can show 1 preview line (#115 → 2) under the title via Option chrome, with scroll height reserved.


## Call-chain evidence (tip)
1. `DialogSessionList` loads sessions → effect schedules `sdk.client.session.messages({sessionID, limit:20})`.
2. Last user message text stored in `previews[sessionID]`.
3. Option builder maps to `previewLines: string[]` via word-fold reducer.
4. `DialogSelect` includes preview length in virtual `rows()` height.
5. `<Option previewLines={option.previewLines} />` paints tree-prefixed muted lines.

## Non-goals
- Does not change server session list API.
- Does not persist previews to disk.
- Does not block UI on network; empty preview is valid.

## Dual-audit repair (post B-02 dual decode)
- Preview decode uses only `result.data` array; role only `info.role` (aligned with tip sync/SDK).
- Removed bare-array and `x.role` fallbacks.
- Current SHA-256: b1a3f544d6017cbabd41a0b99e6b7cf1f0aa0f3dc37d22fb7112502ac3c7669f
- Bytes: 6869

## Dual-audit repair (layout B-01)
- Moved `previewLines` render out of Option (title row flex children).
- Now outer column siblings next to `option.details` under the same parent column as original intent.
- Option no longer takes previewLines prop.
- Current SHA-256: `ff021f72413c7a4e1ceac7453aeec3a4cbe77acd71f411d62dac3260ed2438e2`
- Bytes: 6869

## Independent dual-audit verdict
- FORWARD (111→115): **PASS** — no open B/G
- REVERSE (115→111): **PASS** — no open B/G
- Tip: `states/0115-6e38594c33fc`
- Materialize: `materialize-0115-2026-07-22T19-25-28-236Z`
- Typecheck: `typecheck-0115-2026-07-22T19-26-07-344Z`
- Install: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- Batch release: **PASS**
