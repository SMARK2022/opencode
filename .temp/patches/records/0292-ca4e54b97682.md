# Record: 0292-ca4e54b97682

## 基本信息
- index: 0292
- sha12: ca4e54b97682
- subject: feat(test): 修正所有测试不通过问题
- source date: Wed, 10 Jun 2026 13:20:43 +0800

## Source 行为重建
原始 patch（32KB，662 行）修改 6 个文件：
1. **TUI session/index.tsx**：AutoReviewLine 重构——提取 openReview 函数，box 添加 width/height/flexShrink，text 添加 onMouseUp；新增 openAutoReviewSession 和 openAutoReviewFromToolChrome 函数；InlineTool/BlockTool 添加 navigate/review context 和冒泡兜底
2. **session-integration.test.ts**：删除 "message cell border" describe 块
3. **session-message-render.test.tsx**：新增 3 个 auto-review 点击测试 + clickVisibleText 改造
4. **install-script.test.ts**：Windows WSL bash 路径处理——BASH 常量、bashPath/bashPathForFile/bashEnv/bashCommand/installScriptForBash/quoteForSh 辅助函数
5. **installation.test.ts**：NpmConfig.registry 断言更新
6. **shell.test.ts**：always pattern cmd→dir 适配

## 三方比较
- **TUI session/index.tsx**：路径迁移 packages/opencode/src/cli/cmd/tui/ → packages/tui/src/；目标 AutoReviewLine 已有 #209/#214 注释；目标 InlineTool onMouseUp 使用 `() => {`（无 evt 参数）；目标 BlockTool 已有 `(evt?: TuiMouseEvent) => {`
- **session-integration.test.ts**：目标不存在，跳过（无删除对象）
- **session-message-render.test.tsx**：目标不存在，跳过（需测试基础设施 withRenderedSession 等，目标未移植）
- **install-script.test.ts**：目标存在，有 #167 注释需适配
- **installation.test.ts**：目标已用 `.endsWith()` 方式实现"不硬编码 registry"的同一目标，跳过
- **shell.test.ts**：目标已应用（更完整的 powershell/pwsh 支持），跳过

## 适配内容
- **TUI session/index.tsx**：路径迁移；适配 #209/#214 注释上下文；提取 openReview/openAutoReviewSession/openAutoReviewFromToolChrome 函数；InlineTool/BlockTool 添加冒泡兜底；保留 #209/#214 注释，新增 #292 注释
- **install-script.test.ts**：适配 #167 注释上下文；添加 Windows WSL bash 路径处理辅助函数
- **跳过 4 个文件**：session-integration.test.ts（不存在）、session-message-render.test.tsx（不存在）、installation.test.ts（已实现）、shell.test.ts（已应用）

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（32108 bytes）
- 编辑后 SHA-256: e64ddcae3d32df7df6a08d6ffa71e241570570722f6bfcb43cc461c67be4d877
- 编辑后字节: 16043
- materialize 292: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| TUI session/index.tsx AutoReviewLine | packages/tui/src/routes/session/index.tsx | 路径迁移 + 上下文适配 |
| TUI session/index.tsx openAutoReviewSession | 同上 | 新增函数 |
| TUI session/index.tsx InlineTool | 同上 | 添加 navigate/review + 冒泡兜底 |
| TUI session/index.tsx BlockTool | 同上 | 添加 navigate/review + 冒泡兜底 |
| session-integration.test.ts | 删除 | 目标不存在 |
| session-message-render.test.tsx | 删除 | 目标不存在 |
| install-script.test.ts | 适配 #167 上下文 | Windows WSL bash 路径处理 |
| installation.test.ts | 删除 | 已用不同方式实现 |
| shell.test.ts | 删除 | 已应用 |

## E/C 计算
- E (有效修改行): ~60（TUI AutoReviewLine 重构 30 + InlineTool/BlockTool 适配 15 + install-script 辅助函数 15）
- C (中文注释行): 10
  - TUI: openAutoReviewSession 说明(2) + openAutoReviewFromToolChrome 说明(2) + #292 标记(2)
  - install-script: #292 Windows WSL 说明(2) + #292 bashPath 说明(2)
- 满足 E > 0 时 C >= max(1, ceil(60 * 0.15)) = 9，10 >= 9 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)

## 不一致
- TUI 文件路径迁移：packages/opencode/src/cli/cmd/tui/ → packages/tui/src/
- session-integration.test.ts 不存在：TUI 迁移时未带到 packages/tui/test/
- session-message-render.test.tsx 不存在：测试基础设施（withRenderedSession 等）未移植
- installation.test.ts 已用不同方式实现：.endsWith() 替代 NpmConfig.registry
- shell.test.ts 已应用：目标已支持更完整的 powershell/pwsh

## 物化验证
- 待验证

## 审计历史
- 第一轮双审计：PASS（第一轮双审计通过）

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 291-295，第一轮）
- 正向审计 (ses_011d96b20): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_011d9572e): 5/5 PASS, 无 B/G (N-01~N-02 非阻塞)
- materialize 295: passed 295/295
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)
- E/C: 291=16/8, 292=60/10, 293=80/12, 294=2/1, 295=5/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#215 closeSink finalizer, #289 normalizePowerShellOutput）保留为基线