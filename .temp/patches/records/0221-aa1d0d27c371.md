# Patch 0221 — fix(shell): 修改 PowerShell 模块的日志偏好设置为默认静默，避免输出污染

## 1. Identity
- Index: 221
- Source SHA: aa1d0d27c371fd644a41dbbc60f10505c95bf6fb
- Parent SHA: 0436a82b95aaec4be075ff898984a68a8c8cecbc（patch 220）
- Subject: fix(shell): 修改 PowerShell 模块的日志偏好设置为默认静默，避免输出污染
- Original patch: `.temp/patches/original/0221-aa1d0d27c371.patch`（1404 字节，29 行）
- Current patch: `.temp/patches/current/0221-aa1d0d27c371.patch`（30 行）
- Current patch SHA-256: `c2bdf2f3e472f98dcc538033ef45df67b731f48a44f73c37d4f20b5345044959`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`（v1.17.20 release）
- Built-on state: `0220-0436a82b95aa`（patches 1-220 applied）
- Record path: `.temp/patches/records/0221-aa1d0d27c371.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 1 file：
  1. `packages/opencode/src/tool/shell.ts` — 1 hunk：在 PowerShell 命令包装函数中，将 `$VerbosePreference` 和 `$DebugPreference` 从 `'Continue'` 改为 `'SilentlyContinue'`，并添加中文注释说明原因。
- 每个 hunk 与行为意图：
  - VerbosePreference/DebugPreference 降级为 SilentlyContinue：PowerShell 模块自动加载时（如 `Import-Module` 隐式触发）会向 verbose 和 debug 流写入大量诊断噪声。这些流通过 `*>&1` 合并到 stdout 后会污染终端输出和 LLM 上下文。默认静默后，用户仍可在命令内显式 `$VerbosePreference='Continue'` 重新开启。

## 3. SMARK Intent
- 用户可见行为：PowerShell 命令执行时不再默认输出 verbose/debug 流噪声；用户可在命令内显式重新开启。
- 受保护不变量：InformationPreference 和 WarningPreference 仍为 Continue（有价值的运行时信息不受影响）；ProgressPreference 仍为 SilentlyContinue。
- 正常路径：psUtf8 生成 PowerShell 包装脚本 → VerbosePreference/DebugPreference 默认 SilentlyContinue → 模块自动加载噪声被抑制 → 用户命令输出保持干净。
- 边界路径：用户需要 verbose/debug 输出时，在命令内显式设置 `$VerbosePreference='Continue'` 即可覆盖默认值。

## 4. Upstream Reconstruction
- 同路径目标实现：`packages/opencode/src/tool/shell.ts` 在 tip@220 存在于相同路径。
- 关键差异（adaptation 触发点）：
  1. 原始 patch 修改的是 `psEncoded` 函数（原始代码中 preferences 在 psEncoded 内联）；目标代码中 preferences 在 `psUtf8` 函数内（psEncoded 调用 psUtf8）。适配：修改 psUtf8 中的对应行。
  2. 原始 patch 的 preferences 行没有注释；目标代码有 `#129` 注释 `信息/警告/调试流继续输出` 覆盖 InformationPreference/WarningPreference/VerbosePreference/DebugPreference。适配：更新 #129 注释为 `信息/警告流继续输出`（移除"调试"因为 debug 已静默），并添加 #221 注释解释 verbose/debug 静默原因。
  3. 原始 patch 的注释是单行无标签中文；适配为 3 行 `#221`-tagged 中文注释，满足 C gate。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@220) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| VerbosePreference → SilentlyContinue | `'Continue'` in psUtf8 line 389 | psUtf8 line 392 | 改值 + #221 注释 | current patch diff |
| DebugPreference → SilentlyContinue | `'Continue'` in psUtf8 line 390 | psUtf8 line 393 | 改值 + #221 注释 | current patch diff |
| 静默原因注释 | `#129：信息/警告/调试流继续输出` | psUtf8 line 386 | 更新 #129 注释移除"调试" | current patch diff |

## 6. Current Patch Edit
- Current patch SHA-256: `c2bdf2f3e472f98dcc538033ef45df67b731f48a44f73c37d4f20b5345044959`
- 各编辑（1 file）：
  1. `packages/opencode/src/tool/shell.ts`：1 个 edit block——更新 #129 注释（移除"调试"）；添加 3 行 #221 中文注释解释 verbose/debug 静默原因；将 VerbosePreference 和 DebugPreference 从 'Continue' 改为 'SilentlyContinue'。
- Adaptation 决策：
  - 函数名适配：原始 patch 修改 psEncoded 函数体——目标代码中 preferences 在 psUtf8 函数内（psEncoded 仅调用 psUtf8），因此适配为修改 psUtf8。
  - #129 注释更新：原始 patch 无注释冲突——目标有 #129 注释覆盖 4 个 preferences，其中"调试"不再适用，因此更新为"信息/警告流继续输出"保持注释准确性。
  - 注释扩展：原始 patch 单行注释——适配为 3 行 #221-tagged 中文注释，详细说明噪声来源（模块自动加载）、影响（*>&1 合并污染）、恢复方式（显式开启），满足 C gate。
- Source behavior preserved：VerbosePreference 和 DebugPreference 默认 SilentlyContinue；用户可在命令内显式覆盖。
- Upstream behavior preserved：#129 的 ProgressPreference/InformationPreference/WarningPreference 设置不变；*>&1 流合并不变；psUtf8/psEncoded 调用链不变；所有其他 #129/#130 注释不变。

## 7. Verification
- `git apply --check --index` against state 0220 working tree：PASS（exit 0）。
- Materialize：`bun .temp/patches/src/apply-cumulative.ts --materialize 221` → passed 221/221。
- Typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 221` → 全部 30 个 workspace passed（含 packages/opencode）。
- Report: `reports/typecheck-0221-2026-07-27T08-23-11-395Z.md`
- Effective changed lines `E`（non-blank added lines）：E=6。
- Qualifying distributed Chinese comments `C`（`#221`-tagged 且含 CJK 的 added lines）：C=3。
- Required minimum `max(1, ceil(E * 0.15))`：max(1, ceil(6 * 0.15)) = max(1, ceil(0.9)) = max(1, 1) = 1。
- Gate：3 ≥ 1 → PASS（margin 2）。
- 注释分布：shell.ts 3 条 #221 注释（verbose/debug 静默原因 3 行），1 条 #129 注释更新（移除"调试"保持准确性）。

## 8. Contract Check
- 单 commit：是。
- 完整 source diff 已读：是（1 file，29 行）。
- 完整 upstream 实现已读：是（shell.ts psUtf8 函数相关区段）。
- 无 grep-only 证据：是。
- 无架构式省略：是。
- 无删除或延迟行为：是。
- 无未请求的备用成功路径或 fallback：是。
- 每条可达路径有 owner：是。
- 无残留行为 bug：是。
- 每 patch 中文注释 gate 通过：是（C=3 ≥ 1）。

## 9. Independent Audit
- Pending（累计 audit 待后续批次）。

## 10. Final Verdict

`PASS`（build）— patch built，`git apply --check` PASS on state 0220，materialize PASS 221/221，typecheck PASS 全部 30 workspace，E/C gate PASS（E=6，C=3，need=1，margin 2）。
