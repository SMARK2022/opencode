# Record: 0353-70bf89d5f574（351-400 波审计后扩写版）

## Identity
- index: 0353；sha12: 70bf89d5f574；parent: 15300d6c8656（=352）；subject: fix(harness): parent context range 聚合 + Verified→Executed Commands 改造
- original: .temp/patches/original/0353-70bf89d5f574.patch（7 文件）；current: .temp/patches/current/0353-70bf89d5f574.patch（33055B；sha256=ff2b723094bf6843808cba6c92a0de742f5065c1fbd2a95b90f4f3d813096868）

## Source intent（逐 hunk 行为重建）
1. docs/p1-fix-aggregation-plan.md：新建 308 行方案文档（verbatim 保留）。
2. shell.ts h1：注释修正——isSimpleVerificationCommand 改为独立 regex 说明（不再误挂 verify 命令族）。
3. task.ts：mergeRanges 导入 + path/InstanceState imports；`buildParentInspectedFilesSummary` 导出化并加 worktree 参数；inspected files 摘要从纯字符串列表改为 range 对象化（每文件 {path, ranges}）+ RANGE_LIMIT=8 截断 + 相对路径 display + 管道转义；runTask 调用点传 instance.worktree。
4. util/range.ts：新建（mergeRanges interval-merge 纯函数；目标落点 13 行——比 source 14 行少 1 行注释，该注释引用目标不存在的 compaction.ts renderInspectedFiles，适配删除）。
5. task.inspected-files.test.ts：新建 237 行、13 个行为测试（MessageV2→SessionV1 类型适配）——父上下文聚合的聚合/截断/转义/相对路径全行为锁定。
6. compaction.ts 6 hunks 与 compaction.test.ts 5 hunks：**无载体跳过**（见下）。

## 无载体跳过论证（0298 决议复述 + grep 证据）
- source 的 Verified→Executed Commands 改造依赖 evidence 命令渲染体系：displayPath/renderVerifiedCommands/renderEvidenceHandoff/isSimpleVerificationCommand/EVIDENCE_COMMAND_LIMIT 及 mergeRanges 的首个引入。该体系源自 source 仓库的合并侧分支，**目标 v1.17.20 从未移植**——0298 record（97 行，含双向审计 seal）已裁定跳过同类 hunk。
- 波审计独立复核（ses_f689fe51… Rejected speculation 节）：对目标 compaction.ts（744 行）及全 src grep `renderVerifiedCommands|renderEvidenceHandoff|isSimpleVerificationCommand|EVIDENCE_COMMAND_LIMIT` 零命中；可重放集（1-345）内无任何 `+function` 引入这些基础符号。无 owner 可承载，跳过合规；docs verbatim 段已完整保留该体系的语义记录，供未来 V2 化时参考。

## 三方比较
- 上游 v1.17.20：无 parent context 聚合能力（SMARK 自研 harness 增强），目标 owner 唯一。
- 目标：task.ts 为 V1 SessionV1 harness 路径（仍为现役子代理执行路径，core/V2 无重复 TaskTool——386-390 批审计同口径确认）。
- MessageV2→SessionV1：目标测试 fixture 类型体系适配，断言值不变。

## V1/V2 owner 判定
- parent-context 聚合 owner 在 V1 工具层 task.ts（现役）；compaction evidence 体系在目标无 owner（能力域从未迁移，非"V2 已承载"），跳过非弃置。

## 逐 hunk 映射（物化落点，states/0360+ 实测）
- shell.ts:1196 注释；task.ts:28,68,293-296（导出/worktree/range 对象化/调用点）；util/range.ts 新建；task.inspected-files.test.ts 新建 13 测试；docs 308 行 verbatim。

## E/C 与注释位置
- E≈220-250（排除 docs verbatim 与既有行；record 旧值 586 系计入 docs 行的宽口径，双向审计确认剔除 docs 后仍过门）；C≈38-49（mergeRanges 边界注释、RANGE_LIMIT 截断理由、相对路径与管道转义安全注释、测试意图注释，分散于 task.ts/util/range.ts/测试）。门禁通过。

## 物化与验证
- materialize 353 passed（波内链 360-400 复用延续）；typecheck-0353 全 workspace passed。
- 行为测试：task.inspected-files.test.ts 13 测试 + task.test.ts 全套 = 36 pass / 0 fail（R13 会话实跑；早期 report materialize-0355-…14-38-42 记录 corrupt at :443 为中间态，被 15:40 报告 355/355 取代）。

## 失败修订链
- corrupt at :443（new-file hunk 行数 236→237 重算 + diffSection 尾空行剥离 + testSection import 替换补 + 前缀）→ 修复后全绿。

## 最终行为
- 子代理父上下文获得 range 对象化、聚合截断（≤8）、转义安全的 inspected files 摘要；13 测试锁定；evidence 体系按 0298 决议继续缺席（无载体）。

## 审计残留（N 级）
- 旧 record "15 个测试/12 行 range.ts" 计数失准 → 本版已更正为 13/13；测试输出工件未存档（双向审计 N-04），后续行为测试统一存 D:/Temp/opencode/ 工件目录。
