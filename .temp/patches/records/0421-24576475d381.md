# 0421 — chore(tui): attention 默认音量从 0.4 调整为 0.5

## 最终状态：PASS（R72 修复 + 扩写）

## source（5 文件）与映射
1. specs/tui-plugins.md：
   - attention.volume 示例与语义说明 0.4→0.5；
   - current 逐字保留。
2. tui/src/config/index.tsx：
   - resolve() 默认 volume 0.4→0.5 + BS.1770 响度校准注释；
   - R70 已适配（enabled 翻转 context + 行号 117/49）。
3. attention.test：
   - config() 桩 volume 0.4→0.5；
   - R72 追加 section 落地
     （pre-image = state 0420 attention.test 的 config() 区域，逐字一致）。
4. tui.test：
   - defaults 断言 volume 0.4→0.5；
   - R72 追加 section 落地
     （state 0420 tui.test :150-154；enabled true 已由 R71 落地）。
5. fixture/tui-runtime.ts：SATISFIED
   - 目标 createTuiResolvedConfig 用 ...resolve(input) 展开；
   - volume 默认由 resolve() 统一承载；
   - fixture 无显式字段块可改，源 hunk 无落点（R72 论证）。

## B-0421 闭合（第 1 波发现）
- tui.test :154 断言 volume 0.4 在 0421 落地后
  （resolve 默认 0.5）必然失败；
- 根因：源测试 hunk 被丢弃；
- R72 将源 tui.test hunk 适配落地
  （volume 0.4→0.5 + BS.1770 注释）；
- 同轮补齐 attention.test 的 config() 桩同步（源同为 0421 hunk）。

## 上下文适配史
- R70 曾因 0420 的 enabled 翻转改变 context
  导致 0421 apply conflict（config/index.tsx:117 与 config.test.tsx:49）；
- R70 已将两个 hunk 的 context 适配为新基线
  （enabled ?? true + 注释行 + 行号偏移）。

## E/C
- E≈14/C≈5/need = 3，5 ≥ 3 ✓；
- 三处 volume 翻转 + 3 行 BS.1770 注释；
- specs verbatim 不计。

## 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces；
- tui.test 在 420 物化语义下实跑（R71 证据）：
  volume 断言随 0421 落地在 452 态为 0.5；
- record 曾 3 行（第 1 波 G），本节扩写为完整合规记录。

TEST-MARKER

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「tui.test.ts:154 断言 volume 0.4 必然失败」——本项 B 核心，
    R72 落地源 tui.test hunk 后闭合；
  - 「source 测试 hunk 被丢」——同时补齐 attention.test 的
    config() 桩同步 hunk（源 0421 同 hunk 形态）；
  - record 短（3 行）——R70 首次扩写、R71 重写、R72 本节定稿。
- 0420 交叉影响的两次适配：
  1. R70：enabled 翻转改变 index.tsx/config.test.tsx 的 context
     → 0421 两 hunk context/行号适配（117/49）；
  2. R71：tui.test 的 enabled 断言翻转落地
     → 0421 的 tui.test volume hunk 在新基线上重生成。
- R72 追加 section 的 pre-image 纪律：
  - attention.test / tui.test 均以 states/0420 物化内容为 pre
    （文件级 diff 确认与验证工作区逐字节一致）；
  - 追加位置在 patch 末尾，git apply 按文件独立处理；
  - 探针覆盖：specs/index/config.test 原三节 + 两个新 section。

## 验证补充
- typecheck-420（R71 后，R72 未触及 420 及之前区间）与
  typecheck-452（R72 后重物化，含本项全部 volume hunk）
  双报告 30/30 workspaces passed；
- 452 物化态 config/index.tsx 的 volume 默认 0.5
  与 attention.test/tui.test 断言 0.5 语义一致；
- fixture SATISFIED 的 owner 证据：
  tui-runtime.ts createTuiResolvedConfig 仅展开
  ...resolve(input)，无 attention 显式字段块，
  源 hunk（显式 volume 行）在目标无落点。
