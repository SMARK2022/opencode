# Record 0421-24576475d381

## Identity
- SHA: 24576475d381
- Parent: 478d20b2ba07 (0420)
- Subject: chore(tui): attention 默认音量从 0.4 调整为 0.5
- Status: PASS（materialize 421 + typecheck 421 全部通过）

## Source intent
source 0421（5 文件，5 hunks）：经 BS.1770 响度分析，0.4 时 stereo 综合响度约 -29.7 LUFS 偏轻；0.5 时约 -27.8 LUFS / -13.9 dBTP，与 macOS 系统提示音（Pop -29、Ping -28.8）同区间。将 attention 默认 volume 从 0.4 调整为 0.5：
1. `packages/opencode/specs/tui-plugins.md`：示例 `volume: 0.4 → 0.5`
2. `packages/opencode/src/cli/cmd/tui/config/tui.ts`（loadState）：`?? 0.4 → ?? 0.5`（含 BS.1770 注释块）
3. `test/cli/cmd/tui/attention.test.ts` config() helper：`volume: 0.4 → 0.5`
4. `test/config/tui.test.ts` 断言：`volume: 0.4 → 0.5`
5. `test/fixture/tui-runtime.ts`：`volume: 0.4 → 0.5`

## Target analysis 与适配（关键决策）
- 路径映射：`src/cli/cmd/tui/config/tui.ts` → `packages/tui/src/config/index.tsx` 的 `resolve()`（target 唯一 attention 默认值注入点；loadState 语义由 resolve 承载）。
- **fixture/tui-runtime.ts 在 target 无内联 attention**（结构差异：target 版直接消费 `@opencode-ai/tui/config` 的 `resolve()`，无内联 volume 字段）→ source 该 hunk 的语义（fixture 默认 volume 0.5）由 resolve() 的默认值统一承载（fixture 经 resolve 输出 0.5）→ target 无独立落点，记录说明。
- target 的 `packages/opencode/test/config/tui.test.ts` 断言仍为 `enabled: false`（该文件未受 0420 影响，target 的 loadState 链路默认 enabled 处理与 packages/tui resolve 不同步）→ 0421 只改 volume 行（enabled 语义不属于本 commit）。
- 适配落点（5 处）：specs 0.5、config/index.tsx resolve 0.5+BS.1770 注释、packages/tui/test/config.test.tsx 0.5、packages/opencode/test/config/tui.test.ts 0.5、attention.test.ts config() 0.5。

## E/C
- script：E 约 7（resolve 4 行含注释 3+代码 1、5 处 0.5 值行）— C≥2（BS.1770 注释块 3 行）→ PASS（C=3, need=1）。
- 注释位置：config/index.tsx resolve() 的 BS.1770 响度分析注释块。

## Verification
- materialize 421：passed 421/421（从 0405 重建全链 0406-0421，或复用有效前缀）。
- typecheck 421：30/30 workspaces passed（install 指纹 1165df52… bun 1.3.14 frozen reuse）。
- 行为验证：resolve() 默认 volume 0.5 + attention.test.ts config() 0.5；specs 文档同步。

## 注意
- 0422-0425 尚未实施（本批次续项）。
## 修订记录（批次内）
- **enabled 同步**：packages/opencode/test/config/tui.test.ts 的 `enabled: false` 断言同步为 `true`（0420 的 resolve 默认启用语义在 opencode 包测试中的遗漏项；0421 顺带对齐，否则该测试在 0421 后失败）。
- **fixhunkcounts 事故**：0421 current 曾被 hunk 行数修正脚本破坏（body 丢失）→ 从 fresh 0420 state 重新应用 5 处编辑（specs/config.index/config.test/tui.test/attention.test）→ 重新生成 0421 current（5 hunks）→ materialize 425 --rebuild（passed 425/425）。
- 最终 E/C：E=5、C=3（BS.1770 注释块）、need=1、PASS。
- 复跑：config/tui.test.ts attention defaults（1/1）、config.test.tsx（9/9）、attention（21/21）、malformed-sgr-mouse（7/7）、notifications（9/9）、session-preview（12/12）全部通过。

## 失败修订记录（批次内）
- fixhunkcounts 脚本事故破坏 0421 current（hunk body 丢失、75 行残片）→ 恢复流程：
  1. materialize 420 重建干净的 0421 pre（state 0420）；
  2. 在 0420 state worktree 重新应用 5 处编辑（specs/config/index/config.test/tui.test/attention.test）；
  3. git diff 生成 current/0421（5 hunks，3265 字节）；
  4. materialize 425 --rebuild：passed 425/425；typecheck 425：30/30。
- enabled 同步（0420 遗漏项）：注意该同步是测试断言与既有 resolve 实现的对齐，不是行为变更。

## 行为测试证据（rebuild 后复跑）
- config/tui.test.ts "resolves attention config defaults and overrides"：1/1 passed（enabled true + volume 0.5）。
- packages/tui/test/config.test.tsx：9/9 passed。
- attention.test.ts（packages/opencode）：21/21 passed。
- malformed-sgr-mouse.test.tsx：7/7、notifications.test.ts：9/9、session-preview.test.ts：12/12（见各 record）。

## E/C（最终）
- E=5、C=3（config/index.tsx resolve 的 BS.1770 注释块：默认音量 0.5 的响度依据与 0.4 偏轻结论）、need=1 → PASS。
- 注释位置：packages/tui/src/config/index.tsx resolve() attention 默认区。

## 审计结论
- 双审计（正向 0421→0425；反向 0425→0421）轮次：本 record 描述 0421 的 volume 默认语义（0.4→0.5）、specs/断言/实现三处同步、fixture 结构差异说明（target fixture 无内联 volume，经 resolve 间接承载）、enabled 同步与恢复流程。
