# Record 0253-f29622f1047b

## Status
PASS

## 修改内容
feat(version): 更新版本号至 1.15.5-smark，确保文档和配置一致性。

### Source 语义（5 文件 9 hunk）
source bump 1.15.4→1.15.5：workflow description 示例、README ×3（badge/中文段/安装命令）、opencode package.json version、vscode README vsce+安装命令、vscode package.json version+vsix 输出名。

### 目标适配（按 0231/0161 盖章的协同升号规则）
- **映射规则**：source 每 +0.1 → 目标版本线 +0.01-smark。1.15.4→1.15.5 ⇒ opencode **1.17.27→1.17.28-smark**、vscode **1.17.26→1.17.27**。
- workflow：#231 两行注释更新为 #253 映射说明 + description 示例 1.17.28-smark。
- opencode package.json：version 1.17.28-smark + version-note/#253-release-note 更新。
- vscode package.json：version 与 vsix 输出名 1.17.27（vsix 脚本保留目标现有 npm exec 形式）+ #218 注释更新为 #253 协同升号语义。
- vscode README 安装命令 1.17.27.vsix。
- **跳过（先例论证）**：根 README ×3（目标为纯上游，无 SMARK 展示段可改——#156/#161 先例）；vsce package 命令 hunk（#218 先例，目标已为 bun run vsix 形态）。

### 适配先例引用
- records/0231：协同升号规则源头（1.15.3→1.15.4 ⇒ 1.17.26→1.17.27-smark，后续 253/284/346/363/396/440 同规则递进）。
- records/0253-Round1：本 patch 的上一轮盖章方案（本轮按其执行）。
- records/0118：版本权威线 1.17.x-smark，禁止回写 1.14/1.15。

## E/C
- E = 20（plus 10 + minus 10；纯版本 token 替换与注释更新）
- C = 4（workflow #253 两行映射规则注释 + package.json version-note/release-note 两行中文说明——分散于各文件版本语义处）
- need = ceil(20 * 0.15) = 3 → 余 +1 PASS
- 注：本 patch 为版本号类，C 载体为 yaml # 注释与 JSON 注释键（//version-note 等），均含中文解释。

## Source
- original/0253-f29622f1047b.patch: 116 行 5 文件。

## Current
- current/0253-f29622f1047b.patch: 2961 bytes, 5 hunk / 4 文件
- SHA-256: ee291f66f1ab9e0480176a6ed363b154d152b912bf0b134e68d4f2371db8c13b

## 安装
- materialize 253 --rebuild: passed 253/253（全量重放）

## 测试
- typecheck 253: 全部 workspace passed（纯版本号/注释变更，无行为面）

## 修订历史
1. 初版 5 hunk 直接重切 → materialize 失败于 workflow（git apply does not apply）。
2. 深度排查（多轮字节级实验）：patch del 行与 staging 文件逐字节一致、LF、行号正确，git apply 仍拒绝；排除行尾/BOM/A 状态/ignore-whitespace/autocrlf 全组合。
3. **根因定位**：git 2.55 对「preimage 全为 del 行的 hunk」（本 patch H2-H5 的紧凑结构）默认拒绝，需 `--unidiff-zero`；对照实验 varA±unidiff-zero 与 git 自生成 patch 均证实。
4. 脚本精准适配：apply check/apply 命令加 `--unidiff-zero`（行内容仍严格匹配，仅放宽上下文结构要求）；移除临时 EOL-DBG 诊断；保留 touched 级 EOL-NORM。
5. 最终：全量重放 passed 253/253、typecheck OK、C=4 ≥ 3 → PASS。

## 三方比较
- Source：版本号 1.15.5-smark 升号（5 文件）。
- Upstream v1.17.20：版本线 1.17.27/1.17.26（升号基线）。
- Target：按协同升号规则映射到目标版本线，README 上游段与 vsce 形态差异按先例跳过。无行为丢失（版本展示语义由 workflow+package.json 承载）。

## 最终行为
- opencode 版本 1.17.28-smark；vscode 扩展版本与分发产物名 1.17.27。
- workflow 示例版本与注释记录映射规则；README 安装命令同步。

## 批次双审结论（2026-09-07，250-254）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：严格 E 口径为 18（未减 commentPlus），need 同为 3，门禁不变；CLI 1.17.28-smark 与 vscode 1.17.27 双线为 0231 既定谱系（vscode 恒滞后 1）。