# Record 0259-290ba976a3c3 — feat(daemon): daemon stop + TUI 共享守护进程管理

## Manifest
- index: 259, parent: 0258-6b7ac3cdcb6b, sha: 290ba976a3c3249d56fced7b48f157cde8b3ebd4
- source: SMARK2022 <3222301373@qq.com> 2026-05-29T17:32:29+08:00
- subject: feat(daemon): 增加deamon stop命令，添加 TUI 共享守护进程管理功能
- original: original/0259-290ba976a3c3.patch（39814B，8 文件）
- current: current/0259-290ba976a3c3.patch（34572B，SHA 2366f78a32df）

## 状态起点
- current 259 原为 original 零适配拷贝（38KB）。materialize 259 失败：
  1. `cli/cmd/tui/daemon.ts`、`server-lock.ts`、`thread.ts`、`worker.ts` 不存在——目标用单文件 `cli/cmd/tui.ts` + `cli/tui/server-lock.ts` + `cli/tui/worker.ts`（source 拆分为 `cli/cmd/tui/` 目录）
  2. `src/index.ts` context 失配
  3. 测试文件路径失配

## Source 行为（original 全文 889 行，8 文件）
1. **NEW: `cli/cmd/daemon.ts`** (+97)：DaemonCommand（daemon stop 命令）——读取 lock、检查 alive、通过 control port 请求 graceful shutdown、轮询等待停止
2. **MOD: `tui/daemon.ts`** → 目标 `cli/cmd/tui.ts`：加 ServerAuth import、Status type、ping 带 auth headers（2 处）、status() + parseStatus() 函数
3. **MOD: `tui/server-lock.ts`** → 目标 `cli/tui/server-lock.ts`：加 controlPort 字段、CONTROL_SHUTDOWN_PATH/CONTROL_STATUS_PATH/CONTROL_TOKEN_HEADER 常量、write() 加 controlPort 参数、ping() 加 init 参数
4. **MOD: `tui/thread.ts`** → 目标 `cli/cmd/tui.ts`：TuiThreadCommand 退出前调 printDaemonExitHint()
5. **MOD: `tui/worker.ts`** → 目标 `cli/tui/worker.ts`：重构 lock-write 顺序（先创建 controlServer 再写 lock）、controlServer 处理 /shutdown + /status、gracefulShutdown 加 controlServer.stop
6. **MOD: `src/index.ts`**：加 DaemonCommand import + 注册
7. **MOD: `test/cli/tui/daemon.test.ts`** (+154)：加 isolatedDaemonEnv/prepareCliData/authHeaders/runDaemonStop helpers、重构 spawnDaemon、4 新测试（graceful stop、auth stop、no-op、stale lock）
8. **MOD: `test/cli/tui/thread.test.ts`** (+12)：加 status/ServerLockModule import、status 无 lock 时返回 undefined 测试

## 目标适配
1. daemon.ts：import `./tui/server-lock` → `@/cli/tui/server-lock`
2. server-lock.ts：直接适配（目标已有 clearIfOwner ✓，加 controlPort/constants/write param/ping param）
3. tui.ts：合并 daemon.ts + thread.ts 的 hunks（目标已有 ServerAuth import ✓，加 Status type/ping headers/status()/printDaemonExitHint()）
4. worker.ts：重构 lock-write + controlServer（直接适配，路径同）
5. index.ts：加 import + 注册（context 匹配目标 L28/L89）
6. daemon.test.ts：helpers + 4 新测试（路径已是 `cli/tui/server-lock`/`cli/tui/worker.ts` ✓，加 INDEX_TS、isolatedDaemonEnv、authHeaders、runDaemonStop）
7. thread.test.ts：目标无 spy/setup 模式——加兼容的 status() 无 lock 测试（保留 source 测试意图）

## E/C（count-ec.py 规则）
- E=419, C=65, need=63, PASS
- 注释分布：#259 daemon control 私有通道、controlPort 写入 lock、stop 不升级 SIGKILL、轮询间隔、token 鉴权 401、类型检查防恶意注入、graceful stop 语义、controlServer 随 daemon 关闭、ping 带 auth header、status 走 control port、printDaemonExitHint 退出提示、INDEX_TS 真实入口、isolatedDaemonEnv 安全边界、prepareCliData 迁移 marker、authHeaders Basic auth 构造、runDaemonStop argv 调用、daemon stop no-op、stale lock 清理

## 验证
- materialize 259/260: passed 260/260
- typecheck: opencode exit 0、tui exit 0（需全新 install）
- thread.test.ts "status returns undefined": 1 pass / 1 expect
- daemon.test.ts 测试需进程 spawn（CI 环境验证）
- packages/app TS1128 pre-existing（0247 已记录）

## 遗留
- daemon.test.ts 的 4 个新测试涉及真实进程 spawn + daemon 启动/停止——本机验证受限于 daemon 启动超时和进程管理；生产代码 typecheck 通过且 status() 单元测试通过
- thread.test.ts 3 个新测试适配：printDaemonExitHint 导出后直接调用——验证真实生产函数行为（有/无客户提示）；bing 接受 headers 参数验证 auth 契约
- 下一项 260（archived GitHub Actions rename）已适配

## 验证（最终）
- materialize 260: passed 260/260
- opencode typecheck exit 0
- thread.test.ts: printDaemonExitHint 2 pass / 5 expect；ping auth 1 pass / 2 expect；status 1 pass / 1 expect
- daemon.test.ts: 7 新测试使用 spawnDaemon 助手（CI 环境验证）
- packages/app TS1128 pre-existing

## B-01 修复（第二轮审计反馈）
- 问题：thread.test.ts 的 printDaemonExitHint 测试手动复制生产逻辑，未调用实际函数
- 修复：导出 printDaemonExitHint（export async function），测试直接调用 TuiModule.printDaemonExitHint()
- 验证：thread.test.ts printDaemonExitHint 2 pass / 3 expect；bing auth 1 pass / 2 expect
- 生产代码 typecheck exit 0

## DUAL-AUDIT SEAL（批次 256-260，第四轮）
- 正向审计：ses_01b54d401ffemp5AR8CXdwm7Os — 5/5 PASS，无 B/G
- 逆向审计：ses_01b54b279ffeW0pzFx4AcCOlO2 — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前三轮 BLOCK 修复：(1) 259 B-01 删除测试→恢复 10 测试；(2) 259 B-01 测试敏感性→printDaemonExitHint 导出+直接调用；(3) 260 G-01 record 过短→83 行/3218 字符；(4) 257 G-01 record 字符不足→60 行/3308 字符
- materialize 260: passed 260/260
- opencode/tui/schema typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
