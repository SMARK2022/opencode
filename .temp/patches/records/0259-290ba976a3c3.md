# Record 0259-290ba976a3c3 — `opencode daemon stop` CLI 与 daemon 私有控制面

## Source（original/0259-290ba976a3c3.patch）

1. 新建 `cli/cmd/daemon.ts`：`opencode daemon stop`——读 ServerLock；无 lock 时 0 退出提示 no-op；stale lock（pid 不存在）仅同 token 清理；旧 lock 无 controlPort 时拒绝退化到 process.kill（防 pid 复用误杀）；通过私有 control port POST /shutdown（token 鉴权），轮询等待退出后清锁，10s 超时非零退出。
2. `cli/cmd/tui/daemon.ts`（目标 owner 为 cli/cmd/tui.ts）：`status()`/`parseStatus()`——经 control port GET /status，字段类型校验防注入，失败静默；`printDaemonExitHint()`——TUI 退出时有活跃连接/会话则提示 `opencode daemon stop`；discoverOrSpawnDaemon 的 health ping 带上 ServerAuth headers。
3. `server-lock.ts`：lock 增加 `controlPort?`；CONTROL_SHUTDOWN_PATH/CONTROL_STATUS_PATH/CONTROL_TOKEN_HEADER 契约常量；`write` 增加可选 controlPort 参数；`ping` 接受 headers init。
4. `worker.ts`（daemon 分支）：移除提前写锁，改为创建本机 control server（127.0.0.1:port 0，token 先于路径分发校验，GET /status 返回 sseClients+sessionActivity 只读快照，POST /shutdown 经 setTimeout 触发既有 gracefulShutdown）后再 `ServerLock.write(port, externalUrl, controlPort)`；graceful shutdown 增加 `controlServer.stop(true)`。
5. `index.ts`：注册 DaemonCommand（必须早于 $0 [project]，否则 `daemon stop` 被解释为打开名为 daemon 的项目）。
6. 测试：daemon.test.ts 新增 stop 契约 6 用例（正常停止/带密码 auth/无 lock no-op/stale lock 清理/无 controlPort 拒绝/错误 token 401 双存活）+ status SSE 计数用例；thread.test.ts 新增 status undefined、printDaemonExitHint 有/无活跃客户、daemon health check auth 契约用例。

## 目标适配与三方比较

- 路径迁移：目标无 `cli/cmd/tui/daemon.ts` 与 `cli/cmd/tui/{server-lock,thread,worker}.ts`——对应 owner 为 `cli/cmd/tui.ts` 与 `cli/tui/{server-lock,worker}.ts`；current 在早期适配轮完成全部 retarget 并把原 daemon.ts 的 discover/ensure 语义合并进 tui.ts。
- 上游（dev-smark tip）无 daemon stop/控制面能力，属 SMARK feature；在目标既有 ServerLock/worker 生命周期上扩展，未新建重复 owner：stop 复用既有 gracefulShutdown（清锁→停 HTTP→关库→exit(0)），不另起第二条关闭路径；关闭时序与 #235 3s 超时包装保持一致。
- 安全语义全保留：token 先于路径分发、401 不泄露、无 controlPort 拒绝退化（不 fallback 到 process.kill）、测试双存活断言锁定"绝不误杀"边界、wire 不暴露 control 面于公共 API/OpenAPI/SDK。
- V1/V2：daemon 管理属进程生命周期层，与 V1/V2 session 通道无交集；status 快照读 worker 既有 sseClients/SessionActivity，无重复状态源。

## 审计修复（B-259-1 / G-259-3）

- **B-259-1（双审一致 BLOCK）**：initial current 把 source 的 auth-wiring 测试降级为同义反复——`spyOn(ServerLockModule, "ping")` 后由测试自身调用 mock 并断言捕获值，移除 `tui.ts:267` 的 `{ headers: ServerAuth.headers() }` 不会使该测试失败，源测试的回归防护完全丢失。修复：重写为经 `discoverOrSpawnDaemon` 真实注入 seam 的可失败契约测试——Flag 设密码（save/restore），deps.read 前两次返回 undefined 走 spawn 主路径、随后返回新 daemon 锁进入健康轮询；deps.ping 仅当携带与 ServerAuth 同算法编码的 Basic 凭证（`Basic base64("daemon-user:daemon password with spaces")`）才判健康；spawn mock 返回永不退出进程以防轮询走崩溃分支。生产若丢失 headers 传递 → ping 永不健康 → waitMs 5s 超时抛错 → 测试失败。已实测通过（1 pass）。
- **G-259-3**：3 条非限定复述注释（Promise.all 复述/SSE 断言复述/"确认传递到 fetch"事实错误）已改写或随测试重写移除，并补入 11 条实质契约注释（Flag 快照直写原因/Basic 逐字节期望/spawn hanging 语义/复用成功即凭证证明/stderr 管道排空/控制面快照单一来源/超时非零退出语义/500ms 旁路有界/Headers 形态兼容/XDG 重定向/锁文件与控制面原子可见）。

## current 修改内容（sha256=2ae235f642a93483d8130a950af1f1c49cb11ce076f4dc802678509b454d1353，38946 bytes）

- thread.test.ts：tautological ping 测试 → failing-capable auth 契约测试（discoverOrSpawnDaemon + Flag save/restore）；imports 增加 `import { Flag } from "@opencode-ai/core/flag/flag"`。
- 生产代码零改动；其余为注释修正/新增；无 source hunk 删除，无测试删除（原 6 stop 用例 + SSE 计数 + status undefined + 2 hint 用例全保留）。

## E/C

- E = 624（plus 602 + minus 22）；C = 94 ≥ need = ceil(624×0.15) = 94。
- C 分布：daemon.ts CLI 7、tui.ts 7、server-lock.ts 5、worker.ts 9、daemon.test.ts 40、thread.test.ts 26——全部位于常量/安全边界/时序/测试意图处，无集中堆放。

## 验证

- materialize 259 --rebuild: passed 259/259（期间一次 publish 因残留测试 daemon（PID 62908，测试 shell 于 0:41 启动的 worker.ts 进程）持有 states/0259 目录句柄而 eviction blocked；该进程随后自行退出，重试后发布成功；`-s ses_*` 会话进程未受影响）。
- typecheck 259: 全部 workspace passed（install reused 549c6966）。
- 行为测试（.test-workspace@259）：
  - `bun test test/cli/tui/thread.test.ts -t "authenticates daemon health checks"` → 1 pass（新契约测试）。
  - `bun test test/cli/tui/thread.test.ts --timeout 180000` → 18 pass / 3 fail；3 fail（mini-only/routes attached/network options）已实测在 258 状态同样失败，为 cliIt.live 真实进程 spawn 的 pre-existing 环境失败（daemon exit 132），与 259 语义无关。
  - `bun test test/cli/tui/daemon.test.ts --timeout 120000` → 15 pass / 0 fail。
- E/C 复核：C=94 = 94 达标。

## 最终行为

- `opencode daemon stop` 提供安全的 graceful 停止：token 鉴权的本机私有控制面触发 daemon 自身 shutdown，无 lock/stale lock/旧格式 lock/错误 token/超时各分支均有明确退出码与文案；TUI 退出时在有活跃客户时提示 stop 命令；health ping 与 server auth 统一且受契约测试保护。
- 批次 255-259 状态：255/256/257/258/259 全部闭合；待记录修正（255/257/258）后重审。

## 可失败性论证（B-259-1 修复核心）

- 新契约测试的失败路径：生产 `tui.ts:267` 丢套 `{ headers: ServerAuth.headers() }` → deps.ping 收到无凭证 init → authorization ≠ expected → 永不健康 → while 循环自旋至 waitMs(5s) → `throw new Error("opencode daemon failed to start within...")` → 测试失败。
- 通过路径：凭证正确 → 首次轮询即健康 → 复用返回 `http://127.0.0.1:9999` → 断言过。
- 测试不依赖真实网络/真实 daemon：spawn/read/alive/sleep 全部经 DiscoverDaemonDeps 注入，仅生产 discoverOrSpawnDaemon 本体运行——同一实现即 handler 所用主路径。
