# Patch 0164 — 970ee408ae8c

## Identity
- Index: 164
- SHA12: 970ee408ae8c
- Subject residual: event single-delivery + project scope gate
- Current path: `.temp/patches/current/0164-970ee408ae8c.patch`
- Current bytes: 3631
- Current SHA-256: `edc7f56d6d26b317e2b50ed74e99dc5ad11cfe2f075d1c50380a57af21af2e45`
- Original path: `.temp/patches/original/0164-970ee408ae8c.patch`
- Updated: 2026-07-25T06:08:54.108104Z
- Batch: 161–165
- Tip state after batch: `states/0165-2e65029d4e34`

## Source intent
Fix double local apply of message.part.delta caused by dual project/workspace handler fallthrough (missing return). Add regression locks for single delivery and streaming=true. Original also contains models-snapshot noise.

## Three-way comparison

### Tip baseline (before this residual)
packages/tui useEvent already single-handler (no dual branch). Missing: explicit GlobalEvent.project isolation for shared-daemon SSE. Streaming=true already on tip session Text/Reasoning.

### SMARK original
1-line return after project/global match + source-string tests + 4MB snapshot noise.

### Target current adaptation
Export pure shouldDeliverScopedEvent(directory/project, currentProject):
- directory==='global' → always deliver
- event.project set → deliver only if equals useProject().project() (store.project.id)
- else deliver once
Wire into useEvent.subscribe before handler.
Unit tests import production symbol (not vacuous local handler).
Drop models-snapshot and brittle source-string streaming tests (tip already streaming).

## Owners and changed files
- packages/tui/src/context/event.ts
- packages/tui/test/cli/tui/event-single-delivery.test.ts

### Diff --git paths in current
- `packages/tui/src/context/event.ts`
- `packages/tui/test/cli/tui/event-single-delivery.test.ts`

## Tests and verification commands
event-single-delivery: global always; project match/mismatch; unscoped deliver once.

Commands used in this migration wave:
- `bun .temp/patches/src/apply-cumulative.ts --materialize 164`
- Batch end: `bun .temp/patches/src/apply-cumulative.ts --typecheck 165`
- Latest green typecheck report: `typecheck-0165-2026-07-24T23-32-59-631Z.md` (30 packages passed; install reused `bfd9217687…`)
- Prior batch 156–160 dual FORWARD+REVERSE PASS sealed

## Independent E/C (current patch only)
- E (non-blank non-comment added lines): **33**
- C (Chinese explanatory comments near decisions): **10**
- Required max(1, ceil(E*0.15)): **5**
- Gate: **PASS**
- Sample C sites:
- // #164：全局 directory 事件始终投递（重连/跨实例广播）
- // #164/#165：显式 project 字段必须匹配当前项目，防止共享 daemon SSE 跨项目泄漏
- // #164：global 目录事件无条件放行
- // #165：有 project 时只匹配当前 project，不回退 workspace
- // #164/#165：单次投递前做 project/global 作用域判定
- // #164：单次投递 + 作用域判定；防止 part.delta 双写与跨项目泄漏
- // #164：函数可单测，不依赖完整 TUI mount
- // #164：global 目录事件始终投递
- // #165：project 不匹配则丢弃
- // #164：无 project 字段时放行一次

## Materialize provenance
- Cumulative apply through index 164 is required before batch tip@165.
- Live tip@165 must contain this residual after applying currents 161–165 in order.
- Original SHA-256 must continue to match `.source-proof.json` (immutable).

## Risks / residuals
Workspace cascade after project miss not reintroduced (tip never had it). Project field optional on GlobalEvent v2.

## Audit status
- **PASS** (FORWARD+REVERSE dual independent audit)
