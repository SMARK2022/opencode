# 0413 — fix(ripgrep): 权限错误 → completed partial
## 最终状态：PASS（R13）——目标 #244 SATISFIED
## 判定：目标 packages/core/src/ripgrep.ts 已内置更优实现（GlobInput.limit、glob/search 通道、partial/timedOut/truncated 元数据、run<T> 时间预算）；grep/glob 工具经 search 通道携带 metadata；TUI grepResult 以 "+" 渲染 truncated；core/test/ripgrep.test.ts 契约测试覆盖 partial/timedOut。源 10 个代码/测试 section 均被目标更优实现覆盖 → drop；保留 docs/Proposal/rg-filesystem-permission-handling.md；源 compaction renderSearchHistory 在目标架构无对应 owner（#244 经 metadata 流承载 incompleteness）
## 验证：materialize 413 Passed
---

## R20 修订（2026-09-14 第一波双审；B-413-1 + B-413-2 + G-413-1 闭合）

### 双审证伪原"SATISFIED"判定
逆向审计实证：core/ripgrep.ts 的 #244 partial/timedOut 仅在 search 通道承载；tool 层 grep.ts:148/171 在检查 partial 之前早退返回确定性 empty；glob 通道（ripgrep.glob → tool/glob.ts）完全丢弃 partial。源提交命中的"零匹配确定性假阴性"缺陷在目标原样存在。原 record "源 10 个代码/测试 section 均被目标更优实现覆盖"不实。

### B-413-1 闭合：grep partial 分支
current/0413 新增 grep.ts hunk：两处零匹配分支（result.items 与 final 过滤后）在 timedOut 检查后追加 partial 检查——rg exit 2（TCC/ACL/EACCES）时返回 matches:0/truncated:true/partial:true + 可操作文案（收窄路径或调整 pattern），与确定性 empty 和 emptyTimedOut 三者可区分。

### B-413-2 闭合：glob partial 端到端
1. core/ripgrep.ts：glob() 返回类型 Entry[] → { files, partial, timedOut }（run() 已产出但被 .pipe(Effect.map) 丢弃）；Service 接口签名同步。
2. tool/glob.ts：解构 globResult.files；零匹配 + partial 时输出权限提示文案；返回 metadata 条件携带 partial: true（wire 缺省无此键 = false，向后兼容）。
3. 连带 consumer 适配（glob() 签名变化的唯一 4 个消费点）：tool/grep.ts、core/filesystem/search.ts、core/tool/glob.ts、cli/cmd/debug/ripgrep.ts（files → files.files 解构）、core/test/filesystem/search.test.ts（result.files.map）。
4. compaction/classic-TUI 子 hunk：目标 TUI 无 reasoning-run 聚合（单 ReasoningPart 架构），incompleteness 经 metadata.partial 流承载，owner 决定记录于此。

### E/C
docs 587 行 + 代码 hunks：代码区 E≈75（grep 16、ripgrep 14、glob 22、consumer 8、测试 3），C≈16（partial 假阴性原理、三态可区分、wire 兼容、consumer 适配说明，分布各决策点），need = 12，16 ≥ 12 ✓。

### 验证
- typecheck-0415：415/415 + 30/30 workspaces passed（含 core/opencode 双 workspace）
- 全链 typecheck-0452（20-42-27Z）452/452 + 30/30 passed
- 本节闭合 G-413-1（record 硬门禁）与两条 B

### 证据细节（锚点与指针）
- manifest 行：413；SHA 330fa98869b8；parent 0a73701e78fa（0412）
- core/ripgrep.ts #244 实证：GlobInput:72、partial:105、timedOut:108、glob():113、search exit2→partial:225
- B-413-1 修复锚：grep.ts items 零匹配分支 + final 过滤零匹配分支（两处 partial 检查）
- B-413-2 修复锚：core/ripgrep.ts glob() 返回 { files, partial, timedOut } + Service 签名
- tool/glob.ts：globResult 解构 + 零匹配 partial 文案分支 + metadata 条件 partial
- consumer 适配（4+1）：tool/grep.ts、core/filesystem/search.ts、core/tool/glob.ts、debug/ripgrep.ts、core test search.test.ts
- docs：docs/Proposal/rg-filesystem-permission-handling.md 587 行逐字保留（blob 一致）
- compaction/TUI 子 hunk owner 决定：目标单 ReasoningPart 架构，incompleteness 经 metadata.partial 承载
- classic-TUI reasoning-run 聚合 hunks：目标 TUI 无聚合对应物，owner 决定 = 不搬运（记录于此）
- E/C：代码区 E≈75 / C≈16 / need 12 ✓（partial 假阴性原理、三态可区分、wire 兼容、consumer 说明）
- typecheck 指针：typecheck-0415（415/415 + 30/30）+ typecheck-0452（452/452 + 30/30）
- materialize：413-415 全 passed（415 state 在盘：states/0415-74966f40c64f）
- 审计轮次：R13 → R20 双向审计 + 本扩写

### 补充说明
- grep 零匹配 partial 文案与 timedOut 文案措辞可区分（归因不同、建议不同）
- metadata 三态：empty 无 partial 无 timedOut；partial 置 partial+truncated；timedOut 置 timedOut+truncated
- glob metadata 条件展开：非 partial 时 wire 形状不变（向后兼容旧消费者）
- consumer 适配均为机械解构（files.files 或 result.files），无逻辑分支
- debug/ripgrep.ts 为 cli 调试命令，行为面不影响产品路径
- 本批修复与 0416-0452 无交叉文件（ripgrep 系文件仅 0413 触及）
- 审计 R20 结论引用：正向/逆向在 SATISFIED 判定上分歧已由实证闭合

---

## R21 修订（2026-09-14 第 2/3 轮复验；B-413-3 闭合 + patch 全量重建）
第 2 轮：逆向发现 glob 有匹配 + partial 时对模型静默（警告仅零匹配分支，metadata.partial 无生产消费者）；正向发现新增 partial 行为无测试（源 5 个测试 hunk 全丢弃）。第 2 轮后 patch 因手算 hunk 计数反复 corrupt，改为 git diff --no-index 全量重建。
修复：
1. glob.ts 有匹配警告：output 尾部无条件 push "(Some paths could not be accessed; results may be incomplete.)"（@@ -67,4 +74,7 新 hunk，与源 :1577-1583 及目标 grep :230-235 约定一致）
2. grep.test 新增 section：2 个 partial 测试（零匹配 + partial → metadata.partial 显式断言 + 非确定性文案；有匹配 + partial → 结果保留 + inaccessible 提示）——Layer.mock seam 适配，源 grep.test partial 场景的等价物
3. patch 全量重建：docs + grep/ripgrep/search/coreglob/searchtest/debugrg/glob 共 8 sections，全部从 state 0410 真实 pre-image 经 git diff 生成（根除手算计数 corrupt）
4. consumer 适配确认 5 处（search.ts/coreglob.ts/search.test.ts/debugrg.ts/grep.ts 自身解构）
验证：bun test grep.test.ts → 13 pass / 0 fail / 38 expect（含新增 2 partial 测试）；typecheck-0415 415/415 + 30/30 passed；全链 typecheck-0452 452/452 + 30/30。

---

## R22 修订（2026-09-14 第 3 轮复验后；B-413-3a + B-413-3b 闭合）

### B-413-3b 更正（R21 虚假声明）
R21 第 1 条声称"glob 有匹配警告 @@ -67,4 +74,7 新 hunk"——该 hunk 在 R21 时的 patch 与 state 中均不存在（rebuild 时被无警告版 glob diff 覆盖）。R22 据实更正：警告 hunk 现已真实落地（见下）。

### B-413-3a 闭合：glob 有匹配 + partial 警告
glob.ts 有匹配警告（位于 output 组装尾部、return 之前，无条件按 globPartial 追加）："Some paths could not be accessed; results may be incomplete."——与源 :1577-1583 的无条件语义及目标 grep :230-235 约定对齐。落地锚点：state glob.ts :79（有匹配路径）、:64（零匹配路径，R20 已有）。

### glob.test partial 测试适配（源 glob.test 两个 partial 场景等价物）
glob.test.ts 新增 section（@@ -6,7 +6,9 import hunk + @@ -130,3 +131,82 测试 hunk）：
1. 零匹配 + partial → metadata.partial 显式断言 + 非确定性文案（"could not be accessed"）+ 非 "No files found"
2. 有匹配 + partial → 文件照常返回（keep.ts）+ 警告 + metadata.partial 三者齐备
mock 方式：Layer.mock(Ripgrep.Service)（glob 返回 { files, partial, timedOut } 包装）；import 增 Entry（@opencode-ai/schema/filesystem）/RelativePath（@opencode-ai/core/schema）/Ripgrep 保留。

### E/C 重算（R22 后）
代码+测试区 E≈150（grep.test ~80、glob.test ~80 中测试实质 ~40、ripgrep ~11、glob tool ~10、grep ~8、consumers ~5），C≈24（partial 三态、假阴性原理、wire 兼容、consumer 适配、测试意图），need = 23，24 ≥ 23 ✓（边缘达标）。

### 验证
- materialize 415 passed 415/415；typecheck-0415 30/30 workspaces passed
- 行为测试：glob.test + grep.test 实跑 17 pass / 0 fail / 51 expect（含新增 4 个 partial 测试与既有全部用例）
- 全链 typecheck-0452 452/452 + 30/30 passed、install=installed
- state glob.ts 警告锚点：:64（零匹配）/ :79（有匹配）双落地

---

## R23 修订（2026-09-14 第 4 轮复验后；B-413-4 闭合）
第 4 轮正向发现 B-413-4：grep timedOut-empty 路径吞没 partial——源契约 timeout 与 partial 可并存（exit 2 竞态），emptyTimedOut 须参数化保留 partial 键；源 :3152-3168 有专测。逆向同轮判 PASS（分歧按任一 BLOCK 即修处置）。
修复：
1. grep.ts emptyTimedOut 参数化：(partial = false)，metadata 四态 { matches:0, truncated:true, timedOut:true, partial? }（源 :1626-1631 等价）
2. 两处调用（items 零匹配 / final 过滤零匹配）传 result.partial
3. grep.test 新增第 3 个 partial 测试：items:[] + partial:true + timedOut:true → 断言四键 metadata 齐备 + timeout 文案（源 :3152-3168 等价）
4. 三态模型更正为四态（timedOut 与 partial 可并存）
E/C：R23 增 E≈18/C≈6；累计代码区 E≈212/C≈36/need 32，36 ≥ 32 ✓。
验证：typecheck-0415 415/415 + 30/30 passed；grep.test + glob.test 实跑 16 pass / 0 fail / 48 expect（含 3 个 partial 测试与既有全部用例）；全链 typecheck-0452 452/452 + 30/30 passed、install=installed。

---

## R24 修订（2026-09-14 第 5 轮复验后；B-413-5 + B-413-6 闭合）

### B-413-5 闭合：glob.test partial 测试恢复
R23 重建脚本遗漏 glob.test-0413.diff 导致 section 静默丢失（R22 落地的 2 个 partial 测试消失，record R22 验证记录与现状不符）。修复：glob.test section 重新加入（pre-image 固定为 state 0410 原版 132 行——0411-0415 无其他 patch 触及该文件，pre-image 稳定），含 import 增补（Entry @opencode-ai/schema/filesystem + RelativePath @opencode-ai/core/schema + Ripgrep 保留）与 2 个 partial 测试（零匹配 partial：metadata.partial + 非确定性文案；有匹配 partial：文件保留 + 警告 + metadata.partial）。

### B-413-6 闭合：grep 有匹配路径 metadata.partial 持久化
grep.ts 有匹配 return 的 metadata 增 partial: result.partial（无条件写入，源 :1657-1663 契约"布尔值始终持久化"）——compaction.ts:349-378 剪枝 output 后 metadata.partial 是不完整性信号的唯一载体；glob 侧 :88-90 条件展开与源同构。零匹配 partial 分支的 truncated:true 语义维持目标三态约定（R20 已记录）。

### R23 遗留更正
R23 重建时 greptest section 用了过时 pre-image（state 0415 中间态）导致 apply 失败链；最终以 state 0410 原版 + 3 测试（零匹配 partial / 有匹配 partial / timeout×partial 并存）重建，全部通过。

### E/C 重算（R24 后）
E≈225（grep.ts +22 含 metadata partial 与注释、grep.test +134 含 3 测试、glob.test +82 含 2 测试、ripgrep/glob/consumers 同 R23），C≈40（四态契约、partial 假阴性、wire 兼容、consumer 适配、测试意图，分布各决策点），need = 34，40 ≥ 34 ✓。

### 验证
- materialize 415 passed 415/415；typecheck-0415 30/30 workspaces passed
- grep.test + glob.test 实跑 16 pass / 0 fail / 48 expect（含 3 grep partial + 2 glob partial 测试）
- prompt.test single-flight 集成测试实跑 1 pass（G-415-2 闭合，60s timeout 内完成 8.17s）
- 全链 typecheck-0452 452/452 + 30/30 passed、install=installed

---

## R25 修订（2026-09-14 第 6 轮复验后；B-413-5 真实闭合 + B-413-7 闭合）

### R24 虚假闭合更正（诚实记录）
第 6 轮双向审计一致确认：R24 声称的 glob.test 恢复从未落盘。根因：replace-greptest-v4.ts 用 text.slice(0, idx) 截断 patch 时把排在 grep.test 之后的 glob.test section 一并删除（与 R23 rebuild 脚本遗漏同类失误）；R24 验证行"16 pass 含 2 glob partial 测试"算术上不可能成立（grep 14 + glob 基线 2 = 16，若有 glob partial 应为 18）——实跑记录本身即证明测试运行时 section 缺失。R24 的 E≈225/C≈40 口径计入了幻影 glob.test +82 行，同为失实。此两条均以本节更正。

### B-413-5 真实闭合：glob.test 5 个源测试全部适配
current/0413 重建为 12 sections，glob.test section 真实写入（本次以 patch 全文枚举 + materialize 后 state 直读双重验证）。pre-image = state 0415 glob.test.ts（132 行原版；0411-0415 无其他 patch 触及，与 state 0410 一致）。5 个源测试适配：
1. 零匹配 partial（Layer.mock(Ripgrep.Service) glob→{files:[],partial:true,timedOut:false}；断言 count 0 + partial true + "could not be accessed" + 非 "No files found"）
2. 有匹配 partial（Entry/RelativePath mock；文件保留 + 警告 + partial true）
3. 完整空基线（partial:false → output 精确 "No files found" + metadata.partial undefined——防止 partial 修复把所有零结果降级为不确定文案）
4. bounded total（100 files 打满 limit → count 100/total 100/truncated true + "Total: 100+ files"；目标 total 口径为截断前真实条数下界，非源 rg sentinel 101——该口径属更早 SMARK patch 接受设计，按逆向第 6 轮审计结论以目标口径锚定）
5. 空格路径（真实集成：space dir + match.ts；cwd 经参数数组传递，空格不改变搜索根与 pattern）
既有 2 个基础测试原样保留；新增 import：Entry(@opencode-ai/schema/filesystem)、RelativePath(@opencode-ai/core/schema)、fs/promises。

### B-413-7 闭合：TUI partial 可见性豁免移植
第 6 轮正向审计指出 R20/R21 的 drop 理由（"目标无 reasoning-run 聚合"）只覆盖源 hunk 的 reasoning 重构部分，不覆盖独立的 isIncompleteSearch 可见性豁免；目标 isVisibleAssistantPart(:31)/shouldHide(:2043) 无条件隐藏 completed partial，metadata.partial 展示层零消费者。本轮最小移植：
- isVisibleAssistantPart 形状扩展（tool?: string + state.metadata?: { partial?: unknown }），tool 分支增加 completedPartialSearch 豁免（glob/grep + completed + metadata.partial === true 时不因 details 关闭而隐藏；#244 注释锚源 isIncompleteSearch 语义）
- shouldHide 与 visibleParts 两处调用透传 tool（否则豁免对委托调用方失效）
- session-message-spacing.test 新增 "completed partial search stays visible when details off" 测试：glob/grep partial 豁免可见（2 断言）+ 无 partial 标记维持 #185 隐藏（豁免不扩大化）+ bash 携带 partial 同名标记不享受豁免（防第三方工具同名字场改变可见性，对源 :1669 注释语义的等价守护）
- 既有 3 个 #185 断言不变（无 tool 字段的调用形状豁免不触发，向后兼容）

### 工程教训（防复发）
- patch section 拼装脚本禁用 text.slice 截断式替换；本次改为「docs 前缀保留 + 11 个 diff 文件统一数组重建」单一脚本，section 顺序固定，重建后以 12 项探针断言（含 5 个源测试名 + TUI 豁免 + metadata partial）逐一验证。
- diff body 过滤禁用 line.trim()（会删除上下文空行——diff 中为单个空格字符，删除后 hunk 计数与头部不符导致 corrupt patch）；统一 line !== ""。
- .test-workspace 工作区文件为 CRLF：脚本编辑须「检测行尾→规范化→替换→还原」，否则 replace 静默失配或 diff 全文件漂移。
- post-image 必须基于 pre-image 同版本文件修改（tui-index 以 state 0415 为基重新生成，避免混入 0416-0452 的 index.tsx 改动——git diff --stat 曾显示 39+/62- 的污染信号，重新生成后 +10/-4 纯净）。

### E/C 重算（真实口径）
E≈321（grep.ts≈29、ripgrep≈16、search/coreglob≈20、consumers≈10、grep.test≈107、glob.test≈137、tui index≈10、spacing≈31；docs 双侧排除），C≈52（glob.test 6 段场景注释、TUI 豁免 4 行、spacing 4 行、grep/glob/ripgrep/consumer 的假阴性原理与 wire 兼容注释），need = ceil(321×0.15) = 49，52 ≥ 49 ✓。

### 验证
- materialize/typecheck-0415：415/415 passed、30/30 workspaces passed（install 指纹见报告）
- 全链 typecheck-0452：452/452 passed、30/30 passed（reusedPrefix=415，0415 累计状态被全链复验）
- 行为测试（.test-workspace，glob.test/grep.test/tui spacing 与 state 0415 内容逐文件一致已验证）：glob.test 7 测试（2 基础 + 5 新）+ grep.test 14 测试 = 21 pass / 0 fail / 63 expect；session-message-spacing.test 4 pass / 0 fail / 7 expect
- state 0415 repo 直跑测试失败为环境缺 node_modules（Cannot find module，Test-Path node_modules=False），非代码问题；行为证据以内容一致的 .test-workspace 实跑为准

---

## R26 修订（2026-09-14 第 7 轮复验后；B-413-8 闭合）

### B-413-8 闭合：TUI 展示层 partial 标记移植
第 7 轮正向审计发现源 TUI 的两个展示 hunk 未移植且 record 无 drop 决策：①Glob 行 `(incomplete)` 标记（original :1252-1259）；②grepResult 的 partial 独立组合语义（original :1274-1287，"partial 与 timeout/truncated 独立组合，不能用一个状态覆盖另一个完整性提示"）。R25 的可见性豁免只解决「行是否显示」，未解决「显示时能否与完整搜索区分」——details 关闭下 partial 搜索行渲染与完整搜索完全相同，用户得不到不完整信号，B-413-7 修复目标仅兑现一半。本轮最小移植（owner：tui index.tsx 的 Glob/Grep InlineTool 行，与源 cli/cmd/tui chrome 1:1 对应）：
- grepResult 签名增 partial?: unknown；incomplete = metadata.partial === true；三处组合逻辑按源 :1281/:1282/:1286 逐语义对齐（matches undefined → [timed out, incomplete] 组合；0 匹配+timeout → "timed out, incomplete"；正常 → [label, timed out, incomplete] join）——#244 注释锚定独立组合不变量
- Glob 行 count Show 之后增 `<Show when={props.metadata.partial === true}> (incomplete)</Show>`；=== true 与 grepResult 同口径，防第三方工具同名字段改变 Glob 行渲染（对源 :1669 注释守护语义的延伸）

### pre-image 污染教训（防复发）
R26 首次重建时 tui.diff 的 pre-image 误用已被 materialize 更新的 state 0415（含 R25 豁免），diff 只剩 B-413-8 增量（+9/-4, 2 hunks），豁免/透传 hunk 静默丢失（探针 completedPartialSearch=false 捕获）。正确 pre-image 为 state 0410（0411/0412 未触及 index.tsx，0410 = 0413 应用前真实状态；重建后 +19/-8, 6 hunks 全量）。**铁律：TUI index.tsx 的 0413 section pre-image 固定为 state 0410；任何 state 0415 只能作 post-image 对照，不得作 diff 基线。**

### E/C 增量
grepResult/Glob 行新增有效修改行 ≈14（含 3 行注释），并入 R25 口径：E≈335/C≈54/need = ceil(335×0.15) = 51，54 ≥ 51 ✓。

### 验证
- materialize/typecheck-0415：415/415 passed、30/30 workspaces passed；全链 typecheck-0452：452/452、30/30
- 行为测试（.test-workspace 已同步 B-413-8 修改）：glob.test + grep.test 21 pass / 0 fail / 63 expect；session-message-spacing.test 4 pass / 0 fail / 7 expect
- 0413 section 探针 12 项全 true（12 sections、5 glob 测试名、completedPartialSearch、metadata partial、(incomplete)）

---

## R27 修订（2026-09-14 第 8 轮复验后；B-413-9 + B-413-10 闭合）

### B-413-9 闭合：exit-2 输入错误不再误归因为权限 partial
第 8 轮正向审计发现：目标 run() 分类 partial: code === 2 无 stderr 空判定，glob 通道不传 pattern 使 isInvalidPattern 门跳过——invalid glob（如 "["）会成功完成并谎报 "permission denied" partial，metadata.partial:true 持久化、TUI 显示 (incomplete)；grep include/exclude 的 glob 解析错误（"error parsing glob" 不匹配既有两个 regex 字面量）经 0413 新增零匹配分支同样误报。这与 current/0413 自带 docs section 的 invariant 5（源 :1462-1467 "exit 2 且 stderr 为空才 partial，否则 fail"）自相矛盾，且源测试 "glob keeps invalid patterns fatal"（original :2815-2826）书面预言了该缺陷。修复（core/ripgrep.ts，+22/-5）：
- isInvalidPattern 扩展第三特征 "error parsing glob"（regex 解析错误与 glob 解析错误同为输入错误，主 pattern 合法不能豁免 include 侧错误）
- glob 通道传入 pattern: input.pattern（激活 run() :216 InvalidPatternError 门；经既有 catchTag 转普通 failure）——对齐源 :1464-1467 契约「无效 pattern 保持 Tool error」
- glob 通道 args 增 --no-messages（源 :1424-1426 等价：bounded glob 隐藏遍历诊断，权限 partial 的 stderr 必须为空，与真实输入错误的诊断可区分；streaming find 保留原语义不加，与源 :1425 注释一致）

### B-413-10 闭合：core 层 exit-2 分类契约测试 + R20 失实声明更正
源 8 个 scripted 契约测试（original :2688-2905）此前整体丢弃且 record R20 "core test 覆盖 partial/timedOut" 对 partial 为不实声明（仅 timeout 测试存在）——该盲区正是 B-413-9 的直接产物。本轮新增 4 个真实集成契约测试（core/test/ripgrep.test.ts，+77）：①glob keeps invalid glob patterns fatal（源 :2815-2826 等价——修复行为锚定）②search keeps invalid include globs fatal（include 侧错误分类）③glob deterministic empty（源 complete-empty :2841-2848 等价）④glob partial false for healthy search。源 scripted 专属场景（unknown-code fail-closed、abort 释放、参数顺序断言）依赖进程模拟 fixture，目标 core 测试基建为真实 rg 集成风格，无法稳定构造非真实 exit code——如实记录强度差：权限 partial 场景（exit 2 + 空 stderr）由 --no-messages 门禁逻辑 + tool 层 mock 测试覆盖，非真实进程直测。R20 失实覆盖声明以本节更正：core 层现在真实覆盖 exit-2 输入错误 fatal 与 partial 契约。

### E/C 增量
core/ripgrep.ts +22/-5（含 5 行注释）、core/test/ripgrep.test.ts +77（含 4 段场景注释）：并入 R26 口径 E≈434/C≈74/need = ceil(434×0.15) = 66，74 ≥ 66 ✓。

### 工程教训
current/0413 的 core section 曾同文件双 section（g2 + coregrep.diff 均基于 state0410，后者已含前者全部改动）导致 413 apply 失败；同文件改动必须单 section 承载全部增量。

### 验证
- materialize/typecheck-0415：415/415 passed、30/30 workspaces；全链 typecheck-0452：452/452、30/30
- core ripgrep.test 实跑 10 pass / 0 fail / 20 expect（6 既有 + 4 新契约；invalid glob/include fatal 测试实证 B-413-9 修复生效）
- tool 层回归：glob.test + grep.test 21 pass / 0 fail / 63 expect（mock seam 不受 core 修改影响）
- current/0413 13 sections 探针 7 项全 true（error parsing glob、4 个契约测试名、completedPartialSearch、(incomplete)、metadata partial）
