# Record 0413-330fa98869b8

## Identity
- SHA: 330fa98869b8
- Parent: 0a73701e78fa (0412)
- Subject: fix(ripgrep): Grep/Glob 文件系统权限错误改为 completed partial 而非整体失败
- Status: PASS（批次已闭合；详见 0415 批次结语与双审计结论）（目标适配重写中）

## Source intent
source 0413 共 11 文件（3174 行，docs 587 行）+ TUI reasoning 重构噪音（800 行无关）：
1. 新增 docs/Proposal/rg-filesystem-permission-handling.md（587 行）。
2. file/ripgrep.ts（source 布局）：GlobInput{limit}/GlobResult{items,partial,truncated} 新 contract；filesArgs(input, noMessages)——仅 bounded Glob 注入 --no-messages；新 glob()：sentinel（>limit）不等 exit 直接返回 truncated、code0 complete、code1 空完整兼容、code2+stderr.trim()=="" → partial、其余 → fatal；stderr 从 spawn 起 forkScoped 并发 drain；raceAbort。
3. tool/glob.ts：弃用 files() + take(101)，单次 glob()；totalFiles = files.length + Number(truncated)（101 口径）；0 结果按 partial 分文案；partial 追加跳过说明；metadata.truncated/partial。
4. tool/grep.ts：empty 加 partial:false；emptyPartial 新对象；emptyTimedOut(partial) 参数化；partial 先行于普通 empty（前置 + stat 后双位置）；metadata 统一 partial。
5. compaction.ts renderSearchHistory 加 "(incomplete)"——目标无 Search History 渲染 → 跳过（record）。
6. TUI routes/session/index.tsx：isIncompleteSearch 判定；visiblePartIDs/visibleParts details 关闭放行 partial；ToolPart.shouldHide 放行；Glob renderer "(incomplete)"；grepResult 加 partial 参数与组合文案。其余 ~800 行 reasoning 聚合重构与 prettier 噪音不迁移。
7. 测试：file/ripgrep.test.ts（scripted spawner mock 全契约）；glob.test.ts +4；grep.test.ts partial 组合；session-message-render.test.tsx 可见性（+7 个 reasoning 测试不迁移）；compaction.test.ts Search History——目标无功能跳过。

## Target adaptation strategy
- core/src/ripgrep.ts（owner 迁移）：run() 已内建 `partial: code === 2`（#244）但公开方法丢弃。最小侵入：run input 加 `onPartial?: () => void` 回调（与 onTimedOut 同构）；收紧 partial 判定为 `code === 2 && stderrText === ""`（--no-messages 抑制权限诊断后 stderr 空 = 遍历不完整；stderr 非空 = fatal 输入错误——source 分类语义）；code===2 时先触发 onPartial 再返回 partial。
- glob()：args 加 --no-messages（权限诊断不进入 stderr → code2+stderr 空 → partial）；GlobInput 加 onPartial 透传。
- find()：不加 --no-messages（source streaming files 保持原失败语义）；收紧后 code2+stderr 非空 → failure（修复 target 现状"部分结果静默呈现"）。
- grep()：已有 --no-messages + InvalidPatternError 先行（#244/#138 保留）；GrepInput 加 onPartial 透传。
- tool/glob.ts：onPartial 回调设置 partial 标志；0 结果 partial 分文案；partial 跳过说明；metadata.partial；total 保留 #350 originalCount 口径（record 不一致：source totalFiles=length+truncated）。
- tool/grep.ts：emptyPartial/emptyTimedOut(partial)/partial 先行/组合，与 source 对齐；调用 grep 时 onPartial 回调。
- TUI：isVisibleAssistantPart 放行 completed partial（isIncompleteSearch 目标版：tool∈{glob,grep} && completed && metadata.partial===true）；ToolPart.shouldHide 同步；Glob renderer "(incomplete)"；grepResult 加 partial 组合。不动 reasoning 聚合。
- compaction：目标无 Search History → 跳过并记录。
- 测试：core/test/ripgrep.test.ts 加 code2+stderr 空/非空与 --no-messages 断言（真实 rg 二进制——权限场景用不可读目录或 mock AppProcess 视现有 harness）；opencode tool/glob.test.ts 补 Layer.mock(Ripgrep.Service) seam + 4 用例；grep.test.ts 扩展 onPartial；TUI 可见性测试迁移 packages/tui/test。

## Verification（完成）
- materialize 413：passed 413/413（多次修订后最终版本；cvA 过程中曾因手工合成 ripgrep.test.ts/grep.test.ts 段的 hunk 头计数与顺序问题反复 corrupt，最终用 git blob diff 权威段替换解决）
- typecheck 413：仅剩 4 个 tools.ts TS2554（run.promise 第二参，0415 bridge Shape.promise options 中间态）；0411 的 3 个与 0412 的 4 个已消解
- 行为测试（.test-workspace）：core test/ripgrep.test.ts 7/7（含 fake-rg partial 用例：真实 spawn fake binary .cmd 包装 exit 2 + stderr 空 → onPartial 触发 + items 保留）；opencode tool/glob.test.ts 4/4（2 个 partial mock 用例）；tool/grep.test.ts 13/13（2 个 partial 用例 + 既有 caps broad match 断言修正 64→65）；TUI session-message-spacing.test.ts 5/5（2 个 partial 可见性用例）
- 既有测试修正：grep.test.ts "caps broad match output" 断言 "Found 64+"→"Found 65+"（#350 totalMatches=截断前真实数，tool 多取 1 sentinel；实现在 0412 前已如此，断言与实现不一致属 pre-existing——修复断言，record 记录）
- fatal（code 2 + stderr 非空）分支：Windows 上 fake .cmd 的 stderr 管道在 fast-exit 时丢失（node:child_process 层竞态，非生产问题）→ 该测试改为 POSIX chmod 000 条件注册（win32 跳过）；production 的同类路径由既有 invalid-regex 测试（code 2 + 诊断 → Failure）覆盖

## E/C
- count-ec-auditor.py（多次重导后最终值）：E=279 / C=42 / need=42 → PASS（注释分散于 partial 分类不变量、--no-messages 语义、隐私不回传、空结果文案区分、TUI 可见性、fake-rg 构造原因、onPartial 同步回调、101 sentinel 口径、header 不掺 partial 等）
- 注：早期 record 所记 E=369 为中间态错误值；最终权威 diff 为 279

## 修订历史
- R1：从 source 布局重写 core ripgrep（onPartial 回调 + 收紧 code2+stderr 空）；TUI reasoning 重构噪音 800 行不迁移（语义与目标 ReasoningPart 无关）
- R2：ripgrep.test.ts fake 测试 3 轮（RG_MODE/process.execPath 直启失败 → .cmd 包装 + stderr flush 竞态 → partial 保留、fatal 改 POSIX chmod）
- R3：grep.test.ts 既有断言修正；current 手工合成段全部改用 git blob 权威 diff
- R4（批次双审计非阻塞项记录）：
  - N-1：TUI isIncompleteSearch 实现只检查 `type==="tool" && metadata.partial===true`（调用点已检查 completed），未检查 tool 名 ∈ {glob, grep}——record 原文"tool∈{glob,grep}&&completed&&partial"描述不实，已修正；仓库内仅有 glob/grep 产生 partial 故实际影响限于第三方工具 metadata 字段碰撞（N 级）
  - N-2：grepResult 在 timedOut+partial 同时为真时只显示 "timed out"（与自身注释"独立组合"有出入）——罕见同帧竞态，metadata.partial 仍持久化（N 级）
  - N-3：glob 工具 truncated=length===limit 的 101 口径边缘（恰好 limit 条时误标 truncated）——0405 基线 #350 既有（N 级）
  - N-4：grep.test.ts "searches more than the legacy candidate file limit" 在部分运行中 5000ms 超时（Windows 大文件写入 + 全量 ripgrep）——pre-existing 环境 flake；本地完整实测 13/13 通过（N 级）
  - N-5（R2 审计补充）：source 部分测试断言未逐一适配——complete-empty 文案、bounded-total 101 口径、含空格路径、纯 timedOut partial:false、timedOut+partial 组合、`(incomplete)` 文本渲染断言；对应行为已全部实现于 materialized 目标（glob.ts L69/L85、grep.ts L147/L155/L171/L214、index.tsx L3152/L3209-3217），仅缺直接测试敏感性；已迁移的 partial 测试（glob 2、grep 2、TUI spacing 2）+ core ripgrep 7/7 覆盖主语义（N 级，记录补充）

## 最终行为
- glob/find/grep：code 2 + stderr 空 = partial（completed，items 保留，onPartial 回调）；code 2 + stderr 非空 = fatal 错误；glob 加 --no-messages；find 保持失败语义（不加）
- glob tool：partial-empty 文案（"No files found in accessible paths."）、跳过说明、metadata.partial；total 保持 #350 originalCount
- grep tool：emptyPartial/emptyTimedOut(partial)/partial 先行于普通 empty（前置 + stat 后双位置）/metadata.partial 恒持久化
- TUI：isVisibleAssistantPart 对 completed+partial 搜索放行（details 关闭仍可见）；Glob renderer "(incomplete)"；grepResult partial 独立组合
- compaction：目标无 Search History 渲染 → source 的 renderSearchHistory hunk 跳过（record 记录，无对应语义可承载）