
## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「死副本指向不存在模块」——B 核心，
    R72 skip 桩前移后 422-0435 区间无 import 失败文件；
  - 「0436 才 skip 桩化，不能追溯」——R72 以债务前移反驳：
    补偿可以也应该移到最早引入者（R24/R71 同模式）；
  - record 短（6 行）——本节定稿。
- 结构史：
  1. R13 生成 opencode new-file 测试（139 行，import 源系模块路径）；
  2. 第 2 波复验时 R71 的 slice 事故波及本文件
     （shell.test marker 之后的追加段被误删又重建，
      malformed 段出现重复 section）；
  3. R72 彻底重组：docs / app.tsx / opencode malformed(skip 桩) /
     tui malformed(权威副本) 四 section 定稿。
- 0436 补偿删除的对账：
  - 0436 原 malformed section（skip 桩化 5 行 hunk）
    在 skip 桩前移后目标已存在相同内容，
    保留会造成 apply 冲突 → 整 section 删除；
  - 0436 其余 section 不受影响。

## 验证补充
- 0452 物化态 opencode 版 :2-8 直读：
  test.skip / guard stub / 迁移注释三要素齐备；
- 0452 物化态 tui 版 :1-4 直读：
  import "../src/app" 正向引用（guard 真实实现）；
- 全链 typecheck 30/30 佐证 skip 桩在 422-0435 区间的编译面
  （stub 的 any 返回与 _test.skip 形态无类型错误）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「死副本指向不存在模块」——B 核心，
    R72 skip 桩前移后 422-0435 区间无 import 失败文件；
  - 「0436 才 skip 桩化，不能追溯」——R72 以债务前移反驳：
    补偿可以也应该移到最早引入者（R24/R71 同模式）；
  - record 短（6 行）——本节定稿。
- 结构史：
  1. R13 生成 opencode new-file 测试（139 行，import 源系模块路径）；
  2. 第 2 波复验时 R71 的 slice 事故波及本文件
     （shell.test marker 之后的追加段被误删又重建，
      malformed 段出现重复 section）；
  3. R72 彻底重组：docs / app.tsx / opencode malformed(skip 桩) /
     tui malformed(权威副本) 四 section 定稿。
- 0436 补偿删除的对账：
  - 0436 原 malformed section（skip 桩化 5 行 hunk）
    在 skip 桩前移后目标已存在相同内容，
    保留会造成 apply 冲突 → 整 section 删除；
  - 0436 其余 section 不受影响。

## 验证补充
- 0452 物化态 opencode 版 :2-8 直读：
  test.skip / guard stub / 迁移注释三要素齐备；
- 0452 物化态 tui 版 :1-4 直读：
  import "../src/app" 正向引用（guard 真实实现）；
- 全链 typecheck 30/30 佐证 skip 桩在 422-0435 区间的编译面
  （stub 的 any 返回与 _test.skip 形态无类型错误）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「死副本指向不存在模块」——B 核心，
    R72 skip 桩前移后 422-0435 区间无 import 失败文件；
  - 「0436 才 skip 桩化，不能追溯」——R72 以债务前移反驳：
    补偿可以也应该移到最早引入者（R24/R71 同模式）；
  - record 短（6 行）——本节定稿。
- 结构史：
  1. R13 生成 opencode new-file 测试（139 行，import 源系模块路径）；
  2. 第 2 波复验时 R71 的 slice 事故波及本文件
     （shell.test marker 之后的追加段被误删又重建，
      malformed 段出现重复 section）；
  3. R72 彻底重组：docs / app.tsx / opencode malformed(skip 桩) /
     tui malformed(权威副本) 四 section 定稿。
- 0436 补偿删除的对账：
  - 0436 原 malformed section（skip 桩化 5 行 hunk）
    在 skip 桩前移后目标已存在相同内容，
    保留会造成 apply 冲突 → 整 section 删除；
  - 0436 其余 section 不受影响。

## 验证补充
- 0452 物化态 opencode 版 :2-8 直读：
  test.skip / guard stub / 迁移注释三要素齐备；
- 0452 物化态 tui 版 :1-4 直读：
  import "../src/app" 正向引用（guard 真实实现）；
- 全链 typecheck 30/30 佐证 skip 桩在 422-0435 区间的编译面
  （stub 的 any 返回与 _test.skip 形态无类型错误）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「死副本指向不存在模块」——B 核心，
    R72 skip 桩前移后 422-0435 区间无 import 失败文件；
  - 「0436 才 skip 桩化，不能追溯」——R72 以债务前移反驳：
    补偿可以也应该移到最早引入者（R24/R71 同模式）；
  - record 短（6 行）——本节定稿。
- 结构史：
  1. R13 生成 opencode new-file 测试（139 行，import 源系模块路径）；
  2. 第 2 波复验时 R71 的 slice 事故波及本文件
     （shell.test marker 之后的追加段被误删又重建，
      malformed 段出现重复 section）；
  3. R72 彻底重组：docs / app.tsx / opencode malformed(skip 桩) /
     tui malformed(权威副本) 四 section 定稿。
- 0436 补偿删除的对账：
  - 0436 原 malformed section（skip 桩化 5 行 hunk）
    在 skip 桩前移后目标已存在相同内容，
    保留会造成 apply 冲突 → 整 section 删除；
  - 0436 其余 section 不受影响。

## 验证补充
- 0452 物化态 opencode 版 :2-8 直读：
  test.skip / guard stub / 迁移注释三要素齐备；
- 0452 物化态 tui 版 :1-4 直读：
  import "../src/app" 正向引用（guard 真实实现）；
- 全链 typecheck 30/30 佐证 skip 桩在 422-0435 区间的编译面
  （stub 的 any 返回与 _test.skip 形态无类型错误）。
