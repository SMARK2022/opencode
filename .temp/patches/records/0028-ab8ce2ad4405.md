# Record: Patch 28 — ab8ce2ad4405

## 1. Manifest Entry
- **Index**: 28
- **SHA**: ab8ce2ad4405bb591fad947692c69c5a41f38441
- **Parent**: 0027-7dc9f1ab534a
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加Bash输出压缩功能和相关配置

## 2. Source Behavior (original patch, 8 files)
1. `bash-compress.ts` (NEW, 949 行): 自包含的 Bash 输出压缩器。导出 `CompressionConfig`, `CompressionStats`, `CompressResult`, `DiagnosticContext`, `DiagnosticSnapshot`, `DEFAULT_COMPRESSION_CONFIG`, `BashDiagnosticCollector` 类, `compressVisibleOutput`, `renderDiagnosticAppendix`, `bashCompressionMetadata` 函数。使用 `Buffer`, `process.env` 和纯 TS，无 opencode 依赖。
2. `bash.ts`: 添加 `compress_output` 参数; `bashCompressionEnabled` 辅助函数; `BashDiagnosticCollector` 诊断收集; `compressVisibleOutput` 压缩; `renderDiagnosticAppendix` 附录生成; `compressionGuidance` 提示文本; metadata 返回压缩统计。
3. `bash.txt`: 添加 `${compressionGuidance}` 占位符。
4. `config.ts`: 添加 `bash_compression` 到 `tool_output` schema。
5. `openai-responses-language-model.ts`: `parallel_tool_calls: openaiOptions?.parallelToolCalls ?? true`。
6. `types.gen.ts`: 添加 `bash_compression?: boolean`。
7. `app.tsx`: 添加 `bashCompressionOverride` 信号 + `createEffect` + `bashCompressionEnabled` memo + 切换命令。
8. `routes/session/index.tsx`: 添加 `compress_output` 参数传递。

### 行为目的
- **输出压缩 invariant**: 启用时使用 `BashDiagnosticCollector` 收集诊断信息，`compressVisibleOutput` 压缩可见输出，`renderDiagnosticAppendix` 生成附录
- **配置 invariant**: `bash_compression` 配置字段控制全局启用/禁用
- **TUI 切换 invariant**: 用户可通过命令切换压缩，调用 `sdk.client.config.update` 持久化
- **parallel_tool_calls invariant**: OpenAI 系列显式启用 `parallelToolCalls`

## 3. 上游 (v1.17.20) 行为
- `bash.ts` → `shell.ts` (BashTool → ShellTool); 参数移到 `shell/prompt.ts` (`parameterSchema()`)
- `bash.txt` → `shell/shell.txt` + `shell/prompt.ts` (per-shell 提示系统)
- `config.ts` → 拆分为 `core/src/v1/config/config.ts` (V1 wire) + `core/src/config/tool-output.ts` (V2 runtime)
- `app.tsx` → `tui/src/app.tsx`; 命令 API 从 `command.register` 变为 `appCommands()` 数组
- `routes/session/index.tsx` → `tui/src/routes/session/index.tsx`; Bash→Shell, BlockTool

## 4. 目标适配 (current patch, 8 files)
### 路径迁移
- `bash.ts` → `shell.ts` + `shell/prompt.ts`
- `config.ts` → `core/src/v1/config/config.ts` + `core/src/config/tool-output.ts`
- `openai-responses-language-model.ts` → `core/src/github-copilot/responses/openai-responses-language-model.ts`
- `app.tsx` → `tui/src/app.tsx`
- `routes/session/index.tsx` → 跳过（V2 shell 工具内部读取配置，TUI 不需要传递 compress_output）

### 跳过的文件（V2 上游更强或已覆盖）
- `bash.txt` → `shell/shell.txt`: V2 的 `shell/prompt.ts` 已有更完整的 per-shell 提示系统
- `routes/session/index.tsx`: V2 shell 工具的 execute 函数内部读取配置并计算 compressOutput，TUI 不需要传递

### 保留的 SMARK 语义
- `bash-compress.ts` 完整保留（949 行自包含模块）
- `shell.ts` 添加 `bashCompressionEnabled` 辅助函数 + `compressOutput` 计算 + `compressOutput` 传递到 run
- `shell/prompt.ts` 添加 `compress_output` 参数到 `parameterSchema()`
- `core/src/v1/config/config.ts` 添加 `bash_compression` 字段
- `core/src/config/tool-output.ts` 添加 `bash_compression` 字段
- `openai-responses-language-model.ts` 添加 `?? true` 默认值
- `types.gen.ts` 添加 `bash_compression?: boolean`
- `tui/src/app.tsx` 添加压缩切换命令（适配 V2 的 `appCommands()` 格式）

### 保留的上游优势
- V2 的 `ShellTool` 结构和 `ShellPrompt.render` 系统
- V2 的 `parameterSchema()` 模式
- V2 的拆分配置架构（V1 wire + V2 runtime）
- V2 的 `appCommands()` 命令格式

## 5. E/C 计算
- **E ≈ 100**（bash-compress.ts ~80 + shell.ts ~10 + 其他文件 ~10）
- **C ≈ 5**（shell.ts 2 + config 1 + prompt.ts 1 + app.tsx 1）
- **最低 C = max(1, ceil(100 * 0.15)) = 15**

**注意**: bash-compress.ts 是新文件，E 很大但 C 不足。bash-compress.ts 内部已有英文注释，需要添加中文注释。

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 30 --rebuild`: passed 30/30
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/tui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首次 typecheck 失败**: shell.ts 中 `bashCompressionEnabled` 函数重复定义（旧代码和新代码都添加了函数）; `compressOutput` 在错误作用域（外层生成器而非 execute 函数）; `compress_output` 未添加到 Parameters 类型; run 输入类型未更新
   - 修复: 使用 `count=1` 避免重复; 移动 `compressOutput` 到 execute 函数内; 修正 `parameterSchema()` 替换模式; 修正 run 输入类型替换模式

## 8. 最终行为
- `bash-compress.ts` 提供自包含的输出压缩功能
- `shell.ts` 在 execute 中从配置和参数计算 compressOutput 并传递到 run
- `shell/prompt.ts` 的 `parameterSchema()` 包含 `compress_output` 参数
- 配置 schema（V1 和 V2）都包含 `bash_compression` 字段
- `openai-responses-language-model.ts` 默认启用 `parallelToolCalls`
- `tui/src/app.tsx` 提供压缩切换命令
- V2 路径和结构保留
