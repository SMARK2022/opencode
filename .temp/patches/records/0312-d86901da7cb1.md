# Record 0312-d86901da7cb1 — fix(ci): 修复 Windows 单测竞态与桌面测试导入

## Identity
- index/full SHA: 0312 / d86901da7cb1ac4390fb84c10bee395ec708c68a (short d86901da7cb1)
- manifest parent: 02f84d332c1c (0311)
- subject: fix(ci): 修复 Windows 单测竞态与桌面测试导入
- status: PASS

## Source intent (original/0312-d86901da7cb1.patch, 111 行)
1. `packages/core/src/util/flock.ts`：release 时先原子 rename lockDir→releasePath 再 rm，避免 Windows 并发 rm(lockDir) 删除另一个等待者刚 mkdir 成功的新锁目录；加 `rename` import + `code(err)` 检查 ENOENT/ENOTDIR
2. `packages/desktop/src/main/server.ts`：preferAppEnv 中 loadShellEnv(shell) → loadShellEnv(shell, getLogger())，显式传入 desktop logger
3. `packages/desktop/src/main/shell-env.ts`：移除顶层 `import { getLogger } from "./logging"`，改为 `loadShellEnv(shell: string, logger: Logger = console)`；注释解释 Bun 单元测试直接导入 helper 不应在 import 阶段加载 electron-log
4. `packages/opencode/test/cli/tui/daemon.test.ts`：OPENCODE_DAEMON_STARTUP_IDLE_TIMEOUT_MS 250→2000 + 注释（Windows CI 冷启动超 250ms）

## Three-way comparison
- source/SMARK：
  - flock.ts: 原子 rename+rm 替代直接 rm，Windows 并发安全
  - server.ts: loadShellEnv 传 getLogger()
  - shell-env.ts: logger 参数化 + 默认 console + 注释
  - daemon.test.ts: startup idle 250→2000
- 上游 v1.17.20：
  - flock.ts: 直接 rm(lockDir)，无并发保护
  - server.ts: loadShellEnv(shell) 无 logger 参数
  - shell-env.ts: 模块顶层 import getLogger，Bun 单测导入失败
  - daemon.test.ts: startup idle 250ms
- 目标当前：
  - flock.ts: 直接 rm(lockDir)（L264），需适配
  - server.ts: **已有** `import { getLogger } from "./logging"` + `loadShellEnv(shell, getLogger())`（L5/L47）→ NO-OP
  - shell-env.ts: **已有** `type ShellEnvLogger` + `loadShellEnv(shell: string, logger: ShellEnvLogger)`（L8/L70），但缺默认值和注释 → 加 `= console` 默认 + 注释
  - daemon.test.ts: startup idle 250（L508），需改 2000
- 结论：目标已部分适配 server.ts 和 shell-env.ts（上游更强），需补充 flock.ts 原子 rename+rm、shell-env.ts 默认值+注释、daemon.test.ts 超时调整

## Target adaptation
- flock.ts：L4 加 `rename` import；L264 `rm(lockDir)` → 原子 `rename(lockDir, releasePath)` + `rm(releasePath)` + ENOENT/ENOTDIR 错误处理（使用已有 `code(err)` helper L69）
- shell-env.ts：L70 `loadShellEnv(shell: string, logger: ShellEnvLogger)` → `= console` 默认 + 2 行注释
- server.ts：NO-OP（目标已有 `import { getLogger }` + `loadShellEnv(shell, getLogger())`）
- daemon.test.ts：L508 `"250"` → `"2000"` + 2 行注释
- 方法：state 0311 语义编辑 → git diff → current/0312

## Verification
- materialize 312/312: passed
- typecheck 312: install reused（frozen install cbdc173），packages/app TS1128（环境基线）
- packages/core typecheck: 基线错误（tool-websearch.test.ts 等，非 flock.ts 引入，0311 同存在）
- packages/desktop typecheck: 基线错误（renderer/onboarding.tsx 等，非 shell-env.ts 引入，0311 同存在）
- flock test（.test-workspace synced to 312）: 10 pass / 0 fail / 24 expect() / 7.72s
- shell-env test（.test-workspace）: 5 pass / 0 fail / 15 expect() / 96ms
- daemon test（.test-workspace）: 16 pass / 0 fail / 49 expect() / 158.76s
- E/C: E=12 / C=6 / need=max(1,ceil(12*0.15))=2 → PASS
  - 注释位置：flock.ts L266-267（原子 rename 原因）、shell-env.ts L71-72（electron-log import 风险）、daemon.test.ts L509-510（Windows CI 冷启动窗口）
- source/target HEAD: 未变

## 不一致
- server.ts：source 添加 getLogger import + loadShellEnv(shell, getLogger())，目标已有 → NO-OP（上游已更强，无需重复适配）
- shell-env.ts：source 使用 `type Logger = { log: (...args: unknown[]) => void }`，目标使用 `type ShellEnvLogger = { log: (message: string) => void }`（更窄签名）。保留目标类型名（已有 caller 依赖），仅加默认值+注释
- source 的 shell-env 注释在函数签名后第一行，目标同样放置

## DUAL-AUDIT SEAL（批次 311-315）
- R1 双审计（记录扩展后：四份记录从 <50L/3000C 扩展至 ≥55L/3787C，0315 manifest parent 更正为 cf597333）：
  - 正向审计（ses_fdefde82fffeDVRJftO2F6brVb）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdefdcbf6ffeU0neKZIAxyCnKO）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（9/2、12/6、13/3、19/8、6/4）、累计 apply 315/315、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：packages/app TS1128 环境基线（custom-elements.d.ts 符号链接物化文本）；truncation cleanup 测试 7天旧文件时间戳依赖；typecheck script packages/app 首失败阻断后续 workspace（tsgo -b 模式覆盖 project references）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
