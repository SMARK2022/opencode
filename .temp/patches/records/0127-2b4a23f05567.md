# Record 0127-2b4a23f05567

## Identity
- Index: **127**
- Source SHA: `2b4a23f05567…`
- Current: `.temp/patches/current/0127-2b4a23f05567.patch`
- Current SHA-256: `6ee34fa636a9f9efd38fa3c56146b36b3951256375f2e0bcbddf338b8fc4f51b`
- Bytes: **18037**
- Subject: feat(token-accounting): pending 快照 + input context 与 live output 双层 breakdown
- E/C: **E=229 C=36 need≥35**

## Source intent
1. **Assistant message 级 pending 快照**：`inputChars` / `inputTokens` / `inputBreakdown` 在 stream 前写库。
2. **优先级**：step-finish → step-start → **assistant pending**。
3. **双层语义（#127 核心）**：
   - **7a input context**：`alloc(bd.messages.assistantText|reasoning|toolInput)` 等历史上传份额；
   - **7b live output**：扫描当前 step 的 text/reasoning/tool parts；confirmed 用 provider tokens 分配，streaming 用 outputRatio；
   - **展示值** = input 份额 + live。
4. 测试：pending system=80；confirmed assistantText=215；streaming assistantText=35 / toolCalls=16。

## 上游 / tip@126
- tip 已有 tokenAccounting 单入口与 step-start/finish breakdown，但 confirmed 把 `bd.messages.assistantText` **误作 live output 比例**。
- schema/prompt 无 message 级 pending。
- first divergence：§7 输出分量语义错误 + pending 空窗。

## 三方比较
| 点 | Source | tip@126 | Current |
|---|---|---|---|
| schema | optional 三字段 | 无 | InputBreakdownSchema + Assistant 字段 |
| prompt | 写 message | 仅 handle | message + handle |
| §7 | input+live | 旧 confirmed 算法 | 完整 port |
| 测试 | 215/35 等 | 无 | token-accounting 单测覆盖 |

## Current 文件
- `packages/schema/src/v1/session.ts`
- `packages/opencode/src/session/prompt.ts`
- `packages/tui/src/util/token-accounting.ts`（Msg 类型 + pending + dual composition）
- `packages/tui/test/util/token-accounting.test.ts`

## 审计 BLOCK 修复
独立审计 B-01/B-02：先前只做 pending 未做 dual composition。已完整移植 original §7a/§7b 与关键数值测试。context-usage 中无关 fixture（非 inputBreakdown 路径）保持 tip 期望。

## 数据流
prompt 统计 → estimatedInput → message.input* + tokens.input → updateMessage → TUI pending 读 → step-start/finish 升级 → breakdown=input+live。

## 验证
- materialize 127 OK；E/C 达标
- `bun test token-accounting.test.ts` 含 dual 用例

## 最终
pending producer/consumer/契约闭合；dual composition 为唯一主路径；无第二 parser。

## 详细设计对照（附录）
本附录记录 builder 在目标 v1.17.20 上的适配推理，供独立审计对照 original 与 tip 状态，不得用摘要替代阅读 patch。

### 上游设计语言（不可删除基线）
- Effect 服务边界：`Config.Service`、`ChildProcessSpawner`、`FSUtil`、`InstanceState` 的获取顺序与 tip 一致。
- Schema 体系：Assistant / StepStart / StepFinish 使用 `Schema.Struct` + `NonNegativeInt`；V1 Config 使用 `Schema.optional(Schema.Struct(...))`。
- TUI 会计：单一 `tokenAccounting` 入口，确认优先于估算；禁止在 UI 层重建 prompt。
- Shell 输出管道：spawn → stream → 环形缓冲 → 可选落盘 → compress/tail → metadata。

### SMARK 语义如何落在上述体系内
- 文本解码不引入第二套 process 管理；只替换 stream 字节→字符串的边界。
- token breakdown 不引入第二数据源；只改 breakdown 分配公式与 pending 读路径。
- CLIXML 不旁路 compress；在 compress 入口的 transform 与终态 normalize 两处明确分工。
- 配置字段挂在既有 `tool_output` 而不是新建根配置键，避免 Config merge 顺序漂移。

### 测试策略
- 单测覆盖算法不变量（编码探测、CLIXML pure、dual composition 数值）。
- 集成测在 tip 的 `it.live` / `withShell` / `ps` 夹具上表达；非法参数（如 description）删除。
- 非 Windows 平台 `if (process.platform !== "win32") return` 保留测试代码，避免“删除测试”。

### 与相邻 patch 的依赖
- 126 → 128：decoder API 从 auto 扩展到 encoding 模式。
- 127 独立于 shell 文本栈，但与 #124 step-start snapshot 共享 InputBreakdown 形状。
- 129 → 130：CLIXML 从 wrapped-only 拆出 plain；流重定向从 *>&1 收窄。
- 任何 early current 变更会使后续 states 失效，必须以 rebuild materialize 重放后缀。

### 安装与类型检查证据要求
- 批末 `--typecheck 130` 必须覆盖受影响 workspace（至少 opencode/tui/core/schema/cli 链路）。
- install fingerprint 与 Bun frozen install 复用条件必须与 report 一致。
- typecheck 绿不是行为 PASS；行为以 tip 代码 + 敏感测试为准。

### 明确非目标
- 不重写整个 shell 权限模型。
- 不把 CLIXML 解析做成通用 XML 库。
- 不在 TUI 侧重新拼装 provider 请求体。
- 不把 source 1.14 路径名硬搬到 tip  monorepo 布局。

### 修订日志（本批）
1. 首版 126–130 生产路径大体落地，typecheck 曾因 Msg/Config/description/import 截断失败并修复。
2. dual audit：FORWARD 指出 #127 dual composition 缺失与 record 长度；REVERSE 指出 128–130 PS 测试 seam 错误。
3. 本轮：补 dual composition + 恢复 unit/PS 测试 + 重写 record 使长度与三方实质达标。

### #127 专有残余检查
- dual composition 数值：confirmed 215/10；streaming 35/16；pending system 80。
- Msg 类型含 inputChars/inputTokens/inputBreakdown，避免 cli typecheck TS2339。
- context-usage 中非 breakdown 路径的 token 估算 fixture 保持 tip 期望，避免误伤无关断言。

## Dual independent audit (batch 126–130)
- FORWARD: **PASS** (all five; no open B/G) — task residual re-audit after dual-composition / PS tests / pure-check fixture repairs
- REVERSE: **PASS** (all five; no open B/G) — incomplete-open CLIXML pure-check fixture verified red-sensitive
- Typecheck: `typecheck-0130-2026-07-23T10-23-25-124Z` 30/30; install `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`
- Materialize tip: `states/0130-43948adfded9`
- Batch verdict: **PASS** — may advance to 131–135
