# Record 0362-ddc5e00c6038

## Identity
- manifest parent: 8791d6a09341 (0361)
- subject: fix(provider): connect provider no longer kills running sessions
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0362-ddc5e00c6038` (passed 362/362)
- current patch: `.temp/patches/current/0362-ddc5e00c6038.patch` (14411 bytes)

## Source intent
Previously, connecting or disconnecting a provider from the TUI/Web app made
the server call `disposeAllInstances`, destroying every instance and forcing
other running sessions (including sessions on other providers/models) to be
cancelled by the `SessionRunState` finalizer. The fix replaces that with
`refreshProviderCaches` in the three server-side handlers
(`authSet`/`authRemove`/OAuth callback): it only refreshes the
`Provider.Service` cache via `ScopedCache.invalidateAll` (never touching
`SessionRunState`), then emits `global.disposed` so clients refresh their UI.
Running sessions keep using the old key to finish; the next new message
lazily rebuilds the cache and picks up the new key. The `configUpdate` path
keeps the old behavior (known limitation).

## Source patch anatomy (10 files)
- `packages/app/src/components/dialog-connect-provider.tsx` — drop
  `global.dispose()` in `complete()`.
- `packages/app/src/components/settings-providers.tsx` — drop
  `global.dispose()` in the auth.remove `.then()`.
- `packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx` — drop 3
  `instance.dispose()` calls (Auto/Code/Api methods).
- `packages/opencode/src/cli/cmd/tui/context/sync.tsx` — `global.disposed`
  handler: DaemonStop → exit; other reasons → `bootstrap()`.
- `packages/opencode/src/provider/provider.ts` — add `invalidateAll` to
  Interface + implementation (`ScopedCache.invalidateAll(state.cache)`) +
  `Service.of`.
- `packages/opencode/src/server/global-lifecycle.ts` — add
  `refreshProviderCaches` (invalidateAll + emitGlobalDisposed).
- `packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts` —
  call `refreshProviderCaches()` after authSet and authRemove.
- `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` —
  call `refreshProviderCaches()` after the OAuth callback.
- `packages/opencode/test/fake/provider.ts` — add no-op `invalidateAll` to the
  fake to keep the Interface complete.
- `packages/opencode/test/provider/provider.test.ts` — two tests: list reflects
  env after invalidateAll; getLanguage rebuilds after invalidateAll.

## Target mapping (state 0361)
| Source file | Target file | Status |
|---|---|---|
| `packages/app/src/components/dialog-connect-provider.tsx` | same | ADAPTED (EXISTS; `serverSDK().client.global.dispose()` vs source `globalSDK...`) |
| `packages/app/src/components/settings-providers.tsx` | same | ADAPTED (EXISTS; same `serverSDK()` naming) |
| `packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx` | `packages/tui/src/component/dialog-provider.tsx` | MIGRATED (EXISTS, 3 dispose calls at L281/332/405) |
| `packages/opencode/src/cli/cmd/tui/context/sync.tsx` | `packages/tui/src/context/sync.tsx` | MIGRATED (EXISTS, handler at L462) |
| `packages/opencode/src/provider/provider.ts` | same | ADAPTED (EXISTS; uses `InstanceState` + `ScopedCache`) |
| `packages/opencode/src/server/global-lifecycle.ts` | same | ADAPTED (EXISTS) |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts` | same | ADAPTED (EXISTS) |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` | same | ADAPTED (EXISTS) |
| `packages/opencode/test/fake/provider.ts` | same | ADAPTED (EXISTS) |
| `packages/opencode/test/provider/provider.test.ts` | same | ADAPTED (EXISTS, 2261 lines, different helper style) |

## Target-adaptation decisions
1. **App SDK naming**: the target app uses `serverSDK().client.global.dispose()`
   (not `globalSDK.client...`); only the dispose call is removed, the
   surrounding code is untouched.
2. **TUI sync.tsx handler shape**: the target already diverged from the source
   (uses `(event.properties as { reason?: string }).reason !== "daemon-stop"`
   + `void exit("opencode daemon stopped.")` from batch #273, not
   `daemonStopReason`/`DisposedReason.DaemonStop`). The semantic change is
   preserved: daemon-stop → exit; any other reason → `bootstrap()`. The
   `refreshProviderCaches` emission uses empty `properties` (non-DaemonStop),
   so the else-branch is what fires after auth changes.
3. **`log.warn` → `Effect.logWarning`**: the target's global-lifecycle.ts has
   no `* as Log` import; it already uses `Effect.logWarning("global disposal
   failed", { cause })`. The refresh failure branch uses the same idiom.
4. **`ScopedCache.invalidateAll(state.cache)`**: the target's provider state is
   an `InstanceState` whose `.cache` is a `ScopedCache` keyed by directory;
   `ScopedCache` is added to the effect import and `invalidateAll` invalidates
   every directory entry — matching the source's "all directories" intent.
5. **Tests adapted to `it.instance` style**: the target test file uses
   `testEffect(LayerNode.compile(...))` + `it.instance(...)` with `set`/`remove`
   Env helpers and `Provider.use.*` accessors, not the source's
   `withTestInstance`/`run(ctx, ...)`. The two tests are rewritten in that
   style; `Provider.use.invalidateAll()` is generated by `serviceUse` from the
   new Interface member. Model ID uses the target's `claude-sonnet-4-6`
   (the source's `claude-sonnet-4-20250514` is blacklisted in target tests).
6. **Comment numbering `#362`** on all adaptation comments.

## E/C audit
E=52, C=35, need=8, PASS. (C: CJK rationale comments in the app/TUI dispose
removals, sync.tsx else-branch, provider.ts/global-lifecycle.ts, and tests.)

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 362` →
  "passed 362/362", state at `states/0362-ddc5e00c6038`.
- Report: `materialize-0362-*.json` — reusedPrefix 361, baseState
  `0361-8791d6a09341`, no stoppedAt, no integrityFailure.
- Post-apply verification: `refreshProviderCaches` at global-lifecycle.ts L33,
  `invalidateAll` in Interface (L1333) + impl (L1978) + Service.of (L2349),
  control.ts L20, sync.tsx daemon-stop/branch at L464-471.

## Line accounting
- app dialog-connect-provider.tsx: +2/-1; settings-providers.tsx: +2/-1.
- tui dialog-provider.tsx: +3/-3; sync.tsx: +8/-3.
- provider.ts: +6/-1; global-lifecycle.ts: +13/-0; control.ts: +5/-0;
  provider handler: +3/-1.
- fake/provider.ts: +2/-0; provider.test.ts: +43/-0.

## Risks / notes
- The `configUpdate` path intentionally keeps the old dispose behavior (source
  documents this as a known limitation); this patch does not touch it.
- `refreshProviderCaches` is fire-and-forget safe: `invalidateAll` failures are
  logged and swallowed because auth is already persisted to disk and the next
  access rebuilds lazily.
- The sync.tsx change makes non-daemon-stop `global.disposed` trigger
  `bootstrap()`; the `server.instance.disposed` handler still guards on
  `daemonStopSeen`, so no double-exit occurs.
- Typecheck of the change is covered by the batch-end `--typecheck 365`.

## Per-item diff stats
- fix362.diff bytes: 14223 (raw diff, LF); patch file with header: 14411.
- 10 files, 14 hunks.
