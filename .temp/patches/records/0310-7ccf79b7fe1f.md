# Record 0310-7ccf79b7fe1f — fix(tests): 修复 Windows CI 中 git 提交验证逻辑，优化文件检查方式

## Identity
- index/full SHA: 0310 / 7ccf79b7fe1f54ba04a7b578e676a9e74924e06b (short 7ccf79b7fe1f)
- manifest parent: 7470e100e43f (0309)
- subject: fix(tests): 修复 Windows CI 中 git 提交验证逻辑，优化文件检查方式
- status: PASS（首个编辑快照建立中——record 先行）

## Source intent (original/0310-7ccf79b7fe1f.patch, 74 行)
1. `.temp/chatgpt-browser-agent` gitlink 指针更新 → **skip（target 无 gitlink，0296 先例）**
2. `packages/opencode/test/session/prompt.test.ts`（L1165+）：
   - "Shell review metadata" 测试：增加 `const test = yield* TestInstance`；移除 `external_directory` allow 权限（避免敏感路径触发单独 prompt 阻塞 shell review）；改用项目内 `id_rsa` 文件（`Bun.write(path.join(test.directory, "id_rsa"), "review-output\n")`）替代 `~/.ssh/id_rsa`（避免 Windows 家目录路径和缺失文件错误）；命令 `cat ~/.ssh/id_rsa` → `cat id_rsa`；超时 15_000 → 30_000
3. `packages/opencode/test/tool/repo_clone.test.ts`：commitFile 从 `git diff --quiet` 改为 `git ls-tree --name-only HEAD -- file` → **NO-OP（target 无 repo_clone 测试）**

## Target adaptation plan
- gitlink: skip
- prompt.test.ts: 核实 "Shell review metadata" 测试存在性与上下文后适配（TestInstance、id_rsa、external_directory、超时）
- repo_clone: NO-OP
- 方法：state 0308（前一状态）语义编辑 → git diff → current/0310

## E/C
- E=4, C=4, need=1 → PASS（TestInstance/id_rsa/external_directory 注释×2 + 超时注释×2）

## Verification log
1. apply --check EXIT 0; validate_hunks fixed:0; materialize 310 → passed 310/310
2. typecheck 310: Install reused (cbdc173…); packages/app env baseline; Source/Target unchanged
3. packages/opencode bun typecheck → exit 0; readme.test.ts 9/9/51
4. gitlink skip（0296 先例）；repo_clone hunk NO-OP（target 无 repo_clone 测试）
5. current SHA-256 ebb39a1c24e85c5a15aa99c9f9bda0dedaf01a8ad30f181c937164df1044b567 / 1748B

## 不一致
- 初始因 0309 BLOCK 无法物化；0309 解决后 materialize 310 通过 310/310
- prompt shell review metadata 测试超时风险（同 0308 prompt shell 测试环境性 N 模式）
- 记录时间线：record 首建于编辑前

## 上游对比
上游 prompt.test.ts 有 Shell review metadata 测试（#214 时代）；source 0310 优化该测试（项目内 id_rsa、移除 external_directory allow、超时 15→30s）；repo_clone.test.ts 与 gitlink 为 NO-OP/skip

## Source hunk 映射
prompt.test.ts: TestInstance yield + external_directory allow 移除 + Bun.write id_rsa + cat id_rsa + 超时 15_000→30_000
repo_clone.test.ts: NO-OP
gitlink: skip（0296 先例）

## E/C 注释位置
C 位置: // 本用例只需要触发 SSH 私钥名的 cautious 预审 + // 使用项目内文件避免 Windows runner + // 这是 shell 执行、auto reviewer 子会话和审计回链 + // Windows CI 慢机曾在 15s 边界取消

## Audit trail
- （双审计 seal 在复审通过后追加）

## Install 指纹与 typecheck 证据
- Install: reused (cbdc173ade19a6018199dae8f0b886997e6be93f34ecee72de7851fd77da9348)（0310 未动依赖）
- typecheck 310 报告: Passed 310/310, packages/app TS1128（环境基线）
- packages/opencode bun typecheck → exit 0
- readme.test.ts 9/9/51 expects
- prompt.test.ts Shell review metadata 测试超时风险（同 0308 环境性 N：完整集成链 session+server+shell+reviewer >10s 在慢机上）

## NO-OP 证据
- gitlink .temp/chatgpt-browser-agent: target state repo git ls-files -s -- .temp 为空，无 gitlink 条目（0296 先例）
- repo_clone.test.ts commitFile ls-tree: target 无 repo_clone 工具（packages/opencode/src/tool/ glob 确认 31 个 .ts 文件无 repo_clone）与测试文件

## DUAL-AUDIT SEAL（批次 306-310）
- 第二轮双审计（R2 修复后：记录扩展 + 0309 fixture.ts 死代码→vcs.test.ts 可达）：
  - 正向审计（ses_fdf4eeb92ffevPAio1sf1avQJx）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdf4ed38effeP3yoEqRZsyBC46）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（4/2、79/20、118/26、3/1、4/4）、累计 apply 310/310、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：prompt.test.ts shell 测试 10s 超时（296-300 N-02 模式）；packages/app TS1128 环境基线；indentation stripping N（语法有效、行为不变）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
