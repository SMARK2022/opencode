
## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「tui.test.ts:154 断言 volume 0.4 必然失败」——本项 B 核心，
    R72 落地源 tui.test hunk 后闭合；
  - 「source 测试 hunk 被丢」——同时补齐 attention.test 的
    config() 桩同步 hunk（源 0421 同 hunk 形态）；
  - record 短（3 行）——R70 首次扩写、R71 重写、R72 本节定稿。
- 0420 交叉影响的两次适配：
  1. R70：enabled 翻转改变 index.tsx/config.test.tsx 的 context
     → 0421 两 hunk context/行号适配（117/49）；
  2. R71：tui.test 的 enabled 断言翻转落地
     → 0421 的 tui.test volume hunk 在新基线上重生成。
- R72 追加 section 的 pre-image 纪律：
  - attention.test / tui.test 均以 states/0420 物化内容为 pre
    （文件级 diff 确认与 .test-workspace 逐字节一致）；
  - 追加位置在 patch 末尾，git apply 按文件独立处理；
  - 探针覆盖：specs/index/config.test 原三节 + 两个新 section
    （volume 0.5 ×3 处）。

## 验证补充
- typecheck-420（R71 后，R72 未触及 ≤420 区间）与
  typecheck-452（R72 后重物化，含本项全部三个 volume hunk）
  双报告 30/30 workspaces passed；
- 452 物化态 config/index.tsx 的 volume 默认 0.5
  与 attention.test/tui.test 断言 0.5 语义一致；
- fixture SATISFIED 的 owner 证据：
  tui-runtime.ts :25-30 createTuiResolvedConfig 仅展开
  ...resolve(input)，无 attention 显式字段块，
  源 hunk（显式 volume 行）在目标无落点。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「tui.test.ts:154 断言 volume 0.4 必然失败」——本项 B 核心，
    R72 落地源 tui.test hunk 后闭合；
  - 「source 测试 hunk 被丢」——同时补齐 attention.test 的
    config() 桩同步 hunk（源 0421 同 hunk 形态）；
  - record 短（3 行）——R70 首次扩写、R71 重写、R72 本节定稿。
- 0420 交叉影响的两次适配：
  1. R70：enabled 翻转改变 index.tsx/config.test.tsx 的 context
     → 0421 两 hunk context/行号适配（117/49）；
  2. R71：tui.test 的 enabled 断言翻转落地
     → 0421 的 tui.test volume hunk 在新基线上重生成。
- R72 追加 section 的 pre-image 纪律：
  - attention.test / tui.test 均以 states/0420 物化内容为 pre
    （文件级 diff 确认与 .test-workspace 逐字节一致）；
  - 追加位置在 patch 末尾，git apply 按文件独立处理；
  - 探针覆盖：specs/index/config.test 原三节 + 两个新 section
    （volume 0.5 ×3 处）。

## 验证补充
- typecheck-420（R71 后，R72 未触及 ≤420 区间）与
  typecheck-452（R72 后重物化，含本项全部三个 volume hunk）
  双报告 30/30 workspaces passed；
- 452 物化态 config/index.tsx 的 volume 默认 0.5
  与 attention.test/tui.test 断言 0.5 语义一致；
- fixture SATISFIED 的 owner 证据：
  tui-runtime.ts :25-30 createTuiResolvedConfig 仅展开
  ...resolve(input)，无 attention 显式字段块，
  源 hunk（显式 volume 行）在目标无落点。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「tui.test.ts:154 断言 volume 0.4 必然失败」——本项 B 核心，
    R72 落地源 tui.test hunk 后闭合；
  - 「source 测试 hunk 被丢」——同时补齐 attention.test 的
    config() 桩同步 hunk（源 0421 同 hunk 形态）；
  - record 短（3 行）——R70 首次扩写、R71 重写、R72 本节定稿。
- 0420 交叉影响的两次适配：
  1. R70：enabled 翻转改变 index.tsx/config.test.tsx 的 context
     → 0421 两 hunk context/行号适配（117/49）；
  2. R71：tui.test 的 enabled 断言翻转落地
     → 0421 的 tui.test volume hunk 在新基线上重生成。
- R72 追加 section 的 pre-image 纪律：
  - attention.test / tui.test 均以 states/0420 物化内容为 pre
    （文件级 diff 确认与 .test-workspace 逐字节一致）；
  - 追加位置在 patch 末尾，git apply 按文件独立处理；
  - 探针覆盖：specs/index/config.test 原三节 + 两个新 section
    （volume 0.5 ×3 处）。

## 验证补充
- typecheck-420（R71 后，R72 未触及 ≤420 区间）与
  typecheck-452（R72 后重物化，含本项全部三个 volume hunk）
  双报告 30/30 workspaces passed；
- 452 物化态 config/index.tsx 的 volume 默认 0.5
  与 attention.test/tui.test 断言 0.5 语义一致；
- fixture SATISFIED 的 owner 证据：
  tui-runtime.ts :25-30 createTuiResolvedConfig 仅展开
  ...resolve(input)，无 attention 显式字段块，
  源 hunk（显式 volume 行）在目标无落点。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「tui.test.ts:154 断言 volume 0.4 必然失败」——本项 B 核心，
    R72 落地源 tui.test hunk 后闭合；
  - 「source 测试 hunk 被丢」——同时补齐 attention.test 的
    config() 桩同步 hunk（源 0421 同 hunk 形态）；
  - record 短（3 行）——R70 首次扩写、R71 重写、R72 本节定稿。
- 0420 交叉影响的两次适配：
  1. R70：enabled 翻转改变 index.tsx/config.test.tsx 的 context
     → 0421 两 hunk context/行号适配（117/49）；
  2. R71：tui.test 的 enabled 断言翻转落地
     → 0421 的 tui.test volume hunk 在新基线上重生成。
- R72 追加 section 的 pre-image 纪律：
  - attention.test / tui.test 均以 states/0420 物化内容为 pre
    （文件级 diff 确认与 .test-workspace 逐字节一致）；
  - 追加位置在 patch 末尾，git apply 按文件独立处理；
  - 探针覆盖：specs/index/config.test 原三节 + 两个新 section
    （volume 0.5 ×3 处）。

## 验证补充
- typecheck-420（R71 后，R72 未触及 ≤420 区间）与
  typecheck-452（R72 后重物化，含本项全部三个 volume hunk）
  双报告 30/30 workspaces passed；
- 452 物化态 config/index.tsx 的 volume 默认 0.5
  与 attention.test/tui.test 断言 0.5 语义一致；
- fixture SATISFIED 的 owner 证据：
  tui-runtime.ts :25-30 createTuiResolvedConfig 仅展开
  ...resolve(input)，无 attention 显式字段块，
  源 hunk（显式 volume 行）在目标无落点。
