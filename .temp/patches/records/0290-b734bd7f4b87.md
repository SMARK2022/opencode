# Record: 0290-b734bd7f4b87

## 基本信息
- index: 0290
- sha12: b734bd7f4b87
- subject: feat(sync): 增加消息限制，提升会话消息处理能力
- source date: Wed, 10 Jun 2026 02:40:06 +0800

## Source 行为重建
原始 patch 修改 1 个文件 2 处：
1. **sync.tsx line 567**：`if (updated.length > 200)` → `if (updated.length > 300)`——本地缓存消息数阈值从 200 提升到 300
2. **sync.tsx line 829**：`sdk.client.session.messages({ sessionID, limit: 200 })` → `limit: 300`——会话消息拉取窗口从 200 提升到 300

目的是降低长会话过早截断的风险，让用户能看到更多历史消息。

### Hunk 行为详情
- **Hunk 1（line 567）**：`if (updated.length > 200)` → `if (updated.length > 300)`——本地缓存消息数阈值提升。当 `store.message[info.sessionID]` 的消息数组长度超过阈值时，移除最旧消息以控制内存占用。输入：消息数组长度；输出：布尔值决定是否触发裁剪。Invariant：裁剪逻辑不变，仅阈值变更。
- **Hunk 2（line 829）**：`sdk.client.session.messages({ sessionID, limit: 200 })` → `limit: 300`——会话消息拉取窗口提升。全量同步时从服务端拉取的消息数量上限。输入：sessionID + limit 参数；输出：消息数组。Invariant：API 调用结构不变，仅 limit 值变更。
- 两个阈值必须对齐：本地缓存窗口（hunk 1）和拉取窗口（hunk 2）必须同步变更，否则拉取的消息数超过缓存阈值会被立即裁剪，造成无谓的丢弃。

## 三方比较
- **文件路径迁移**：source 路径 `packages/opencode/src/cli/cmd/tui/context/sync.tsx` 在目标中不存在；目标已将 TUI 拆分到独立包 `packages/tui/src/context/sync.tsx`（936 行）
- **Hunk 1 上下文不匹配**：
  - source 期望 `const updated = store.message[info.sessionID]`，目标实际为 `const updated = store.message[event.properties.info.sessionID]`（访问路径变了）
  - source 期望 `if` 语句上方无注释，目标实际有 `// #162：本地缓存消息数阈值与 200 窗口对齐` 注释行
- **Hunk 2 上下文不匹配**：
  - source 期望 `limit: 200` 上方无注释，目标实际有 `// #162：会话消息拉取窗口扩至 200，降低长会话过早截断` 注释行
  - 目标在 `if (!options?.force...)` 和 `Promise.all` 之间插入了 6 行 #153 相关逻辑
- 文件路径迁移根源：目标已将 TUI 从 `packages/opencode/src/cli/cmd/tui/` 拆分到独立包 `packages/tui/src/`，sync.tsx 路径变更
- Hunk 1 访问路径变更：source `store.message[info.sessionID]` → 目标 `store.message[event.properties.info.sessionID]`，由目标事件系统重构导致
- #162 注释行：目标在两处阈值上方各有 #162 中文注释行（记录 200 窗口对齐决策），source 无此注释——需作为上下文
- 行号偏移：source 567/829 → 目标 579/855，由路径迁移后的文件重组和 #162/#153 注释行插入导致

## 适配内容
- **文件路径**：`packages/opencode/src/cli/cmd/tui/context/sync.tsx` → `packages/tui/src/context/sync.tsx`
- **Hunk 1**：适配目标上下文——`event.properties.info.sessionID` 访问路径 + `#162` 注释行作为 context
- **Hunk 2**：适配目标上下文——`#162` 注释行作为 context + 调整行号

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（1829 bytes）
- 编辑后 SHA-256: e9ee4978e2dc0f80cb20334054dcc5a208e00d9f228ccce8eb28a33298d9dfbc
- 编辑后字节: 1406
- materialize 290: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| sync.tsx @@ -567 hunk 1 | packages/tui/src/context/sync.tsx @@ -579 | 路径迁移 + 上下文适配 |
| sync.tsx @@ -829 hunk 2 | packages/tui/src/context/sync.tsx @@ -855 | 路径迁移 + 上下文适配 |

## E/C 计算
- E (有效修改行): 4（两处 200→300 值修改 + 两处 #290 中文注释）
- C (中文注释行): 2
  - sync.tsx hunk 1: "#290：本地缓存窗口从 200 提升至 300，降低长会话过早截断风险" (1 行)
  - sync.tsx hunk 2: "#290：会话消息拉取窗口同步提升至 300，与本地缓存窗口对齐" (1 行)
- 满足 E > 0 时 C >= max(1, ceil(4 * 0.15)) = 1，2 >= 1 ✓

## 安装与测试
- materialize 290: passed 290/290
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)

## 不一致
- 文件路径迁移：packages/opencode/src/cli/cmd/tui/ → packages/tui/src/
- 上下文偏移：#162 注释行 + event.properties.info.sessionID

## 物化验证
- sync.tsx hunk 1：`if (updated.length > 300)` 正确应用，#162 注释行保留，#290 注释行添加 ✓
- sync.tsx hunk 2：`limit: 300` 正确应用，#162 注释行保留，#290 注释行添加 ✓
- 文件路径正确：`packages/tui/src/context/sync.tsx`（非 source 的 `packages/opencode/src/cli/cmd/tui/context/sync.tsx`）✓
- 两个阈值对齐：本地缓存窗口 300 = 拉取窗口 300 ✓
- materialize 290: passed 290/290 ✓

## 审计历史
- 第一轮双审计：PASS（第一轮双审计通过）（批次 286-290）

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 286-290，第一轮）
- 正向审计 (ses_0122cab55): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_0122c9807): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 290: passed 290/290
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 286=0/0, 287=0/0, 288=4/2, 289=30/5, 290=4/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#282 if(appendix) 守卫）保留为基线，SMARK exitNotice 正确叠加


## R8 修复记录（2026-08-14）
- 背景：current 0290 结构损坏（缺 diff --git 头、hunk 乱序 880/579/855/878、878/880 hunk 存在把 200 与 300 slice 同时保留的双份代码 bug）。
- 修复：按 source 语义重建为 2 个替换 hunk：
  - @@ -579,4: if (updated.length > 200) → 300（+ #290 注释）
  - @@ -855,5: session.messages limit 200 → 300（+ #290 注释）
- 删除 878/880 的双份 slice hunk（source 0290 无 slice 改动，仅 threshold/limit）。
- 最终 E/C：E=2, C=2, need=1, PASS
- 验证：materialize 290 passed；strict apply --check EXIT 0
