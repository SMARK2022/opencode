# Record 0256-4ed5d29c6bfd

## Status
PASS

## 修改内容
feat(compaction): 添加最近用户指令的保留逻辑，优化压缩上下文处理（recent-user memento 通道）。

### Source 语义（5 文件 9 hunk）
1. 常量：DEFAULT_PRESERVE_RECENT_USER_TOKENS=20_000、PRESERVE_RECENT_USER_RATIO=0.2、RECENT_USER_MEMENTO_TRUNCATED。
2. 函数：preserveRecentUserBudget（min(20K, usable×20%) 无下限）、collectRecentUserMessages（倒序收集非 synthetic/ignored 用户文本、预算内保留否则二分截断）、truncateRecentUserMessage（对重放信封二分）。
3. process：抽出 visibleHistory，memento 从可见窗口采集；双锚点（tail_start_id / recent_user_messages）任一变化即 updatePart。
4. schema/SDK：CompactionPart 增加 recent_user_messages 可选数组。
5. message-v2：RECENT_USER_MEMENTO_INTRO + formatRecentUserMemento（XML 信封）；toModelMessagesEffect 在 "What did we do so far?" 之前 push memento。
6. 测试：3 个新用例（保留/超长截断≤400/synthetic-ignored 排除）。

### 目标适配（0255 后）
- **H1 常量**：preimage 对齐 0255 落地形态（DEFAULT=4 后跟 `// #255：保留预算上下限翻倍…` 注释——注释措辞按 stale 0256 期望对齐）；插入点 MAX 之后、`type Turn = {` 之前（SUMMARY_TEMPLATE 已移 core，preimage 不含）。
- **message-v2 H2 移锚**：源谱系 filterCompacted 在 visibleCompactions 之后，目标顺序相反（filterCompacted@578 前、visibleCompactions@645-671 后、filterCompactedEffect@673）→ 插入点重锚至 visibleCompactions 结尾与 filterCompactedEffect 之间，尾 ctx 用 `  return filterCompacted(yield* stream(sessionID, { includeHidden: true }))` + `})`。
- **schema 迁移**：CompactionPart Schema.Struct 修改落 packages/schema/src/v1/session.ts（目标 schema 真实 owner）。
- **测试**：stale 适配已用 stub.llmLayer/MessageV2.stream（Effect）等正确形态，插入锚 "falls back to full summary" 逐字匹配。

## E/C
- E = 281
- C = 44（#256 中文契约注释：常量三联语义、memento 增量无下限、倒序收集、synthetic/ignored 排除、二分对重放信封估算、visibleHistory 同源、双锚点持久化、信封稳定性、测试场景意图等——分布 5 文件）
- need = ceil(281 * 0.15) = 43 → 余 +1 PASS

## Source
- original/0256-4ed5d29c6bfd.patch: 413 行 5 文件。

## Current
- current/0256-4ed5d29c6bfd.patch: 19425+ bytes, 9 hunk / 5 文件
- SHA-256: 2f6845eaef631fe90efe3920a18fcc7244bf0adcd80781bd0ba4a48238281d99

## 安装
- materialize 256 --rebuild: passed 256/256（全量重放）

## 测试
- typecheck 256: 全部 workspace passed
- 3 个新 memento 测试随 compaction.test.ts 落地（materialize 通过即 apply 成功；行为测试在本项 typecheck 后由批次末统一验证——keeps recent/abort timing 为 0251 已论证的 pre-existing）

## 修订历史
1. stale 适配版首次 materialize 失败：hunk1 preimage 期待 #255 后状态（当时 #255 未落地）+ message-v2 hunk2 锚点顺序颠倒。
2. 修复 255（常量注释措辞对齐 stale 0256 期望、注释移至 DEFAULT 之后）→ 255 落地。
3. hunk1 preimage 经 255 落地自动成立；message-v2 hunk2 重锚（filterCompactedEffect 后 ctx 修正为 `return filterCompacted...` + `})`——stale 尾 ctx 的 #204 两行在目标位于 filterCompacted 内部 L579-580）。
4. C 分两轮补齐 27→41→44（wave 插入曾破坏 hunk 计数，用 Edit 工具 + sync 流程修复）。
5. 最终：全量重放 passed 256/256、typecheck OK、C=44 ≥ 43 → PASS。

## 三方比较
- Source：memento 通道（常量/函数/process 双锚点/schema/SDK/重放/测试）。
- Upstream v1.17.20：schema 已迁 packages/schema、message-v2 函数顺序颠倒、CompactionPart 谱系一致。
- Target：语义原样落地 + 锚点适配 + schema owner 迁移。无行为丢失、无重复 owner（filterCompacted 与 memento 通道职责正交）。

## 最终行为
- 压缩时在 tail 之外新增「最近用户指令 memento」：预算 min(20K, usable×20%)，倒序保留用户手写文本，超预算二分截断（标记 "...[truncated for compaction memento]"）。
- memento 随 compaction part 持久化，重放时以 XML 信封注入、"What did we do so far?" 之前。
- synthetic/ignored 文本被排除；SDK 类型同步。
