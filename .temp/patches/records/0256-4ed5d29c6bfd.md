# Record 0256-4ed5d29c6bfd — feat(compaction): 最近用户指令保留（memento）

## Manifest
- index: 256, parent: 0255-9d86acdde912, sha: 4ed5d29c6bfd9d6400ce6842b8533f3311d60922
- source: SMARK2022 <3222301373@qq.com> 2026-05-29T15:04:46+08:00
- subject: feat(compaction): 添加最近用户指令的保留逻辑，优化压缩上下文处理
- original: original/0256-4ed5d29c6bfd.patch（19082B，4 文件）
- current: current/0256-4ed5d29c6bfd.patch（final）

## 状态起点与问题
- current 256 原为 original 的零适配拷贝（19082B 与 original 逐字节一致，含英文注释），materialize 256 失败：
  1. compaction.ts 常量 hunk context 失配——目标 255 状态在 DEFAULT_TAIL_TURNS 前有 #255 注释行，original context 无
  2. message-v2.ts CompactionPart schema hunk 失配——original 在 message-v2.ts L235 定义字段，目标 CompactionPart 定义在 packages/schema/src/v1/session.ts L207-214（owner 迁移）
  3. compaction.test.ts / types.gen.ts hunk 原可 apply（offset -26/-80）但测试用 stub.layer（目标为 stub.llmLayer）、stream 直接传（目标为 Effect 需 yield*）
- 结论：本项为"original 零适配"典型，需全文适配。

## Source 行为（original 全文 413 行，4 文件 9 hunk）
1. **compaction.ts 常量**（hunk1）：DEFAULT_TAIL_TURNS/MIN/MAX 后新增
   - DEFAULT_PRESERVE_RECENT_USER_TOKENS = 20_000（Codex memento 绝对上限）
   - PRESERVE_RECENT_USER_RATIO = 0.2（预算 = usable 的 20%，与整轮 tail 的 25% 独立）
   - RECENT_USER_MEMENTO_TRUNCATED = "...[truncated for compaction memento]"（截断标记）
2. **compaction.ts 3 函数**（hunk2，preserveRecentBudget 后）：
   - preserveRecentUserBudget({cfg, model, outputTokenMax})：min(20K, max(0, floor(usable*0.2)))——无下限（memento 是 summary+tail 的增量，小模型允许缩到 0 防再溢出）
   - collectRecentUserMessages({messages, maxTokens})：newest→oldest 反向遍历 user 消息（跳过 compaction marker 消息）；只收集显式用户文本（排除 synthetic/ignored part）；按 formatRecentUserMemento 封装的 token 估算逐条加入；超预算时截断该条并 break
   - truncateRecentUserMessage({id, text, result, maxTokens})：二分搜索截断点——对同一格式化信封做 token 估算，best 满足预算的最大前缀 + 截断标记
3. **compaction.ts layer**（hunk3）：select 前 visibleHistory = history.filter(!hidden)；recentUserMessages = collectRecentUserMessages({messages: visibleHistory, maxTokens: preserveRecentUserBudget({cfg, model, outputTokenMax: flags.outputTokenMax})})；select({messages: visibleHistory})（替换原内联 history.filter）
4. **compaction.ts layer**（hunk4）：tail_start_id 更新改双锚点：
   - nextRecentUserMessages = recentUserMessages.length > 0 ? recentUserMessages : undefined
   - 条件：compactionPart && (tail_start_id 变化 || JSON(recent_user_messages) 变化)
   - 更新写入 tail_start_id + recent_user_messages
5. **message-v2.ts schema**（hunk5）：CompactionPart 加 recent_user_messages: optional(Array(Struct({id: MessageID, text: String, truncated: optional(Boolean)})))
6. **message-v2.ts formatRecentUserMemento**（hunk6，WithParts 后）：RECENT_USER_MEMENTO_INTRO 常量 + formatRecentUserMemento(messages)：空返回 ""；否则 "<recent-user-messages>" + intro + <message index id truncated> text </message> 每项 + 闭合
7. **message-v2.ts toModelMessagesEffect**（hunk7，compaction 分支）：push "What did we do so far?" 前先 push recentUserMemento（formatRecentUserMemento(part.recent_user_messages ?? [])，非空才 push）——memento 在旧 summary prompt 前，新模型先看到最新用户意图
8. **compaction.test.ts**（hunk8，+169 行 3 测试）：
   - "preserves recent user instructions in compacted context when the full tail is not retained"：2 user + assistant + summary compaction；断言含 <recent-user-messages>/latest instruction/"What did we do so far?" 且 instruction 在 prompt 前；cfg tail_turns:1 preserve_recent_tokens:20
   - "bounds overlong recent user instructions in compacted context"：100K 字符指令 + context 10K/8K 模型；断言含 start/truncated 标记、不含 end、memento estimateText ≤ 400
   - "keeps synthetic and ignored user text out of recent user mementos"：synthetic part + ignored part + 真实指令；断言只含 real，不含 synthetic/ignored 文本
9. **types.gen.ts**（hunk9，L702）：CompactionPart 加 recent_user_messages?: Array<{id, text, truncated?}>

## 目标适配（编辑 0255 状态 → git diff 生成 current）
1. compaction.ts 常量：插在 MAX_PRESERVE_RECENT_TOKENS 后 type Turn 前（#255 注释行纳入 context）
2. 3 函数插在 preserveRecentBudget 后
3. layer：visibleHistory/recentUserMessages 替换 select messages（flags.outputTokenMax 可用——L184 flags 已解析）
4. 双锚点更新
5. schema：packages/schema/src/v1/session.ts L212 tail_start_id 后加字段（owner 迁移——目标 CompactionPart 唯一定义处；core/v1/session.ts re-export 自动传播类型，opencode typecheck 验证）
6. formatRecentUserMemento 插 message-v2.ts filterCompacted 前（L604）——目标无本地 WithParts 定义
7. toModelMessagesEffect compaction 分支（L258-263）
8. 测试 3 个插 falls-back 测试前：stub.layer → stub.llmLayer（目标 llm() 返回 {push, llmLayer}）；**MessageV2.stream(session.id) → yield* MessageV2.stream(session.id)**（目标 stream 返回 Effect<WithParts[]>，source 为同步数组）
9. types.gen.ts L617-625 CompactionPart 加字段（0196 直改先例）

## 修订历程（本项多次失败）
- v1（original 拷贝）：materialize 失败——compaction.ts/message-v2.ts context 失配
- v2（状态编辑生成）：comma 笔误（双锚点条件尾部 `),` 多余逗号）→ tsgo 语法错误
- v3：message-v2 join 的 `"\n"` 在插入脚本中被写成真实换行（`.join("` 跨行）→ "Unterminated string literal"；修复后 hunk header 行数 28→27 未同步 → "corrupt patch"
- v4：测试 hunk 177→176 header 修复 → apply 成功
- v5：测试运行 TypeError（filterCompacted 收到 undefined）——stream 需 yield*；3 处修复 → 3 测试 pass（5+5+4 expect）
- v6：E/C 不足（C=28 need=39）→ 补 11 条 #256 中文注释（分散于测试意图/预算语义/schema 字段/生成文件）；注释首轮插入漏 + 前缀 → 修复 9+2 条；hunk header recount（用 python 精确重算 + check256hunks.py 验证 all OK）
- final：E=260, C=39, need=39 PASS

## E/C（count-ec.py 规则独立复算）
- E=260, C=39, need=39, PASS
- 注释分布：#256 常量区（整轮尾部之外的 memento 通道、20K/0.2 预算、截断标记模型可见性）、预算无下限理由、反向收集顺序、synthetic/ignored 排除、二分搜索同一信封、visibleHistory 共用、双锚点更新、memento 前置 prompt、信封稳定性、truncated 持久化、生成文件同步、测试意图 6 条

## 验证
- materialize 256: passed 256/256（多次重跑确认）
- 3 新测试：preserves 5 expect / bounds 5 expect / keeps synthetic 4 expect 全 pass
- 全文件 60 测试：58 pass + 1 skip + 1 pre-existing fail（retry-backoff Windows 计时，与 240 相同，非本项引入）
- typecheck：opencode exit 0、schema exit 0、sdk/js exit 0
- core/v1/session.ts re-export 验证：CompactionPart 类型自动传播（schema 定义 → core re-export → opencode 消费）
- sdk/js types.gen.ts 直改符合 0196 先例（生成文件同步 schema，无 API 端点变化无需跑 generate）

## 遗留
- 无。下一项 257（memento 从 rawHistory 采集 + 20%/20K 回调修复）待处理。

## E/C（count-ec.py）
- E=260, C=39, need=39, PASS
- SHA: d7beabeaf885

## 验证（最终）
- materialize 260: passed 260/260
- 3 新测试 pass（5+5+4 expect）
- opencode typecheck exit 0

## DUAL-AUDIT SEAL（批次 256-260，第四轮）
- 正向审计：ses_01b54d401ffemp5AR8CXdwm7Os — 5/5 PASS，无 B/G
- 逆向审计：ses_01b54b279ffeW0pzFx4AcCOlO2 — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前三轮 BLOCK 修复：(1) 259 B-01 删除测试→恢复 10 测试；(2) 259 B-01 测试敏感性→printDaemonExitHint 导出+直接调用；(3) 260 G-01 record 过短→83 行/3218 字符；(4) 257 G-01 record 字符不足→60 行/3308 字符
- materialize 260: passed 260/260
- opencode/tui/schema typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
