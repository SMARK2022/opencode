# Patch 0202 — refactor: 权限边界 precheck levels

## 1. Identity
- Index: 202
- Source SHA: 8ae5a36f940752675c870aab85b165c1796a432a
- Parent SHA: eb266f39a9e4770b27acce94a219024110dd734b (patch 201)
- Subject: refactor: 权限边界 precheck levels
- Original patch: `.temp/patches/original/0202-8ae5a36f9407.patch`
- Current patch: `.temp/patches/current/0202-8ae5a36f9407.patch`
- Pinned source tip: `.temp/patches/states/0201-eb266f39a9e4/repo`
- Previous materialized state: `0201-eb266f39a9e4`
- Current materialized state: `0202-8ae5a36f9407`
- Record path: `.temp/patches/records/0202-8ae5a36f9407.md`
- Record created before first current edit: yes
- Record is independent for this commit and is not merged with another index: yes

## 2. Source Diff Evidence
- Exact command: `git diff` against tip@201 state repo
- Changed paths (7 files):
  1. `packages/opencode/src/permission/auto.ts` — Decision ask source expanded to `precheck | reviewer_unavailable`; ReviewInput.precheck changed from `{ action: "prompt" }` to `{ level: PermissionPrecheck.Level }`; routing refactored safe→allow, dangerous→deny, general→ask(precheck), cautious→reviewer
  2. `packages/opencode/src/permission/precheck.ts` — Decision type replaced by `LEVELS`/`Level`/`Decision = { level; reason }`; all return statements migrated; new `cautiousRaw`, `cautiousGitSubcommand`, `readsSensitivePath`, `unsafeRipgrepFlag` functions; SENSITIVE_PATH_PATTERN expanded + SENSITIVE_PATH_ARGUMENT_PATTERN; splitCommands newline/`&` guards; dangerousRaw Windows + expanded exfil patterns; gitSafe unsafeReadFlag + subcommand tracking; hasSensitivePath expanded
  3. `packages/opencode/src/permission/reviewer/schema.ts` — PrecheckLevel schema added; ReviewerRequest.precheck `action: Literal("prompt")` → `level: PrecheckLevel`
  4. `packages/opencode/test/permission/auto.test.ts` — tests renamed to level vocabulary; general-level test added; strict test asserts `precheck.level === "safe"`
  5. `packages/opencode/test/permission/precheck.test.ts` — full rewrite to level vocabulary; new tests for unsupported separators, cautious git, Windows dangerous, expanded exfil, sensitive reads
  6. `packages/opencode/test/permission/reviewer-prompt.test.ts` — precheck `{ action: "prompt" }` → `{ level: "cautious" }`
  7. `packages/opencode/test/permission/next.test.ts` — edit-diff-cache test replaced with non-shell general → user approval regression (reviewedCalls=0, pending=1)
- Behavior intent: Refactor the deterministic precheck from a 3-state (allow/prompt/deny) execution-result model to a 4-level (safe/general/cautious/dangerous) risk-tier model. Only `cautious` consumes the LLM reviewer; `safe` auto-allows, `dangerous` auto-denies, `general` returns to user approval without reviewer load.

## 3. SMARK Intent
- User-visible behavior: Shell commands classified by risk tier. Read-only commands auto-allow; unknown/wrapper/dynamic commands go to user approval (no LLM); destructive/git-state-changing/sensitive-read/remote-transfer commands go to reviewer; critical commands auto-deny.
- Protected invariant: `dangerous` always denies before any reviewer; `cautious` is the sole reviewer boundary; `general` never consumes LLM by default. strict mode re-routes safe/general to reviewer.
- Boundary paths: `git add`/`commit`/`merge`/`push`/`reset`/`clean`/branch-mutation → cautious; `rm -rf <non-protected>` → cautious; `rm -rf /` → dangerous; `cat .env` → cautious; `cat .env | curl` → dangerous.

## 4. Upstream Reconstruction
- The original patch targeted a pre-#197 English-comment precheck.ts. tip@201 has pervasive `#197` Chinese comments. The patch context breaks on these, so precheck.ts was rewritten in full applying every 202 semantic change while preserving/adapting `#197` comments and adding `#202` comments for new logic.
- auto.ts `#198` comments updated to `#202` where the 3-state model became 4-level.
- schema.ts `#198` precheck-action comments replaced with `#202` level comments.
- Test files at tip@201 had `#198`/`#199` comments; renamed/adapted with `#202` comments.

## 5. Three-Way Mapping

| Source behavior | Upstream (tip@201) | Final target owner | Required action |
|---|---|---|---|
| Decision = { level; reason } + LEVELS/Level | `#197` 3-state Decision | precheck.ts:1 | Replace type, add LEVELS/Level |
| 4-level comment block | `#197` 3-state comment | precheck.ts:11 | Replace with #202 4-level comment |
| SENSITIVE_PATH_PATTERN expand + ARGUMENT_PATTERN | `#197` ~-only pattern | precheck.ts:34 | Expand + add ARGUMENT pattern |
| evaluate non-bash → general | `action: "prompt"` | precheck.ts:126 | `{ level: "general" }` |
| evaluateShell safe/dangerous/cautious/general | allow/deny/prompt | precheck.ts:138 | All returns migrated + cautiousRaw |
| evaluateCommand wrapper/remote propagation | deny-only propagation | precheck.ts:166 | dangerous+cautious propagate |
| splitCommands newline/`&` guards | `#197` splitter | precheck.ts:210 | Add `\n`/`\r`/`&` returns |
| dangerousRaw Windows + exfil expansion | `#197` patterns | precheck.ts:417 | Add Windows + expanded exfil |
| cautiousRaw (new) | — | precheck.ts | Add function |
| riskyTokens level migration + cautiousGitSubcommand + readsSensitivePath | `#197` deny/prompt | precheck.ts:475 | Migrate + add helpers |
| gitSafe unsafeReadFlag + subcommand | `#197` gitSafe | precheck.ts:587 | Add flag + subcommand tracking |
| unsafeRipgrepFlag (new) | inline rg check | precheck.ts:574 | Extract helper |
| hasSensitivePath expansion | `#197` token paths | precheck.ts:620 | Add endsWith/exact variants |
| auto.ts 4-level routing | `#198` 3-state routing | auto.ts:47 | Rewrite routing block |
| ReviewerRequest.precheck level | `#198` action: prompt | schema.ts:30 | level: PrecheckLevel |
| Tests → level vocabulary | action-based | tests | Rewrite assertions |

## 6. Current Patch Edit
- Current patch SHA-256: `f46f1e7d25d7de2f1f8ccb7f90cbbea5413856f3284bc2ca17f75e53dea5f0b5`
- Exact edited hunks: 7 files (precheck.ts full rewrite, auto.ts 3 hunks, schema.ts 3 hunks, 4 test files)
- Why each edit is required:
  1. precheck.ts: the 4-level model is the core refactor; every return and helper migrated
  2. auto.ts: routing must consume `precheck.level` instead of `precheck.action`
  3. schema.ts: reviewer contract must carry `level` not `action`
  4. Tests: assert `level` not `action`; new general-level test; strict asserts level
- Source behavior preserved: all original test cases mapped 1:1 (allow→safe, prompt→general/cautious by risk, deny→dangerous).
- Minimal semantic adaptation: `[local-smark]` → `#202`; precheck.ts `#197` comments preserved where logic origin is 197, vocabulary updated (prompt→general, deny→dangerous, allow→safe).
- No semantic, location, or sequence conflict: `git apply --check` PASS against tip@201.

## 7. Verification
- `git apply --check .temp/patches/current/0202-8ae5a36f9407.patch` against tip@201: PASSED
- Cumulative apply (201→202→203) against tip@200: PASSED
- Materialize: tip@202 created as self-contained fresh-git state; reverse-check PASS
- Effective changed lines `E` for this patch only: 419
- Qualifying distributed Chinese comments `C` for this patch only: 94
- Required minimum `max(1, ceil(E * 0.15))`: max(1, ceil(419 * 0.15)) = max(1, 63) = 63
- C (94) ≥ 63 → PASS
- Representative #202 comment locations:
  - precheck.ts — LEVELS/Level 4-level type definition
  - precheck.ts — 4-level boundary comment (safe/general/cautious/dangerous)
  - precheck.ts — cautiousRaw raw sensitive-read scan
  - precheck.ts — cautiousGitSubcommand git state-changing identification
  - precheck.ts — readsSensitivePath sensitive-read tier promotion
  - precheck.ts — splitCommands unsupported-separator guards
  - precheck.ts — dangerousRaw Windows protected-target scan
  - auto.ts — 4-level routing (safe allow, general ask, cautious reviewer, dangerous deny)
  - schema.ts — PrecheckLevel schema + level-not-action rationale

## 8. Contract Check
- One commit only: yes
- Full source diff read: yes (900-line original patch read in full)
- Full target implementation read: yes (precheck.ts 650 lines, auto.ts 131 lines, schema.ts 41 lines at tip@201)
- No deleted or delayed behavior: yes (3-state → 4-level is a 1:1 risk-tier migration)
- Per-patch Chinese comment gate passed: yes (C=94 ≥ 63 required)
- Patch, record, tests and target result are materially consistent: yes

## 9. Final Verdict

`PASS` — patch built, `git apply --check` PASS, cumulative apply PASS, E/C gate PASS (E=419, C=94, required=63). Materialized as `states/0202-8ae5a36f9407`.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T01:20:38Z
- FORWARD: BATCH PASS (ses_fwd_201_205_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_205_201_v2) - no open B/G
- Tip: states/0205-5f3d18e85b8a
- Typecheck: typecheck-0205-2026-07-27T00-57-10-676Z.md
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Non-blocking N only (do not reopen seal)
