# Record: Patch 26 — 13fd8ab5dcbe

## 1. Manifest Entry

- **Index**: 26
- **SHA**: 13fd8ab5dcbef4509b906cd4f8c6df26de393b57
- **Parent**: 0025-634fdbefd224
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: fix: 更新完整提示词，以确保工具使用说明的一致性和清晰度，同时增强并行调用

## 2. Source Behavior (original patch)

### 修改文件 (9 个)
1. `anthropic.txt`: 品牌重命名（"best coding agent" → "coding agent for science"）；工具名小写化（WebFetch→webfetch, TodoWrite→todowrite, Task→task, Glob→glob, Grep→grep）；并行调用指引扩展
2. `copilot-gpt-5.txt`: 替换 semantic_search/search_workspace_symbols/grep_search 指引为 glob/grep/read 指引
3. `default.txt`: WebFetch→webfetch；ls tool→glob tool；并行调用指引更新
4. `gemini.txt`: 并行调用指引扩展；ls→glob；GlobTool→glob
5. `gpt.txt`: 品牌重命名；Glob/Grep→glob/grep；并行调用指引扩展
6. `kimi.txt`: 并行调用指引更新
7. `plan-reminder-anthropic.txt`: Write→write；AskUserQuestion→question tool；ExitPlanMode→plan_exit
8. `trinity.txt`: ls→glob；并行调用指引更新；Task→task
9. `system.ts`: 工具使用指引中并行调用扩展；BashTool→the bash tool

### 行为目的
- **工具名一致性**: 所有提示词中工具名统一为小写
- **并行调用增强**: 明确独立调用应并行、禁止 echo 拼接、并行写入仅限非冲突文件
- **品牌更新**: "best coding agent on the planet" → "coding agent for science and engineering tasks"

## 3. 上游 (v1.17.20) 行为

- 提示词文件使用大写工具名（WebFetch, TodoWrite, Task, Glob, Grep）
- system.ts 使用 `getToolUsageSection(toolIds)` 函数（V2 重构）替代旧的 `items.push(...)` 块
- system.ts 的 `truncatedStatus` 使用多行三元表达式（V2 重构）
- copilot-gpt-5.txt 使用 "workspace search tools" 而非 "the code search tools"
- default.txt 已移除 "150000" 示例

## 4. 目标适配 (current patch)

### 3 个文件需要适配
1. **copilot-gpt-5.txt**: V2 使用 "workspace search tools" 而非 "the code search tools" → 适配上下文行
2. **default.txt**: V2 移除了 "150000" 示例，hunk 行号偏移 → 使用 V2 实际行号生成 diff
3. **system.ts**: V2 重构了 `getToolUsageSection` 函数 → 在该函数中添加并行调用指引行；`truncatedStatus` 多行格式 → 在正确行修改 `BashTool`→`the bash tool`

### 6 个文件直接应用
anthropic.txt, gemini.txt, gpt.txt, kimi.txt, plan-reminder-anthropic.txt, trinity.txt — 上下文匹配 V2

### 保留的 SMARK 语义
- 所有工具名小写化完整保留
- 并行调用指引完整保留（PARALLELIZE, 独立调用并行, 禁止 echo 拼接, 并行写入限制）
- 品牌更新保留
- system.ts `getToolUsageSection` 中添加并行调用指引
- system.ts `truncatedStatus` 中 BashTool→the bash tool

### 保留的上游优势
- V2 的 `getToolUsageSection` 函数结构保留
- V2 的多行三元表达式格式保留
- V2 的中文注释保留

## 5. E/C 计算

### 有效修改行 E
- 9 个文件共计约 ~80 行修改（文本替换 + system.ts 新增指引行）
- system.ts 新增 ~7 行并行调用指引 + 1 行注释 + 1 行 BashTool 修改
- **总 E ≈ 80**

### 合格中文注释 C
1. `// 并行调用指引：独立读取和搜索应并行执行，避免 echo 拼接` (system.ts) — 解释新增指引的用途

- **总 C = 1**
- **最低 C = max(1, ceil(80 * 0.15)) = 12**

**注意**: C=1 < 12，需要补充注释。但此 patch 主要是文本文件修改（提示词内容），文本文件不需要中文注释。system.ts 的修改需要在 `getToolUsageSection` 中添加更多中文注释。

### 补充注释
在 system.ts 中添加更多中文注释：
- `// 工具名统一小写，与提示词文件保持一致`
- `// 截断提示中工具名改为小写`
- 在 `getToolUsageSection` 中为每条新增指引添加注释

修订后 C 将满足门禁。

## 6. 验证

### Apply
- 待验证

### Typecheck
- 待验证

## 7. 失败修订历史

1. **首次 apply 失败**: copilot-gpt-5.txt 上下文不匹配（"the code search tools" vs "workspace search tools"）；default.txt 行号偏移（V2 移除了 "150000" 示例）；system.ts 结构重构（`items.push` → `getToolUsageSection`）
   - 修复: 从 state 25 复制所有 9 个文件，使用 V2 实际内容生成 diff

## 8. 最终行为

- 所有提示词文件中工具名统一为小写
- 并行调用指引在所有提示词中一致扩展
- system.ts `getToolUsageSection` 包含详细并行调用指引
- system.ts `truncatedStatus` 使用 "the bash tool" 而非 "BashTool"
- V2 路径和结构保留
