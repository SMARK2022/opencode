# Record 0242-ad4e0d983c19
## Status
PASS

## 修改内容
feat(compaction): 重构会话压缩逻辑，添加 SessionCompaction.run 方法，添加 SessionPrompt.compact 路由，移除旧 compaction task 路径。
Source 7 文件，目标适配 8 文件（Hidden schema 迁移到 schema 包）。

### 路径迁移与 Owner
- `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts` → 不变（summarize 改用 promptSvc.compact）
- `packages/opencode/src/session/compaction.ts` → 不变（新增 run + hideIncomplete，create 返回 SessionV1.User）
- `packages/opencode/src/session/message-v2.ts` → 不变（visibleCompactions 过滤未完成 marker；latest 只检测 subtask）
- `packages/opencode/src/session/prompt.ts` → 不变（新增 compact 接口；loop 移除 compaction task 分支；auto compact 改用 run）
- `packages/schema/src/v1/session.ts` → Hidden schema 的 reason 增加 "compaction-cancelled"（源在 message-v2.ts，目标迁移到 schema 包）
- 测试：compaction.test.ts（+2 新测试，2 处 filterCompacted→ssn.messages）、messages-pagination.test.ts（3 测试语义更新 + 1 新增）、prompt.test.ts（+compact BusyError 测试）

### API 差异与适配
- Source 的 compact 接口含 agent/auto/overflow 参数；目标 SummarizePayload 只有 providerID/modelID/auto——目标 compact 接口为 `{sessionID, model?}`，agent 从 lastAssistant 推断，是目标的正确适配。
- Source 用 MessageV2.* 类型，目标用 SessionV1.*（V1/V2 架构拆分）。
- Source llm() helper 返回 { push, layer }，目标返回 { push, llmLayer }。

### 测试适配
- compaction.test.ts "keeps recent pre-compaction turns" / "anchors repeated compactions"：`msgs = filterCompacted(...)` → `msgs = yield* ssn.messages(...)`（源 242 同改）。
- prompt.test.ts "compact rejects with BusyError"：新增（waitForBusy 等待 busy 状态 + 15s 超时适配 Windows 慢环境；源 3s 超时在目标环境不足）。
- prompt.test.ts "manual compaction stops after summarizing retained tool tail"（#137）：改为 `prompt.compact` 直接调用（242 移除 loop 的 marker task 分支后，create+loop 模式不再触发压缩）。
- prompt.test.ts "auto compaction resumes without compacting retained old tail usage again"（#140）：改为 `prompt.compact` + 新 user 消息恢复（保留"压缩后不重复压缩"核心语义）。
- messages-pagination.test.ts：3 个 marker 测试更新为"drops compaction marker"语义 + 新增 latest tasks 测试。

## E/C
- E = 303（按 patch 逐行统计的有效修改行，注释/空行不计）
- C = 53（#242 中文注释：run/hideIncomplete/onExit 边界、visibleCompactions/latest、compact 路由、stop-to-break 终止、ensureRunning 边界、busy 先行、auto 透传、defaultAgent 兜底、先取消息再推导、失效 agent 回退、冷启动超时放宽、测试意图）
- Need = ceil(303 * 0.15) = 46
- Status: PASS

### 物化修复记录（2026-09-06 Round 8，EPERM 冷却重试后 typecheck/测试全绿）
- 修复 1：handlers/session.ts `runState.assertNotBusy(info.sessionID)` → `info.id`（V2 SessionV1.Info 主键字段是 id）。
- 修复 2：groups/session.ts summarize endpoint error 声明补 `SessionBusyError`（manual summarize 走 compact 后真实会产生 busy 拒绝，与 prompt/abort 等写端点同声明；新增 groups/session.ts 节）。
- 修复 3：prompt.ts Interface compact 类型 `ProviderID/ModelID` → `ProviderV2.ID/ModelV2.ID`（V2 命名空间类型，两 import 已存在）。
- 修复 4：compaction.ts `MessageV2.AbortedError` → 直接 import 的 `AbortedError`（core/v1/session 错误模型；取消归属语义不变）。
- 修复 5（行为缺陷）：message-v2.ts visible 被 0242 适配替换成 visibleCompactions 时丢失 #205 hidden 剔除——hidden anchor 漏进 filterCompacted 返回窗口（messages-pagination "uses hidden retained-tail anchors" 失败实证）。改为链式组合：先剔 hidden（锚点仍参与边界遍历）再剔悬挂 compaction，两个语义共存。messages-pagination 53/53 全绿。
- E = 310，C = 60（新增：visible 链序契约注释、summarize endpoint busy 声明注释、AbortedError 引入注释）；Need = ceil(310*0.15) = 47；PASS。
- 验证：--materialize 243 passed；--typecheck 243 全部 30 workspace passed；tui locale+session-pending 23/23；messages-pagination 53/53。

### 审计修复记录（Round 7）
- N-01 收尾（逆向 Round 6）：空会话/ghost 测试 15s 墙钟预算冷启动偶发超时 → 放宽到 60s（与测试命令默认一致），两测试分别 6/6、3/3 冷启动通过。

### 审计修复记录（Round 6）
- B-01（逆向 Round 5）：compact 对失效 agent 名裸 throw defect——源 compact 从不解析 agent（摘要固定用内置 compaction agent），agent 删除/改名后 summarize 从可成功压缩退化为 HTTP 500 → 改为 `(yield* agents.get(agentID)) ?? (yield* agents.defaultAgent())`，与空会话兜底、源 handler 的 `?? defaultAgent` 一致。新增测试 "compact succeeds when the last message agent was removed from config"（ghost-agent-xyz 消息 + compact 成功）。

### 审计修复记录（Round 5）
- B-01（正向 Round 4）："hides visible compaction state when interrupted" 测试固定 1s/250ms 墙钟上限在冷启动下间歇 TimeoutError（实测 13 次 6 次失败）→ 按仓库测试规范放宽：Deferred.await(ready) 1s→10s 就绪窗口、Fiber.await 250ms→5s 中断完成等待。修复后冷启动 10/10 pass，断言语义（失败+中断原因+marker 隐藏）全部保留。

### 审计修复记录（Round 4）
- B-01（正向 Round 3）：空会话 compact 仍经 lastAssistant 抛 Impossible（defaultAgent 兜底不可达）→ compact 改为先取消息列表再推导：空会话直接落入 lastUser ?? defaultAgent 分支，不再调用会抛错的 lastAssistant。
- 新增空会话 compact 测试：compact succeeds on an empty session via defaultAgent（空会话 + llm.text 队列，断言返回 summary assistant 不抛错）。

### 审计修复记录（Round 3）
- B-01（双向）：summarize busy 顺序——assertNotBusy 提到 revertSvc.cleanup 之前（被拒请求不破坏撤销状态）。
- B-02（双向）：空会话 summarize 抛 500 → compact 用 agents.defaultAgent() 兜底。
- B-01（逆向 auto）：SummarizePayload.auto 被丢弃 → compact 接口加 auto 参数并透传到 compaction.run。

### 审计修复记录（Round 2）
- B-05/B-01（双向审计）：auto compact 两处缺 stop-to-break 检查 → 已补（压缩失败终止 loop，避免无限重试）。
- B-06/B-02（双向审计）：manual compact 未走 runner → 已改用 state.ensureRunning（busy/cancel/并发边界与正常 prompt 一致）。
- B-07（逆向审计）：无 assistant 会话抛错 → 已改为从最新 user 推断 agent，无 assistant 也可压缩；model 回退链 last assistant → agent.model → currentModel。

### 注释分布
- compaction.ts：run 一步路径、hideIncomplete 只隐藏 marker/summary 对、onExit 清理（9 条）
- message-v2.ts：先过滤未完成 marker、latest 只检测 subtask（5 条）
- prompt.ts：compact 路由、loop 移除 compaction task、auto compact 用 run（6 条）
- handler/session.ts：compact 内部 assertNotBusy（2 条）
- schema/session.ts：compaction-cancelled reason（1 条）
- compaction.test.ts：run 边界、中断清理、tasks 空断言（6 条）
- messages-pagination.test.ts：失败 summary 边界（1 条）
- prompt.test.ts：BusyError 快速失败（4 条）

## Source
- original/0242-ad4e0d983c19.patch: 7 文件（5 生产 + 3 测试）

## Current
- current/0242-ad4e0d983c19.patch: 32861 bytes, 8 entries
- SHA-256: 04b5c86d4648dc1d036259d18572e8be9857061d3015c205ad78a13f6413e666

## 安装
- Install: reused, fingerprint a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b

- compaction.test.ts: 54/55 pass（1 个 pre-existing 失败：stops quickly when aborted during retry backoff——10s retry backoff 中断时间断言，Windows 环境中断传播慢，测试体与 240 相同，242 不 touch retry 路径）
- prompt.test.ts: compact BusyError、manual compaction、auto compaction、compact succeeds on an empty session 4 个 242 测试全部 pass；6 个 shell 测试 pre-existing 失败（Windows shell 超时，测试体 240/245 相同）
- compaction.test.ts: hides-visible-compaction-state 冷启动 10/10 pass（修复前 13 次 6 次失败）；全文件 54 pass/1 skip/1 fail（唯一失败为 pre-existing retry backoff，测试体与 240 相同）
- E/C 验证：`python D:\Temp\opencode\count-ec.py` 输出 242: E=233, C=35, need=35, PASS

## 最终行为
- SessionCompaction.use.run：create + processCompaction 一步完成，中断时 hideIncomplete 清理（隐藏 marker/summary 对，assistant 标记 AbortedError）
- SessionPrompt.compact：assertNotBusy → lastAssistant 推断 agent → compaction.run → 返回 lastAssistant
- loop 移除 compaction task 分支；auto compact 改用 compaction.run（overflow 检测后直接执行）
- latest() 只检测 subtask，compaction 不再可重放
- filterCompacted 用 visibleCompactions 先过滤未完成 marker，再过滤 hidden

## RECOVERY-ERA INVESTIGATION + PLAN（2026-09-03，全链验证循环）
state-241 断链（7 文件 session 域大迁移）。脚本 --materialize 241 已通过并发布 states/0241-a254f367d936（对照基准树）。
### source 语义（compaction 编排迁移）
1. summarize 走 promptSvc.compact（assertBusy 双守卫），session.ts 删 compactSvc
2. compaction.ts：+run（直接执行 create+processCompaction，onExit 失败时 hideIncomplete 清理 scratch 标记）、create 返回 msg、Interface +run、create 返回类型 void→MessageV2.User
3. message-v2.ts：Hidden reason + "compaction-cancelled"；visibleCompactions()（dangling 标记/摘要对从 prompt replay 剔除）；latest() tasks 排除 compaction
4. prompt.ts：+compact 方法（ensureRunning 边界内直调 compaction.run）；loop 删 compaction task 分支；auto/overflow 改 compaction.run（+ stop→break）
### V2 类型差异（已实测 0241 树）
- compaction.ts Interface：model 类型 = { providerID: ProviderV2.ID; modelID: ModelV2.ID }（非 Provider.Model）；process/create 的 messages = SessionV1.WithParts[]；effect import 行 = L16 `import { Effect, Option, Layer, Context } from "effect"`（需加 Exit, Schema? 仅 Exit）
- prompt.ts 接口 L212-216：WithParts = SessionV1.WithParts（非 MessageV2.WithParts）；SessionCompaction.Service 已在 L230 yield
- session.ts：compactSvc @ L59；Summarize handler @ L299-315（#153 directory 上下文注释已在）
- message-v2.ts：Hidden 机制已由 #106 中文注释版存在（L494 区）——0242 的英文注释块需替换为对 #106 版的扩展（加 compaction-cancelled reason + 中文注释）；filterCompacted/latest 结构需对照 V2 行号
- 测试：compaction.test.ts Hunk#1 已过（import 区）；其余 hunk 与 messages-pagination.test.ts/prompt.test.ts 需按 V2 测试形态适配
### 实施顺序（下轮）
1. compaction.ts（权威生成法：copy→edit→diff --no-index）→ 2. prompt.ts → 3. session.ts → 4. message-v2.ts → 5. 三测试文件 → --materialize 243
### 迁移判定
PORT_TO_V2：compaction 直接执行架构在 V2 的 SessionCompaction owner 上重建；ProducerV2/ModelV2/SessionV1 类型体系内完成；无 V1 workaround 复制。

### 进度更新（同日晚）
- compaction.ts：**已完成**（权威生成法 c242-a/b + diff --no-index，含 Exit import/Interface+run/create 返回 SessionV1.User/hideIncomplete/run+onExit 清理/Service.of+run；中文注释 9 行分布 hideIncomplete/run/onExit 清理契约处）
- session.ts：**已完成**（权威生成法 s242-a/b；删 SessionCompaction import+compactSvc；Summarize 在 #153 store.provide/info 形态上加 assertBusy(info.sessionID) + promptSvc.compact 直调；中文注释 4 行）
- --materialize 243 确认：错误列表仅剩 message-v2.ts:69 与 prompt.ts:297
- 下轮：prompt.ts（7 hunk，Interface 212-216 SessionV1.WithParts 版、compact 插入 L1311 Impossible 后、loop compaction 分支 L1430-1445 删、auto create L1645/overflow L1710 改 run、Service 返回区加 compact）→ message-v2.ts（Hidden 英文注释块替换为 #106 中文版扩展 + visibleCompactions + latest tasks）→ 3 测试文件

### 完成记录（同日深查后续）
0242 已通过脚本链验证（passed 242/243）。最终实施（全部权威生成法 copy+edit+diff --no-index）：
- compaction.ts ✓：Exit import、Interface+run、create 返回 SessionV1.User、hideIncomplete/run+onExit（中文注释 9 行）
- session.ts ✓：删 SessionCompaction import+compactSvc、Summarize 改 promptSvc.compact + assertBusy(info.sessionID)（#153 store.provide/info 形态适配，中文注释 4 行）
- message-v2.ts ✓：visible() 改 visibleCompactions 前置、删 compaction break 行、+visibleCompactions 函数、latest 注释中文版+tasks 仅 subtask（中文注释 5 行）
- schema/v1/session.ts ✓（新增节）：Hidden reason + "compaction-cancelled"（中文注释 3 行——V2 的 Hidden Schema owner 在 schema 包而非 message-v2）
- prompt.ts ✓：Interface+compact（SessionV1.WithParts）、compact 方法（ensureRunning 边界，中文注释 5 行）、loop 删 compaction 分支、auto/overflow 改 run+stop→break（中文注释 4 行）、Service+compact
- messages-pagination.test.ts ✓：3 测试改 dangling 剔除断言 + 新增 latest.tasks 测试
- prompt.test.ts ✓：+compact BusyError 测试（Shell semantics 区前）
- compaction.test.ts：Hunk#1（run 生命周期 2 测试）保留；Hunk#2/#3（filterCompacted→ssn.messages）判定 V2 已满足移除（V2 测试已是 ssn.messages 形态，grep 实证）
- V2 Hidden Schema owner 实测在 packages/schema/src/v1/session.ts L83-88（#98/#153 中文注释版）——非 message-v2
