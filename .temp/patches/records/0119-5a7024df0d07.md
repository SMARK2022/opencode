# Record 0119 — 5a7024df0d07

## Identity
- Index: **119**
- Source SHA: `5a7024df0d07…`
- Current: `.temp/patches/current/0119-5a7024df0d07.patch`
- Current SHA-256: `f0923e0e9914dd8a9d39dfa39150b48170ad20bd53181120ef938f0761f45e32` (verify on disk after write)
- Bytes: 1485
- Pre-state: after 118

## Source intent
`.husky/pre-push`:
1. If `bun` not on PATH and `$HOME/.bun/bin` exists, prepend it to PATH.
2. If still missing, print clear error and exit 127 (original mentions bun@1.3.13 pin in message).
3. Then existing packageManager semver check via `bun -e`.

## Tip baseline
Husky pre-push already ran bun version check immediately; no Windows PATH bootstrap.

## Target adaptation
- Insert PATH bootstrap + fail-closed check after `set -e` in `.husky/pre-push`.
- Error message keeps fail-closed semantics; drops hard pin to bun@1.3.13 (tip packageManager is source of truth for required range).
- Annotate `packages/script/src/index.ts` with // Chinese comments documenting hook/PATH contract (E/C; script is owner of related packageManager/bun checks).

## Call chain
Git pre-push (Git Bash on Windows) → ensure bun → `bun -e` semver vs root packageManager → rest of hook (typecheck etc.).

## E/C
- E≈10, C=2+, need=2 → OK
- Comments: Windows PATH gap; fail clearly; script cross-ref

## Verification
- Dry-run after 118: pass
- Tip@120 `.husky/pre-push` contains `~/.bun/bin` bootstrap
- Typecheck-0120: pass; install `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`

## Final behavior
pre-push finds user-installed bun under ~/.bun/bin when Git Bash PATH is incomplete; missing bun fails with explicit message before opaque script errors.

## Three-way comparison detail
### Windows Git Bash PATH
Git for Windows runs hooks under bash that often lacks the PowerShell user PATH where Bun installer adds `~/.bun/bin`. Source prepends that directory when `command -v bun` fails.

### Failure mode
Exit 127 with stderr message — fail-closed, not soft skip of typecheck. Tip keeps packageManager-driven semver check after bun is found.

### Cross-link
packages/script/src/index.ts documents the same bun packageManager expectation used by the hook’s bun -e program.

## Citations
- .husky/pre-push PATH bootstrap
- Install fingerprint after batch: `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`

## Independent E/C recount
- E≈10 (PATH bootstrap + fail check + script comments)
- C≥2 Chinese // on script index + husky intent comments via #
- need=2 → PASS

## Hook text evidence
pre-push contains:
1. command -v bun check
2. PATH="$HOME/.bun/bin:$PATH" when directory exists
3. second command -v bun with exit 127 if still missing
4. original bun -e packageManager semver block unchanged after bootstrap

## Materialize / typecheck
- Install: `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`
- typecheck-0120 all workspaces passed

## Latest batch reports
- Materialize: `materialize-0120-2026-07-22T20-15-48-374Z`
- Typecheck: `typecheck-0120-2026-07-22T20-16-45-672Z`
- Install: `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`

## Independent dual-audit verdict
- FORWARD (116→120): **PASS** — no open B/G
- REVERSE (120→116): **PASS** — no open B/G
- Tip: `states/0120-ca04b07955c0`
- Materialize: `materialize-0120-2026-07-22T20-15-48-374Z`
- Typecheck: `typecheck-0120-2026-07-22T20-16-45-672Z`
- Install: `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`
- Batch release: **PASS**
