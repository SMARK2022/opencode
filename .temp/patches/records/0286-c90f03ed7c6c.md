# Record: 0286-c90f03ed7c6c

## 基本信息
- index: 0286
- sha12: c90f03ed7c6c
- subject: feat: 归档 archive GitHub action integration with opencode
- source date: Mon, 8 Jun 2026 16:32:13 +0800

## Source 行为重建
原始 patch 是纯 rename patch（18 个文件，similarity index 100%）：
1. 将 `packages/containers/` 下的 8 个文件重命名到 `archived/containers/`（README.md, base/Dockerfile, bun-node/Dockerfile, publish/Dockerfile, rust/Dockerfile, script/build.ts, tauri-linux/Dockerfile, tsconfig.json）
2. 将 `github/` 下的 10 个文件重命名到 `archived/github/`（.gitignore, README.md, action.yml, bun.lock, index.ts, package.json, script/publish, script/release, sst-env.d.ts, tsconfig.json）

归档的目的是将不再活跃维护的 containers 和 GitHub Action 集成代码移到 archived/ 目录，保留历史记录但不在主包路径中。

### Hunk 组 1：packages/containers/ → archived/containers/（8 文件）
- 涉及文件：README.md, base/Dockerfile, bun-node/Dockerfile, publish/Dockerfile, rust/Dockerfile, tauri-linux/Dockerfile, script/build.ts, tsconfig.json
- similarity index 100%：文件内容完全不变，git 仅记录路径迁移
- 输入：packages/containers/ 下 8 个现有文件
- 输出：archived/containers/ 下 8 个相同内容文件
- Invariant：无字节级修改，renamed 文件的 git blob hash 保持不变

### Hunk 组 2：github/ → archived/github/（10 文件）
- 涉及文件：.gitignore, README.md, action.yml, bun.lock, index.ts, package.json, script/publish, script/release, sst-env.d.ts, tsconfig.json
- similarity index 100%：同上，纯路径迁移无内容修改
- github/ 目录包含 GitHub Action 集成入口（action.yml 定义 action 元数据，index.ts 是 action 入点）
- script/publish 和 script/release 是无扩展名的发布脚本
- Invariant：无内容修改，git blob hash 一致

## 三方比较
- 目标 `packages/containers/` 存在，包含所有 8 个源文件 ✓
- 目标 `github/` 存在，包含所有 10 个源文件 ✓
- 目标 `archived/` 已存在但仅含 `github-workflows/` 子目录，`archived/containers/` 和 `archived/github/` 均不存在（目标路径空闲）✓
- 现有 `archived/github-workflows/` 与 patch 不相交，无冲突 ✓
- 纯 rename patch，无内容修改，无代码变更风险
- 目标 `packages/containers/` 8 个文件的 git blob hash 与 source pre-image 一致，rename 可直接执行
- 目标 `github/` 10 个文件的 git blob hash 与 source pre-image 一致，rename 可直接执行
- 目标 `archived/github-workflows/` 子目录与 `archived/github/` 路径不相交（不同子目录名），无覆盖风险
- 两个 rename 组之间无顺序依赖，git apply 可按任意顺序执行
- `git apply --check` 退出码 0，确认所有 18 个 rename 的源路径存在且目标路径空闲

## 适配内容
- 无适配 needed，current patch 与 original 完全相同
- `git apply --check` 退出码 0

## current 修改前后
- 编辑前 SHA-256: 9c375245ffda787003eaeb636fd84aa53078537f5e7c1954f7cba1b1780d6875
- 编辑前字节: 4073
- 编辑后 SHA-256: 9c375245ffda787003eaeb636fd84aa53078537f5e7c1954f7cba1b1780d6875（未修改）
- 编辑后字节: 4073
- materialize 286: passed 286/286

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| 18 个 rename hunks | 同 original | 纯 rename，无适配 |

## E/C 计算
- E (有效修改行): 0（纯 rename patch，无生产代码修改）
- C (中文注释行): 0
- 满足 E=0 时 C=0 ✓

## 安装与测试
- typecheck: N/A（纯 rename，无代码变更）
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)

## 不一致
- 无

## 物化验证
- 18 个文件正确从 packages/containers/ → archived/containers/ 和 github/ → archived/github/ 重命名 ✓
- archived/containers/ 包含全部 8 个文件（README.md, base/Dockerfile, bun-node/Dockerfile, publish/Dockerfile, rust/Dockerfile, script/build.ts, tauri-linux/Dockerfile, tsconfig.json）✓
- archived/github/ 包含全部 10 个文件（.gitignore, README.md, action.yml, bun.lock, index.ts, package.json, script/publish, script/release, sst-env.d.ts, tsconfig.json）✓
- 原 packages/containers/ 和 github/ 目录已清空/移除 ✓
- 文件内容未修改（similarity 100%，blob hash 不变）✓
- archived/github-workflows/ 既有内容未受影响 ✓
- materialize 286: passed 286/286 ✓

## 审计历史
- 第一轮双审计：PASS（第一轮双审计通过）（批次 286-290）

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 286-290，第一轮）
- 正向审计 (ses_0122cab55): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_0122c9807): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 290: passed 290/290
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 286=0/0, 287=0/0, 288=4/2, 289=30/5, 290=4/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#282 if(appendix) 守卫）保留为基线，SMARK exitNotice 正确叠加