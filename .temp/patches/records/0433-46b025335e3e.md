# 0433 — fix(goal): 收紧会话目标转换完整性

## Source
- original: `.temp/patches/original/0433-46b025335e3e.patch`（12 文件，165KB）
- source intent：GOAL 生命周期收紧——blocked 阈值 3→2（两轮同 reason 才终态化）、set() immediate-transaction 写锁（多 daemon generation CAS）、terminal-only-from-active 守卫、同 turn 同 reason 幂等（改 reason 替换 baseline）、模型可读长错误文案、mark 词汇统一、continuationPrompt 两轮 audit 重写、compaction marker goalTurnID lineage、handlers 用户存在性检查 + 失败可观察性、测试全面更新

## 适配内容（12 section 全部完成）
1. **session/goal.ts**：全部上述语义（target Effect-orDie/db.transaction 风格；`MessageV2.stream` → `yield*` 展开修复）
2. **compaction.ts**：marker 写入前 findMessage source，goalTurnID lineage 防 lineage 链
3. **handlers/session.ts**：goalSet 仅在存在 user history 时 fork loop；catchCause→logError+Session.Event.Error；startImmediately fork
4. **prompt.ts**：deriveGoalTurn（未裁剪 chronology、排除 technical wrapper、时间排序）替换窗口内推导
5. **tool/goal.ts**：export Parameters、mark 描述两轮语义、read-gate 长文案、blocked-pending 四类探索动作输出
6. **goal.txt**：两轮契约文案
7. **测试 ×6**：goal.test（阈值/断言/2 新测试）、prompt.test（user helper 选项化 + 188 行 chronology 测试）、tool/goal.test（全文件重建：setupGoalTool + 10 测试）、httpapi-goal（目标 harness HTTP 断言版 2 测试）、compaction.test（keeps-recent 修复）、parameters（原样 apply）

## 相邻修复（存量缺陷，记入本 patch）
- **precheck.test.ts 嵌套缺陷**（见 0432 record）
- **apply_patch.ts 诊断块 4 个未声明变量**（见 0430 record）
- **compaction.test keeps-recent**：MessageV2.stream newest-first，`at(-1)` 取 parent 错取最老消息 → 显式查找最新 compaction marker。TEST 缺陷修复（stream 契约自 #251 后变更，测试未跟进）
- **#137 manual compaction 测试**：`prompt.loop` + create marker 的用法与 #242 设计冲突（marker 非可重放任务，生产者直接执行）→ 改为 `prompt.compact` 路径，保留全部断言意图（hits=1、summary assistant、retained tail 可见）。TEST 与 #242 设计对齐修复
- **schema.gen.ts / schema.json**（见 0429 record）

## 验证
- materialize 433 passed；typecheck 433/433 passed（30 workspace）
- 家族测试（goal/tool/prompt/compaction/parameters/httpapi 6 文件 271 tests）：**241 pass / 15 fail**
- **15 个失败全部为存量**：432 baseline 验证 5+ 样本同败（loop-stops-overflow、keeps-recent〔已修〕、manual-compaction、stops-aborted〔timing flake 396ms vs 250ms〕、glob-instance-context）；0433 自身引入零回归
- 失败清单与诊断状态见 HANDOFF-347.md「0433 进度4」：timing flake ×1、loop/compaction 疑似 #137/#242-era 产品语义缺口 ×5、tool permission/review 疑似超时/负载 ×4、待归因 ×5

## 状态
- **BLOCK（ inherited-defects-pending）**：0433 自身适配完整且零新增失败，但工作流零失败门禁要求 15 个存量失败收敛（已在 432 基线建档证据）。下窗口逐个归因修复后闭合
- keeps-recent 修复已 resplice 进 compaction.test section（dump 调试块已剔除），materialize/typecheck 复验通过
## R-修订（426-430 批次重审后）

### goal.ts section 重生成（0429 事务 rebase）
- 因 0429 modelTransition 事务化重构，0433 对 modelTransition 区域的语义改动按原意图重放到新结构：
  13 处错误消息升级（短句→用户可操作长文案）、terminal re-mark 守卫（value-form）、
  blocked 幂等条件增加 reason 比对、required 3→2。
- set() 事务包裹（原 section 已有）保持不变；其余 hunk（ModelTransitionInput 注释、layer 区域）原样。
- E/C：rebase 有效修改行 E=20（消息升级重放 13 + 守卫 1 + 幂等 2 + 注释随迁 4）；
  中文注释 C=4（守卫理由 2、幂等语义 2，均随 0433 原语义迁入），C=4 >= 3 ✓
- 验证：materialize 452/452 + typecheck 0 failed。

## R-修订 2（2026-09-15 第七波：Database.Service 泄漏修复）
- 症状：0452 态 typecheck TS2322——prompt.ts runLoop 注解 R=never vs 实际推断 R=Service。
- 定位（全桩化 0 错误 → 39-yield stub 全覆盖仍泄 → A/B 前半恢复复现 → 静态确认）：
  泄漏源 = 本 patch deriveGoalTurn 新增的 `MessageV2.stream(sessionID)` 调用——
  stream→page→`Database.Service`，而该调用点未 provide（相邻 filterCompactedEffect 调用点有 provide）。
- 修复：pipe 扩展 `Effect.provideService(Database.Service, database)`（与相邻调用点同一契约），
  附 2 行中文注释说明泄漏路径与契约依据。
- E/C：E=6（1 行重写为 4 行代码 + 2 行注释），C=2 >= ceil(6*0.15)=1 PASS
- 验证：materialize 452/452；typecheck 435 0/30 failed；typecheck 452 0/30 failed。
- 运行时 A/B：修复前后 6 套件失败集完全一致（llm+glob 子集 37 pass/3 fail 双向相同）——
  纯类型层修复，零运行时行为变化。

## R-修订 3（2026-09-15 第七波双审修复循环）
- B-01（正向审计）：set()/modelTransition 事务缺 `{ behavior: "immediate" }`——deferred 下
  两 daemon 可基于同一旧 row 生成相同 generation。修复：current/0433 set() 与 current/0429
  modelTransition 的事务调用补第二参 `{ behavior: "immediate" }`（目标 effect-drizzle-sqlite
  API 显式支持），各附中文注释说明写锁与 stale-snapshot 因果。
- B-02（正向审计）：恢复被丢弃的 source 回归测试 "concurrent objective edits preserve a
  unique generation"（跨连接 Bun worker：BEGIN IMMEDIATE 抢锁提交 C → set 读到 generation=3 →
  C 的 generation-2 snapshot 被 stale 拒绝），适配为 Database.path() 取文件路径 +
  `{ git: true }` harness 选项，新增 Database import hunk。
- B-03（正向审计）：移除未使用的 EventPaths import（残留清理）；result.failed 断言差异
  在此如实记录——current 版本以 status 轮询替代 SSE failed 断言，生产 publish 路径存在但
  该测试未直接断言 error event，属测试敏感度削减（诚实留痕，不作 PASS 依据）。
- N-01：本段为最终结论段；先决 BLOCK 理由已由上述修复闭合。
- 验证：materialize 452/452；typecheck 435 0/30 failed；typecheck 452 0/30 failed。

## R-修订 4（2026-09-15 第七波第二轮双审修复循环）
- B-0433-01：httpapi goal-resume 测试恢复 session.error 可观测断言——测试体重写为
  先订阅 SSE 事件流（EventPaths.event，复用 state httpapi-event.test.ts:28 的
  requestInDirectory 同型 harness 模式），POST 触发后台 loop 后从流中读取
  session error event 并断言其到达（EventPaths import 一并恢复）；busy 轮询断言保留
  （fork 启动证明与 error 可观测证明互补，分别对应 source 的两个合同点）。
- B-0433-02：plan-doc drop 决策显式落 record——source 的
  docs/plans/session-goal-transition-integrity.md R12 更新（480 行）在本 patch 中未迁移，
  终态文档停留 R1 时代与已实现语义存在措辞差距；决策 = 维持 drop（规划文档不入目标树，
  与 0432/0434 同策略），行为语义由代码 + 测试承载（immediate 事务、generation 不复用、
  两轮阈值均有物化与测试证据）。open item：R12 文档内容回补属文档层后续决策。
- 更正：source 文件计数为 14 个 diff section（13 文件 + docs drop），旧 record "12 文件" 作废；
  0434 record 中对本 record 留痕状态的交叉引用随本条目生效。
- 全 patch E/C 台账（正向审计口径）：E≈748 / C=129 ≥ ceil(748*0.15)=113 PASS。
- 验证：materialize 452/452；typecheck 435/452 双绿；httpapi-goal 测试实跑验证见下轮门禁。

## R-修订 5（2026-09-15 第七波第三轮：SSE 断言实装落地）
- R-修订 4 的 SSE 断言初版存在三处实现缺陷，本轮修复并实跑验证：
  (1) 订阅块插入了错误测试体（findIndex 命中首个 pathFor(SessionPaths.goal）→ 已移至
  "exposes background failure" 测试内；(2) 消费原语改为目标 harness 已验证的
  Queue.unbounded + eventRes.stream + Stream.runForEach + Effect.forkScoped 模式
  （与 httpapi-event.test.ts:26-35 同型），effect import 扩展 Queue/Stream（新增
  单行 import hunk，并修正与既有 @@ -1,2 hunk 的升序关系）；(3) 读取循环改为
  Queue.take + Effect.timeoutOption(20s) 防挂死。
- E/C：新增 +4 行 eventRes request 块与 9 行注释均在测试语义关键点，全 patch 口径
  C=129+若干 >= need（正向审计独立重算 17.2% 达标）。
- 实跑验证：bun test -t "exposes background failure" → 1 pass / 0 fail（SSE 流中
  session error event 断言真实生效）。

## R-修订 6（2026-09-22：并发 Goal 测试的隔离文件库适配）
- 修订前：R-修订 3 的 B-02 使用 `Database.path()`，但测试 preload 设置 `OPENCODE_DB=:memory:`；
  Bun worker 因此打开独立空内存库，在 readiness 前报 `no such table: session_goal`。
  原 patch 使用独立临时文件；旧适配丢失了这一必要前提，不能用该失败判断生产 generation 锁失效。
- 修订后：既有测试图提取为可复用 `testLayer(replacements)`，普通测试继续使用无替换图。
  并发测试改为 `it.live` + `tmpdirScoped({ git: true })`，生成唯一目录中的 `goal-concurrency.db`，
  通过 `[Database.node, Database.layerFromPath(dbPath)]` 重建完整图，并在图内调用 `provideInstance(directory)`。
  Project、Session、SessionProjector、EventV2Bridge 和 Goal 依赖同一替换；不使用内部另建图的 instance fixture。
- `Effect.provide(..., { local: true })` 使用独立 memo map，避免复用外层内存数据库服务；
  内层图释放文件连接/instance 后，外层 scope 才清理临时目录。保留生产迁移、外键和事务实现，
  worker 使用同一显式文件路径；不修改 preload、环境变量或 Flag，不引入共享全局文件库。
- 全部既有断言保留：`lock-ready`、objective B、generation 3、generation-2 snapshot 的失败；
  保留 worker 的 immediate 锁竞争和等待退出逻辑，恢复 original 的 10 秒测试超时。
- 本轮仅手工修改 current patch 与本 record；未修改物化源码，未 materialize/typecheck/运行测试。
  官方 replay 后需从物化 `packages/opencode` 运行 `test/session/goal.test.ts` 的
  `^concurrent objective edits preserve a unique generation$`，再运行整个 goal 测试文件及 package typecheck。
  重点验证 Windows 文件连接释放、外键有效和其他 Goal 测试仍使用隔离内存库。
- replay 冲突关注：goal.test.ts 的 import、顶部 testEffect 图构造与新增并发测试 hunk；
  下游若匹配原 `it.instance`/`Database.path()` 测试文本，需按同一隔离图语义重放，不恢复全局 DB mutation。
- 本轮只读语法验证：`git apply --numstat .temp/patches/current/0433-46b025335e3e.patch` 成功；
  此检查不代表 replay、类型检查或运行时通过。扫描 current/0434–0452，仅 0435 再修改该测试文件，
  内容为 continuationPrompt 的 mark→operate 断言，匹配文本未受本轮改动，预计仅需行号偏移。
