# 0247-9c4cf37be9e4 record — feat(prompt): 更新 promptOffsetWidth 函数注释并在多个地方应用

## Manifest
- index 247, sha `9c4cf37be9e45c174c5b6bacb900a7bfaaa0c225`, parent `9711fd18599a` (0246)
- subject: feat(prompt): 更新 promptOffsetWidth 函数注释并在多个地方应用，确保文本处理逻辑准确

## Original 身份
- original/0247-9c4cf37be9e4.patch: 14843 字节, 5 entries（prompt-display.ts、tui component/prompt/index.tsx、prompt-submit-transport.test.tsx）

## Source 行为（逐 hunk）
1. **prompt-display.ts**：`promptOffsetWidth` 私有 → `export`，补中文注释（Textarea offset 不是 JS 字符索引：换行占 1 位置、宽字符按终端宽度）。
2. **prompt/index.tsx**：
   - 2a: import promptOffsetWidth
   - 2b: editor 提交路径 `newStart + virtualText.length`（JS 索引）→ `promptOffsetWidth(content.slice(0, newStart))` + `promptOffsetWidth(virtualText)`（Textarea offset 口径）
   - 2c: `input.cursorOffset = Bun.stringWidth(content)` → `promptOffsetWidth(content)`
   - 3: 重写 `syncExtmarksWithPromptParts`——弃用 produce draft 直接同步，改为基于 `input.plainText` 重新定位每个 virtualText（indexOf 循环 + 最小距离 + 去重叠），重建 parts 后 `setStore` + `restoreExtmarksFromParts`，防止 OpenTUI 按 JS 长度移动 extmark 导致宽字符前插坐标漂移
   - 4/5: pasteText/pasteAttachment `Bun.stringWidth` → `promptOffsetWidth`
3. **测试**：2 个渲染帧测试（bracketed paste 摘要、[Image 1] 占位符，中文宽字符前插后高亮仍正确）+ waitForSpanLine/colorKey helpers + withPrompt run 回调加 app 参数

## 目标适配（Round 1）
- **路径迁移**：`packages/opencode/src/cli/cmd/tui/...` → `packages/tui/...`；prompt-display.ts → `packages/tui/src/prompt/display.ts`（函数已是 export，只补中文注释）；测试 → `packages/tui/test/cli/cmd/tui/prompt-submit-transport.test.tsx`。
- **hunk 2a**：import 已存在（`../../prompt/display`）→ 跳过。
- **hunk 2b**：目标变量是 `normalized`（normalizePromptContent 结果）而非 `content` → `promptOffsetWidth(normalized.slice(0, newStart))`。目标该段含空行分隔（file/agent 分支间），按目标文本锚定。
- **hunk 2c**：目标 `Bun.stringWidth(normalized)` → `promptOffsetWidth(normalized)`。
- **hunk 3**：目标 syncExtmarksWithPromptParts 与 source 删除段一致（含空行差异）→ 按 source 新版重写（保留 #247 中文注释）。依赖（input.plainText、store.extmarkToPartIndex、PromptInfo["parts"]、setStore("prompt","parts")、restoreExtmarksFromParts）目标均存在。
- **hunk 4/5**：目标 pasteText/pasteAttachment **已使用 promptOffsetWidth**（前序已实现）→ 语义已完成，跳过（record 注明）。
- **测试 hunk**：目标 withPrompt 是四参 `(override, run, state, options)`——保持目标签名，run 回调加 `app` 第三参，`await run(prompt, route, app)`；新增测试用 `tmp.path` 第三参（替代 source 的 `Global.Path.state`，目标测试不用 Global）；waitForSpanLine/colorKey 照搬（加 #247 注释）。

## 生成方式
- gen-247.py：复制 246 状态 repo（含 .git）→ 修改 3 文件 → git diff。
- current/0247-9c4cf37be9e4.patch: 12913 字节, 3 entries, SHA-256: 26da6dee5e98f95130bcbbbf750d41f73d4e873c14825ce6ee0e8929f63151b3

## 验证
- apply-cumulative --materialize 247: passed 247/247
- tui typecheck: tsgo --noEmit 通过
- prompt-submit-transport.test.tsx: 8/8 pass（含 2 个新测试 6 expect）

## Pre-existing 问题记录
- **packages/app typecheck 失败（TS1128 custom-elements.d.ts）**：经 baseline clone（v1.17.20 原始 HEAD 4473fc3c9）独立验证，**baseline 全量编译同样失败**——app/src/custom-elements.d.ts 内容为裸路径字符串 `../../ui/src/custom-elements.d.ts`（非法 TS 语句），upstream pre-existing 缺陷。245 批次 typecheck 通过是 tsgo 增量缓存掩盖（app 无变化跳过重建）；本批次 overlay 状态变更 mtime 触发全量重建暴露。非本 patch 引入，不构成回归。脚本 typecheck 首错停止于 packages/app；其余 workspace 手动验证通过（tui 通过）。

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0247-9c4cf37be9e4

## E/C 最终（count-ec.py）
- E=129, C=20, need=20, PASS（注释：#247 前缀 14 条 + display grapheme 口径 + 2b normalized 索引同源 + 2c cursorOffset + hunk3 去重叠/restore + waitForSpanLine 整行匹配）

## 验证（最终）
- apply-cumulative --materialize 250: passed 250/250（累计）
- prompt-submit-transport.test.tsx 8 pass
- opencode/tui typecheck 通过
