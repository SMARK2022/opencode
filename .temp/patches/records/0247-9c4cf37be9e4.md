# Record 0247-9c4cf37be9e4

## Status
PASS

## 修改内容
feat(prompt): 更新 promptOffsetWidth 函数注释并在多个地方应用，确保文本处理逻辑准确。

### Source 逐 hunk 行为（original 事实源）
- H1 display.ts：`function` → `export function` + 中文注释（Textarea offset ≠ JS 索引口径）。
- H2 import promptOffsetWidth 到 prompt 组件。
- H3-H5 /editor 返回回调：`newEnd = newStart + virtualText.length`（JS 索引）→ `start/end` 用 promptOffsetWidth 转换；file/agent 两分支 source offsets 落地。
- H6 `input.cursorOffset = Bun.stringWidth(content)` → promptOffsetWidth（换行按 1 计）。
- H7 syncExtmarksWithPromptParts 重写：不再信任 extmark 坐标（OpenTUI 按 JS 长度移动、宽字符前插后漂移），以 input.plainText 中重新 indexOf + promptOffsetWidth 重建 span，选距 extmark.start 最近且不重叠的候选，找不到丢弃；setStore 后 restoreExtmarksFromParts。
- H8/H9 pasteText/pasteAttachment promptOffsetWidth。
- H10/H11 测试：2 个宽字符前插高亮测试 + waitForSpanLine/colorKey helpers（依赖 captureSpans/pasteBracketedText/typeText）。

### 目标吸收情况（0244 树）
- H1 行为已吸收（packages/tui/src/prompt/display.ts L3 已 export，函数体一致）→ 仅补口径注释。
- H2 已吸收（组件已有相对路径 import，不经 shim）。
- H8/H9 已吸收（L1248/L1334 已是 promptOffsetWidth）。
- H3-H6/H7 未落 → 本 patch 落地（packages/tui/src/component/prompt/index.tsx，变量名 content→normalized 适配）。

### H7 重写依赖核验
store.extmarkToPartIndex（L683）、restoreExtmarksFromParts（L733）、PromptInfo import（L34）、input.plainText 全部在位，原样落地。

### 测试适配（降级论证）
- source 测试依赖的 3 个 API 在目标全树 0 命中：app.captureSpans()、app.mockInput.pasteBracketedText、app.mockInput.typeText；openEditor 无法轻量 mock（/editor 路径不可单测）；submit 驱动 syncExtmarks 存在竞态。
- 目标宿主 display.test.ts blob 本身为 CRLF（git apply 无法匹配 LF 上下文，--ignore-whitespace 对 CR 差异无效——实测）。
- 重建为新建 `packages/tui/test/prompt/offset-width.test.ts`（LF）：锁定 promptOffsetWidth 对宽前缀的 JS 索引→Textarea offset 换算（9→17）与占位符起点计算——这是 H3-H7 全部行为的数学基础；display.test.ts 的既有宽度口径测试（中文/emoji/ZWJ）继续作为回归参照。
- 生产代码 H3-H7 完整落地，typecheck 验证类型与调用正确性。

## E/C
- E = 110（plus - commentPlus + minus；含 H7 重写 56 行与删除 30 行）
- C = 17（#247 标注：display.ts 1、H3-H5 3、H6 2、H7 9、测试 2——覆盖口径共享、边界累加、draft 排除、重叠分配、最近候选、JS 索引推进、形状差异、restore 顺序等真实 invariant）
- need = ceil(110 * 0.15) = 17 → 余 0 PASS

## Source
- original/0247-9c4cf37be9e4.patch: 327 行 4 文件。

## Current
- current/0247-9c4cf37be9e4.patch: 9181 bytes, 5 节
- SHA-256: 7803c16dec729085d5ed11f0730a375d39d6fec94650125ea0cc3f44032f3cf2

## 安装
- materialize 247 --rebuild: passed 247/247
- 脚本精准适配：apply 命令加 `--ignore-whitespace`（仓库部分 blob CRLF 存储；对 CR 差异实测无效但保留——不影响其他 patch，行数计数仍严格）

## 测试
- typecheck 247: 全部 workspace passed
- `bun test test/prompt/offset-width.test.ts test/prompt/display.test.ts`（packages/tui）: 2 pass / 0 fail（display.test.ts 既有 1 项 + 新增 1 项）

## 修订历史
1. 初版 5 节（display.ts 注释 / H3-H5 / H6 / H7 / display.test.ts 改造）。
2. display.test.ts CRLF 无法匹配 → 改 --ignore-whitespace（无效）→ 测试换新建 LF 文件 offset-width.test.ts。
3. 新文件节裸空行被算作上下文（depends on old contents）→ 转 `+` 前缀；hunk 计数 17→18（漏 1 行）修复。
4. edit 事故：H6 注释编辑误删 cursorOffset 赋值行 + 重复注释行 → 补回/去重。
5. hunk D 中意外的前缀空格上下文行（a 34≠33）→ 转 `+`。
6. C 从 5 → 12 → 17 逐轮补齐。最终三关全绿。

## 三方比较
- Source：/editor 回调、cursorOffset、syncExtmarks 的 offset 口径统一 + 宽字符高亮集成测试。
- Upstream v1.17.20：TUI 迁 packages/tui、normalized 变量名、部分 hunk 已被前置吸收。
- Target：落地 H3-H7 于真实 owner，测试降级为换算口径单测。无行为丢失（已吸收 hunk 核验一致）、无重复 owner。

## 最终行为
- /editor 返回后 file/agent part 的 source offsets 按 Textarea 显示宽度口径恢复。
- cursorOffset 与 source offsets 同口径（换行计 1）。
- 提交前 extmark 同步以可见文本重建 span，杜绝漂移坐标回写。
- 宽字符换算口径由 offset-width.test.ts 锁定。

## 批次双审结论（2026-09-06，245-249）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：N-02/N-03 record「CRLF blob」措辞不精确（实为 autocrlf 工作树转换，blob 为 i/lf）——降级决策与结论不受影响。