# Record: Patch 33 — 6dd9afdea412

## 1. Manifest Entry
- **Index**: 33
- **SHA**: 6dd9afdea412124b2d89300fc17f7f72d6fa1728
- **Parent**: 0032-b815a5764d5b
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加交互和审查代理，增强权限管理和描述

## 2. Source Behavior (original patch, 1 file)
1. `agent.ts`: 在 `build` 和 `plan` 代理之间插入两个新代理
   - `interactive`: bash 设为 "ask"，其他工具使用 defaults 权限
   - `review`: bash/edit/write/apply_patch 均设为 "ask"，比 interactive 更严格

### 行为目的
- **交互代理 invariant**: 适合需要逐步审批的场景，bash 执行前需用户确认
- **审查代理 invariant**: 适合 code review 场景，文件修改和命令执行均需确认
- **权限模型 invariant**: 使用 Permission.merge(defaults, fromConfig, user) 三层合并

## 3. 上游 (v1.17.20) 行为
- V2 agent.ts 已有 `build` 和 `plan` 代理
- V2 使用 `Permission.merge` 和 `Permission.fromConfig` 权限模型
- V2 无 `interactive` 和 `review` 代理

## 4. 目标适配 (current patch, 1 file)
### 适配的文件
1. `agent.ts`: 在 V2 的 `build` 和 `plan` 代理之间插入 `interactive` 和 `review` 代理，添加 7 条中文注释解释权限决策

### 保留的 SMARK 语义
- interactive 代理的 bash="ask" 权限配置
- review 代理的 bash/edit/write/apply_patch="ask" 权限配置
- 两个代理的 mode="primary" + native=true 注册

### 保留的上游优势
- V2 的 Permission.merge/fromConfig 权限模型
- V2 的代理注册结构

## 5. E/C 计算
- **E = 42**（新增代码行）
- **C = 7**（中文注释）
- **最低 C = max(1, ceil(42 * 0.15)) = 7**
- **7 ≥ 7，通过**

### 注释位置
1. `// 交互代理：bash 需要确认，适合需要逐步审批的场景` — interactive 代理用途
2. `// bash 设为 ask：交互模式下每次命令执行前需用户确认` — bash 权限决策
3. `// mode: primary + native: true：作为内置主代理注册，可直接在会话中选择使用` — 注册方式
4. `// 审查代理：编辑类工具和 bash 均需确认，适合 code review 场景` — review 代理用途
5. `// edit/write/apply_patch 设为 ask：审查模式下文件修改需用户逐次确认` — 编辑权限决策
6. `// bash 设为 ask：命令执行同样需要确认` — bash 权限决策
7. `// 审查代理同样作为内置主代理注册，权限比交互代理更严格` — 权限对比

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 35 --rebuild`: passed 35/35
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/session-ui:passed, packages/tui:passed, packages/ui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首轮审计 G-02**: C=0, 需要 C>=6
   - 修复: 添加 7 条中文注释解释代理权限决策的 invariant 和边界

## 8. 最终行为
- interactive 代理: bash="ask"，其他工具使用默认权限，作为内置主代理可用
- review 代理: bash/edit/write/apply_patch="ask"，比 interactive 更严格，适合 code review
- V2 权限模型和代理注册结构保留
