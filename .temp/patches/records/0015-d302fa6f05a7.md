# Record: Patch 15 — d302fa6f05a7

## 1. Manifest Entry
- **Index**: 15
- **SHA**: d302fa6f05a734b420714024b68f27bb14d82f6e
- **Parent**: 0014-61bcd2aae67c
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat(provider): 更新claudecode提供者以支持动态鉴权模式，优化API密钥和基本URL配置

## 2. Source Behavior (original patch, 2 files, 9 hunks)
1. `claudecode.ts` (8 hunks):
   - Hunk 1: 移除 `xxh64` import，重组常量区块（添加中文分区注释）
   - Hunk 2-7: 为 MAIN_SYSTEM_BLOCK/getSession/computeFingerprint/buildBillingHeaderBlock/buildMetadataUserId/patchCch 添加 JSDoc 注释
   - Hunk 8: createClaudeCodeFetch 重构 — 添加 authMode 参数、system blocks 合并逻辑（buildSystemBlocks + concat 原始 system）、metadata 合并（保留原有字段只覆盖 user_id）、动态 thinking 分发、CCH 计算与替换
   - Hunk 9: provider.ts custom — authMode 参数传递、ANTHROPIC_AUTH_TOKEN/BASE_URL 环境变量优先

### 行为目的
- **authMode invariant**: 支持动态鉴权模式（x-api-key vs Bearer），由 provider 传入
- **System blocks merge invariant**: 保留原始 system 内容，将 billing header + CLI identity + main system block 插入到最前面
- **Metadata merge invariant**: 保留原有 metadata 字段，只覆盖 user_id
- **Dynamic thinking invariant**: 按模型和用户配置分发 thinking 策略（disabled/adaptive/enabled+budget）
- **CCH computation invariant**: 对完整 body 做 xxHash64，取低 20 位转 5 位 hex，替换占位符
- **Environment variable priority invariant**: ANTHROPIC_AUTH_TOKEN > CLAUDECODE_API_KEY > auth.key

## 3. 上游 (v1.17.20) 行为
- V2 claudecode.ts 已有 patch 13 添加的 authMode、CCH、system blocks 基础结构
- V2 使用 `redact-thinking-2026-02-12` beta（patch 13 添加，patch 15 需要移除）
- V2 provider.ts 已有 custom loader 结构

## 4. 目标适配 (current patch, 2 files, 5 hunks)
### 适配的文件
1. `claudecode.ts` (4 hunks):
   - Hunk 1: 移除 redact-thinking beta，更新注释
   - Hunk 2: buildRequestHeaders 添加注释
   - Hunk 3: patchCch 区域注释更新
   - Hunk 4: createClaudeCodeFetch — system blocks 合并、metadata 合并、动态 thinking、CCH

2. `provider.ts` (1 hunk):
   - Hunk 5: custom loader — authMode 传递、环境变量优先级

### 跳过的 hunks（4 个，全部 cosmetic）
- Original hunk 1 (xxh64 import 移除 + 常量重组): xxh64 移除由 patch 16 处理（纯 TS 实现），常量重组是 cosmetic
- Original hunks 2-7 (JSDoc 注释): 纯注释添加，无行为变更

### 保留的 SMARK 语义
- authMode 动态鉴权完整保留
- System blocks merge 逻辑完整保留（buildSystemBlocks + concat）
- Metadata merge 逻辑完整保留
- Dynamic thinking 分发完整保留
- CCH computation 完整保留
- provider.ts 环境变量优先级完整保留

## 5. E/C 计算
- **E = 112**（代码行）
- **C = 18**（中文注释）
- **最低 C = max(1, ceil(112 * 0.15)) = 17**
- **18 ≥ 17，通过**

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --materialize 15`: passed 15/15
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 最终行为
- authMode 动态鉴权（x-api-key/Bearer）由 provider 传入
- System blocks merge 保留原始 system，插入 billing header + CLI identity + main system block
- Metadata merge 保留原有字段，只覆盖 user_id
- Dynamic thinking 按模型和用户配置分发
- CCH computation 对 body 做 xxHash64 并替换占位符
- provider.ts 环境变量优先级: ANTHROPIC_AUTH_TOKEN > CLAUDECODE_API_KEY
- redact-thinking beta 被移除（patch 23 会重新添加）
