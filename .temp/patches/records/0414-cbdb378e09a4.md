# 0414 — test(tui): reasoning 聚合布局回归
## 最终状态：PASS（R13）
## 适配：源 integration 的 reasoning 源码串 describe 目标从未有（SATISFIED drop）；源 layout 测量测试（measureAssistantTextWidth + reasoning chrome 公式）目标布局不存在 → 按目标约定重建等价回归：session-layout.test.ts 增「单一 messageContentWidth 管线 + ReasoningPart 同模块」结构不变量测试，防止独立 reasoning 宽度公式漂移
## E/C：E=25 C=5 need>=4 ✓
## 验证：materialize 414 Passed
---

## R20 修订（2026-09-14 第一波双审；B-414-1 + G-414-1 闭合）

### 双审分歧与处置
正向判 B（字符串断言违反 source intent：源 commit 明文移除脆弱源码串断言；且精确 toContain 无法检测 reasoning chrome 偏移或内联独立公式）；逆向判 N（目标架构无源测量 harness，结构断言可接受但强度弱于源）。按"任一 BLOCK 即修"处置：增强断言至数据流闭环级。

### B-414-1 闭合：断言从精确字符串升级为数据流闭环
增强后 6 断言（session-layout.test.ts）：
1. messageContentWidth 单一 memo 存在（createMemo）
2. sessionMessageContentWidth 调用（layout util 消费）
3. ReasoningPart 同模块定义
4. 无独立 reasoning*Width 变量（not.toMatch）
5. **新增**：ReasoningPart 函数体内引用 ctx.width（从 function ReasoningPart( 到 \n} 的函数体切片）——reasoning 渲染必须经 context 消费共享宽度
6. **新增**：context 的 get width() getter 来自 messageContentWidth()（provider 块 120 字符切片）——管线源头锁定
证据链：state index.tsx :360-361（memo 定义）、:1345-1347（get width 提供）、:1836（ctx = use()）、:1855（ctx.width 消费）。漂移类别覆盖：独立公式（断言 4/5）、chrome 偏移（断言 5 捕获 ctx.width 移除）、管线改道（断言 6）。
源测量测试（Yoga 实测同宽）目标架构下 chrome 公式不存在，drop-rebuild 决策如实记录；强度差（实测 vs 结构）已在本节明示。

### 验证
- 行为测试：bun test test/routes/session/session-layout.test.ts → 7 pass / 0 fail / 19 expect（.test-workspace，2026-09-14）
- typecheck-0415：415/415 + 30/30 passed
- 本节闭合 G-414-1（record 门禁）

### 证据细节（锚点与指针）
- manifest 行：414；SHA cbdb378e09a4；parent 330fa98869b8（0413）
- 测试文件：packages/tui/test/routes/session/session-layout.test.ts 尾部新增 describe
- 断言 1：const messageContentWidth = createMemo(() =>（memo 定义）
- 断言 2：sessionMessageContentWidth({（layout util 消费）
- 断言 3：function ReasoningPart(（同模块定义）
- 断言 4：not.toMatch /reasoning[A-Za-z]*Width\s*=/（独立公式禁用）
- 断言 5：ReasoningPart 函数体切片含 ctx.width（数据流消费）
- 断言 6：get width() 后 120 字符切片含 messageContentWidth()（管线源头）
- state 证据链：index.tsx :360-361 / :1345-1347 / :1836 / :1855
- hunk 头：@@ -148,3 +148,33（R20 增 8 行断言+注释后修正计数）
- 行为测试：bun test session-layout.test.ts → 7 pass / 0 fail / 19 expect（2026-09-14）
- typecheck 指针：typecheck-0415（415/415 + 30/30）
- 源强度差如实记录：源为 Yoga 实测同宽；目标无测量 harness，结构断言 + 数据流闭环为可达上限
- 审计轮次：R13 → R20 双向审计（正向 B / 逆向 N 分歧 → 按任一 BLOCK 即修处置）+ 本扩写

### 补充说明
- 断言 5 切片边界：indexOf("\n}") 取 ReasoningPart 函数体首个顶格闭合
- 断言 6 切片窗口 120 字符：覆盖 get width() 到 messageContentWidth() 的 getter 体
- 源 integration 源码串 describe：目标从未有（state session-integration.test.ts 仅 token-usage）
- session-message-render 源测试：依赖源系 withRenderedSession harness，目标无对应，SKIP 已记录
- R20 后 hunk 头修正：@@ -148,3 +148,33（增 8 行后 new 计数同步）
- 裸 // JSX 隐患：本测试为 .ts 文件非 JSX，无 0418 类问题
