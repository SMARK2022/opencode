# Record 0414-cbdb378e09a4

## Identity
- SHA: cbdb378e09a4
- Parent: 330fa98869b8 (0413)
- Subject: test(tui): 补强 reasoning 聚合布局回归
- Status: PASS（批次已闭合；详见 0415 批次结语与双审计结论）

## Source intent
source 0414 共 2 文件（5057 字节）：
1. `packages/opencode/test/cli/cmd/tui/session-integration.test.ts`：删除 describe("reasoning part") 内 4 个对 session 路由**源码字符串**的脆弱断言（createThrottledSignal/displayContent/Thinking 头部/▲ collapse/streaming code 正则）——已被字符帧/事件/交互测试替代，防止重构时脆弱失败。
2. `packages/opencode/test/cli/cmd/tui/session-layout.test.ts`：session layout width 循环内为 measureAssistantTextWidth 增加 `kind: "text" | "reasoning"` 分支——用真实 Yoga 树实测（text: 边框1+padding3=4；reasoning: 边框1+边框1+padding2=4）断言两条路径对同一 `expected`（SESSION_MESSAGE_LEFT_CHROME=4），防两套宽度公式漂移。

## Target analysis（0414 预研）
- hunk1 文件（session-integration.test.ts）：目标不存在（TUI 测试已迁 packages/tui/test）；目标无任何对 session 路由源码字符串断言、无 reasoning 布局脆弱测试 → 删除意图天然满足 → 该 hunk SKIP，record 记录。
- hunk2 owner：`packages/tui/test/routes/session/session-layout.test.ts`（153 行：sessionMessageContentWidth 纯函数 3 测试 + CJK 渲染 3 测试）。
- 目标真实 chrome 与 source 不变量不符（record 0184 已放弃 Yoga 树实测）：
  - TextPart：`<box paddingLeft={3} marginTop={1} flexShrink={0}>` 无边框 → text chrome = 3
  - ReasoningPart：外层 `<box paddingLeft={3}>` + 折叠预览内层 `<box paddingLeft={2}>` → 折叠预览 chrome = 5
  - 公式 `sessionMessageContentWidth = width - sidebar - 3 - 4 - 2`（SESSION_MESSAGE_LEFT_CHROME=4 与两侧均不符，pre-existing 状态）
- 因此 verbatim Yoga 树适配在目标必然失败；且改生产 chrome（方案 A）超出 test-only 范围并破坏既有视觉。

## Target adaptation（决策）
- hunk1：SKIP（意图天然满足，record 记录）
- hunk2：以目标可承载的最小语义补强——新增 `describe("session message chrome decomposition (#414)")`：
  - 锁定公式的 chrome 分解恒等式（terminalWidth 恰好等于公式各分量之和时 output 恢复到干净值），任何一侧常量/公式漂移都会在此失败——与 source "防两套宽度公式漂移"意图同构（target 上两路径 chrome 不同，无法锁同宽，锁分解）
  - 注释说明与 source 的差异：source 用 Yoga 树锁同宽；本分支 0184 已按纯函数+常量契约承载，0414 锁公式分解 + text/reasoning 真实 chrome（3/5）记录在案
- 不迁移 reasoning 聚合（目标 ReasoningPart 为旧式 singleton，无聚合重构）

## Verification（完成）
- materialize 414：passed 414/414（复用 0413 前缀；current/0414 由 state 0413 的 git diff 直接导出，权威单一文件段）
- typecheck 414：仅剩 4 个 tools.ts TS2554（0415 bridge 中间态，预期）；tui workspace 全绿
- 行为测试（.test-workspace packages/tui）：bun test test/routes/session/session-layout.test.ts → 8 pass / 0 fail（原 6 + #414 新增 2）
- hunk1（删除脆弱字符串断言）：目标无对应文件/无等价断言 → SKIP（意图天然满足）
- hunk2（layout 回归）：以 chrome 分解恒等式测试承载（见决策）

## E/C
- count-ec-auditor.py：E=15 / C=7 / need=3 → PASS（注释解释 source/target chrome 差异、分解恒等式意图、分量数值含义）

## 最终行为
- packages/tui/test/routes/session/session-layout.test.ts 新增 describe("session message chrome decomposition (#414)")：锁定公式（主列3 + 消息chrome4 + gutter2）的分解不变量——任一常量或公式分量漂移即失败，等价承载 source“防两套宽度公式漂移”意图
- reason/text 两路径真实 chrome（3/5）及与 source 的差异记录在注释中，供后续维护对齐

## 目标 chrome 调查（决策依据）
- TextPart（index.tsx L2005）：`<box paddingLeft={3} marginTop={1} flexShrink={0}>` 无边框 → text chrome = 3
- ReasoningPart（index.tsx L1875-1882）：外层 `<box paddingLeft={3}>` + 折叠预览内层 `<box paddingLeft={2}>`（L1791 区间）→ 折叠预览 chrome = 5
- layout.ts：SESSION_SIDEBAR_WIDTH=42、SESSION_MAIN_HORIZONTAL_PADDING=3（私有）、SESSION_MESSAGE_LEFT_CHROME=4（私有）、SESSION_SCROLLBAR_GUTTER=2（私有）；公式 = width - sidebar - 3 - 4 - 2（Max(1, ...)）
- source 不变量：text=边框1+padding3=4、reasoning=边框1+边框1+padding2=4，均等于 SESSION_MESSAGE_LEFT_CHROME=4 → 同宽；目标两路径 chrome 不同且公式（4）与两者均不符（pre-existing 状态，0184 弃用 Yoga 树实测时已接受）
- 结论：verbatim Yoga 树测量必然失败；改生产 chrome（归一化）超出 test-only 范围 → 锁公式分解 + 注释明示差异

## 验证过程与审计要点
- current/0414 = 单个权威文件段（state 0413 git diff 导出，1934 字节）：packages/tui/test/routes/session/session-layout.test.ts 追加 1 describe + 2 测试
- 物化 414 passed（复用 0413 前缀）；typecheck 414：tui workspace 全绿、opencode 仅剩 0415 bridge 中间态 4 个 TS2554（预期，0415 消解）
- bun test test/routes/session/session-layout.test.ts：8 pass / 0 fail（原 #184 三测试 + CJK 三测试 + #414 两测试）
- hunk1（session-integration 脆弱断言删除）：目标无该文件/无等价断言 → 删除意图天然满足，SKIP；hunk2 按上述决策适配