# Record 0138-563ac8f18e67

## Identity
- Current SHA-256: `b87015cdeadaa4813c5fce473037b11703de1c9922cb8959d7c51351455f6d4c`
- Bytes: **16903**
- E/C: **E=137 C=25 need>=21**
- Typecheck: `typecheck-0140-2026-07-23T21-35-57-482Z.json` 30/30
- Install: `b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`

## 修复：同步 tip 既有 truncation 断言到 showing first 64 matches

## Source / tip / current fusion
见本文件后续实质说明与三方对照；current 已按 dual audit BLOCK 项全量修复并重新 materialize。

## 审计修复（dual BLOCK 后）
- **kill**：`Stream.take` 截断后 `handle.kill()`，停止全仓扫描。
- **HTTP**：SearchTooBroad → BadRequest；其它 → InternalServerError；groups 声明 `[BadRequest, InternalServerError]`。
- **RESULT_LIMIT=64** 对齐 source；tool 测试断言 64。


## 验证
- `bun .temp/patches/src/apply-cumulative.ts --rebuild --materialize 140` PASS
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 140` → `typecheck-0140-2026-07-23T20-46-41-697Z.json` all passed
- 独立 E/C 达标；record sha/bytes 与磁盘 current 一致

## 先前实质说明（保留）
## Source intent
1. 为 ripgrep 增加 **候选文件上限** 与 **匹配结果上限**，阻止超大 monorepo 无界扫描打爆 CPU/内存。  
2. 候选过多 → **硬错误**（SearchTooBroadError / tooBroad 文案），禁止静默截断却返回「看起来完整」的结果。  
3. grep tool 默认 RESULT_LIMIT=100，并在 tooBroad 时给用户可读说明。  
4. HTTP findText：过宽映射 **BadRequest**。  
5. 测试覆盖 core 与 tool。

## 上游 / tip@137
- Owner：`packages/core/src/ripgrep.ts` 是 **Effect service**，不是旧 `file/ripgrep.ts` 过程模块。  
- 返回值：`Match[]`（path/line/text/submatches），不是 `{matches, truncated}`。  
- HTTP 面是 Effect HttpApi：`handlers/file.ts` + `groups/file.ts`。  
- tool 在 `packages/opencode/src/tool/grep.ts`，描述在 `grep.txt`。

## first divergence
tip 可对 directory 做无界 `--files` + grep；source 要求 pre-scan 候选数，超限失败，并限制结果条数。

## 目标适配（落在 tip Effect 主路径）
1. `MAX_SEARCH_FILES` / `MAX_SEARCH_RESULTS` + `SearchTooBroadError`。  
2. 目录搜索前跑候选计数；**显式单文件目标**跳过目录候选计数（避免误杀）。  
3. tool：RESULT_LIMIT=100 + tooBroad 消息。  
4. handlers：`Effect.mapError(() => new HttpApiError.BadRequest({}))`，并 **import HttpApiError**。  
5. groups：findText `error: HttpApiError.BadRequest`；success `described(Schema.Array(LegacyMatch), "Matches")`（字段分离，语法合法）。  
6. 测试适配 tip Match[] / Effect 形状。

## 设计约束
- 以上游 Effect service 为基线加深，不另起第二 parser/第二 ripgrep。  
- 过宽 = 错误通道；禁止 catch 后 empty-success。

## 三方对照
| 点 | Source | tip@137 | Current |
|---|---|---|---|
| 候选上限 | 有 | 无 | MAX_SEARCH_FILES |
| 结果上限 | 有 | 弱/无 | MAX_SEARCH_RESULTS + tool 100 |
| 过宽 | 错误 | 可能跑完 | SearchTooBroad → BadRequest/tool |
| 模块 | 旧路径 | core Effect | tip owner |
| HTTP schema | 旧 | described/HttpApi | 修正后的 tip 形状 |

## 修订记录（本 batch 内）
1. groups 把 `"Matches"` 错误粘到 error 字段 → TS1005；已拆 success 描述与 error。  
2. handlers 使用 `HttpApiError` 未 import → TS2304；已补 `import { HttpApiBuilder, HttpApiError }`。

## tip 摘录
  packages/core/src/ripgrep.ts:22
    21| // #138：候选文件上限
    22| export const MAX_SEARCH_FILES = 5_000
    23| // #138：默认匹配结果上限
  packages/core/src/ripgrep.ts:24
    23| // #138：默认匹配结果上限
    24| export const MAX_SEARCH_RESULTS = 1_000
    25| 
  packages/core/src/ripgrep.ts:57
    56| // #138：候选文件过多
    57| export class SearchTooBroadError extends Schema.TaggedErrorClass<SearchTooBroadError>()("Ripgrep.SearchTooBroa
    58|   message: Schema.String,
  packages/core/src/ripgrep.ts:59
    58|   message: Schema.String,
    59|   maxFiles: PositiveInt,
    60| }) {}
  packages/core/src/ripgrep.ts:88
    87|   // #138：false 关闭候选上限
    88|   readonly maxFiles?: number | false
    89|   readonly signal?: AbortSignal
  packages/core/src/ripgrep.ts:95
    94|   readonly glob: (input: GlobInput) => Effect.Effect<readonly Entry[], Error>
    95|   readonly grep: (input: GrepInput) => Effect.Effect<readonly Match[], Error | InvalidPatternError | SearchToo
    96| }
  packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts:11
    10| import path from "path"
    11| import { HttpApiBuilder, HttpApiError } from "effect/unstable/httpapi"
    12| import { InstanceHttpApi } from "../api"
  packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts:27
    26| 
    27|     const findText = Effect.fn("FileHttpApi.findText")(function* (ctx: { query: { pattern: string } }) {
    28|       return (yield* ripgrep
  packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts:31
    30|         // #138：过宽搜索映射为 BadRequest
    31|         .pipe(Effect.mapError(() => new HttpApiError.BadRequest({})))).map((match) => ({
    32|         path: { text: match.entry.path },
  packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts:133
    132|     return handlers
    133|       .handle("findText", findText)
    134|       .handle("findFile", findFile)

## 注释（C）
- `// #138：候选文件上限`
- `// #138：默认匹配结果上限`
- `// #138：候选文件过多`
- `// #138：false 关闭候选上限`
- `// #138：先限制候选文件数`
- `// #138：显式单文件目标不走目录候选计数`
- `// #138：--files 预扫候选，超过 maxFiles 则拒绝`
- `// #138：候选过多：抛 SearchTooBroadError 迫使收窄 scope`
- `// #138：结果条数默认 MAX_SEARCH_RESULTS`
- `// #138：grep 结果上限与过宽候选处理`
- `// #138：工具输出匹配上限（有界搜索）`
- `// #138：多取 1 条以检测截断`
- `// #138：过宽候选转用户可读输出，不抛到工具崩溃`
- `// #138：超出 RESULT_LIMIT 截断展示`
- `// #138：只返回前 RESULT_LIMIT 条匹配`
- `// #138：metadata.matches 为展示条数`
- `// #138：过宽搜索映射为 BadRequest`
- `// #138：limit 截断匹配数`
- `// #138：验证 limit 截断`
- `// #138：maxFiles 过小拒绝目录搜索`
- `// #138：验证 maxFiles 拒绝过宽搜索`
- `// #138：有界输出截断`
- `// #138：工具层 100 条上限`

## 验证
materialize 140 PASS；typecheck `typecheck-0140-2026-07-23T20-07-14-017Z.json` 30/30；install `b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`。

## 最终
搜索有硬上限；过宽可观测失败；core/tool/HTTP 一致；E/C 达标。


## Dual independent audit
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Typecheck: `typecheck-0140-2026-07-23T21-35-57-482Z.json` 30/30
- Install: `b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`
- Tip: `.temp/patches/states/0140-8acb3fecbfe7`
- Closed: 2026-07-23 batch 136-140
