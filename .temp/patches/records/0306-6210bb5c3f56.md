# Record 0306-6210bb5c3f56 — feat(session): 增加会话列表的限制和查询时间范围，优化会话检索性能

## Identity
- index/full SHA: 0306 / 6210bb5c3f56e0ff45bdb32a5dcf2f56d962376a (short 6210bb5c3f56)
- manifest parent: 3f959c5549fd (0305)；sourceTip d0ceb469…
- subject: feat(session): 增加会话列表的限制和查询时间范围，优化会话检索性能
- status: PASS（首个编辑快照建立中——record 先行）

## Source intent (original/0306-6210bb5c3f56.patch, 2 hunk 集)
1. `packages/opencode/src/cli/cmd/tui/component/dialog-session-list.tsx`：
   - `SESSION_LIST_PREVIEW_SESSION_LIMIT` 50 → 400（预览列表可展示更多会话）
   - 搜索查询 `sdk.client.session.list({ search: input.query, limit: 30, ...input.filter })` → `{ search, start: Date.now() - 90*24*60*60*1000, limit: 100, ...filter }`（搜索限定最近 90 天、上限 100）
2. `packages/opencode/src/cli/cmd/tui/context/sync.tsx`：
   - `listSessions` 的 `.list({ start: Date.now() - 30*24*60*60*1000, ...sessionListQuery() })` → `{ start: 90 天, limit: 1200, ...sessionListQuery() }`（侧栏同步列表扩展 90 天窗口并固定上限 1200）

## Target adaptation plan
- Owner 迁移（0299 先例）：`packages/tui/src/component/dialog-session-list.tsx` 与 `packages/tui/src/context/sync.tsx`
- sync.tsx L300 直改（上下文与 source pre-image 同构）；dialog-session-list.tsx 需核实目标重构后的调用形态（loadDialogSessionList/list 封装，L29 `limit: search ? 30 : 100`）后按目标结构适配 start/limit 语义
- 方法：state 0305 语义编辑 → git diff 权威生成 → 替换 current/0306（当前为 pristine email 格式原样，含旧路径——materialize 310 停于 #306 的失败源）

## E/C（count-ec-auditor.py）
- E=4, C=2, need=ceil(4*0.15)=1 → **PASS**
- C 位置：SESSION_LIST_PREVIEW_SESSION_LIMIT=400 注释（预览覆盖更完整，避免滚动补拉）；createDialogSessionListQuery 搜索分支注释（90 天窗口与 sync 侧一致，避免全历史检索拖慢预览）

## Verification log
1. `git apply --check fix306.diff`（state 0305 干净树）→ EXIT 0；validate_hunks → fixed: 0
2. `--materialize 306` → `passed 306/306`，state 0306 建立（current/0306 由 pristine email 原样替换为权威适配 diff，修复 materialize 310 停于 #306 的旧路径/格式失败）
3. `--typecheck 306`：Install `reused (cbdc173…)`（0306 未动依赖，指纹不变正确）；Typecheck packages/app:failed（已知环境基线）；Source/Target unchanged: true
4. packages/tui `bun typecheck` → exit 0
5. current 最终校验：SHA-256 `eec085381f745817ac30bad10c92b7f98b100a4eec1a69f770cd0ddad899ade6` / 1881 字节

## Target adaptation 详情
1. dialog-session-list.tsx：`SESSION_LIST_PREVIEW_SESSION_LIMIT` 50→400（source hunk 1 直改）+ 注释更新
2. dialog-session-list.tsx：source 搜索调用（start=90d、limit 100）适配进目标 `createDialogSessionListQuery` 的搜索分支：`limit: search ? 30 : 100` → `limit: 100`，搜索分支增加 `start: Date.now() - 90*24*60*60*1000`；browse（非搜索）分支行为不变（limit 100 保持）——最小语义改动，未扩大接口、未改变 browse 窗口
3. sync.tsx L300：`start` 30 天→90 天 + `limit: 1200`（source hunk 2 直改）

## 不一致与修订记录
- current/0306 初始为 original 原样（email 格式 + 旧路径 packages/opencode/src/cli/cmd/tui/...），无法在目标 apply——按 0299 迁移先例映射到 packages/tui 两文件并生成权威 diff
- 记录时间线：record 首建于编辑前（2026-08-21，R3 起记录先行纪律）

## 上游对比
上游 v1.17.20 无 dev-smark 会话列表增强（SESSION_LIST_PREVIEW_SESSION_LIMIT、搜索时间窗口、sync listSessions 窗口/上限均为 SMARK 独有）；目标 createDialogSessionListQuery 为先前 SMARK 适配重构的 owner，承载搜索查询语义

## Source hunk 映射
source hunk 1: dialog-session-list.tsx SESSION_LIST_PREVIEW_SESSION_LIMIT 50→400（目标 packages/tui/src/component/dialog-session-list.tsx L53）
source hunk 2: dialog-session-list.tsx 搜索查询 start=90d+limit 100（适配进目标 createDialogSessionListQuery 搜索分支，browse 不变）
source hunk 3: sync.tsx listSessions start 30→90d+limit 1200（目标 packages/tui/src/context/sync.tsx L300）

## E/C 注释位置
C 位置 1: createDialogSessionListQuery 搜索分支 // 搜索限定最近 90 天窗口（与 sync 侧一致）
C 位置 2: SESSION_LIST_PREVIEW_SESSION_LIMIT=400 // 预览覆盖更完整

## Audit trail
- （双审计 seal 在复审通过后追加）

## DUAL-AUDIT SEAL（批次 306-310）
- 第二轮双审计（R2 修复后：记录扩展 + 0309 fixture.ts 死代码→vcs.test.ts 可达）：
  - 正向审计（ses_fdf4eeb92ffevPAio1sf1avQJx）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdf4ed38effeP3yoEqRZsyBC46）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（4/2、79/20、118/26、3/1、4/4）、累计 apply 310/310、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：prompt.test.ts shell 测试 10s 超时（296-300 N-02 模式）；packages/app TS1128 环境基线；indentation stripping N（语法有效、行为不变）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）


## R12 封存版恢复 + 测试适配（2026-09-10/11）

- **事故**：9-2 封存版（eec08538…/2251B，record 校验段被截断）在数据丢失事件中被覆写为 verbatim original（V1 路径），git HEAD 捕获的是损坏版。
- **重建**：V2 owners 语义变换——dialog-session-list.tsx 预览 50→400（+注释）、list query 补 90 天窗口（保持目标 search?30:100 结构）；sync.tsx 30→90 天 + limit 1200。初版 1746B/72195922。
- **补遗**：typecheck 发现 `packages/tui/test/component/dialog-session-list.test.ts` 两处 toEqual 需适配 query 新增的 start 字段（`start: expect.any(Number)` + 注释）——封存版亦应包含此适配（record 未载，推断遗漏）；追加密封版 SHA 校验待后续 ODB 复查。
- **重建后 SHA-256**：`19970d0ccffc32c83b6bde52c31bccf9d499de557acbdc03da1c62626abe89d7` / 2856 bytes。
- **验证**：materialize 310 passed 310/310（全链重放）；typecheck 310 全包 passed；dialog-session-list 测试 5 pass / 0 fail。
## R-FIX 13 项行为修复（2026-09-17，终审扫描后用户授权）

- 搜索路径 limit search?30:100 → 100（source 语义：统一 100，90 天窗口内 >30 命中不再截断）。

## 2026-09-22 测试回归修订
- 后续独立 general 审计指出 source 仅限制搜索时间窗口，current 的无条件 start 错误影响默认浏览。修订为仅 search 非空时写入 start，恢复浏览历史可达性；浏览测试恢复无 start 的精确断言。
- 编辑前 current SHA-256 `a222ec014aa5bbc71d78251ad85f0400f066cde084f9a182c9900de47d416853`，2842 bytes。
- TUI 全套测试发现 search query 期望 limit=30，实际=100；original 明确把搜索上限提高到 100，因此是此前生产修复未同步测试的遗漏，不应降低生产上限迎合旧断言。
- 本轮把同一 current 内的搜索测试更新为 100，并纠正相邻注释的“窄结果集”旧表述；保留 search trim、scope、roots 和 start 的既有断言。
- 本轮验证与独立复审待完成，历史 PASS 不覆盖此次修订。
- 修后 SHA-256 `bc66368ecb88eccd661bbe3147b4aae80662f924f0858d9103daf9b82a8d93c2`。
- 修后官方测试宿主 packages/tui 的隔离全套测试：385 pass / 3 skip / 0 fail，搜索查询断言通过。报告 `typecheck-0452-2026-09-22T08-32-15-603Z.md`：累计 452/452、30/30 typecheck；install 复用 `02cb1c9055216e2a1e1f79247eb3146962aeceec50059570a894f3180d40dbe6`，source/target unchanged=true。独立复审待完成。
