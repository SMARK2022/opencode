# 0449-329197629bfa fix(tui): 统一 bash 命令输出与展开预览的空行间距

## Identity
- index / full SHA: 0449 / 329197629bfa（parent = 0448）
- subject: fix(tui): 统一 Shell 命令与输出的隔行间距
- original: 36554B / 589 行 / 3 文件
- current: 35965B / 3 文件（docs 原样 + 标签删除重定位 + 测试新建等价）
- status: 闭合（materialize passed 449/449；链至 450 typecheck 全 passed）

## 逐文件适配
1. docs/plans/tui-bash-command-output-spacing.md：新建 529 行 plan（原样，blob 链基座）。
2. 源 packages/opencode/src/cli/cmd/tui/routes/session/index.tsx → owner 重定位 packages/tui/src/routes/session/index.tsx（目标 TUI 已迁移至独立包，先前批次完成）。
   - 源 hunk：移除 Model context output 内联 Show 标签（3 行）+ 输出包 marginTop={1} box。
   - 目标实测：Shell 容器 `<box gap={1}>` 已提供命令/输出间空行（容器级 gap 与源 marginTop 语义等价——目标结构与源 pre-image 不同：目标有 isRunning/Spinner 分支与 limited() 截断）。适配 = 仅删除 3 行标签块；marginTop 不引入（会与 gap 叠加成双倍间距，偏离统一空行语义）。
   - 行尾保真：目标 blob 为混合行尾（LF），采用 git show :path（index 版）字节提取 + regex 单块删除，diff 恰为 -3 行（避免全文件行尾重写噪声）。
3. 源 session-integration.test.ts（标签断言翻转）→ 目标无该文件（源码断言测试体系未迁移）→ 新建等价测试 packages/tui/test/routes/session/shell-output-spacing.test.ts：Shell 不含 Model context output 标签 + Shell 函数体含 gap={1} 间距（读取 routes/session/index.tsx 源码断言，与源测试同型）。

## superseded/等价声明
- marginTop={1} 由既有 gap={1} 等价承担：目标容器 gap 对所有相邻子元素生效，删除标签后命令与输出间恰一空行，与折叠预览一致——源的两项意图（标签移除、空行分隔）均达成。
- session-integration.test.ts 的断言翻转由新建 shell-output-spacing.test.ts 承担（not.toContain 标签 + 间距存在断言）。

## 验证
- materialize 449：passed 449/449。
- typecheck 链至 450：全 passed。
- diff 最小性：index.tsx -3 行（无行尾重写）；测试 +25 行新文件。

## E/C
- E=15、C=4 >= ceil(15×0.15)=3（26.7%）PASS。注释：新测试的 #449 语义说明 2 行、gap 等价说明 1 行、标签移除理由（标题后缀与背景色已足够区分）经目标注释形式保留于测试。

## 原始 patch 结构与 hunk 映射
- original 589 行：L15-549 为 docs 新建段（@@ -0,0 +1,529）；L550-572 为生产 hunk（@@ -2714,11 +2714,12，function Shell 上下文）；L573-589 为测试段（@@ -150,8 +150,12，断言翻转 + 新增 marginTop 断言）。
- current 映射：docs 原样；生产变更以目标路径 packages/tui/src/routes/session/index.tsx 呈现为 -3 行删除（marginTop 以 gap 等价不引入）；测试以新文件呈现（+25 行）。

## 语义要点（供审计复核）
- 源的两条用户可见意图：(1) 移除 Model context output 内联标签（上下文模式靠标题后缀 returned to model 与背景色区分）；(2) 命令与输出之间统一空行（与折叠预览的 [header, "", output] 空行一致）。
- 目标实现路径：(1) 直接删除标签 Show 块；(2) gap={1} 已在目标容器上生效——标签删除后输出上移一位，gap 保证命令行与输出间恰一空行。
- 目标 Shell 的额外结构（isRunning 分支 + Spinner + limited() 截断 + shellCanCollapse 提示）为更早批次的演进，源 hunk 的 pre-image 不含这些结构，直接映射不可行——因此采用意图级适配而非行级移植。

## 失败修订史
- 首轮对 state 448 worktree 的 edit 写入 CRLF 导致 diff 全文件重写（@@ -1,3901）——改用 git show :path 字节提取 + regex 删除，diff 缩至 -3 行。
## E/C 合格注释清单（C=4）
- shell-output-spacing.test.ts：#449 语义块注释 2 行（标签移除理由：标题后缀 + 背景色足够区分；空行统一与折叠预览一致）。
- gap 等价合同注释 1 行（源 marginTop 由目标容器 gap 承担的映射说明）。
- Shell 间距断言说明 1 行（函数体切片 + gap={1} 存在性检查的合同）。

## 独立核验指引（供审计）
- 标签移除：state 450 的 packages/tui/src/routes/session/index.tsx 中 grep "Model context output" 应为 0 命中。
- diff 最小性：current/0449 中 index.tsx 段应为 -3 行删除、无 + 行（gap 等价不引入 marginTop）。
- 新测试：packages/tui/test/routes/session/shell-output-spacing.test.ts 25 行、两条源码断言。
- 行尾保真：index.tsx 的 diff 仅 -3 行（blob 为混合行尾，未发生全文件行尾重写）。
## 与折叠预览一致性验证的推理链
- 折叠预览 shellPreviewText = [header, "", output()].join("\n")（含空行）——源 patch 引用此作为"空行一致"的基准。
- 展开路径在目标为 <box gap={1}> 的子元素间距：删除标签后子元素序列为 [命令行, 输出块]，gap 恰产生一空行——与折叠基准一致。
- 若额外引入 marginTop={1}：输出块上方将出现 gap+margin 两段间距，破坏"统一空行"目标——故不引入，并将此映射记入 record 与测试注释。
## 测试的运行面
- shell-output-spacing.test.ts 为源码断言（读文件文本），无 daemon/渲染依赖，跨平台即时执行。
- 断言二将 Shell 函数体切片（起点 function Shell(props: ToolProps)，窗口 6000 字符）后检查 gap={1}，避免命中文件其它区域的 gap 用法。
## 相关上下文（非本 patch 范围但影响判读）
- 目标 Shell 的 v2 调试渲染器（session-v2.tsx Bash，box gap={1}）为 debug-only，非生产路径；源 plan 文档将其列为 observed 排除项。
- bash-compress.ts 只影响 output 内容（压缩），不影响间距；源 plan 文档同列排除。
- 源 patch 的 throwaway 反馈脚本（D:\Temp\opencode\shell-spacing-feedback.ts）为源侧开发工具，不属于仓库内容，未迁移。
## Round-3 审计修订（B-03 关闭）
- 审计发现：Round-2 的折叠一致性推理链引用了目标不存在的 shellPreviewText 形态，且目标折叠预览（preview join 数组 + filter(Boolean)）结构性不含分隔空行——INV-02 在折叠视图缺失承载，等价声明不成立。
- **修复**：折叠预览 join 数组显式插入空行元素 `""`（命令与输出之间），filter 改为 `(x): x is string => x !== undefined`（只剔除 undefined、保留空串）——折叠视图现在呈现 `$ cmd` 空行 输出前 10 行，与展开视图 gap={1} 的空行分隔一致，INV-02 双视图一致成立。
- 0449 current 的 index.tsx hunk 更新为合并 hunk（折叠修复 + 标签删除，git diff 实际生成，缩进/行尾保真）；shell-output-spacing.test.ts 追加折叠 join 断言（`"", previewText(output(), 10)]` 存在性）与 filter 合同注释。
- 验证：materialize 链重建至 450 全过（typecheck-0450-2026-08-30T19-43-18-449Z：450/450、30 包 passed、No failures、install reused 813972ec）。
- Round-2 中与本修复冲突的旧推理段（gap 等价声明中的折叠基准引用）以本节为准：gap 承担展开侧、join 空行承担折叠侧，两者共同承载 INV-02。
## Round-4 重审修订（B-01/G-01 关闭——上轮修订被证伪后的真实修复）
- 上轮（Round-3）修订声明被重审证伪：current/0449 的 tui+测试段因 PowerShell `Set-Content -NoNewline` 处理 git diff 数组时吞掉全部换行，形成 2634 字节单行损坏段——git apply 静默跳过后 typecheck 报告产生虚假 "applied/passed"（INV-01/02/03 在物化链中零承载）。审计的 state 450 文件内容、mtime、git apply 探针与 report 差集四处证据成立。
- **真实修复**：materialize 448 重建基线 → state worktree 重放全部修改（字节级删除标签 3 行 + 折叠 join 插入 `""` 空行元素与窄化 filter + 新建含折叠断言的 shell-output-spacing.test.ts）→ git diff 以**字符串数组**直接 `-join "`n"` 组装 current/0449（绕过 Set-Content -NoNewline 陷阱）→ LF 完整性验证（34814B、603 LF、最大行长 511）→ materialize 链重建至 450 → **state 450 实证**：标签 0 命中、折叠 join 含 `""`、新 filter 在位、新测试文件存在。
- E/C 更新：additions 含折叠修复（-1/+1 替换）与测试 2 条增强断言/注释 → E=17、C=5 ≥ ceil(17×0.15)=3 PASS。
- Round-3 段中与本次事实冲突的"链重建全过"声明以本节为准：Round-3 的链重建应用的是损坏 patch，本次为首次真实物化。