# Record 0342-c632a9d1e958

## Identity
- SHA: c632a9d1e95828e38ddc8dd352f50a917df124dc
- Parent: 35d89d3b7bfe (0341)
- Subject: fix(test/fixture): 修复 disposeAllInstances 超时保护 poison cachedDisposeAll 导致 Linux Bus 测试失败
- Status: PASS

## Source intent

### fixture.ts changes

#### Hunk 1 — `@@ -53,8 +53,9 @@` (export DISPOSAL_TIMEOUT + withDisposalTimeout)
The upstream commit exports `DISPOSAL_TIMEOUT` and `withDisposalTimeout` (adds `export` keyword to both, plus a blank line between them) so that `fixture.test.ts` can import them directly for unit testing `withDisposalTimeout` in isolation rather than going through `disposeAllInstances` / `InstanceStore`.

#### Hunk 2 — `@@ -75,18 +76,15 @@` (disposeAllInstances: allSettled → all)
The upstream commit replaces `Promise.allSettled` with `Promise.all` in `disposeAllInstances` and removes the `for...of` rejection-logging loop. Rationale: `disposeInstance` (in `instance-registry.ts`) already uses `Promise.allSettled` internally to capture all disposer rejections, so `store.disposeAll()` never rejects from a single disposer failure. The outer `allSettled` + rejection log loop was dead code that could never execute. `Promise.all` restores behavior parity with the green-test baseline (16bb317b8b).

### fixture.test.ts changes

#### Hunk 1 — `@@ -1,7 +1,7 @@` (import update)
Adds `DISPOSAL_TIMEOUT` and `withDisposalTimeout` to the import from `./fixture`, alphabetically ordered.

#### Hunk 2 — `@@ -26,49 +26,73 @@` (test restructure: withDisposalTimeout unit tests + disposeAllInstances cleanup)
Replaces the entire `describe("disposeAllInstances")` block with two describe blocks:

1. **`describe("withDisposalTimeout")`** — 3 new tests:
   - `forwards the resolved value when the promise settles normally`: verifies the happy path transparently forwards the value (not intercepted by timeout)
   - `resolves within the timeout when the promise hangs`: uses a synthetic `new Promise(() => {})` to test timeout without poisoning `cachedDisposeAll`
   - `does not emit a timeout warning when the promise settles first`: verifies `clearTimeout` fires on early settlement

2. **`describe("disposeAllInstances")`** — 2 restructured tests:
   - `does not throw when a disposer rejects`: renamed from "does not throw when disposal rejects", loads real instance + rejecting disposer
   - `resolves quickly on empty cache`: new empty-path test

The critical fix: the old "completes within a bounded time even when an internal disposal hangs" test used `registerDisposer(() => new Promise(() => {}))` which poisoned `cachedDisposeAll` via `Effect.cachedWithTTL(Duration.zero)` — the stuck fiber was permanently cached, making ALL subsequent `disposeAll` calls join the same stuck fiber. The new hang test uses a synthetic Promise directly, bypassing `InstanceStore` entirely.

## Three-way comparison

| Aspect | SMARK original | Upstream (dev) | Target (0340+0341 state) |
|---|---|---|---|
| fixture.ts path | same | same | same |
| fixture.test.ts path | same | same | same |
| disposeAllInstances entries | 2 (InstanceRuntime + runTestInstanceStore) | 2 | 1 (InstanceRuntime only) |
| Comment in hang test | "testInstanceRuntime 的 cachedDisposeAll" | same | "cachedDisposeAll" (0340 shortened) |

## Target adaptation

### Path mapping
No path migration — both files remain at `packages/opencode/test/fixture/`.

### disposeAllInstances array adaptation
The upstream's `disposeAllInstances` has two entries:
```ts
withDisposalTimeout(InstanceRuntime.disposeAllInstances(), "InstanceRuntime"),
withDisposalTimeout(runTestInstanceStore((store) => store.disposeAll()), "InstanceStore"),
```
The 0340 target only has one:
```ts
withDisposalTimeout(InstanceRuntime.disposeAllInstances(), "InstanceRuntime"),
```
The `runTestInstanceStore` call was removed by an earlier 0340 adaptation (the target's `InstanceStore` integration differs). The adaptation preserves the single entry and applies the `allSettled→all` change + comment rewrite to match the upstream's semantic intent. The new comment says "disposeAllInstances() 不会因单个 disposer 失败而 reject" (adapted from upstream's "store.disposeAll() 不会...") to match the target's single-call structure.

### fixture.test.ts old-comment adaptation
The target's `disposeAllInstances` test block has slightly different comment text (e.g., "hang 测试会向 cachedDisposeAll" vs upstream's "hang 测试会向 testInstanceRuntime 的 cachedDisposeAll"). Since 0342 REMOVES the entire old test block and replaces it with new content, this difference is immaterial — the oldString matches the target's actual text.

## Per-hunk mapping table

| Hunk | File | Upstream context | Target context | Adaptation |
|---|---|---|---|---|
| 1 | fixture.ts | `const DISPOSAL_TIMEOUT` + `const withDisposalTimeout` | same | Add `export` + blank line (identical) |
| 2 | fixture.ts | 2-entry allSettled + log loop | 1-entry allSettled + log loop | Replace with 1-entry `Promise.all`, no log loop; adapt comment text |
| 1 | fixture.test.ts | import without DISPOSAL_TIMEOUT/withDisposalTimeout | same | Add both to import (identical) |
| 2 | fixture.test.ts | old disposeAllInstances describe with testInstanceRuntime comment | old describe with cachedDisposeAll comment | Replace entire block with new tests (identical new content) |

## Verification

- **Materialize**: `passed 342/342` — patch applies cleanly to state 0341
- **E/C**: E=34, C=25, need=6, **PASS** — the new test comments (CJK, descriptive) provide strong coverage for the executable test/assertion lines
- **Typecheck**: packages/opencode passes `tsgo --noEmit` (exit 0)

## 不一致 (target structure differences)

1. **disposeAllInstances single entry**: The target has only `InstanceRuntime.disposeAllInstances()` in the array, missing the upstream's `runTestInstanceStore((store) => store.disposeAll())` entry. The `allSettled→all` change applies to the single-entry array; the rejection-logging loop removal is identical. The new comment adapts "store.disposeAll()" → "disposeAllInstances()" to match the target's structure.

2. **Old test comment text**: The target's old `disposeAllInstances` test comments mention `cachedDisposeAll` without the `testInstanceRuntime` prefix that the upstream has. Since 0342 removes these comments entirely, the adaptation's oldString matches the target's actual text and the new comments are copied verbatim from upstream.

3. **DISPOSAL_TIMEOUT reference in hang test**: The upstream's old hang test hardcoded `5_000` in the assertion (`expect(elapsed).toBeLessThan(5_000 + 2_000)`). The new test uses `DISPOSAL_TIMEOUT + 2_000` (imported constant). This is a direct 1:1 copy from upstream — no adaptation needed.
