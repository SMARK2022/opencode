# Patch Record 0014

## 1. Identity

- Index: 14
- Source SHA: `61bcd2aae67c27fd798e3b32f1e22d09f159bcba`
- Parent SHA: `bbbf5f1dd94da64931b95ae8ee236f3509f7b486`
- Subject: `feat(prompt): 优化请求助手的输入输出统计逻辑，确保在无助手消息时返回零值`
- Original patch: `original/0014-61bcd2aae67c.patch`
- Current patch: `current/0014-61bcd2aae67c.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Pinned source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Previous materialized state: `states/0013-bbbf5f1dd94d` (retained)
- Current materialized state: `states/0015-d302fa6f05a7` (cumulative through batch end)

## 2. Source Diff Evidence

- Complete diff read: Yes, 255-line original patch.
- Changed paths: 3 TUI files (all path-migrated)
  - `packages/tui/src/component/prompt/index.tsx` — 3 hunks
  - `packages/tui/src/feature-plugins/sidebar/context.tsx` — multiple hunks
  - `packages/tui/src/routes/session/subagent-footer.tsx` — 3 hunks
- Every hunk and behavior intent:
  1. **prompt/index.tsx hunk 1**: Add `if (requestAssistants.length === 0)` early-return with zeros. Remove `globalConfirmed = sumConfirmed(assistants)`. Replace `activeAssistants` fallback with `last = requestAssistants.at(-1)!` (no fallback needed because of the guard).
  2. **prompt/index.tsx hunk 2**: Replace `currentInput = requestConfirmed.input + pending` with current-step model: `hasInFlightTail`, `lastStepFinish`, `currentStepInputConfirmed`/`currentStepOutputConfirmed`. `currentInput = currentStepInputConfirmed + pending`. `totalInput = requestConfirmed.input + pending` (not `globalConfirmed`).
  3. **prompt/index.tsx hunk 3 (cost)**: `msg.reduce(...)` → `requestAssistants.reduce(item.cost || 0)` — **DROPPED** in adaptation (keep `session?.cost`).
  4. **sidebar/context.tsx**: Same current-step split. Original also removes `cost` memo and adds `cost` to `StateValue` — **DROPPED** in adaptation (keep `session()?.cost`).
  5. **subagent-footer.tsx**: Same as prompt. Cost hunk **DROPPED** (keep `session()?.cost`).
- Tests/config/schema/migration/generated changes: None.

## 3. SMARK Intent

- User-visible behavior: Shift display from "request cumulative" to "current-step" model. The outer `input`/`output` numbers now represent only the current step's confirmed tokens (the last `step-finish` part's tokens, or 0 if there's an in-flight tail of pending tool calls). Parenthesized total shows the request cumulative.
- Protected invariant: `hasInFlightTail` = `!last.time.completed && lastParts.some((_, i) => i > lastSFIdx)` — when there are pending tool calls after the last step-finish, the current step hasn't been confirmed yet, so `currentStep*Confirmed = 0`. `lastStepFinish` is the last `step-finish` part in `lastParts` (found by reverse search).
- Normal path: Compute `requestAssistants` → guard empty → `requestConfirmed = sumConfirmed(requestAssistants)` → find `lastStepFinish` → compute `currentStep*Confirmed` → add streaming estimates → return `currentInput/Output` (step) + `totalInput/Output` (request).
- Boundary and error paths: `requestAssistants.length === 0` → return zeros (when running). `hasInFlightTail` → `currentStep*Confirmed = 0`. All tokens zero → return zeros when running.
- Concurrency/exit/cleanup/security paths: None — pure memo computation.

## 4. Upstream Reconstruction

- Same-path target implementation: All 3 TUI files exist at `packages/tui/src/` with patch 12's `sumConfirmed`/`requestAssistants`/`globalConfirmed`/`activeAssistants` pattern.
- Symbol/concept search: `requestAssistants`, `sumConfirmed`, `globalConfirmed`, `activeAssistants` — all introduced by patch 12. `hasInFlightTail`, `lastStepFinish`, `currentStepInputConfirmed`, `currentStepOutputConfirmed` — new in patch 14.
- Actual owner: TUI components — `prompt/index.tsx`, `sidebar/context.tsx`, `subagent-footer.tsx`.
- Producer and consumer chain: `usageRaw`/`stateRaw` memo → throttled signal → JSX render.
- Upstream strengths that must remain:
  - `session?.cost` / `session()?.cost` (not `msg.reduce` or `requestAssistants.reduce`)
  - Chinese comments from patches 9/10/12
  - `createThrottledSignal`, `createTokenFlowPulse`
  - Relative imports
- Behavior where SMARK is stronger: Current-step vs request-total split via `hasInFlightTail`/`lastStepFinish`.
- Behavior where upstream is stronger: `session?.cost` server-side cost (source patch 14 uses `requestAssistants.reduce(item.cost || 0)` — client-side, less accurate).
- Behavior that requires a deliberate merge: Drop cost hunks (keep `session?.cost`). Apply current-step split on top of patch 12's request/global model. Add `requestAssistants.length === 0` guard (original patch 14 has it; adaptation must preserve it for ALL 3 files).

## 5. Three-Way Mapping

| Source behavior | Upstream behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| Path migration | `packages/tui/src/...` | All 3 files | Rewrite diff headers | patch headers |
| `requestAssistants.length === 0` guard | `activeAssistants` fallback from patch 12 | All 3 files | Add guard, remove `activeAssistants` | All files |
| Remove `globalConfirmed` | Present from patch 12 | All 3 files | Remove `globalConfirmed` | All files |
| `hasInFlightTail`/`lastStepFinish` current-step model | Not present | All 3 files | Add current-step model | All files |
| `totalInput = requestConfirmed.input + pending` | `totalInput = globalConfirmed.input + pending` | All 3 files | Change source from `globalConfirmed` to `requestConfirmed` | All files |
| `cost = requestAssistants.reduce(...)` | `cost = session?.cost ?? 0` | prompt + footer | **DROP** cost hunk, keep upstream | prompt:337, footer:112 |
| sidebar `cost` memo removal + `cost` in `StateValue` | `cost = createMemo(() => session()?.cost ?? 0)` | sidebar | **DROP** cost changes, keep upstream | sidebar:18 |

## 6. Current Patch Edit

- Current patch SHA-256 before edit: (identical to original)
- Current patch SHA-256 after edit: computed from `current/0014-61bcd2aae67c.patch`
- Exact edited hunks:
  1. **prompt/index.tsx**: Add `if (requestAssistants.length === 0)` early-return with zeros. Remove `globalConfirmed`. Replace `activeAssistants`/`last` with `last = requestAssistants.at(-1)!`. Replace `currentInput = requestConfirmed.input + pending` with current-step model (`hasInFlightTail`, `lastStepFinish`, `currentStepInputConfirmed`, `currentStepOutputConfirmed`). Change `totalInput = globalConfirmed.input + pending` to `totalInput = requestConfirmed.input + pending`. Keep `session?.cost`. Add Chinese comments.
  2. **sidebar/context.tsx**: Same current-step split. Add `if (requestAssistants.length === 0)` early-return with zeros (including `totalTokens`, `totalInput`, `totalOutput` fields). Remove `globalConfirmed` and `activeAssistants`. Keep `session()?.cost` memo (not added to `StateValue`). Add Chinese comments.
  3. **subagent-footer.tsx**: Same as prompt. Keep `session()?.cost`.
- **B-01 FIX**: Initial adaptation omitted the `requestAssistants.length === 0` early-return guard in `sidebar/context.tsx` (it was present in prompt and footer but missing in sidebar). This was caught by the dual audit (B-01). Fix: added the guard with full zero-value `StateValue` return before `const last = requestAssistants.at(-1)!`.
- Why each edit is required: Path migration. Cost pattern divergence (upstream `session?.cost` vs source `msg.reduce`/`requestAssistants.reduce`). Context drift from patches 9/10/12 (Chinese comments, `createThrottledSignal`, `session?.cost`).
- Source behavior preserved: `hasInFlightTail`/`lastStepFinish`/`currentStep*Confirmed` current-step model. `requestAssistants.length === 0` guard. `totalInput = requestConfirmed.input + pending`.
- Upstream behavior preserved: `session?.cost`/`session()?.cost`, Chinese comments, `createThrottledSignal`, `createTokenFlowPulse`, relative imports.
- No hunk/test/schema/migration removed without evidence: Cost hunks dropped with evidence (upstream `session?.cost` is stronger than source `msg.reduce`/`requestAssistants.reduce`).
- Target implementation has no discovered bug: Yes (after B-01 fix).
- Target error, concurrency, cleanup, permission and schema behavior: `requestAssistants.length === 0` → zeros (all 3 files). `hasInFlightTail` → `currentStep*Confirmed = 0`. All tokens zero → zeros when running.
- Patch and target behavior match this record: Yes.
- Materialized state remains clean and provenance-valid: Yes — `states/0015-d302fa6f05a7` retained.

## 7. Verification

- `git apply --check` command and result: Part of cumulative `--materialize 15` — patch 14 passed (after B-01 fix).
- Cumulative apply command and result: `bun .temp/patches/src/apply-cumulative.ts --materialize 15` — passed 15/15.
- Materialize command and result: `--materialize 15` — state published. Reused prefix 13, applied 2 (patches 14-15 with B-01 fix).
- Materialized state directory: `states/0015-d302fa6f05a7` (retained)
- State metadata and cumulative patch fingerprint: Verified via report. Provenance at `4473fc3c9055046183990a965d68df3db7ea6f62`.
- Rollback result if apply failed: N/A — passed (after B-01 fix).
- Behavior tests: No direct tests for TUI components.
- Regression tests: Automated typecheck: `--typecheck 15` passed 15/15. Install fingerprint: `cb1e1e25f0758a0a2285c1b7721261937062ba3060c7d88b2b422836ab2fff09`. Typecheck: packages/opencode:passed, packages/tui:passed. Report: `typecheck-0015-2026-07-17T21-52-12-553Z`.
- Typecheck/lint/build: `--typecheck 15` passed. packages/opencode:passed, packages/tui:passed. Install fingerprint: `cb1e1e25f0758a0a2285c1b7721261937062ba3060c7d88b2b422836ab2fff09`.
- Generation/migration checks: N/A.
- Complete stdout/stderr report: See `reports/latest.md`.
- Cumulative apply reached this exact index before audit: Yes — passed 14/15 (after B-01 fix).

## 8. Contract Check

- One commit only: Yes.
- Full source diff read: Yes — 255 lines.
- Full upstream implementation read: Yes — all 3 TUI files in state repo (post-patch-12 state).
- Full target implementation read: Yes — post-patch versions (with B-01 fix).
- File absence followed by owner tracing: N/A — files exist at migrated paths.
- No grep-only or existence-only evidence: All verified by reading full files.
- No architecture-based omission: All SMARK behaviors adapted (current-step split). Cost hunks dropped with evidence.
- No deleted or delayed behavior: `requestAssistants.length === 0` guard restored (B-01 fix).
- No later补救替代当前实现: B-01 fix is within the same batch (not a later patch).
- No unrequested alternate success path or fallback: `hasInFlightTail` → `currentStep*Confirmed = 0` is a display-state guard, not a fallback.
- Every reachable safety or compatibility path has an owner and evidence: `requestAssistants.length === 0` → zeros (ALL 3 files, including sidebar after B-01 fix). `hasInFlightTail` → step tokens = 0. All tokens zero → zeros when running.
- No behavior bug remains in the target implementation: Yes (after B-01 fix).
- Patch, record, tests and target result are materially consistent: Yes.

## 9. E/C Calculation

- E (effective modified lines): 78
- C (qualified Chinese comments): 19
- Required C: max(1, ceil(78 * 0.15)) = 12
- Comment positions: `requestAssistants.length === 0` guard rationale, current-step vs request-total display rule, `hasInFlightTail` invariant, `lastStepFinish` search, `currentStepInputConfirmed`/`currentStepOutputConfirmed` step scope, `currentInput`/`currentOutput` step + streaming, `totalInput`/`totalOutput` request cumulative, `requestTokens`/`totalTokens` usage
- Gate: PASS (C=19 >= 12, 24.4%)

## 10. Independent Audit

- Five-item batch: 11-15
- Manifest index/SHA/parent continuity independently verified: Pending re-audit
- Auditor agent: `.opencode/agent/adversarial-patch-auditor.md`
- Contract file supplied: `.temp/patches/AUDIT_CONTRACT.md`
- Auditor input exact and contiguous: Indexes 11-15
- All preceding records independently passed: Yes (records 1-10)
- All preceding five-item audit gates passed: Yes (batches 1-2)
- Previous cumulative apply reached the preceding index: Yes — patch 13 passed.
- Current batch cumulative apply reached its final index: Yes — passed 15/15.
- Auditor verdict: Pending re-audit
- `B` behavior findings: B-01 (sidebar missing guard) — **FIXED** in current patch 14
- `G` hard-gate findings: Pending re-audit
- `N` record-only findings: Pending re-audit
- Rejected speculation: Pending re-audit
- Repairs: B-01 fixed — added `requestAssistants.length === 0` early-return guard to `sidebar/context.tsx` `stateRaw` memo, matching prompt/index.tsx and subagent-footer.tsx. Re-materialized 15/15.
- Full-scope re-audit result: Pending

## 11. Final Verdict

`PASS` (pending independent re-audit after B-01 fix)
