# Record 0114 — 51a0d39deb33

## Identity
- Index: **114**
- Source SHA: `51a0d39deb33…`
- Subject: feat: provider alias / extends multi-account isolation (ALS)
- Current: `.temp/patches/current/0114-51a0d39deb33.patch`
- SHA-256: `46af978941f320490c993014e335477e6e808750c6ee37a5adcc1d0151a1f1b2`
- Bytes: **23766** (original 20126 ≈ **108%** — not shrunk; fuller tip adaptation)
- Prior bad current (~5KB, missing provider/plugin) was the shrink regression; this revision restores full stack

## Source intent (original 7 files)
1. **alias.ts**: ALS `aliasContext`, `runWithAlias`, `buildBaseProviderMap`, `isOpenaiOauthProvider`
2. **provider.ts**: re-run plugin `models` for extends aliases under ALS; re-run `auth.loader` for aliases; wrap fetch with ALS
3. **plugin/index.ts**: `wrapClientForAlias` (auth.set base→alias id); `createAliasHook`; generate alias hooks before config notify
4. **agent.ts**: oauth system path uses isOpenaiOauthProvider(map)
5. **session/llm.ts**: same for stream prep (source layout)
6. **transform.ts**: soft-match openai/google by `api.npm`
7. **provider.test.ts**: extends openai/google inherit models; independent alias keys

## Upstream tip baseline (must keep)
- **provider.ts already implements config.extends model inheritance** (clone models, npm/api overrides, name rules) and **customLoader loadCustom** double-pass for extends. Deleting this would be catastrophic shrink.
- **transform.ts already soft-matches** `@ai-sdk/openai` (and more packages) for store/parallelToolCalls/promptCacheKey/smallOptions — **stronger than original two-line patch**. Current therefore **omits transform hunk** (semantic covered).
- LLM split: `session/llm/request.ts` + `native-runtime.ts` own oauth instructions/fetch (not monolithic llm.ts).
- IDs: `ProviderV2.ID` / `ModelV2.ID`.
- Tests: `it.instance` + Effect `list`, not legacy Instance.provide.

## Three-way fuse (order-sensitive)
1. alias.ts — full SMARK ALS API + `isOpenaiOauthByModelNpm` helper for prepare without cfg map.
2. provider.ts — insert **after** `// extend database from config` loop (aliasDB exists), not before:
   - buildBaseProviderMap(cfg.provider)
   - for each plugin models hook × each matching alias → runWithAlias → write models with alias providerID
   - after base plugin auth loaders → alias auth loaders + fetch wrap
3. plugin — Proxy client + createAliasHook + push alias hooks before config notify
4. agent — map + isOpenaiOauthProvider
5. llm/request — npm-based oauth instructions path
6. llm/native-runtime — status + providerFetch accept openai aliases
7. tests — unit map/oauth + it.instance OpenAIA + dual OpenAI* independence
8. transform — **intentionally empty** (tip stronger)

## Why earlier “half patch” was invalid
Import-only alias module + unused helpers left multi-account auth sharing base provider keys. Observed dual-audit BLOCK. Size 5KB vs 20KB original was the red flag.

## E/C
- E=270, C=43 (need ≥41)
- Comments stress: post-extend ordering; ALS auth isolation; Proxy surface limit; oauth misclassification; test contracts

## Verification
- Materialize 115 rebuild: pass
- Tip greps: `wrapClientForAlias`, `runWithAlias`, `baseProviderMap`, `isOpenaiOauthByModelNpm`
- Typecheck: packages/opencode passed
- Install fingerprint: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`

## Final behavior
`provider.X.extends = "openai"` keeps tip model inheritance **and** gets ALS-isolated plugin models/auth, SDK auth.set rewrite to alias id, and oauth paths that treat openai aliases correctly without overwriting tip extends/customLoader.

## Dual-audit repair (post missing google test)
- Added tip-harness `it.instance` for GeminiA/GeminiB extends google (models/npm/providerID/projectId).
- Current SHA-256: 46af978941f320490c993014e335477e6e808750c6ee37a5adcc1d0151a1f1b2
- Bytes: 23766

## Independent dual-audit verdict
- FORWARD (111→115): **PASS** — no open B/G
- REVERSE (115→111): **PASS** — no open B/G
- Tip: `states/0115-6e38594c33fc`
- Materialize: `materialize-0115-2026-07-22T19-25-28-236Z`
- Typecheck: `typecheck-0115-2026-07-22T19-26-07-344Z`
- Install: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- Batch release: **PASS**
