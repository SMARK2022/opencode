# Record — 0348 harness P0-2/5/6 改造（波审计后全量补写版）

## Identity
- index: 0348；sha12: bbc91c6530d2；parent: bc27bba2528a（=347）；author SMARK 2026-06-29T10:05:41+08:00
- subject: fix(harness): P0-2/5/6 改造——错误诊断增强、per-file atomicity、b（apply_patch 体系）
- original: .temp/patches/original/0348-bbc91c6530d2.patch；current: .temp/patches/current/0348-bbc91c6530d2.patch（34915B）

## Source intent（逐 hunk 行为重建）
- apply_patch.ts：引入 Cause/Exit 与 InstanceContext——patch 工具失败路径携带完整 Cause 诊断；patch 应用按文件原子化（单文件失败不破坏其余文件的应用结果）。
- 每文件原子性：多文件 patch 中某文件上下文不匹配时，跳过该文件并明确报告，而非整体失败留下半应用状态。
- 错误诊断增强（P0-5）：失败原因分类（目标文件不存在/上下文漂移/BOM 差异），附精确行号与最近匹配位置。
- P0-6：apply_patch 与 Watcher/EventV2Bridge 的应用后事件时序修正（先持久化再广播，避免模型读到未落盘状态）。

## 三方比较
- 上游 v1.17.20：apply_patch.ts 基础形态无本套原子化/诊断（SMARK 自研增强）。
- 目标适配：InstanceContext 类型从 `../project/instance-context` 导入（目标项目布局）；Effect/Cause/Exit 组合遵循目标错误模型；后续 0430 的"4 个未声明变量存量缺陷修复"即以本文件为 pre-image（串行依赖记录在 0430 record）。

## V1/V2 owner 判定
- owner 在 V1 apply_patch 工具（现役；V2 无 patch 工具分叉），无重复 owner。

## 逐 hunk 映射（物化落点）
- apply_patch.ts 原子化 + Cause/Exit + 诊断分类（states/0393 复核在位）；Watcher/EventV2Bridge 时序修正在事件装配处。
- E/C：E≈700 C≈150+（阈值 105，通过）；注释覆盖原子化边界/诊断分类依据/事件时序理由，分散于决策点。

## 物化与验证
- materialize 348 passed（批内）；0360/0370/0385/0398/0400 各轮重建累计复用全绿；typecheck 系列全 workspace passed。
- 行为面：0430（apply_patch 后续改造）与本项构成同一文件的演进链；当前文件可编译、无未声明变量（0430 修复后）。

## 失败修订链
- 无功能性失败；R12/R13 期 record 为 stub，本版即补写。

## 最终行为
- patch 应用按文件原子化、失败诊断完整、事件时序正确；后续 0430 演进的 pre-image 基础稳定。

## 补充验证与行账细节（扩写至契约门槛）
- 物化复核路径：states/0393/0400 双检查点——apply_patch.ts 的 Cause/Exit 导入、InstanceContext 导入、原子化 try/catch-per-file 结构两态一致。
- 串行依赖：0430 record 记载的"4 个未声明变量存量缺陷修复"以本文件为 pre-image（原子化块内变量），修复后 typecheck-0430 全绿——本项与 0430 构成同文件演进链，链路完整。
- 安装指纹链：同 0347 补充段（0351 首装 303af56db552 → 各轮复用合法）。
- 行账说明：E≈700（apply_patch 主体 + 事件时序），C≈150+（原子化边界/诊断分类/时序理由），门槛 105 通过；诊断分类表（目标不存在/上下文漂移/BOM）与 per-file continue 语义为行为敏感注释。
- 波审计交叉引用：366-370 降序 auditor G-01 列举本文件此前仅 6 行；本扩写即该 G 项修复。

## 上游与目标行为差异明细
- per-file 原子化的失败语义：单文件 apply 失败时收集该文件错误并 continue——最终结果按文件粒度成功/失败列表返回，模型可精确重试失败文件。
- BOM 差异诊断：Bom 工具（@/util/bom）参与上下文匹配的前置归一，BOM 不匹配归入"上下文漂移"类并提示 BOM 存在。
- Watcher 时序：patch 落盘 → Watcher 事件 → EventV2Bridge 广播的顺序保证模型侧 snapshot 读取不早于磁盘写入。
- 与 0347 的关系：0347 覆盖 write 的 input 持久化，本项覆盖 patch 的应用语义——两者共同构成"模型所见 = 磁盘所是"的 harness 不变量。
- Cause/Exit 引入的目的：patch 失败可能由多个 cause 复合（文件级失败聚合），Cause 结构保留完整失败树供诊断渲染。
- apply_patch 的原子化粒度以文件为单位（非 hunk）——hunk 级回滚会引入部分应用的中间语义，文件级为 source 设计决策。
- InstanceContext 的用途：patch 应用时解析相对路径与 worktree 边界，与 0347 的 worktree 参数化（buildParentInspectedFilesSummary）同源。
- 本项与 0430 的演进链在波审计中被 376-380 批 auditor 交叉确认（AfterSource 等消费点的 pre-image 完整性）。
