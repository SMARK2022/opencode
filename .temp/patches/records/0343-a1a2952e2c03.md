# Record 0343-a1a2952e2c03

## Identity
- SHA: a1a2952e2c03a82c5cc8105ed2a787647c9404bc
- Parent: c632a9d1e958 (0342)
- Subject: fix(tui): 修复 BlockTool 折叠/展开后内容区渲染空白
- Status: PASS

## Source intent

### Hunk 1 — `@@ -2407,6 +2407,16 @@` (index.tsx, BlockTool new state)
The upstream commit adds three new reactive primitives to `BlockTool`:
- `hasPreview = createMemo(() => props.preview !== undefined)` — tracks whether a preview node exists
- `bodyMounted` signal + `createEffect` — body lazy-mounts on first expand (or immediately if no preview), then stays mounted permanently. This avoids paying diff/tree-sitter render cost on first paint while preventing the remount-after-destroy bug.

The core fix: OpenTUI's renderable cannot reliably remount the same JSX object after `<Show>` destroys it, causing blank content after collapse→expand→collapse. The new approach keeps preview and body permanently mounted once mounted, hiding via `maxHeight=0 + overflow=hidden` instead of `<Show>` mutual exclusion.

### Hunk 2 — `@@ -2452,15 +2462,27 @@` (index.tsx, BlockTool JSX restructure)
Replaces the single `<box><Show>` block with two `<Show>`-wrapped `<box>` blocks:
- **Preview box**: visible when collapsed (marginTop=1), hidden when expanded (maxHeight=0, overflow=hidden)
- **Body box**: lazy-mounted via `bodyMounted()`, hidden when collapsed-with-preview (maxHeight=0), falls back to `previewLines` clipping for no-preview blocks (TodoWrite compatibility)

### Test file hunk — `@@ -1074,6 +1074,71 @@` (session-message-render.test.tsx)
Adds a new regression test "notebook source card keeps content after collapse and re-expand" that exercises the collapse→expand→collapse→expand cycle. Also adds an optional `output` parameter to `completedToolPart` helper for real tool output passthrough.

## Three-way comparison

| Aspect | SMARK original | Upstream (dev) | Target (0340 state) |
|---|---|---|---|
| Source path | `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` | same | `packages/tui/src/routes/session/index.tsx` (migrated per 0299) |
| Test path | `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` | same | **Does not exist** in target state |
| `collapsed` memo | exists: `createMemo(() => collapsible() && !expanded())` | exists | **Does not exist** — target inlines `canCollapse() && !expanded()` |
| `collapsible()` | exists as memo/signal | exists | Target uses `canCollapse()` signal instead |
| BlockTool line range | ~2407-2470 | same | ~2684-2800 (different line numbers due to TUI package refactoring) |

## Target adaptation

### Path mapping
- Source: `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx` (migrated per 0299 precedent)
- Test: `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` → **SKIPPED** (file does not exist in target state; the TUI migration did not carry this test file)

### `collapsed` memo introduction
The upstream's BlockTool already has a `collapsed = createMemo(() => collapsible() && !expanded())`. The target inlines this as `canCollapse() && !expanded()` everywhere. The adaptation introduces a `collapsed = createMemo(() => canCollapse() && !expanded())` (mapping `collapsible()` → `canCollapse()`) so the new JSX code can use `collapsed()` verbatim from the upstream, minimizing semantic divergence. This is a faithful structural adaptation — the target's `canCollapse()` is the equivalent of the upstream's `collapsible()`.

### JSX structure adaptation
The target's existing BlockTool JSX uses `canCollapse() && !expanded() && !props.preview` in the box's `maxHeight`/`overflow` props, while the upstream uses `collapsed() && !props.preview`. After introducing the `collapsed` memo, the replacement maps cleanly: the old `<box><Show>` block is replaced with the new preview/body structure using `collapsed()`, `hasPreview()`, and `bodyMounted()` — all verbatim from the upstream's new code.

### Test file skip
The test file `session-message-render.test.tsx` does not exist in the target state (neither in `packages/tui/test/` nor in `packages/opencode/test/cli/cmd/tui/`). The TUI migration (0299+) did not carry this test file. The `completedToolPart` helper change (adding optional `output` parameter) and the new regression test are skipped. The source fix (BlockTool) is the core change and applies fully.

## Per-hunk mapping table

| Hunk | Upstream file | Target file | Adaptation |
|---|---|---|---|
| 1 (state) | `packages/opencode/.../index.tsx:2407` | `packages/tui/.../index.tsx:2728` | Add `hasPreview`, `collapsed`, `bodyMounted`, `createEffect` after `expanded` signal; introduce `collapsed` memo (not in target) |
| 2 (JSX) | `packages/opencode/.../index.tsx:2452` | `packages/tui/.../index.tsx:2778` | Replace `<box><Show>` with preview/body `<Show>` structure; oldString uses `canCollapse() && !expanded()` (target inline style) |
| 3 (test) | `packages/opencode/test/.../session-message-render.test.tsx:1074` | **SKIPPED** | Test file not migrated to TUI package |
| 4 (helper) | `packages/opencode/test/.../session-message-render.test.tsx:2060` | **SKIPPED** | `completedToolPart` helper not in target |

## Verification

- **Materialize**: `passed 343/343` — patch applies cleanly to state 0342
- **E/C**: E=26, C=5, need=4, **PASS** — the 3 new CJK comment lines (OpenTUI renderable explanation + body lazy-mount rationale) cover the 26 executable lines (memo definitions, signal creation, effect, JSX attributes)
- **Typecheck**: packages/tui passes `tsgo --noEmit` (exit 0)

## 不一致 (target structure differences)

1. **`collapsed` memo missing in target**: The upstream's BlockTool has `const collapsed = createMemo(() => collapsible() && !expanded())` as a pre-existing primitive. The 0340 target inlines `canCollapse() && !expanded()` in every usage site instead. The adaptation introduces `collapsed = createMemo(() => canCollapse() && !expanded())` (mapping `collapsible()` → `canCollapse()`) to enable verbatim reuse of the upstream's new JSX code. The `canCollapse()` signal in the target is monotonically increasing (only goes false→true) which is semantically equivalent to the upstream's `collapsible()` for the `collapsed` derivation.

2. **Test file not migrated**: `session-message-render.test.tsx` does not exist in the target. The TUI migration (patches 0299+) split the TUI source into `packages/tui/` but did not carry the render test. The regression test for collapse→expand→collapse→re-expand and the `completedToolPart` `output` parameter are skipped. The source fix is complete and self-contained.

3. **BlockTool line numbers**: The target's BlockTool starts at ~line 2684 (vs upstream's ~2407) due to TUI package refactoring that added/moved code above BlockTool. The relative structure within BlockTool is otherwise identical.
