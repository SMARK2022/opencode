# Record 0271-99f73306645c

## Patch Identity
- Index: 0271
- SHA12: 99f73306645c
- Subject: fix(test): exitcode 修复，增加每次运行 test 后的重置退出码功能
- Original SHA-256: f33424db582efb8c8554fabb97ede65f90b8668bdefcec50ac35b397b8ea2c64
- Original bytes: 1672
- Current SHA-256: b289618b82c7ecabfc436c4566b52956b0634a26bfd9d15e5aef2426ef69414b
- Current bytes: 1841
- Files: packages/opencode/test/cli/tui/thread.test.ts

## Source Intent
源 patch 的完整行为目的和 invariant 见 original/0271-99f73306645c.patch。

## Adaptation Details
目标 thread.test.ts 使用子进程测试模式（cliIt.live + opencode.spawn），无现有 afterEach 块、无 Flock/Flag import、无 const stop。
源 patch 期望的上下文（afterEach + ThreadModule._setSpawn + mock.restore）在目标中不存在。
适配方案：
1. 在 import 行添加 afterEach（从 bun:test 导入）
2. 在 mock import 后添加 const originalExitCode = process.exitCode
3. 在 describe 块开头添加 afterEach(() => { process.exitCode = originalExitCode })
注释位置：
- #271：保护测试进程的退出码，避免 TuiThreadCommand 设置的 exitCode 在测试间泄漏（模块顶层）
- #271：每次测试后重置退出码，避免检验分支设置的 exitCode 影响后续用例（afterEach 块）

## E/C Calculation (独立计算)
- E=5 C=2 need=1 PASS
- E (独立计算): 5
- C (独立计算): 2
- 注释分布：见上方适配详情中的注释位置列表

## Three-way Comparison
- Source (SMARK): 原始 patch 的完整行为语义
- Upstream (v1.17.20): 目标基线的已有实现，作为不可删除基线
- Target (current): 适配后的实现，保留上游优势 + SMARK 全部有效语义

## Target State
- Baseline: 0270-f9dec59534d4 (batch 266-270 PASS)
- Materialized: 275/275 passed
- Typecheck: opencode exit 0, tui exit 0, core exit 0
- packages/app TS1128 pre-existing (不阻塞)

## Verification
- materialize 275: passed 275/275
- opencode typecheck: exit 0
- tui typecheck: exit 0
- core typecheck: exit 0
- install 指纹: reused

## Hunk Mapping
- Source hunks: preserved or adapted per target architecture
- No test deletions
- No source behavior omissions
- No empty current patches


## Source Behavior Analysis
源 patch 在 thread.test.ts 中：
1. 模块顶层添加 const originalExitCode = process.exitCode（在 const stop 后）
2. afterEach 块中添加 process.exitCode = originalExitCode
3. 添加英文注释说明 TuiThreadCommand 设置 process.exitCode 的原因

源动机：TuiThreadCommand.handler 在 tui.ts 中 5 处设置 process.exitCode = 1。进程内测试直接调用 handler 时 exitCode 会泄漏到测试进程。

## Upstream Comparison
目标 v1.17.20 的 thread.test.ts（432 行）使用不同测试架构：
- 子进程模式：cliIt.live + opencode.spawn，exitCode 在子进程内
- 进程内测试：调用 discoverOrSpawnDaemon，不调用设置 exitCode 的 handler
- yargs 测试：handler 被替换为 () => {}
- 无 afterEach 块，使用 try/finally 清理
- 无 Flock/Flag import，无 const stop

## Target Adaptation
1. 添加 afterEach 到 import
2. 添加 const originalExitCode = process.exitCode
3. 在 describe 开头添加 afterEach 块

## Hunk Mapping
| Source Hunk | Source Context | Target Adaptation |
|---|---|---|
| Hunk 1 | const stop = new Error('stop') | 添加到 mock import 后 |
| Hunk 2 | afterEach + ThreadModule._setSpawn | 新建 afterEach 在 describe 开头 |

## Failure Revision History
- 无失败修订

## Final Behavior
每次测试后重置 process.exitCode，防止校验分支设置的 exitCode 影响后续用例。

## Verification Commands
- Materialize: bun .temp/patches/src/apply-cumulative.ts --materialize 271
- Typecheck: cd packages/opencode && bun typecheck (exit 0)
- E/C 独立计算方法：逐行扫描 patch 中 + 开头行，排除 +++ 头和空行；// 开头且含中文且 >12 字符计为 C；其余非空非注释计为 E
- 安装指纹: reused (与 270 一致)

## Current Patch SHA-256
- 编辑前: 1708 bytes (original == current)
- 编辑后: 见 record header

## Comment Position Detail
1. 模块顶层 (line ~22): // #271：保护测试进程的退出码，避免 TuiThreadCommand 设置的 exitCode 在测试间泄漏
2. describe 内 afterEach (line ~30): // #271：每次测试后重置退出码，避免检验分支设置的 exitCode 影响后续用例


## DUAL-AUDIT SEAL（批次 271-275）
## DUAL-AUDIT SEAL（批次 271-275，第一轮）
- 正向审计 (ses_013c01df5ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013c001adffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 275: passed 275/275
- opencode typecheck exit 0, tui typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 271=5/2, 272=7/2, 273=66/12, 274=0/0, 275=0/0 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
