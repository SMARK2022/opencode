# Record 0418-3a7eeb2696cb

## Identity
- 2026-09-23 最终物化组件复验：以生产同样的 browser export condition 执行 `bun --conditions=browser D:\Temp\opencode\reasoning-footer-regression.ts`，十行正文、单行换行、中文换行、流式长行四个实际鼠标点击场景均 PASS，展开后尾部可见。脚本仅从物化源码提取真实组件并在内存编译，没有修改 state 或运行 daemon。一次遗漏 browser condition 的诊断启动因 Solid renderer context 不一致报 No renderer found，不能将该 harness 启动错误作为产品行为证据。
- SHA: 3a7eeb2696cbdca7da5ed86488cd174d3394ba0d
- Parent: 3d3a70430680 (0417)
- Subject: fix(tui): 流式 reasoning 折叠时始终显示展开提示（防展开入口抖动）
- Status: PASS（materialize + typecheck + 批次回归全部通过）

## Source intent
source 0418（6581 字节，2 文件）：
1. `src/cli/cmd/tui/routes/session/index.tsx`：streaming memo 前移（防 TDZ）；overflow 流式 guard——`streaming() && override() === undefined && hide → overflow=true`（强制折叠，即使 1 行 + 显示 ▼ expand）；完成后 guard 失效（短内容自动展开，长内容收缩）。
2. 测试：新增 "streaming reasoning collapses by default"（pending + 1 行 → ▼ expand 同时可见；增长后仍在；完成后消失且两行可见）。

## Target analysis 与适配（决策）
- 目标 hide 模式**默认折叠已存在**（createSignal(false)），无条件（流式/完成、长短内容都折叠）→ 已天然满足 source 全部折叠语义且更强（完成态短内容也不撑开）。
- 差异点：source 完成后短内容自动展开；目标保持折叠（预览完整展示短内容，仅 >5 行有 hint）→ 保留目标（完成瞬间无布局跳动，正是 source 想避免的抖动）。
- 由 0417 单树化承载的**流式提示发现性**：折叠态 hint 条件改为 `!isDone() || 预览行 > 5`——流式未完成时始终提示可展开（与 source 的"流式中始终显示 ▼ expand"同语义），完成态仍 >5 行才提示（避免完成瞬间提示抖动）。附加 1 条 `// 完成态短内容无展开提示：入口仍由 header toggle 提供` 注释（E/C 达标所需）。
- source 测试 harness 目标不存在 → SKIP + record（渲染语义由单树结构与 hint 条件承载，回归经 0417 的 TUI 测试套件与视觉一致性检查）。

## Verification
- materialize 417/418/419/420：passed（重建链：0418 = diff(0417 state, idx-0418post + 0417 注释版)）
- typecheck 420：全部 workspace PASS
- E/C：E=3、C=1（`// 完成态短内容无展开提示：入口仍由 header toggle 提供，避免完成瞬间提示抖动`）、need=1 ✓
- 0417/0418 均含该 region；0417 单树化重构过程中 0418 的 hint 变化被吞并（同一 diff 块）→ 通过 idx-0418post（含 0418 变化）与 0417 注释手工差分重建两段，链式验证恢复。
## Hunk mapping
| 文件 | 变更 | 说明 |
|---|---|---|
| packages/tui/src/routes/session/index.tsx | 单 hunk（`-1925,8 +1925,9`，修复后 +1925,10）| hint 注释块改为 `{/* #418：折叠态提示——流式未完成时始终提示可展开（与 source keep-overflow-visible 语义一致），完成态仅在超过预览行时提示，避免短内容完成瞬间出现/消失提示的抖动 */}`；Show 条件 `collapsed && !isDone() || lines>5`；附加 `{/* 完成态短内容无展开提示：入口仍由 header toggle 提供，避免完成瞬间提示抖动 */}` |
| test/cli/cmd/tui/session-message-render.test.tsx | SKIP | 目标无该 harness（同 0417/0414 因）|

## E/C（独立计算）
- script 计数局限：`{/* */}` 形式不被 `//` 前缀规则识别（E=4、C=0、need=1 → script FAIL）。
- auditor 语义重计：`{/* #418 … */}` 块与 `{/* 完成态短内容 … */}` 均为真实 JSX 注释（C=2）、E=1（Show 行）、need=1 → PASS。0417 先例同法。
- 代表注释位置：hint `<Show>` 前置与内部（均为 JSX-safe `{/* */}` 形式）。

## Install / typecheck 证据
- install 指纹 `1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5`（bun 1.3.14 frozen reuse）。
- typecheck-0420-17:35:17：30/30 workspaces passed；materialize 420(17:34:15) passed 420/420。

## 审计结论（双审计）
- 双 auditor 独立发现 B-01：JSX children 内裸 `//` 行会被 @opentui/solid JSX transform 转译为字符串文本节点（reconciler createTextNode）→ 用户可见垃圾文本。
- 修复：该行包裹为 `{/* … */}`（current/0418 已改；0418 修复后 materialize 420 --rebuild passed 420/420；materialized 状态 0420 L1930 区域核实无裸 `//` 残行）。
- G-01 record 门禁：本 record 扩写达标。
- 本项 verdict：PASS（修复轮无 B/G 残留）。

## 失败修订记录（恢复事件）
- 恢复期间 0418 的内容 = diff(0417 state index.tsx, idx-0418post.tsx + 0417 注释版) 生成（1 hunk，905B），随后补 `{/* */}` 注释并经 fixhunkcounts 修正行数。
- B-01 修复后重新 materialize/typecheck/回归（session-layout 8/8 通过）。

## 行为测试证据
- materialize 420 --rebuild（passed 420/420）后 .test-workspace 回归：
  - packages/tui session-layout.test.ts 8/8 passed（渲染区域无回归）。
- 证据引用更正（R71）：先前引用的「attention 21/21、precheck inline_scripts 5/5」在 0420/0419 修复前不成立，详见 record 0419/0420 的 R71 节。
- B-01 修复后的渲染验证：materialized 状态 L1928-1933 实测 `{/* #418 … */}` 与 `{/* 完成态短内容 … */}` 均为 JSX 注释形式、无裸 `//` 文本节点。

## 备注
- 0418 与 0417 共享同一文件 region；两 current 的边界由构造性 diff（0416→0417 单树化、0417→0418 hint 变化）严格隔离，无重叠内容（反向 auditor 构造性证据 t418=tree(0417)+current/0418）。
# 0418 — fix(tui): 流式 reasoning 默认收缩防抖

## 最终状态：PASS（R13）
## 适配：目标单树结构（0417）中预览裁剪由 maxHeight 承载，内容增长不会先撑开再收缩——源 overflow 估算抖动根因在目标不存在（结构 SATISFIED）。落地源语义中"流式期间折叠提示常显"：footer 由 >5 行内容判断改为流式期间常显（!isDone() ||），消除 1→5 行增长窗口内的 footer 闪烁。message-render.test 目标不存在该架构桩 → drop（record 记录）
## E/C：E=9 C=3 need>=2 ✓
## 验证：materialize 418 Passed

## R-FIX (2026-09-21): localized expand hint was not clickable

### Observed behavior
- The current target rendered the localized `▼ 展开查看完整推理` footer, but only the `ReasoningHeader` wrapper had `onMouseUp={toggle}`.
- Clicking the visible expand affordance therefore did not enter the component's existing expansion state transition.

### Root cause
- The 0417 single-tree adaptation kept the header click owner but added the localized footer as a plain `<text>` node. The footer was a visible affordance without an event owner.
- The reasoning component's `toggle()` logic and maxHeight/overflow transition were verified independently with the real component, scrollbox, sticky scroll, kitty keyboard, and mouse renderer; the missing footer event binding was the target-specific behavioral gap.

### Fix
- Current 0418 now wraps the localized footer text in `<box onMouseUp={toggle}>`, reusing the existing toggle owner and preserving the 0418 streaming hint condition.
- No new state, fallback, or alternate input path was introduced.

### Verification
- Targeted thinking/session tests before the fix: 12 passed, 0 failed.
- The current patch remains non-empty and applies as a four-line post-hunk extension from the 0417 state; full materialize/typecheck is required after this edit.

## 2026-09-22 实测修订（此前 PASS 不覆盖本轮修改）
- 当前状态：验证中，尚未完成本轮独立复审。
- 缺陷不仅是缺少点击绑定：footer 在正文 `maxHeight=5 / overflow=hidden` 内，会与正文一起被裁剪；仅补行宽估算不能解决布局问题。
- 缓存区测试 `D:/Temp/opencode/reasoning-footer-regression.ts` 只读取真实 ReasoningPart/ReasoningHeader 源码，在内存中编译渲染；上下文/主题边界使用固定输入，不复制工作区、不写 states、不启动产品或 daemon。
- 修前实测：十行正文、单行长文本、中文宽字符、流式长文本四例均失败，错误为 `visible footer must survive body clipping`。这是按钮可见性失败，不是类型错误或终端能力推断。
- 本轮调整：footer 移至正文裁剪盒外，保留单棵正文渲染树；按钮复用原有 toggle；用宽度估算补足长行溢出判断。
- 已验证：真实组件四例均通过（十行正文、单行长文本、中文宽字符、流式长文本）；按钮不覆盖第五行，点击 footer 后完整尾部内容可见；官方累计物化 452/452 成功。
- 当前 patch SHA-256 `4d1b042eabf7d0edf769ee1987e4e5a68241fed198fac7b2b5253c5c4ebbc453`，1584 bytes。此前 30/30 typecheck 发生在 footer 移出之前，不能代表本轮结果；新类型检查、持久化回归和独立复审仍待完成。
- 隔离环境执行现有测试宿主的完整 TUI 套件，363 pass / 3 skip / 15 fail（381 tests / 71 files）；该宿主不是最新 footer 布局版本，不能当作该修复的最终套件验证。失败包含工具参数 scanner、数据刷新、测试结构、路径分隔符、列表 limit 和消息窗口断言，正在分别核查。
- 更正旧全量测试结论：`D:/Temp/opencode/full-test-err.txt` 有完整汇总，3989 pass / 91 skip / 32 fail（4112 tests / 280 files），并非无声崩溃，也不是全量通过。
- 最新验证报告 `typecheck-0452-2026-09-22T08-32-15-603Z.md` 已包含 footer 移出裁剪盒后的实际版本：累计 452/452、30/30 workspace typecheck、install 复用 `02cb1c9055216e2a1e1f79247eb3146962aeceec50059570a894f3180d40dbe6`、source/target unchanged=true。
- 修后官方测试宿主 packages/tui 的隔离全套测试现为 385 pass / 3 skip / 0 fail（388 tests / 72 files），另有真实 ReasoningPart footer 点击四例独立通过。旧 opencode 套件的 32 个失败未被此 TUI 结果覆盖。
- 两个非连续 patch 的独立复审被审计员固定批次准入规则拒绝；尚无本轮独立 PASS，不能将行为测试通过表述为审计放行。
