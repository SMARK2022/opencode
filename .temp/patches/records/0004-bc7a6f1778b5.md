# Patch Record 0004

## 1. Identity

- Index: 4
- Source SHA: `bc7a6f1778b518523c4c04009a93e940bb22c2eb`
- Parent SHA: `a461c299ff2a104ebdf3a5e94944ce6a80057c70`
- Subject: `fix(session): 修复获取 git 上下文的调用方式，确保正确执行`
- Original patch: `original/0004-bc7a6f1778b5.patch`
- Current patch: `current/0004-bc7a6f1778b5.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Pinned source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Previous materialized state: cumulative state through index 3 (not retained; verified via `--typecheck 10`)
- Current materialized state: `states/0010-fc6394523010` (cumulative through batch 2 end)

## 2. Source Diff Evidence

- Exact command: `git show --format=fuller --no-ext-diff --binary --find-renames bc7a6f1778b518523c4c04009a93e940bb22c2eb`
- Complete diff read: Yes, full 34-line original patch read.
- Changed paths:
  - `packages/opencode/src/session/system.ts` — 2 hunks
- Every hunk and behavior intent:
  1. **system.ts hunk 1 (import fix)**: Change `import { Flag } from "@/flag"` to `import { Flag } from "@/flag/flag"` — fix broken barrel import path.
  2. **system.ts hunk 2 (call fix)**: Change `const gitContext = yield* getGitContext` to `const gitContext = yield* getGitContext()` — `getGitContext` is an `Effect.fn` which returns a function; `yield*` of a bare function value is a type/runtime error; must invoke `()` to get the `Effect`.
- Tests/config/schema/migration/generated changes: None.

## 3. SMARK Intent

- User-visible behavior: Fix a runtime bug where `getGitContext` (an `Effect.fn`) was not invoked, causing `yield*` to receive a function value instead of an `Effect`, which would crash or produce incorrect behavior when building the system prompt.
- Protected invariant: `Effect.fn("name")(generator)` returns a function that must be called to produce an `Effect`. `yield*` requires an `Effect`, not a function.
- Normal path: Call `getGitContext()` to produce the `Effect`, then `yield*` to execute it and get the git context string.
- Boundary and error paths: N/A — this is a fix to the normal path.
- Concurrency/exit/cleanup/security paths: N/A.

## 4. Upstream Reconstruction

- Same-path target implementation: `packages/opencode/src/session/system.ts` EXISTS (post-patch-1 state, 214 lines).
- Symbol/concept search when same path is absent:
  - `import { Flag } from "@/flag"` → DOES NOT EXIST in target. The adapted patch 1 replaced `Flag` with `RuntimeFlags` from `@/effect/runtime-flags`. There is no `Flag` import in the target's `system.ts` at all.
  - `import { Flag } from "@/flag/flag"` → DOES NOT EXIST. The `packages/opencode/src/flag/` directory does not exist in the target. `Flag` moved to `packages/core/src/flag/flag.ts` (imported as `@opencode-ai/core/flag/flag`), but `system.ts` no longer imports it — it uses `RuntimeFlags.Service` instead.
  - `getGitContext` → EXISTS at target line 70 (definition) and line 129 (buggy call site). `getGitContext` is defined as `Effect.fn("SystemPrompt.gitContext")(function* () { ... })` — confirmed it returns a function.
- Actual owner: `packages/opencode/src/session/system.ts` — `SystemPrompt` service layer, `environment` function.
- Producer and consumer chain: `getGitContext` is defined in the layer closure; consumed by `environment` function which calls `yield* getGitContext` (buggy) → should be `yield* getGitContext()`.
- Upstream tests and fixtures: No direct tests.
- Upstream schema/migration/generation behavior: None.
- Upstream strengths that must remain: `Effect.fn` pattern, `RuntimeFlags` service, `InstanceState.context`, all upstream architecture from patch 1.
- Behavior where SMARK is stronger: The bug fix itself — calling `getGitContext()` correctly.
- Behavior where upstream is stronger: `RuntimeFlags` service pattern (replaced `Flag` env var pattern).
- Behavior that requires a deliberate merge: Hunk 1 (import fix) is OBSOLETE — the `Flag` import was removed by the adapted patch 1. Hunk 2 (call fix) is still valid and needed.
- Behavior proved fully equivalent upstream: `getGitContext` definition is identical between source and target (both use `Effect.fn`).

## 5. Three-Way Mapping

| Source behavior | Upstream behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| `import { Flag } from "@/flag"` → `"@/flag/flag"` | No `Flag` import in target (uses `RuntimeFlags`) | system.ts | DROP hunk — obsolete, `Flag` not imported | system.ts has no Flag import |
| `yield* getGitContext` → `yield* getGitContext()` | Same bug exists in target (line 129) | system.ts | KEEP hunk, re-contextualize for target's surrounding lines | system.ts line 129 |

## 6. Current Patch Edit

- Current patch SHA-256 before edit: (identical to original)
- Current patch SHA-256 after edit: computed from `current/0004-bc7a6f1778b5.patch`
- Exact edited hunks:
  1. **Hunk 1 (import fix)**: DROPPED. The `Flag` import does not exist in the target's `system.ts`. The adapted patch 1 replaced `Flag` with `RuntimeFlags` from `@/effect/runtime-flags`. The `@/flag` and `@/flag/flag` module paths do not exist in the opencode package. Applying this hunk is impossible and semantically wrong.
  2. **Hunk 2 (call fix)**: KEPT with re-contextualized context. The target has `const ctx = yield* InstanceState.context` (line 128) before the buggy line, and `const references = yield* Effect.gen(...)` (lines 130-132) after it — these are not in the source patch's context. Added comment: "getGitContext 是 Effect.fn，返回的是函数而非 Effect，必须调用以获取 Effect".
- Why each edit is required: Hunk 1 dropped because `Flag` import was replaced by `RuntimeFlags` in adapted patch 1. Hunk 2 re-contextualized because the target has additional lines (`const ctx`, `const references`) inserted by the adapted patch 1.
- Source behavior preserved: The bug fix (`getGitContext()` call) is fully preserved.
- Upstream behavior preserved: All upstream architecture from patch 1 preserved.
- No hunk/test/schema/migration removed without evidence: Hunk 1 dropped with evidence — `Flag` import does not exist in target, `@/flag` path does not exist.
- Target implementation has no discovered bug: Yes — the `getGitContext()` call bug is fixed.
- Target error, concurrency, cleanup, permission and schema behavior: `Effect.fn` must be invoked to produce an `Effect`; `yield*` on a bare function would be a type error.
- Patch and target behavior match this record: Yes.
- Materialized state remains clean and provenance-valid: Yes.

## 7. Verification

- `git apply --check` command and result: Part of cumulative `--materialize 5` — patch 4 passed.
- Cumulative apply command and result: `bun .temp/patches/src/apply-cumulative.ts --materialize 5` — passed 5/5.
- Materialize command and result: `--materialize 5` — state published.
- Materialized state directory: `states/0010-fc6394523010/repo`
- State metadata and cumulative patch fingerprint: Verified.
- Rollback result if apply failed: N/A — passed. (Note: initial hand-written patch had incorrect hunk header line counts causing "corrupt patch at line 13"; fixed by using `diff -u` to generate correct counts.)
- Behavior tests: No direct tests for `SystemPrompt.environment`.
- Regression tests: `--typecheck 10` on v1.17.20 baseline — packages/opencode:passed.
- Typecheck/lint/build: `--typecheck 10` passed 10/10. Install fingerprint: `041e15a7234252aeb9cda2a6a04f62fd306e39973291ff9e97480567ba62a7d4`. No typecheck errors in `system.ts`.
- Generation/migration checks: N/A.
- Complete stdout/stderr report: See `reports/latest.md`.
- Cumulative apply reached this exact index before audit: Yes — passed 4/5.

## 8. Contract Check

- One commit only: Yes.
- Full source diff read: Yes — 34 lines.
- Full upstream implementation read: Yes — `system.ts` post-patch-1 (214 lines), `runtime-flags.ts` (80 lines), `git/index.ts` for `Effect.fn` pattern.
- Full target implementation read: Yes — post-patch `system.ts` (216 lines).
- File absence followed by owner tracing: `@/flag` → traced to `@opencode-ai/core/flag/flag` (but not imported in system.ts); `Flag` → traced to `RuntimeFlags.Service`.
- No grep-only or existence-only evidence: Verified by reading full files and `Effect.fn` definition.
- No architecture-based omission: Hunk 1 dropped because the import it fixes doesn't exist — not an architecture-based omission but a factual absence.
- No deleted or delayed behavior: None — the bug fix is applied.
- No later补救替代当前实现: None.
- No unrequested alternate success path or fallback: None.
- Every reachable safety or compatibility path has an owner and evidence: `getGitContext()` call fix — owner is `system.ts` `environment` function.
- No behavior bug remains in the target implementation: Yes — the call bug is fixed.
- Patch, record, tests and target result are materially consistent: Yes.

## 9. Independent Audit

- Five-item batch: 1-5
- Terminal two-item batch exception, if applicable: No
- Manifest index/SHA/parent continuity independently verified: Pending audit
- Auditor agent: `.opencode/agent/adversarial-patch-auditor.md`
- Contract file supplied: `.temp/patches/AUDIT_CONTRACT.md`
- Auditor input exact and contiguous: Indexes 1-5
- All preceding records independently passed: Pending (records 1-3 pending audit)
- All preceding five-item audit gates passed: N/A (first batch)
- Previous cumulative apply reached the preceding index: Yes — patch 3 passed.
- Current batch cumulative apply reached its final index: Yes — passed 5/5.
- Auditor verdict: Pending
- `B` behavior findings: Pending
- `G` hard-gate findings: Pending
- `N` record-only findings: Pending
- Rejected speculation: Pending
- Repairs: Pending
- Full-scope re-audit result: Pending

## 10. Final Verdict

`PASS` (pending independent audit)
