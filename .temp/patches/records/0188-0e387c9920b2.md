# Record 0188 — 0e387c9920b2

- Index: 188
- Source SHA12: 0e387c9920b2
- Batch: 186-190
- Updated: 2026-07-26T18:20:02Z
- Builder self-check: READY-FOR-AUDIT

## 1. Source intent

Update the `read` tool's instruction text (`read.txt`) to tell the model: after a successful `edit`, `write`, or `apply_patch`, skip re-reading the file to confirm — the tool itself errors if it didn't work. This reduces redundant tool calls.

Source: `packages/opencode/src/tool/read.txt` (1 line added)

## 2. Tip architecture

Tip has `packages/opencode/src/tool/read.txt` with different content from source (tip already has stub-status and range notes from earlier patches). The skip-reread instruction line is absent on tip.

Tip also has `packages/opencode/src/tool/read.ts` which loads `read.txt`.

## 3. Adaptation (source → tip mapping)

| Source | Tip |
|---|---|
| `packages/opencode/src/tool/read.txt` | same path |
| (no source change) | `packages/opencode/src/tool/read.ts` (added `#188` Chinese note for C gate) |

## 4. Implementation (hunk map)

### read.txt
- Added: `- After a successful edit, write, or apply_patch, skip re-reading the file to confirm—the tool errors if it didn't work.`
- Placed before the image/PDF bullet (tip-specific placement; content identical to source).

### read.ts
- Added `// #188：成功 edit/write/apply_patch 后禁止再 read 确认，工具自身会报错` at file top (C gate; semantic note for maintainers).

## 5. Verification

- materialize `passed 190/190`; tip `states/0190-8d2352918cad`.
- typecheck `typecheck-0190-2026-07-26T18-17-46-010Z.md`: all workspaces passed.
- No runtime tests (documentation-only patch).

## 6. LIVE IDENTITY

- Current: `.temp/patches/current/0188-0e387c9920b2.patch`
- Bytes: 1396
- SHA-256: `74685daeb18f7db32f846c68e5fba112fcad34b456be7723a8304fbeca339e6a`
- E=1, C=1, need=1, gate=PASS
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Tip: `states/0190-8d2352918cad`
## 7. Source vs tip content alignment

Source `read.txt` is a short instruction file for the `read` tool. The source patch adds exactly one bullet:
```
- After a successful edit, write, or apply_patch, skip re-reading the file to confirm—the tool errors if it didn't work.
```

Tip `read.txt` has evolved through earlier patches (stub status, range/offset notes, 16KB cap, image/PDF support). The skip-reread instruction is absent on tip and is added in the same spirit: reduce redundant confirmation reads after mutations that already self-report errors.

### Placement difference
- Source: appended at end of file.
- Tip: inserted before the image/PDF bullet (tip has additional trailing content from earlier patches that makes end-of-file placement less prominent).
- Content is byte-identical to source; only placement differs.

## 8. read.ts annotation rationale

`packages/opencode/src/tool/read.ts` loads `read.txt` as the tool description. A one-line `#188` Chinese comment was added at the file top to satisfy the C gate (Chinese decision comment on a git-touched modification point). The comment restates the behavioral constraint for maintainers: after successful edit/write/apply_patch, the tool itself errors on failure, so a confirmation read is unnecessary.

This annotation does not change runtime behavior — `read.ts` loads `read.txt` unchanged.

## 9. Why no runtime test

This patch modifies agent-facing instruction text only. The `read` tool's runtime behavior (file reading, error reporting) is unchanged. A test asserting "model does not re-read after edit" would require an LLM integration test, which is out of scope for a patch migration. The instruction text itself is the contract.

## 10. Risk assessment

- **Low risk**: Documentation-only change. No runtime logic modified.
- **Regression risk**: None — the instruction is additive; existing read behavior is unchanged.
- **Compatibility**: The instruction is agent-facing; it does not affect tool API or permissions.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-26T18:23:37Z
- FORWARD: BATCH PASS (ses_060825b0fffed22nrlffLRoNo5) — no open B/G
- REVERSE: BATCH PASS (ses_060825af9ffe2U4eGLGAoe5q9a) — no open B/G
- Tip: `states/0190-8d2352918cad`
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Focused tests: sync 4 pass; pending-tool-input 8 pass; session layout+spacing 6 pass; total 18/0
- Non-blocking N only (do not reopen seal): N-01 createPendingToolInputStats comment drift; N-02 frame harness adapted to headline contract; N-03 read.txt bullet placement; N-04 #190 E/C borderline; models-snapshot omission justified
