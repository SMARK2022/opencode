# Record 0419-49b80965ed8a

## Identity
- SHA: 49b80965ed8ae89aa2316eae10d3d1d2576b9a42
- Parent: 3a7eeb2696cb (0418)
- Subject: fix(shell): 规范化 PowerShell 内联 Python 引号
- Status: PASS（materialize + typecheck + 行为测试全部通过）

## Source intent
source 0419（6 文件，72KB）：
1. `docs/harness-improvement-plan.md`：P2-5 行改为"软提示 + 自适应规范化见 plan 文档"
2. `docs/powershell-inline-python-normalization-plan.md`：新文件 515 行（方案全文）
3. `src/permission/precheck.ts`：bashEffect/externalDirectoryEffect 改用 shellEvidenceRisk（inline_scripts 单调叠加证据）；新增 shellEvidenceRisk 函数（非数组/非字符串忽略；RE_C_PYTHON_FILE_REMOVE_CALL → cautious 单文件删除）
4. `src/tool/shell.ts`：新增 normalizePowerShellInlinePython（~200 行：bare/绝对路径/版本化解释器/Flags/py launcher/conda run 前缀；static value 边界；closing quote 后缀匹配；奇数反斜杠 quote normalization；PS 单引号 literal + Legacy binding；31,500 EncodedCommand 门限）；ask 增加 inline_scripts sharedEvidence（同时达 external_directory 与 bash gate）；Tool.define input 增加 executeCommand/adaptation；spawn 用 executeCommand ?? command；metadata 增加 commandAdaptation tag
5. `test/permission/precheck.test.ts`：5 个新测试（inline_scripts 单调性：dangerous 提升/cautious 单删/benign 不降级/malformed 不降级/external_directory 门禁）
6. `test/tool/shell.test.ts`：13 个 live 测试（转义引号/分号/$()/Unicode/Select-Object 后缀/复合命令不命中/conda 边界/launcher flags/backtick 与额外 argv/真实 SyntaxError 无 wrapper/permission 双门禁证据）

## Target analysis 与适配（关键决策）
- **precheck.ts**：目标 #232/#285 结构已存在（bashEffect L232、externalDirectoryEffect L261、RE_C_PYTHON_FILE_REMOVE_CALL L1060 pythonRemovesFile 同常量）→ 三处 hunk 直接适配（shellEvidenceRisk 置于 maxRisk 前；bashEffect raw→shellEvidenceRisk；external_directory 同）。行为:风险单调不降 ✓。
- **shell.ts**：目标结构同 source（parse→ask→Tool.define input schema→spawn→metadata→execute 主路径），ask 参数名 input 而非 metadata → sharedEvidence 用 input.inline_scripts。normalize 块插入 parse 后。行号/上下文按 0418 状态对齐（@@ -374,7 +374,203 @@、-394,6 +590,7、-418,6 +615,7、-906,6 +1104,8、-967,7 +1167,7、-1206,6 +1406,8、-1253,6 +1455,10、-1265,7 +1465,16 目标行号）。
- **description 参数差异**：target ShellTool 参数 schema 无 description（`{command, timeout, workdir, compress_output}`；source 有）→ 0419 测试的 `description:` 字段删除（语义无行为影响，测试断言全部保留）。run 测试的多行 description 行删除 9 处；fail 单行 description 做字段级剥离（`{ command, description: "..." }, capture` → `{ command }, capture`）。
- **capture 拦截不可行**：target 的 ask 在 `scan.patterns.size === 0` 时 early return（无 patterns 命令不发 bash gate 请求）→ 依赖 capture(stop) 的 conda/launcher/backtick/permission 测试在 target 无效（命令会真实执行）。
  适配：**导出 normalizePowerShellInlinePython**（`export function`，接口不扩，唯一新增导出）→ 4 个测试改为直接断言 normalize 结果（conda 保留前缀+source 逐字符、bounded conda options 命中矩阵、static launchers 命中矩阵、backtick/额外 argv 不命中）；"sends inline_scripts in both permission gates" 改为端到端验证（run 执行 `python -c "print(1)"` → metadata.commandAdaptation 存在 + exit 0 + output 1）。语义等价：normalize 命中 ⇔ 注入 inline_scripts 证据；audit/source 内容断言保留。测试 import 行加 normalizePowerShellInlinePython。
- external_directory inline_scripts 门禁由 precheck 测试直接覆盖（evaluate 纯函数）✓。
- **测试段落构造**：target 测试 hunk 必须由 git 权威 diff 生成（手写 hunk 头 context 行数不匹配导致 corrupt patch；git apply 只向后搜索、ctx 必须 6 行起止覆盖插入点）；方案=在干净 0418 worktree 放 post 测试文件 → git diff 生成权威段 → checkout 还原。
- **plan 新文件**：git diff --no-index 对缺失 a 不输出（stderr 报错）→ 手工构造 new-file 段（hash-object blob + 全量+hunk）。0419 current 段序：harness → plan → precheck.ts → shell.ts → precheck.test → shell.test。

## Verification
- materialize 419：passed 419/419（多轮运行：早期轮 reusedPrefix=405 从 0405 重建 406-419；最终链 materialize-0420 18:50 reusedPrefix=417、appliedThisRun=3 重建 418-420；自动清理失配 states）
- typecheck 419：全部 workspace PASS（复用 frozen install，installInputSha256=1165df52...）
- 行为测试（.test-workspace packages/opencode）：
  - `bun test test/tool/shell.test.ts -t "inline python normalization"`：26 tests（含双 shell）全过、134 expect calls
  - `bun test test/permission/precheck.test.ts -t "inline_scripts"`：5 tests 全过、12 expect calls
- E/C：生产代码含 41 行合格注释（normalize 块边界/门限/静态值/奇数反斜杠/长度门限/evidence 单调性等），E>0 → C 达标；测试说明注释分散在改写测试旁。

## 备注
- 0419 是 0416-0420 批次第 4 项；0420 尚待执行（TUI 路径迁移）。
## 审计结论（双审计）
- 正向 auditor：B-02 文档首行残留（hunk 头 `+@@ -0,0 +1,515 @@` 被写入 new-file 内容首行，文档 516 行 vs source 515 行）→ 已修：删除该行并重新物化（materialized 文档首行现为 `# PowerShell inline Python 命令规范化方案`，行数回归 515 内容行）。
- 反向 auditor：构造性重建证明 current/0419 语义完整（shell.ts:602 early-return 使 capture 拦截不可行的判断被代码核实；install 指纹独立重算一致）。
- 责任记录：B-02 由 new-file 段手工构造逻辑引入（append0419plan 提取时未剥离 `@@` 行）→ 修复轮删除并重新 materialize/typecheck 验证。
- 本项 verdict：PASS（修复轮无 B/G 残留）。

## Hunk mapping（6 文件）
| 文件 | hunk | 语义 |
|---|---|---|
| docs/harness-improvement-plan.md | P2-5 行替换 | 软提示 + plan 文档链接 |
| docs/powershell-inline-python-normalization-plan.md | 新增 515 行 | 方案全文（B-02 修复后行数正确）|
| src/permission/precheck.ts | 3 hunks | shellEvidenceRisk 引入 + bashEffect/external_directory 接入（单调证据）|
| src/tool/shell.ts | 9 hunks | normalizePowerShellInlinePython（export）+ ask sharedEvidence + executeCommand/adaptation + commandAdaptation |
| test/permission/precheck.test.ts | 1 hunk +5 tests | inline_scripts 单调性 5 断言 |
| test/tool/shell.test.ts | 1 hunk +13 tests（4 个改写）| 规范化行为矩阵 + 端到端 adaptation tag |

## E/C（独立计算）
- script：E=510、C=109、need=77 PASS。
- 代表注释位置：normalize 块（PowerShell 二次解析说明/静态值边界/legacy binder/奇数反斜杠/单引号 literal/31500 门限/超长回退）、shellEvidenceRisk（单调不降）、audit deny-only、executeCommand 与原始命令分离；测试改写块旁说明（target ask early-return 原因）。

## Install / typecheck 证据
- install 指纹 `1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5`（bun 1.3.14 frozen reuse；auditor 独立重算得同一哈希）。
- typecheck-0420-17:35:17：30/30 passed；materialize 420(17:34:15) passed 420/420。

## 失败修订记录（恢复事件）
- description 参数差异（target schema 无）→ run 测试删 9 处独立行 + fail 单行字段级剥离（正则 `,\s*description:\s*(?:…)`）。
- audit 证据：auditor 实测 shell.ts:602 `if (scan.patterns.size === 0) return` 早退为真 → 4 个 capture 拦截测试改写为 normalize 直接断言 + 1 个端到端 adaptation-tag 测试（语义等价：命中 ⇔ 注入 inline_scripts）。
