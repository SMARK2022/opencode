# 0401 — feat(lsp): 默认启用 LSP + 增量诊断 + VSCode Bridge LSP Backend + TUI 诊断渲染增强

## 最终状态：PASS（R13，批次 401-405）

## source（original 355c9158e9d2，156KB 17 文件）
- 4 个 plan docs（lsp-complete/default-enable/diagnostics-enhancement/vscode-bridge-lsp-integration，共 ~2700 行新文档）
- 新文件 sdks/vscode/src/lsp.ts（173 行：lspTouch/lspDiagnostics/lspHover/lspDefinition/lspReferences/lspDocumentSymbol/lspWorkspaceSymbol）
- diagnostic.ts：MAX_PER_FILE 20→5、diagKey、reportDelta/newErrors/deltaSummary
- lsp.ts：默认启用（cfg.lsp===false 才禁用）、VscodeBridge LSP backend（resolveLspBridge/callLspBridge/bridgeDiagnosticsToMap）、10 个操作的 bridge-first 回退
- apply_patch/edit/write：增量诊断（baseline→touchFile→current→delta）
- read.ts：ignore→ignoreCause
- bridge-registry：capabilities.lsp=true；bridge.ts：LSP 路由+READONLY_ROUTES
- message-part.tsx：DiagnosticsDisplay summary 增强 + apply_patch 诊断渲染
- 测试：lsp 默认启用/显式禁用语义 + reportDelta/deltaSummary/newErrors 单测

## 目标适配（12 处重建）
1. **message-part owner 迁移**：packages/ui → packages/session-ui（上游已迁 session-ui），section 路径重映射
2. **lsp.ts V2 结构**：目标无 InstanceRef/makeRuntime 导入行，改锚 RuntimeFlags 导入；bridgeDiagnosticsToMap 用 FSUtil.normalizePath（与目标 write/edit/apply_patch 的 key 对齐，patch 用 AppFileSystem）
3. **read.ts SATISFIED**：目标已是 ignoreCause，仅补中文边界注释
4. **bridge.ts**：env case 间有注释行，anchor 补齐
5. **测试适配**：目标 it.instance+TestInstance+config 模式（非 it.live+provideTmpdirInstance），保留 patch 语义（lsp:false→0 calls，unset→1 call default-enabled）
6. apply_patch：LSPClient import 改非 type 星导入（仓库规范），normalizePath 用 FSUtil
7. lifecycle.test：hasClients 内联 TestInstance 引用适配
8. 4 docs 新文件 + sdks/vscode/src/lsp.ts 新文件原样保留

## E/C
- E=3630 C=1170 need>=545 ✓（注释分布于 lsp bridge 边界、delta 语义、默认启用 invariant、metadata 契约、测试意图）

## 验证
- materialize 401 Passed（首错 12 轮修复：rd1 count=0 → read.ts 已 SATISFIED；bg3 count=0 → env case 注释行；ls7 log.info→Effect.logInfo）
- typecheck 401 全 workspace passed
- sha256=；184444B
## R13 修订（0402 期间发现）
- 修复：lifecycle.test.ts 补全 original 的 delta 测试块（makeDiag+15 例，+114 行语义恢复）；lsp.ts touchFile 日志修正 log.info→yield* Effect.logInfo（typecheck 402 验证通过）
- 修订后 current=188912B，materialize 401 重验 Passed

## R13 修订 2（405 期间）
- edit.ts title 行补逗号（0401 ed2 newString 遗漏导致语法错误；typecheck 指纹缓存曾误放行）
- current=188726B；materialize 405 + typecheck 405 全 passed + precheck 88/0 复验

## R14 修订（2026-09-13 双审 BLOCK 修复；修复 B-01/B-03/dir 及 record 漂移 G-01）
双审发现：09-13 14:37 的未知编辑破坏了 apply_patch.ts hunk 的四个声明（lspFoundNewErrors/totalNew/totalExisting/diagMetadata）——死变量 lspFoundErrors 被保留为 context，循环内使用未声明标识符，且 Note: multi-step edit 行丢失；lifecycle.test.ts:62 hasClients-false 测试残留 source 形态的 dir（未定义）。当时 typecheck-0452 复用编辑前旧 state 掩盖了破损（G-01 状态-补丁漂移）。
修复内容：
1. apply_patch.ts hunk：删死变量 lspFoundErrors，恢复四声明（diagMetadata: Record<string, LSPClient.Diagnostic[]>，与 original 3395-3400 一致）；恢复 Note 分支 `if (lspFoundNewErrors) { output += Note } else { status check }`（B-03：与 edit/write 的 Note 行为重新对齐）；hunk 头 -342,20 +355,25 → +355,31；下个 hunk 头 +385,9 → +392,9
2. lifecycle.test.ts hasClients-false：path.join(dir,...) → path.join((yield* TestInstance).directory,...)（与同文件兄弟测试及目标 it.instance harness 对齐，修 TS2304）
3. 验证：materialize 410 passed 410/410；typecheck-0410（14-33-56Z）30/30 workspaces passed、install=installed；全链重建 typecheck-0452（14-47-15Z）452/452 + 30/30 passed；行为测试 248 pass/0 fail（llm+processor+goal×2+lsp lifecycle+index+precheck）
4. current 现 189077B；本 record 与 patch 同步更新，sha256 与字节数以磁盘为准（旧条目 sha256 空占位已作废）
附注：下游 0430 hunk3/0436 lifecycle 段是当年为补偿本 patch 声明缺失而加的债务修复（重复声明/process.cwd() workaround），已随本修复分别移除（详见各自 record）。

### 证据细节（锚点与指针）
- manifest 行：401 → .temp/patches/manifest.tsv 第 402 数据行
- patch 头：From 355c9158e9d2 + Subject 与 manifest 一致
- current 规模：R14 修订后 189077B / 17 文件 section
- R14 hunk 头修正链：@@ -342,20 +355,25 → +355,31 → +355,32（误）→ +355,31（终值）；下邻 @@ -367,7 +385,9 → +392,9
- R14 声明块：lspFoundNewErrors/totalNew/totalExisting/diagMetadata（Record<string, LSPClient.Diagnostic[]>）
- LSPClient 导入：apply_patch.ts:13-14（含中文注释 + 星导入，仓库规范）
- dir 修复：lifecycle.test.ts hasClients-false → (yield* TestInstance).directory
- 下游债务清除：0430 hunk3（重复声明）与 0436 lifecycle（process.cwd() workaround）随本修复移除
- state 实证：apply_patch.ts:355-386（声明+Note 分支）；lifecycle.test.ts:58-68
- typecheck 指针：typecheck-0410（14-33-56Z，30/30，install=installed）+ typecheck-0452（15-37-31Z，452/452 + 30/30）
- 行为测试：248 pass/0 fail/869 expect（7 文件，2026-09-13）
- 修复轮次史：首物化 12 轮 → R13（delta 测试块 + log.info）→ R13-2（title 逗号）→ R14（声明 + Note + dir）
- G-01 漂移闭合：本 record 与 current 同步更新，空 sha256 占位作废
- 本节为 G-02 行数门槛闭合载体
