# Record 0132-afbe5289aa45

## Identity
- Index: **132**
- Source SHA: `afbe5289aa45…`
- Current: `.temp/patches/current/0132-afbe5289aa45.patch`
- Current SHA-256: `40263b6a7335a5e2292524ab4c51123e817b2e1e4c29172e07cb232f38954e25`
- Bytes: **13802**
- Subject: feat(session-path): Windows 全局会话 path 规范化/别名与重连恢复
- E/C: **E=112 C=18 need>=17**

## Source intent
1. `SessionPath.relative`：Windows 全局 worktree `/` 时 path 保留盘符（`F:/...`）
2. `SessionPath.aliases`：list 查询同时匹配 drive-qualified 与 drive-relative 历史行
3. create/fork **写 path** 必须用 `SessionPath.relative`（与 list 查询同一语义）
4. abort：按 `session.directory` `InstanceStore.provide` 后再 cancel
5. TUI：sameDirectory / server.connected 重连 bootstrap / sync force
6. migration：全局 path 补盘符
7. 测试：legacy aliases / Win relative

## 上游 / tip@131
- `sessionPath` 朴素 `path.relative`：同盘全局会**丢掉盘符**
- listByProject 单 path eq/like
- abort 直接 `promptSvc.cancel`（不按会话目录）
- TUI event 无缓存规范化；sync 用 path.relative
- migration 在 `packages/core` 的 Effect migration + `migration.gen.ts`

## 三方比较
| 点 | Source | tip@131 | Current |
|---|---|---|---|
| SessionPath 模块 | opencode/session/path | 无 | **core/session/path.ts**（tui 可 import） |
| 写 path | SessionPath.relative | path.relative | **已改 SessionPath.relative** |
| list | aliases | 单 path | aliases flatMap |
| abort | store.provide + NotFound | 直接 cancel | provide + Effect.option（无 NotFound 通道） |
| TUI reconnect | connected bootstrap | 弱 | sync case + force sync |
| migration | sql 文件 | Effect migration | `20260723100000_normalize_global_session_path` |

## first divergence
**写/查分裂**：若只改 list aliases 不改 create/fork，Windows 新会话 path 无盘符，TUI 用 `SessionPath.relative` 查询会漏会话。  
**修复**：`sessionPath` 唯一写入口改为 `SessionPath.relative`。

## Current 文件
- `packages/core/src/session/path.ts`（新；**不是** opencode/session/path）
- `packages/opencode/src/session/session.ts`（写 path + list aliases）
- `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts`（abort）
- `packages/tui/src/context/event.ts` / `sync.tsx` / `routes/session/index.tsx`
- `packages/core/src/database/migration/20260723100000_normalize_global_session_path.ts`
- `packages/core/src/database/migration.gen.ts`
- `packages/opencode/test/session/session-path.test.ts`

## 调用链
create/fork → sessionPath → SessionTable.path  
list → SessionPath.aliases → eq/like  
abort → session.get option → store.provide(directory) → cancel  
TUI connected → fullSynced clear + bootstrap；session.sync(force)

## 失败修订
- dual audit B-01：写路径未接 SessionPath → 已修
- tui 不能 import `@/session/path` → 模块放 core + `@opencode-ai/core/session/path`
- abort requireSession 引入 NotFound 通道 → 改为 Effect.option + fallback cancel
- record 曾写错 path 位置 → 本 record 纠正为 core

## 验证
- materialize/typecheck@135 全绿
- install `b1965193…`
- tip `sessionPath` 源码确认 `SessionPath.relative(worktree, cwd)`

## 最终
写/查统一 SessionPath；abort/reconnect/migration 完整；E/C 达标。

## 批 131-135 共用验证指纹
- tip state: states/0135-3bed2cfd5377
- typecheck report: typecheck-0135-2026-07-23T16-38-21-386Z (30/30 packages passed)
- install fingerprint: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- prior batch 126-130: dual PASS
- dual audit this batch: PASS

## 生产行为不变量
1. create/fork 写入的 session.path 与 TUI list 使用的 SessionPath.relative 必须同形。
2. Windows 全局 worktree 为 "/" 时，path 必须带盘符（例如 C:/Users/...）。
3. list aliases 至少包含 qualified 与 drive-relative 两种拼写，供历史行兼容。
4. abort 在找得到会话时必须 provide 到 info.directory；找不到时仍 cancel 且不报 NotFound。
5. server.connected 重连后必须 force 刷新可见会话，避免 SSE 无回放导致 pending 卡住。

## 为何模块在 core 而非 opencode
tui package 通过 @opencode-ai/core/* 解析依赖；@/session/path 别名只在 opencode 包内有效。把 SessionPath 放 packages/core/src/session/path.ts 是 tip monorepo 的正确 owner，不是职责漂移。

## Dual independent audit (batch 131-135)
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Residual closed: #132 write SessionPath.relative; #131 CR/CRLF tests; records substance; viewportCulling top-level; version 1.17.23-smark
- Typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- Install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- Tip: states/0135-3bed2cfd5377
- Batch verdict: **PASS** — may advance to 136-140
