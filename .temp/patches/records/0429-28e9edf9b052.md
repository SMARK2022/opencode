# Record 0429-28e9edf9b052

## Identity
- SHA: 28e9edf9b052
- Parent (source): 5640234ea7cb
- Subject: fix(goal): 修复 GOAL 生命周期完整性——read gate、reason、代际 CAS 和恢复
- Status: PASS

## Source intent（20 文件，541 hunks，1,122,122 字节）
1. docs/plans/session-goal-transition-integrity.md 修订
2. migration/20260714083829_session-goal-integrity/（migration.sql + snapshot.json）新增
3. TUI：component/dialog-goal.tsx + context/sync.tsx（goal 对话/上下文同步）
4. server routes instance/httpapi/groups/session.ts + handlers/session.ts（goal API：read gate、reason、CAS）
5. session/compaction.ts、goal.sql.ts、goal.ts、message-v2.ts、prompt.ts
6. tool/goal.ts + goal.txt
7. 测试：session/goal.test.ts、storage/goal-migration.test.ts、tool/goal.test.ts
8. SDK generated：sdk/js/src/v2/gen/sdk.gen.ts、types.gen.ts、sdk/openapi.json（生成物）

## Pre-apply 结果（verbatim current）
materialize 429 首错停止，主要问题：
- dialog-goal.tsx、context/sync.tsx、goal.sql.ts、goal.ts：**index 不存在**（target owner 迁移——TUI 在 packages/tui/，goal.sql 在 target 结构不同）
- groups/session.ts、handlers/session.ts：goal 结构 hunk 上下文不匹配
- compaction.ts：offset -496 成功（target 更短）
- 其余文件待逐文件确认

## 适配方向（占位，待调研后更新）
- 追踪 target 的 goal 语义 owner（session/goal.*、tool/goal.ts、TUI dialog、server routes、SDK 生成）
- migration：target migration 目录结构（packages/opencode/migration/）——新增目录 verbatim
- SDK generated：按 target 生成流程（bun run generate）

## 实施与验证（最终）
### 适配内容（19 文件 + docs + core 测试）
- core/session/goal.sql.ts：SessionGoalTable 加 6 列（generation/reason/blocked_reason/blocked_streak/blocked_last_turn_id/terminal_turn_id）
- 新 migration 20260728120000_add_goal_integrity.ts：ALTER TABLE ADD COLUMN（6 列）+ 旧 terminal 行 legacy reason 回填
- schema.gen.ts bootstrap DDL 同步；migration.gen.ts 注册
- schema/session-goal.ts：Goal 加 generation/reason
- schema/v1/session.ts：User 加 goalTurnID
- opencode/session/goal.ts：SetInput.reason、MAX_REASON_CHARS、ReadSnapshot/ModelTransitionInput/ModelTransitionResult、set 的 generation 递增 + terminal reason 校验 + objective-only edit 自动 active、modelTransition（CAS/blocked streak/active recovery，db.transaction + orDie）、accountUsage expected CAS（SQL 原子增量）
- opencode/session/prompt.ts：goalTurn 创建（eligibleTurnID/previousID/userInitiated）、goal_continuation marker、SessionTools.resolve 传 goalTurn、expectedUsage 捕获 + accountUsage 移到所有退出路径前
- opencode/session/compaction.ts：replay/continue 继承 goalTurnID
- opencode/session/tools.ts：resolve input goalTurn + extra 传递
- tool/goal.ts：GoalTurnContext + read gate + mark/reason + modelTransition 结果分类
- tool/goal.txt：read gate/mark/reason/streak 文档
- server groups/session.ts：GoalSetPayload.reason；handlers/session.ts：goalSet reason 透传 + active 后 fork prompt loop
- TUI dialog-goal.tsx（reason 显示）+ context/sync.tsx（generation/reason 字段）
- 测试：goal.test.ts（accountUsage 语义重写 + generation mismatch + terminal reason + modelTransition 测试）、tool/goal.test.ts（target 版 8 测试：read gate/complete/streak/recovery）、core session-goal-migration.test.ts 扩展（integrity 列 + legacy marker）
- SDK 生成物（source 的 openapi.json/v2/gen）：target 无对应 goal API 面（target openapi 不含 goal）→ 记录差异（source 的 SDK 生成面在 target 无承载；goal schema 变更由 schema 包 + core SQL 承载）

### 关键适配决策
- target 用 Effect 风格 db（EffectDrizzleSqlite）→ modelTransition 用 db.transaction((tx) => Effect.gen) + 外层 orDie（SqlError→defect）
- target 的 SessionV1.User 来自 schema 包 → goalTurnID 加在 schema/v1/session.ts
- target 的 goal-migration 测试在 core/test/session-goal-migration.test.ts（非 source 的 opencode/test/storage/）
- target 的 tool/goal.test.ts 无对应（source 新增）→ target 版（AppNodeBuilder + InstanceBootstrap/Config 桩 + InstanceStore 去重）

### Verification
- materialize 429 passed；typecheck 429 passed（全部 21 workspace）
- goal.test.ts 44/44、tool/goal.test.ts 8/8、session-goal-migration.test.ts 2/2
- E/C = 1108/213（need 167）PASS

### 过程中失败与修复
- modelTransition SqlError 类型泄漏（orDie 位置）→ 移到 db.transaction 外层
- goal.test.ts accountUsage 3 参（8 处）→ source hunk + 手动替换
- tool/goal.test.ts source 版无法编译（@/bus/defaultLayer）→ target 版重写
- InstanceBootstrap unbound → 补桩；InstanceStore 重复 import → 去重
- current 生成/重建循环（0428 state 被清）→ 重建 0428 + 重放编辑脚本

## 双审计修复记录
- B-02（正向 auditor）：goal.txt 缺两行 guardrail（"Do not use `blocked` merely because the work is hard..." 与 "Do not mark a goal complete merely because its budget is nearly exhausted..."）——根因：current 的 goal.txt 段用 extract_post（只取 + 行）导致 context 行丢失（12 行→10 行）。修复：goal.txt 补回两行（在 "Once the blocked threshold..." 后），用 no-index（0426-pre vs 0430-post）重新生成 current/0429 的 goal.txt 段。
- N-0429-1：SDK 生成物（openapi/v2-gen）无 target 对应面（双 auditor 独立验证 target openapi 0 goal 引用）→ 处理合理。
