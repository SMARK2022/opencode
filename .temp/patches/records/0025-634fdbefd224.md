# Record: Patch 25 — 634fdbefd224

## 1. Manifest Entry

- **Index**: 25
- **SHA**: 634fdbefd22401005a5478b1167ef8b013b2ce44
- **Parent**: 0024-5a7f139b02d1
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 优化粘贴文本处理逻辑，确保虚拟占位符正确替换为原始内容，修复坐标系统不匹配问题

## 2. Source Behavior (original patch)

### 修改文件
- `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx`
  - **Hunk #1**: submit expansion `Buffer.subarray` → `lastIndexOf`/`slice` 修复虚拟文本替换
  - **Hunk #2**: `pasteText` 函数 `extmarkEnd = extmarkStart + virtualText.length` → `Bun.stringWidth(virtualText)`
  - **Hunk #3**: `pasteAttachment` 函数 `extmarkEnd = extmarkStart + virtualText.length` → `Bun.stringWidth(virtualText)`

### 行为目的
- **坐标一致性 invariant**: extmark 的起始和结束位置必须使用显示宽度（`Bun.stringWidth`）而非字符串长度（`.length`）
- **原因**: 虚拟文本可能包含非 ASCII 字符，`.length` 返回 UTF-16 编码单元数，与终端显示宽度不一致

## 3. 上游 (v1.17.20) 行为

- V2 已将 TUI 路径迁移到 `packages/tui/src/`
- V2 的 `pasteText` 函数已使用 `promptOffsetWidth(virtualText)` 计算显示宽度（比 SMARK 的 `Bun.stringWidth` 更强：包含 grapheme 感知）
- V2 的 `pasteAttachment` 函数仍使用 `virtualText.length`（未修复）
- V2 的 submit expansion 使用 `expandTrackedPastedText`（比 SMARK 的 `lastIndexOf`/`slice` 更强：grapheme 感知的 `displaySlice`）

## 4. 目标适配 (current patch)

### 携带的 SMARK 语义（patch 25 自己的 hunk #3）
- `pasteAttachment` 函数: `extmarkEnd = extmarkStart + virtualText.length` → `promptOffsetWidth(virtualText)`
- 使用 V2 的 `promptOffsetWidth` 而非 SMARK 的 `Bun.stringWidth`（V2 更强：包含 grapheme 感知的 `Intl.Segmenter` + `Bun.stringWidth`）
- 按"上游更强时，必须把上游完整实现保留为不可删除基线"规则使用 V2 的 `promptOffsetWidth`
- 对于 ASCII `virtualText`（`[PDF ${n}]`/`[Image ${n}]`），`.length === promptOffsetWidth(virtualText)`，此变更是 no-op 但忠实携带 source intent

### 跳过的 SMARK 语义（有正当理由）
- **Hunk #1** (submit expansion `Buffer.subarray` → `lastIndexOf`/`slice`): V2 上游使用 `expandTrackedPastedText`（`prompt/part.ts:24-28`）+ `displaySlice`（`prompt/display.ts:25-27`），使用 grapheme 感知的 `Intl.Segmenter` + `Bun.stringWidth`，比 SMARK 的 `lastIndexOf`/`slice` 更正确
- **Hunk #2** (`pasteText` `extmarkEnd` `.length` → `Bun.stringWidth`): V2 上游已使用 `promptOffsetWidth(virtualText)`（`prompt/index.tsx:1257`），比 SMARK 的 `Bun.stringWidth` 更正确

### 不包含的变更（属于后续 patch）
- extmark 更新 effect 的 `newEnd` 修复（`newStart + virtualText.length` → `newStart + promptOffsetWidth(virtualText)`）属于 SMARK patch 247，不在本 patch 范围内。该修复将在 patch 247 迁移时携带，届时 `newStart` 和 `newEnd` 将同时转换为显示宽度坐标。

## 5. E/C 计算

### 有效修改行 E
- prompt/index.tsx:
  - `pasteAttachment` `extmarkEnd` 修改: 1 行（`virtualText.length` → `promptOffsetWidth(virtualText)`）
  - **总 E = 1**

### 合格中文注释 C
1. `// extmark 偏移使用显示宽度而非字符串长度，确保非 ASCII 内容坐标正确` — 直接位于 `extmarkEnd` 修改行之上，解释为什么使用 `promptOffsetWidth` 而非 `.length`

- **总 C = 1**
- **最低 C = max(1, ceil(1 * 0.15)) = 1**
- **1 ≥ 1，通过**

### 注释位置
- `pasteAttachment` 函数: 注释 1 直接位于 `const extmarkEnd = extmarkStart + promptOffsetWidth(virtualText)` 之前

## 6. 验证

### Apply
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 25 --rebuild`
- 结果: passed 25/25

### Typecheck
- packages/core: passed
- packages/opencode: passed
- packages/tui: passed

### Install 指纹
- `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史

1. **首轮审计 BLOCK (G-01)**: 中文注释不在修改代码附近
   - 修复: 将注释移到 `newEnd` 修改行之上

2. **二轮审计 BLOCK (B-01)**: `newEnd` 修复属于 SMARK patch 247，不属于 patch 25。将后续 patch 的变更提前引入造成 scope violation 和坐标系统混用（`newStart` 仍为 JS 字符串索引而 `newEnd` 为显示宽度）
   - 修复: 移除 `newEnd` 变更，改为携带 patch 25 自己的 hunk #3（`pasteAttachment` `extmarkEnd` `.length` → `promptOffsetWidth(virtualText)`），使用 V2 的 `promptOffsetWidth` 而非 SMARK 的 `Bun.stringWidth`

## 8. 最终行为

- `pasteAttachment` 的 `extmarkEnd` 使用 `promptOffsetWidth(virtualText)` 计算显示宽度
- `pasteText` 已由 V2 上游使用 `promptOffsetWidth`
- submit expansion 由 V2 上游的 `expandTrackedPastedText`/`displaySlice` 处理（比 SMARK 更强）
- extmark 更新 effect 的 `newEnd` 修复留给 patch 247 迁移（届时同时修复 `newStart` 和 `newEnd`）
- V2 的 `promptOffsetWidth` 使用 grapheme 感知的 `Intl.Segmenter` + `Bun.stringWidth`，比 SMARK 的 `Bun.stringWidth` 更正确
- 中文注释紧邻修改代码，满足 15% 门禁
