# Record 0085-66ddc4e2fe6c

## 1. Identity

- **Index:** 85
- **Source SHA12:** `66ddc4e2fe6c`
- **Subject:** feat(token-estimate): 优化字符与token计算，考虑附件对输入字符的影响
- **Current path:** `current/0085-66ddc4e2fe6c.patch`
- **Paths adapted:** original `packages/opencode/src/cli/cmd/tui/util/*` → target `packages/tui/src/util/*`
- **Baseline:** `4473fc3c9055046183990a965d68df3db7ea6f62`
- **Source tip:** `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- **Parent batch:** 80–84 dual PASS; state prefix `0084-7759649fd14d`

## 2. Upstream design (state-84)

Target already had:

- `charsPerTokenFromHistory` in `packages/tui/src/util/token-estimate.ts` with 100k reverse scan window.
- Daemon-side history calibration in `packages/opencode/src/session/prompt.ts` also at 100k chars.
- Context UI `computeContextData` with daemon `inputBreakdown` buckets (`userText`, `toolInput`, `toolOutput`, `attachments`, etc.) and separate `toolCallTokens` / `toolResultTokens`.

What was still wrong relative to SMARK #85: step-finish `inputChars` used for density calibration still included attachment character mass, so base64/vision payloads inflated chars-per-token and distorted text estimates. UI also mapped `bd.messages.attachments` through the same text ratio instead of the visual attachment estimator.

## 3. Source / SMARK intent (original patch)

1. **token-estimate:** when accumulating calibration samples,  
   `totalChars += max(0, inputChars - inputBreakdown.messages.attachments)`.
2. **prompt.ts:** same subtraction for `historyInputChars`; raise sample cap 50k→100k (already present on target).
3. **context-usage:**  
   - input message tokens: text chars / ratio **plus** `msg.details.attachments` (visual), not `(userText+attachments)/ratio`.  
   - details.attachments field uses `msg.details.attachments`.  
   - Original also delegated local `computeInputRatio` to `charsPerTokenFromHistory` (already done on target).

## 4. Target adaptation (this current)

| File | Change |
|---|---|
| `packages/tui/src/util/token-estimate.ts` | Subtract attachments from `totalChars` in `charsPerTokenFromHistory`. |
| `packages/opencode/src/session/prompt.ts` | Subtract attachments when accumulating `historyInputChars`. |
| `packages/tui/src/util/context-usage.ts` | `inputMessageTokens = round(userText/ratio) + msg.details.attachments`; details.attachments = visual estimate. |

Mapping note: SMARK one-liner used `(userText+toolOutput)/ratio + attachments` while target already splits tool output into `toolResultTokens`. Equivalent SMARK intent is “attachments do not share text density, use visual tokens.”

## 5. E / C

Independent on this current only:

- **E = 6** (small delta)
- **C = 4** (comments on attachment exclusion / visual bucket)
- **need ≥ 1**
- **Gate: PASS**

## 6. Verification

- Materialize cumulative through 89 applies this current (prefix).
- State-89 greps: `messages?.attachments` / `bd?.messages?.attachments` in token-estimate and prompt; context-usage uses `msg.details.attachments`.
- Monorepo typecheck at batch end green.

## Dual independent audit (batch 85-89)

- **FORWARD:** PASS (all five, no open B/G) — ses_0786eabd4ffeXUBUo8w9wkU0zf
- **REVERSE:** PASS (all five, no open B/G) — ses_0786eabc1ffemlej5xXCzvttGF
- **State:** `states/0089-94eb97b93fad`
- **Typecheck:** `typecheck-0089-2026-07-22T02-03-28-357Z.md` monorepo packages passed
- **Batch verdict:** PASS
