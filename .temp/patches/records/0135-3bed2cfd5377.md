# Record 0135-3bed2cfd5377

## Identity
- Index: **135**
- Source SHA: `3bed2cfd5377…`
- Current: `.temp/patches/current/0135-3bed2cfd5377.patch`
- Current SHA-256: `10dc32b19baedd5d78b3956dd8a9ec11a41ad7998148f5b7de49494e171233af`
- Bytes: **3531**
- Subject: feat(session): 流式消息渲染 — streaming 标志与 viewport culling
- E/C: **E=8 C=5 need>=2**

## Source intent
1. streamingActive：messages 中存在 role=assistant 且 !time.completed
2. 流式时关闭 viewport culling，避免滚动裁剪造成跳动/残影
3. AssistantText：drawUnstyledText/streaming 绑 message 未完成
4. AssistantReasoning：同上绑推理未完成
5. source 实现分布在 session-v2.tsx 与 session/index.tsx；tip 无 session-v2，owner 是 packages/tui/src/routes/session/index.tsx 的 TextPart/ReasoningPart

## 上游 / tip@134
- PART_MAPPING 已有 text/reasoning/tool 组件
- code/markdown 已支持 streaming / drawUnstyledText props，但推理预览硬编码 drawUnstyledText=false、streaming=true
- ScrollBoxOptions 顶层有 viewportCulling?: boolean；viewportOptions 是 BoxOptions，放 viewportCulling 会 TS2353

## 三方比较
| 点 | Source | tip 前 | Current |
|---|---|---|---|
| streamingActive | 有 | 无 | messages memo 旁 createMemo |
| culling API | viewportCulling | 无 | scrollbox 顶层 viewportCulling={!streamingActive()} |
| TextPart streaming | !message.completed | 恒 true | createMemo 绑定 completed |
| Reasoning streaming | props.streaming | 恒 true/false | !isDone()（part.time.end） |
| session-v2 文件 | 有 | 无 | 不迁文件，改 tip owner |

## first divergence
流式时仍 culling + 标志硬编码。主路径是单一 UI 状态门控，不是第二渲染器。

## Current 文件
packages/tui/src/routes/session/index.tsx

## 调用链
sync.data.message → messages() → streamingActive → scrollbox.viewportCulling
message.time.completed / part.time.end → TextPart/ReasoningPart → markdown/code props

## 注释位置
streamingActive 定义；viewportCulling 禁用原因；reasoning/text 绑定完成态原因。

## 失败修订
1. 误把 viewportCulling 放进 viewportOptions → typecheck 失败 → 移到 scrollbox 顶层
2. 不存在的 session-v2 路径不作为 owner
3. record 垫片删除，改为完整有效信息

## 验证
typecheck 30/30；tip 源码可见顶层 viewportCulling 与 streamingActive；reasoning 使用 isDone。

## 与上游 OpenTUI 关系
@opentui/core ScrollBox.d.ts 声明 viewportCulling 为 ScrollBoxOptions 字段，与 BoxOptions.viewportOptions 分离；适配必须遵守该类型边界。

## 最终
流式渲染门控完成；类型合法；E/C 达标。


## 渲染不变量
1. 只要存在未完成 assistant，scrollbox.viewportCulling 为 false。
2. 所有 assistant 完成后，culling 恢复 true，恢复大列表性能。
3. TextPart streaming 随 message.time.completed 翻转。
4. ReasoningPart streaming/drawUnstyledText 随 part.time.end（isDone）翻转，与 tip 注释一致：推理完成可独立于 message 完成。

## OpenTUI 类型边界
ScrollBoxOptions.viewportCulling 是 scrollbox 组件自身属性。viewportOptions 传给内部 viewport Box，类型为 BoxOptions，没有 culling 字段。错误放置会导致 packages/cli 引用 tui 时 TS2353。

## 非目标
- 不重写 markdown 渲染器。
- 不迁移不存在的 session-v2.tsx。
- 不强制 flexShrink 全量 prettier 回放。


## 与 tip 既有优化共存
ReasoningPart 已有 50ms 节流 displayContent 与 hide 模式折叠。#135 不拆除这些优化，只修正 streaming/culling 门控。TextPart 继续使用 markdown 组件；流式时 streaming=true 让底层可走增量路径。

## 手动验收建议
开新会话让 assistant 长时间流式输出：滚动列表不应出现大块空白闪烁。完成后恢复 culling，长历史滚动应保持流畅。

## 批 131-135 共用验证指纹
- tip state: states/0135-3bed2cfd5377
- typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- prior 126-130 dual PASS

## Dual independent audit (batch 131-135)
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Residual closed: #132 write SessionPath.relative; #131 CR/CRLF tests; records substance; viewportCulling top-level; version 1.17.23-smark
- Typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- Install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- Tip: states/0135-3bed2cfd5377
- Batch verdict: **PASS** — may advance to 136-140
