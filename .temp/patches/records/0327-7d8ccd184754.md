# Record 0327-7d8ccd184754 — feat(reviewer): 实现隐式模型解析和回退逻辑，确保 reviewer 使用父会话模型

## Identity
- index/full SHA: 0327 / 7d8ccd1847545f7ae384b85fe2faa5453a1026a2 (short 7d8ccd184754)
- manifest parent: ce20c9938a89 (0326)
- subject: feat(reviewer): 实现隐式模型解析和回退逻辑，确保 reviewer 使用父会话模型
- current SHA-256: 50bc7398b3e33c17 / 23509 bytes
- status: PASS

## Source intent (original/0327-7d8ccd184754.patch, 502 行, 2 files)
1. `packages/opencode/src/permission/reviewer/service.ts` (L14-120): 加 resolveImplicitReviewerModel + parentSessionModel + resolveModelWithFallback 函数；未配置 model 时用父会话 SessionTable.model 而非全局 defaultModel
2. `packages/opencode/test/permission/reviewer-service.test.ts` (L121-502): 3 新测试 + 3 fixture functions + reviewInput 签名扩展

## Three-way comparison
- source/SMARK: reviewer 未配置 model 时用父会话模型而非全局 defaultModel，避免漂移到认证失效的 provider
- 上游 v1.17.20: reviewer 用全局 Provider.defaultModel()，可能漂移到认证失效的 provider
- 目标当前: service.ts 有 reviewer 但用 inline defaultModel/getSmallModel/getModel；无 resolveImplicitReviewerModel
- 结论: 加 resolveImplicitReviewerModel + parentSessionModel + helper functions + 测试

## Target adaptation
- service.ts: 同路径 `packages/opencode/src/permission/reviewer/service.ts`
  - Import 适配: `ModelID/ProviderID` from `@/provider/schema` → `ModelV2/ProviderV2` from `@opencode-ai/core/{model,provider}`
  - Type 适配: `ModelID.make()` → `ModelV2.ID.make()`，`ProviderID.make()` → `ProviderV2.ID.make()`
  - Variable 适配: `sessions.get/findMessage` → `session.get/findMessage`（singular）
  - `Effect.catchTag("ProviderModelNotFoundError", ...)` 保持不变（验证 ModelNotFoundError tagged as "ProviderModelNotFoundError"）
- test: 同路径 `packages/opencode/test/permission/reviewer-service.test.ts`
  - Import 适配: `Provider/ProjectID` → `ProviderV2/ProjectV2`
  - Type 适配: `ModelID/ProviderID/ProjectID` → `ModelV2.ID/ProviderV2.ID/ProjectV2.ID`
  - **关键修复**: 3 个 `Layer.mock(Session.Service)` fixture 缺少 `messages` 方法 → 加 `messages: () => Effect.succeed([])` 到 3 个 fixture
  - 原因: `parentSessionModel` 调用 `session.findMessage()` 内部调用 `session.messages()`，mock 缺少导致 "Unimplemented method 'messages'" 错误
- 方法: state 0326 语义编辑（edit327.py subagent + 手动 messages mock 修复）→ git diff → current/0327

## 逐 hunk 映射
| Source hunk | Target file | 状态 |
|-------------|-------------|------|
| Hunk 1 (imports) | service.ts L8-12 | ✓ V2 type 适配 |
| Hunk 2 (resolveImplicitReviewerModel call) | service.ts L119 | ✓ 替换 inline model resolution |
| Hunk 3 (resolveImplicitReviewerModel + helpers) | service.ts L234+ | ✓ 逐字复制 + V2 type 适配 |
| Hunk 4-7 (test imports + fixtures + tests) | reviewer-service.test.ts | ✓ V2 type 适配 + messages mock 修复 |

## Verification
- materialize 327/327: passed
- typecheck 327: install reused（frozen cbdc173），packages/app TS1128（环境基线），integrityFailure None
- reviewer service test（.test-workspace synced to 327）: `bun test ./test/permission/reviewer-service.test.ts --timeout 60000` → 8 pass / 0 fail / 59 expect() / 5.89s
- E/C: E=342 / C=66 / need=max(1,ceil(342*0.15))=52 → PASS
- source/target HEAD: 未变
- states 保留 ≤5

## 不一致
- 目标无 `@/provider/schema` → 用 `@opencode-ai/core/model` + `@opencode-ai/core/provider` 替代
- 目标无 `@/project/schema` → 用 `@opencode-ai/core/project` 替代
- 目标用 `ModelV2.ID`/`ProviderV2.ID`/`ProjectV2.ID` 替代 `ModelID`/`ProviderID`/`ProjectID`
- 目标 service.ts 用 `session`（singular）而非 `sessions`（plural）作为 Session.Service 变量名
- **关键修复**: 3 个测试 mock fixture 缺少 `messages` 方法 → 加 `messages: () => Effect.succeed([])` 满足 `parentSessionModel` 的 `session.findMessage()` 调用链

## R2 fix (TS2322 type error)
- Forward auditor found TS2322 at create mock: { ...childSession, ...input } widens required Info fields to undefined
- Fix: added as Session.Info type assertion to 3 create mock fixtures
- Typecheck: exit 0, no errors
- Tests: 8/8 pass

## DUAL-AUDIT SEAL (batch 326-330)
- R3 dual audit (after R1+R2 fixes: 0327 as Session.Info type cast, 0328 non-qualifying comments removed+replaced):
  - Forward (ses_fdac2d006): 5/5 PASS, no open B/G
  - Backward (ses_fdac2bb3): 5/5 PASS, no open B/G
- Verified: manifest/proof hashes, original LF-SHA, current SHA+bytes, install cbdc173 reused, E/C all pass (5/2, 342/66, 328/51, 45/13, 165/57), cumulative apply 330/330, states <=5, source/target unchanged
- Environmental N: packages/app TS1128; 0328 thin C margin (51 vs 50); 0329 pre-existing sudo test fail
- Batch: APPROVE (5/5 PASS, no open B/G)