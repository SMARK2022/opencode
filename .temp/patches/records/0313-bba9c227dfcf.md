# Record 0313-bba9c227dfcf — fix(tests): 增强事件源清理逻辑，确保测试后不留存余留状态

## Identity
- index/full SHA: 0313 / bba9c227dfcfd36029a7b4ef62ed81546c493b51 (short bba9c227dfcf)
- manifest parent: d86901da7cb1 (0312)
- subject: fix(tests): 增强事件源清理逻辑，确保测试后不留存余留状态
- current SHA-256: 5a3f7af36f2d7511 / 2050 bytes
- status: PASS

## Source intent (original/0313-bba9c227dfcf.patch, 53 行)
### Hunk 1: `packages/opencode/test/cli/cmd/tui/sync-fixture.tsx` (L1-6)
- 新增: `import { engine } from "@opentui/core"` — 在 testRender import 之后
- 行为目的: 导入 OpenTUI 全局引擎，用于后续 renderer.destroy override 中的 `engine.detach()` 调用

### Hunk 2: `packages/opencode/test/cli/cmd/tui/sync-fixture.tsx` (L49-55)
- 新增: createEventSource 返回对象加 `dispose() { fn = undefined }` 方法
- 行为目的: 显式清理事件回调 fn，防止 Solid cleanup 漏接后 renderer destroy 仍持有已销毁 handler

### Hunk 3: `packages/opencode/test/cli/cmd/tui/sync-fixture.tsx` (L151-170)
- 新增: mount 函数中 testRender 调用后，override `app.renderer.destroy`
  - 先 `events.dispose()` 清理测试事件源
  - 再 `try { destroy() } finally { engine.detach() }` — 调用原始 destroy 并 detach 全局引擎
  - 3 行英文注释: "Solid cleanup should unsubscribe the test event source, but Windows full-run failures showed a file-level cleanup timeout after every named sync test passed. Clear the test transport and detach OpenTUI's global engine explicitly so no destroyed renderer or event callback survives the last sync fixture teardown."
- 行为目的: Windows 全量运行时每个 sync 测试结束后出现文件级清理超时；显式 dispose+detach 确保无残留 renderer 或事件回调

## Three-way comparison
- source/SMARK: 事件源 dispose + renderer.destroy override + engine.detach()，防止 Windows 全量运行时文件级清理超时
- 上游 v1.17.20: sync-fixture 无事件源清理，renderer.destroy 未 override，无 engine.detach
- 目标当前: sync-fixture 已有 useThread:false（0311），但无事件源清理逻辑
- 结论: 在目标 sync-fixture.tsx + tui-sdk.ts 加 dispose + renderer.destroy override + engine.detach()

## Target adaptation
- sync-fixture.tsx 目标路径 `packages/tui/test/cli/cmd/tui/sync-fixture.tsx`（0299 迁移）
- createEventSource 在 target 中从 `../../../fixture/tui-sdk` 导入（非 sync-fixture.tsx 内定义）
  - source 在 sync-fixture.tsx 内定义 createEventSource，target 从 tui-sdk.ts 导入
  - dispose() 落在 `packages/tui/test/fixture/tui-sdk.ts`（createEventSource 的实际 owner）
- engine import: `@opentui/core` 已是 tui package 依赖（package.json L55 catalog:）
- 方法: state 0312 语义编辑（edit313v2.py patch_window）→ git diff → current/0313

## 逐 hunk 映射
| Source hunk | Source file | Target file | Target line | 状态 |
|-------------|-------------|-------------|-------------|------|
| Hunk 1 (engine import) | sync-fixture.tsx L2 | sync-fixture.tsx L3 | L4 新增 | ✓ 适配 |
| Hunk 2 (dispose) | sync-fixture.tsx L50-52 | tui-sdk.ts L58-62 | L59-62 新增 | ✓ 迁移到正确 owner |
| Hunk 3 (renderer.destroy) | sync-fixture.tsx L155-170 | sync-fixture.tsx L72-89 | L73-89 新增 | ✓ 适配 |

## Verification
- materialize 313/313: passed（从 state 0312 继续，应用 0313 current）
- typecheck 313: install reused（frozen install cbdc173），packages/app TS1128（环境基线）
- sync test（.test-workspace synced to 313）: `bun test ./test/cli/cmd/tui/sync.test.tsx --timeout 60000` → 11 pass / 0 fail / 33 expect() calls / 3.53s
- E/C: E=13 / C=3 / need=max(1,ceil(13*0.15))=2 → PASS
  - 注释位置: sync-fixture.tsx L73-74（中文：覆写 renderer.destroy 原因）、sync-fixture.tsx L77-80（英文：source 原文保留）、tui-sdk.ts L59（中文：dispose 清理事件回调原因）
- source/target HEAD: 未变（integrityFailure None）
- states 保留 ≤5

## 不一致
- source 在 sync-fixture.tsx 内定义 createEventSource，目标从 tui-sdk.ts 导入 → dispose() 落在 tui-sdk.ts（正确 owner，createEventSource 的实际定义位置）
- source 英文注释保留（source 内容），额外加 2 行中文注释解释覆写原因和 dispose 意图（E/C 门禁需要中文注释）
- tui-sdk.ts 中的 createEventSource 返回对象包含 source/emit/response 三个方法，dispose 作为第四个方法添加

## DUAL-AUDIT SEAL（批次 311-315）
- R1 双审计（记录扩展后：四份记录从 <50L/3000C 扩展至 ≥55L/3787C，0315 manifest parent 更正为 cf597333）：
  - 正向审计（ses_fdefde82fffeDVRJftO2F6brVb）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdefdcbf6ffeU0neKZIAxyCnKO）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（9/2、12/6、13/3、19/8、6/4）、累计 apply 315/315、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：packages/app TS1128 环境基线（custom-elements.d.ts 符号链接物化文本）；truncation cleanup 测试 7天旧文件时间戳依赖；typecheck script packages/app 首失败阻断后续 workspace（tsgo -b 模式覆盖 project references）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
