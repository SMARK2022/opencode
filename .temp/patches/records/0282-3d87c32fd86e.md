# Record: 0282-3d87c32fd86e

## 基本信息
- index: 0282
- sha12: 3d87c32fd86e
- subject: feat: 更新统一的shell压缩与截断回显 Implement opencode tool output notice format design
- source date: Sun, 7 Jun 2026 18:41:39 +0800

## 状态
- PASS（重建后待批次 281-285 重新双审计）

## 背景：为什么重建
批次 281-285 第四轮审计后，发现 `current/0282` 被误覆盖为 original patch 内容（无法 apply 到 target 架构）。本 record 重建完整的适配版 current：
1. 从 state 0281（前序物化状态）全新复制工作区
2. 逐 hunk 应用全部 source 语义（生产 + 测试 + docs）
3. 修正 prompt.ts hunk 缩进破坏问题（旧版注释/语句顶格）
4. 补齐 E/C 注释至门禁（E=147 → C=23 ≥ ceil(147*0.15)=23）
5. 重新物化 282-285 + typecheck + 全部受影响测试

## Source 行为重建
原始 patch 引入统一的工具输出提示格式设计：
1. **新增 docs/tool-output-notice-format-design.md**（529 行设计文档）：定义 `opencode_notice` 和 `opencode_excerpt` 两种结构化 tag，替代旧的各种 ad-hoc 格式
2. **新增 packages/opencode/src/util/output-notice.ts**（85 行工具函数）：提供 `outputStats`、`formatOutputTruncatedNotice`、`formatExecutionNotice`、`formatCompactionClearedNotice` 等格式化函数
3. **修改 message-v2.ts**：`"[Old tool result content cleared]"` → `formatCompactionClearedNotice()`；`"[Tool output truncated for compaction: omitted N chars]"` → `"[... compaction truncated N chars]"`
4. **修改 prompt.ts**：`"<metadata>User aborted the command</metadata>"` → `formatExecutionNotice({ severity: "warning", reason: "user_abort" })`
5. **修改 shell.ts**：超时消息 → `formatExecutionNotice({severity:"warning",reason:"timeout",timeout_ms})`；取消消息 → `formatExecutionNotice({severity:"warning",reason:"user_abort"})`；截断输出 → `formatOutputTruncatedNotice({source:"shell",total,shown:{direction:"tail",...},path})`；`<shell_metadata>` 包裹 → 直接 `meta.join("\n")`；新增 `normalizedStats = outputStats(normalized)`（截断 notice 的 total 基于 normalize 后文本，与落盘内容一致）
6. **修改 truncate.ts**：移除 `hasTaskTool`/`evaluate` import；移除 `hitBytes` 分支与 `removed`/`unit`/`hint` 自然语言提示；改为 `formatOutputTruncatedNotice({source:"tool",total:outputStats(text),shown:{direction,...outputStats(preview)},path:file})`；布局保持 head/tail 方向差异（head: preview 在前；tail: notice 在前）
7. **修改 4 个测试文件**：
   - truncation.test.ts：9 处断言改为 notice 格式；新增 "does not count final newline as an extra notice line"；Task tool 提示两个测试改名并改为紧凑断言
   - shell.test.ts：4 处 abort/truncation 断言改为 notice 格式（user_abort/timeout/output_truncated/路径）
   - message-v2.test.ts：3 处断言（compaction_cleared notice、`[... compaction truncated 6 chars]` 内联 marker、execution user_abort notice 替代 shell_metadata）
   - prompt.test.ts：4 处断言（3 处 reason="user_abort"、1 处 output_truncated + path）

## 三方比较
- **truncate.ts**：目标用 `FSUtil`/`LayerNode`/`NodePath`、`Agent.Info` 类型；`hasTaskTool`/`evaluate` 存在于目标需移除；`totalBytes`（仅未截断判定用）保留
- **shell.ts**：目标经历 #215/#130 压缩管道重构（normalizePowerShellOutput/compressVisibleOutput/tail/end 管道）；`diag.end()` 后插 `normalizedStats`；`end = tail(compressed.text, limits...)` 不变
- **message-v2.ts**：目标 `compacted` 分支为 `part.state.time.compacted`，适配占位正确
- **prompt.ts**：目标 `finish` 块内 `if (aborted)` 分支；缩进已修正为 16 空格
- **测试文件**：prompt.test.ts 断言 4 处（3041 属 it.instance queued loop caller 测试、其余 3 处属 unix 包装测试）；目标行号偏移 handled

## 适配内容
- output-notice.ts：新文件（85 行），保留 source 全部语义 + 中文注释（行数统计末尾换行处理、字节不做归一化的原因、harness 边界提示定位、空值剔除、compact size 拼接、字节格式约定、XML 转义安全原因、标量属性约束、notice 固定 source 理由、compaction source 标记理由）
- tool-output-notice-format-design.md：新文件（529 行），source 完整复制
- truncate.ts：移除 evaluate import、hasTaskTool、hitBytes 分支、removed/unit/hint；添加 output-notice import；head/tail 循环按 maxBytes 直接 break；notice 生成与布局
- shell.ts：单行 import 追加；normalizedStats 在 diag.end() 后；timeout/abort/truncated/meta 4 处替换 + 注释
- message-v2.ts：import 追加；truncateToolOutput 内联 marker；compacted 分支 notice + 注释
- prompt.ts：import 追加；aborted 分支 notice + 注释（16 空格缩进）
- 4 个测试文件：断言全部更新为 notice 格式

## current 修改前后（最终版重建）
- 编辑前（被污染状态）SHA-256: 1f57d4e9（original 全文）
- 重建过程 SHA-256: f4904a3a748c1da1 → d027651b66e0ea83
- 最终 SHA-256: d027651b66e0ea83
- 最终字节: 37661
- 文件数: 10（5 生产 + 4 测试 + 1 docs），37 hunks
- materialize 282: passed 282/282；materialize 285: passed 285/285

## E/C 计算（最终版）
- E (有效修改行): 147（含 docs/test 全部新增行，count-ec-auditor.py 口径）
- C (中文注释行): 23
  - output-notice.ts: 12 行（末尾换行统计说明×2、harness 边界提示说明×2、notice source 固定理由、compaction source 理由、空值剔除、compact size 拼接、字节格式约定、标量属性约束、XML 转义安全原因×2）
  - truncate.ts: 4 行（head bytes 直接截断说明、notice 唯一标识/path 恢复说明、totalBytes 仅判定用说明、paaujagent 签名保留）
  - shell.ts: 3 行（normalizedStats total 独立于压缩、shown/total 描述说明、execution notice 取代 shell_metadata 说明）
  - message-v2.ts: 2 行（compact 后 notice 语义、清理 vs 截断区分）
  - prompt.ts: 2 行（不经 ShellTool 收尾、不重新引入 metadata 包裹）
- 满足 E > 0 时 C >= max(1, ceil(147*0.15)) = 23，23 >= 23 ✓ PASS

## 安装与测试
- typecheck（--typecheck 285）: passed 285/285；packages/opencode exit 0；packages/app TS1128 pre-existing（非阻塞）
- truncation.test.ts: 20/20 pass（含新增 final-newline 测试 + 2 个 compact notice 测试）
- message-v2.test.ts: 38/38 pass
- shell.test.ts abort 组: 5/5 pass（0282 全部 2 处断言所在测试通过）
- shell.test.ts truncation 组: 4/5 pass（唯一失败 = 0283 引入的 "keeps a visible tail preview" 5s 超时，pre-existing Windows shell 环境）
- prompt.test.ts: 56 pass / 14 skip / 7 fail（全为 Windows shell E2E 超时：auto review/cancel/queued loop 等，0242 记录已确认同类 6 个 pre-existing 失败；0282 断言的 1 处 it.instance 测试 10s 超时也属此类，3 处 unix 包装 Windows 上 skip）
- 归因证据：0237 记录 "58 pass / 5 fail（预存 shell timeout）"、0242 记录 "6 个 shell 测试 pre-existing 失败（Windows shell 超时）"、audit-206-210 "默认 5s 超时不足，需 --timeout 60000"

## 不一致
- prompt.test.ts 4 处断言无法在 Windows 上实测（超时/unix skip）；由 shell.test.ts abort 组 5/5 + truncation 组 4/5 + message-v2 38/38 间接证实 notice 格式正确
- 0283 的 "keeps a visible tail preview" 失败与 0282 无关（测试体为 0283 新增）

## 重建过程记录（for auditor）
- src_repo 重建：D:/Temp/trunc282b-4uv7x9kq/repo（state 281 全新复制 + 19 个生产 edits + 24 个测试 edits + docs）
- prompt.ts 缩进修复：注释/语句从顶格/32 空格修正为 16 空格 uniform
- output-notice.ts 从旧 trunc282 repo 恢复（内容不变，85 行）
- 生成脚本：D:/Temp/opencode/gen_282_final.py（commit 281-base → overlay files → git diff --cached HEAD）
- 旧 trunc282-rtqrirxq 目录保留（只读参考，无 node_modules 影响）

## 待办
- 批次 281-285 重新双审计（正向 + 逆向），auditor 需核对：10 文件完整、E=147/C=23、prompt.ts 缩进、source 语义无遗漏（hasTaskTool 移除、hitBytes 移除、notice 格式、docs 完整）


## R8 修复记录（2026-08-14）
- 背景：0282 current 曾被发现被误覆盖为 original patch（无法 apply 到 target 架构），本 record 完成完整重建。
- 重建：从 state 281 全新复制工作区，逐 hunk 应用 source 语义（10 文件：5 生产 + 4 测试 + 1 docs），修正 prompt.ts hunk 缩进，补齐 output-notice.ts 新文件。
- E/C 补注释：output-notice.ts（12 条）、truncate.ts（4 条）、shell.ts（3 条）、message-v2.ts（2 条）、prompt.ts（2 条）。
- 关键修复：truncate.ts head 循环 maxBytes 截断丢失（rebuild 时注释替换掉了 break）；prompt.ts 注释缩进从 32 空格修正为 16 空格；message-v2.ts 重复 import 去重。
- 最终 E/C：E=145, C=23, need=22, PASS
- 验证：materialize 282 passed；truncation.test.ts 20/20、message-v2.test.ts 38/38、shell abort 组 5/5、truncation 组 4/5（唯一失败为 0283 的 tail preview 5s 超时，pre-existing Windows 环境）

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `82815ce29e98aeae5989244e359974e3701235632724500a900155d59344297d` / bytes: 44245
- E/C 最终：E=145, C=23, need=22, PASS
- 修复 B-01：docs/tool-output-notice-format-design.md hunk 中重复的 `+++ b/...` 行伪影删除（git apply 曾将其作为文档首行 `++ b/...` 物化），hunk header 修正为 @@ -0,0 +1,529 @@（529 个 + 行与 source 529 行一致）。
- 验证：materialize 300 后文档首行为 `# opencode 工具输出提示格式设计`（无伪影）；记录文档行数表述与物化一致（529 行正文）。
