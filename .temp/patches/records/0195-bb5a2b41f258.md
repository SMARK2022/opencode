# Patch 0195 — bb5a2b41f258 — feat: models-snapshot.js 更新

## 修改内容

### 适配变更
- 原 patch 刷新 `packages/core/src/models-snapshot.js`（4MB vendor 数据，+11 个模型跨 9 个 provider）
- tip 不存在 `models-snapshot.js`——模型数据通过 `OPENCODE_MODELS_DEV` 构建时 define 注入
- `packages/core/src/models-dev.ts`：
  - `loadSnapshot`（line 197-198）读取 `OPENCODE_MODELS_DEV`，数据来自 `MODELS_DEV_API_JSON` 环境变量
  - 新增 `export const MODELS_DEV_EMBEDDED = typeof OPENCODE_MODELS_DEV !== "undefined"`（line 135）
  - 该常量暴露构建时 catalog 嵌入状态，供诊断使用
- 移除原 current 对 `packages/core/src/snapshot.ts` 的错误注释（该文件是文件系统 Snapshot API，非模型 catalog owner）

### 源 patch 文件清单
- `packages/core/src/models-snapshot.js`（4146332 bytes，vendor JSON blob）— tip 不存在

### 目标 owner 追踪
- `packages/core/src/models-dev.ts:132` — `declare const OPENCODE_MODELS_DEV`
- `packages/opencode/script/build.ts:202` — `OPENCODE_MODELS_DEV: generated.modelsData`
- `packages/opencode/script/generate.ts:11` — `export const modelsData = process.env.MODELS_DEV_API_JSON`
- `packages/cli/script/build.ts:92` — `OPENCODE_MODELS_DEV: modelsData`
- `packages/cli/script/generate.ts:3` — `export const modelsData = process.env.MODELS_DEV_API_JSON`

## 不一致与修复
- G-01（双审 v1）：原 current E=0 C=1（仅注释），gate 失败 → 改为 E=1 C=2，gate PASS
- B-01（reverse auditor v1）：models catalog refresh 丢失 → tip 的 OPENCODE_MODELS_DEV 构建管线已替代 vendor 快照
  - tip baseline (v1.17.20) 的 catalog 已比 SMARK (v1.17.18) 更新
  - catalog 数据通过 MODELS_DEV_API_JSON 在构建时获取，不再提交为源码文件
  - MODELS_DEV_EMBEDDED 常量在正确 owner（models-dev.ts）上添加
- N-01（forward auditor v5）：MODELS_DEV_EMBEDDED 当前无消费者 → 诊断用常量，无害

## E/C
- E = 1（`export const MODELS_DEV_EMBEDDED = typeof OPENCODE_MODELS_DEV !== "undefined"`）
- C = 2（`#195` 中文注释解释 catalog 源迁移和构建时注入）
- need = max(1, ceil(1 * 0.15)) = 1
- gate = PASS（C >= need，余量 1）

## 注释位置
- `models-dev.ts:133-134`：
  - `// #195：源 patch 刷新 models-snapshot.js vendor 数据；tip 用 OPENCODE_MODELS_DEV 构建时注入替代`
  - `// #195：catalog 数据通过 MODELS_DEV_API_JSON 环境变量在构建时获取，不再提交为源码文件`

## Source
- SHA: bb5a2b41f258
- 意图：刷新 models-snapshot.js vendor 数据（+11 models, 9 providers）
- 原 patch 仅修改 1 个文件（models-snapshot.js），无生产代码逻辑变更

## 上游
- tip 用 OPENCODE_MODELS_DEV 构建时 define 替代 vendor 文件——更强（支持离线 + 在线刷新）
- script/generate.ts 从 MODELS_DEV_API_JSON 获取数据
- build.ts 注入 OPENCODE_MODELS_DEV define
- models-dev.ts loadSnapshot 读取 define
- tip baseline (v1.17.20) catalog 已比 SMARK (v1.17.18) 更新——源意图已被上游覆盖

## 目标
- packages/core/src/models-dev.ts（正确 owner，非 snapshot.ts）
- 添加 MODELS_DEV_EMBEDDED 常量
- 移除对 snapshot.ts 的错误注释

## Current
- SHA-256: df1e126f3e661e5a
- Bytes: 1094
- 1 个文件（models-dev.ts）

## 安装
- typecheck-0195-2026-07-27T16-27-07-996Z.md 通过：30/30 workspaces passed
- install reused: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f

## 测试
- typecheck: PASS（packages/core 全绿）
- 无行为测试（catalog 数据是构建时注入，非运行时行为）

## 失败修订
- 原 current 注释在错误文件（snapshot.ts 而非 models-dev.ts）且 E=0 C=1 → 移至正确 owner 并添加真实代码行
- v1 forward/reverse auditor 均 BLOCK（G-01 E=0 C=1）→ 修复为 E=1 C=2

## 最终行为
- MODELS_DEV_EMBEDDED 常量暴露构建时 catalog 嵌入状态
- tip 的 OPENCODE_MODELS_DEV 管线已完全覆盖源 patch 的 catalog 刷新意图
- catalog 数据通过 MODELS_DEV_API_JSON 环境变量在构建时获取
- 无运行时行为变更（catalog 数据在构建时注入，非运行时读取文件）
