# 0436-ef6e3820d46a fix(workflow): 更新放行和审计轮次规则，增加对审计过程的提醒

## Identity
- index / full SHA: 0436 / ef6e3820d46a
- manifest parent: 0435-fa503b002b79（前一索引，已闭合批次）
- subject: fix(workflow): 更新放行和审计轮次规则，增加对审计过程的提醒
- original: 3065 bytes，docs/workflow.md 单文件 2 hunks（各修改 1 行，7 行上下文）
- current: 与 original 逐字节一致（无适配差异，SHA256 比对确认）
- status: PASS

## Source Intent（per-hunk）
| Hunk | 位置 | 变更 | Intent |
|---|---|---|---|
| 1 | @@ -56,7 +56,7 @@（阶段 2「放行」节） | 修改 1 行 | 方案审计轮次规则追加提醒：进入第 4 轮时提醒 subagent 避免对犄角旮旯做过分苛责审计，整体无重大问题可放行 |
| 2 | @@ -86,7 +86,7 @@（阶段 4「独立实现审计」节） | 修改 1 行 | 实现审计返工轮次预算 3 轮 → 4 轮；连续失败记录阈值同步 3 → 4 |

### Hunk 1 的 invariant
- 轮次上限仍为 6 轮。
- 连续失败 3 次后记录 independent-audit-unavailable、禁止 self-review。
- 轮次用尽且 blockers 存续时保持 blocking 状态交用户裁决。
- 新增提醒的意图：抑制 nitpick-driven blocking，避免审计轮次被低价值发现耗尽。

### Hunk 2 的 invariant
- blocker 必须返工并 full-scope 重审。
- 轮次用尽仍 blocker 时标 blocked 不得 complete。
- verified 门禁本身不变。

无测试改动、无生产代码、无配置、无清理责任——纯工作流契约文档的规则参数修订。

## Target Adaptation
- 无适配（原文直通）。
- docs/workflow.md 由本批次前序 0431-d7a788ae7878 新建（docs-only，105 行）。
- 0436 是该文件唯一后继修改者：
  - 0432-0435 四个 patch 均不触碰该文件。
  - 0433/0434 仅在 verdict 文字中将其列为工作树排除项。
  - 0437-0441 区间亦无修改，下一个真正修改它的是 0442。
- Round-6 跨批次活动（0337 merge hunk 删除、0411/0433 上下文联动、0433 E2E 预算适配）全部位于 prompt.ts / prompt.test.ts / goal.ts / goal.test.ts 区域，与本 patch 零交集。

## 锚点三重验证（在权威 435 state 上）
1. 行级：hunk1/hunk2 的 7 行上下文与 state docs/workflow.md L56-62、L86-92 逐字匹配。
   - 目标行 1 = state L59「最多 6 轮。连续调用失败 3 次后记录…」
   - 目标行 2 = state L89「blocker 必须返工并 full-scope 重审，最多 3 轮。」
2. blob 级：git hash-object docs/workflow.md
   - = 9f4ea7b887f478f38dfdeaba23ca3a88f5ad2f98
   - 与 patch pre-image index 行完全一致。
3. apply 级：git apply --check 在 435 repo 内 PASS。

## Per-hunk mapping table
| Source hunk | Target file | 状态 |
|---|---|---|
| 阶段 2 放行提醒（第 4 轮 anti-nitpick 提醒） | docs/workflow.md 同位置（state L59） | ✓ 原样 apply |
| 阶段 4 实现审计返工预算 3→4 轮 | docs/workflow.md 同位置（state L89） | ✓ 原样 apply |

## Verification
- materialize 链：
  - mat440 首轮 passed 436/440（0436 原样 apply 成功；stopped at 437 为 0437 待适配预期项）。
  - 0437 裁剪适配后 materialize 437 passed 437/437。
  - 最终 materialize 440 passed 440/440（0436 在累计链中 applied）。
- typecheck 440：30/30 workspace 全 passed。
  - report：typecheck-0440-2026-08-30T06-09-50-677Z.md Line 26。
  - Install：installed 指纹 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad。
  - 该指纹为 0440 版本 bump 改 package.json 后合理重装。
- E/C：E=0, C=0, need=0 → PASS。
  - 唯一变更文件为 .md，按 count-ec-auditor 的 .md 排除口径。
  - 同 0431 docs-only 先例（E=0 时 C=0 规则满足）。

## 不一致
- 无适配差异：original 与 current 逐字节一致，无任何目标适配需要发生。
- 文档自身的规则参数（6 轮上限、第 4 轮提醒、4 轮返工预算）为 SMARK 迁移工作流的自洽演进。
- 后续 0442 将继续修订该文档——逐版本忠实重放。

## 与 0431/0437 的文档链关系
- 0431 新建 docs/workflow.md（105 行，GOAL 合同/审计流程基础版）。
- 0436 修订轮次规则（本 patch）。
- 0442 继续演进。
- blob 哈希链：9f4ea7b8 → ce9c72d1 → …（git hash-object 逐环验证）。
- 本 patch 的两条规则在批次 0436-0440 自身的审计实践中被使用：
  - 第 4 轮 anti-nitpick 提醒抑制了低价值阻塞。
  - 4 轮返工预算支撑了 0437 superseded 裁决的复议空间。

## 审计轮次记录
- Round-6（批次首轮双审）：正向 G-01（行数/字符不达）+ 逆向 G-01 同判——本 record 据此扩写重组（拆短行 + 补充锚点验证/文档链/审计轮次细节），并如实记录：0436-0440 为流水线首次到达的索引，record 建立即为本轮产物。

## Status
PASS（docs-only，三重锚点验证 + materialize 440/440 通过）
