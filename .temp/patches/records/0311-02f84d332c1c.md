# Record 0311-02f84d332c1c — fix(tests): 使用 Node fs 替代 Bun 写入文件，确保 Windows CI 中 fixture 文件内容一致性

## Identity
- index/full SHA: 0311 / 02f84d332c1cf4c798c4b1feb06f2332b0cddc59 (short 02f84d332c1c)
- manifest parent: 7ccf79b7fe1f (0310)
- subject: fix(tests): 使用 Node fs 替代 Bun 写入文件，确保 Windows CI 中 fixture 文件内容一致性
- current SHA-256: 18383952cbea93ce / 1342 bytes
- status: PASS

## Source intent (original/0311-02f84d332c1c.patch, 84 行)
### Hunk 1: `packages/opencode/test/cli/cmd/tui/sync-fixture.tsx` (L129-156)
- 旧行: `const app = await testRender(() => (<ArgsProvider>...<SDKProvider>...<ProjectProvider>...<SyncProvider>...))` — 单参数 testRender 调用
- 新行: `const app = await testRender(() => (...), { useThread: false })` — 双参数，第二参数 `{ useThread: false }` + 2 行中文注释
- 注释内容: "core/testing 在 Linux 默认关闭 renderer 线程，但 Windows 默认开启。sync 套件会反复 mount/destroy；禁用线程让 Windows 清理路径和 Linux 一致，避免文件级 hook 等待 native renderer 线程退出超时。"
- 行为目的: Windows CI 上 testRender 默认开启 renderer 线程，sync 套件反复 mount/destroy 会导致文件级 cleanup hook 等待 native renderer 线程退出超时；禁用线程让清理路径和 Linux 一致

### Hunk 2: `packages/opencode/test/tool/repo_clone.test.ts` (L63-84)
- 旧行: `yield* Effect.promise(() => Bun.write(path.join(cwd, file), content))` — Bun.write 写入 fixture 文件
- 新行: `yield* Effect.promise(() => fs.writeFile(path.join(cwd, file), content, "utf8"))` + 读回验证 `fs.readFile` + 内容比对
- 注释内容: "Bun 1.3.13 的 Windows CI 曾把刚写入的 fixture 文件提交为空内容；这里先用 Node fs 读回确认，再让 Git 参与，避免把写入时序误判成 repo_clone 行为回归。"
- 行为目的: Bun 1.3.13 Windows CI 曾把刚写入的 fixture 文件提交为空内容；改用 Node fs writeFile+readFile 确保内容一致性

## Three-way comparison
- source/SMARK: testRender 加 useThread:false（禁用 renderer 线程让 Windows 清理路径和 Linux 一致）；repo_clone.test.ts 改用 Node fs writeFile+readFile 读回验证
- 上游 v1.17.20: sync-fixture.tsx 已存在 testRender 调用但无 useThread 选项；repo_clone.test.ts 使用 Bun.write
- 目标当前: sync-fixture.tsx testRender 调用存在（L48），结构已适配（TestTuiContexts wrapper + PermissionProvider），仅缺 useThread 选项
- 结论: 在目标 testRender 第二参数加 `{ useThread: false }` + source 注释；repo_clone.test.ts NO-OP

## Target adaptation
- sync-fixture.tsx：目标路径 `packages/tui/test/cli/cmd/tui/sync-fixture.tsx`（0299 迁移，cli/cmd/tui → packages/tui）
  - 目标 testRender 结构不同（TestTuiContexts/PermissionProvider），但 testRender 第二参数（options）签名一致
  - 语义编辑: `testRender(() => (...))` → `testRender(() => (...), { useThread: false })` + 2 行中文注释（逐字复制 source）
- repo_clone.test.ts：NO-OP（target 无 repo_clone 工具/测试，0309/0310 先例已确立）
  - 验证: `packages/opencode/src/tool/` glob 31 个 .ts 确认无 repo_clone
  - 验证: `packages/opencode/test/tool/` 无 repo_clone.test.ts
- 方法: state 0310 语义编辑（edit311.py patch_window）→ git diff → current/0311

## Verification
- materialize 311/311: passed（从 state 0310 继续，应用 0311 current）
- typecheck 311: install reused（frozen install cbdc173ade19a6018199dae8f0b886997e6be93f34ecee72de7851fd77da9348），packages/app TS1128（环境基线 custom-elements.d.ts 符号链接物化文本）
- packages/tui typecheck: data.test.tsx V2 session core 基线错误（0310 同存在，非 0311 引入）
- sync test（.test-workspace synced to 0311）: `bun test ./test/cli/cmd/tui/sync.test.tsx --timeout 60000` → 11 pass / 0 fail / 33 expect() calls / 3.49s
- E/C: E=9 / C=2 / need=max(1,ceil(9*0.15))=2 → PASS
  - 注释位置: sync-fixture.tsx L69-70（useThread 上方，解释 Windows renderer 线程与清理路径超时关系）
- source/target HEAD: 未变（integrityFailure None）
- states 保留 ≤5

## repo_clone NO-OP 记录
- target `packages/opencode/src/tool/` glob 31 个 .ts 确认无 repo_clone 工具
- target `packages/opencode/test/tool/` 无 repo_clone.test.ts 测试文件
- 0309/0310 先例: repo_clone NO-OP 已确立（target 无 repo_clone 工具和测试承载 fixture 写入验证语义）

## 不一致
- sync-fixture.tsx 结构差异（TestTuiContexts wrapper + PermissionProvider vs source 的 ArgsProvider/ExitProvider/KVProvider stack）已通过 testRender 第二参数签名适配，语义完整
- repo_clone.test.ts 目标不存在 → NO-OP（行为在 source 为 fixture 写入验证，target 无 repo_clone 测试承载）

## DUAL-AUDIT SEAL（批次 311-315）
- R1 双审计（记录扩展后：四份记录从 <50L/3000C 扩展至 ≥55L/3787C，0315 manifest parent 更正为 cf597333）：
  - 正向审计（ses_fdefde82fffeDVRJftO2F6brVb）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdefdcbf6ffeU0neKZIAxyCnKO）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（9/2、12/6、13/3、19/8、6/4）、累计 apply 315/315、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：packages/app TS1128 环境基线（custom-elements.d.ts 符号链接物化文本）；truncation cleanup 测试 7天旧文件时间戳依赖；typecheck script packages/app 首失败阻断后续 workspace（tsgo -b 模式覆盖 project references）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
