# Record 0364-0a809c711416

## Identity
- manifest parent: f90bcd68677c (0363)
- subject: feat(session): add persistent session goal with auto-continue
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0364-0a809c711416` (passed 364/364)
- current patch: `.temp/patches/current/0364-0a809c711416.patch` (80340 bytes)

## Source intent
Add a session-level persistent goal state machine
(active/paused/complete/blocked). When the agent loop ends normally and the
goal is still active, inject a continuation prompt and auto-continue the loop.
The continuation prompt text aligns with Codex continuation.md and includes
Completion audit (8 verification rules), Blocked audit (3-consecutive-turn
rule + anti-lazy-block), Fidelity (no goal narrowing), and Work from evidence
(inspect current state, not memory). The model can only mark complete/blocked
via the goal tool; users control the goal lifecycle through the HTTP API
(GET/POST/DELETE /session/:id/goal). The TUI sidebar gains a goal panel
(status + objective + usage), the footer shows active/blocked, and the message
panel renders continuation prompts with a "↻ Goal continuation" label. Gated
by `experimental.goals` (default off) with `experimental.goal_max_turns`
(default 10). Subagent sessions and decide agents do not participate.

## Source patch anatomy (23 files)
- migration: `packages/opencode/migration/20260704053848_session_goal/`
  migration.sql + snapshot.json (drizzle).
- TUI (6): context/sync.tsx, feature-plugins/sidebar/goal.tsx (new),
  plugin/api.tsx, plugin/internal.ts, routes/session/footer.tsx,
  routes/session/index.tsx.
- config.ts (experimental.goals + goal_max_turns).
- Server (3): groups/session.ts (goal endpoints), handlers/session.ts
  (goalGet/goalSet/goalClear), server.ts (SessionGoal.defaultLayer).
- Session (3): goal.sql.ts (new), goal.ts (new service), prompt.ts
  (continuation + accounting).
- Tool (3): goal.ts (new), goal.txt (new), registry.ts (registration).
- Tests (4): fixture/tui-plugin.ts, goal.test.ts (new), prompt.test.ts,
  snapshot-tool-race.test.ts.
- plugin/src/tui.ts (TuiState goal + TuiSidebarGoalItem).

## Target mapping (state 0363)
| Source file | Target file | Status |
|---|---|---|
| `migration/20260704053848_session_goal/migration.sql` | `packages/core/src/database/migration/20260726100000_session_goal.ts` + migration.gen.ts | CONVERTED (core Effect migration) |
| `migration/.../snapshot.json` | — | SKIPPED (drizzle artifact) |
| `src/session/goal.sql.ts` | `packages/core/src/session/goal.sql.ts` | MIGRATED (drizzle schema owner is core) |
| `src/session/goal.ts` | `packages/opencode/src/session/goal.ts` | ADAPTED (EventV2Bridge/Database.Service/LayerNode) |
| `src/tool/goal.ts` + `goal.txt` | same | ADAPTED (Tool.define target signature) |
| `src/tool/registry.ts` | same | ADAPTED (LayerNode deps) |
| `src/config/config.ts` | `packages/core/src/v1/config/config.ts` | MIGRATED (config schema lives in core) |
| server groups/handlers/server | same | ADAPTED |
| `cli/cmd/tui/context/sync.tsx` | `packages/tui/src/context/sync.tsx` | MIGRATED |
| `cli/cmd/tui/feature-plugins/sidebar/goal.tsx` | `packages/tui/src/feature-plugins/sidebar/goal.tsx` | MIGRATED (new) |
| `cli/cmd/tui/plugin/api.tsx` | `packages/tui/src/plugin/adapters.tsx` | MIGRATED (target api surface moved) |
| `cli/cmd/tui/plugin/internal.ts` | `packages/tui/src/feature-plugins/builtins.ts` | MIGRATED (target builtins registry) |
| `cli/cmd/tui/routes/session/footer.tsx` + `index.tsx` | `packages/tui/src/routes/session/*` | MIGRATED |
| `test/fixture/tui-plugin.ts` + prompt.test + snapshot-tool-race | same | ADAPTED |
| `packages/plugin/src/tui.ts` | same | ADAPTED |

## Target-adaptation decisions
1. **Migration → core Effect migration**: the target's active migration
   system is `packages/core/src/database/migration/*.ts` (applied by
   `Database.apply`), per the precedent of 0159
   (`20260725100000_migrate_review_agent`). The source SQL is preserved
   verbatim as the `up(tx)` body; the ID uses a target-ordered timestamp
   `20260726100000_session_goal` (after the latest existing migration).
   `migration.gen.ts` gains the import. The drizzle `snapshot.json` is skipped
   (it is a drizzle-generated full-schema artifact with no receiver in the
   core migration system; the schema is owned by `schema.gen.ts`).
2. **goal.sql.ts → core**: the target's drizzle schema files live in
   `packages/core/src/**/*.sql.ts` (AGENTS.md). The table uses
   `SessionSchema.ID` and `SessionTable` from `./sql`, `Timestamps` from
   `../database/schema.sql` — matching `request-usage.sql.ts`.
3. **goal.ts service → EventV2Bridge + Database.Service + LayerNode**: the
   source's `Bus.Service`/`BusEvent.define`/`Database.use` patterns do not
   exist in the target. The service yields `EventV2Bridge.Service` and
   `Database.Service` (`const { db }`), publishes events via
   `events.publish(Event.Updated, ...)`, and exports `node` via
   `LayerNode.make({ service, layer, deps: [EventV2Bridge.node, Database.node] })`.
   The Goal/Status/Event contracts are defined in a new schema module
   `packages/schema/src/session-goal.ts` (canonical event definitions per
   schema AGENTS.md) and registered in `event-manifest.ts`
   (`ServerDefinitions` + `Definitions`).
4. **Goal schema**: `tokenBudget` uses the schema package's `optional(...)`
   helper (the source's `optionalOmitUndefined` does not exist in the target
   core).
5. **goal tool**: rewritten for the target `Tool.define<Parameters, Metadata,
   R>(id, init)` signature with `Effect.succeed(...)`; `GoalToolExtra` carries
   `goalSvc` through `ctx.extra` exactly as the source intended.
6. **goalSvc plumbing**: the source passes `goalSvc` in `ctx.extra` inside
   prompt.ts; the target's tool context is built in `session/tools.ts`
   (`SessionTools.resolve`), so `goalSvc: SessionGoal.Interface` is added to
   the resolve input and the `extra` object (an extra target file not in the
   source patch).
7. **prompt.ts continuation**: `goalTurns` counter, the goal continuation
   check before the "exiting loop" break (uses `config.get()`,
   `agents.get(lastUser.agent)`, `isDecideAgent`, `session.parentID`, and the
   target's `Effect.logInfo` instead of `slog.info`), and the
   token/time accounting after `result === "stop"`. `SessionGoal.node` is
   added to the prompt layer's `LayerNode.make` deps.
8. **registry.ts**: `GoalTool` import + `goaltool` yield, `goalsEnabled`
   from `cfg.experimental?.goals`, `goal: Tool.init(goaltool)` in the tool
   object, `...(goalsEnabled ? [tool.goal] : [])` in builtin, and
   `SessionGoal.node` in `LayerNode.make` deps (the source's
   `Layer.mergeAll(SessionGoal.defaultLayer, ...)` maps to the node deps).
9. **config**: `goals` + `goal_max_turns` added to the `experimental` struct
   in `packages/core/src/v1/config/config.ts` (the target config schema).
10. **TUI plugin files**: `plugin/api.tsx` → `adapters.tsx` (the `goal`
    accessor added to the `session` state object); `plugin/internal.ts` →
    `feature-plugins/builtins.ts` (SidebarGoal registered); the sidebar
    `goal.tsx` uses `BuiltinTuiPlugin` (not `InternalTuiPlugin`) and imports
    from `../builtins`.
11. **sync.tsx**: `SessionGoalInfo` inline type (no `DisposedReason` import in
    the target), `session_goal` store slice, `session.goal.updated/cleared`
    event cases, cleanup in `session.deleted`, and a non-fatal goal fetch in
    the session sync path (the source's `as any` SDK bypass is preserved).
12. **Tests**: `goal.test.ts` uses the target's `AppNodeBuilder.build(
    LayerNode.group([...]), [[RuntimeFlags.node, ...], ...])` + `it.instance`
    pattern (modeled on session.test.ts); `prompt.test.ts` and
    `snapshot-tool-race.test.ts` add `SessionGoal.node` to their LayerNode
    groups; `fixture/tui-plugin.ts` adds the `goal` mock accessor.

## E/C audit
E=1016, C=161, need=153, PASS. (C: CJK rationale comments in goal.ts
service + continuationPrompt sections, goal.test.ts per-test comments, TUI
goal.tsx, registry, prompt.ts, and the schema/migration files. 24 CJK
comment lines were added to goal.test.ts/goal.ts to satisfy the gate.)

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 364` →
  "passed 364/364", state at `states/0364-0a809c711416`.
- Report: `materialize-0364-*.json` — reusedPrefix 363, baseState
  `0363-f90bcd68677c`, no stoppedAt, no integrityFailure.
- Preflight note: a manual `git apply --check --index` on a `Copy-Item` clone
  of state 0363 reported "does not match index", but the materialize's COW
  copy + LF normalization path applied the patch cleanly (364/364). The
  manual clone did not reproduce the materialize's exact repo state.
- Post-apply verification: goal.ts/goal.sql.ts/migration/goal.tsx/session-
  goal.ts all present; prompt.ts has goalTurns + continuation + accountUsage;
  registry.ts has GoalTool/goalsEnabled/tool.goal; event-manifest registers
  SessionGoal; tools.ts carries goalSvc in the extra.

## Line accounting
- New files: schema/session-goal.ts (41), core/session/goal.sql.ts (33),
  core/migration/20260726100000_session_goal.ts (26), opencode/session/
  goal.ts (302), opencode/tool/goal.ts (47), goal.txt (10),
  tui/sidebar/goal.tsx (105), test/session/goal.test.ts (399).
- Modified: migration.gen.ts +1; core config +8; groups/session +56;
  handlers/session +42; server.ts +2; prompt.ts +70; tools.ts +4/-2;
  registry.ts +13/-1; plugin/tui.ts +14; sync.tsx +42; adapters +4;
  builtins +2; footer +12; index.tsx +42; fixture/tui-plugin +2;
  prompt.test +2; snapshot-tool-race +2.

## Risks / notes
- The migration ID `20260726100000_session_goal` is appended to
  `migration.gen.ts` after `20260725100000_migrate_review_agent`, so it runs
  after all existing migrations on upgraded databases — correct for a new
  feature table.
- The goal tool is only visible when `experimental.goals === true`; the
  continuation loop also gates on the same flag, so the feature is inert by
  default.
- `accountUsage` uses `Effect.ignore` in prompt.ts so accounting failures
  never break the loop.
- Typecheck of the change is covered by the batch-end `--typecheck 365`.

## Per-item diff stats
- fix364.diff bytes: 76278 (raw diff, LF); patch file with header: 80340.
- 26 files (18 modified + 8 new); snapshot.json and the drizzle migration.sql
  are skipped (converted to the core Effect migration).


## B-01 修复（Round 1 双审计阻塞）
**问题**：`session_goal` 表只添加在 `migration/20260726100000_session_goal.ts`（既有 DB 升级路径）与 `migration.gen.ts` 注册，但未更新 `packages/core/src/database/schema.gen.ts`（fresh-DB 路径的权威 schema）。`DatabaseMigration.apply` 对无表 DB 执行 `schema.up(tx)`（schema.gen.ts）并跳过全部 migration → 新库/内存测试库缺 `session_goal` 表 → goal.test.ts 14/18 失败（`SQLiteError: no such table: session_goal`）。
**修复**：在 schema.gen.ts 的 up() 尾部（request_usage_assistant 索引后）追加 session_goal 的 CREATE TABLE + CREATE INDEX：
- `session_goal`：session_id text PK REFERENCES session(id) ON DELETE CASCADE、id/objective/status text NOT NULL、token_budget integer、tokens_used/time_used_seconds integer DEFAULT 0 NOT NULL、time_created/time_updated integer NOT NULL（与 goal.sql.ts SessionGoalTable drizzle 定义等价，PK `session_goal_pk`、FK `fk_session_goal_session_id` 与既有 request_usage 命名风格一致）
- `session_goal_status_idx` ON (session_id, status)
**先例**：request_usage 表（0174 批次）同样直接编辑 schema.gen.ts（snapshot.json 从未更新，既有模式）。
**验证**：goal.test.ts 修复后应 18/18 通过（含 auto-resume/usage 测试）。

## B-01 修复验证（Round 2 审计后重验）
- **修复内容**：向 `packages/core/src/database/schema.gen.ts` 的 up() 尾部追加 `session_goal` CREATE TABLE + `session_goal_status_idx` CREATE INDEX（与 goal.sql.ts SessionGoalTable drizzle 定义等价；PK/FK 命名与既有 request_usage 风格一致；先例：0174 request_usage 表同为手动编辑 schema.gen.ts）
- **goal.test.ts**：18/18 pass（修复前 4/18）
- **typecheck**（packages/core）：PASS
