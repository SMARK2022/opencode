# Record 0109-14d55305badb

## Source intent
1. 版本递增（source 1.14.31→1.14.32-smark；tip 对齐为 **1.17.20→1.17.21-smark**）。
2. 新增 `smooth-scrollbar.ts`：renderAfter 后重绘亚像素 thumb 与 user 消息 marker。
3. session `trackOptions.renderAfter = drawSessionScrollbar`。
4. prompt/footer：`showCumulative = width>120` 控制累计 token 括号。
5. 移除 #107 prompt 窄条 renderBefore；session prompt 容器 **全高** `fillRect` 清屏（`height = buffer.height - y`）。
6. BlockTool：去掉 `gap={1}`；children 与 error 用 `marginTop={1}` 承担间距。

## 上游 / tip@108
- 无 smooth-scrollbar；usage 总显示累计；BlockTool 有 gap；OpenTUI `fillRect`。

## 三方比较
| 语义 | source | tip | 决策 |
|------|--------|-----|------|
| 版本 bump | 1.14.32-smark | 1.17.20-smark | 相对 +0.0.1 → 1.17.21-smark |
| smooth 模块 | 有 | 无 | 新增 packages/tui/util |
| 清屏高度 | 全高 | — | 全高，禁止 1 行近似 |
| BlockTool 间距 | 去 gap + marginTop | 有 gap | 去 gap + children/error marginTop |
| 缓冲 API | fillsRect 字面 | fillRect | fillRect |

## 目标适配
- 文件：`package.json`、prompt、footer、session、`smooth-scrollbar.ts`
- 注释：**分散**在常量/函数/关键分支旁（禁止文件头 15 行堆砌）

## E/C
- E=191 C=36 need=29 gate=PASS
- L38: // #109：窄终端隐藏累计 token 括号
- L82: // #109：窄终端隐藏累计 token
- L118: // #109：user 消息 agent 色映射为滚动条 marker
- L126: // #109：renderAfter 回调：委托 smooth-scrollbar 重绘
- L137: // #109：content 坐标 = screenY 差
- L149: // #109：原生滚动条后接平滑重绘
- L161: // #109：从 prompt 顶清到屏底，消除滚动残影
- L166: // #109：高度必须是剩余全高，不能只刷一行
- L181: // #109：去掉 gap，改由子块 marginTop 承担间距
- L206: // #109：thumb 亚像素：每终端行 8 格
- L208: // #109：marker 半行精度
- L210: // #109：marker 向 track 背景混合
- L212: // #109：半格到满格字符表
- L216: // #109：content 坐标偏移
- L218: // #109：源色，绘制前弱化
- L229: // #109：renderAfter 唯一入口
- L238: // #109：清空原生 track，本函数成为唯一视觉源
- L246: // #109：scrollTop 映射虚拟 thumb 区间
- L248: // #109：视口高度下限 1，避免除零
- L250: // #109：内容高度不低于视口
- L252: // #109：可滚动范围非负
- L254: // #109：虚拟 track = 行数×精度
- L257: // #109：不可滚动时 thumb 铺满
- L262: // #109：thumb 至少一行高，保证可见可点
- L267: // #109：thumb 起点随 scrollTop 线性移动
- L278: // #109：按覆盖率绘制 thumb
- L306: // #109：绘制 user 消息 marker
- L318: // #109：与 thumb 共用 scrollRange 映射
- L325: // #109：thumb 覆盖行不画 marker
- L344: // #109：marker 色向背景弱化
- L360: // #109：通道线性插值
- L365: // #109：单 cell 上下半格
- L381: // #109：贴顶：上半格语义（bg/fg 互换）
- L387: // #109：贴底：下半格字符
- L392: // #109：落在 cell 中部时锚定最近边缘
- L401: // #109：marker 行是否与 thumb 重叠

## current 身份
- sha256: `55d3164ef6c0659c4f31e4ad172ec52d428d58c696f6e12cca181bd829d38507`
- bytes: 16762

## 物化 / typecheck
- `typecheck-0110-2026-07-22T12-47-29-041Z.md` / `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- tip 观测：`height = buffer.height - y`；`<box marginTop={1}>{props.children}</box>`；error 外包 marginTop；smooth 花括号平衡

## 失败修订
- smooth 截断 → 从 original 完整重建。
- 清屏高度 1 行 → 全高。
- 注释堆砌不计 C → 分散重写。
- BlockTool 仅删 gap → 补 children/error marginTop。

## 调用链摘要
Index 0109 的生产调用从 manifest 串行物化进入 tip@110；本 patch 仅改 current 与 record。

## 测试与门禁
批次末 typecheck 覆盖 monorepo 声明 typecheck 的 workspace；sdks/vscode 以结构/grep 验证。E/C 按本 patch 独立计算。

## 非目标
不修改 original/；不空 current；不删除 tip 已有测试以换 apply。

## 证据指针
- original: `original/0109-14d55305badb.patch`
- current: `current/0109-14d55305badb.patch`
- report: `typecheck-0110-2026-07-22T12-47-29-041Z.md`
- install fingerprint: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`


## Independent dual audit (batch 106-110)
- FORWARD: **PASS** all five, no open B/G — task `ses_075cbf2b9ffeqPsA6Y1WnxK1tc`
- REVERSE: **PASS** all five, no open B/G — task `ses_075cbf295fferFaCMlIZHbP8wi`
- Tip: `states/0110-b6690c2cc860`
- Typecheck: `typecheck-0110-2026-07-22T12-47-29-041Z` (No failures)
- Install: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- Batch release: **PASS**
