# Record 0258-6b7ac3cdcb6b — daemon 侧 provider endpoint 状态查询

## Source（original/0258-6b7ac3cdcb6b.patch）

1. 新建 `src/server/shared/tui-endpoint-status.ts`：daemon 进程级 endpoint 状态 cache（ACTIVE_WINDOW 10s、REFRESH_INTERVAL 1s、PROBE_TIMEOUT 3s），inFlight Promise 并发去重，HEAD 探测经 NetworkProxy 路由，仅 502/503/504 判 down，异常返回 down + null latency。
2. HTTP API：`groups/tui.ts` 新增 `ProviderEndpointStatusQuery`（url query + WorkspaceRoutingQueryFields）与 `ProviderEndpointStatus` wire schema（ok/down、latency、route direct/proxy + reason、checkedAt；不暴露 init 态与 proxy URL）；`TuiPaths.providerEndpointStatus` + GET endpoint 注册。
3. handler：`handlers/tui.ts` `providerEndpointStatus` 调 `getEndpointStatus`，不可规范化 URL 返回 BadRequest。
4. TUI 侧：`sdk.tsx` 抽 `fetchWithHeaders`（raw daemon fetch 携带 attach/auth headers）；`sidebar/context.tsx` ping 改为订阅 daemon `/tui/provider-endpoint-status`（移除 TUI 进程内 NetworkProxy 直连探测）。
5. 测试：`tui-provider-endpoint-status.test.ts` 4 用例（可达 200+hits=1、并发合并 hits=1、不可达 200+down、非法 URL 400）；`httpapi-exercise` 新 scenario。

## 目标适配与三方比较

- current 为本工作区完整适配版（此前轮次已按目标 HttpApi/effect unstable API、TUI plugin API 与 0122/0139 既有语义重写），本项物化零修改即通过；本次仅补注释。
- 语义核查：目标无任何 daemon 侧 endpoint 状态能力（0258 之前 sidebar 在 TUI 进程内直连 NetworkProxy 探测——source 正是针对该缺陷：多 TUI 重复探测、env proxy 与真实请求来源不一致、无人使用时仍持续后台请求）。daemon cache + ACTIVE_WINDOW 逐出 + 并发去重为 source 全量语义，全部落位。
- 与上游关系：上游（dev-smark tip）无对应能力，属 SMARK feature；在目标 owner（server/shared + tui feature-plugins）按目标 Effect/HttpApi 风格从零构建，未引入重复 owner：sidebar 不再直连 NetworkProxy，仅订阅 daemon。
- 安全边界保留：wire 不含 proxy URL（防凭据泄漏）、只允许 http(s) origin、BadRequest 不泄漏解析细节、loopback 强制 direct。

## current 修改内容（sha256=adfb4637e64e2d4ae0b5b99becfe60045ee4bc618618940dff1f028f08750c5e，25105 bytes）

- 本轮新增 13 条 #258 中文契约注释：tui-endpoint-status.ts 4（inFlight 去重入口/finally 清空/HEAD 语义/网关错误口径）、测试 4（hits=1 首探/并发合并核心断言/200+down 不变量/400 前置拒绝）、sidebar 3（客户端独立超时/await 后 active 守卫/down 语义委托）、sdk.tsx 1（header 覆盖优先级）、exercise 1（loopback direct）。
- 生产代码零改动；无 source hunk 删除。

## 逐 hunk 映射（original → current）

| original 文件/hunk | current 对应 | 适配说明 |
|---|---|---|
| shared/tui-endpoint-status.ts（新文件 139 行） | 同路径新建 | 常量三联/Entry.inFlight 去重/normalize http(s) 校验/probe HEAD+超时/502-504 判 down 逐项落位 |
| groups/tui.ts schema+paths+endpoint | 同路径 | WorkspaceRoutingQueryFields 展开、ProviderEndpointStatus wire 不含 init/代理 URL |
| handlers/tui.ts providerEndpointStatus | 同路径 | BadRequest 语义（不可规范化 URL）逐字保留 |
| sdk.tsx fetch 替换 | 同路径 | fetchWithHeaders 抽取 + attach/auth headers 携带 |
| sidebar/context.tsx ping 重写 | 同路径 | TUI 进程内 NetworkProxy 直连探测完全移除（旧 owner 不保留为 fallback），改为订阅 daemon |
| httpapi-exercise scenario | 同路径 | loopback 未监听端口验证 200+down+direct |
| tui-provider-endpoint-status.test.ts（新 141 行） | 同路径新建 | 4 契约用例（hits=1/并发合并/200+down/400） |

## 审计修复

- 双审计指出 C=49 < need=62 → 本轮补 13 条 #258 中文契约注释（inFlight 去重入口/finally 清空/HEAD 语义/网关错误口径/测试 hits=1 首探/25ms 窗口合并/200+down 不变量/400 前置拒绝/客户端独立超时/await 后 active 守卫/down 语义委托/header 覆盖优先级/loopback direct）。
- 补注释后重跑 sync-hunks.ps1 修正新文件 hunk 头 b 计数 → materialize 258 --rebuild passed → typecheck 全绿 → 行为测试重跑 4/0。

## E/C

- E = 423（plus 404 + minus 19）；C = 65 ≥ need = ceil(423×0.15) = 64。
- C 分布：endpoint-status 常量三联 3 + Entry/timer/schema/API/handler/exercise 测试等既有 + 本轮新增分散于 6 文件。

## 验证

- materialize 258 --rebuild: passed 258/258。
- typecheck 258: 全部 workspace passed（install reused 549c6966）。
- 行为测试：`bun test test/server/tui-provider-endpoint-status.test.ts --timeout 120000`（.test-workspace@258）→ 4 pass / 0 fail / 13 expect。
- E/C 复核：C=65 ≥ 64 达标。

## V1/V2 与 owner 判定

- endpoint 状态观测在上游 v1.17.20 与目标均不存在——纯 SMARK feature；不存在 V1 被移除需要迁移的语义。
- owner 唯一性：daemon cache 是唯一探测者；TUI 旧 NetworkProxy 直连路径被删除而非保留为 fallback——无双 owner、无隐藏成功路径；endpoint-status 测试用真实 TCP probe server，断言可在生产回归时失败（非同义反复）。

## 最终行为

- daemon 成为 provider endpoint 状态的唯一观测源：多 TUI 共享 cache，1s 内至多一次真实 HEAD，10s 无查询即逐出；TUI sidebar 只订阅 daemon 结果（proxy 路由、延迟、ok/down），本进程不再直接探测 provider；受保护连接下 raw fetch 携带与 SDK 相同的 attach/auth headers。
- 批次 255-259 状态：255/256/257/258 PASS；259 预研完成待物化。
