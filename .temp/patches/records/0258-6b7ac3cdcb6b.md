# Record 0258-6b7ac3cdcb6b — feat(endpoint-status): daemon 侧 provider endpoint 状态查询

## Manifest
- index: 258, parent: 0257-60f600d1dcb2, sha: 6b7ac3cdcb6b92622ca6a40e55a824b805d8a85c
- source: SMARK2022 <3222301373@qq.com> 2026-05-29T16:21:03+08:00
- subject: feat(endpoint-status): 添加deamon侧 provider endpoint 状态查询功能，支持 TUI 读取 daemon 统一网络状态
- original: original/0258-6b7ac3cdcb6b.patch（23723B，6 文件）
- current: current/0258-6b7ac3cdcb6b.patch

## 状态起点
- current 258 原为 original 零适配拷贝（与 original 逐字节一致）。

## Source 行为（original 全文 544 行）
1. **sdk.tsx**（hunk1-2，L40/L207）：`fetchWithHeaders(input, init)` 合并 props.headers + init.headers 后调 (testTransport?.fetch ?? fetch)；sdk.fetch = fetchWithHeaders——raw daemon fetch 必须携带与生成 SDK 相同的 attach/auth headers
2. **sidebar/context.tsx**（hunk3-4，L6/L20/L109）：删 NetworkProxy import、加 useSDK；ping 改为 GET /tui/provider-endpoint-status?url=&directory=（sdk.url + sdk.directory），3s timeout，res.ok 检查，json 解析 {status: ok|down, latency, route{type}}；setIsProxy(status.route.type==="proxy")；down→setPingStatus("down")+latency null；catch→down。proxy/latency 必须由 daemon 统一计算（多 TUI 复用同一 daemon）
3. **groups/tui.ts**（hunk5-7，L4/L32/L45/L172）：import WorkspaceRoutingQueryFields；ProviderEndpointStatusQuery = Struct({...WorkspaceRoutingQueryFields, url: String})（query 非 body 保幂等 GET）；ProviderEndpointStatus = Struct({url, status: Literals(["ok","down"]), latency: NullOr(Number), route: Struct({type: Literals(["direct","proxy"]), reason: String}), checkedAt: Number})（不暴露具体 proxy URL 防凭据泄漏；wire 无 init）；TuiPaths.providerEndpointStatus = `${root}/provider-endpoint-status`；HttpApiEndpoint.get("providerEndpointStatus", ...) query/success/error: BadRequest + OpenApi 注释
4. **handlers/tui.ts**（hunk8-10，L1/L103/L124）：import getEndpointStatus from "@/server/shared/tui-endpoint-status"；providerEndpointStatus Effect.fn：getEndpointStatus(ctx.query.url) → null 则 BadRequest；.handle 注册
5. **新文件 shared/tui-endpoint-status.ts**（138 行）：
   - ACTIVE_WINDOW=10_000（超窗丢弃，避免无人用 sidebar 持续 HEAD）、REFRESH_INTERVAL=1_000（多 TUI 共享 daemon cache 一秒最多一次探测）、PROBE_TIMEOUT=3_000
   - Entry {status?, lastSeen, inFlight?}；entries Map；timer unref
   - normalizeEndpoint(input)：URL parse + 仅 http/https → return url.origin；否则 undefined
   - getEndpointStatus(input)：normalize → entries 取/建 → lastSeen 更新 → ensureTimer → shouldRefresh ? refresh : entry.status
   - createEntry：首次不写 status（handler 等首个 probe 完成再响应）
   - shouldRefresh：inFlight || !status || now-checkedAt >= 1s
   - ensureTimer：unref setTimeout(tick, 1s)
   - tick：删超窗 entry；shouldRefresh → refresh；非空续 timer
   - refresh：inFlight 复用；probe().then(存 status).finally(清 inFlight)
   - probe(url)：NetworkProxy.resolveProxyRoute + routedFetch HEAD（3s timeout, purpose provider）→ 502/503/504 → down，else ok；catch → down
6. **httpapi-exercise**（hunk11，L1341）：scenario GET /tui/provider-endpoint-status?url=http://127.0.0.1:1 → 200，body.url 归一化 origin、status down、route.type direct
7. **新测试 tui-provider-endpoint-status.test.ts**（134 行，4 用例）：
   - reachable：probeServer 204 → 200、hits 1、body ok/direct、latency/checkedAt Number
   - coalesce：25ms delay + 并发 2 请求 → hits 1、body 相同
   - down：close 后 → 200、status down、latency null、direct
   - malformed："not a provider URL" → 400
   - Server.Default().app.request 用 x-opencode-directory header

## 目标适配（待完成）
- 路径迁移：packages/opencode/src/cli/cmd/tui → packages/tui/src（?）；sidebar/context.tsx → packages/tui/src/feature-plugins/sidebar/context.tsx（?）
- @tui/context/sdk、@/server/... 等路径别名
- NetworkProxy 在 core 的导出路径
- testTransport fetch、sdk.directory/sdk.url 目标可用性

## E/C（待统计）
## 验证（待）

## E/C 最终（count-ec.py 规则）
- E=310, C=47, need=47, PASS
- 注释分布：#258 raw daemon headers 合并、proxy/latency daemon 统一计算、query 幂等 GET、wire 只暴露 ok/down、不泄漏 proxy URL、daemon 统一观测源、TUI directory 路由、normalizeEndpoint HTTP(S) 限制、handler 等待首个 probe、entries 进程级单例、checkedAt 时间戳、BadRequest 语义、x-opencode-directory 路由、sdk fetch 分工

## 验证（最终）
- materialize 258: passed 258/258
- typecheck: opencode exit 0、tui exit 0、sdk/js exit 0（需全新 install——tsgo 增量缓存在 reused install 下会看到旧 schema；删除 install-state + node_modules 后 --typecheck 258 全新 install 通过）
- tui-provider-endpoint-status.test.ts: 4 pass / 13 expect
- compaction 256/257 测试仍 pass（preserves recent user 5 expect）
- httpapi-exercise: provider-endpoint-status scenario PASS（fail=3 是 pre-existing request_usage missing，非 258 引入）
- packages/app TS1128 pre-existing（0247 已记录）

## 修订历程
- v1（original 零适配拷贝）→ 状态编辑生成 current → materialize 258 通过
- v2：测试文件 Session.defaultLayer 不存在 → 改用 tmpdir + Server.Default().app.request + Effect.scoped；test 未 import → 加 test from bun:test
- v3：E/C 不足（C=42 need=47）→ 补 5 条 #258 注释（+前缀修复 + recount hunk）
- v4：typecheck 报 recent_user_messages 不存在——tsgo 增量缓存在 reused install 下看到旧 schema（仅 3 hidden reasons 无 recent_user_messages）；删除 install-state + node_modules 强制全新 install 后通过
- final：E=310, C=47, need=47 PASS

## DUAL-AUDIT SEAL（批次 256-260，第四轮）
- 正向审计：ses_01b54d401ffemp5AR8CXdwm7Os — 5/5 PASS，无 B/G
- 逆向审计：ses_01b54b279ffeW0pzFx4AcCOlO2 — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前三轮 BLOCK 修复：(1) 259 B-01 删除测试→恢复 10 测试；(2) 259 B-01 测试敏感性→printDaemonExitHint 导出+直接调用；(3) 260 G-01 record 过短→83 行/3218 字符；(4) 257 G-01 record 字符不足→60 行/3308 字符
- materialize 260: passed 260/260
- opencode/tui/schema typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
