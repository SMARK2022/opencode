# Record: Patch 21 — b94a79527811

## 1. Manifest Entry

- **Index**: 21
- **SHA**: b94a79527811d967986a92a4fb8ba572801beebf
- **Parent**: 0020-aa6db0f3264f
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加用户输入和请求开销估算功能，优化上下文管理

## 2. Source Behavior (original patch)

### 新增文件
- `packages/opencode/src/cli/cmd/tui/util/token-estimate.ts` (177 行)
  - 提取 token 估算逻辑为共享模块，包含 8 个导出函数
  - `estimateDataUrlInputTokens`: 估算 base64 图片/PDF 的输入 token
  - `estimateUserInputTokens`: 估算用户输入部件列表的 token
  - `estimateRequestOverhead`: 系统提示词 + 工具定义开销
  - `sumConfirmed`: 累加 step-finish 的已确认 token
  - `getContextSize`: 获取助手消息的上下文大小
  - `getBootstrapInputTokens`: 引导估算——用户输入 + 系统开销 或 上一轮上下文 + 用户输入
  - `computeFinalTokens`: 当前 step 确认值 + 流式估算 + 引导 token
  - `getContextLimit`: 获取上下文窗口大小限制

### 修改文件 (3 个消费者)
- `prompt/index.tsx`: 替换内联 `sumConfirmed` 为共享模块调用；添加 `getBootstrapInputTokens` 引导估算；用 `computeFinalTokens` 替换内联流式估算逻辑；`requestAssistants.length === 0` 时显示引导 token 而非零值
- `sidebar/context.tsx`: 同上模式；添加 `isRunning` memo；`requestAssistants.length === 0` 时显示引导 token
- `subagent-footer.tsx`: 同上模式；`requestAssistants.length === 0` 时显示引导 token

### 行为目的
- **引导估算 invariant**: 助手回复前显示预估输入 token = 用户输入 + 系统开销（首轮）或 上一轮上下文 + 用户输入（后续轮）
- **流式估算 invariant**: `computeFinalTokens` 统一封装当前 step 确认值 + pending 工具估算 + 引导 token
- **currentInput 取较大值**: `Math.max(currentStepInputConfirmed, bootstrapInputTokens)` 确保显示不低于引导估算
- **错误语义**: 无新错误路径，所有函数返回数值
- **清理责任**: `computeFinalTokens` 无副作用，不需要清理

## 3. 上游 (v1.17.20) 行为

- 三个消费者文件（prompt, sidebar, footer）各自内联 `sumConfirmed` 和流式估算逻辑
- `assistants.length === 0` 和 `requestAssistants.length === 0` 时返回零值
- 无引导估算功能
- TUI 路径从 `packages/opencode/src/cli/cmd/tui/` 迁移到 `packages/tui/src/`
- 导入路径从 `@/util/token` 迁移到 `@opencode-ai/core/util/token`

## 4. 目标适配 (current patch)

### 路径迁移
- `packages/opencode/src/cli/cmd/tui/util/token-estimate.ts` → `packages/tui/src/util/token-estimate.ts`
- `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx` → `packages/tui/src/component/prompt/index.tsx`
- `packages/opencode/src/cli/cmd/tui/feature-plugins/sidebar/context.tsx` → `packages/tui/src/feature-plugins/sidebar/context.tsx`
- `packages/opencode/src/cli/cmd/tui/routes/session/subagent-footer.tsx` → `packages/tui/src/routes/session/subagent-footer.tsx`

### 导入迁移
- `import { estimate as estimateTokens } from "@/util/token"` → `import { estimate } from "@opencode-ai/core/util/token"`

### V2 适配
- 保留 V2 的 `session?.cost` 模式（prompt/index.tsx 和 subagent-footer.tsx），不回退到 `requestAssistants.reduce((sum, item) => sum + (item.cost || 0), 0)`
- 保留 V2 的中文注释（来自 patch 12 的 `// 累加已确认的 step-finish token` 等），在替换内联逻辑时自然移除
- sidebar/context.tsx 添加 `isRunning` memo 使用 `props.api.state.session.status()`

### 保留的 SMARK 语义
- 引导估算功能完整实现：`getBootstrapInputTokens` 在三个消费者中调用
- `computeFinalTokens` 统一封装流式估算逻辑
- `currentInput = Math.max(currentStepInputConfirmed, bootstrapInputTokens)` 保留
- `promptInputEstimate = requestConfirmed.input === 0 ? bootstrapInputTokens : 0` 保留

### 保留的上游优势
- V2 路径结构和导入规范
- V2 的 `session?.cost` 模式
- V2 的中文注释风格

## 5. E/C 计算

### 有效修改行 E
- token-estimate.ts (新文件): ~120 行实质代码（类型 + 常量 + 8 个函数）
- prompt/index.tsx: ~20 行净新增（import, getParts, requestModel, bootstrapInputTokens, computeFinalTokens 调用, pct 变更）
- sidebar/context.tsx: ~25 行净新增（import, isRunning, bootstrap, computeFinalTokens 调用）
- subagent-footer.tsx: ~20 行净新增（import, getParts, requestModel, bootstrapInputTokens, computeFinalTokens 调用）
- **总 E ≈ 185**

### 合格中文注释 C
token-estimate.ts 中的中文注释（37 条）：
1. `// 系统提示词的 token 开销估算常量` — 解释常量用途
2. `// 服务端组装的系统提示包含模型身份、工作目录...` — 解释系统提示组成
3. `// 工具定义的 token 开销估算常量` — 解释常量用途
4. `// 每个工具包含名称、描述和 JSON Schema...` — 解释工具定义组成
5. `// 估算 data URL（base64 编码的图片/PDF）的输入 token 数` — 函数用途 + base64 细节
6. `// 图片按每 750 字符约 1 token 估算（Anthropic 视觉编码），上限 1600 token` — 估算比率和上限
7. `// PDF 按 base64 负载密度估算，每 1100 字符约 1 token` — 估算比率
8. `// 图片 token 随分辨率增长，但 Anthropic 对单图有 1600 token 上限` — 边界约束
9. `// 估算用户输入部件列表的 token 数` — 函数用途
10. `// 遍历 text/file/agent/subtask 四种部件类型...` — 遍历范围
11. `// synthetic 和 ignored 的 text 部件跳过，因为它们不发送到 API` — invariant
12. `// 估算单次请求的系统开销：系统提示词 + 工具定义` — 组成说明
13. `// 此值用于引导估算——当尚无助手回复时...` — 用途说明
14. `// 累加指定助手消息列表中所有 step-finish 部件的已确认 token` — 函数用途
15. `// step-finish 是 API 返回的每轮确认点...` — 语义说明
16. `// input 包含 cache.read + cache.write，因为缓存 token 也计入上下文窗口` — invariant
17. `// 获取指定助手消息的上下文大小` — 函数用途
18. `// 从部件列表末尾向前查找最后一个 step-finish...` — 查找方向原因
19. `// 末尾查找是因为上下文大小以最新一轮确认为准...` — invariant
20. `// 引导估算：在助手回复前预估用户输入的上下文 token` — 核心概念
21. `// 策略：如果存在上一轮助手消息...` — 策略说明
22. `// previousAssistant 通过 parentID 不匹配最后用户消息来排除当前请求的助手` — 算法细节
23. `// 有历史上下文时累加用户输入；否则用用户输入 + 系统开销` — 分支逻辑
24. `// 计算最终 token：当前 step 确认值 + 流式估算 + 引导 token` — 核心概念
25. `// 流式估算从最后一个 step-finish 之后的部分部件提取...` — 估算范围
26. `// hasInFlightTail 判断尾部是否有未完成工具调用...` — 判断逻辑
27. `// requestConfirmed.input 为 0 时（首轮请求），用引导 token 补充 totalInput` — 边界处理
28. `// 字符数 / 4 是通用 token 估算比率...` — 常量解释
29. `// 尾部有进行中的工具调用时当前 step 尚未确认，currentStep 显示 0` — invariant
30. `// currentInput 取 currentStepInputConfirmed 和 bootstrapInputTokens 的较大值...` — 显示策略
31. `// 确保显示不低于引导估算，避免首轮显示 0 后跳变的用户体验问题` — UX 原因
32. `// 获取上下文窗口大小限制` — 函数用途
33. `// 用于计算 context 百分比，在引导估算和实时估算中均被调用` — 用途说明

消费者文件中的中文注释：
34. `// 引导估算：在助手回复前预估用户输入的上下文 token` (prompt)
35. `// 查找当前请求对应的模型，用于计算 context 百分比` (prompt)
36. `// 引导 token：用户输入 + 系统开销 或 上一轮上下文 + 用户输入` (prompt)
37. `// 计算引导 token 占上下文窗口的百分比` (prompt)
38. `// 从最后一条消息添加流式估算，统一委托给 computeFinalTokens` (prompt)
39. `// 使用 actualTotalTokens 计算百分比，与显示值一致` (prompt)
40. `// 引导估算：在助手回复前预估用户输入的上下文 token` (sidebar)
41. `// 无助手消息时显示引导估算值（运行中）或零值（空闲）` (sidebar)
42. `// 引导估算：显示预估的输入 token 和百分比` (sidebar)
43. `// 引导估算：在助手回复前预估用户输入的上下文 token` (footer)
44. `// 查找当前请求对应的模型，用于计算 context 百分比` (footer)
45. `// 引导 token：用户输入 + 系统开销 或 上一轮上下文 + 用户输入` (footer)
46. `// 计算引导 token 占上下文窗口的百分比` (footer)
47. `// 从最后一条消息添加流式估算，统一委托给 computeFinalTokens` (footer)

- **总 C ≈ 47**（含 token-estimate.ts 中的 33 条 + 消费者中的 14 条）
- 排除纯 identifier 翻译后 **C ≈ 35**
- **最低 C = max(1, ceil(185 * 0.15)) = 28**
- **35 ≥ 28，通过**

## 6. 验证

### Apply
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 25 --rebuild`
- 结果: passed 25/25

### Typecheck
- packages/core: passed
- packages/opencode: passed
- packages/tui: passed

### Install 指纹
- `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史

1. **首轮审计 BLOCK (B-01)**: 消费者未接线——current patch 只包含 token-estimate.ts 模块，三个消费者文件的内联 `sumConfirmed` 和流式估算逻辑未被替换为共享模块调用，引导估算功能完全缺失
   - 修复: 从 state 20 复制三个消费者文件，应用 SMARK 原始 patch 的消费者变更（路径迁移到 V2），生成新的 current patch 包含 4 个文件

2. **首轮审计 BLOCK (G-01)**: 中文注释比例不足——E≈110, C≈7, 需要 C≥17
   - 修复: 重写 token-estimate.ts，将简单翻译注释替换为解释 invariant、边界、常量、策略的实质性中文注释，C 提升到 35+

## 8. 最终行为

- token-estimate.ts 共享模块被三个消费者正确导入和使用
- 引导估算在助手回复前显示预估输入 token
- `computeFinalTokens` 统一封装流式估算逻辑
- V2 路径和导入规范保留
- `session?.cost` 模式保留
- 中文注释比例满足 15% 门禁
