# Record 0092-2f1ab5812e3b

## Source intent
original 将 kernel restart **并入** `vscode_notebook_env`：`operation` 枚举含 `restart`；删除独立 kernel 工具/路由/模块；env 超时拉到 **120s**；run timeout 上界 **3_000_000**；命令面板仅 env 子操作选择。

## Upstream / pre-state
91 已提供 restart 实现与 `/notebook/kernel`。上游仍无 env multi-op。目标 env 为 info(+早期 configure/save 视前缀而定)。

## Target adaptation
- **删除** `kernel.ts`、`/notebook/kernel`、`vscode_notebook_restart_kernel`。
- **唯一入口** `notebookEnv`：`info|configure|restart|save`；非法 operation 硬失败。
- plugin env：`operation` 必选；**120_000** ms；run schema `.max(3_000_000)`。
- commands：QuickPick 无 restart label；env 二级选择 `Select operation`。
- probe 超时 **30s**（info 内核探测）。

## Design notes
- 不保留双入口「兼容」：双入口违反 SMARK 合并意图且造成 agent 分叉。
- configure 在 92 仍为 best-effort（status 五态在 93）。
- 面板只调用 `notebookEnv`，避免引用已删模块。

## Verification
- tip 94：`kernel.ts` 不存在；plugin 无 restart 工具；有 `120_000` 与 `3_000_000`；commands 含 `Select operation`。
- typecheck 94 全绿；E/C 达标。


## Three-way comparison
| 维度 | #91 tip | SMARK merge intent | #92 tip |
|------|---------|--------------------|---------|
| restart 工具 | 独立 | 删除 | 已删 |
| /notebook/kernel | 有 | 删除 | 已删 |
| kernel.ts | 有 | 删除 | 已删 |
| env.operation | 无 restart | 含 restart | 枚举含 restart |
| env timeout | 10s 量级 | 120s | 120_000 |
| run max | 600_000 可能 | 3_000_000 | 3_000_000 |

## Call chain after merge
Agent → `vscode_notebook_env` with `operation:"restart"` → bridge `/notebook/env` → `notebookEnv` switch → private `restartNotebookKernel`（仍在 env.ts 内）→ 同 91 的 Jupyter 公开命令路径。

命令面板：`notebookBridgeTools` → pick `vscode_notebook_env` → pick operation label ∈ {info,configure,restart,save} → `notebookEnv({filePath, operation, reason})`。

## Invariants
- 无第二 restart 入口
- 非法 operation 抛错
- string filePath 兼容仍映射 info
- probe 30s 只影响 info 活跃内核探测，不截断 restart 的 120s 工具窗

## Comment map
env/plugin/commands/bridge 上决策注释：双入口删除、120s、3e6、Select operation、dirty save 等。


## Materialize / typecheck provenance
- 累计脚本：`bun .temp/patches/src/apply-cumulative.ts --materialize 94 --rebuild` → tip `states/0094-81efcbb1d8cb`。
- typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 94` → report `typecheck-0094-2026-07-22T07-25-34-017Z.md`，Failure Output: No failures；含 `packages/opencode`、`packages/tui` 等全部 listed workspace `passed`。
- install：reused fingerprint `67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a`。
- 注意：`sdks/vscode` 不在 monorepo typecheck 矩阵内，扩展侧正确性依赖 apply 成功 + 结构 grep（无 kernel.ts 于 tip 94、status 字段、Select operation 等）而非 `bun typecheck` 包条目。

## Risks consciously accepted
- 无真实 Jupyter E2E：无法在 CI 态点击内核选择 UI；契约以 status 字段与命令存在性检查表达。
- TUI 10/20 只改 GenericTool 主路径；其它仍用 3 参 collapse 的调用保持旧阈值（兼容）。


## Post-r7 #92 completeness
- `saveNotebook`：`waitForDirtyStateToSettle` + `DIRTY_SETTLE_TIMEOUT_MS=10_000`；`saved = !afterDirty`；version/dirty/saveReturned/saveError；`note` 禁止 unprompted save。
- light configure：`executeCommand(SELECT_KERNEL_CMD, { notebookUri, skipIfAlreadySelected: true })`（五态机仍由 #93）。
- descriptions：save “Do not save unprompted”。


## Settle helper (must exist as definition)
`const DIRTY_SETTLE_TIMEOUT_MS = 10_000` + `async function waitForDirtyStateToSettle` using `vscode.workspace.onDidChangeNotebookDocument` + timeout dispose. Call site alone is insufficient.

## Files / hashes
- current: `0092-2f1ab5812e3b.patch`
- sha256: `075d4c4618adcd27cf1c350b6e5edc76986b53f1c36522ef1b85d13c901bf7e7`
- bytes: 34198
- E=236 C=39 need=36 gate=PASS
- materialize tip: states/0094-81efcbb1d8cb
- typecheck report: typecheck-0094-2026-07-22T07-25-34-017Z.md (all listed workspaces passed; install fingerprint 67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a)
- updated: 2026-07-22T06:10:00Z

## Independent dual audit (batch 90-94)
- FORWARD: **PASS** all five, no open B/G — task `ses_07747ffb8ffeoOHdlE46H0FgNE` (r11)
- REVERSE: **PASS** all five, no open B/G — task `ses_07747ff96ffe5KTvNREEFo8UZM` (r11)
- Tip: `states/0094-81efcbb1d8cb`
- Typecheck: `typecheck-0094-2026-07-22T07-25-34-017Z.md` (No failures; install `67995d9429bc…dab9c2a`)
- Batch release: **PASS**
