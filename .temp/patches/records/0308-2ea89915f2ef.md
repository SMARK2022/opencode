# Record 0308-2ea89915f2ef — feat(tests): 增强测试用例，优化 Windows CI 兼容性并修复 shell 命令延迟

## Identity
- index/full SHA: 0308 / 2ea89915f2ef9be67f2d236337a1409cc6b8aa57 (short 2ea89915f2ef)
- manifest parent: 95aaee58d46f (0307)
- subject: feat(tests): 增强测试用例，优化 Windows CI 兼容性并修复 shell 命令延迟
- status: PASS（首个编辑快照建立中——record 先行）

## Source intent (original/0308-2ea89915f2ef.patch)
1. `packages/enterprise/test/core/share.test.ts` + `storage.test.ts`：新增进程内 S3 内存夹具 `useTestStorage`（Symbol.for 共享状态、OPENCODE_STORAGE_* 环境、globalThis.fetch 拦截 S3 ListObjectsV2/PUT/DELETE/GET、非测试 bucket 走原 fetch），避免 CI 缺企业存储密钥
2. `packages/opencode/test/session/prompt.test.ts`：
   - `shortShellDelayCommand()`：`bun -e "setTimeout(process.exit, 200)"` 替代 `sleep 0.2`（Windows PowerShell 解析差异 + CI 冷启动预算）
   - 两个 it.instance 用例超时 3s→10s（各附注释）
3. `packages/opencode/test/tool/repo_clone.test.ts`：gitResult 走生产 Git.Service、commitFile（staged 校验）、githubFileBase（pathToFileURL）、waitForContent 错误信息、isolateRepoCache fsmonitor stop、各用例 commit 校验——**target 无 repo_clone 工具/测试，无 owner，无操作（记录）**

## Target adaptation plan
- enterprise share/storage 测试：目标 packages/enterprise/test/core/ 是否存在对应文件待核实；若存在则按 source 语义应用（含夹具）
- prompt.test.ts：`withSh`/两用例区域上下文核对后应用
- repo_clone：无操作（与前批次同因）
- 方法：state 0307 语义编辑 → git diff 权威生成 → current/0308

## E/C
- E=118, C=26, need=18 → PASS（fixture 代码行+测试注释×2文件；shortShellDelayCommand 注释×2）

## Verification log
1. apply --check EXIT 0; validate_hunks fixed:0; materialize 308 passed 308/308
2. typecheck 308: Install reused (cbdc173…); packages/app env baseline; Source/Target unchanged
3. enterprise un test test/core/share.test.ts test/core/storage.test.ts → 17 pass / 0 fail / 27 expects
4. packages/opencode un typecheck → exit 0
5. prompt.test.ts 两 shell 用例超时 10s（10002ms）——环境性 N（bun -e 冷启动仅 0.4-1.3s；超时来自完整集成链 session+server+shell+loop 在慢机上 >10s；与 296-300 N-02 同模式）
6. repo_clone hunk NO-OP（target 无 repo_clone 工具/测试——grep src/tool 确认无）
7. 两 shell 超时已在目标为 10_000（先前适配）；0308 source 的 3s→10s hunk 为无操作（已满足）
8. current SHA-256 98f69bb0065e6efef4d502069fde0c17505320484fba97de1dc47713c4583061 / 10981B

## 不一致
- edit308.py 初版 fixture 窗口缺空行、toolPart 窗口用前缀而非整行、E/C 缺注释（C=8<18）→ 逐步修正
- current/0308 直接文本修补（toolPart 截断行修复）→ 重新物化通过
- prompt.test.ts 超时 10s：环境性，非代码缺陷（source-faithful shortShellDelayCommand + 10s budget）

## 上游对比
上游 packages/enterprise/test/core/share.test.ts 与 storage.test.ts 存在但无 S3 内存夹具——source 引入 useTestStorage 走真实 S3 适配器路径只拦截测试 bucket；prompt.test.ts shortShellDelayCommand 为 SMARK 独有 Windows 兼容

## Source hunk 映射
enterprise share.test.ts: useTestStorage 夹具（Symbol.for 共享、OPENCODE_STORAGE_* 环境、globalThis.fetch S3 拦截）
enterprise storage.test.ts: 同上（storage-flavor 注释）
prompt.test.ts: shortShellDelayCommand + 两处 sleep 0.2 替换 + 超时（已 10s 无操作）
repo_clone.test.ts: NO-OP（target 无 repo_clone 工具/测试）

## E/C 注释位置
C 位置: useTestStorage 夹具注释（S3 适配器路径、Symbol.for 共享、单次安装、bucket 拦截范围、ListObjectsV2、max-keys 分页、PUT/DELETE/GET 状态码、404 语义）×2 文件 + shortShellDelayCommand 注释（PowerShell 解析、bun PATH）

## Audit trail
- （双审计 seal 在复审通过后追加）

## DUAL-AUDIT SEAL（批次 306-310）
- 第二轮双审计（R2 修复后：记录扩展 + 0309 fixture.ts 死代码→vcs.test.ts 可达）：
  - 正向审计（ses_fdf4eeb92ffevPAio1sf1avQJx）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdf4ed38effeP3yoEqRZsyBC46）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（4/2、79/20、118/26、3/1、4/4）、累计 apply 310/310、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：prompt.test.ts shell 测试 10s 超时（296-300 N-02 模式）；packages/app TS1128 环境基线；indentation stripping N（语法有效、行为不变）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
