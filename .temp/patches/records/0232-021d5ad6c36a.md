# Record 0232-021d5ad6c36a

## Status
PASS（materialize 232/232，E/C gate PASS（E=271 C=44 need=41），行为测试 87/87 + next.test.ts 相关 1/1，typecheck 随批次 231-235 末项验证）

## 修改内容
feat(permission): 增强外部目录访问的权限审查逻辑——所有 external_directory 边界统一为 cautious review seam，危险 shell payload 保持 deterministic deny。
- `docs/opencode-sandbox-windows-design.md` — 3 hunk：external_directory 段落更新（危险 payload deterministic deny、非危险进入 cautious）、ASCII 图 general shell→general、cautious/strict 单元格、level 说明（general allow unless strict）、PermissionPrecheck 覆盖范围更新
- `packages/opencode/src/permission/auto.ts` — evaluate 路由重写：删除 isShellExternalDirectory/isToolExternalDirectory 两个分支（shell-origin allow、无证据 ask）与 hasToolExternalDirectoryEvidence 辅助；general（任意权限）默认 allow，reviewer 只承接 cautious；isShell 变量随之移除
- `packages/opencode/src/permission/precheck.ts` — evaluate 顶部新增 externalDirectoryEffect 调用（先于 structuredFileEffect）；删除 external_directory&&action_kind==="shell" 内联分支；新增 externalDirectoryEffect 函数（shell action_kind 复用 evaluateShell，dangerous 透传 deny，其余一律 cautious "external directory access requires auto review"）；structuredFileEffect 删除 apply_patch 项目外删除分支（由 externalDirectoryEffect 统一覆盖），edit-delete 分支保留原 reason 并加注释
- `packages/opencode/src/tool/shell.ts` — external_directory ask 注释更新（dangerous payload deterministic deny、其他外部路径 cautious review 边界）
- `packages/opencode/test/permission/auto.test.ts` — shellExternalDirectory 辅助增加 metadata 参数与展开；"uses shell evidence..." 重写为三场景（native auto path-only → cautious reviewer、shell git status → cautious reviewer、rm -rf / → deny precheck）；两个"user approval path"测试改为"routes ... to reviewer"（ask→deny reviewer、called false→true）；新增 malformed evidence→reviewer 测试；general allow 测试扩展 edit 场景；删除两处 reason 断言
- `packages/opencode/test/permission/next.test.ts` — "ask - auto routes non-shell general tools to reviewer" 改名并断言 reviewedCalls 1→0（general 不再消耗 reviewer）
- `packages/opencode/test/permission/precheck.test.ts` — workspace delete 断言去 reason；updates 注释更新；apply_patch external delete 测试改名并去 reason；新增三个测试（external directory access cautious、dangerous shell external denied、conflicting tool metadata 仍 denied）

## Source 行为意图
1. external_directory 不再区分来源证据：shell-origin 复用同一条命令的确定性预审（dangerous 保持 deny，避免 rm -rf / 仅因跨越外部路径边界就变成 reviewer 可审批）；非 dangerous 一切外部路径（含 glob/grep/lsp/repo_overview 只读工具）进入 cautious review 边界，不退回可点击 ask。
2. general 层级语义改变：未知命令（shell 或非 shell）默认 allow unless strict——reviewer 只承接 cautious；delete 等不可逆效果由 precheck 提升为 cautious 后送审。
3. hasToolExternalDirectoryEvidence 移除：不再需要逐工具 operation/payload 证据判定是否进入 reviewer。
4. 测试意图：验证 path-only 外部路径、shell 外部路径、畸形 tool 证据均路由到 reviewer；dangerous shell 在 external_directory 门禁仍 deny；普通 edit 不消耗 reviewer。

## 不一致（source vs target 适配）
- source 注释为英文/无编号；目标按既有 #NNN 约定加 #232 中文注释（路由语义、externalDirectoryEffect 不变量、测试翻转意图）。
- shell.ts 的 source hunk 上下文注释（"Auto 模式下，项目外路径访问不能先退回普通 ask；把同一次 shell..."）在目标 shell.ts 不存在（目标 ask 直接 patterns/always→metadata）→ 按 source 新版本语义插入 3 行 #232 注释。
- 目标 precheck.ts 中 #230 注释（structuredFileEffect 优先、apply_patch 项目外删除）在适配中保留或由 #232 说明取代。
- 测试目标结构差异（#203/#208/#209/#210 注释、额外测试）→ 逐测试按 source 语义翻转，保留目标既有测试。
- next.test.ts 的 general 路由测试在目标有 #203 注释块 → 更新为 #232 语义（deterministic allow）。

## E/C
- E=271，C=44（#232 中文注释：auto.ts 路由与证据移除说明 8、precheck.ts externalDirectoryEffect 与 evaluate 说明 10、shell.ts 3、auto.test.ts 12、next.test.ts 5、precheck.test.ts 6），need=ceil(0.15*271)=41。Gate PASS（margin 3）。
- 注释分散于关键逻辑：路由四层级、externalDirectoryEffect 的 dangerous 透传不变量、cautious seam 命名、删除证据统一覆盖、测试断言翻转意图。

## 注释位置
- auto.ts：路由块（#232 四级负载边界、general allow 语义）、hasToolExternalDirectoryEvidence 移除处。
- precheck.ts：evaluate 顶部（bash/external_directory 覆盖说明）、externalDirectoryEffect（危险载荷透传 deny、cautious seam 独立于逐工具证据）、structuredFileEffect（delete 专属 reason）。
- shell.ts：external_directory ask 注释。
- auto.test.ts / next.test.ts / precheck.test.ts：各翻转/新增测试的意图注释。

## Source
- SHA: 021d5ad6c36a
- 意图：feat(permission): 增强外部目录访问的权限审查逻辑，确保危险 shell payload 被拒绝，其他外部路径进入谨慎审查边界
- parent: bba4bb002a48（231）

## Upstream
- 目标 v1.17.20 已有 precheck evaluate、PermissionAuto.evaluate 路由、structuredFileEffect（230 移植）；232 在既有路由与 precheck 主路径上改造，不重写 reviewer 调用链、fail-closed、strict 语义、errorTag/合约校验等既有能力。

## Target/Current
- 7 文件；current patch 约 28.9 KB（适配后），original 约 31.4 KB。
- materialized 232 状态：auto.ts 无 isShellExternalDirectory/hasToolExternalDirectoryEvidence；precheck.ts 有 externalDirectoryEffect；测试全部翻转。

## 安装/测试
- materialize 232 PASS（231 前缀复用 + 232 应用）。
- 行为测试（232 状态同步到测试工作区）：`bun test test/permission/auto.test.ts test/permission/precheck.test.ts` 87/87 pass；`bun test test/permission/next.test.ts -t "auto allows non-shell general"` 1/1 pass（reviewedCalls=0 断言成立）。
- typecheck 随批次末项统一验证。

## 失败修订
1. regen 流程两次踩坑：先复制修改后的 231 状态到 0232 再 materialize 231（覆盖修改丢失）→ 修正流程：materialize 231 重建干净 pre-image 后重新应用修改；临时目录 D:\Temp\opencode\state-232 保存目标状态。
2. count-ec 首次 E=271 C=33 need=41 不足 → 补 8 行合格中文注释（auto.ts 路由/证据移除、auto.test.ts 两处、precheck.test.ts 两处、next.test.ts 一处）→ C=44 PASS。

## 最终行为
- external_directory 权限（任何来源）统一进入 cautious review：shell-origin 复用命令预审（dangerous 保持 deny），path-only 只读工具进入 reviewer 而非可点击 ask；general（shell/非 shell）默认 allow unless strict；delete 效果由 precheck 提升 cautious；畸形 tool 证据由 reviewer 拒绝；文档同步更新。

## DUAL-AUDIT SEAL
批次 231-235 第三轮双独立审计封印（2026-08-06）：
- 正向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 逆向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 三轮修复验证：G-01（231 record 长度）✓、B-01（233 filetype none 哨兵）✓、B-01 r2（235 gracefulShutdown 3s 总超时）✓
- 测试：daemon 8/8（×2稳定）、filetype 2/2、exit 2/2、notebook 12/12、preview-diff 8/8、session-list 19/19、typecheck 30/30
- install 指纹 a4aae9d1（reused）、source HEAD 未变、integrity 无失败
- BATCH PASS
