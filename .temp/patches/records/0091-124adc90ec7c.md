# Record 0091-124adc90ec7c

## Source intent
original 新增 **独立** Jupyter kernel restart：HTTP `/notebook/kernel`、插件工具 `vscode_notebook_restart_kernel`、扩展 `kernel.ts`、命令面板项；固定 `jupyter.restartkernel`；临时关闭 `jupyter.askForKernelRestart` 并 **finally 恢复**；reason 仅日志；成功表示 **requested** 非 confirmed。

## Upstream
上游无 notebook kernel restart bridge。目标在 90 时仅有 env 快照（info），无 restart 命令面。

## Target adaptation / serial provenance
本项是 **中间态**：完整落地独立 restart 面，为 92 合并做前缀。禁止跳过 91 直接在 92 从零出现 restart——serial 审计要求 91 状态可独立物化且行为可验证。

## Current design
- `sdks/vscode/src/notebook/kernel.ts`：`restartNotebookKernel`；扩展探测；命令注册检查；config 临时关闭+restore；`notebookUri` 显式绑定。
- `bridge.ts`：`POST /notebook/kernel`。
- plugin：`vscode_notebook_restart_kernel`，timeout **30s**。
- commands：独立 QuickPick label。
- 注释门禁：决策型中文注释覆盖 requested/finally/不走 lm.invokeTool/30s 等。

## Why not merge into env yet
92 的职责是删除双入口并统一 timeout；91 必须先证明 restart 主路径正确，再在 92 迁移 owner。

## Verification
- materialize 91 通过；92 删除 `kernel.ts` 与独立工具。
- E/C 满足 ceil(E*0.15)。


## Three-way comparison
| 维度 | SMARK original | Upstream | Final at #91 tip |
|------|----------------|----------|------------------|
| 工具 | vscode_notebook_restart_kernel | 无 | 有（中间态） |
| HTTP | /notebook/kernel | 无 | 有 |
| 模块 | kernel.ts | 无 | 有 |
| 成功语义 | requested | n/a | requested:true |
| 确认弹层 | 临时关 askForKernelRestart | n/a | finally 恢复 |
| 超时 | 30s | n/a | 30_000 |

## Call chain
1. Plugin tool execute → `call("/notebook/kernel", args, context, 30_000)`
2. bridge switch → `restartNotebookKernel(body)`
3. resolveNotebook → activate Jupyter → check RESTART_CMD → maybe update config → executeCommand(RESTART_CMD, {notebookEditor:{notebookUri}}) → finally restore config

## Invariants
- 未装 Jupyter：硬 summary，不抛未处理异常淹没 bridge
- 命令未注册：明确失败 data，不 silent success
- reason 不改变重启命令参数
- 成功只保证“已请求”，不保证变量已清零完成

## Serial note
#92 将删除本项公开的独立入口；审计 91 时必须在 **states/0091-*** 验证存在性，不能只看 tip 94。


## Materialize / typecheck provenance
- 累计脚本：`bun .temp/patches/src/apply-cumulative.ts --materialize 94 --rebuild` → tip `states/0094-81efcbb1d8cb`。
- typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 94` → report `typecheck-0094-2026-07-22T07-25-34-017Z.md`，Failure Output: No failures；含 `packages/opencode`、`packages/tui` 等全部 listed workspace `passed`。
- install：reused fingerprint `67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a`。
- 注意：`sdks/vscode` 不在 monorepo typecheck 矩阵内，扩展侧正确性依赖 apply 成功 + 结构 grep（无 kernel.ts 于 tip 94、status 字段、Select operation 等）而非 `bun typecheck` 包条目。

## Risks consciously accepted
- 无真实 Jupyter E2E：无法在 CI 态点击内核选择 UI；契约以 status 字段与命令存在性检查表达。
- TUI 10/20 只改 GenericTool 主路径；其它仍用 3 参 collapse 的调用保持旧阈值（兼容）。

## Files / hashes
- current: `0091-124adc90ec7c.patch`
- sha256: `e6e7ba89464d23ba17447e9f86bfad9a322abb7ca6f80ed5f626a0656b9e655a`
- bytes: 12667
- E=117 C=36 need=18 gate=PASS
- materialize tip: states/0094-81efcbb1d8cb
- typecheck report: typecheck-0094-2026-07-22T07-25-34-017Z.md (all listed workspaces passed; install fingerprint 67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a)
- updated: 2026-07-22T06:10:00Z

## Independent dual audit (batch 90-94)
- FORWARD: **PASS** all five, no open B/G — task `ses_07747ffb8ffeoOHdlE46H0FgNE` (r11)
- REVERSE: **PASS** all five, no open B/G — task `ses_07747ff96ffe5KTvNREEFo8UZM` (r11)
- Tip: `states/0094-81efcbb1d8cb`
- Typecheck: `typecheck-0094-2026-07-22T07-25-34-017Z.md` (No failures; install `67995d9429bc…dab9c2a`)
- Batch release: **PASS**
