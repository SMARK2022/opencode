# Record 0363-f90bcd68677c

## Identity
- manifest parent: ddc5e00c6038 (0362)
- subject: chore(version): version 1.15.8-smark → 1.15.9-smark
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0363-f90bcd68677c` (passed 363/363)
- current patch: `.temp/patches/current/0363-f90bcd68677c.patch` (1574 bytes)

## Source intent
A release version increment. `packages/opencode/package.json` bumps
`1.15.8-smark` → `1.15.9-smark`, and `README.md` syncs 6 SMARK version
references from `1.15.7` to `1.15.9`: the "Current SMARK version" badge, the
branch description (`当前版本 1.15.7` → `1.15.9`), the install command
examples (`--version 1.15.7-smark` → `1.15.9-smark`, two places), and the
VS Code Notebook extension note (`CLI 1.15.7` → `1.15.9`).

## Source patch anatomy (2 files)
- `README.md` — 4 hunks, 6 SMARK-specific version strings.
- `packages/opencode/package.json` — 1 hunk, version line replacement.

## Target mapping (state 0362)
| Source file | Target file | Status |
|---|---|---|
| `README.md` | `README.md` | SKIPPED (see below) |
| `packages/opencode/package.json` | same | ADAPTED (version `1.17.29-smark` → `1.17.30-smark`) |

## Target-adaptation decisions
1. **README skipped per established precedent (#161/#231/#284/#287/#346)**:
   the target `README.md` is the pure upstream English README with only three
   SMARK additions (database migration notice L46, VS Code extension note L105,
   ChatGPT bridge note L122). None of the source patch's six version strings
   exist in the target:
   - No "Current SMARK version" badge (target header shows only upstream
     badges: discord/npm/build-status).
   - No `dev-smark` branch-description line (`当前版本 1.15.7`).
   - No SMARK install-command section (`--version 1.15.x-smark`).
   - The only related line, L105, uses version-agnostic wording
     (`该扩展版本 1.15.5 对应 SMARK CLI 1.15.5-smark 及之后版本` = "and
     later versions"), so a version bump does not change its meaning.
   Version display semantics are carried by `package.json` and the release
   workflow, exactly as recorded in `//284-release-note` and `//287-release-note`.
2. **Version mapping**: source bump `1.15.8→1.15.9` maps to the target version
   line increment `1.17.29-smark → 1.17.30-smark`, following the precedent
   chain #231 (1.17.26→1.17.27), #253 (1.17.27→1.17.28), #346
   (1.17.28→1.17.29). The `//version-note` comment prohibits writing back to
   the 1.14/1.15 line.
3. **Release note**: added `"//363-release-note"` JSONC comment documenting the
   mapping (`#363 协同升号至 1.17.30-smark；源 patch bump 1.15.8→1.15.9 映射到
   目标版本线递增`), placed directly after the version line and before the
   existing `//346-release-note`, matching the newest-first annotation order.

## E/C audit
E=1, C=1, need=1, PASS. (E: the `"version": "1.17.30-smark"` line; C: the
`//363-release-note` JSONC comment with CJK text.)

## Materialization
- First attempt FAILED at `git apply --check` with "corrupt patch at line 21":
  the header extraction (lines 0..18 of the original patch) had included the
  first fragment of the source README diff (`diff --git a/README.md` + hunk
  header) because the 0363 body text is only 3 lines (vs 8 lines in 0361/0362).
  The dangling README fragment made the patch malformed. Rebuilt the header
  through the last body line (line 12) + blank line + adapted diff.
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 363` →
  "passed 363/363", state at `states/0363-f90bcd68677c`.
- Post-apply verification: package.json shows `"version": "1.17.30-smark"`
  with `//363-release-note` immediately after.

## Line accounting
- `packages/opencode/package.json`: +1/-1 (version line replaced + release
  note added, net +1 line).
- `README.md`: +0/-0 (skipped).

## Risks / notes
- The version line and release-note are JSONC comments (`//`-prefixed keys),
  which bun's JSONC parser tolerates; this matches the existing annotation
  pattern accumulated across prior patches.
- No behavioral risk: this is a metadata-only change. The skipped README
  strings carry no functional meaning in the target because the target README
  never adopted the SMARK badge/install sections.
- Typecheck of the change is covered by the batch-end `--typecheck 365`.

## Per-item diff stats
- fix363.diff bytes: 1406 (raw diff, LF); patch file with header: 1574.
- 1 file, 1 hunk (README skipped).


## README.md hunk 跳过说明（与 0287 先例一致）
source 0363 的 README.md hunk（4 处 1.15.7→1.15.9：badge `current-1.15.7`、`关于本分支` 中文段落、`--version 1.15.7-smark` 安装命令、Notebook 扩展配合版本）全部基于 SMARK 增强版 README 的上下文。目标 README.md 为纯上游英文版（已验证：`dev-smark`、`关于本分支`、`1.15.7` 字符串均不存在于目标），patch pre-image 上下文完全不匹配。
0287 先例已确认：SMARK 品牌/版本段落从未移植到目标 README（source 更新 22 个翻译文件时目标跳过）。因此 README hunk 无法应用且无有效语义载体——跳过并记录。
README 为 .md 文档，E/C 排除，不影响 E/C 门禁。版本号变更的实质语义（package.json version）已在本 patch 应用（1.17.29→1.17.30-smark）。
