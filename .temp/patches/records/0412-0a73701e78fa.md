# Record 0412-0a73701e78fa

## Identity
- SHA: 0a73701e78fa
- Parent: 7bd1e1560289 (0411)
- Subject: feat(notice): 工具取消与长耗时执行 Notice 增加 elapsed_ms
- Status: PASS（中间态 typecheck 依赖 0415，批次末验证）

## Source intent
source 0412 共 9 文件（1073 行）：
1. 新增 `docs/tool-cancellation-elapsed-notice-forensic-report.md`（312 行设计文档）。
2. output-notice.ts（3 hunks）：ExecutionNotice 加 source?（缺省 shell）、reason 加 "completed"、elapsed_ms?；新增 LONG_EXECUTION_NOTICE_MS=120s、normalizeElapsed、formatLongExecutionNotice、formatShellExecutionNotice（timeout>user_abort>exit>long completed>none 单一 outcome 优先级）。
3. shell.ts（2 hunks）：import 换 formatShellExecutionNotice；删除分散 timeout/user_abort/exit 三处决策，统一在 emptyOutput 确定后调用 outcome policy，唯一一次追加 notice。
4. processor.ts（3 hunks）：interruptedToolMetadata 加可选 elapsedMs 参数（有限非负才写入 executionElapsedMs）；failToolCall abort 时传 elapsed + 保留原 start；cleanup 同样传 elapsed。
5. message-v2.ts（2 hunks）：import 加 formatExecutionNotice；error 分支仅对带 executionElapsedMs marker 的新 interrupted 记录追加 user_abort Notice（output-available/error-text 两条路径），旧记录原样，无效 marker 忽略。
6. prompt.ts（4 hunks）：import 去 Semaphore 加 Deferred/Fiber；cancelLocks Semaphore → cancelOps Deferred single-flight（原子 check-and-set + onExit 完成 Deferred）；local/MCP 工具包装透传 AbortSignal（Bash 豁免）。
7. 测试：message-v2.test.ts +5（marker 追加/无 output/旧记录不追溯/无效 marker）；processor-effect.test.ts cleanup +2 断言（executionElapsedMs 存在且 number）；shell.test.ts import + 6 断言（abort/timeout/exit0/42/7/9×2 从精确 /> 改前缀含 elapsed_ms）+ 文件末尾 13 个 pure policy 测试。

## Target adaptation（0412 为目标重写）
- output-notice.ts：3 hunks 全部落点（#412 注释风格，保留目标 #282/#289/#293 既有注释）；source 的 formatExecutionNotice 默认 source 语义保持。
- shell.ts：2 hunks verbatim 语义（上下文偏移 6 行内命中）。
- processor.ts：hunk1 签名适配（目标无 `const log = Log.create` 行，仅改 interruptedToolMetadata 本身）；hunk2 failToolCall 保留目标 #209 演进（非 abort 错误保留原 metadata 而非 source 的 undefined）+ abort 传 elapsed；hunk3 cleanup verbatim 语义。
- message-v2.ts：import 行直接改目标 L32（上下文不同）；error 分支 hunk 上下文吻合 verbatim。
- prompt.ts：import 改目标 L49（去 Semaphore 加 Deferred——Fiber 无使用不加，record 记录；0415 不依赖该 import）；cancelOps 使用目标现有 #394 快照 body（sessions.messages 而非 source 的 MessageV2.stream——目标 #153 明确放弃 stream 快照）；**Deferred.done 直接采用 0415 的最终语义 succeed**（done 在 source 0412 即存在类型/语义缺陷：Deferred<Exit<void>>.done 需要 Exit<Exit<void>> 类型参数 → TS2345；且 done 会把失败 Exit 送入 Deferred 失败通道使 joiner Exit.isFailure 分支不可达）。**注意：0415 的 hunk D（done→succeed + 注释）因本项直接采用最终语义，在 0415 修订时将跳过并记录。**
- prompt.ts 的 AbortSignal hunk（3/4）：目标落点 tools.ts（local execute + MCP listTemplates/read/工具循环 4 处 run.promise 透传）；Bash 豁免用 item.id !== ShellID.ToolID（tools.ts 新增 ShellID import）；**桥梁签名（EffectBridge.Shape.promise 的 options 参数）由 0415 hunk A 提供**——0412 单 index typecheck 预期报 4 个 TS2554（run.promise 第二参），0415 后消失。
- prompt.ts 的 `MessageV2.stream` 快照：目标保留 sessions.messages（#153/#394 注释），语义等价（pendingIds 快照原子性解释不同，行为一致）。

## Verification
- materialize 412：passed 412/412（state 0412 因编辑导出经用户授权删除重建；current/0412 曾因 succeed 修复未更新 hunk 头 @@ -315,36→34 +317,50→51 出现 corrupt patch，修复后通过）
- typecheck 412：单 index 仅剩 4 个 tools.ts TS2554（Expected 1 arguments, got 2 at 206/378/462/580）——0415 bridge.ts Shape.promise options 签名中间态依赖，批次末 typecheck 415 验证
- typecheck 411 的 3 个中间态错误（formatLongExecutionNotice/elapsed_ms/interruptedToolMetadata 双参）由本项解决 ✓

## 不一致记录
1. target failToolCall 非 abort 分支保留原 metadata（上游 #209 演进）vs source 的 undefined——目标更强，保留。
2. target cancel body 用 sessions.messages 快照 vs source MessageV2.stream——目标 #153 注释明确放弃 stream，语义等价。
3. 0412 source 的 Fiber import 无使用，目标不加（noUnusedLocals=false 不会报错；为保持 import 整洁不引入）。
4. Deferred.done → succeed 直接采用 0415 最终语义（源代码里 0412 done 版自身类型不匹配且语义有缺陷）。
5. MCP convertMcpTool hunk（0415 source Hunk B）：目标 mcp/catalog.ts:62 已传 signal（上游更强），0415 修订时 Drop 并记录。

## 行为测试（.test-workspace/repo/packages/opencode 实测）
- bun test test/tool/shell.test.ts -t "execution notice policy" --timeout 60000：12 pass / 0 fail（source 声称 13 个、实际 12 个 pure 测试，逐一核对源 patch 共 12 个 test 块）
- bun test test/session/message-v2.test.ts --timeout 120000：50 pass / 0 fail（含 5 个新用例）
- bun test test/session/processor-effect.test.ts --timeout 120000：20 pass / 0 fail（含 executionElapsedMs 存在+number 断言）

## E/C
- count-ec-auditor.py：E=338 / C=76 / need=51 → PASS。注释分散于 notice policy 优先级、elapsed 规范化、marker 防伪造、cancelOps 原子性与 Deferred 语义、AbortSignal 透传豁免、测试意图等关键逻辑处。

## 最终行为
- output-notice.ts：ExecutionNotice 支持 source("shell"/"tool")/reason("completed")/elapsed_ms；LONG_EXECUTION_NOTICE_MS=120s；normalizeElapsed 将 NaN/Infinity/负值归一为 0；formatLongExecutionNotice 阈值判定；formatShellExecutionNotice 按 timeout>user_abort>exit(non-zero/empty)>long completed>none 返回唯一 Notice
- shell.ts：删分散 timeout/user_abort/exit push，改在 emptyOutput 确定后统一调用 formatShellExecutionNotice 一次追加；所有异常 outcome 带 elapsed_ms
- processor.ts：interruptedToolMetadata 增加 elapsedMs 可选参（有限非负才写入 executionElapsedMs 覆盖同名键防伪造）；failToolCall/cleanup abort 时传 elapsed 并保留原 start（failStart）
- message-v2.ts：仅对带 executionElapsedMs marker 的新 interrupted 记录追加 user_abort Notice（output-available 拼接 output / output-error 拼接 errorText）；旧记录原样、无效 marker（NaN/±Infinity/-1/"abc"）忽略
- prompt.ts：cancel 改为 Deferred single-flight（既见原子 check-and-set + onExit 完成）+ #394 sessions.messages 快照保留；tools.ts 4 处 run.promise 透传 { signal }（Bash 豁免）

## R2 审计非阻塞项（已记录，不阻塞）
- N-0412-a：current/0412 prompt.ts 段头 post blob hash（b5417095…）为 stale（实际应用 blob 独立重建为 9313d39a…）；git apply 忽略头 hash，content 验证一致、物化 415/415 通过（与 0406 批次同类 N）
- N-0412-b：MCP resource `list` 包装点（tools.ts L248 区域）无 AbortSignal 透传（其余 4 处有）——source 0412 本就无 MCP resource 工具（目标特有包装），取消路径经 2s settle + marker replay 仍带 notice；语义无丢失，记为不对称 N 级

## 注释位置清单（E/C 门禁依据）
- output-notice.ts：policy 优先级（timeout 优先理由）、normalizeElapsed 边界、120s 阈值产品决定
- processor.ts：elapsedMs server-owned 防伪造、abort 保留 metadata、全程 marker 说明
- message-v2.ts：旧记录不追溯伪造 elapsed、output-available/error-text 双路径拼接
- prompt.ts：cancelOps 原子性（同步块无 yield 切让）、onExit 不可中断终态转换、Deferred 语义
- tools.ts：Bash 豁免理由、MCP 双重中断保证
- 测试：marker/无 output/旧记录/无效 marker 四类意图、policy 互斥与阈值