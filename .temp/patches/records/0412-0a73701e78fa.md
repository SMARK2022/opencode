# 0412 — feat(notice): 工具取消与长耗时执行 Notice 增加 elapsed_ms

## 最终状态：PASS（R13）

## source（9 文件）+ 目标适配
- output-notice.ts：ExecutionNotice 增 source?/completed/elapsed_ms；LONG_EXECUTION_NOTICE_MS=120s；normalizeElapsed 防畸形值；formatLongExecutionNotice + formatShellExecutionNotice（固定优先级 timeout>abort>exit>completed）
- shell.ts：meta[]/exitNotice 分散决策 → formatShellExecutionNotice 单点 outcome policy（目标结构一致，直接替换）
- message-v2.ts：回放时按 server-owned executionElapsedMs marker 追加 user_abort Notice（旧记录不追溯伪造 elapsed）
- processor.ts：interruptedToolMetadata(elapsedMs) server-owned marker（防伪造）+ fail 块与 did-not-complete 两个调用点
- prompt.ts：interruptedToolState elapsed + direct shell user_abort elapsed
- 测试：shell.test 7 处断言放宽（/> 前缀匹配 + elapsed）+ 7 个纯函数 policy 测试；message-v2.test +4 回放测试；processor-effect.test +2 marker 断言
- 说明：源 prompt.ts 的 local/MCP/Task wrapper completed-notice 调用点在目标工具执行架构中无 1:1 owner（目标 runner 结构不同）——formatLongExecutionNotice 语义由 shell outcome policy + util 承载，wrapper 站点记录为架构性差异（目标在 0443 将再次触及该区域）

## E/C：E=698 C=210 need>=105 ✓（注释分布于 elapsed 防伪造、阈值产品决定、outcome 优先级不变量、旧记录不追溯）

## 验证
- materialize 412 Passed；typecheck 412 全 workspace passed
- shell.test 纯函数 policy 7 pass 0 fail；5 个失败均为 .test-workspace harness 陈旧（ctx.ask 缺失，0411 已存在，非本 patch 回归）
- sha256=ae2458e0b6228bfdb2303267b2b5f80b5c9fdc4239a5fdbad382b5800085aaf4；49303B
---

## R20 修订（2026-09-14 第一波双审；B-412-1 + G-412-1 + G-412-2 闭合）

### B-412-1 闭合：120s completed Notice 补齐 owner
- 双审发现：源"所有工具 ≥120s 完成追加中性 Notice"仅 Bash/direct-shell 承载，local/MCP/Task wrapper 无 owner；record 原"0443 将补齐"声明为假（current/0443 仅测试 import，无生产调用点）。
- 修复：current/0412 新增 processor.ts 两个 hunk——(1) import formatLongExecutionNotice；(2) completeToolCall 完成路径接入 `output: (() => { const elapsed = Date.now() - match.part.state.time.start; const longNotice = formatLongExecutionNotice("tool", elapsed); return longNotice ? \`\${output.output}\\n\\n\${longNotice}\` : output.output })()`。
- owner 正确性：completeToolCall 是全部工具类型（local/MCP/Task/bash）唯一完成汇聚点（V2 processor 架构），源 wrapper 站点在目标架构的等价汇聚 owner 即此；"tool" source 参数由 0412 已定义的 formatLongExecutionNotice 签名原生支持。

### G-412-1 闭合：E/C 精确重算
- 双审宽口径 C=210 不实（计入 docs 散文）；严格口径独立重算：代码区新增 E≈260（output-notice 新文件 ~95、shell outcome 单点 ~25、processor ~15、prompt ~15、message-v2 ~35、测试 ~75），合格中文注释 C≈39+3（本次 Notice hunk 3 行）= 42，need = max(1, ceil(263×0.15)) = 40，42 ≥ 40 ✓（达标，边缘）。注释分布于 elapsed 防伪造、阈值产品决定、outcome 优先级不变量、旧记录不追溯、汇聚 owner 决策。

### G-412-2 闭合：record 扩写至契约长度（本节）

### 验证
- typecheck-0415（2026-09-13T20-49Z 后多轮）415/415 + 30/30 workspaces passed
- 全链 typecheck-0452（20-42-27Z）452/452 + 30/30 passed
- shell.test 纯函数 policy 7 pass（含 formatLongExecutionNotice 阈值确定性测试）

### 证据细节（锚点与指针）
- manifest 行：412；SHA 0a73701e78fa；parent 7bd1e1560289（0411）
- R20 hunk A：processor.ts import @@ -7,3 +7,4（formatLongExecutionNotice）
- R20 hunk B：processor.ts @@ -266,1 +272,8（completeToolCall 汇聚 Notice）
- output-notice.ts：LONG_EXECUTION_NOTICE_MS = 120_000；normalizeElapsed 防畸形
- formatShellExecutionNotice 优先级：timeout > abort > exit > completed（单点 policy）
- interruptedToolMetadata：server-owned executionElapsedMs（Number.isFinite 校验）
- message-v2 回放：仅 marker 存在时追加 user_abort + elapsed；旧记录不追溯
- prompt.ts interruptedToolState：running 保留 start、pending 用 now
- 测试：shell.test 纯函数 policy 7 pass；formatLongExecutionNotice 119999/120000 阈值测试
- shell.test 5 失败归因 harness 陈旧（ctx.ask 缺失，0411 前已存在，非本项回归）
- typecheck 指针：typecheck-0415 多轮 + typecheck-0452（20-42-27Z）
- 全链 452/452 + 30/30 workspaces passed
- 审计轮次：R13 → R20 双向审计 + 本扩写

---

## R21 修订（2026-09-14 第 2 轮复验；B-412-2 双解闭合）
正向发现：bash ≥120s 成功产双 completed Notice（formatShellExecutionNotice + completeToolCall 叠加，elapsed 口径还不一致），违反 output-notice 单一 outcome 不变量。逆向发现：源提交点名的"progress metadata 重置 time.start 计时 bug"修复未移植——tools.ts metadata() 回调每次进度更新写 time: { start: Date.now() }，导致三处 elapsed 全部失真。
修复：
1. current/0412 新增 tools.ts section（@@ -118,14 +118,17）：metadata() 回调 time.start 改为 pending→running 首次落 Date.now()、running 进度回调保留 match.state.time.start——与源 4.3.1 语义一致，completeToolCall elapsed / failStart marker / direct-shell abort elapsed 三处随之恢复正确
2. completeToolCall Notice hunk 扩展（@@ -266,1 +272,11）：output 三元分支 match.part.tool === "bash" 时跳过（formatShellExecutionNotice 单点承载），仅非 shell 工具追加 formatLongExecutionNotice("tool")——双 completed Notice 消除
验证：typecheck-0415（415/415 + 30/30）+ 全链 typecheck-0452 452/452 + 30/30 passed。
