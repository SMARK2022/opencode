# Patch Record 0054

## 1. Identity
- Index: 54 · SHA12 `d09a5763d22f`
- Current SHA-256: `0647cf0a919734ae0cacdf714bca929fb07a2fc9bc7fe2722d7126048f184c35` · bytes 2720
- State: `states/0054-d09a5763d22f` · cumul=None status=None builtAt=None
- Baseline 4473fc3c… · tip d0ceb469…

## 2. Upstream design
Windows path membership: V2 packages/core/src/fs-util.ts FSUtil.contains already returns false when path.relative is absolute (cross-drive). packages/opencode/src/util/filesystem.ts delegates to it.

Agent defaults: after earlier SMARK agent order, primaries include build then interactive then review then plan. defaultAgent picks first non-subagent non-hidden entry.

Installation version fetch builds registry URLs via NpmConfig.registry, not a hard-coded npmjs host.

## 3. SMARK design
Post-merge test and code fixes: Windows contains absolute-relative guard (production); when build is disabled expect next primary interactive not plan; when testing no primary visible, disable all SMARK primaries including interactive/review; installation tests assert path suffix /opencode-ai/ plus InstallationChannel rather than a fixed host.

## 4. This adaptation
Skip production contains hunks — upstream already stronger. Keep test-only current on packages/opencode/test/agent/agent.test.ts and test/installation/installation.test.ts with V2 it.instance/Effect harness adaptation. Chinese comments at agent order / disable-all / registry endsWith.

## 5. Why endsWith is acceptable
Host-agnostic registry URLs are the production contract. endsWith path segment is slightly weaker than full URL equality but matches multi-registry reality.

## 6. Metrics
E=7 C=5 required=2 gate=PASS

## 7. Verification
- materialize 54; agent/installation filtered tests pass under cumulative suites
- Depends on agent order from earlier patches (interactive insertion)

## 8. Verdict
`PASS` — dual independent auditors FORWARD+REVERSE batch 50-54, no open B/G.

## 9. Dual audit closure
- Batch 50-54 FORWARD: PASS
- Batch 50-54 REVERSE: PASS

## 10. Call chain (tests only)
1. Agent list builder applies config disable flags to SMARK primary agents.
2. `defaultAgent` walks the ordered primary list for first visible non-subagent.
3. Tests assert: disable build → interactive; disable all primaries → fallback behavior; installation fetch URL path ends with channel segment under whatever registry host NpmConfig chose.

## 11. Why production contains hunks are omitted
V2 `FSUtil.contains` already implements the cross-drive absolute-relative false branch that SMARK added to filesystem util. Re-applying a second copy in opencode util would either no-op (if already delegated) or fight ownership. Correct adaptation is test alignment + skip duplicated production.

## 12. Residuals
Depends on earlier agent-order patches inserting interactive/review. Registry assertion is endsWith, not full URL, by design for multi-registry hosts.
## 13. Owner files (tests)
- `packages/opencode/test/agent/agent.test.ts` — defaultAgent expectations under disable flags.
- `packages/opencode/test/installation/installation.test.ts` — registry URL path suffix under NpmConfig.
