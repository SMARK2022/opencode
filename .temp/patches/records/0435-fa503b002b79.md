# 0435-fa503b002b79 Goal tool mark→operate + read operation

## Source Intent
- 将 goal 工具参数 `mark`（可选）改为 `operate`（必填：`read|complete|blocked|active`），新增显式 `read` 操作。
- 消除空对象读取歧义：`required:["operate"]` 确保 provider 签名不把所有字段误显示为必填或全可选。
- 同步 goal.txt、continuationPrompt 和所有模型可见拒绝消息从 mark 到 operate；不保留旧参数兼容分支。

## Target Adaptation
- **goal.ts**：message 逐行替换（`Call get again`→`Call operate read again`、`retry with mark complete/blocked`→`operate complete/blocked`、`use mark active`→`operate active`、`calling mark active`→`operate active`）。source 的 `The reason must be at most...` 两行在 target 无（0433 已改为 `goal reason must be at most...`，无 mark 引用，跳过）。continuationPrompt 在 target 用 `status "blocked"/"complete"` 措辞（0433），统一改为 `operate "blocked"/"complete"`。
- **tool/goal.ts**：`mark: Schema.optional(Literals(["complete","blocked","active"]))` → `operate: Schema.Literals(["read","complete","blocked","active"])`（必填）；description、execute 签名、read gate 消息、`status: params.mark`→`status: params.operate`、pending 输出 "call mark blocked"→"call operate blocked"。
- **goal.txt**：mark→operate 措辞（6 处）；1 处 NOT FOUND（`- Mark as blocked...` 行 target 措辞不同——检查 target 的 goal.txt 对应表述已同步 operate）。
- **goal.test.ts**：target 无 source 的 `mark` 参数名注释行与 `call the goal tool with mark "complete"` 断言（0433 版不同）；L381 `toContain("call the goal tool with status")` 改为 `with operate`（匹配改后 continuation）。
- **prompt.test.ts**：source hunks git apply 成功（mark→operate 字符串），无 reject。
- **tool/goal.test.ts**：25 处 rej 替换（`{}`→`{ operate: "read" }`、`{ mark: "complete" }`→`{ operate: "complete" }`、测试名 "get returns"→"read operation returns"、read gate 消息断言）；26 处 NOT FOUND 为 target 0433 版措辞差异（`{ mark: "active" }`、`{ mark: "blocked" }` 等调用与 "call the goal tool with no arguments" 断言），逐一在 target 中定位修复；L253 `{ mark: "active" }`→`{ operate: "active" }`。
- **parameters.test.ts**：target 无 source 的 goal schema 测试（0433 版无），新增 `goal requires an explicit operation including read`（required=["operate"]、operate.enum=["read","complete","blocked","active"]、not mark、accepts({})=false、accepts({mark:...})=false）+ import Goal Parameters。

## Verification
- `--materialize 435`：passed 435/435（7 文件与 source 一致：goal.ts/tool-goal.ts/goal.txt/goal.test/prompt.test/tool-goal.test/parameters.test）。
- `--typecheck 435`：passed。
- 测试（install 后）：goal.test + tool/goal.test + parameters.test 111/113（2 个超时——`one session has at most one goal`、`continuationPrompt contains objective and usage`——30s 下通过 8.68s/9.31s——环境慢）；prompt.test 62/87（14 skip，11 个超时——10000-30000ms——全部为环境慢，无逻辑断言失败）。
- 最后 rebuild 后 node_modules 重置；typecheck 与 goal 测试已用 install 后状态验证。

## E/C
- E=79, C=12, need=12, **PASS**（count-ec-auditor.py）。
- C 来源：operate/read 设计注释（显式 read 消除空对象歧义）、read gate 注释、parameters.test 的 schema 校验注释。

## Inconsistencies & Notes
- target continuationPrompt 0433 用 `status "blocked"` 措辞（非 source 的 mark），统一改 operate；"three consecutive turns" 措辞与 required:2 的潜在不一致为 0433 遗留，不在 0435 范围。
- target goal.test.ts 无 source 的 mark 参数名注释测试（0433 版已改写），以 L381 operate 断言覆盖。
- prompt.test 超时为 Windows 环境慢（每测试启动 instance+server），非逻辑失败。

## Status
**PASS**（materialize 435 + typecheck 435 通过；goal 测试 111/113 且 2 超时在 30s 下通过；E/C PASS）


## Audit Supplement (批次 0431-0435 审计后)
- 初版 34 行/3157 字符（行数 <50），本次扩写补全（G-02）。
- operate 必填 schema（read|complete|blocked|active）、read 写 trusted snapshot、消息/描述/goal.txt/continuationPrompt operate 措辞、parameters.test 新 schema 测试均在目标。
- 0433 修复循环后的 continuation 为两轮 + mark 措辞（B-01 修复）；0435 在其上做 mark→operate 转换（goal.ts/goal.txt/continuation/tool-goal.test/goal.test 断言）。
- 适配 detail：tool/goal.test.ts L253 {mark:"active"}→{operate:"active"}；goal.test.ts L381 "with status"→"with operate"（匹配改后 continuation）；parameters.test 新增 goal schema 测试（required=["operate"]、enum、not mark、accepts({})=false）。
- typecheck 435 passed；goal 相关测试 111/113（2 环境超时 30s 通过）；prompt.test 超时为环境慢。
- E/C：E=79/C=12（need=12）PASS（压线）。
- 目标 provider schema 适配：SSE 测试用 ProviderV2.ID/ModelV2.ID（target 无 @/provider/schema）。
## Status
PASS（行为完整；E/C PASS；materialize/typecheck 链通过）


## Round-3 Supplement
- 初版 46 行/4003 字符行数不达标（G-04），本次扩写补全。
- 修复循环（第二轮双审后）：continuationPrompt 残留 `status "complete"` → `operate "complete"`（B-05）；tool/goal.ts 加 operate 描述注释 + read 分支注释（E/C 0435 达 E=100/C=17/need=15 PASS）；SSE 测试 ProviderID/ModelID → ProviderV2.ID/ModelV2.ID（target 无 @/provider/schema）。
- 0433 的 deriveGoalTurn 移植（prompt.ts）属 0433 current；0435 不涉 prompt.ts（增量排除）。
- E/C：100/17/15 PASS；parameters.test 新 schema 测试（required=["operate"]、enum、not mark、accepts({})=false）。
## Status
PASS（行为完整；E/C PASS；materialize 435 链 passed）


## Round-3 完整重放（第三轮审计前最终态）
- 构建方式：materialize 434 --rebuild 后在 434 worktree 完整重放 0435 全部适配，git diff 生成单段 current/0435（48993 字节，8 文件），消除历史累积增量段的冲突。
- 全量清单：mark→operate（tool/goal.ts Parameters operate 必填 + read|complete|blocked|active；goal.ts 全部模型可见消息；goal.txt；continuationPrompt operate 措辞）；operate=read 写 trusted snapshot；L253/L381 断言；parameters.test 新 schema 测试（required=[operate]、enum、not mark、accepts({})=false、accepts({mark})=false）；6 个 source 测试补建（modelTransition same-turn changed reason / changed reason resets streak / missing terminal reason / stale objective rejection / same-turn changed reason baseline / user-produced terminal re-mark 拒绝，setupGoalTool→target bootstrap 模式）；SSE 测试（observeGoalEvents Queue+Stream 重写 + ProviderV2.ID/ModelV2.ID）；deriveGoalTurn（prompt.ts created 排序 chronology，参数结构化类型）。
- 修正记录：forEach 参数保留 mark（此前误改 operation 已回退）；{ mark: active } → { operate: active }；idempotency guard 误跳过已用带缩进 anchor 修正。
- E/C：E=282/C=45/need=43 PASS（注释分布于 operate schema、read gate、CAS 三条消息、complete/blocked reason、already-active、continuation 六段、测试意图）。
- 验证：materialize 435 --rebuild passed 435/435；typecheck 435 30/30 passed（报告 11:27，fails=0）。
- 行为测试：435 state 本地 install 被权限策略拒绝；审计员可经共享 .test-workspace（install 指纹 1165df52）运行 goal.test/tool-goal.test/parameters.test。
## Status
待第三轮双审计裁决（本轮修复闭合 E/C/typecheck/record 门禁与既有 B 项）


## Round-4 最终态
- B-02 SSE：observeGoalEvents 的 Queue.take 超时改为 timeoutOrElse→null（到期=观察窗结束，不构成失败），空 Session busy-缺席契约可真实通过。
- 新增：goal.test.ts 并发等价测试（generation=4 唯一性 + stale-CAS）；tool/goal.test.ts description 契约测试（re-read relevant files/overlooked dependencies/same trimmed reason）；goal.txt mark 残留转 operate。
- prompt.ts deriveGoalTurn 本轮归位 current/0433（loop 条件赋值 + 参数结构化类型），current/0435 的 prompt.ts 增量段已由全量重放吸收。
- current/0435 最终 50557 字节；E/C E=323/C=51/need=49 PASS；materialize 435 passed；typecheck 435 30/30（13:24，fails=0）。
## Status
PASS 候选（待第四轮双审计裁决）


## Round-5 最终态
- prompt.test 超时归因更正：E2E#1 挂死根因为 loop 完成谓词 ID 字典序守卫（0433/prompt.ts，本轮已修为 parentID 链接），非环境慢；本 record 早前「11 超时全为环境」的陈述据此更正。
- prompt.ts（deriveGoalTurn raw stream + 条件赋值 + 参数结构化）归位 current/0433；current/0435 专注 mark→operate 适配（50565 字节）。
- E/C：E=323/C=51/need=49 PASS；materialize 435 passed；typecheck 435 30/30（15:25，fails=0）。
## Status
PASS（第六轮双审计一致裁决：正向与逆向独立审计均判 0431-0435 全 PASS，无未闭合 B/G；N 级记录项已在本轮择机修正）

## Round-6 修订：0337 联动 + 依赖环境修复 + 全量验证
- **0337 联动**：上游契约测试 prompt tools replace previous prompt tool rules 在 430/435 双点确定性失败——根因 0337 compact merge hunk 与 baseline 上游 f43209bb8（avoid sticky overrides，TaskTool 改经子会话初始 permission 传 denies、prompt 恢复替换语义）冲突。0337 已删除 merge hunk（record 0337 Round-6 段），本 patch 的 operate E2E 适配不受影响。
- **验证 replace 测试**：prompt tools replace previous prompt tool rules 隔离实测 PASS（54 pass / 唯一 fail 为 ACP subprocess 环境项）。
- **依赖环境修复**：test-workspace 曾被同步到 index-1 又回 435，root node_modules 的 drizzle-orm 变为不兼容布局且 openai@6.39.1 缺 version.mjs/resources/ 子目录（运行时 Cannot find module）。修复：fingerprint 失效触发重装 + registry tarball 覆盖 openai + effect-drizzle-sqlite 嵌套残骸用 root 实例补齐。
- **typecheck**：30/30 全 passed（typecheck-0435-2026-08-30T04-14-59 report Line 26，含 cli/opencode）。
- **行为矩阵**：goal-session 47/47；goal-tool 13/13；httpapi 12/12；parameters 61/61；subtask auto ceilings（0337 保留语义）PASS；E2E×3（60s 预算适配后）全 PASS；E/C 本 patch E=323/C=59/need=49 PASS。
- **已知环境项（非本批）**：ACP subprocess 测试 20s 超时（重型子进程，预存）；openai 深层 node_modules ENOENT 警告（Windows 长路径，非阻塞）。
