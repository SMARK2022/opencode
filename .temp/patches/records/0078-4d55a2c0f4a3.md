
## 全链复查发现（2026-08-31 正向审计 B-0078-01）
- V1/V2 行为分裂：V1 read DEFAULT_READ_LIMIT 降至 400，但 V2 read-filesystem.ts MAX_READ_LINES 仍为 1000，且注释「默认行上限…对齐」已失实。两条 V1/V2 路径均为活路径（registry.ts:98 / builtins.ts:41）。
- 0066 record 的 lockstep 承诺将 V2 对齐义务放在 #78。修复需要在 read-filesystem.ts 将 MAX_READ_LINES 降至 400 并更正注释。此变更需新增 hunk（跨文件编辑），标记为待后续 patch 处理。
- 安全网：V2 的 MAX_BYTES 24KB 累计上限仍在（read-filesystem.ts:282,578），无无限读取风险。