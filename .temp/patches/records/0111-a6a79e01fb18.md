# Record 0111 — a6a79e01fb18

## Identity
- Index: **111**
- Source SHA: `a6a79e01fb18c6a8d662d3445bc511c620a3f01d`
- Subject: feat: 优化 BlockTool 可折叠逻辑（sticky canCollapse）
- Current path: `.temp/patches/current/0111-a6a79e01fb18.patch`
- Current SHA-256: `7688d72f08d188598579fa7f41761afe0bdfb0ec1baeefbc5a94b5c1635bf854`
- Current bytes: **7932** (original 3144 — larger because tip needs dual-path sticky)
- Original path: `.temp/patches/original/0111-a6a79e01fb18.patch`
- Pre-state: `states/0110-b6690c2cc860`
- Materialize/typecheck evidence: batch end `states/0115-6e38594c33fc`, report `typecheck-0115-2026-07-22T18-51-10-448Z` (all packages `passed`, install `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`)

## Source intent (every original behavior)
Original file: `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` only.

1. **BlockTool props**: optional `maxLines?`, `threshold?`, `totalLines?` (defaults 10 / 20 when used).
2. **Memos**: `previewLines = maxLines ?? 10`, `threshold = threshold ?? 20`.
3. **Sticky canCollapse**: `createSignal(false)` + `createEffect` that sets true when `previewLines() > 0 && totalLines > threshold`, and **never** clears once true.
4. **expanded**: local signal toggled on mouse click when collapsible.
5. **UI**: children box gets `maxHeight={previewLines}` + `overflow:hidden` when collapsible and not expanded; footer text “Click to expand/collapse”.
6. **Hover**: include canCollapse in hover condition so expand affordance is discoverable.

Invariant: measured line count may jitter during streaming; expand control and collapsed presentation must not disappear once overflow was observed.

## Upstream / tip design (pre-111, non-removable)
- Owner moved to `packages/tui/src/routes/session/index.tsx`.
- Shell/Write already implement width-aware collapse via **outer** `collapseToolOutput(text, maxLines, maxChars(), threshold)` returning `{ output, overflow }`, local `expanded`, and `limited` memo. This is **stronger** than BlockTool-only maxHeight (also char budget).
- BlockTool is shared chrome (border, spinner, context view). Putting sticky only inside BlockTool without `totalLines` wires leaves a **dead API**.

## Three-way decision
| Concern | Upstream tip | SMARK original | Fused result |
|--------|--------------|----------------|--------------|
| Truncation quality | collapseToolOutput + maxChars | BlockTool maxHeight only | Keep tip truncation |
| Sticky latch | none (overflow can flicker) | canCollapse latch | Latch on outer path + BlockTool |
| API surface | no totalLines | BlockTool props | Add props + wire Shell/Write |
| Path | packages/tui | packages/opencode/.../tui | packages/tui only |

## Concrete current edits
1. BlockTool: full sticky API + expand UI (source).
2. Shell: `shellCanCollapse` latch on `collapsed().overflow`; `limited`/onClick/Show use latch; setter still reads `collapsed().overflow`.
3. Write: same with `writeCanCollapse`.
4. Wire three BlockTool call sites: Shell `title={title()}`, Write `← Write`, Write `# Wrote` with `maxLines/threshold/totalLines`.

## What was NOT done (and why)
- Did not delete outer collapseToolOutput (would regress char-aware truncation).
- Did not move collapse ownership solely into BlockTool for all tools (Edit/diff tools keep their own overflow models).

## E/C (this patch only)
- Effective lines **E=52**
- Qualifying Chinese comments **C=8** (need ≥8)
- Comment sites: collapse contract props; preview/threshold memos; canCollapse latch; click expand; maxHeight; shell sticky; latch must read overflow; write sticky

## Tests / verification
- Cumulative apply 1…115: pass at materialize 115 rebuild
- Typecheck affected workspaces via batch-end: all passed
- Tip greps: `shellCanCollapse`, `writeCanCollapse`, `totalLines=` ×3

## Failure history
- R1: path not found old CLI TUI → migrate packages/tui
- R2: sticky only in BlockTool → dual-audit BLOCK (dead path)
- R3: wire totalLines + outer sticky; fix mistaken replacement of latch condition with self-read

## Final behavior claim
Once Shell/Write output overflows threshold, expand UI stays and limited output stays truncated until user expands, even if overflow boolean flickers; BlockTool sticky API is live when callers pass totalLines.

## Dual-audit repair (post B-01/B-02)
- Removed Shell/Write `totalLines=` wiring that dual-activated BlockTool maxHeight/footer.
- Kept BlockTool sticky API gated by `totalLines !== undefined` only.
- Shell/Write sole owner: outer `collapseToolOutput` + sticky latch.
- Current SHA-256: 7688d72f08d188598579fa7f41761afe0bdfb0ec1baeefbc5a94b5c1635bf854
- Bytes: 7173

## Independent dual-audit verdict
- FORWARD (111→115): **PASS** — no open B/G
- REVERSE (115→111): **PASS** — no open B/G
- Tip: `states/0115-6e38594c33fc`
- Materialize: `materialize-0115-2026-07-22T19-25-28-236Z`
- Typecheck: `typecheck-0115-2026-07-22T19-26-07-344Z`
- Install: `48748d10ad658138eca6634b79298f113fb7ef7553de4ce10d2948c100404ad1`
- Batch release: **PASS**
