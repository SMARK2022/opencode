# Patch Record 0006

## 1. Identity

- Index: 6
- Source SHA: `bb430162951a92c9139ee820da30866a4cdfbc6d`
- Parent SHA: `6c994e2db366fa0231cd37279657b9afbcbbf735`
- Subject: `feat(usage): 优化使用信息计算，确保在无回复时显示零值`
- Original patch: `original/0006-bb430162951a.patch`
- Current patch: `current/0006-bb430162951a.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Pinned source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Previous materialized state: cumulative state through index 5 (not retained; verified via `--typecheck 10`)
- Current materialized state: `states/0010-fc6394523010` (cumulative through batch end)

## 2. Source Diff Evidence

- Complete diff read: Yes, 172 lines.
- Changed paths: 3 TUI files (path migrated from `packages/opencode/src/cli/cmd/tui/` to `packages/tui/src/`)
- Behavior intent:
  1. Widen `UsageInfo.context` to `string | undefined`
  2. Add `isRunning` from `status()` memo; return zeroed display when no assistant message and running
  3. Replace `last.tokens.*` with `cumTokens` (aggregate across all step-finish parts)
  4. Return zeroed display when all tokens zero and running

## 3. Adaptation Summary

- Path migration: `packages/opencode/src/cli/cmd/tui/` → `packages/tui/src/`
- Context fuzz: Prior SMARK patches (3, 5) added Chinese comments and `const session` line — all modified lines present verbatim
- `subagent-footer.tsx`: needs new `status` memo (not in target); uses `sync.data.session_status` + `route.sessionID`
- `sidebar/context.tsx`: multi-line zero return collapsed to single line (formatting only)
- All `last.tokens.*` → `cumTokens.*` aggregation preserved
- Upstream `session?.cost` pattern preserved

## 4. Verification

- Cumulative typecheck: `--typecheck 10` passed 10/10 on v1.17.20 baseline
- Install fingerprint: `041e15a7234252aeb9cda2a6a04f62fd306e39973291ff9e97480567ba62a7d4`
- Typecheck: packages/opencode:passed, packages/tui:passed
- E=72, C=11, required C=max(1,ceil(72*0.15))=11. Comments at: isRunning guard, zero-value display, cumTokens aggregation, zero-token fallback.
- Comment positions: `// 会话运行中（非 idle）时即使没有助手回复也显示零值用量` (isRunning), `// 累加所有 step-finish parts 的 token（last.tokens 仅含最后一步）` (cumTokens), `// 运行中但 token 尚未到达时显示零值，避免 footer 闪烁` (zero fallback), `// 始终显示零值，sidebar 从 prompt 发送起即可见` (sidebar zeros), `// 获取会话状态，用于判断是否正在运行` (status memo in footer)

## 5. Final Verdict

`PASS` (pending independent audit)
