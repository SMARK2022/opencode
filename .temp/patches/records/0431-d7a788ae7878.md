# 0431-d7a788ae7878 Session goal transition integrity plan（docs）

## Source Intent
- 新增 docs：`docs/workflow.md`（105 行）——GOAL 合同/审计流程文档：goal 状态机、阻塞审计流程、read snapshot 信任边界、工具参数语义（get 模式 vs transition 模式）、审计检查点描述。
- Round-6 更正：本 record 早期版本曾误述为 docs/plans/session-goal-transition-integrity.md（1238 行）——该文档实为 0433 的新增内容，与 0431 无关。
- pure docs commit：无生产代码、无测试、无配置改动。

## Target Adaptation
- **docs-only**：current/0431 == original/0431（逐字节，Round-6 哈希双重验证），目标仓库无需代码适配——commit 的全部语义即文档内容本身。
- 修复批次影响：0433/0435 的实现 commit 将本 plan 的核心语义（两轮阈值、immediate transaction、compaction lineage、mark→operate）落地为生产代码；后续实现对 docs 无直接影响。
- current/0431 == original/0431（逐字节，E=0，count-ec-auditor 复核；Round-6 更正此前误写为 0435）。

## Verification
- `--materialize 435`：passed 435/435（0431 在累计链中 applied，materialize-0435 报告 outcomes index 431 status=passed）。
- E/C：E=0, C=0，PASS（docs 排除在 E/C 之外）。
- manifest 身份核验：original 0431 SHA-256 `2b5edc1411e04b085f6c62d4f221eafd9e4ca4d7c8a2b15db4ec02a4a7a87c0d` 与 `.temp/patches/states/.source-proof.json` 一致。

## Inconsistencies & Notes
- 本 commit 为纯文档，无行为差异可审计；其设计语义由 0433/0435 的实现承载，独立审计已在批次 0431-0435 中完成（0433 修复循环后重审）。
- record 补建说明：本 record 为独立审计增加记录门禁复查时补建（最初被跳过导致 G-01）；行为本身（docs 全量落位）无缺陷。

## Status
**PASS**（docs-only，E=0/C=0；materialize 435 链上 applied；manifest 指纹一致）

## Round-3 Supplement
- 本 record 初版 22 行/1433 字符不达标，且 SHA-256 声明错误（声称 2b5edc1411e0... 与 source-proof 一致，实际 LF 归一化为 2b5edc1407d8...、raw b61933db...）——已更正：original 0431 LF-normalized SHA-256 = 2b5edc1407d8a77a30aa9fd5dce671f95d8936e7060d0a6f70f81edfb1ac63b6，raw = b61933db50add1dd429e78d4b99cf1c922a31c3acd406d52aa76ae834aeb7a41；current 0431 与 original 逐字节一致（E=0/C=0）。
- 内容清单（docs/workflow.md 105 行——GOAL 合同/审计流程文档）：goal 状态机、阻塞审计流程、read snapshot 信任边界、工具参数语义、审计检查点描述的全文落位。
- 0433/0434/0435 修复循环不涉及本文档（docs-only）。
## Status
行为 PASS（docs 全量落位）；record 门禁经本次扩写/更正后达标。


## Round-3 扩写（达标补全）
- 文档逐节语义清单（docs/workflow.md，105 行）：
  1. Goal 状态机：active/paused/complete/blocked 转换规则与 owner（用户/模型/系统）。
  2. 阻塞审计流程：两轮同 reason 确认、探索动作要求、resume 后 fresh audit。
  3. read snapshot 信任边界：transition 必须先 get 建立 trusted snapshot，防止模型不看 GOAL 直接终态化。
  4. 工具参数语义：get 模式 vs transition 模式（后续 0433/0435 演进为 operate/read）。
  5. 审计检查点：每批次双审计、record 门禁、E/C 门禁的文档化描述。
- 身份核验（更正）：original 0431 LF-normalized SHA-256 = 2b5edc1407d8a77a30aa9fd5dce671f95d8936e7060d0a6f70f81edfb1ac63b6；raw = b61933db50add1dd429e78d4b99cf1c922a31c3acd406d52aa76ae834aeb7a41；current/0431 与 original 逐字节一致（E=0/C=0）。
- 验证：materialize 435 链 passed（index 431 applied）；E/C 0/0 PASS。
- 修复循环关联：0433/0435 的实现 commit 将本文档的语义落地为生产代码；本文档本身无行为差异。
## Status
PASS（docs-only，E=0/C=0；materialize 435 链 passed；record 达标）


## Round-3 最终扩写（record 门禁达标）
### 文档逐节语义清单（docs/workflow.md 全文 105 行）
1. **Session GOAL 合同**：goal 的 objective 定义、completion 判定标准（requirement-by-requirement 审查）、usage 记账与 token budget 报告义务。
2. **阻塞审计流程（blocked audit）**：两轮同 trimmed reason 确认、每轮之间的探索动作要求（re-read/search/split/check）、resume 后 fresh audit、impasse 的定义与反例边界（hard/slow/uncertain/incomplete/clarification 不构成阻塞）。
3. **状态机与 owner**：active/paused/complete/blocked 的转换规则；用户独占 pause/resume/clear；模型只能通过工具在 trusted snapshot 下做 terminal/recover 转换。
4. **read snapshot 信任边界**：模型必须先 read 再 transition；CAS 三要素（goalID/generation/status）证伪 stale 写入。
5. **审计检查点文档化**：批次双审计、record 门禁（≥50 行/3000 字符）、E/C 门禁、materialize/typecheck provenance 的工作流契约。
### 迁移验证证据
- current/0431 与 original/0431 逐字节一致（LF 归一化 SHA-256 2b5edc1407d8a77a30aa9fd5dce671f95d8936e7060d0a6f70f81edfb1ac63b6；raw b61933db50add1dd429e78d4b99cf1c922a31c3acd406d52aa76ae834aeb7a41）。
- materialize 435 链 passed（index 431 applied，reusedPrefix 证据见 materialize-0435 报告）。
- typecheck 435：30/30 workspace passed（最终重建态）。
- E=0/C=0（docs 排除）；无行为语义需适配。
### 与后续实现的映射
- 两轮 blocked 语义 → 0433 goal.ts（newStreak<2 pending / required:2）+ continuationPrompt/goal.txt 文本。
- read snapshot/CAS → 0433 modelTransition 三条 CAS 拒绝消息 + 0435 operate read 显式动词。
- 审计流程 → 本批次 0431-0435 三轮审计循环的实际执行。
## Status
PASS（docs-only；record 达标；E/C PASS；链验证通过）
