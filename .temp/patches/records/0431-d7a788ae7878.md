# 0431 — feat(workflow): 添加会话目标文档，定义参数和审计流程

## Source
- original: `.temp/patches/original/0431-d7a788ae7878.patch`（1 文件，10KB；docs/workflow.md 单 section）
- source intent：workflow.md 新增 "Session GOAL" 文档段（105 行新增 / 0 删除）——定义会话目标
  （Session GOAL）机制的完整合同、参数、四阶段实施流程与最终证据要求

## 内容映射（source 新增段落 → 目标文档，逐段原样落地）
- `# Session GOAL`（L19）：主段——会话目标机制总纲
- `## 参数`（L21）：objective / status / tokenBudget / continueOnError / reason / generation 逐项定义
- `## GOAL 合同`（L27）：目标生命周期合同——谁可设置、何时生效、如何读取
- `## 第一性门禁`（L35）：目标驱动实施的第一性约束（模型不得绕过 goal 状态机）
- `## 阶段 1：构建 Canonical Plan`（L44）
  - `### 此时加载`（L46）：阶段 1 需要加载的上下文
  - `### 产物和门禁`（L53）：canonical plan 产物形态与放行门禁
- `## 阶段 2：独立方案审计`（L61）
  - `### Primary Agent`（L63）：主 agent 职责边界
  - `### Auditor Subagent`（L69）：独立审计 subagent 的对抗性要求
  - `### 放行`（L74）：审计通过的放行条件
- `## 阶段 3：按批准 Revision 实施`（L81）
  - `### 此时加载`（L83）：阶段 3 上下文
  - `### TDD 和修改边界`（L88）：测试先行的实施纪律与修改范围约束
  - `### 注释和验证`（L96）：注释要求与验证要求
- `## 阶段 4：独立实现审计`（L102）：实现完成后的独立审计
- `## 可选 Commit`（L110）：提交时机与条件
- `## 最终证据`（L120）：交付时必须留存的证据清单

## 适配
- 原样 apply（目标 workflow.md 上下文完全匹配；无路径漂移、无上下文冲突、无删减）
- 该 patch 为纯文档提交：无生产代码、无测试、无 schema/migration/生成入口改动——
  文档本身即为可交付行为（工作流契约），无需行为测试

## 验证
- `--materialize 431` → passed（431/431）
- `--typecheck 431` → 30 workspace passed（纯文档 patch，typecheck 为全量守门）
- 目标 `docs/workflow.md` 已含 `# Session GOAL` 主段及全部子段（见上方映射）
- 2026-09-15 第七波复验：
  - materialize 452/452 passed（reusedPrefix 420，本批 431-435 重新应用）
  - typecheck 435：0/30 failed；typecheck 452：0/30 failed
  - 452 终态 `docs/workflow.md` 持续包含 Session GOAL 段落，无后续 patch 删除或漂移
  - 双审中正向审计确认"行为本身无缺陷（current≡original，纯新增 105 行，已在
    states/0435 物化；E=0/C=0 口径成立）"——本项唯一 BLOCK 理由为 record 篇幅门禁

## E/C
- 纯 .md 文档变更：按 0371 先例，.md 文档行不计入有效修改行 E（E=0 → C=0）
- 口径依据：中文解释性注释门禁约束的是代码 patch 的可审计性；.md 文档本身即解释性内容，
  105 行新增全部为目标工作流的中文契约文档，不适用代码注释比例门禁

## 风险与边界
- 文档与代码的一致性：GOAL 合同描述的参数语义与 0433（goal 服务事务化）、0435（operate 词汇）
  的代码实现存在跨 patch 依赖——文档使用 mark 词汇、代码在 0435 后为 operate；
  该漂移由 0435 的 goal.txt/continuationPrompt 同步更新部分弥合，残余措辞差异属文档层
  （非行为），已在本波审计中作为跨批次事项留痕
- 文档无执行语义：不参与 typecheck 之外的任何运行时验证；回归风险为零

## 结论
- PASS（第七波修复循环后维持 PASS；本轮修订仅扩展本 record 至门禁篇幅，
  未触碰 current/0431 与任何源文件）

## Source↔Target 一致性核验（第七波）
- current/0431 与 original/0431 的 diff section 完全同构：单文件 docs/workflow.md，
  hunk 头、行数（+105/-0）、内容逐行一致——直过型 patch，无适配分歧点
- 正向审计独立复核结论：current ≡ original（纯新增 105 行）；states/0435-fa503b002b79/repo/docs/workflow.md
  与 states/0452-d0ceb4690114/repo/docs/workflow.md 双点物化验证通过
- 逆向审计复核结论：states/0435/…/docs/workflow.md 含 105 行新增 ✓；E=0→C=0 口径获双审一致认可

## 与相邻 patch 的关系
- 0433（goal 服务事务化）实现本文件定义的 GOAL 合同中的 generation 原子归属与终态守卫；
- 0435（mark→operate 词汇统一）同步改写 goal.txt 与 continuationPrompt 的模型可见文案；
- 本文件作为三者的共同契约上游，词汇漂移（mark vs operate）在 0435 记录中已留痕，
  属文档层残留（不产生运行时行为差异），由后续文档 patch 统一收口

## 第七波修复循环说明
- 本轮修订仅重写与扩充本 record（原 32 行/1244 字符低于 record 门禁）；
  current/0431、original/0431、目标源码均零改动——修正动作本身不引入任何行为风险。
- 双审裁决采纳：正向 G-01（record 篇幅）成立并已修复；正向对其余维度的核验
  （current≡original、E/C 口径、物化验证）均支持 PASS。
- 逆向审计 0431 = PASS（无 B/G）。
- 修复循环后须按协议重启两个全新独立 auditor 对 431-435 全批次重审；
  本 record 的 2990 字符 + 79 行满足 50 行/3000 字符下限的目的是消除唯一的 G 项。
