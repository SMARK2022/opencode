# Record 0131-5baa17d8044d

## Identity
- Index: **131**
- Source SHA: `5baa17d8044d…`
- Current: `.temp/patches/current/0131-5baa17d8044d.patch`
- Current SHA-256: `6f97cfe562a0a578a63b9b5d49cc823915d0a8bb34eb3ae6daf4de1ede75df5f`
- Bytes: **18620**
- Subject: feat(line-ending): 共享行尾工具，patch/edit/write 保留原始行尾并隐藏纯换行 diff
- E/C: **E=168 C=26 need>=26**

## Source intent（original 事实源）
1. 新增 `packages/opencode/src/util/line-ending.ts`：
   - `normalizeLineEndings`：`\r\n?` → `\n`（含 CR-only）
   - `detectLineEnding`：优先 CRLF，再 CR，再 LF
   - `splitLines`：逻辑行分割（CR-only 不能整段当一行）
   - `convertToLineEnding`：写回文件级行尾
2. `patch/index.ts`：`parsePatch`/`deriveNewContentsFromChunks`/`generateUnifiedDiff` 用 `splitLines`；写回 `convertToLineEnding(..., detectLineEnding(original))`
3. `apply_patch`/`edit`/`write`：`createTwoFilesPatch` 与 `diffLines` 前 normalize，避免 CRLF-only 显示为内容变更
4. 测试：CRLF-only / CR-only 的 metadata fallout 与写回保留

## 上游 / tip@130
- `edit.ts` 已有**局部** normalize/detect/convert，但仅 CRLF/LF，**无 CR-only**，且未共享
- `patch` 用 `text.split("\n")` + `join("\n")` 会把 CRLF/CR 文件写回为 LF
- Bom 模块只处理 BOM，不负责行尾

## 三方比较与适配
| 点 | Source | tip@130 | Current |
|---|---|---|---|
| 共享 util | line-ending.ts | 无（edit 内联） | 新建 util，edit 改 import |
| CR-only | detect 含 `\r` | 无 | 完整三态 |
| patch 写回 | convert 原行尾 | 强制 LF | convert+detect |
| metadata diff | normalize | 原文 | apply_patch/edit/write normalize |
| 测试 | 7 个 fallout 用例 | 部分 CRLF 保留 | util 5 例 + write/edit/apply_patch 敏感例 |

## first divergence 与主路径
**first divergence**：patch 写回强制 LF；diff 元数据把行尾当内容。  
**owner**：`util/line-ending` + patch 写回 + 三工具 metadata 路径。  
**禁止**：第二套行尾算法、在 formatter 后再改行尾策略。

## Current 文件映射
- `packages/opencode/src/util/line-ending.ts`（新）
- `packages/opencode/src/patch/index.ts`
- `packages/opencode/src/tool/apply_patch.ts`
- `packages/opencode/src/tool/edit.ts`
- `packages/opencode/src/tool/write.ts`
- `packages/opencode/test/util/line-ending.test.ts`（新）
- `packages/opencode/test/tool/edit.test.ts`（CRLF/CR 编辑保留）
- `packages/opencode/test/tool/write.test.ts`（CRLF/CR 覆写 metadata）
- `packages/opencode/test/tool/apply_patch.test.ts`（CRLF/CR apply）

## 调用链
edit/write/apply_patch execute → Bom.readFile →（匹配用 normalize/split）→ 写回 convert(detect) → createTwoFilesPatch(normalize, normalize) → permission/TUI metadata。

## 注释位置（C）
util 四函数边界；patch 写回；apply_patch/edit/write 的 diff 归一说明；测试意图注释。

## 失败修订
- original 路径/import 与 tip Effect 测试 harness 不同 → 测试改为 `it.instance`/`TestInstance`
- 曾追加非法顶层 `test()` 导致 TS2593 → 删除并改用 tip harness
- dual audit B-02：缺测试 → 本轮补 util+tool 敏感测试

## 验证
- materialize 累计至 135：`states/0135-3bed2cfd5377`
- typecheck：`typecheck-0135-2026-07-23T16-38-21-386Z` 30/30
- install：`b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`
- unit：line-ending 5 tests（workspace）

## 上游设计语言保留
Effect Tool.define、Bom.read/write、createTwoFilesPatch/diffLines 仍在原 owner；line-ending 只做纯函数边界。

## 最终
共享 line-ending 完成；patch 保留写回行尾；metadata 隐藏纯换行 diff；CR/CRLF 测试具备红绿敏感性；E/C 达标。

## 批 131-135 共用验证指纹
- tip state: states/0135-3bed2cfd5377
- typecheck report: typecheck-0135-2026-07-23T16-38-21-386Z (30/30 packages passed)
- install fingerprint: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- prior batch 126-130: dual PASS
- dual audit this batch: PASS

## 生产行为不变量
1. patch 应用后，原文件若是 CRLF 或 CR-only，写回必须保持该风格。
2. permission/TUI 看到的 unified diff 在仅行尾变化时不得出现 -Line/+Line 内容对。
3. detect 优先级固定：CRLF > CR > LF，避免混合文件误判。
4. Bom 与行尾正交：先 Bom.split/join，再在 text 域做 line-ending。

## 测试红绿含义
- line-ending.test.ts：纯函数表驱动，任何 CR 处理回退会立刻红。
- write line-ending metadata：同逻辑内容覆写 CRLF/CR 文件，diff 不得含 -Line 1。
- edit preserve：LF oldString 能匹配 CRLF/CR 文件并写回原行尾。
- apply_patch：逻辑行匹配更新 CRLF/CR 文件。

## Dual independent audit (batch 131-135)
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Residual closed: #132 write SessionPath.relative; #131 CR/CRLF tests; records substance; viewportCulling top-level; version 1.17.23-smark
- Typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- Install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- Tip: states/0135-3bed2cfd5377
- Batch verdict: **PASS** — may advance to 136-140
