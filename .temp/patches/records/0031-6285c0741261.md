# Record: Patch 31 — 6285c0741261

## 1. Manifest Entry
- **Index**: 31
- **SHA**: 6285c0741261794c6c56843af4203e7ef2d51971
- **Parent**: 0030-98ebae3df2df
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: fix: 更新 Flag 模块导入路径以适配上游架构重构

## 2. Source Behavior (original patch, 2 files)
1. `bun.lock`: 将版本号从 1.14.25 改为 1.14.25-smark，并为所有包添加 npmmirror 镜像 URL
2. `system.ts`: 将 `import { Flag } from "@/flag/flag"` 改为 `import { Flag } from "@opencode-ai/core/flag/flag"`

### 行为目的
- **Flag 导入路径迁移**: 将 Flag 模块从 `@/flag/flag` 迁移到 `@opencode-ai/core/flag/flag`，适配上游 core 包重构
- **bun.lock 镜像**: 配置 npmmirror 国内镜像源

## 3. 上游 (v1.17.20) 行为
- V2 已将 `Flag` 完全重构为 `RuntimeFlags`（`import { RuntimeFlags } from "@/effect/runtime-flags"`）
- V2 system.ts 不再有 `Flag` 引用，使用 `RuntimeFlags.Service` 和 `RuntimeFlags.node`
- V2 `ToolRegistry` 已从 `@/tool` 迁移到 `@/tool/registry`
- V2 `Shell` 已从 `@/shell/shell` 迁移到 `@opencode-ai/core/shell`
- V2 bun.lock 版本为 1.17.20，内容完全不同

## 4. 目标适配 (current patch, 1 file)
### 跳过的文件
- `bun.lock`: 生成文件，V2 版本完全不同（1.17.20 vs 1.14.25），镜像 URL 通过 bunfig.toml/.npmrc 配置而非 lock 文件

### 适配的文件
1. `system.ts`: 原始 patch 的 Flag 导入路径迁移已由上游 RuntimeFlags 重构完成。添加 2 行中文注释记录此事实。

### 保留的 SMARK 语义
- Flag 导入路径迁移的意图（适配上游 core 包重构）已由上游 RuntimeFlags 重构实现
- bun.lock 镜像配置是环境/部署配置，非行为不变式

### 保留的上游优势
- V2 的 RuntimeFlags 模块（替代 Flag）
- V2 的 ToolRegistry 导入路径（`@/tool/registry`）
- V2 的 Shell 导入路径（`@opencode-ai/core/shell`）

## 5. E/C 计算
- **E = 0**（仅添加注释行，无有效代码修改）
- **C = 2**（`// Flag 模块已在上游重构中迁移为 RuntimeFlags，导入路径已更新` + `// 原始 SMARK patch 31 的 Flag→@opencode-ai/core/flag 迁移已由上游完成`）
- **E = 0 时 C = 0** — 注意：C=2 但 E=0，注释行不计入有效修改行
- **门禁通过**（E=0 时无 C 要求）

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 35 --rebuild`: passed 35/35
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/session-ui:passed, packages/tui:passed, packages/ui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首次 apply 失败**: bun.lock 版本不匹配（1.14.25 vs 1.17.20）；system.ts 导入路径不匹配（Flag vs RuntimeFlags）
   - 修复: 丢弃 bun.lock hunk；system.ts 添加注释记录上游已完成迁移

## 8. 最终行为
- Flag 模块导入路径迁移已由上游 RuntimeFlags 重构完成
- bun.lock 镜像配置跳过（生成文件，V2 版本不同）
- V2 路径和结构保留
