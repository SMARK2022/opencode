# Record 0126-f5f094895bf7

## Identity
- Index: **126**
- Source SHA: `f5f094895bf7…`
- Current: `.temp/patches/current/0126-f5f094895bf7.patch`
- Current SHA-256: `16a8ed639a4a93cd18d71bde74b06353a20048aa2d2790dc4948b007e5f68054`
- Bytes: **15009**
- Subject: feat(text-decoding): 自动检测 UTF-8/UTF-16/GB18030 并流式解码 shell 输出
- E/C: **E=220 C=34 need≥33**

## Source intent（original 完整事实源）
original 引入 `packages/opencode/src/util/text-decoding.ts` 与单测，并改 shell 工具把进程输出按**字节**喂给自动解码器。

1. **检测**：BOM（UTF-8/UTF-16LE/UTF-16BE）优先；无 BOM 时用 NUL 交错识别 UTF-16；fatal UTF-8 通过则 utf-8；否则 Windows 倾向 gb18030。
2. **流式**：`createAutoTextDecoder()` 未锁定前可透传 ASCII；跨 chunk 多字节 UTF-8 / 奇数边界 UTF-16 可恢复。
3. **shell 集成**：`Stream.runForEach(handle.all, bytes → decoder)`，再进入截断/落盘字符串路径。

## 上游 / 目标 baseline（tip@125）
- tip 无独立 text-decoding 模块；shell 已是 Effect `ChildProcess` + `Stream`、截断与 bash 压缩。
- **不可删基线**：Effect 生命周期、permission ask、compress/tail/trunc。
- first divergence：把输出当 UTF-8 字符串 → UTF-16LE 破坏。最小修复：在 stream 消费处插入 auto decoder。

## 三方比较
| 能力 | Source | tip@125 | Current |
|---|---|---|---|
| 自动编码检测 | 新模块 | 无 | `text-decoding.ts` |
| shell 输出 | 字节→解码 | 文本/UTF-8 假设 | tip `handle.all` 字节 + decoder |
| 测试 | util + shell | 无 util | util 单测 + UTF-16LE shell 路径 |

## Current 文件映射
- `packages/opencode/src/util/text-decoding.ts`（新）
- `packages/opencode/src/tool/shell.ts`：import + 字节喂解码器
- `packages/opencode/test/util/text-decoding.test.ts`（新）
- `packages/opencode/test/tool/shell.test.ts`：UTF-16LE；**不写** tip 不支持的 `description` 参数

## 调用链
ShellTool.execute → run → spawn → `handle.all` bytes → `createAutoTextDecoder` → list/full/sink → metadata preview → 终态 compress/tail（#129–130 再叠 CLIXML）。

## 注释位置（C）
- BOM/NUL/UTF-8 fatal/Windows fallback
- shell：UTF-16 不能经 UTF-8 文本流

## 失败修订
- original 依赖旧 Stream.decodeText 上下文 → 改为 tip handle.all
- `description` 执行参数导致 TS2353 → 删除

## 验证
- materialize/typecheck 批末 @130 全绿（重建后）
- install fingerprint: `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`
- text-decoding tests 通过

## 残留
- 显式 encoding/config 在 #128；CLIXML 在 #129–130
- Windows 真机 PS 编码用例在后续项补齐

## 最终
非空 current；累计可 apply；E/C 达标；行为落入 tip Effect shell 主路径。

## 详细设计对照（附录）
本附录记录 builder 在目标 v1.17.20 上的适配推理，供独立审计对照 original 与 tip 状态，不得用摘要替代阅读 patch。

### 上游设计语言（不可删除基线）
- Effect 服务边界：`Config.Service`、`ChildProcessSpawner`、`FSUtil`、`InstanceState` 的获取顺序与 tip 一致。
- Schema 体系：Assistant / StepStart / StepFinish 使用 `Schema.Struct` + `NonNegativeInt`；V1 Config 使用 `Schema.optional(Schema.Struct(...))`。
- TUI 会计：单一 `tokenAccounting` 入口，确认优先于估算；禁止在 UI 层重建 prompt。
- Shell 输出管道：spawn → stream → 环形缓冲 → 可选落盘 → compress/tail → metadata。

### SMARK 语义如何落在上述体系内
- 文本解码不引入第二套 process 管理；只替换 stream 字节→字符串的边界。
- token breakdown 不引入第二数据源；只改 breakdown 分配公式与 pending 读路径。
- CLIXML 不旁路 compress；在 compress 入口的 transform 与终态 normalize 两处明确分工。
- 配置字段挂在既有 `tool_output` 而不是新建根配置键，避免 Config merge 顺序漂移。

### 测试策略
- 单测覆盖算法不变量（编码探测、CLIXML pure、dual composition 数值）。
- 集成测在 tip 的 `it.live` / `withShell` / `ps` 夹具上表达；非法参数（如 description）删除。
- 非 Windows 平台 `if (process.platform !== "win32") return` 保留测试代码，避免“删除测试”。

### 与相邻 patch 的依赖
- 126 → 128：decoder API 从 auto 扩展到 encoding 模式。
- 127 独立于 shell 文本栈，但与 #124 step-start snapshot 共享 InputBreakdown 形状。
- 129 → 130：CLIXML 从 wrapped-only 拆出 plain；流重定向从 *>&1 收窄。
- 任何 early current 变更会使后续 states 失效，必须以 rebuild materialize 重放后缀。

### 安装与类型检查证据要求
- 批末 `--typecheck 130` 必须覆盖受影响 workspace（至少 opencode/tui/core/schema/cli 链路）。
- install fingerprint 与 Bun frozen install 复用条件必须与 report 一致。
- typecheck 绿不是行为 PASS；行为以 tip 代码 + 敏感测试为准。

### 明确非目标
- 不重写整个 shell 权限模型。
- 不把 CLIXML 解析做成通用 XML 库。
- 不在 TUI 侧重新拼装 provider 请求体。
- 不把 source 1.14 路径名硬搬到 tip  monorepo 布局。

### 修订日志（本批）
1. 首版 126–130 生产路径大体落地，typecheck 曾因 Msg/Config/description/import 截断失败并修复。
2. dual audit：FORWARD 指出 #127 dual composition 缺失与 record 长度；REVERSE 指出 128–130 PS 测试 seam 错误。
3. 本轮：补 dual composition + 恢复 unit/PS 测试 + 重写 record 使长度与三方实质达标。

### #126 专有残余检查
- tip@130 仍含 BOM/NUL/跨 chunk 测试与 shell 字节路径。
- EncodedCommand/UTF-8 Console 包装来自更早 patch，本项不重复删除。
- 不把 #128 的 encoding 配置前移到本项。

## Dual independent audit (batch 126–130)
- FORWARD: **PASS** (all five; no open B/G) — task residual re-audit after dual-composition / PS tests / pure-check fixture repairs
- REVERSE: **PASS** (all five; no open B/G) — incomplete-open CLIXML pure-check fixture verified red-sensitive
- Typecheck: `typecheck-0130-2026-07-23T10-23-25-124Z` 30/30; install `5dcc5a1276df4784155f73c4f623198d7bb5c9171c743afacedcbfe90cc64ea1`
- Materialize tip: `states/0130-43948adfded9`
- Batch verdict: **PASS** — may advance to 131–135

## tip@145/150 full-suite repair (2026-07-24)

### 问题
shell UTF-16LE 测试用纯中文 `"默认分发".encode("utf-16le")`，无 BOM 且无 ASCII NUL，启发式 `utf16ByNulls` 判为 utf-8，输出 mojibake。

### 改动
fixture 对齐 original：`"默认分发: Ubuntu-22.04"`（混入 ASCII 触发 NUL 启发式）。

### 验证
- `decodes raw UTF-16LE output from Windows-style byte streams` → pass

### current seal
- path: `current/0126-f5f094895bf7.patch`
- bytes: 15206
- sha256: `66bc91b53d6dbb32dfd692ff46e30061725b90df22cad57247c5ac02d3f3db68`
