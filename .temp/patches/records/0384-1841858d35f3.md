# Record 0384-1841858d35f3

## Source patch

- Source: `fix(tui,voice): 修复 QuestionPrompt/DialogPrompt 录音计时器冻结在 00:00` (SMARK2022, 2026-07-06)
- Original: `.temp/patches/original/0384-1841858d35f3.patch` (156 lines, 3 files)
- Replay index: 0384, sha 1841858d35f3de74e268b04d9d675a55d58cb53c
- Manifest parent: `128a66a0e161` (source-repo parent, NOT sequential to 0383 —
  same out-of-chain manifestation as 0383; processed in manifest index order)

## Source intent

`QuestionPrompt` (`question.tsx`) and `DialogPrompt` (`dialog-prompt.tsx`) render
the voice recording footer status via
`PromptVoiceInput.voiceInputStatusText(voiceInputStatus(), voiceShortcut() || voiceShortcutFallback)` —
without a third `now()` argument and without any `createRefreshClock`-driven
periodic refresh. The mechanism: during steady-state recording,
`voiceInputStatus()` no longer changes, so the inline JSX expression has no
reactive dependency that re-evaluates; the `now` argument (when present) is
evaluated only once at recording-start instant, and the timer display freezes at
00:00 forever. The main Prompt already had the proven pattern
(`createRefreshClock(() => running() || voiceInputStatus().type === "recording")`
+ `now()` passed into `voiceInputStatusText`).

Fix per component:
1. `import { createRefreshClock } from "../../util/signal"`.
2. `const now = createRefreshClock(() => voiceInputStatus().type === "recording")`
   right after the voice shortcut signals.
3. Pass `now()` as the third argument at the footer call site.
4. The test file gains a `createRefreshClock` behavior-contract test (active ticks
   / inactive stops / dispose cleans up the interval). This util previously had
   zero test coverage; the contract lives at 10ms interval via `createRoot`
   isolation so real 1s waits are avoided. Every DialogPrompt consumer (Goal
   create/edit, Session rename, Provider config, Plugin API) is fixed implicitly.

## State analysis (state 0383)

- `packages/tui/src/routes/session/question.tsx` EXISTS (606 lines). The voice
  setup block at lines 43-48 has exactly the source-hunk context:
  `voiceInputBusy` / `voiceShortcut` / `voiceShortcutFallback`, then
  `function insertVoiceText` with the stale-ref comment. The footer
  `voiceInputStatusText(...)` call is at line 589 (single-line, matching the
  source hunk). Imports: the file imports `Spinner` from `../../component/spinner`
  but NOT `createRefreshClock`.
- `packages/tui/src/ui/dialog-prompt.tsx` EXISTS (212 lines); the same voice block
  at lines 40-42 and the footer call at line 186. It ALSO already uses the
  `description?: () => JSX.Element` shape introduced by the 0.4.3 upgrade
  (upstream #35226) — unrelated to this patch's hunks; the footer block context
  (lines 179-189) is untouched by that upgrade.
- `packages/tui/src/util/signal.ts` EXISTS with
  `export function createRefreshClock(active: Accessor<boolean>, ms = 1000)` at
  line 128 — signature matches the test's `createRefreshClock(active, 10)` usage.
- The main Prompt's canonical usage confirmed at
  `packages/tui/src/component/prompt/index.tsx:380-385` (`now()` + three-arg
  `voiceInputStatusText`), proving the fix pattern is the established one.
- `packages/tui/test/prompt/prompt-voice-input.test.ts` EXISTS (611 lines). The
  "formats active footer status..." test (lines 81-88) is where the new test slots
  in after; imports are `bun:test`, `path`, `../fixture/fixture`, the
  `../../src/prompt-voice-input` namespace, with NO solid-js import yet.

## Port decision

Full port of all three files with path adaptation only (`packages/opencode/src/
cli/cmd/tui/...` → `packages/tui/src/...`, tests `packages/opencode/test/cli/tui/
prompt-voice-input.test.ts` → `packages/tui/test/prompt/prompt-voice-input.test.ts`).
Import paths shift exactly one directory level (question.tsx keeps `../../util/
signal`; dialog-prompt.tsx keeps `../util/signal`; the test imports
`../../src/util/signal` instead of the source's `../../../src/cli/cmd/tui/util/
signal`). No other adaptation needed — `createRefreshClock`, `voiceInputStatusText`
three-arg form, and the voice-input API are identical in the target.

## Edit script (semantic)

1. `question.tsx`:
   - imports: `import { createRefreshClock } from "../../util/signal"` inserted
     between the `Spinner` and `PromptVoiceInput` imports (mirrors source order).
   - After `voiceShortcutFallback`: the 2-line rationale comment
     (录音计时器需要每秒刷新 now 信号驱动 footer 的 Recording MM:SS 走数；缺少它时
     voiceInputStatusText 的 now 参数只在录音开始瞬间求值一次，计时器冻结在 00:00。)
     + `const now = createRefreshClock(() => voiceInputStatus().type === "recording")`.
   - Footer line 589: add `, now()` as the third argument.
2. `dialog-prompt.tsx`: same three edits with `../util/signal` import path and the
   line-186 call site.
3. `prompt-voice-input.test.ts`:
   - Add `import { createRoot, createSignal } from "solid-js"` after `path`.
   - Add `import { createRefreshClock } from "../../src/util/signal"` after the
     prompt-voice-input namespace import.
   - Insert the 47-line behavior-contract test (source-verbatim content: createRoot
     fixture with 10ms clock, microtask flush for the idle baseline, tick-advances
     on active, stops on inactive, and dispose-kills-interval assertions with the
     catch-dispose rethrow guard) after the footer-status test.

## E/C gate

`count-ec-auditor.py`: E=39, C=16, need=6 → PASS. CJK comments carry the
freeze mechanism (no reactive dependency during steady recording), the
QuestionPrompt/DialogPrompt/main-Prompt three-consumer scope, the
zero-coverage-before rationale, the idle-baseline microtask subtlety, and the
dispose interval-cleanup contract.

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix384.diff` (7,586 bytes, 3 files, all modified)
- Current patch: `.temp/patches/current/0384-1841858d35f3.patch`
- Materialized state: `.temp/patches/states/0384-1841858d35f3`

## Materialize

`--materialize 384` → `passed 384/384`, state `0384-1841858d35f3` published
(sequential chain 0381→0384, out-of-chain parent entries for 0383/0384 handled by
index order). Scratch tree verified identical to state-0383's index before edits.

## Verification

- `--typecheck 384` (mid-batch) and batch-end `--typecheck 385`: `packages/tui`
  PASS both runs (imports, `createRefreshClock` signature, three-arg
  `voiceInputStatusText` all typecheck against 0.4.3 types).
- Runtime in the synced test workspace (Windows, real solid-js):
  `bun test test/prompt/prompt-voice-input.test.ts` → **21 pass / 0 fail**
  (16 pre-existing + 5 new: the createRefreshClock clock test included),
  57 expect calls.
- The fix is present in materialized state 0384: grepping state 0384's
  `question.tsx` / `dialog-prompt.tsx` shows both `createRefreshClock` consts and
  both `now()` third arguments.

## Notes / deviations from source

- No behavioral deviation; only import-path depth changed for the TUI migration.
- Source comment text kept verbatim (mechanism explanation remains accurate for
  the target: prompt/index.tsx is still the main Prompt consumer).
- The 0.4.3-era `description` function shape in dialog-prompt.tsx coexists
  untouched — this patch's hunks do not intersect it.
## R13 闭合（批次 381-385）
- 生产语义 SATISFIED：目标 voice 实际 owner 是主 Prompt（component/prompt/index.tsx L381-386 已含 createRefreshClock now + voiceInputStatusText(…, now()) 完整修复，且 now 语义更强：isRunning() || recording）；question/dialog-prompt 在目标架构中无 voice UI（0372 目标侧为精简 imports），无调用点即无冻结问题 → 生产 sections 不迁移。
- 测试 section 重建：prompt-voice-input.test.ts 补 solid-js createRoot/createSignal + createRefreshClock（目标路径 ../../src/util/signal）imports，createRefreshClock 契约测试（tick/stop/dispose 清理）插入到 transcribing 测试闭合 }) 之后。
- 修订 2 次：首次插入点在 expect 行后导致 test() 嵌套（Cannot call test() inside a test，1 fail）→ 锚改为 transcribing 测试闭合 }) 之后；脚本改为幂等（plusLines 从 original 提取）+ index 定位闭合括号。
- final：3035B sha256=7b5c0f1e3fc8cb9b17d327694ee5e2e52aa23d0b2a9975a1556badf92f95ecc1；E=50 C=13 阈值 8 通过
- 验证：typecheck 385 全 workspace passed；bun test prompt-voice-input 18 pass / 0 fail（含新契约测试）

## R15 修复（2026-09-13 重审 B-01 闭合）
- B-01：R14 曾错误判定"question/dialog-prompt 无 voice UI 调用点"而丢弃生产 hunks——实为 0372 R14 已重建完整 voice 接线但未加 createRefreshClock。修复：两组件补 import createRefreshClock + const now = createRefreshClock(() => voiceInputStatus().type === "recording") + oiceInputStatusText(..., now()) 三参调用。计时器冻结缺陷消除
- 修改载体：tw 直接修改（0384 patch 为 test-only 形态不变）；生产修复与 0372 R14 的 voice 基础设施在 tw 合并生效
- 验证：voice 测试 20 pass / 0 fail（含 3 个 controller 回归测试）

## R23 生产修复入 patch（2026-09-13 13:09)
- 原 current 仅含测试 hunk；createRefreshClock 生产改动此前只在 tw。现已生成完整生产 section 并插入 patch 头部：
  question.tsx（3 hunk）：Spinner import 后加 createRefreshClock import；voiceShortcutFallback 后加 clock 定义（中文注释说明 recording 状态每秒重渲染 invariant）；voiceInputStatusText 第三参 now()。
  dialog-prompt.tsx（3 hunk）：同构。
- 修订记录：首版 now() 误置于 JSX 逗号表达式位置（TS18007，typecheck packages/cli 失败），修正为 voiceInputStatusText 第三参数。2 处修正。
- E=6（2 import + 2 clock 定义行含注释 + 2 调用行修改），C=2（每组 clock 定义处中文注释），C>=ceil(6*0.15)=1 ✓
- 验证：materialize 400 passed 400/400；typecheck-0400 30 workspace passed=30 failed=0（报告 typecheck-0400-2026-09-13T05-06-52-735Z）。
