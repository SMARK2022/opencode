# Record: 0291-502202613796

## 基本信息
- index: 0291
- sha12: 502202613796
- subject: feat(shell): 增强ShellTool以确保在返回之前等待最终输出元数据
- source date: Wed, 10 Jun 2026 12:04:18 +0800

## Source 行为重建
原始 patch 修改 2 个文件，6 个 hunk：
1. **shell.ts import**：添加 `Fiber` 到 effect import（`{ Effect, Schema, Stream }` → `{ Effect, Fiber, Schema, Stream }`）
2. **shell.ts forkScoped 赋值**：`yield* Effect.forkScoped(...)` → `const output = yield* Effect.forkScoped(...)`，捕获 stream 消费 fiber 引用
3. **shell.ts Fiber.join**：在 kill 块和 return 之间插入 `yield* Fiber.join(output)`，确保子进程退出后等待 Effect stream 完成缓冲 chunk 处理；4 行中文注释说明 Linux fast-exit 竞态原因
4. **shell.ts 删 sink-close 块**：删除 `[local-smark] shell compression: close sink stream` 内联块和 `durationMs`/`displayOutput` 变量
5. **shell.test.ts streaming 命令**：`sleep 0.1` → `sleep 0.3` + 2 行中文注释
6. **shell.test.ts 新测试**：新增 "waits for final output metadata before returning" 测试，验证延迟 metadata 回调不影响最终输出组装

## 三方比较
- **shell.ts import**：目标为 `{ Effect, Stream }`（无 `Schema`），需适配为 `{ Effect, Fiber, Stream }`
- **shell.ts forkScoped**：目标 body 结构与 source pre-image 一致（`Stream.runForEach(handle.all, (bytes) => onChunk(decoder.write(bytes)))` + `Effect.ensuring`），行号偏移 ~478 行
- **shell.ts Fiber.join**：目标 kill/return 结构一致，行号偏移 ~478 行
- **shell.ts sink-close 块**：目标已使用 `closeSink` finalizer（#215 适配），不存在内联 sink-close 块、`durationMs`、`displayOutput` 变量——目标实现已优于 patch 0291 的删除意图，此 hunk 应跳过
- **shell.test.ts**：目标 `description:` 属性已被 283 移除（TS2353 修复），需从测试中移除；行号偏移 ~384 行

## 适配内容
- **import**：`{ Effect, Stream }` → `{ Effect, Fiber, Stream }`（适配目标无 `Schema`）
- **forkScoped 赋值**：适配目标行号和上下文（#126 注释行）
- **Fiber.join**：适配目标行号；保留 4 行中文注释
- **sink-close 块**：跳过（目标已用 finalizer 替代，更优）
- **shell.test.ts streaming**：移除 `description:` 属性；添加 #291 前缀注释
- **shell.test.ts 新测试**：移除 `description:` 属性；添加 #291 前缀注释

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（5450 bytes）
- 编辑后 SHA-256: dd6aae9cd13ce2464ff4cb41ac34f10fc1335d05ca239dd9acbc13a1fd8ca54d
- 编辑后字节: 4286
- materialize 291: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| shell.ts import | 适配目标 import | Effect, Stream → Effect, Fiber, Stream |
| shell.ts forkScoped 赋值 | 适配行号 + #126 注释上下文 | const output = yield* |
| shell.ts Fiber.join | 适配行号 + #291 注释 | yield* Fiber.join(output) |
| shell.ts sink-close 块 | 删除（跳过） | 目标已用 finalizer 替代 |
| shell.test.ts streaming | 适配行号 + 移除 description | sleep 0.1→0.3 |
| shell.test.ts 新测试 | 适配行号 + 移除 description | waits for final output metadata |

## E/C 计算
- E (有效修改行): ~16
  - shell.ts: import(1) + forkScoped 赋值(1) + Fiber.join(1) + 注释(4) = 7
  - shell.test.ts: streaming 命令修改(1) + 注释(2) + 新测试(~25, 含注释) = 28
  - 减去纯注释行: -4
- C (中文注释行): 8
  - shell.ts: "#291：将 forkScoped 赋值给 output 变量..." (1 行)
  - shell.ts: "#291：子进程 close 只说明 OS 管道已关闭..." (4 行)
  - shell.test.ts: "#291：0.3 秒用于制造可观察的流式输出边界..." (2 行)
  - shell.test.ts: "#291：final 是本用例的最后输出哨兵..." (3 行，但部分是 source 原有注释)
- 满足 E > 0 时 C >= max(1, ceil(16 * 0.15)) = 3，8 >= 3 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)

## 不一致
- shell.ts import：目标无 `Schema`（source 有），适配为 `{ Effect, Fiber, Stream }`
- shell.ts sink-close 块：目标已用 `closeSink` finalizer 替代（#215），不存在 patch 要删除的代码
- shell.test.ts description：283 已移除（TS2353 修复）

## 物化验证
- shell.ts: Fiber import ✓, const output = yield* forkScoped ✓, Fiber.join(output) ✓
- shell.test.ts: sleep 0.3 ✓, 新测试无 description ✓

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 291-295，第一轮）
- 正向审计 (ses_011d96b20): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_011d9572e): 5/5 PASS, 无 B/G (N-01~N-02 非阻塞)
- materialize 295: passed 295/295
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)
- E/C: 291=16/8, 292=60/10, 293=80/12, 294=2/1, 295=5/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#215 closeSink finalizer, #289 normalizePowerShellOutput）保留为基线