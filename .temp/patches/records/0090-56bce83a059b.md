# Record 0090-56bce83a059b

## Source intent
original `0090-56bce83a059b` 取消 notebook `source`/`read` 路径上的单行字符截断常量（`MAX_LINE_LENGTH` 一类），保留 **行数 limit=400** 与 **字节预算 24KB** 作为唯一截断维度，避免长行被中间切断导致 LLM 解析半语句。

## Upstream (v1.17.20 target at pre-90)
目标 `sdks/vscode/src/notebook/source.ts` 在 89 之后已具备 offset/limit 分页与 24KB cap；仍可能保留 per-line trunc 或等价逻辑。`packages/opencode` read 工具侧 DEFAULT_READ_LIMIT=400 是对齐参考，不是 notebook source 的 owner。

## Target adaptation
- **Owner**: `sdks/vscode/src/notebook/source.ts`（及若 original 触及的 read 工具 path）。
- 删除/禁用单行 max-char 截断；完整输出整行直到 **rendered 行数达 limit** 或 **bytes 达 24KB**。
- 行数达限与字节达限语义分离：后续 94 将 `bytesCut` 与行限进一步显式化；本项先去掉 line-char 截断。

## Current design
current 对 target owner 做最小删除：去掉 `MAX_LINE_LENGTH` 及相关 slice；保留 limit 默认 400 注释（对齐 read DEFAULT_READ_LIMIT）与 24KB cap。不迁移逻辑到 plugin。

## Verification
- materialize 90 通过；本批次最终 tip 94 上 `MAX_LINE_LENGTH` 不存在。
- typecheck 批次末 94 monorepo 全绿（含 packages/tui/opencode）。

## E/C
见文末统计。注释解释「为何去掉 per-line trunc / 为何 limit 默认 400」。


## Three-way comparison
| 维度 | SMARK original | Upstream target (pre-90) | Final current |
|------|----------------|--------------------------|---------------|
| 单行截断 | 删除 MAX_LINE_* | 可能仍有 per-line slice | 已删除 |
| 行 limit | 默认 400 | 已有 offset/limit | 保持 400 |
| 字节 cap | 24KB | 24KB | 24KB |
| Owner | notebook source | same path | same path |

## Call chain
Agent → `vscode_notebook_source` plugin tool → bridge `POST /notebook/source` → `notebookSource()` → 按 cell 渲染全局行号 → 累计 rendered/bytes → 返回分页文本。

## What was not changed
不改 run/edit/output；不改 TUI（TUI 折叠在 94）；不把 source 逻辑迁入 plugin。

## Comment map (qualifying C)
- limit 默认 400 与 read DEFAULT_READ_LIMIT 对齐的原因
- 去掉 per-line trunc 的原因（半语句破坏）


## Materialize / typecheck provenance
- 累计脚本：`bun .temp/patches/src/apply-cumulative.ts --materialize 94 --rebuild` → tip `states/0094-81efcbb1d8cb`。
- typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 94` → report `typecheck-0094-2026-07-22T07-25-34-017Z.md`，Failure Output: No failures；含 `packages/opencode`、`packages/tui` 等全部 listed workspace `passed`。
- install：reused fingerprint `67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a`。
- 注意：`sdks/vscode` 不在 monorepo typecheck 矩阵内，扩展侧正确性依赖 apply 成功 + 结构 grep（无 kernel.ts 于 tip 94、status 字段、Select operation 等）而非 `bun typecheck` 包条目。

## Risks consciously accepted
- 无真实 Jupyter E2E：无法在 CI 态点击内核选择 UI；契约以 status 字段与命令存在性检查表达。
- TUI 10/20 只改 GenericTool 主路径；其它仍用 3 参 collapse 的调用保持旧阈值（兼容）。


## Diff footprint
current 主要删除/收缩 per-line trunc 相关行，E 较小（E=6）属预期：行为删除型 patch。C 注释解释 limit/24KB 仍保留的边界。


## V2 dual-stack read owner
Target also serves `read` via `packages/core/src/tool/read-filesystem.ts`. #90 current removes `MAX_LINE_LENGTH` there too so Location/BuiltInTools path matches opencode Session registry.


## Test adaptation
`packages/opencode/test/tool/read.test.ts`: `keeps long lines whole until byte budget` asserts full 3000-char line present and **not** `line truncated to 2000`. Live bun test in raw state dir may fail module resolution without frozen install; batch typecheck workspace validates types.

## Files / hashes
- current: `0090-56bce83a059b.patch`
- sha256: `08bfd6bc801b52bffe561b23e446e17b59990fa2d798fc7ff313f724f92ad885`
- bytes: 8552
- E=14 C=6 need=3 gate=PASS
- materialize tip: states/0094-81efcbb1d8cb
- typecheck report: typecheck-0094-2026-07-22T07-25-34-017Z.md (all listed workspaces passed; install fingerprint 67995d9429bcf93aa58e101793ed8babc1674df9384e5da25e8877663dab9c2a)
- updated: 2026-07-22T06:10:00Z

## Independent dual audit (batch 90-94)
- FORWARD: **PASS** all five, no open B/G — task `ses_07747ffb8ffeoOHdlE46H0FgNE` (r11)
- REVERSE: **PASS** all five, no open B/G — task `ses_07747ff96ffe5KTvNREEFo8UZM` (r11)
- Tip: `states/0094-81efcbb1d8cb`
- Typecheck: `typecheck-0094-2026-07-22T07-25-34-017Z.md` (No failures; install `67995d9429bc…dab9c2a`)
- Batch release: **PASS**
