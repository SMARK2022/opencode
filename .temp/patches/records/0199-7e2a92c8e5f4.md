# Patch #199 — feat(permission): wire auto review into permission flow

- **Hash**: 7e2a92c8e5f4
- **Original**: `.temp/patches/original/0199-7e2a92c8e5f4.patch`
- **Adapted**: `.temp/patches/current/0199-7e2a92c8e5f4.patch`
- **Base state**: `.temp/patches/states/0198-09fcecbcdcc7/repo` (tip@198)
- **E/C counts**: E=582, C=93, ceil(E*0.15)=88, C >= ceil(E*0.15) → PASS
  - E=582 为 effective modified lines（非空、非 import 的 added lines），含代码行与注释行
  - C=93 为合格 #199 中文解释性注释行数，分布在 index.ts/test/schema/core/app-runtime 5 个文件的关键决策点
  - 修订：移除 15 行 `注释——` 标签英文注释和 64 行 `测试——` 测试名复述注释，替换为真实决策点中文注释
- **git apply --check**: PASS

## Summary

Patch 199 wires the auto-review pipeline (created by patches 196-198) into the
permission ask flow. When a permission rule evaluates to `auto`, the permission
service now runs `PermissionAuto.evaluate` which delegates to the deterministic
precheck, then the optional reviewer, before falling back to user approval.

The original patch targeted the old architecture (Bus, Layer.mergeAll, local
Action/Event/Error types). The adaptation remaps everything to the target's
LayerNode + AppNodeBuilderV1 + EventV2Bridge + PermissionV1 namespace.

## Files Changed (8)

1. `packages/schema/src/v1/permission.ts` — Add ReviewCompleted, ReviewFailed,
   CircuitBroken event definitions to the Event object and Definitions inventory.
   Uses `define()` from `../event` instead of `BusEvent.define`.

2. `packages/core/src/v1/permission.ts` — Add `AutoDeniedError` class with a
   non-bypassable message. Extend the `Error` union to include `AutoDeniedError`.

3. `packages/opencode/src/permission/reviewer/service.ts` — Import LayerNode,
   add `export const node` with deps `[Config.node, Provider.node, Session.node]`.
   The raw `layer` is used (not `defaultLayer`) per node convention.

4. `packages/opencode/src/permission/cache/session-cache.ts` — Import LayerNode,
   add `export const node` with empty deps (InstanceState is scope-based, not a
   node dependency).

5. `packages/opencode/src/permission/reviewer/circuit-breaker.ts` — Import
   LayerNode, add `export const node` with empty deps.

6. `packages/opencode/src/effect/app-runtime.ts` — Import PermissionReviewer,
   add `PermissionReviewer.node` to the LayerNode.group before `Permission.node`.
   The builder topologically sorts by deps, so array order is for readability.

7. `packages/opencode/src/permission/index.ts` — The main wiring change:
   - Add imports: Config, PermissionAuto, PermissionReviewer,
     PermissionSessionCache, PermissionCircuitBreaker, Option
   - Add `auto` branch in the evaluate loop (needsAuto, autoPatterns)
   - Insert `if (needsAuto) { ... }` block before `if (!needsAsk) return`
   - Translate `bus.publish` → `events.publish` for ReviewCompleted,
     ReviewFailed, CircuitBroken events
   - Translate `AutoDeniedError` → `PermissionV1.AutoDeniedError`
   - Add `fromConfig` filtering for `approvals_reviewer` / `auto_review` keys
   - Add `isAction` / `isPermissionObject` type guards
   - Add `expand` comment about home-relative pattern locality
   - Update `Permission.node` deps to include `PermissionSessionCache.node`
     and `PermissionCircuitBreaker.node`

8. `packages/opencode/test/permission/next.test.ts` — Rewrite test envs using
   AppNodeBuilder/LayerNode.group style:
   - `reviewedEnv`: AppNodeBuilder.build with Permission.node +
     PermissionReviewer.node (replaced with deterministic stub) +
     PermissionCircuitBreaker.node (replaced with stub). Real
     PermissionSessionCache.node is built from Permission.node deps.
   - `circuitEnv`: AppNodeBuilder.build with PermissionCircuitBreaker.node +
     Config.node for threshold-from-config testing.
   - Add `reviewed` and `circuit` test runners via `testEffect(...)`.
   - Add unit tests: fromConfig auto, fromConfig ignores control fields,
     merge auto, evaluate auto, disabled auto.
   - Add it.instance tests: auto resolves safe commands, auto falls back for
     opaque commands, auto denies dangerous commands, auto denies hidden
     payloads, auto denies critical wrappers, auto falls back for credential
     reads, auto fail-closes mixed requests.
   - Add reviewed.instance tests: reviewer allow doesn't bypass ask, cache
     stores only auto patterns, cache for same request, cache key includes
     command context, cache key includes edit diff, non-denial recording.
   - Add circuit.instance test: configured denial thresholds.
   - Adapt `Permission.AutoDeniedError` → `PermissionV1.AutoDeniedError`.
   - Adapt `Permission.Ruleset` → `PermissionV1.Ruleset`.

## Key Adaptation Decisions

### Bus → EventV2Bridge

The original used `bus.publish(Event.ReviewCompleted, { ... })`. The target
uses `events.publish(Event.ReviewCompleted, { ... })` where `events` is the
`EventV2Bridge.Service` yielded in the layer. The Event definitions are created
with `define()` from the schema event module, not `BusEvent.define`.

### Layer.mergeAll → LayerNode.group + AppNodeBuilderV1

The original `defaultLayer` used `Layer.provide(Bus.layer)` plus
`Layer.provide(PermissionSessionCache.defaultLayer)` etc. The target declares
deps in `LayerNode.make({ ..., deps: [...] })` and the builder resolves them
automatically. No manual `Layer.provide` needed.

### Error classes in core, not opencode

`AutoDeniedError` lives in `packages/core/src/v1/permission.ts` alongside
`DeniedError`, `RejectedError`, etc. It is accessed as `PermissionV1.AutoDeniedError`
by consumers. The opencode permission index never re-exports error classes
directly — it only exports `Event = PermissionV1.Event`.

### Reviewer node deps

`PermissionReviewer.node` declares deps `[Config.node, Provider.node, Session.node]`
because the reviewer layer yields all three services. Session does NOT import
Permission in the target architecture (verified via grep), so there is no module
initialization cycle. The original patch's cycle concern was specific to the old
architecture where Session imported Permission.

### Permission.node deps

`Permission.node` now declares deps `[EventV2Bridge.node, PermissionSessionCache.node,
PermissionCircuitBreaker.node]`. The reviewer is NOT a dep because it is accessed
via `Effect.serviceOption(PermissionReviewer.Service)` — it is an optional
collaborator that may be absent in smaller runtimes and unit tests.

### Test env replacements

The `reviewedEnv` includes `PermissionReviewer.node` in the group (replaced with
a deterministic stub Layer) so that `Effect.serviceOption` returns `Some`. The
replacement mechanism in `LayerNode.compile` creates a new node with the stub
layer and empty deps, so Config/Provider/Session are not required. The
`PermissionCircuitBreaker.node` is also replaced with a stub for deterministic
non-denial counting. `PermissionSessionCache.node` uses its real layer (built
from Permission.node's deps).

### fromConfig filtering

The `approvals_reviewer` and `auto_review` keys in the permission config are
control fields for the auto pipeline, not permission names. `fromConfig` now
skips them explicitly. The `isAction` and `isPermissionObject` guards ensure
only valid permission actions are emitted as rules, preventing config metadata
from leaking into the evaluator.

## Verification

- `git apply --check` against tip@198 state: PASS
- All 5 changed files produce a clean unified diff
- 93 qualified Chinese #199 comments at key decision points (strict counting)
- 0 `注释——` tagged English comments, 0 `测试——` test-name-repeat comments
- E=582 (effective modified lines), C=93, gate C >= ceil(E*0.15)=88 → PASS (margin 5)

## Current
- SHA-256: `fe2418a678cf94484036528d0e57c857bf51946e7493443003584ab9045ae512`
- Bytes: 35444
- Lines: 796

## DUAL-AUDIT SEAL

- Sealed: 2026-07-26T23:58:47Z
- FORWARD: BATCH PASS (ses_fwd_196_200_v3) - no open B/G
- REVERSE: BATCH PASS (ses_rev_200_196_v3) - no open B/G
- Tip: states/0200-d4f24d4434a4
- Typecheck: typecheck-0200-2026-07-26T23-48-04-286Z.md
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Non-blocking N only (do not reopen seal)
