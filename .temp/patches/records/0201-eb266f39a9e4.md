# Patch 0201 — feat(permission): 增加 Auto 智能体

## 1. Identity
- Index: 201
- Source SHA: eb266f39a9e4770b27acce94a219024110dd734b
- Parent SHA: d4f24d4434a46b93a51a10830c470fbb9832a346 (patch 200)
- Subject: feat(permission): 增加 Auto 智能体以应用 auto 权限路由
- Original patch: `.temp/patches/original/0201-eb266f39a9e4.patch`
- Current patch: `.temp/patches/current/0201-eb266f39a9e4.patch`
- Pinned source tip: `.temp/patches/states/0200-d4f24d4434a4/repo`
- Previous materialized state: `0200-d4f24d4434a4`
- Current materialized state: `0201-eb266f39a9e4`
- Record path: `.temp/patches/records/0201-eb266f39a9e4.md`
- Record created before first current edit: yes
- Record is independent for this commit and is not merged with another index: yes

## 2. Source Diff Evidence
- Exact command: `git diff` against tip@200 state repo (worktree sharing tip@200 object DB)
- Changed paths (2 files):
  1. `packages/opencode/src/agent/agent.ts` — trim remeda import, EXPLICIT_ONLY_PRIMARY_AGENT_NAMES, Auto agent def, list() localeCompare sort, defaultInfo EXPLICIT_ONLY filter
  2. `packages/opencode/test/agent/agent.test.ts` — Auto list assertion, interactive regression test, Auto behavior test, Auto default_agent selection test, renamed "all primary disabled" test
- Behavior intent:
  - Adds a visible native primary `Auto` agent that reuses build's permission shape but sets `bash: "auto"`, routing shell commands through the deterministic precheck / reviewer / user fallback chain.
  - `EXPLICIT_ONLY_PRIMARY_AGENT_NAMES = new Set(["Auto"])` keeps Auto out of the implicit default-agent fallback so that disabling other primary agents does not silently enable auto shell routing.
  - `list()` refactored from remeda `pipe`/`values`/`sortBy` to `Object.values(...).toSorted` with `localeCompare`, dropping the remeda `pipe/sortBy/values` imports.
  - `defaultInfo` filters out EXPLICIT_ONLY agents so the no-config fallback stays the historical default (build), not Auto.
  - Tests assert Auto is listed, interactive keeps ask gates, Auto is build-like + bash:auto with inherited safety boundaries, and Auto is selectable via `default_agent`.

## 3. SMARK Intent
- User-visible behavior: A new `Auto` agent is selectable via `default_agent`. It behaves like `build` except shell commands enter the auto permission review chain. Without explicit `default_agent`, the fallback remains `build`/`interactive`/`decide` — Auto never becomes the implicit default.
- Protected invariant: Auto must be explicit-only. Disabling all standard primary agents must NOT auto-select Auto (would silently enable auto shell routing). The renamed test "defaultAgent throws when only explicit Auto primary agent remains" locks this.
- Normal path: `list()` returns Auto alongside other native agents; `defaultInfo()` skips Auto for the no-config fallback.
- Boundary/error paths: If all non-Auto primaries are disabled and no `default_agent` is set, `defaultInfo` throws "no primary visible agent found" because Auto is EXPLICIT_ONLY.

## 4. Upstream Reconstruction
- Same-path target implementation: Both paths exist at tip@200.
- The original patch context assumes the pre-#197/#198 English-comment state. tip@200 has `#197`/`#198`/`#200` Chinese comments and an `interactive` agent (with ask gates) and `decide` agent between build and the insertion points.
- Adaptation: `[local-smark]` markers converted to `#201` Chinese comments. remeda import trimmed (`mergeDeep` only). Auto agent inserted after `interactive` (before `decide`), not before a non-existent scout. `repo_clone`/`repo_overview` assertions set to `"allow"` (tip@200 build defaults are `"*": "allow"` with no deny rules for these names; the original patch's `"deny"` reflected a different base).

## 5. Three-Way Mapping

| Source behavior | Upstream (tip@200) | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| Trim remeda import | `import { mergeDeep, pipe, sortBy, values } from "remeda"` | agent.ts:19 | Drop pipe/sortBy/values | patch hunk @@ -14 |
| EXPLICIT_ONLY after RESERVED | `#200` RESERVED_AGENT_NAMES block | agent.ts:87 | Insert after RESERVED | patch hunk @@ -79 |
| Auto agent def | interactive agent exists, no scout | agent.ts:186 | Insert after interactive, before decide | patch hunk @@ -163 |
| list() localeCompare | remeda pipe/sortBy/values | agent.ts:384 | Replace with toSorted+localeCompare | patch hunk @@ -384 |
| defaultInfo EXPLICIT_ONLY filter | `Object.values(agents).find(...)` | agent.ts:405 | Replace with Object.entries + EXPLICIT_ONLY check | patch hunk @@ -403 |
| Auto list assertion | "returns default native agents" test | agent.test.ts:51 | Insert after build assertion | patch hunk @@ -52 |
| interactive + Auto behavior tests | after build test | agent.test.ts:76 | Insert before "review agent id" test | patch hunk @@ -75 |
| Auto default_agent selection | before "custom agent mode all" test | agent.test.ts:691 | Insert before custom-agent test | patch hunk @@ -652 |
| Rename "all primary disabled" + comment | test at tip@200 line 772 | agent.test.ts:772 | Rename + insert comment after decide disable | patch hunk @@ -719 |

## 6. Current Patch Edit
- Current patch SHA-256: `241cdaf442aa2d0731d118167b7069dc9138c150948471d87b4310cafaa52692`
- Exact edited hunks: 2 files, 9 hunks (5 in agent.ts, 4 in agent.test.ts)
- Why each edit is required:
  1. remeda import trim — list() no longer uses pipe/sortBy/values
  2. EXPLICIT_ONLY set — the explicit-only invariant for Auto
  3. Auto agent def — the new native primary agent with bash:auto
  4. list() refactor — localeCompare sort replacing remeda
  5. defaultInfo filter — keep Auto out of implicit fallback
  6. Auto list assertion — Auto must be visible
  7. interactive + Auto behavior tests — regression + behavior contract
  8. Auto default_agent selection — explicit selection works
  9. Rename + comment — clarify the test now leaves Auto enabled (explicit-only)
- Source behavior preserved: Auto is build-like + bash:auto; all other permissions inherit build defaults; external_directory boundaries inherited.
- Minimal semantic adaptation: `[local-smark]` → `#201`; repo_clone/repo_overview `deny`→`allow` (tip@200 has no deny rules for these); insertion positions adjusted for interactive/decide agents.
- No semantic, location, or sequence conflict: `git apply --check` PASS against tip@200.
- Materialized state remains clean and provenance-valid: reverse-check PASS against tip@201.

## 7. Verification
- `git apply --check .temp/patches/current/0201-eb266f39a9e4.patch` against tip@200: PASSED
- Cumulative apply (201→202→203) against tip@200: PASSED
- Materialize: tip@201 created as self-contained fresh-git state; reverse-check PASS
- Effective changed lines `E` for this patch only: 92
- Qualifying distributed Chinese comments `C` for this patch only: 21
- Required minimum `max(1, ceil(E * 0.15))`: max(1, ceil(92 * 0.15)) = max(1, 14) = 14
- C (21) ≥ 14 → PASS
- Representative #201 comment locations:
  - agent.ts — EXPLICIT_ONLY_PRIMARY_AGENT_NAMES rationale (explicit-only invariant)
  - agent.ts — Auto agent def (build-like + bash:auto, boundaries not relaxed)
  - agent.ts — list() localeCompare rationale (mixed-case ordering)
  - agent.ts — defaultInfo EXPLICIT_ONLY exclusion (no-config fallback stays historical)
  - agent.test.ts — Auto list/behavior/default_agent/implicit-default regression rationales

## 8. Contract Check
- One commit only: yes (single patch file)
- Full source diff read: yes (224-line original patch read in full)
- Full target implementation read: yes (agent.ts 525 lines, agent.test.ts 786 lines at tip@200)
- No deleted or delayed behavior: yes (all original behavior preserved)
- Per-patch Chinese comment gate passed: yes (C=21 ≥ 14 required)
- Patch, record, tests and target result are materially consistent: yes

## 9. Final Verdict

`PASS` — patch built, `git apply --check` PASS, cumulative apply PASS, E/C gate PASS (E=92, C=21, required=14). Materialized as `states/0201-eb266f39a9e4`.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T01:20:38Z
- FORWARD: BATCH PASS (ses_fwd_201_205_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_205_201_v2) - no open B/G
- Tip: states/0205-5f3d18e85b8a
- Typecheck: typecheck-0205-2026-07-27T00-57-10-676Z.md
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Non-blocking N only (do not reopen seal)
