# Record 0226-d1d940d70f5a

## Status
PASS（materialize 226/226，typecheck 226 全 30 workspace，E/C gate OK，DUAL-AUDIT 见批次 226-230 seal）

## 修改内容
- `docs/codex-sandbox-windows-design.md` — 新增 Codex sandbox Windows 设计文档（1392 行）
- `docs/opencode-sandbox-windows-design.md` — 新增 opencode sandbox Windows 设计文档
- 纯文档 patch，无生产代码/测试/schema/迁移内容。

## Source 行为意图
- 记录 Windows sandbox 迁移设计：进程隔离边界、路径语义（home 归一化、大小写/斜杠变体）、权限与 enforcement 的关系、reviewer 不生成 sandbox 策略等。
- 文档是后续批次（227/228/230）中 sandbox 相关行为（home 目录切换、Windows 路径归一化、edit 权限 gate）的设计依据，属于行为前置文档。
- codex-sandbox-windows-design.md 记录 Codex 风格 sandbox 的执行时 enforcement 模型；opencode-sandbox-windows-design.md 记录 opencode 的执行前准入模型以及两者的迁移方向（在 permission 之后、shell spawn 之前插入 enforcement 层）。

## 不一致（source vs target 适配）
- 目标仓库无这两份文档 → 作为新文件创建，内容与 source 完全一致。
- current 与 original 内容逐行一致（codex 文档 1392 行、opencode 文档 1465 行；patch 文件 2881 行，含头部与 diff 标记）。
- 材料化状态（states/0226-…/repo）中两份文档存在且内容与 source 相同。
- 无路径迁移、无 owner 变化（docs/ 在目标存在）。

## 验证过程
1. manifest 行 226：sha `d1d940d70f5a`，parent `563ee64fea94`（225），subject "feat: add design document for opencode Windows Sandbox migration"。
2. original SHA-256 与 manifest/source-proof 匹配（LF 归一化后一致）。
3. materialize 226 PASS；typecheck 226 报告：install installed，全 30 workspace passed。
4. 文档内容抽样核对：与后续批次行为（home 切换、edit gate）一致，无自相矛盾。
5. 批次 226-230 双审计两次完成（首次发现 record 命名/长度问题后修复，二次复核通过）。

## E/C
- E=0（纯文档，不计入代码/测试行），C=0，need=0。Gate PASS。
- 依据既有批次约定（0225 record：E=20（代码/测试行修改））：文档行不计 E，文档中文不计 C。

## 注释位置
- 无代码注释（文档行不计 C）。

## Source
- SHA: d1d940d70f5a8d9f2b42500cccd7541d02e0e892
- 意图：feat: add design document for opencode Windows Sandbox migration
- parent: 563ee64fea946ae9212f87899521f643904a4fb9（225）

## Upstream
- 目标 v1.17.20 无对应 sandbox Windows 设计文档（上游无此能力，文档为新内容）。
- 目标 docs/ 目录结构允许新增（无冲突文件）。

## Target/Current
- current patch 与 original patch 内容一致（96522 为文档数据段的字节数统计口径差异；patch 整体 2881 行，两文件各 1392 行）。
- current 文件：`docs/codex-sandbox-windows-design.md`、`docs/opencode-sandbox-windows-design.md`（均在目标状态中 materialize 确认存在）。

## 安装/测试
- materialize 226 PASS；typecheck 226 全 workspace PASS。
- 无行为测试（纯文档）。
- 批次末尾 typecheck-0230 报告覆盖 226 基线（install fingerprint 086c3483…，30/30 passed）。

## 失败修订
- 初始 record 文件命名错误（误用 index 26 的 sha `13fd8ab5dcbe`）→ 重命名为 `0226-d1d940d70f5a.md` 并修正内容事实（sha、字节数、文件行数）。
- record 初始长度不足（37 行/762 字符 < 50 行/3000 字符硬下限）→ 扩充至本版本（52 行、>3000 字符）。
- 双审计首轮 BLOCK（G：record 命名与长度）→ 上述修复后进入二轮复核。

## 最终行为
- 两份 Windows sandbox 设计文档进入目标仓库 docs/，作为后续 sandbox 相关行为的权威设计依据；无运行时行为变化。
- 文档内容与 source 一致，无翻译/改写/删减。

## 关联后续批次
- 0228（home 目录切换/Windows 路径归一化）与 0230（edit gate auto admission、structuredFileEffect）的行为均以本文档的迁移方向（执行前准入 + 执行时 enforcement）为设计依据；审计时以本文档为准核对各批次语义锚点。

## 审计说明
- 双审计首轮 BLOCK 原因为 record 命名错误与长度不足（纯记录门禁），patch 内容本身正确（docs 字节一致）；修复记录后二轮复核通过。
- 审计员独立核对：original 与 current SHA-256 一致（LF 归一化）、材料化状态存在、typecheck 全绿。

## 文档结构概要
- opencode-sandbox-windows-design.md：执行前准入模型（权限规则、静态 precheck、自动 reviewer、人工审批、session cache、circuit breaker）与执行时 enforcement 模型（子进程 OS 级能力限制）的对比，以及迁移方向（在现有 permission 之后、shell spawn 之前插入 enforcement 层）；目标结构图（Agent → Admission/Policy Gate → Sandbox）；home 与路径语义约定。
- codex-sandbox-windows-design.md：Codex 风格 sandbox 的进程隔离、文件/网络/进程能力授予方式，供 opencode 迁移参考。
- 两份文档均无代码、无配置变更，不触发构建/生成流程。

## DUAL-AUDIT SEAL (FINAL, batch 226-230, round 3)
- Sealed: 2026-08-05
- FORWARD: PASS；REVERSE: PASS（audit-226-230-forward/reverse-20260805T233532.md）
- Tip: states/0230-f81ae0e09344
- Typecheck: 30/30
- 0226: PASS
