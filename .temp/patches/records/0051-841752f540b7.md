# Patch Record 0051

## 1. Identity
- Index: 51 · SHA12 `841752f540b7`
- Current SHA-256: `11c8b5268f24576e14b8a4b15590382a4f7898bab05e0d8095336213bd92152f` · bytes 5397
- State: `states/0051-841752f540b7` · cumul=None status=None builtAt=None
- Baseline 4473fc3c… · tip d0ceb469…

## 2. Upstream design
Claude Code provider wrapper (`packages/opencode/src/provider/claudecode.ts`) finalizes request bodies and may rewrite `cch=00000` via `patchCch` (xxHash of body). That makes the earliest system prefix change every turn, defeating stable-prefix caching.

`SystemPrompt.environment` in `packages/opencode/src/session/system.ts` assembles prompt parts. If volatile env/model identity is placed before static tool guidance, the prefix hash churns with cwd/date/git/shell.

## 3. SMARK design
1. Stop calling `patchCch` on the finalize path so `cch` stays the placeholder.
2. Move environment/model block to the end of `environment()` parts so static tool usage + guidance stay prefix-stable.

## 4. This adaptation
- `finalBodyStr = JSON.stringify(patchedBody)` only (no patchCch call). Helpers may remain defined (dead) matching source style.
- V2 model+env is one joined part; move the entire part after toolUsage/static sections.
- Chinese comments at cch intent and env-at-end.

## 5. Why not delete patchCch helper
Source disabled the call without deleting the helper; deletion is cleanup for a later chore, not required for the billing-header invariant.

## 6. Metrics
E=16 C=4 required=3 gate=PASS

## 7. Verification
- materialize 51; covered by cumulative typecheck through later batch ends
- No dedicated unit test for cch/order (source shipped none)

## 8. Verdict
`PASS` — dual independent auditors FORWARD+REVERSE batch 50-54, no open B/G.

## 9. Dual audit closure
- Batch 50-54 FORWARD: PASS
- Batch 50-54 REVERSE: PASS

## 10. Call chain
1. Provider finalizes Claude Code request body → previously `patchCch` rewrote `cch` from body hash.
2. System prompt assembly builds an ordered list of parts; earliest static parts become the cacheable prefix.
3. Volatile env (cwd/date/git/shell/model) must not sit in that prefix or every turn busts cache.
4. After this patch: `cch` stays placeholder; env/model part is last in `environment()`.

## 11. Why env-at-end is stronger than "after toolUsage" only
V2 can have multiple static guidance segments after toolUsage. Moving the entire model+env joined part to the absolute end of the parts list maximizes the stable prefix under V2's real layout, which is a strict improvement over only swapping with toolUsage when more static parts exist.

## 12. Residuals
Dead `patchCch` helper may remain until a later cleanup. No unit test asserts cch placeholder or part order in this source commit.
## 13. Owner files
- `packages/opencode/src/provider/claudecode.ts` — finalize path stops hash-rewriting `cch`.
- `packages/opencode/src/session/system.ts` — environment/model part order moved to end of parts list.
## 14. Provenance note
This record was rewritten after reverse audit G-01 (padding/incomplete provenance). Content is three-way design only; no filler.
