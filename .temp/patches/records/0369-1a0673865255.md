# Record 0369-1a0673865255

## Identity
- manifest parent: 8fba91e68d89 (0368)
- subject: fix(test): 修复 CI 全平台批量失败：SDK goal 方法测试 + HttpApi gate
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0369-1a0673865255` (passed 369/369)
- current patch: `.temp/patches/current/0369-1a0673865255.patch` (5488 bytes)

## Source intent
删除两个假设 SDK 不含 goal 方法的回归测试。CI 中 turbo test:ci 依赖
^build，SDK build 会重新生成 gen 文件（包含 goal 端点方法），导致
toBeUndefined 断言收到 [Function: goal] 必然失败。合并重复的 config
验证测试为单个。补充 POST /session/preview httpapi-exercise scenario，
解决 0366 引入端点缺少 scenario 导致 --fail-on-missing 报 missing=1
的问题。

## Source patch anatomy (2 files)
1. `test/server/httpapi-exercise/index.ts` — 在 goal clear scenario 与
   append-prompt 之间插入 preview scenario（空 sessionIDs → 空对象）。
2. `test/session/goal.test.ts` — config 测试注释更新 + 断言行内联合并；
   删除 "SDK Session2 class does not have goal methods" 与
   "dialog-goal.tsx uses fetch not SDK method" 两个 it.live 测试。

## Target mapping (state 0368)
| Source file | Target file | Status |
|---|---|---|
| `test/server/httpapi-exercise/index.ts` | same | ADAPTED (EXISTS; 0368 已插 goal scenario，preview 插其后) |
| `test/session/goal.test.ts` | same | ADAPTED (EXISTS; 删除 0368 引入的两个 it.live) |

## Target-adaptation decisions
1. **scenario 插入位置**：目标文件中 0368 的 goal scenarios 位于
   `.delete("/session/{sessionID}/goal", ...)` (L1706-1713) 与
   `.post("/tui/append-prompt", ...)` (L1715) 之间；preview scenario 插在
   goal clear scenario 之后，与 source 相对位置一致。`route("/session/
   preview", {})` 无模板参数，body 传 `{ sessionIDs: [], limit: 2 }`。
   该 scenario 依赖 0366 注册的 POST /session/preview 端点（workspace-
   routing 例外 + PreviewPayload），覆盖率检查由此闭合 本批次端点（goal×3 + preview）覆盖率闭合；request_usage* 3 个 MISS 为 0269 既有适配遗留（source 0269 scenarios 未迁移，state 0366 已缺失），非本批引入。
   `.json(200, ...)` 断言 `Object.keys(body).length === 0`，与 handler 的
   空数组短路 return {} 契约一致。
2. **config 测试合并**：0367 引入的
   "goal_max_turns is readable from top-level config without experimental
   gate" 用例保留；`const maxTurns = ...; expect(maxTurns).toBe(10)` 合并
   为 `expect((cfg as any).goal_max_turns ?? 10).toBe(10)` 行内断言；
   注释补充 CI 重新生成 SDK 的背景说明（config 字段位置不受 build 影响）。
   目标中该用例使用 TestConfig 桩（0367 决策），合并后断言仍成立。
3. **删除两个 it.live 回归测试**：与 source 完全一致地删除。0368 中的
   dialog-goal 文件路径适配（../../tui/...）随之消失，无残留引用；
   `import("@opencode-ai/sdk/v2")` 的动态导入仅在被删测试中使用，无孤立
   import 问题（均为局部动态 import）。

## E/C audit
E=13, C=6, need=2, PASS. (C：preview scenario 的注释 3 条 + config
测试注释更新 2 条 + 行内断言背景注释 1 条；净删除行不参与计数。)

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 369` →
  "passed 369/369"，state at `states/0369-1a0673865255`。
- Report: `materialize-0369-2026-08-24T21-20-31-868Z.json` — reusedPrefix
  368, baseState `0368-8fba91e68d89`, stoppedAt 空, integrityFailure 空。
- Post-apply verification: preview scenario 位于 goal clear 之后；
  goal.test.ts 中不再含 "SDK Session2 class" 与 "uses fetch not SDK" 两个
  it.live；config 用例为行内断言单行。

## Line accounting
- httpapi-exercise/index.ts: +17。
- goal.test.ts: -42/+3（两个回归测试整体删除，config 用例注释增强）。

## Risks / notes
- 删除 SDK 方法回归测试后，dialog-goal.tsx 使用 fetch 直调的事实不再有
  测试守卫；改为依赖 httpapi-exercise 的 goal scenario 端到端验证端点
  契约（本质更稳：验证 HTTP 行为而非文件内容）。
- goal.test.ts 的 `it.live` 从文件消失后无类型引用残留；TestConfig 桩与
  Config.node 组覆盖保留（仍被 config 用例使用）。
- 若未来 SDK 重新生成包含 goal 方法，可考虑将 dialog-goal/sync 切回
  sdk.client.session.goal* 调用并删除 useGoalApi；0369 的 CI 注释已
  记录该路径。

## Per-item diff stats
- fix369.diff bytes: 4559 (raw diff, LF); patch file with header: 5488。
- 2 文件（全部修改）。