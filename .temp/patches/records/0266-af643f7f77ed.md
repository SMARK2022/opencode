# Record 0266-af643f7f77ed — fix(actions): CI workflow Windows runner fix

## Manifest
- index: 266, parent: 0265-751a4815335c, sha: af643f7f77edccba091557c03cc7fe32dbfffe42
- original: original/0266-af643f7f77ed.patch（2632B，48 行，1 文件）
- current: current/0266-af643f7f77ed.patch（2632B，零适配，与 original 逐字节一致）

## Source 行为（original 全文 48 行，1 文件）
1. **.github/workflows/test.yml**（3 hunk）：
   - Hunk 1（L69）：job env `OPENCODE_EXPERIMENTAL_DISABLE_FILEWATCHER: ${{ runner.os == 'Windows' && 'true' || 'false' }}` -> `${{ matrix.runner == 'windows-latest' && 'true' || 'false' }}`。GitHub Actions 在 workflow 解析阶段无法使用 `runner.os` context，导致 Actions 直接拒绝运行。`matrix.runner` 在解析阶段可用。
   - Hunk 2（L266）：upstream-warnings job env，同上修正。
   - Hunk 3（L380）：`bun --cwd "${{ matrix.path }}" test` -> `bun test --cwd "${{ matrix.path }}"`。`bun --cwd` 会被 Bun 误解为 `bun` 的 `--cwd` flag 而非 `test` 子命令的参数。正确语法是 `bun test --cwd`。

## 上游 owner 分析
- 目标 v1.17.20 由 263 创建 test.yml（core-required + upstream-warnings 矩阵）
- 266 修正 CI workflow 解析问题：runner context 在 workflow 解析阶段不可用
- matrix.runner 在解析阶段可用，替代 runner.os 避免 Actions 直接拒绝运行
- bun test --cwd 是正确的 Bun 测试命令语法

## 目标适配
- 零适配：current 与 original 逐字节一致（2632B = 2632B）
- test.yml 由 263 创建，context 完全匹配 source 的期望

## Hunk 映射
- 3 hunk 全部零适配（context 完全匹配 source）

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- 无生产代码变更，无需 typecheck
- materialize 266: passed 266/266

## E/C 计算细节
- GitHub Actions YAML 配置不计入 E（非 production/test code）
- E=0, C=0, need=0 PASS

## 修订历程
- v1: original 拷贝，零适配
- final: 与 original 逐字节一致

## 验证（完整）
- materialize 270: passed 270/270（含 266）
- opencode typecheck exit 0
- core typecheck exit 0
- packages/app TS1128 pre-existing（自 0247）

## 不一致性分析
- 无不一致性：source 和目标 test.yml 完全一致
- 263 创建的 test.yml 包含 core-required（opencode/core/http-recorder/tui/sdk/client）和 upstream-warnings（app）矩阵
- 266 的 3 个 hunk 修正了 runner context 和 bun 命令语法问题

## CI workflow 结构详述
### core-required job 矩阵
- opencode Linux/Windows: bun turbo test:ci --filter=opencode
- core Linux/Windows: bun run --cwd packages/core test:ci
- http-recorder Linux/Windows: bun run --cwd packages/http-recorder test:ci
- tui Linux: bun run --cwd packages/tui test:ci
- sdk/js Linux: bun run --cwd packages/sdk/js test:ci
- client Linux: bun run --cwd packages/client test:ci
- 所有 core-required job 必须通过, 否则 CI 阻塞

### upstream-warnings job 矩阵
- app Linux: bun run --cwd packages/app test:ci
- 仅警告, 不阻塞 CI
- packages/app TS1128 pre-existing 在此 job 中报告

### 266 修正详情
- runner.os 在 workflow 解析阶段不可用（GitHub Actions 限制）
- matrix.runner 在解析阶段可用（matrix 在 job 级别定义）
- bun --cwd 被误解为 bun 的 flag 而非 test 子命令参数
- bun test --cwd 是正确语法（--cwd 是 test 子命令的 flag）

## Source-proof 验证
- manifestJsonSha256: 6f162934... 与 state.json 一致
- manifestTsvSha256: 788f6cfd... 与 state.json 一致
- sourceTip: d0ceb469... 一致
- originalSha256: LF 归一化后与 source-proof 匹配
- CRLF 适配: Windows 工作区有 CRLF, LF 归一化后哈希匹配

## DUAL-AUDIT SEAL（批次 266-270）
## DUAL-AUDIT SEAL（批次 266-270，第四轮）
- 正向审计 (ses_013f2f0a1ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013f2cbaeffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 266=0/0, 267=6/2, 268=66/10, 269=60/12, 270=38/6 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
