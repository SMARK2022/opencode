# Patch 0212 — 26bf6a4193b6 — feat(session): 增强会话视口处理，确保流式增长时底部内容可见

## 修改内容

### B-01 修复：contentGrowthKey 跟踪文本长度而非仅 part 数量
- `packages/tui/src/routes/session/index.tsx`：
  - 原 `contentGrowthKey` 仅跟踪 `part.length`（part 数量），text delta 原地修改 part 文本不改数组长度 → 效果不触发
  - 修改为遍历所有 part，累计 text/tool-raw 字符长度 → text delta 改变字符总量 → 效果触发
  - `applyPartDelta` 在 `sync.tsx` 中原地修改 `part[field] = existing + delta`，不改 part 数组长度
  - 修复后：流式 text/raw 增长时 `contentGrowthKey` 变化 → `createEffect(on(contentGrowthKey, ...))` 触发 → 贴底跟随

### 保留（已有）
- `lastMaxScrollTop` 变量跟踪上一次最大滚动位置
- `createEffect(on(contentGrowthKey, ...))` 一行容差贴底跟随逻辑
- `wasStuckToBottom = scroll.y >= lastMaxScrollTop - 1`（一行容差）
- `maxScrollTopIncreased = maxScrollTop > lastMaxScrollTop`（内容增长判定）
- `scroll.scrollTo(maxScrollTop)` 跟随到新底部
- `stickyScroll={true}` + `stickyStart="bottom"` 保留（上游 sticky 基线）

### B-02 修复：新增回归测试
- `packages/tui/test/routes/session/session-viewport-follow.test.ts`（新文件）：
  - `shouldFollowStreamingBottom` 纯函数测试（6 个用例）
  - 覆盖：内容增长+贴底→跟随、一行容差→跟随、上滚超过容差→不跟随、内容未增长→不跟随、已在底部→不跟随
  - `contentGrowthKey tracks text length not just part count`：验证 text delta 不改 part.length 但改字符总量

## 不一致与修复
- B-01（双审 v1）：contentGrowthKey 仅计 part 数量，text delta 不触发 → 改为累计 text/raw 字符长度
- B-02（双审 v1）：源回归测试被删除 → 新增纯函数测试覆盖跟随决策 + contentGrowthKey 信号
- 源 patch 使用 `renderAfter` 回调（post-layout）；tip 使用 `createEffect(on(contentGrowthKey, ...))`（pre-layout）
  - tip 的 Solid reactivity 在 contentGrowthKey 变化后调度 effect，scrollbox 在 effect 运行时已更新 scrollHeight
  - `renderAfter` 在 tip scrollbox 上未作为顶层 prop 使用（仅在 scrollbar trackOptions 中使用）
  - `createEffect` + 文本长度跟踪是 tip 架构下的最小语义适配

## E/C
- E = 52（非注释非空新增代码行）
- C = 19（分散在决策点的 #212 中文解释性注释）
- need = ceil(52 * 0.15) = 8
- gate = PASS（C >= need，余量 11）

## 注释位置
- index.tsx:264-270：contentGrowthKey 改为文本长度跟踪的原因、applyPartDelta 原地修改行为
- index.tsx:1290-1296：一行容差、sticky 仅精确 max、手动上滚不归一化
- session-viewport-follow.test.ts：每个测试用例的 invariant 和测试意图

## Source
- SHA: 26bf6a4193b6
- 意图：流式内容增长时视口贴底跟随，一行容差消除视觉脱节
- 原 patch 修改 2 个文件：session/index.tsx（renderAfter + syncSessionViewportStuckToBottom）+ session-message-render.test.tsx（回归测试）

## 上游
- tip scrollbox 有 `stickyScroll` + `stickyStart="bottom"`——仅从精确 max 跟随，无一行容差
- tip 无 `renderAfter` 在 scrollbox 顶层（仅 scrollbar trackOptions）
- tip 使用 Solid reactivity（createMemo/createEffect）——替代 renderAfter

## 目标
- packages/tui/src/routes/session/index.tsx：contentGrowthKey 修改
- packages/tui/test/routes/session/session-viewport-follow.test.ts：新增测试

## Current
- SHA-256: d0047a7d1a37d982
- Bytes: 6237
- 2 个文件

## 安装
- typecheck-0215 通过：30/30 workspaces passed
- install reused: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f

## 测试
- typecheck: PASS
- session-viewport-follow.test.ts: 6 pass, 0 fail

## 失败修订
- v1 双审 BLOCK（B-01 contentGrowthKey 漏 text delta, B-02 测试删除）
- 修复：contentGrowthKey 改为文本长度跟踪 + 新增纯函数回归测试

## 最终行为
- 流式 text/raw 增长时 contentGrowthKey 变化 → createEffect 触发 → 一行容差贴底跟随
- 用户手动上滚超过一行时不跟随——用户可检查上一行直到新内容到达
- sticky scroll 保持精确 max 跟随（上游基线）
- contentGrowthKey 跟踪所有 part 的 text + tool-raw 字符总量
