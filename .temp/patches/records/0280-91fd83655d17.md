# Record 0280-91fd83655d17

## Patch Identity
- Index: 0280
- SHA12: 91fd83655d17
- Subject: feat(permission): 增强子代理会话权限推导
- Current SHA-256: d62daf2e785d3376f61da572f6e258c774b58f872e8ae1f4cf79f04daa2edc32
- Current bytes: 32203
- Files: subagent-permissions.ts + reviewer/service.ts + task.ts + plan-mode-subagent-bypass.test.ts + reviewer-service.test.ts + task.test.ts

## Source Intent
Source patch 的完整行为目的见 original/0280-91fd83655d17.patch。

## Adaptation Details
- 276: image.test.ts 添加 tokenBudget 测试；read.test.ts 适配 LayerNode 模式（Image.node 代替 Image.defaultLayer），添加有效 JPEG base64 和损坏图片测试
- 277: image.ts 添加 tokenBudget 支持（保留 photon，添加 targetMaxBase64Bytes 辅助函数）；read.ts 收敛图片处理到 Image.Service（替换 processImageWithTokenBudget），添加 Image.Service 到 Tool.define context type；registry.ts 添加 Image.node 到 deps
- 278: opencode.jsonc 适配 references 块上下文（hunk header 从 -2 改为 -14）；session-ses_1837.md 新文件
- 279: context-usage.tsx 添加 contextUsageRefreshKey 函数（轻量刷新信号）；dialog-model.tsx 添加 currentModelKey memo 和字符串 key
- 280: subagent-permissions.ts 重写 deriveSubagentSessionPermission（PermissionV1 类型，parentAgentCeiling/forwardParentSessionRule/childSpecificCeilings/subagentDenyOverrides 辅助函数）；reviewer/service.ts 简化 reviewerEnabled

## E/C Calculation
- E=57 C=13 need=9 PASS

## Verification
- materialize 280: passed 280/280
- opencode typecheck: exit 0
- tui typecheck: exit 0
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: reused


## Source Behavior Analysis
源 patch 增强子代理会话权限推导：
1. subagent-permissions.ts: 重写 deriveSubagentSessionPermission（ceiling 推导 + Wildcard 匹配）
2. reviewer/service.ts: 简化 reviewerEnabled（移除 metadata.agent 判断）
3. prompt.ts: 更新 metadata.agent 注释
4. task.ts: 更新注释
5. 测试文件

## Upstream Comparison
目标使用 PermissionV1（非 Permission）。
目标 deriveSubagentSessionPermission 签名不同（parentAgentPermission 作为独立参数）。
目标 reviewer/service.ts 有 #203/#225 注释前缀。

## Target Adaptation
1. subagent-permissions.ts: 使用 PermissionV1 类型，parentAgentPermission 作为独立参数；添加 parentAgentCeiling/forwardParentSessionRule/childSpecificCeilings/subagentDenyOverrides/evaluateSubagentRule 辅助函数
2. reviewer/service.ts: 简化 reviewerEnabled（移除 auto_review 和 metadata.agent 判断，只检查 user）

## Hunk Mapping
| Source File | Target File | Adaptation |
|---|---|---|
| subagent-permissions.ts | subagent-permissions.ts | PermissionV1 类型 + ceiling 推导（与 source 逐字一致） |
| reviewer/service.ts | reviewer/service.ts | reviewerEnabled 简化（#203/#225） |
| prompt.ts | prompt.ts | DROP（目标已由 #230 处理） |
| task.ts | task.ts | ctx.agent 父权限源 + setPermission 恢复重算 |
| plan-mode-subagent-bypass.test.ts | test/agent/plan-mode-subagent-bypass.test.ts | 7 个用例全部适配（target derive 签名） |
| reviewer-service.test.ts | test/permission/reviewer-service.test.ts | implicitAuto fixture + 测试（ConfigV1.Info 类型） |
| task.test.ts | test/tool/task.test.ts | 3 个用例（primary_tools deny 语义适配） |

## Comment Positions
1. #280：子代理权限推导需 Wildcard 匹配父子规则范围
2. #280：父代理权限作为独立参数，增强子代理会话权限推导
3. #280：父代理 catch-all deny 时，其显式 deny 仅作为 UI 细节
4. #280：父代理和会话的 ask/auto/deny 上限合并
5. #280：父代理规则转为子代理上限
6. #280：catch-all deny 是自执行默认，不转发给委派型子代理
7. #280：父代理 catch-all deny 时，非 edit 的显式 deny 不转变
8. #280：父 allow 不能放宽子代理的 ask/auto/deny
9. #280：父会话规则转发，allow 仅作为共享异常保留
10. #280：子代理窄范围上限推导
11. #280：子代理特定 deny 保持终止性
12. #280：reviewer 只在 Permission.ask 已命中 auto action 后被调用
13. #280：是否进入 auto review 由权限计算决定

## Verification Commands
- Materialize: passed 280/280
- Typecheck: opencode exit 0
- E/C: E=57 C=13 need=9 PASS

## Final Behavior
子代理会话权限推导增强：父代理和会话的 ask/auto/deny 上限合并；allow 仅在子代理已允许时保留；子代理特定 deny 保持终止性。reviewer 启用由权限计算决定而非 agent 名称。

## 第二轮修复（第一轮双审计 BLOCK 反馈）

### B-01: 测试丢弃——部分修复
- test/agent/ 目录确实存在，但源测试用例需要大量适配（PermissionV1 类型、LayerNode 模式）
- 当前聚焦于行为修复（B-02），测试适配留待后续

### B-02: task.ts setPermission 恢复——已修复
- 添加 nextPermission 变量提取
- 恢复 task 时调用 sessions.setPermission 重新计算权限
- nextSession 使用 nextPermission

### E/C 修复后
- E=78 C=12 need=12 PASS

### 验证
- materialize 280: passed 280/280
- opencode typecheck exit 0

## 第五轮修复（批次 276-280 重审 B-01/B-04）

### B-01: 9 个测试用例丢弃——已全量补齐
- plan-mode-subagent-bypass.test.ts 补齐 5 个缺失用例：
  1. "permission meet tightens child-specific allows behind wildcard deny fallbacks"
  2. "permission meet does not replay tool wildcard deny over child-specific tightened allows"
  3. "nested subagents keep auto ceilings inherited from the parent session"
  4. "parent session allow cannot relax child ask or auto decisions"
  5. "parent ask remains a user-approval ceiling over child auto"
- reviewer-service.test.ts 补齐 implicitAuto fixture + "enables reviewer for auto requests without depending on the executing agent name" 测试
- task.test.ts 补齐 3 个用例（records subagent / recomputes resumed / keeps primary tool）
- 适配：deriveSubagentSessionPermission target 签名（{parentSessionPermission, subagent}, parentAgentPermission）；ConfigV1.Info["permission"] 类型；ConfigPermissionV1 → ConfigV1 修正（TS2694/TS2322 修复）

### B-04: record hunk 映射表与 patch 矛盾——已重写
- 原表写 "task.ts → DROP"、"测试文件 → DROP（test/agent/ 不存在）"，但 current patch 明确修改 task.ts 和 test/agent/。已重写为如实映射。

### primary_tools 语义适配
- source 测试期望 primary_tools 是 allow（child.permission bash=auto）；target 的 primary_tools 是 deny（#108 前序语义，task.test.ts:504 明确锁定）。
- 适配：保留 target deny 语义（更强安全边界），测试改断言为 deny 并改名 "keeps primary tool denies at least as strict as parent admission ceilings"，验证 primary_tools deny 不被 parent auto ceiling 覆盖成 allow。

### E/C 修复后
- E≈250 C≈45 need=38 PASS（新增 9 个测试 + 注释）

### 验证
- materialize 280: passed 280/280
- opencode typecheck exit 0, tui typecheck exit 0
- plan-mode 12 pass / reviewer 5 pass / task 23 pass — 40 pass 0 fail

## DUAL-AUDIT SEAL（批次 276-280）
## DUAL-AUDIT SEAL（批次 276-280，第七轮）
- 正向审计 (ses_012cf5d9dffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_012cf42b2ffe): 5/5 PASS, 无 B/G (N-01~N-08 非阻塞)
- materialize 280: passed 280/280
- opencode typecheck exit 0, tui typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 276=38/6, 277=66/10, 278=0/0, 279=219/33, 280=134/41 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `cca274fc5f5bf7392a79a7cd966603fe9fda9fa8e2e167e66b2e40187db7d020` / bytes: 30652
- E/C 最终：E=385, C=59, need=58, PASS
- 修复 B-04：subagent-permissions.ts 模块 doc 头更新为 source 0280 的四点契约（parent agent admission ceilings + session runtime ceilings + allow 仅共享异常），错误旧 doc（'Parent agent restrictions only govern that agent'）删除；权威 hunk 由 state 279 语义应用 + git diff 生成（@@ -1,14 +1,20 @@ + @@ -16,20 +22,98 @@）。
- 修复 G-02：6 处重复注释改写为语义各异（catch-all deny 3 行、父 allow 2 行、子代理 deny 2 行、task.ts 职责 2 行、reviewer 2 行）。
- 验证：state 279 上 git apply --check EXIT 0；materialize 300 passed；typecheck/测试全绿。

## 审计修复记录（R10，B-01 修正）
- B-01（正向审计）：forwardParentSessionRule 使上游测试 "subtask child inherits parent session external_directory allow"（prompt.test.ts）确定性失败——父会话 external_directory allow 在子代理无同边界 allow 时被丢弃（general 子代理默认 external_directory 为 ask）。
- 修复：适配该上游测试到 SMARK 0280 新契约（allow 仅共享异常）——改名 "subtask child inherits parent session external_directory rules tied to child-shared boundaries"，断言父 allow 边界在子代理不允许时降级 ask（not.toEqual arrayContaining allow + evaluate ask），task deny 断言保留；权威 hunk 由 state 0278 语义编辑 + git diff 生成并追加至 current/0280。
- 验证：materialize 280/300 passed；子代理测试 1/1 pass；task 23/23、plan-mode 12/12、reviewer-service 5/5 pass；opencode/tui typecheck exit 0。
- 最终 E/C：E=388, C=61, need=59, PASS
