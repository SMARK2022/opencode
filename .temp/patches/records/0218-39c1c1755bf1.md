# Patch 0218 — fix(package): 修正版本号格式

## 1. Identity
- Index: 218
- Source SHA: 39c1c1755bf14695a402692da8748cbaaad749a9
- Parent SHA: 5aff2e10e3e9 (patch 217)
- Subject: fix(package): 修正版本号格式，确保与发布一致
- Original patch: `.temp/patches/original/0218-39c1c1755bf1.patch`（1683 字节，33 行）
- Current patch: `.temp/patches/current/0218-39c1c1755bf1.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`（v1.17.20 release）
- Built-on state: `0215-9e190548fc7a`（patches 1-215 applied；patches 216-217 不触及 sdks/vscode/package.json）
- Record path: `.temp/patches/records/0218-39c1c1755bf1.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 1 file：
  1. `sdks/vscode/package.json` — 版本号 `"1.15.3.0"` → `"1.15.3"`；vsix 脚本中文件名 `1.15.3.0` → `1.15.3`。
- 每个 hunk 与行为意图：
  - 版本号修正：`1.15.3.0` 不是合法 semver，vsce 打包和 Marketplace 发布需要标准 `MAJOR.MINOR.PATCH` 格式。
  - vsix 脚本同步：输出文件名必须与版本号一致，否则发布脚本找不到 vsix。

## 3. SMARK Intent
- 用户可见行为：vsce 打包生成的 vsix 文件名与 package.json 版本号一致，Marketplace 发布不因版本格式错误失败。
- 受保护不变量：版本号必须是合法 semver `MAJOR.MINOR.PATCH`，不含额外 `.0` 后缀。
- 正常路径：`bun run vsix` → 生成 `SMARK2022.opencode-ide-bridge-1.15.3.vsix`。
- 边界路径：N/A（纯配置修正）。

## 4. Upstream Reconstruction
- 同路径目标实现：`sdks/vscode/package.json` 存在于 tip@215。
- 目标版本号：`"1.17.26"`（合法 semver，修正已应用）。
- 目标 vsix 脚本：`dist/SMARK2022.opencode-ide-bridge-1.17.26.vsix`（文件名与版本号一致）。
- 源 vs 目标差异：源从 `1.15.3.0` 修正为 `1.15.3`；目标已经是 `1.17.26`（正确格式，修正已在历史中完成）。
- Upstream strengths：目标版本号格式已正确，无需修改。
- Behavior that requires a deliberate merge：源修改的版本号在目标中已被后续版本更新覆盖（1.15.3 → 1.17.26），但格式修正的语义意图（不含 .0 后缀）已在目标中满足。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@215) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| 版本号 1.15.3.0 → 1.15.3 | 版本号已为 1.17.26（正确格式） | sdks/vscode/package.json | 不修改版本号——添加文档注释记录适配 | current patch |
| vsix 脚本文件名同步 | vsix 脚本已用 1.17.26 | sdks/vscode/package.json | 不修改 vsix 脚本——已与版本号一致 | current patch |

## 6. Current Patch Edit
- Current patch 1 个 edit block：
  1. `sdks/vscode/package.json`：在 `"version": "1.17.26"` 后添加 `"//"` 字段，记录 `#218：版本格式已正确——源 patch 修正 1.15.3.0→1.15.3，目标已为 1.17.26 无需修改`。
- Adaptation 决策：
  - 不修改版本号：目标已为 `1.17.26`（合法 semver），源修正 `1.15.3.0→1.15.3` 的语义意图（去除 .0 后缀）已在目标中满足。
  - 不修改 vsix 脚本：目标 vsix 脚本已使用 `1.17.26`，与版本号一致。
  - 添加文档注释：JSON 不支持注释，使用 `"//"` 字段约定记录适配原因，确保 current patch 非空。
  - 注释为中文 #218 标记：满足中文注释 gate。
- Source behavior preserved：版本号格式正确性（无 .0 后缀）已在目标中满足。
- Upstream behavior preserved：版本号 `1.17.26` 不变、vsix 脚本不变、所有其他字段不变。

## 7. Verification
- `git apply --check` against state 0215 working tree：PASS（exit 0）。
- Effective changed lines `E`（non-blank added lines，含代码与英文注释，排除纯空行和纯 #218 中文注释）：E=1。
- Qualifying distributed Chinese comments `C`（`#218`-tagged 且含 CJK 的 added lines）：1。
- Required minimum `max(1, ceil(E * 0.15))`：max(1, ceil(1 * 0.15)) = max(1, 1) = 1。
- Gate：1 ≥ 1 → PASS。
- 注释分布：sdks/vscode/package.json 1 条（版本格式适配说明）。
- 无跨 patch 或预存注释聚合：全部 1 注释为 `#218`-tagged 且仅由本 patch 引入。

## 8. Contract Check
- 单 commit：是。
- 完整 source diff 已读：是（1 file，33 行）。
- 完整 upstream 实现已读：是（sdks/vscode/package.json）。
- 无 grep-only 证据：是。
- 无架构式省略：是（源修正已在目标中满足，以文档注释记录适配）。
- 无删除或延迟行为：是。
- 无未请求的备用成功路径或 fallback：是。
- 每条可达路径有 owner：是。
- 无残留行为 bug：是。
- 每 patch 中文注释 gate 通过：是（C=1 ≥ 1）。

## 9. Independent Audit
- Pending（累计 audit 待后续批次）。
- Typecheck: cumulative `--typecheck 219` PASS（packages/opencode:passed）。
- Report: `reports/typecheck-0219-2026-07-27T06-33-10-806Z.md`

## 10. Final Verdict

`PASS`（build）— patch built，`git apply --check` PASS on state 0215，E/C gate PASS（E=1，C=1，need=1）。

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T08:04:17Z
- FORWARD: BATCH PASS (ses_fwd_216_220_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_220_216_v2) - no open B/G
- Tip: states/0220-0436a82b95aa
- Typecheck: typecheck-0220-2026-07-27T07-43-08-935Z.md
- Install: 6caf0c192b005ee2771bebc192462cc82c634b63eda1d115a6fa95da3a14fddb
- Non-blocking N only (do not reopen seal)
