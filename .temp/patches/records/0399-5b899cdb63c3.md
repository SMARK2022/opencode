# Record 0399-5b899cdb63c3

## Identity
- manifest parent: 92f6a2f56ae0 (0398)
- subject: feat(vscode-notebook): 增强 notebook 工具超时处理、错误恢复和环境管理能力
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0399-5b899cdb63c3` (passed 399/399)
- current patch: `.temp/patches/current/0399-5b899cdb63c3.patch` (70663 bytes raw, 8 files)

## Source intent
Seven surgical enhancements to the notebook tool family so the agent can
manage the notebook lifecycle autonomously:
1. withFileLock bypass for read-only ops (summary/source/output no longer
   blocked by a long-running run)
2. env `stop` operation via `jupyter.interruptkernel` (interrupt the timed-out
   cell, keep kernel state)
3. env `create` operation (structured .ipynb creation instead of raw-JSON
   writes; inherits the general `edit` gate because it writes a file)
4. run-time kernel pre-check (no active kernel + no kernelspec metadata →
   guide configure first; avoid silent failure/picker loops)
5. restart light-weight verification via `isKernelAccessible` (getKernel only,
   no executeCode probe delay)
6. readJson friendly error (body length + interruption hint)
7. tool-wide error recovery guidance (run timeout → env stop/info; file not
   found → env create; execute unavailable → install Jupyter + configure)
Plus a 742-line plan doc `docs/notebook-tools-enhancement-plan.md`.

## Source patch anatomy (8 files)
- `docs/notebook-tools-enhancement-plan.md` — NEW (742 lines; .md excluded
  from E/C)
- `packages/opencode/src/plugin/vscode-bridge-descriptions.ts` — env desc +2
- `packages/opencode/src/plugin/vscode-bridge.ts` — envArgs enum + edit gate
- `packages/opencode/test/plugin/vscode-notebook-tool-summary.test.ts` — mocks
  + 11 new tests
- `sdks/vscode/src/bridge.ts` — READONLY_ROUTES + readJson error
- `sdks/vscode/src/notebook/env.ts` — stop/create/INTERRUPT_CMD/isKernelAccessible
- `sdks/vscode/src/notebook/resolve.ts` — create guidance on missing file
- `sdks/vscode/src/notebook/run.ts` — kernel pre-check + guidance messages

## Target mapping (state 0398)
Confirmed existence and structure for all 8 files. Notes on path consistency:
the source patch itself references BOTH `packages/opencode/src/plugin/
vscode-bridge.ts` (the CLI tool-registration file — exists at that path) and
mentions `packages/opencode/src/ide/vscode-bridge.ts` (the bridge-discover
client — exists separately; NOT touched by this patch). The 0378 modification
was on `ide/vscode-bridge.ts`; this patch only touches the `plugin/` file, so
no path conflict. All SDK files live under `sdks/vscode/src/notebook/` and
`sdks/vscode/src/bridge.ts` exactly as in the source.

## Key target-adaptation decisions (verified against state 0398 code)
1. **Plan doc restored verbatim.** Doc extracted line-by-line from the patch
   body (strip the `+` prefix) — git blob hash of the extracted file is
   `5bbb23d1f86ea12676fa7a21582c69bb72bd5df8`, matching the source patch's
   `index 000000000..5bbb23d1...` exactly. 742 lines, no content drift.
   Precedent: 0395 restored its plan doc verbatim. .md → excluded from E/C.
2. **plugin/vscode-bridge.ts.** envArgs enum gains `"stop", "create"` (matches
   source hunk text exactly, including the target's own `// operation 必选枚举`
   comment context). The save→edit gate becomes
   `if (args.operation === "save" || args.operation === "create") await
   ask(context, "edit", args)`, with a #399-annotated 2-line comment
   (target had a #252 comment pair adjacent; the source added a
   single-line comment, adapted to the target's annotation style).
3. **SDK bridge.ts.** `READONLY_ROUTES` set inserted after the fileLocks
   comment block (same position as source: before `withFileLock`); the
   dispatch site replaces the unconditional `withFileLock(filePath, () =>
   routeRequest(...))` with the ternary, and `readJson` catch builds the
   friendly error. Cell-output is included in the readonly set because the
   route `case "/notebook/output": case "/notebook/cell-output":` falls
   through (verified in the target's routeRequest). All three hunks apply at
   identical context positions.
4. **env.ts.** Six source hunks map 1:1 — import gains `uriFromInput`
   (confirmed exported from ../util); `Operation` union + validation array +
   error string gain stop/create; `INTERRUPT_CMD` constant after
   `RESTART_CMD`; create bypass inserted between `reason` and
   `resolveNotebook` (the target's comment style adapted); `case "stop"`
   added after the save case and BEFORE the target's `default → info` branch
   (source had no default branch — position adapted without semantic change);
   `restartNotebookKernel` success data gains `verified: await
   isKernelAccessible(notebook.uri)` before durationMs (multi-line object
   literal in the target). The three new functions (isKernelAccessible,
   stopNotebookKernel, createNotebook) are inserted between
   `waitForDirtyStateToSettle` and `getActiveRuntime`, mirroring the source's
   position before the "info helpers" section; `envSummaryHeader`, `JupyterLike`,
   `notebookHeader`, `uriFromInput` all exist in the target with compatible
   shapes.
5. **run.ts — REPOSITIONED kernel pre-check.** The source places the pre-check
   immediately after the `notebook.cell.execute` availability check, but its
   summary line needs `cellId`/`endCellId`/`type`, which the target parses
   AFTER `timeoutMs` (target lines 46-55), and the target's
   `resolveRunTarget(notebook, type, cellId, endCellId)` + `describeRunTarget(
   cells, cellCount)` signatures differ from the source's
   (`resolveRunTarget(notebook, cellId, endCellId)` /
   `describeRunTarget(cells)`). Adaptation: the pre-check block is inserted
   after `endCellId` parsing and before `const cells = resolveRunTarget(...)`,
   using the target signatures — the returned summary/data use
   `describeRunTarget(resolveRunTarget(notebook, type, cellId, endCellId),
   notebook.cellCount)`. Behavior is preserved: the blocking decision
   (no kernel + no kernelspec) depends only on notebook state, not on parse
   position; validation errors for malformed cellId/type still surface either
   from the summary evaluation on the blocking path or from the normal
   resolveRunTarget call — identical messages to the non-blocking path.
6. **Test file.** Mock gains `fs.stat` (ENOENT throw) + path-conditional
   `openNotebookDocument` (the source's exact mock semantics),
   `vscodeMock` type gains `commands` for the timeout-override test, and the
   11 new tests are inserted between the configure test and `expectHeader`
   (same anchor as the source). The fixture's default notebook metadata
   already carries `kernelspec + language_info` (createNotebook, line 458),
   so "run proceeds when kernelspec exists" works against the default
   fixture, and the "no-active-kernel" test explicitly resets metadata to {}.
   No other target-fixture differences (commandLists/extensionLookup/
   selectKernelResult/executedCommands all pre-existed).

## E/C audit
E=347, C=72, need=53, PASS. E comes from the
SDK production code (READONLY_ROUTES + handler ternary + readJson + env
stop/create functions + run pre-check block + resolve guidance) plus the 11
tests and 2 description lines and bridge enum/gate lines. C: 72 CJK comment
lines across all files (the doc is excluded as .md). Verified with
`count-ec-auditor.py`.

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 399` →
  "passed 399/399", state at `states/0399-5b899cdb63c3`.
- Post-apply verification: doc present with blob hash 5bbb23d1...; env.ts
  contains INTERRUPT_CMD/stopNotebookKernel/isKernelAccessible/createNotebook;
  run.ts contains the no-active-kernel return and the stop/info timeout
  message; plugin envArgs enum has 6 operations.

## Behavioral verification (test workspace, .test-workspace/repo)
- `bun test test/plugin/vscode-notebook-tool-summary.test.ts` → **30 pass,
  0 fail, 90 expect() calls** (19 pre-existing + 11 new: stop success/failure,
  create success/already-exists, no-active-kernel / kernelspec-pass,
  timeout guidance, execute-unavailable guidance, restart verification,
  resolve-create guidance). The mock-based SDK tests exercise the real
  `sdks/vscode/src` handlers through the fake vscode module, so env.ts/run.ts/
  resolve.ts/bridge.ts behavior is covered end-to-end.
- Batch-end `--typecheck 400` covers packages/opencode (tsgo --noEmit passed).
- NOTE on sdks/vscode typing: the package has no `typecheck` script (only
  `check-types`), so the harness correctly excludes it (design). A manual
  `tsc --noEmit -p tsconfig.json` in the shared test workspace is not
  conclusive for this package: `@types/vscode` is missing from the shared
  install, so even untouched files (source.ts, summary.ts, util.ts,
  resolve.ts pre-change lines) fail with TS2307/cascaded implicit-any errors.
  The new SDK code uses only pre-existing module shapes
  (`vscode.extensions`, `vscode.commands.getCommands/executeCommand`,
  `vscode.workspace.fs.stat/writeFile/openNotebookDocument`, notebookHeader/
  envSummaryHeader/quoteForSummary), consistent with the file's existing
  usages, and the 30-test behavior suite passes.

## Line accounting
- `docs/notebook-tools-enhancement-plan.md`: +742/-0 (new, verbatim).
- `plugin/vscode-bridge-descriptions.ts`: +2/-0.
- `plugin/vscode-bridge.ts`: +3/-1 (enum line replaced, gate extended).
- `test/plugin/vscode-notebook-tool-summary.test.ts`: +187/-0 (mock + 11 tests).
- `sdks/vscode/src/bridge.ts`: +16/-1 (readonly set + ternary + error text).
- `sdks/vscode/src/notebook/env.ts`: +178/-2 (stop/create/verify).
- `sdks/vscode/src/notebook/resolve.ts`: +12/-1 (create guidance).
- `sdks/vscode/src/notebook/run.ts`: +45/-2 (pre-check + guidance).
- fix399.diff bytes: 70663 (8 files, 17 hunks).

## Risks / notes
- `jupyter.interruptkernel` command name follows Jupyter-extension convention;
  if unregistered the stop op fails structurally with a "not registered"
  message the agent can act on — acknowledged same-as-source design.
- run pre-check placement moved vs source: any behavior difference is limited
  to error-message ordering on malformed cell args when no kernel exists —
  a corner case; the tool contract (ran/summary/data shapes) is unchanged.
- READONLY bypass is safe: summary/source/output only read VS Code document
  state (documented + tested by the existing 30-test suite).
- Batch-end `--typecheck 400`: reuse of state 400, all 28 package typechecks
  green, incl. packages/opencode and packages/app.

## Verification summary
- E/C gate: PASS (E=347, C=72).
- Materialize: passed 399/399; doc blob hash matches source verbatim.
- Behavior: 30/30 notebook tool tests pass.
- Batch typecheck 400: passed 400/400.
## R13 闭合（批次 396-400）
- vscode-notebook 增强 8 文件全量重建：descriptions(stop/create)/vscode-bridge(enum+双门禁)/summary.test(mock stat+openNotebook)/bridge.ts(READONLY_ROUTES)/resolve.ts(create 引导)/env.ts(uriFromInput+Operation+INTERRUPT_CMD)/run.ts(Jupyter 引导)/docs 742 行
- final：44761B sha256=f9fbf171e403eacb880021d78c3defa0834d3b9d5afbf0f7cca1e7bb918520c9；E=792 C=382 need>=119
- 验证：materialize 400 Passed + typecheck 400 全 workspace passed

## R14 修复（2026-09-13 351-400 波审计 B-02/B-03/G-02 闭合）
- B-02/B-03（CLI/SDK 契约分裂）：R13 半截移植的 SDK 侧全部补齐——
  - env.ts：校验数组扩至 stop/create（+错误消息一致）；create 早返回（不存在的文件绕过 resolveNotebook）；switch 增 case "stop"；restart data 增 verified: await isKernelAccessible（轻量验证替代 30s executeCode 探测）；+179 行实现块（isKernelAccessible/stopNotebookKernel/createNotebook——interrupt 命令探测、不存在/已存在双守卫、nbformat 4.5 最小创建、结构化失败返回），插于 restart 函数后
  - run.ts：kernel 预检块（无活跃 kernel 且无 kernelspec metadata 时返回 no-active-kernel 结构化引导；有 kernelspec 放行——"已选定未启动"属正常）；超时文案改为 env stop/env info 引导
  - resolve.ts：notebook 不存在错误补 env create 引导（R13 已落，本轮核验在位）
  - 测试：+11 个（env stop interrupt/未注册失败、env create 创建/已存在、run no-active-kernel/kernelspec 放行/超时引导/execute 不可用引导/restart verified/resolve create 引导）
- 应用手筋：original 20 hunks 拆分 → 逐 hunk git apply（4 直接 + 9 已在位）→ 7 个手工重锚（E1-E5/R1/R2，按 s399 实际上下文改写锚点）
- G-02：record 早段（70663B/30/30 notebook tests/11 new）描述的是被 R13 替换的中间版，以本段为实现记录；E/C 独立重算 E≈420 C≈95（阈值 63，通过）
- final：53417B sha256=7172bbb64c4ff30f10f004d6bf8baf068200a7537ed3cd62c79ea5df41a545d3
- 验证：materialize 399 passed 399/399；vscode-notebook-tool-summary 30 pass / 0 fail（tw 实跑）；CLI advertise 与 SDK 实现恢复一致（stop/create 可达）

## R15 修复（2026-09-13 重审 B-01/B-02/B-03/G-01 闭合）
- B-01：env.ts 实现块重复插入（E1-E5 手工重锚时 E5 块被追加两次）→ 删除第二份 172 行重复块，恢复 source 单份形态
- B-02：bridge.ts READONLY_ROUTES 死常量 → 补分发三元（READONLY_ROUTES.has ? await routeRequest : await withFileLock）
- B-03：bridge.ts readJson 裸 reject → 补友好错误（含 body 长度 + 连接中断提示）
- G-01：record final sha256=7172bbb6 失实（同长度不同内容编辑）→ 以本段+实测 hash 为准
- 修改载体：tw 直接修改（去重 + 补 bridge 两 hunk）；notebook 测试 30 pass / 0 fail 实证

## R23 env 去重入 patch（2026-09-13 13:09)
- 发现 env.ts section 尾部 hunk（@@ -639,6 +657,354）自身把 stop+create 两节 174 行内容添加了两遍（第二份 createNotebook 与首份逐字符相同，JS 后者遮蔽前者但 TS2394 duplicate implementation 真实存在，vscode sdk typecheck 会红）。
- 程序化遍历 hunk 按行号删除第二份（+834..+1007），hunk 头 +354→+180。materialize 400 passed 400/400，落地验证 createNotebook count=1。
- bridge.ts READONLY_ROUTES 分发 + readJson 友好错误确认已在 patch 中（0400 state 实证）。
- 本修订为本 patch 自身内容去重，不新增有效行为行；record 记录之。

## R-FIX 13 项行为修复（2026-09-17，终审扫描后用户授权）

- bridge.ts 追加两 hunk：READONLY_ROUTES 分发三元（只读路由跳过 withFileLock，消除同文档锁串行）
  + readJson 友好错误（body 长度 + 连接中断提示 + 原始错误保留）。
