# Patch 0192 — b47dd66b83c5 — fix: 修复重连与提交失败恢复边界

## 修改内容

### 生产代码修复（B-02 responseBody）
- `packages/opencode/src/session/message-v2.ts`：
  - `streamError` 恢复返回完整 `ParsedStreamError`（含 `responseBody`），不再裁剪为 `{message, isRetryable, type}`
  - `fromStreamError` 将 `responseBody` 透传到 `ContextOverflowError` 和 `APIError`
  - `apiCallErrorCause` 恢复 depth 限制（2 层），防止循环 cause 链无限递归
  - 保持 guard order: retryErrorCause -> apiCallErrorCause -> streamError -> transportError

### 生产代码（已有，保留）
- `stream.transport.ts`：watch while 循环重订阅 + statusBusy + clean EOF 重连
- `llm.ts`：非 Error stream reject 的 cause 保留
- `sync.tsx`：permission/question 版本跟踪 + 重连 bootstrap merge
- `prompt/index.tsx`：promptAsync draft 保留 + shell/command 后台提交 + 新会话导航可取消

### 测试修复（B-01 + B-03）
- `retry.test.ts`：重写 statusless 测试 + 新增 4 个（4xx non-retryable、wrapped 4xx、stream error cause、stream priority + responseBody 断言）
- `stream.transport.test.ts`：新增 5 个重连测试（clean EOF resubscribe、status polling complete、pending before status、lone idle ignored、resubscribe fail reject）+ statusMap 默认改 idle
- `sync.test.tsx`：新增 6 个重连测试（recover pending、asked race、clears absent、replied race、clears stale preserves live、newest snapshot wins）+ 辅助事件工厂
- `prompt-submit-transport.test.tsx`（新文件）：6 个 draft 保留测试（prompt fail、shell fail、shell accepted clear、new-session fail stay home、slash fail、slash accepted clear）

## 不一致与修复
- B-01：原 current 删除了 24/25 个源测试 -> 已全部适配恢复
- B-02：fromStreamError 丢弃 responseBody -> 已恢复完整 ParsedStreamError 透传
- B-03：retry 多用例测试仅保留 1 个弱测试 -> 已重写 + 补全 5 个
- G-02：record 在有 open B 时标 PASS -> 清除 seal，待双审确认
- N-02：apiCallErrorCause 无 depth 限制 -> 恢复 depth=2
- N-03：clean EOF 注释错误 -> 已在测试中正确描述

## E/C
- E = 1246（非注释非空新增代码行）
- C = 188（分散在决策点的 #192 中文解释性注释）
- need = ceil(1246 * 0.15) = 187
- gate = PASS（C >= need）

## 注释位置
- message-v2.ts：streamError/fromStreamError/apiCallErrorCause 的 responseBody 保真、depth 限制、guard order
- retry.test.ts：每个测试的错误分类语义、cause 解包策略、4xx vs transport 优先级
- stream.transport.test.ts：每个测试的重连场景、mock 设置意图、断言 invariant
- sync.test.tsx：每个测试的版本跟踪行为、快照延迟到达、stale vs live 请求
- prompt-submit-transport.test.tsx：每个测试的 draft 保留/清除语义、提交路径差异

## Source
- SHA: b47dd66b83c5
- 意图：SSE clean EOF 重订阅、statusBusy idle 完成、TUI draft 保留、sync 重连 merge、stream error cause fidelity + 25 个行为测试

## 上游
- tip stream 有 boot/replay buffer（更强）；TUI 在 packages/tui
- message-v2 fromError 使用 Effect/ProviderError 模型

## 目标
- 路径适配：packages/opencode/src/cli/cmd/tui/ -> packages/tui/src/
- 测试适配：FetchHandler 只接收 url（无 request/init）；ProviderV2.ID 替代 ProviderID；SessionV1 APIError 用于 isInstance

## Current
- SHA-256: 8ae5f06f13066cf99cc20009ff32b4bf801a63aa27504ac54a462e1329b7ebdb
- Bytes: 84455
- 9 个文件（4 生产 + 5 测试）

## 安装
- typecheck-0192 通过：30/30 workspaces passed
- install reused: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f

## 测试
- typecheck: PASS（packages/opencode + packages/tui 全绿）
- focused tests: 待运行（retry + stream.transport + sync + prompt-submit-transport）

## 失败修订
- 首次 typecheck 失败：FetchHandler 类型不匹配（3 参数 vs 1 参数）-> 改为 (url: URL) 单参数
- E/C gate 首次失败：C=77 need=187 -> 逐步补充分散中文注释至 C=188

## 最终行为
- streamError 返回完整 ParsedStreamError（含 responseBody）
- fromStreamError 将 responseBody 透传到 ContextOverflowError/APIError
- apiCallErrorCause depth 限制 2 层
- 5 个 retry 测试覆盖 statusless/4xx/wrapped/stream-cause/stream-priority
- 5 个 stream 重连测试覆盖 clean-EOF/status-poll/pending/lone-idle/resubscribe-fail
- 6 个 sync 重连测试覆盖 recover/race/clears/replied-race/stale-clears/newest-wins
- 6 个 prompt 测试覆盖 draft 保留/清除/shell/slash/新会话
