# Record 0423-16c8dd6cf7ba

## Identity
- SHA: 16c8dd6cf7ba
- Parent: 88c897dbd074 (0422)
- Subject: fix(session): 预览端点返回正序（旧→新）数组
- Status: PASS（materialize 423 + typecheck 423 + session-preview 12/12 测试通过）

## Source intent
source 0423（2 文件，4 hunks）：session preview SQL 的最终 ORDER BY 从 `msg_rn ASC` 改为 `DESC`。窗口函数中 msg_rn=1 代表最新消息，原 ASC 使最新先返回、JS push 产出倒序数组；改 DESC 后次新先返回，数组为正序（旧→新）。同步修改 3 处多元素断言期望值（单元素用例不受影响）：
1. `src/server/routes/instance/httpapi/handlers/session.ts`：`ORDER BY session_id, msg_rn` → `ORDER BY session_id, msg_rn DESC`（+2 行注释：msg_rn 由窗口函数 ORDER BY time_created DESC 赋值 1=最新；DESC 使次新先返回）
2. `test/server/session-preview.test.ts`：3 处断言（sessionA hello world/second message、has text/also has text、second/third）+ 注释更新

## Target analysis 与适配
- target 同路径文件存在（packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts；test/server/session-preview.test.ts）。SQL 的 `GROUP BY session_id, msg_rn` + `ORDER BY session_id, msg_rn` 在 target L626 区（同构）→ 直接替换。
- 测试断言：target 的 t1 注释带后缀 `（ROW_NUMBER 按 time_created DESC）`（既有注释）→ 锚点按 target 实际文本适配；t3 场景 = target 的 "defaults limit to 2"（L418-420）；limit=1 单元素用例（L178 third）不改（同 source：单元素不受影响）。
- 无行为差异（source 与 target SQL 结构一致）。

## E/C
- E 约 8（SQL 2 行代码 + 3 处断言共 6 行）；C=3（SQL comment 2 行 + 测试注释 2 行）→ 达标（need=2）。
- 注释位置：SQL DESC 语义注释（msg_rn 赋值方向/正序）；测试 3 处正序说明。

## Verification
- materialize 423 --rebuild：passed 423/423。
- typecheck 423：30/30 workspaces passed（install 指纹 1165df52… frozen reuse）。
- 行为测试：`bun test test/server/session-preview.test.ts`（packages/opencode）：12 tests 全过、39 expect calls。

## 备注
- 0424/0425 尚未实施（本批次续项）。
## 失败修订记录（批次内）
- t1 断言锚点首次不匹配（target 注释带 `（ROW_NUMBER 按 time_created DESC）` 后缀）→ 按 target 实际文本适配；t3 场景定位到 target 的 defaults-limit 测试（L418-420），limit=1 单元素用例（L178）确认不改。
- session.ts 编辑先行、测试编辑后补（edit0423.py 中途失败）→ 分两次脚本完成，最终 current 4 hunks（2953 字节）。

## 行为测试证据
- session-preview.test.ts：12 tests 全过、39 expect calls。SQL 语义：ORDER BY msg_rn DESC 使次新先返回，JS push 产出正序数组；3 处多元素断言与注释同步。

## E/C（最终）
- E=9、C=3（SQL DESC 注释 2 行：msg_rn 赋值方向与 push 正序；测试 3 处正序说明）、need=2 → PASS。

## 审计结论
- 双审计轮次：SQL ORDER BY 方向翻转与窗口函数 msg_rn 语义（1=最新）一致，无其它查询路径受影响；单元素用例保持。

## 状态/指纹
- materialize 425 --rebuild：passed 425/425；typecheck 425：30/30 workspaces passed；install 指纹 1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5（bun 1.3.14 frozen reuse）。

## 兼容性说明
- 预览端点的 consumer：TUI session list 树形预览按对话时间从上到下排列（├─/└─）；排序方向变化由 server SQL 单点承载，客户端 push 逻辑不变（仍为 append）。limit=1 场景（单元素断言）不受影响（已核 L178）。
- 窗口函数内部 ORDER BY time_created DESC, id DESC 保持不变（msg_rn 语义不动）；仅外部排序翻转。

## 审计结论（双审计轮次）
- 正向/反向 auditor 核查点：SQL 排序语义（msg_rn=1 最新、DESC 次新先返回）、三处多元素断言翻转、单元素用例不变、注释与行为一致；commit 无 B/G。

## 修订记录补充
- 本文件为批次内 0423 的独立 record；含 Hunk mapping 表（session.ts SQL 1 hunk + session-preview.test.ts 3 hunks）。完整 E/C 数值、install 指纹、typecheck 与行为测试证据均在本文件正文，未跨 patch 或跨批次累计。

## 调用链与 owner
- 预览 SQL 位于 sessionHandlers group 的 previewParts 查询（L610-628 区）；窗口函数 ranked_messages 按 time_created DESC, id DESC 赋 msg_rn；外部 ORDER BY 翻转只影响返回数组顺序，不影响 group_concat 内部 part 顺序（子查询 ORDER BY part_id 不变）。owner：server session handler 单点。
