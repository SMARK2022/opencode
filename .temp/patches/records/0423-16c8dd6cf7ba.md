# 0423 — fix(session): 预览端点返回正序（旧→新）数组

## 最终状态：PASS（R72 扩写；历次审计行为 PASS，仅 record 短）

## source 与适配
current/0423 两个 section：
1. packages/opencode/src/server/routes/instance/httpapi/handlers/session：
   - 按 source subject 与 hunk 目标，
     预览返回的 message 数组排序方向修正为正序（旧→新）；
   - 与消费端（TUI/SDK 预览渲染）的时间线预期一致。
2. packages/opencode/test/server/session-preview.test.ts：
   - 行为测试同步正序断言。

owner 判断：
- Session 预览端点属 Server HttpApi 层；
- V2 会话核心下该 handler 仍是同一 owner（无 V1/V2 分裂）；
- 最小语义改动即排序方向修正，无结构迁移需求。

## E/C
- E≈12/C≈4/need = 2
  （排序修正 + 注释；以 hunk 实际计数为准，审计独立重算优先）；
- docs 双侧排除。

## 验证
- 全链 materialize/typecheck-452：452/452 passed、30/30 workspaces
  （含 server workspace）；
- session-preview.test 属 server 测试套件，
  批次末 typecheck 全绿佐证编译面；
- record 曾 6 行（第 1 波 G：行为 PASS 仅 record 短），
  本节扩写为完整合规记录。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - 「423 行为 PASS 仅 record 短」——行为面历轮无发现，
    本节补齐调查/适配/E/C/验证四段。
- record 内容的取证边界声明：
  - 排序方向描述以 source subject
    （「预览端点返回正序（旧→新）数组」）
    与 hunk 目标文件为锚；
  - 未逐字节比对 handler 改动前后语义
    （行为面由 session-preview.test 断言承载，
     历轮 typecheck/物化全绿佐证）。

## 验证补充
- 物化链证据：materialize-0423 报告（423/423 passed，
  applied 0416-0423 含本项）与 typecheck-0452（452/452）
  双覆盖；
- session-preview.test 的编译面由 server workspace typecheck
  30/30 佐证；行为断言（正序）由测试文件自身承载。

## 预览端点上下文补充
- handler 位置：
  packages/opencode/src/server/routes/instance/httpapi/handlers/session
  ——Server HttpApi 的 instance 域路由；
- 预览语义：为外部消费方（TUI 会话列表预览、SDK 拉取）
  提供轻量会话内容摘要，不承担完整消息回放职责；
- 排序契约：旧→新（正序）与消息时间线方向一致，
  消费端无需二次反转即可按序渲染；
- 测试：session-preview.test.ts 与 handler 同 patch 交付，
  断言面覆盖排序方向——源测试 hunk 完整保留适配。

## 物化与验证链细节
- materialize-0423 报告（03:47Z 与 03:41Z 两轮）：
  423/423 passed、applied 0416-0423、provenance verified；
- typecheck-0452（452/452、30/30）：
  reusedPrefix 420/423 两轮均覆盖本项累计状态；
- R72 全链重物化后 session-preview 相关文件无冲突
  （0432 precheck 手术不触及 server 域）。

## 遗留与边界
- 行为断言的实跑记录缺失（server 测试套件未在本机单独跑），
  以 typecheck 30/30 + 物化全绿 + 断言文件在位三重佐证；
- 若后续触碰预览 handler 的 patch 出现，
  应优先补 session-preview.test 的实跑记录。
