# Patch 0168 — 2c720138d339

## Identity
- Index: **168**
- Full SHA: `2c720138d3397839f61349a04ba4d9b5d0a43372`
- SHA12: `2c720138d339`
- Parent: `f2d3d0d44b8a` (#167)
- Subject: feat(provider): custom HTTP headers and User-Agent handling (`header-ua`)
- Authored: 2026-05-18T22:14:06+08:00
- Current path: `.temp/patches/current/0168-2c720138d339.patch`
- Current bytes: **12001**
- Current SHA-256: `3635b4743d136b37a9a0acbb8a6ab85cba30c8b63755b346445028cd92121669`
- Original path: `.temp/patches/original/0168-2c720138d339.patch`
- Original bytes: **9686**
- Record updated: 2026-07-25T08:23:58.182313+00:00
- Batch: **166–170**
- Tip base: `states/0167-f2d3d0d44b8a` (pre-168)
- Tip after: `states/0168-2c720138d339`

## Source intent (immutable original)
Original files:
1. `packages/opencode/src/config/provider.ts` — named `options.headers` + `options["header-ua"]`
2. `packages/opencode/src/provider/provider.ts` — export `requestHeaders` / `requestUserAgent`; resolveSDK normalize; fetch UA rewrite with empty/prefix guard
3. `packages/opencode/src/session/llm.ts` — merge `Provider.requestHeaders` after default UA in stream headers
4. `packages/opencode/test/provider/provider.test.ts` — two pure unit tests
5. `packages/opencode/test/session/llm.test.ts` — two wire tests for headers UA and header-ua shortcut

Behavioral contracts:
| Capability | Rule |
|---|---|
| `provider.options.headers` | Extra request headers (string map) |
| `provider.options["header-ua"]` | Shortcut → `User-Agent` |
| UA precedence | Explicit `headers["User-Agent"]` (any case) **wins over** `header-ua` |
| Normalize | Drop raw `header-ua` from SDK options; fold UA into canonical `headers` |
| Stream headers | After default `opencode/<version>` UA so config can override |
| Fetch guard (#168) | If configured UA set, rewrite only when current UA empty **or** `startsWith(configuredUA)` |

## Three-way comparison

### Tip@167 strengths
- Config moved to **`packages/core/src/v1/config/provider.ts`** (StructWithRest already accepts free-form keys; named fields still needed for schema/docs)
- Session headers built in **`session/llm/request.ts`** (not inline `llm.ts`)
- Dual runtime: AI-SDK path + experimental **`native-runtime.ts`**
- Free-form `options.headers` already used in some tip tests (nvidia billing etc.) but **no** `header-ua`, **no** shared `requestHeaders`, **wrong native merge for UA override**

### SMARK original
- Introduces first-class header-ua + requestHeaders + fetch guard + stream merge + tests

### Target adaptation
| Original | Tip owner | Adaptation |
|---|---|---|
| opencode `config/provider.ts` | `core/src/v1/config/provider.ts` | Add named `headers` + `header-ua` beside `apiKey`/`baseURL`/`version` |
| `provider/provider.ts` | same | Export helpers; resolveSDK normalize; fetch startsWith guard; keep NetworkProxy |
| `session/llm.ts` headers | `session/llm/request.ts` | Value-import Provider; merge after default UA block |
| (none) | `session/llm/native-runtime.ts` | Use `Provider.requestHeaders(provider.options)` so dual-runtime parity |
| tests | same package paths | Unit pure; integration rewritten to tip `it.instance` + Effect + `waitRequest`/`drain` |

## Current hunk map (diff --git)
1. `packages/core/src/v1/config/provider.ts`
2. `packages/opencode/src/provider/provider.ts`
3. `packages/opencode/src/session/llm/request.ts`
4. `packages/opencode/src/session/llm/native-runtime.ts`
5. `packages/opencode/test/provider/provider.test.ts`
6. `packages/opencode/test/session/llm.test.ts`

## Independent E/C
- **E:** **133** (approx independent recount ~131)
- **C:** **21** Chinese `#168` decision comments
- Required: **20**
- Gate: **PASS**
- C sites: named schema vs rest; UA precedence; strip shortcut; fetch empty/prefix policy; request merge after default UA; native shared policy; unit vs wire test intent

## Materialize / typecheck / tests
- `bun .temp/patches/src/apply-cumulative.ts --materialize 168` → passed
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 168` → green; install fingerprint `bfd9217687…`
- Focused tests (test-workspace `packages/opencode`):
  - `bun test test/provider/provider.test.ts -t "header-ua|User-Agent overrides"` → **2 pass**
  - `bun test test/session/llm.test.ts -t "configured provider"` → **2 pass** (headers + header-ua)
- Batch-end later: `typecheck-0170-2026-07-25T07-56-46-317Z.md` (30 packages)

## Inconsistencies / revisions during adaptation
1. Blind original apply fails: config path moved; llm headers site moved
2. Native path required for parity under experimental native LLM (not in original file list but required by tip dual-runtime)
3. Integration tests rewritten from `withTestInstance` to tip Effect `it.instance`
4. Dead `providerHeaders` helper left in native-runtime after switch (N only; not a second success path)

## Relation to #169
#169 depends on this surface and **removes** the startsWith fetch guard so AI-SDK suffix UAs still allow configured UA to win. #168 intentionally ships the prefix guard; #169 is sequential primary-path refinement, not a competing fallback.

## Final tip behavior after #168
Configured provider headers and `header-ua` reach the wire; explicit UA overrides shortcut; fetch preserves unrelated UAs under the #168 guard; AI-SDK and native share `Provider.requestHeaders`.

## Dual independent audit (batch 166–170)
- FORWARD: **PASS** (all five; no open B/G) — task after record expansion re-audit
- REVERSE: **PASS** (all five; no open B/G) — independent reverse re-audit
- Prior cycle: BLOCK only on record size G for 167–170; records expanded ≥50 lines & ≥3000 chars; behavior had no open B
- Materialize: `materialize-0170-2026-07-25T07-56-36-492Z`
- Typecheck: `typecheck-0170-2026-07-25T07-56-46-317Z` (30/30; install `bfd9217687…`)
- Sealed: 2026-07-25T08:32:23.632271+00:00
- Batch verdict: **PASS** — may advance to 171–175
