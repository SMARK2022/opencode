# Record: 0298-9398ed0d72cb

## 基本信息
- index: 0298
- sha12: 9398ed0d72cb
- subject: feat(compaction): 增强文件证据处理，支持文件大小和修改时间的准确记录
- source date: Sat, 13 Jun 2026 15:48:42 +0800

## Source 行为重建
原始 patch（4567961 字节，4.5MB，主体是 models-snapshot.js）修改 5 个文件：
1. **bun.lock**：packages/opencode version 1.15.3-smark → 1.15.6-smark
2. **docs/prompts.md**：方案摘要前插入"请你进行完整的调研而不是只进行简单的脱离底层实现以及边界情况的顶层规划。"
3. **packages/core/src/models-snapshot.js**：版本号 + 模型快照内容更新（4.5MB 主体）
4. **packages/opencode/src/session/compaction.ts**（7 hunk）：
   - import AppFileSystem
   - InspectedFileEvidence 加 size/modifiedMs 字段
   - 删除 MutationEvidence/collectMutationPaths（基于 mutation 事件序列）
   - 新增 statSize/statModifiedMs/formatEvidenceModified/isNotFoundError/statFailureEvidence/currentFileEvidence
   - renderInspectedFiles 改为 Effect 化：对已截断的可见行逐个 `input.fs.stat()`，磁盘 stat 实时比对判定 current/stale/deleted
   - renderEvidenceHandoff 加 fs 参数并 Effect 化
   - layer 接线 AppFileSystem.Service + defaultLayer
5. **compaction.test.ts**（8 hunk）：测试框架加 AppFileSystem.defaultLayer；localTimestamp/writeEvidenceFile/removeEvidenceFile 辅助函数；新增"uses current disk state for inspected file freshness"测试（unchanged=current / changed=stale / deleted / missing-metadata=stale）；modifiedMs 用 localTimestamp 本地时区；断言 stale → current 等

## 关键判断：compaction 部分在目标中无载体
- 0298 的 compaction 改动是**对"已存在 evidence 功能"的增量增强**（delete 侧显示旧 collectMutationPaths/renderInspectedFiles 已存在），不是引入新功能。
- 该 evidence 基础功能（renderEvidenceHandoff、renderInspectedFiles、renderVerifiedCommands、renderOutstandingTodos、completedToolEvents、canonicalEvidencePath、displayPath、evidenceCell、mergeRanges、EVIDENCE_* 常量、EVIDENCE_HANDOFF_KIND）在 source 中来自**合并侧分支**，不在 452 项 first-parent patch 集内。
- 已用 grep 验证：这些符号只在 0298/0345/0350/0353/0354/0410 出现，且 0298 是第一个出现 renderEvidenceHandoff 的 patch（作为 minus 修改而非新增定义）。没有任何 first-parent patch 以 `+function` 形式引入这些基础函数（0044 只引入单参 displayPath，0180 引入 stats 专用 ToolEventSeed，均非 compaction evidence）。
- 目标 compaction.ts 是 V1/V2 混合版本（SessionV1 + MessageV2），完全没有 evidence 概念（grep Inspected/Evidence/Verified/Outstanding/stale/lastRead 零匹配）。
- 目标 read.ts 有 size/modifiedMs metadata（#142 移植），但 compaction 侧从未消费。
- 0298 的 7 个 compaction hunk 全部依赖 evidence 基础符号，无法独立承载。从零构建需要完整恢复 renderEvidenceHandoff 整个体系（renderVerifiedCommands 等在 0350/0353 中才显示为已存在函数，而非本 patch 定义），超出单 patch 范围。
- 结论：0298 的 compaction.ts/compaction.test.ts 改动在目标中无载体（evidence 基础来自 source 合并侧分支，目标从未移植），记录为跳过。

## 三方比较
- **bun.lock**：目标版本线 1.17.28-smark（0297 state package.json）；0231 先例：0298 的 bun.lock bump 被 subsumed（lockfile 由 install 重生成，不单独适配）
- **docs/prompts.md**：目标存在（73 行），可 apply（单行插入）
- **models-snapshot.js**：目标存在（2MB，#229 生成物），0298 hunk 是版本内容更新；按 #195/#204/#229 先例跳过（build-time 注入策略）
- **compaction.ts**：无载体（见上）
- **compaction.test.ts**：无载体

## 适配内容
- **docs/prompts.md**：单行插入"请你进行完整的调研而不是只进行简单的脱离底层实现以及边界情况的顶层规划。"（在"只输出方案，不要改代码。"之后）
- **跳过硬编码**：bun.lock（subsumed）、models-snapshot.js（build 生成物）、compaction.ts/compaction.test.ts（evidence 基础缺失，来自合并侧）

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（4567961 bytes）
- 编辑后 SHA-256: 506810b5e50d3f022eeb31ba2a8f1368489f359f8b9917907f14cae2146a04f3
- 编辑后字节: 646
- materialize 298: passed 298/298

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| bun.lock @@ -384,7 | 跳过 | subsumed（0231 先例） |
| docs/prompts.md @@ -59,6 | @@ -64,6 | 单行插入，行号偏移 +5 |
| models-snapshot.js @@ -1,3 | 跳过 | build 生成物，#229 先例 |
| compaction.ts import/类型/helpers/渲染/layer | 跳过 | evidence 基础缺失 |
| compaction.test.ts | 跳过 | 同上 |

## E/C 计算
- E (有效修改行): 1（docs/prompts.md 单行插入）
- C (中文注释行): 1（插入行本身是中文指令文案，描述调研要求边界）
- 满足 E > 0 时 C >= max(1, ceil(1 * 0.15)) = 1，1 >= 1 ✓
- 注：source 的 compaction 增强（大量 E）因无载体跳过，不累计

## 安装与测试
- typecheck: packages/opencode exit 0, packages/tui exit 0（独立验证）
- install 指纹: 890e205aa90b98acfd845b47d858c5680b59fb96e807f3165ffcb0610ff5f386 (reused)
- 无行为测试（docs 单行插入）

## 不一致
- compaction evidence 基础功能来自 source 合并侧分支，不在 first-parent 集内，目标从未移植
- models-snapshot.js 跳过
- bun.lock bump subsumed

## 物化验证
- materialize 298 passed 298/298
- 后续 299/300 依赖 298 状态，累计 apply 成功

## 状态
- PASS（待批次审计）

## Typecheck 证据（R9 补充）
- 自动 --typecheck 300 报告在 packages/app TS1128（pre-existing 环境基线）处首错停止，未列出其余 affected workspace；
- 补充报告：.temp/patches/reports/typecheck-0300-supplement-296-300.md（实际 un typecheck runs：packages/opencode exit 0、packages/tui exit 0，install 指纹 890e205a，bun 1.3.14）

## DUAL-AUDIT SEAL（批次 296-300，gate-repair 封存）
- 正向审计（forward, gate-repair stream A, ses_ff9f1812affesRYtvJ2Q4589OQ）: 5/5 PASS，无 open B/G（N-01~N-05 记录/证据口径类非阻塞）
- 逆向审计（backward, gate-repair stream B, ses_ff9f160e2ffePOYCcsppytFHz6）: 5/5 PASS，无 open B/G（N-01~N-06 非阻塞）
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 890e205a（独立重算一致）、packages/app TS1128 环境基线（custom-elements.d.ts 为符号链接物化文本，prior to batch）、opencode/tui typecheck exit 0（独立复跑）、97~170 项受影响测试现场通过
- E/C 双审计独立重算全部达标（0296:54/12、0297:9/2、0298:0/0 或 1/1、0299:1001~1002/186、0300:17/3）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
