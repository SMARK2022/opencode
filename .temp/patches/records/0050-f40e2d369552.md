# Patch Record 0050

## 1. Identity
- Index: 50 · SHA12 `f40e2d369552`
- Current SHA-256: `8b62e8df7f24d758ab5c830e26866855b3fa63d7e5fb0fc6fc4490f22635ffea` · bytes 5359
- State: `states/0050-f40e2d369552` · cumul=None status=None builtAt=None
- Typecheck authority for this index / chain: batch reports through 50; later batch-end install fingerprint may evolve. Prefer reports that list Typecheck all-pass after apply of this index.
- Baseline 4473fc3c… · tip d0ceb469…

## 2. Upstream design
Compaction is owned by core: `packages/core/src/session/compaction.ts` builds the user-facing summary prompt (`SUMMARY_TEMPLATE` + `buildPrompt`), while the compaction agent system prompt lives at `packages/opencode/src/agent/prompt/compaction.txt`. V2 taxonomy emphasizes Objective / Important Details / Work State / Next Move rather than SMARK’s Goal/Constraints wording. Tests in `packages/core/test/session-compaction.test.ts` assert section headers appear in `buildPrompt` output.

## 3. SMARK design
Rewrite compaction into a **recovery snapshot** (not a transcript): text-only / no tools, preserve symbols and line numbers, drop stale **or resolved** noise, structure Files & Code + Errors & Fixes, stronger rules for next steps and critical context.

## 4. This adaptation
- Port SMARK section **Files & Code** (replacing Relevant Files) and **Errors & Fixes** into core `SUMMARY_TEMPLATE` while keeping V2 section skeleton.
- Align compaction.txt CRITICAL / recovery language with SMARK intent.
- Update core tests: `## Files & Code` + `## Errors & Fixes` expectations so CI matches template.
- Chinese comments at token limits, template, and buildPrompt.

## 5. Why keep V2 labels
Forcing full Goal/User Constraints renames would discard upstream structure without a consumer requirement in this tree. Layering SMARK Files/Errors is the minimal fusion that carries recovery-critical content.

## 6. Metrics
E=22 C=4 required=4 gate=PASS

## 7. Verification
- materialize 50; session-compaction tests 2 pass
- typecheck chain includes this surface when cumulative typecheck run after apply

## 8. Verdict
`PASS` — dual independent auditors FORWARD+REVERSE batch 50-54, no open B/G.

## 9. Dual audit closure
- Batch 50-54 FORWARD: PASS
- Batch 50-54 REVERSE: PASS

## 10. Call chain
1. Compaction agent is invoked with the system prompt from `agent/prompt/compaction.txt`.
2. Core `buildPrompt` fills `SUMMARY_TEMPLATE` with prior work context.
3. Model returns a structured summary that later turns treat as recovered working state.
4. Tests in `session-compaction.test.ts` pin section headers so template renames cannot silently regress.

## 11. Upstream vs SMARK section map
- Upstream keeps Objective / Important Details / Work State / Next Move framing.
- SMARK recovery adds Files & Code (paths + symbols) and Errors & Fixes (what broke and what fixed it).
- Adaptation keeps upstream headings and injects the two SMARK recovery sections where consumers and tests look for them.

## 12. Residuals
Later SMARK commits may retune prompt wording again; this index only owns the first recovery-section fusion and the matching test header expectations.
