# 0251-a78bedf5a562 record — feat(compaction): 确保已总结的历史不会重复上传

## Manifest
- index 251, sha `a78bedf5a5627eef17041e721c9b8bd14634930d`, parent `615fcee6765d` (0250)
- subject: feat(compaction): 更新压缩逻辑，确保已总结的历史不会重复上传，优化摘要处理

## Original 身份
- original/0251-a78bedf5a562.patch: 7900 字节, 2 files（compaction.ts 1 hunk、compaction.test.ts 3 hunk）

## Source 行为（逐 hunk）
1. **compaction.ts**：`const history = ...` → `const rawHistory = ...` + `const history = MessageV2.filterCompacted(rawHistory)`。压缩上传与普通 prompt 共用 filterCompacted 的 active replay window——raw history 保留被 summary 覆盖的旧 head 作审计数据，但不能再次进 provider 请求（否则工具输出按 TOOL_OUTPUT_MAX_CHARS 重复截断累计膨胀）。
2. **compaction.test.ts**：新增测试 "updates compacted summaries without replaying already summarized head"（两次 process，第二次断言含 previous-summary/active tail 不含旧 head 大 tool output）；"ignores previous summaries when sizing the retained tail" 加 anchor 用户消息；firstCompaction 从 messages.at(-1) 取 info.id + updatePart 建 tail_start_id。

## 目标适配（Round 1）
- **compaction.ts hunk context 失配**：目标 250 状态中 cfg 与 history 之间被 requestUsage.begin 块（0017 patch 引入，含 `variant: userMessage.model.variant` + `}) as any as ...` 后缀）隔开，source hunk 的 4 行前置 context（variant/})/cfg/history）不连续。适配：删失效前置 context，以目标 L364 `const history = ...` 为被替换行、L365-367（prior/hidden/previousSummary）为 context。
- **测试 hunk**：3 个 hunk 可 apply（offset -16）；新增测试体 `stub.layer` → `stub.llmLayer`（目标 llm() helper 只暴露 { push, llmLayer }，无 layer——前序适配已确立）。
- MessageV2.filterCompacted / CompactionPart / createSummaryAssistantMessage 等依赖目标均已存在（242/205 前序）。

## 生成方式
- gen-251.py：复制 250 状态 → `git apply --exclude=compaction.ts`（只 apply 测试 hunk）→ 手动改 compaction.ts（rawHistory + filterCompacted + #251 注释）→ 改 stub.layer → git diff。
- current/0251-a78bedf5a562.patch: 7265 字节, 2 entries, SHA-256: 760183fc7df3957b0a81ec191b3420ca8df70e7915615274609ba692666cd9fe

## 验证
- apply-cumulative --materialize 251: passed 251/251
- compaction.test.ts "updates compacted summaries without replaying": 1 pass (9 expect)
- 待批次验证 typecheck + E/C

## 状态
- materialized: states/0251-a78bedf5a562

## E/C 最终（count-ec.py）
- E=74, C=12, need=12, PASS（#251 注释：filterCompacted active window、raw history 审计数据、TOOL_OUTPUT_MAX_CHARS 膨胀、测试意图）

## 验证（最终）
- materialize 255: passed 255/255（累计）
- compaction.test.ts 251 测试 pass（9 expect）
- opencode typecheck 通过


## 补充说明（审计 G-02 收尾）
### source/上游/目标三方比较
- source 意图：压缩上传必须与普通 prompt 共用同一 active replay window——数据库 raw history 保留被 summary 覆盖的旧 head 作审计/恢复数据，但这些旧 tool results 不能在后续 summary 更新时再次进 provider 请求，否则每条工具输出按 TOOL_OUTPUT_MAX_CHARS 重新截断并累计，膨胀压缩请求。
- 上游（目标基线）：MessageV2.filterCompacted（message-v2.ts L605-672，含 #204/#205/#242 boundary 逻辑）已是 newest→oldest 压缩边界 replay window 的唯一实现——作为不可删除基线保留；0251 只在压缩上传路径接入它。
- 目标：compaction.ts L366 rawHistory（slice 到 compaction marker）、L372 history = filterCompacted(rawHistory)、后续 completedCompactions/hidden/select 都在过滤后的 history 上运行。
### 适配细节
- compaction.ts hunk context：目标 cfg 与 history 之间被 requestUsage.begin 块（0017 patch 引入）隔开——删失效前置 context（variant/})/cfg 三行），以 L364 history 行为锚、L365-367（prior/hidden/previousSummary）为 context。
- 测试适配：stub.layer → stub.llmLayer（目标 llm() helper 只暴露 { push, llmLayer }）；MessageV2.CompactionPart → SessionV1.CompactionPart（目标 message-v2.ts 的 CompactionPart 从 @opencode-ai/core/v1/session import，非导出）。
### 行为验证
- 251 测试 "updates compacted summaries without replaying already summarized head"：1 pass（9 expect）——第二次 process 捕获 provider 输入含 previous-summary/active tail，不含旧 head 大 tool output。
- 255 常量（DEFAULT_TAIL_TURNS=4）与 251 filterCompacted 交互验证：251 测试用显式 tail_turns:1 override 隔离；255 测试在默认 4 轮下 7 expect 通过。
### E/C 与验证
- E=74, C=12, need=12 PASS（#251 注释：filterCompacted active window 语义、raw history 审计数据用途、TOOL_OUTPUT_MAX_CHARS 膨胀风险、测试意图 2 条）
- materialize 255: passed 255/255（累计）；opencode typecheck 通过；compaction.test.ts 相关测试全绿。
- typecheck-0255 报告：.temp/patches/reports/typecheck-0255-2026-08-08T15-01-13-533Z.md（install 指纹 c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c；packages/app pre-existing TS1128 失败，0247 已记录 baseline 缺陷；opencode/tui typecheck 手动验证通过）

## DUAL-AUDIT SEAL — 批次 251-255（重审通过）
- 正向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；G-01（typecheck-0255 报告）、G-02（records ≥50 行/≥3000 字符）、G-03（0252 身份行 28766B/4725314f）均已验证修复。
- 逆向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；仅 N-01~N-04 记录级观察（seal 格式、自动 typecheck 覆盖、sdks/vscode node-gyp 环境、lockfile 差异）。
- 两份结果一致，批次 251-255 放行。
