# Record 0251-a78bedf5a562

## Status
PASS

## 修改内容
feat(compaction): 更新压缩逻辑，确保已总结的历史不会重复上传，优化摘要处理。

### 回退发现
- record 旧版记载适配版 7265B（760183fc…）已丢失；现存 current 曾为 original 原样。本轮重建。

### Source 逐 hunk 行为
1. compaction.ts：process 上传前 `history = MessageV2.filterCompacted(rawHistory)`——压缩上传与普通 prompt 共用同一 active replay window；旧 head 的 tool results 不能再次进入 provider 请求（TOOL_OUTPUT_MAX_CHARS 反复截断累计会膨胀请求）。
2. 测试新增 "updates compacted summaries without replaying already summarized head"（两次 process，断言 previous-summary+active tail 在、old head/新 tail 不在）。
3. fixture 增强：anchor 消息（"existing retained anchor"）。
4. "ignores previous summaries" fixture 显式持久化 tail_start_id 边界（anchor），防长 summary 挤掉已持久化 tail。

### 目标适配（0249 树 → 0251）
- owner `packages/opencode/src/session/compaction.ts:364`（0242 迁移产物；`-` 行逐字一致，前置 context 被 #0017 requestUsage.begin 块隔开）→ 重锚至 requestUsage 尾行。
- 测试 H2 锚 L1503（`{ git: true },` + `)]`）；`stub.layer` → `stub.llmLayer`（目标 llm() 返回形态）；`config: cfg({ tail_turns: 1, preserve_recent_tokens: 10_000 })` 形态与 L1064 先例一致。
- H4 `MessageV2.CompactionPart` → `SessionV1.CompactionPart`（message-v2 不 re-export 该类型；L308 先例）。
- 新增 H5（目标特有适配）：keeps recent 测试第二次 process 传 raw stream 而非预过滤流——生产 process 内部已统一 filterCompacted，测试输入与数据库真实形态对齐。

## E/C
- E = 84（plus - commentPlus + minus）
- C = 16（+// 全中文口径：#251 标注 6 行 + source 自带中文注释 10 行——覆盖 replay window 不变量、TOOL_OUTPUT_MAX_CHARS 膨胀机制、active window 双条目契约、tail 边界锚定意图）
- need = ceil(84 * 0.15) = 13 → 余 +3 PASS

## Source
- original/0251-a78bedf5a562.patch: 143 行 2 文件。

## Current
- current/0251-a78bedf5a562.patch: 7035 bytes, 5 hunk / 2 文件
- SHA-256: 78ae33bb26562db09dbdacaca706375f0b617f8e307a5a01b3c6a02cd52dd07c

## 安装
- materialize 251 --rebuild: passed 251/251

## 测试
- typecheck 251: 全部 workspace passed
- `bun test --timeout 120000 test/session/compaction.test.ts`（packages/opencode）: 52 pass / 2 fail / 1 skip
- **2 个 fail 均为 pre-existing**（0249 完整组合——0249 生产 + 0249 测试原版——实测同样失败）：
  1. `stops quickly when aborted during retry backoff`：Windows 慢盘 timing（973ms > 250ms 断言），已知环境性失败。
  2. `keeps recent pre-compaction turns across repeated compactions`：0249 基线实测 fail（探针证实 marker2 无 tail_start_id 的机制在 0249 已存在——小窗口不触发 tail 裁剪时 L465 不写 tail，filterCompacted 的 marker 遍历提前 break）。该失败与本 patch 的 process 内过滤无关；本 patch 的新测试（H2）与增强测试（H3/H4 的 ignores previous summaries）全部通过。

## 修订历史
1. 初版 4 hunk：compaction.ts 重锚失败（requestUsage 尾行 `)]` 被误写为 `])`——read 工具方括号包装造成连串误判，最终以字节码为唯一真相修正）。
2. keeps recent 失败排查：H5 raw-input 适配 + H5 锚定漂移（--ignore-whitespace 下 8/6 空格模糊匹配导致 hunk 落错位置，a 坐标修正为应用时真实位置 1603）。
3. pre-existing 判定：0249 完整组合实测复现同一失败 → 记录证据链，非本 patch 缺陷。
4. 最终：materialize OK、typecheck OK、52 pass（含新测试与增强测试）、C=16 ≥ 13 → PASS。

## 三方比较
- Source：process 上传前过滤已总结 head + 两个 fixture 增强 + 新回归测试。
- Upstream v1.17.20：compaction 在 app 层（0242 迁移谱系），filterCompacted 由 #204/#205/#242 谱系实现。
- Target：生产逻辑原样落地（重锚）；测试按目标 helper 形态适配（llmLayer/SessionV1 类型/cfg 键）；新增 raw-input 适配对齐生产语义。无行为丢失、无重复 owner（V2 core 的 compaction 不在本 patch 范围，record 0242 已界定）。

## 最终行为
- 压缩上传的 history 经 filterCompacted 过滤——已总结 head 的旧 tool results 不再重复进入 provider 请求。
- 新回归测试锁定：两次压缩后 previous-summary 与 active tail 在、old head 与未纳入 tail 的新消息不在。
- "ignores previous summaries" fixture 显式持久化 tail_start_id 边界。

## 批次双审结论（2026-09-07，250-254）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：keeps recent 失败的显现路径经 process 内 filterCompacted（message-v2.ts:606 无 tail_start_id 即 break），但产生 marker2 无 tail 的生产条件（compaction.ts:251/:465）均为 #251 未触碰的既有代码；H5 反而消除测试侧预过滤盲区。