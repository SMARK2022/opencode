# Record 0395-adb288a4cc53

## Source patch

- Source: `fix(tui): DialogMessage 移除 usePromptRef 改用 submit prop 修复点击消息崩溃` (SMARK2022, 2026-07-06)
- Original: `.temp/patches/original/0395-adb288a4cc53.patch` (16,501 bytes, 374 lines, 4 files)
- Replay index: 0395, sha adb288a4cc532207a60290673ffc7549406ef6ec

## Source intent

修复 0393 C1 Retry 引入的运行时崩溃：点击消息 → `dialog.replace(() =>
<DialogMessage />)` → DialogMessage 内 `usePromptRef()` 抛
"PromptRef context must be used within a context provider"。

**根因（组件树层级）**：DialogProvider 在 PromptRefProvider **之上**
（app.tsx 258 vs 262）。`dialog.replace()` 的渲染区
（`{value.stack.at(-1)!.element}`）是 `{props.children}`（含 App → 各
Provider）的**兄弟节点**，SolidJS context 只向下传递，因此 dialog 渲染区
拿不到 PromptRefProvider 的 context。useSync/useSDK/useRoute 正常是因为它们
的 provider 都在 DialogProvider 之上。

**修复**：DialogMessage 移除 usePromptRef import 与调用，新增可选
`submit?: () => void` prop；由 session/index.tsx（PromptRefProvider 内，
`promptRef.current?.submit()` 可用）在调用点注入，经 DialogTimeline 透传；
Retry onSelect 的 `promptRef.current?.submit()` 改为 `props.submit?.()`。
三处调用（DialogTimeline 的 replace 内 + 消息点击的 replace 内）全覆盖，
同时补齐 DialogTimeline 的 submit prop 声明。行为：submit 未注入时
optional chaining 静默降级不提交（不崩溃）。

新增 264 行方案文档 docs/promptref-context-fix-plan.md（组件树证据 +
方案对比 + 边界路径）。

## State analysis (state 0394)

4 个目标（3 代码 + 1 文档）在 state-0394 全部存在，结构细节：

1. `packages/tui/src/routes/session/dialog-message.tsx`：0393 版本已含
   `import { usePromptRef } from "../../context/prompt"`（相对路径）、
   `const promptRef = usePromptRef()`、Retry onSelect 末行
   `promptRef.current?.submit()`——source pre-image 逐字对应；
   target 其余部分（useClipboard/stripPromptPartIDs）不受影响。
2. `packages/tui/src/routes/session/dialog-timeline.tsx`：47 行，props 无
   submit，replace 内 `<DialogMessage ... setPrompt={props.setPrompt} />`。
3. `packages/tui/src/routes/session/index.tsx`：两处调用点——"Jump to
   message" 的 DialogTimeline（行 663，setPrompt={(promptInfo) =>
   prompt?.set(promptInfo)}）与 UserMessage onMouseUp 的 DialogMessage
   （行 1460）；`promptRef` 在行 247 由 usePromptRef 取得（PromptRefProvider
   之内）。
4. dialog-message 从 `packages/opencode/src/cli/cmd/tui/` 已迁移到
   `packages/tui/src/`（相对 import）——source 路径在此处改写。

## Port decision

全语义移植（4 文件，3 TUI 组件 + 1 文档），适配点：

1. **docs/promptref-context-fix-plan.md**：提取 264 行逐字还原（blob 与源码
   index 哈希 644a71bd73 一致）。.md 不计 E/C。
2. **dialog-message.tsx**：删除 usePromptRef import 行；props 类型加
   `submit?: () => void` + 3 行 #395 注释（不在此组件内 usePromptRef 的原因
   与 prop 传递方案）；删除 `const promptRef = usePromptRef()`；
   `promptRef.current?.submit()` → `props.submit?.()`。
3. **dialog-timeline.tsx**：props 加 `submit?: () => void` + 2 行注释；
   DialogMessage 调用加 `submit={props.submit}`。
4. **index.tsx**：两处调用点各加 `submit={() => promptRef.current?.submit()}`
   与 2 行 #395 注释（JSX 属性区注释，TSX 解析器按 trivia 处理——typecheck
   已验证）。

不再引入额外 import；`promptRef` 已存在于 index.tsx 作用域（行 247），零
新依赖。

## E/C gate

`count-ec-auditor.py`: E=6, C=9, need=1 → PASS（docs 全排除）。E 6 行：
两个 submit prop 声明 + props.submit?.() + dialog-timeline 透传 + 两个注入
回调；C 9 行：3+2+2+2 的四处 #395 注释。

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix395.diff`（15,112 bytes，4 文件 8 hunk）
- Current patch: `.temp/patches/current/0395-adb288a4cc53.patch`
  （覆盖原逐字占位）

## Materialize

首版 `--materialize 395` 通过；因 0393/0394 的重建（handler 类型修复 +
污染纠正），链上以 `--materialize 395 --rebuild` 复验通过，最终
`passed 395/395`，state 发布为 `states/0395-adb288a4cc53`。前置验证：
fix395.diff 在 state-0394 独立拷贝 `git apply --index` 首发通过，三个代码
文件 SHA256 与 scratch 逐字节一致、文档 blob 与源码哈希 644a71bd73 一致。

## Verification

- 前置 `git apply --index` 通过（4 文件 cleanly）。
- 应用后 3 个代码文件与 scratch 一致；文档 blob 与源码逐字相同。
- batch-end `--typecheck 395`：30/30 workspaces PASS（install reused，
  含 packages/tui 的 JSX prop 类型、unused-import 消除、submit 链类型）。

## Notes / deviations from source

- 路径迁移：source 的 `packages/opencode/src/cli/cmd/tui/routes/session/*`
  在 target 为 `packages/tui/src/routes/session/*`（相对 import）——行为
  逐字一致。
- source 无 JSX 内注释；target 在 JSX 属性区加了 #395 注释（既有 target
  惯例），parser 按 trivia 丢弃，未影响渲染。
- 运行期验证（点击消息不崩溃）需人工 TUI 操作，typecheck 只能证明
  prop 类型与调用链完整；revert-compact 测试不涉及 TUI 组件，无自动回归
  可加（与 source 方案一致）。

## Batch 0391-0395 最终状态

- materialize：0391/0392/0393/0394/0395 全部 `passed N/N`；state 链
  `0391-ec7682c3da9e` → `0395-adb288a4cc53` 完整连续，无删除项。
- typecheck（批末）：`--typecheck 395` → `passed 395/395`，30/30
  workspaces PASS，install=reused，无 integrityFailure。
- E/C 门禁：5/5 PASS（0391: 0/0；0392: 134/43；0393: 329/72；0394:
  71/31；0395: 6/9——docs 全排除规则下 need 全部满足）。
- recount_hunks.py 已运行（无警告；materialize 的 `git apply --index`
  是 hunk 一致性的最强证明）。
- 累计 diff bytes：2,326 + 12,625 + 84,271 + 16,522 + 15,112 = 130,856；
  累计文件：1+2+9+6+4 = 22 文件（含 4 个 .md 设计文档与 3 个迁移 TUI
  文件全部落位）。
- 跳过项：0 个（本批无缺失文件；0391 的原始 CRLF patch 改语义编辑、
  0392 的两个 import 行因 target 已导入而省略、0393 的 revert toolFiles
  过滤链因 target 无此结构而保留既有简化形态、0394 的 !existing.busy
  分支因 target Runner 语义不同未引入——均为适配而非跳过）。

## DUAL-AUDIT SEAL (batch 0391-0395)
- R2 forward auditor (ses_fc56e5dd): 5/5 PASS, batch APPROVE, no open B/G
- R2 backward auditor (ses_fc56e49c): 5/5 PASS, batch APPROVE, no open B/G
- R1->R2 revision loop closed: 0392 B-01 (breaker counter on dead tool-result error branch -> AI SDK failures emit tool-error) fixed via countConsecutiveError closure helper wired into both tool-error + tool-result error paths; G-01 (no test run record) resolved
- Post-fix verification: processor-effect 17/17 (incl. consecutive-error 2/2, cross-turn doom_loop now fires), revert-compact 13/13, cancel-race 1/1; typecheck 395: 30/30 workspaces PASS
- N items closed: 0392 record E/C aligned to 160/59/24; stacked-patch hygiene noted
- Install: 0d7dfb52 reused, bun 1.3.14; integrityFailure None
- Verdict: BATCH 0391-0395 CLOSED
