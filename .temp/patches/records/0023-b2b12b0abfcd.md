# Record: Patch 23 — b2b12b0abfcd

## 1. Manifest Entry

- **Index**: 23
- **SHA**: b2b12b0abfcde9a9d7c5a2202c01726b337d3588
- **Parent**: 0022-17b46ab162f5
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加 extends 字段以支持从现有提供者类型继承模型和默认值，更新相关逻辑，同时添加 claude 的 header

## 2. Source Behavior (original patch)

### 修改文件
- `packages/opencode/src/config/provider.ts`
  - 在 `Info` schema 中添加 `extends: Schema.optional(Schema.String)` 字段
  - 添加 description: "Inherit models and defaults from an existing provider type"

- `packages/opencode/src/provider/claudecode.ts`
  - 在 `buildRequestHeaders` 的 betas 数组中添加 `"redact-thinking-2026-02-12"`
  - 这个 beta header 控制是否对 thinking blocks 进行 redaction

- `packages/opencode/src/provider/provider.ts`
  - 在 `custom` 函数的 anthropic custom-loader 中添加 `redact-thinking-2026-02-12` 到 anthropic-beta header
  - 在 layer 的 config 处理循环中添加 `extends` 继承逻辑:
    - `baseType = provider.extends`
    - `existing = baseType ? database[baseType] : database[providerID]`
    - 克隆基础提供者的模型，更新 `providerID` 和 `api.npm`/`api.url`
    - `name: provider.name ?? (baseType ? providerID : existing?.name ?? providerID)` — 用户 name 优先
    - `models: inheritedModels` — 使用克隆的模型
    - `modelsDev[lookupProviderID]` — 使用 baseType 查找 modelsDev

### 行为目的
- **extends 继承 invariant**: 当 `provider.extends` 设置时，从基础提供者类型继承模型和默认值
- **name 优先级 invariant**: `provider.name` > `baseType ? providerID : existing?.name` > `providerID`
- **providerID 覆盖**: 继承的模型必须更新 `providerID` 为当前提供者 ID
- **modelsDev 查找**: extends 时从基础提供者类型查找 modelsDev 信息
- **redact-thinking invariant**: claudecode 和 custom anthropic provider 都发送 `redact-thinking-2026-02-12` beta header

### redact-thinking 生命周期
- Patch 13: 添加 `redact-thinking-2026-02-12` 到 claudecode.ts
- Patch 15: 移除 `redact-thinking-2026-02-12` 从 claudecode.ts（同时禁用系统块注释）
- Patch 23: 重新添加 `redact-thinking-2026-02-12` 到 claudecode.ts 和 provider.ts
- Source tip (d0ceb469): 仍然保留 `redact-thinking-2026-02-12` — 从未被再次移除

## 3. 上游 (v1.17.20) 行为

- config/provider.ts 的 `Info` schema 没有 `extends` 字段
- claudecode.ts 的 betas 数组不包含 `redact-thinking-2026-02-12`（patch 15 已移除）
- provider.ts 的 anthropic-beta header 不包含 `redact-thinking-2026-02-12`
- provider.ts 的 config 处理循环直接使用 `database[providerID]`，无继承逻辑
- V2 配置路径从 `packages/opencode/src/config/provider.ts` 迁移到 `packages/core/src/v1/config/provider.ts`
- V2 使用 `ProviderV2.ID.make()` 而非 `ProviderID.make()`

## 4. 目标适配 (current patch)

### 路径迁移
- `packages/opencode/src/config/provider.ts` → `packages/core/src/v1/config/provider.ts`
- `packages/opencode/src/provider/claudecode.ts` → `packages/opencode/src/provider/claudecode.ts`（路径不变）
- `packages/opencode/src/provider/provider.ts` → `packages/opencode/src/provider/provider.ts`（路径不变）

### V2 适配
- `extends` 字段添加到 V2 的 `Info` schema (`packages/core/src/v1/config/provider.ts`)
- `ProviderID.make()` → `ProviderV2.ID.make()`
- `ModelID.make()` → `ModelV2.ID.make()`
- 保留 V2 的 `mergeDeep` 函数调用
- 保留 V2 的 `modelsDev` 查找模式
- 保留 V2 的 `iife` 函数调用

### 保留的 SMARK 语义
- `extends` 字段和继承逻辑完整保留
- `redact-thinking-2026-02-12` 在 claudecode.ts 和 provider.ts 中都添加
- `name: provider.name ?? (baseType ? providerID : existing?.name ?? providerID)` — 修正为 SMARK 的优先级
- `inheritedModels` 克隆逻辑保留
- `modelsDev[baseType ?? providerID]` 查找保留

### 保留的上游优势
- V2 配置路径和 schema 结构
- V2 的 `ProviderV2`/`ModelV2` 命名空间
- V2 的 `mergeDeep` 和 `iife` 工具函数

## 5. E/C 计算

### 有效修改行 E
- config/provider.ts:
  - extends 字段 + 注释 + description: 4 行
  - 小计: ~4 行
- claudecode.ts:
  - redact-thinking 行: 1 行
  - 注释修改: 1 行
  - 小计: ~2 行
- provider.ts:
  - anthropic-beta header 修改: 1 行
  - baseType + existing 修改: 2 行
  - inheritedModels: 3 行
  - name 优先级修改: 1 行
  - models 修改: 1 行
  - modelsDev 修改 (2处): 2 行
  - 中文注释: 5 行
  - 小计: ~15 行
- **总 E ≈ 21**

### 合格中文注释 C
1. `// 继承现有提供者类型的模型和默认值` (config/provider.ts) — 字段用途
2. `// beta 特性列表，重新启用 redact-thinking 隐藏思考内容，按模型动态添加 context-1m` (claudecode.ts) — 修改的注释，解释 redact-thinking 重新启用
3. `// 支持从现有提供者类型继承模型和默认值` (provider.ts) — 继承逻辑入口
4. `// 克隆基础提供者的模型，覆盖 providerID 为当前提供者` (provider.ts) — 克隆策略
5. `// 用户指定 name 优先；extends 时默认用 providerID 而非继承的 name` (provider.ts) — 优先级 invariant
6. `// extends 时从基础提供者类型查找 modelsDev` (provider.ts) — 查找策略

- **总 C = 6**
- **最低 C = max(1, ceil(21 * 0.15)) = 4**
- **6 ≥ 4，通过**

### 注释位置
- config/provider.ts: 注释 1 在 `extends` 字段之前
- claudecode.ts: 注释 2 是修改后的 betas 数组注释
- provider.ts: 注释 3-6 分散在 extends 继承逻辑的关键位置

## 6. 验证

### Apply
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 25 --rebuild`
- 结果: passed 25/25

### Typecheck
- packages/core: passed
- packages/opencode: passed
- packages/tui: passed

### Install 指纹
- `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史

1. **首轮审计 BLOCK (B-02)**: `redact-thinking-2026-02-12` beta header 被错误跳过——声称"conflicts with later removal"但 SMARK source tip 仍然保留 redact-thinking，从未被再次移除
   - 修复: 在 claudecode.ts 的 betas 数组和 provider.ts 的 anthropic-beta header 中都添加 `redact-thinking-2026-02-12`，更新 claudecode.ts 的注释

2. **首轮审计 BLOCK (B-03)**: provider `name` 优先级错误——current patch 使用 `baseType ? providerID : (provider.name ?? existing?.name ?? providerID)` 导致用户指定的 `name` 在 extends 时被忽略
   - 修复: 改为 SMARK 的优先级 `provider.name ?? (baseType ? providerID : existing?.name ?? providerID)` — 用户 name 始终优先

## 8. 最终行为

- `extends` 字段允许从现有提供者类型继承模型和默认值
- `redact-thinking-2026-02-12` 在 claudecode 和 custom anthropic provider 中都发送
- `name` 优先级: 用户指定 > extends 时 providerID > 继承的 name > providerID
- 继承的模型更新 `providerID` 为当前提供者 ID
- `modelsDev` 查找使用 `baseType ?? providerID`
- V2 路径和 schema 结构保留
- 中文注释比例满足 15% 门禁
