# Record 0250-615fcee6765d

## Status
PASS

## 修改内容
feat(reviewer): 添加权限审查服务测试，确保 OAuth 和非 OAuth 请求的行为一致性。

### Source 逐 hunk 行为
- service.ts：H1 导入 Auth/buildBaseProviderMap/isOpenaiOauthProvider；H2 layer 绑定 auth；H3 streamText 请求改 Effect.all 四路并发（getLanguage/getProvider/config.get/auth.get），OAuth 判定 `isOpenaiOauthProvider(providerInfo.id, buildBaseProviderMap(cfg.provider)) && authInfo?.type === "oauth"`，命中时把可信 reviewer 指令（system messages）提升为顶层 `instructions` 并从 messages 剔除——evidence 留在 user message（Codex 后端对缺顶层 instructions 返回 400，而不可信证据不可提升为高优先级指令）；H4 messages 换 reviewerMessages；H5 defaultLayer 提供 Auth。
- 测试（新增 242 行）：三个 fixture（openai+oauth 400-契约 / openai+apiKey / custom+oauth）经真实 fetch 拦截 + OpenAI Responses SSE function-call 流断言 wire body：instructions 有无、system/developer item 剔除、shell 证据逐 token 保留。

### 目标适配（0249 树）
- owner 未迁移：`packages/opencode/src/permission/reviewer/service.ts`（879 行）。
- H1 锚定修正：original 锚文件首行（git apply 不允许 oldpos=1 漂移），目标 L1-2 已被 #203 的 LayerNode/ConfigPermissionV1 导入占据 → 改锚 L7（MessageV2）后插入。
- H2 上下文适配：#208 的 `const session`（原 sessions）改名与注释纳入上下文，auth 绑定插在 config 与 provider 之间。
- H3 重锚：以目标 L372-381 实际行（含两条 #209 注释）为上下文，Effect.all/判定/instructions 提升/reviewerMessages 逻辑原样保留。
- H5 改写：#235 已把 defaultLayer 重写为 `LayerNode.compile(Config.node/Provider.node)` 形态且 Auth.defaultLayer 不存在 → 改为对 **L905 node deps** 加 `Auth.node`（LayerNode 依赖图编译期完整性；FSUtil 由 Config.node 的 deps 链传递满足）。
- 测试：`ModelID/ProviderID`（目标无 src/provider/schema）→ `ModelV2.ID`/`ProviderV2.ID`（core，与 test/fake/provider.ts 先例一致）；`Config.Info` 为模块私有类型 → 去返回标注用结构推断；`approvals_reviewer: "auto_review" as const` 满足字面量联合。

## E/C
- E = 258（plus - commentPlus + minus；含 252 行新测试文件）
- C = 44（`+//` 中文解释性注释全口径：source 自带 27 行 + #250 新增 17 行——覆盖判定矩阵、Codex 400 契约、证据不可提升、SSE 事件序列完整性、LayerNode 依赖图等真实 invariant）
- need = ceil(258 * 0.15) = 39 → 余 +5 PASS

## Source
- original/0250-615fcee6765d.patch: 341 行 2 文件。

## Current
- current/0250-615fcee6765d.patch: 16254 bytes, 6 hunk / 2 文件
- SHA-256: bc615eba9fdb01d89867e2ffed7b72462f210fbf0f9a5b42bfd1e9839870e559

## 安装
- materialize 250 --rebuild: passed 250/250

## 测试
- typecheck 250: 全部 workspace passed
- `bun test --timeout 120000 test/permission/reviewer-service.test.ts`（packages/opencode）: 3 pass / 0 fail

## 修订历史
1. 初版 6 hunk：H1 锚定失败（oldpos=1 不允许漂移）→ 重锚 L7。
2. H3 `language/providerInfo` 两行误写为 ctx（会重复声明）→ 改 del 行（经 3 次缩进试错：Substring 偏移事故）。
3. H3 尾部漏 L382 `#209 wrapLanguageModel` 注释 ctx（序列不连续）→ 补入。
4. TS2322 defaultLayer：Auth.node 缺 FSUtil deps → 弃 defaultLayer provide 方案，改 L905 node deps 加 Auth.node（#198 deps 行替换，带 #198 注释上下文锚；ctx 缩进 2→0 修正）。
5. TS2694 Config.Info 私有 → 结构推断；approvals_reviewer as const。
6. 最终：materialize OK、typecheck OK、3/0 测试、C=44 ≥ 39 → PASS。

## 三方比较
- Source：OAuth/Codex 的 instructions 提升契约 + 三 fixture wire-body 回归。
- Upstream v1.17.20：reviewer 服务为 #209/#235 谱系的流式实现（目标已有 streamText + tool 决策），本 patch 叠加 OAuth 判定分支。
- Target：H3/H5 按目标 #208/#209/#235 谱系重锚与 deps 化；测试按 core ID 类型适配。无行为丢失、无重复 owner。

## 最终行为
- openai+oauth：reviewer 策略进顶层 instructions，system item 剔除，shell 证据保留在 user input。
- openai+apiKey 与 custom+oauth：instructions 缺席，system prompting 保留，证据完整。
- node deps 图包含 Auth.node，LayerNode 编译期依赖完整。
- 3 个 wire-body 回归测试锁定判定矩阵。

## 批次双审结论（2026-09-07，250-254）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：修订历史中「Auth.node 缺 FSUtil deps」措辞应为「Auth.node 的 deps=[FSUtil.node] 经 LayerNode CheckDependencies 类型门由 Config/Provider node 的 deps 链传递满足」；defaultLayer 在目标为无消费者死导出（唯一接线为 app-runtime 的 node 图）。