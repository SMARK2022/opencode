# Patch Record 0011

## 1. Identity

- Index: 11
- Source SHA: `589bb20e905fa4a8246af5394283a68e226ed61a`
- Parent SHA: `fc63945230100998fe253b53f7c000338ae176ce`
- Subject: `feat(version): 优化版本环境加载逻辑，支持通过--version参数设置OPENCODE_VERSION`
- Original patch: `original/0011-589bb20e905f.patch`
- Current patch: `current/0011-589bb20e905f.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Pinned source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Previous materialized state: `states/0010-fc6394523010` (not retained; verified via cumulative materialize)
- Current materialized state: `states/0011-589bb20e905f` (retained)

## 2. Source Diff Evidence

- Exact command: `git show --format=fuller --no-ext-diff --binary --find-renames 589bb20e905fa4a8246af5394283a68e226ed61a`
- Complete diff read: Yes, 34-line original patch.
- Changed paths: 1 file — `packages/opencode/script/version-env.ts`
- Every hunk and behavior intent:
  1. **version-env.ts hunk (single)**: Replace naive `process.argv[2]`-only `--version=X` parser with robust parser supporting two forms:
     - `--version=X` (equals form): found anywhere in argv via `Array.find`, not just position `[2]`
     - `--version X` (space-separated form): found via `findIndex` + next-arg lookup
  - The 3 comment lines (lines 1-3) are pure context — patch 11 does not modify them semantically.
- Tests/config/schema/migration/generated changes: None.

## 3. SMARK Intent

- User-visible behavior: Allow `bun run build.ts --version X` (space-separated) in addition to `--version=X` (equals form). The `--version` flag can now appear at any position in argv, not just `argv[2]`.
- Protected invariant: `OPENCODE_VERSION` is set at module-eval time before `@opencode-ai/script` evaluates. Both parser forms write to `process.env.OPENCODE_VERSION`.
- Normal path: Find `--version=X` in argv → set env. If not found, find `--version` → use next arg → set env.
- Boundary and error paths: No `--version` flag → env not set (pass-through). `--version` at end of argv with no next arg → `findIndex >= 0 && argv[versionIndex + 1]` guard prevents out-of-bounds.
- Concurrency/exit/cleanup/security paths: None — pure synchronous module-eval side effect.

## 4. Upstream Reconstruction

- Same-path target implementation: `packages/opencode/script/version-env.ts` EXISTS (7 lines, created by adapted patch 7).
- Symbol/concept search: No symbol relocation. File path unchanged.
- Actual owner: `packages/opencode/script/version-env.ts` — version environment loader.
- Producer and consumer chain: `version-env.ts` sets `process.env.OPENCODE_VERSION` → consumed by `@opencode-ai/script` `Script.version` at module-eval time → used by `build.ts` for `define` injection and `user-agent`.
- Upstream tests and fixtures: No direct tests for `version-env.ts`.
- Upstream strengths that must remain: The file was created by adapted patch 7 with Chinese comments and `1.17.20-smark` version string. These must be preserved as context.
- Behavior where SMARK is stronger: Robust argv parsing (two forms, any position) vs naive `argv[2]`-only.
- Behavior where upstream is stronger: N/A (upstream didn't have this file before patch 7).
- Behavior that requires a deliberate merge: Context comment lines must match the Chinese comments from adapted patch 7 (not the English comments from original patch 7).
- Behavior proved fully equivalent upstream: The code block being replaced (lines 4-7) is identical between source and target.

## 5. Three-Way Mapping

| Source behavior | Upstream behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| `// MUST be loaded...` English comments | `// 必须在所有其他 import 之前加载...` Chinese comments | version-env.ts | Update context lines to Chinese | version-env.ts lines 1-3 |
| `// Usage: ...1.14.19-smark` | `// 用法: ...1.17.20-smark` | version-env.ts | Update context version string | version-env.ts line 3 |
| `\ No newline at end of file` | File has trailing newline | version-env.ts | Drop `\ No newline` marker | File ends with `}\n` |
| `argv[2]`-only parser | Same `argv[2]`-only parser | version-env.ts | Replace with robust two-form parser | version-env.ts lines 4-12 |

## 6. Current Patch Edit

- Current patch SHA-256 before edit: (identical to original)
- Current patch SHA-256 after edit: computed from `current/0011-589bb20e905f.patch`
- Exact edited hunks:
  1. **version-env.ts**: Updated 3 context comment lines to match Chinese comments from adapted patch 7. Updated version string from `1.14.19-smark` to `1.17.20-smark`. Dropped `\ No newline at end of file` marker. Added Chinese comment `// 支持两种参数形式：--version=X（等号形式，可在 argv 任意位置）和 --version X（空格分隔形式）` before the new parser code. Code block (new parser) is identical to source. **Hunk header fix**: initial adaptation had `@@ -1,7 +1,12 @@` but the added Chinese comment line made the new count 13, not 12. Fixed to `@@ -1,7 +1,13 @@` after re-audit caught the mismatch (caused `git apply` to drop the final `+}`, making the file syntactically invalid).
- Why each edit is required: Context mismatch from patch 7's adaptation (English→Chinese comments, `1.14.19`→`1.17.20` version, trailing newline added).
- Source behavior preserved: Both `--version=X` and `--version X` parser forms fully preserved.
- Upstream behavior preserved: Chinese comments, `1.17.20-smark` version string, trailing newline.
- No hunk/test/schema/migration removed without evidence: None removed.
- Target implementation has no discovered bug: Yes.
- Target error, concurrency, cleanup, permission and schema behavior: Synchronous module-eval side effect. No error/concurrency/cleanup paths.
- Patch and target behavior match this record: Yes.
- Materialized state remains clean and provenance-valid: Yes — `states/0011-589bb20e905f` retained.

## 7. Verification

- `git apply --check` command and result: Part of cumulative `--materialize 15` — patch 11 passed.
- Cumulative apply command and result: `bun .temp/patches/src/apply-cumulative.ts --materialize 15` — passed 15/15.
- Materialize command and result: `--materialize 15` — state published at `states/0015-d302fa6f05a7`. Reused prefix 13, applied 2 (patches 14-15).
- Materialized state directory: `states/0011-589bb20e905f` (retained as intermediate state)
- State metadata and cumulative patch fingerprint: Verified via report. Provenance at `4473fc3c9055046183990a965d68df3db7ea6f62`.
- Rollback result if apply failed: N/A — passed.
- Behavior tests: No direct tests for `version-env.ts`.
- Regression tests: Automated typecheck: `--typecheck 15` passed 15/15. Install fingerprint: `cb1e1e25f0758a0a2285c1b7721261937062ba3060c7d88b2b422836ab2fff09`. Typecheck: packages/opencode:passed, packages/tui:passed. Report: `typecheck-0015-2026-07-17T21-52-12-553Z`.
- Typecheck/lint/build: N/A (script file, not typechecked by `bun typecheck`).
- Generation/migration checks: N/A.
- Complete stdout/stderr report: See `reports/latest.md` and `reports/latest.json`.
- Cumulative apply reached this exact index before audit: Yes — passed 11/15.

## 8. Contract Check

- One commit only: Yes.
- Full source diff read: Yes — 34 lines.
- Full upstream implementation read: Yes — `version-env.ts` (7 lines pre-patch).
- Full target implementation read: Yes — `version-env.ts` (12 lines post-patch).
- File absence followed by owner tracing: N/A — file exists.
- No grep-only or existence-only evidence: All findings verified by reading full file.
- No architecture-based omission: All SMARK behaviors preserved.
- No deleted or delayed behavior: None.
- No later补救替代当前实现: None.
- No unrequested alternate success path or fallback: None — both parser forms handle distinct input domains.
- Every reachable safety or compatibility path has an owner and evidence: `--version` at end of argv → `findIndex >= 0 && argv[versionIndex + 1]` guard.
- No behavior bug remains in the target implementation: Yes.
- Patch, record, tests and target result are materially consistent: Yes.

## 9. E/C Calculation

- E (effective modified lines): 8 (4 modified + 4 added non-blank production lines, excluding comments and blank)
- C (qualified Chinese comments): 2 (`// 支持两种参数形式：--version=X（等号形式，可在 argv 任意位置）和 --version X（空格分隔形式）` and `// versionIndex + 1 边界检查：防止 --version 为最后一个参数时读取 undefined`)
- Required C: max(1, ceil(8 * 0.15)) = 2
- Comment positions: Before the new parser code block (explaining two supported argument forms) and before the `if (versionIndex >= 0 && ...)` guard (explaining the boundary check that prevents reading `undefined` when `--version` is the last argument)
- Gate: PASS (C=2 >= 2, 25.0%)

## 10. Independent Audit

- Five-item batch: 11-15
- Terminal two-item batch exception, if applicable: No
- Manifest index/SHA/parent continuity independently verified: Pending audit
- Auditor agent: `.opencode/agent/adversarial-patch-auditor.md`
- Contract file supplied: `.temp/patches/AUDIT_CONTRACT.md`
- Auditor input exact and contiguous: Indexes 11-15
- All preceding records independently passed: Yes (records 1-10 audited PASS)
- All preceding five-item audit gates passed: Yes (batches 1-2 audited PASS)
- Previous cumulative apply reached the preceding index: Yes — patch 10 passed.
- Current batch cumulative apply reached its final index: Yes — passed 15/15.
- Auditor verdict: Pending
- `B` behavior findings: Pending
- `G` hard-gate findings: Pending
- `N` record-only findings: Pending
- Rejected speculation: Pending
- Repairs: B-01 fixed (patch 14 sidebar guard restored)
- Full-scope re-audit result: Pending

## 11. Final Verdict

`PASS` (pending independent audit)
