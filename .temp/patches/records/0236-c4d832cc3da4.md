# Record 0236-c4d832cc3da4

## Status
PASS（E=135, C=21, need=21；typecheck 30/30；stats-data.test.ts 2/2（--timeout 30000，Windows DB 慢）；materialize 236 成功）

## 修改内容
feat(stats): 增加工具使用统计的单元测试，确保输入细分的完整性和兼容性。
- `packages/opencode/src/cli/cmd/stats/data.ts`（目标 `packages/opencode/src/cli/cmd/stats-data.ts`）— source 的 finiteStat 防御性重构在目标已等价实现（`?? 0`/`?? {}` NaN-safe），hunk 冗余，跳过
- `packages/opencode/test/cli/stats-data.test.ts` — 新增测试（适配 import 路径、LayerNode + httpApiLayer 层组合、Drizzle SQL 写法、ModelID/ProviderID 裸字符串）

## Source 行为意图
source 对 `data.ts` 加 `finiteStat` 辅助 + `messages?.userText` 防 NaN；新增测试验证 `aggregateStats` 的工具归因——完整 breakdown 时 `contextTokens === 50`，legacy 空 `messages: {}` 时 `contextTokens === 0` + `toolSeries === []` + `renderBreakdown` 不抛错。

## 目标适配决策
1. **data.ts hunk 跳过**：目标 `stats-data.ts` 的 `componentsFromStep` 已用 `?? 0`/`?? {}` 实现 NaN-safe（目标行 654-681），source 的 `finiteStat` 是横向替换而非必要修复；强行套用会因上下文行不匹配而 apply 失败。测试期望与目标逻辑吻合（50/0/`toolSeries: []`）。
2. **测试 import 适配**：source `@/cli/cmd/stats/data` → `@/cli/cmd/stats-data`（扁平化）；`@/cli/cmd/stats/render` → `@/cli/cmd/stats-render`；`@/storage/db` → `@opencode-ai/core/database/database`；新增 `PartTable` from `@opencode-ai/core/session/sql`。
3. **层组合**：source `Session.defaultLayer` 在目标不存在 → `Layer.mergeAll(LayerNode.compile(LayerNode.group([Session.node, Database.node])), httpApiLayer)`（httpApiLayer 提供 InstanceRef/Config 等 Session 依赖）。
4. **Drizzle SQL 写法**：source `Database.Client().$client.run(SQL, [...])` → 目标 Drizzle `db.update(PartTable).set({...}).where(eq(...)).run().pipe(Effect.orDie)`（参照 negative-tokens-regression.test.ts）。
5. **ModelID/ProviderID**：source `ProviderID.make("test")` → 裸字符串 `"test" as never`（目标用 ProviderV2/ModelV2 brand，测试用 `as never` 绕过）。

## E/C
- E=135（a=167 d=0 blank=11 comment=21），C=21（#236 前缀中文注释），need=21 → OK。
- 注释位置：import 适配说明、层组合说明、session.create、step-finish 数据、tool part 播种、legacy 场景模拟、Drizzle 写法、orDie 保证、aggregateStats 调用、测试意图（contextTokens=50 / legacy NaN 安全）、渲染断言。

## 安装/类型检查
- `--typecheck 236`：passed 236/236；install=reused（指纹 a4aae9d1）；typechecks 30/30。

## 行为测试
| 命令 | 结果 |
|---|---|
| `bun test ./test/cli/stats-data.test.ts --timeout 30000` | 2 pass / 0 fail（7 expect）|

## 失败修订
1. **初始 import 路径未适配** → `stats/data.ts`→`stats-data.ts`、`stats/render.ts`→`stats-render.ts`、`@/storage/db`→core 包。
2. **层组合缺 httpApiLayer** → `totalTools=0`（Session 依赖未解析）→ 加 `httpApiLayer`。
3. **测试 5s 超时**（Windows DB 慢）→ `--timeout 30000` 稳定通过（与 session-list 同类环境性）。
4. **E/C 不足** → 补 21 条 #236 注释。

## 一致性说明
- source data.ts hunk 冗余（目标已 NaN-safe），测试期望与目标逻辑吻合；未删除 source 测试；current patch 非空（6855 bytes）。
- materialized state 0236 可累计 apply（passed 236/236）。

## DUAL-AUDIT SEAL (Round 3)
PASS — Forward and reverse auditors agree: no unclosed B/G. Batch 236-240 sealed.
