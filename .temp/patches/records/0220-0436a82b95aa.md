# Patch 0220 — feat: precheck增强，enhance permission precheck tests with new classifications and scenarios

## 1. Identity
- Index: 220
- Source SHA: 0436a82b95aaec4be075ff898984a68a8c8cecbc
- Parent SHA: dddd6516ffadfbd68734622a63e09cf94f2df731（patch 219）
- Subject: feat: precheck增强，enhance permission precheck tests with new classifications and scenarios
- Original patch: `.temp/patches/original/0220-0436a82b95aa.patch`（104855 字节，1968 行）
- Current patch: `.temp/patches/current/0220-0436a82b95aa.patch`（950 行）
- Current patch SHA-256: `d967de2cae68b7c6245c04df029e9c44788dd76693265ae94928dc83629e5d7a`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`（v1.17.20 release）
- Built-on state: `0217-5aff2e10e3e9`（patches 1-217 applied；patches 218-219 不触及本 patch 2 个目标文件）
- Record path: `.temp/patches/records/0220-0436a82b95aa.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 2 files：
  1. `packages/opencode/src/permission/precheck.ts` — 近乎完全重写：新增设计哲学头部注释块；新增 SYSTEM_DESTRUCTIVE_COMMANDS/USER_ACCOUNT_COMMANDS 常量集合；拆分 SENSITIVE_PATH_PATTERN 为 CORE（不含 .pem/.key）+ full（含 .pem/.key），新增 SENSITIVE_PATH_LOCAL_ARGUMENT_PATTERN；新增 29 个预编译正则常量（RE_D_* dangerous + RE_C_* cautious）；dangerousRaw 重写为预编译正则版本，新增 decode-pipe、authorized_keys、sudoers、chmod setuid、iptables flush、ufw disable、kill -9 -1、扩展 reverse shell 等模式；cautiousRaw 重写为预编译正则版本，新增 shell RC 写入、git hook 写入、cron/systemd 写入、schtasks、Register-ScheduledTask、echo pipe to interpreter 等模式；rawExecutableMatch 提取 isShellActive 辅助函数；splitCommands/tokenize POSIX 单引号反斜杠修复；unwrap cmd /c 改为 `tokens.slice(index + 1).join(" ")`，移除 bun x 检查（移至 classifyTokens）；riskyTokens 重命名并扩展为 classifyTokens（新增 15+ 个威胁类别：系统破坏、权限变更、特权升级、用户管理、防火墙、服务管理、进程终止、定时任务、注册表、网络监听、HTTP 服务器、包管理器安装、包执行器等）；新增 classifyGit 替代 cautiousGitSubcommand；safeTokens bun 总是 false；BANNED_AUTO_ALLOW_PREFIXES 新增 npx/pipx/uvx；protectedDeleteTarget 扩展为 POSIX 系统根目录 + macOS 特有目录；hasSensitivePath 使用 isSensitiveKeyFile 上下文感知 .pem/.key 检测。
  2. `packages/opencode/test/permission/precheck.test.ts` — 翻转 bun x cowsay 期望（general→cautious）；canAlwaysAllowPrefix 新增 npx/pipx/uvx/bun x 断言；追加 ~320 行新测试覆盖所有新增分类场景。
- 每个 hunk 与行为意图：
  - 预编译正则常量：模块加载时编译一次，避免热路径重复编译。RE_D_* 使用 regex literal `/.../i` 形式，反引号用 `\x60` 转义避免 template literal 冲突。
  - dangerousRaw 重写：所有 inline regex 替换为预编译常量引用，新增 decode-pipe（base64/openssl/xxd/gunzip 管道到解释器）、authorized_keys 写入、sudoers 修改、chmod setuid、iptables flush、ufw disable、kill -9 -1、扩展 reverse shell（bash >&/dev/tcp、mkfifo、PowerShell TCPClient）。
  - cautiousRaw 重写：新增 shell RC 写入、git hook 写入、cron/systemd 写入、schtasks 创建、Register-ScheduledTask、echo pipe to interpreter。使用不含 .pem/.key 的窄版 SENSITIVE_PATH_LOCAL_ARGUMENT_PATTERN 减少误报。
  - classifyTokens 扩展：按威胁类别组织——文件删除/移动、原始磁盘写入、系统破坏、Git 操作（委托 classifyGit）、权限变更（setuid dangerous）、特权升级（visudo/setcap）、用户管理、防火墙（flush/disable dangerous）、服务管理（mask dangerous）、进程终止（kill -9 -1 dangerous）、定时任务、注册表（Run/RunOnce dangerous）、网络监听、HTTP 服务器、包管理器安装、包执行器、远程文件传输。
  - isShellActive 提取：从 rawExecutableMatch 中提取，判断给定位置是否处于 shell 可执行上下文（无引号=可执行，单引号=字面量，双引号=$()/`/$ 活跃）。
  - POSIX 单引号反斜杠修复：splitCommands 和 tokenize 中 `if (char === "\\")` 改为 `if (char === "\\" && quote !== "'")`，POSIX 单引号内反斜杠为字面量。
  - cmd /c join：从 `tokens[index + 1].includes(" ") ? ... : joinShellTokens(...)` 简化为 `tokens.slice(index + 1).join(" ")`，统一使用空格拼接以保留内容完整性。
  - bun 移至 classifyTokens：unwrap 中 bun x 不再 ask（general），改为 classifyTokens 中 cautious。safeTokens 中 bun 总是 false。
  - protectedDeleteTarget 扩展：新增 POSIX 系统根目录（/usr、/var、/boot、/opt、/root、/home、/lib、/lib64、/bin、/sbin、/sys、/proc、/dev）和 macOS 特有目录（/Library、/Applications、/System、/Users），以及 Windows 系统目录（C:\Windows、C:\Program Files、C:\Users）。
  - isSensitiveKeyFile：.pem/.key 文件仅在路径包含 ssl/tls/cert/pki/private/secret/.ssh/.gnupg 时才判定为密钥文件，减少 i18n key 文件、配置模板等开发文件的误报。

## 3. SMARK Intent
- 用户可见行为：文件删除/移动/权限变更/包安装/包执行器等操作路由到 reviewer（cautious）；系统破坏/防火墙清空/反弹 shell/后门持久化等操作直接拒绝（dangerous）；.pem/.key 文件仅在安全上下文中判定为敏感；POSIX 单引号内反斜杠正确处理为字面量。
- 受保护不变量：包装器永不 safe；dangerous 短路；splitCommands 遇未建模语法降级；预编译正则在模块加载时一次编译。
- 正常路径：命令 → dangerousRaw（预编译 RE_D_* 短路 dangerous） → cautiousRaw（预编译 RE_C_* + rawExecutableMatch 文件删除/移动 + 持久化写入） → rawWrapperScripts 递归 → splitCommands → evaluateCommand → classifyTokens（15+ 威胁类别） → safeTokens。
- 边界路径：splitCommands 遇重定向返回 undefined → cautiousRaw 的 RE_C_* 模式在 raw 层捕获持久化写入。

## 4. Upstream Reconstruction
- 同路径目标实现：2 个文件在 tip@217 均存在于相同路径。
- precheck.ts 关键差异（adaptation 触发点）：
  1. 原始 patch 220 基于原始 post-217 状态（英文注释 + [local-smark] 标记），目标为适配后 post-217 状态（#NNN 中文注释）——全部适配为 #220 中文注释。
  2. 原始 patch 使用 `String.raw` + `+ "`" +` 字符串拼接处理反引号——适配为 regex literal `/.../i` + `\x60` 转义，避免 template literal 冲突。
  3. 原始 patch 的 dangerousRaw/cautiousRaw 是 inline regex——适配为预编译 RE_D_*/RE_C_* 常量引用。
  4. 原始 patch 的 riskyTokens 是独立函数——适配为 classifyTokens（重命名 + 扩展），新增 classifyGit 替代 cautiousGitSubcommand。
  5. 原始 patch 的 hasSensitivePath 直接判断 .pem/.key——适配为 isSensitiveKeyFile 上下文感知判断。
  6. 原始 patch 的 protectedDeleteTarget 使用 POSIX_ROOT_ALTERNATION 变量——适配为 inline posixRoots Set。
- precheck.test.ts 关键差异：原始 patch 基于原始 post-217 测试文件——目标已有 #217 适配的测试，本 patch 在其基础上翻转 bun x 期望并追加新测试。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@217) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| 设计哲学头部注释块 | 无 | precheck.ts line 1 | 插入 #220 注释块 | current patch diff |
| SYSTEM_DESTRUCTIVE/USER_ACCOUNT 常量 | 无 | FILE_MOVE_COMMANDS 后 | 插入常量 + #220 注释 | current patch diff |
| SENSITIVE_PATH_CORE/FULL/LOCAL 拆分 | 单一 SENSITIVE_PATH_PATTERN | SENSITIVE_PATH_PATTERN 行 | 拆分为 3 个模式 + #220 注释 | current patch diff |
| 29 个预编译 RE_D_*/RE_C_* 常量 | 无 | RAW_FILE_MOVE_PATTERN 后 | 插入常量块 + #220 注释 | current patch diff |
| dangerousRaw 预编译重写 | inline regex | dangerousRaw 函数体 | 替换函数体 + #220 注释 | current patch diff |
| cautiousRaw 预编译重写 | inline regex | cautiousRaw 函数体 | 替换函数体 + #220 注释 | current patch diff |
| rawExecutableMatch + isShellActive | inline 逻辑 | rawExecutableMatch 函数 | 替换 + 新增 isShellActive | current patch diff |
| splitCommands POSIX 修复 | `if (char === "\\\\")` | splitCommands | 加 `&& quote !== "'"` | current patch diff |
| tokenize POSIX 修复 | `if (char === "\\\\")` | tokenize | 加 `&& quote !== "'"` | current patch diff |
| unwrap cmd /c join | `tokens[index+1].includes(" ")?...` | unwrap cmd 分支 | 改为 `tokens.slice(index+1).join(" ")` | current patch diff |
| unwrap 移除 bun x | `if (cmd === "bun" && tokens[1] === "x")` | unwrap | 替换为注释 | current patch diff |
| classifyTokens 扩展 | riskyTokens | riskyTokens 函数 | 替换为 classifyTokens + classifyGit | current patch diff |
| cautiousGitSubcommand 移除 | 存在 | classifyGit 后 | 删除 | current patch diff |
| safeTokens bun false | 无 | safeTokens npm 行后 | 插入 `if (cmd === "bun") return false` | current patch diff |
| hasSensitivePath + isSensitiveKeyFile | 直接 .pem/.key 判断 | hasSensitivePath 函数 | 替换为 isSensitiveKeyFile 上下文感知 | current patch diff |
| protectedDeleteTarget 扩展 | 仅 /etc + $env:SystemDrive | protectedDeleteTarget | 扩展为 POSIX 系统根 + macOS + Windows | current patch diff |
| BANNED_AUTO_ALLOW npx/pipx/uvx | 无 | rsync 后 | 追加 5 个前缀 | current patch diff |
| test bun x flip | general | line 52 | 改为 cautious + #220 注释 | current patch diff |
| test ~320 新测试 | 无 | 文件末尾 | 追加 + #220 注释 | current patch diff |

## 6. Current Patch Edit
- Current patch SHA-256: `d967de2cae68b7c6245c04df029e9c44788dd76693265ae94928dc83629e5d7a`
- 各编辑（2 files）：
  1. `packages/opencode/src/permission/precheck.ts`：语义重建（非逐行 hunk 应用）+ 97 条 `#220` 中文注释（头部设计哲学 4 条、新常量 2 条、敏感路径拆分 3 条、预编译正则 2 条、dangerousRaw 重写 10 条、cautiousRaw 重写 6 条、rawExecutableMatch/isShellActive 2 条、splitCommands/tokenize POSIX 2 条、unwrap cmd /c + bun x 2 条、classifyTokens 30+ 条覆盖 15 个威胁类别、classifyGit 3 条、safeTokens bun 1 条、hasSensitivePath/isSensitiveKeyFile 3 条、protectedDeleteTarget 5 条、BANNED_AUTO_ALLOW 1 条）。
  2. `packages/opencode/test/permission/precheck.test.ts`：翻转 bun x 期望 + canAlwaysAllowPrefix 新增 5 条 + 追加 ~320 行新测试（15 个新 test block 覆盖解码管道、后门写入、RC 文件、git hooks、cron/systemd、setuid/setgid、权限变更、用户管理、防火墙、服务管理、进程终止、定时任务、注册表、网络监听、HTTP 服务器、包安装、包执行器、远程传输、保护根扩展、.pem/.key 上下文感知、echo 管道、反弹 shell 扩展、系统破坏命令、tokenizer 修复、cmd /c 完整性、git 状态变更）+ 20 条 `#220` 中文注释。
- Adaptation 决策：
  - 反引号处理：原始 patch 使用 `String.raw` + `+ "`" +` 字符串拼接——适配为 regex literal `/.../i` + `\x60` 转义，避免 template literal 冲突且更简洁。3 个含反引号的 RE_D_* 常量（RE_D_RM_RF_ROOT、RE_D_WINDOWS_PROTECTED_DELETE、RE_D_PS_RECURSIVE_DELETE_ROOT）使用此方式。
  - 注释风格：原始 patch 的 `// ============================================================` 分节注释和英文注释全部适配为 `// #220：` 中文注释。
  - classifyTokens 语义重建：原始 patch 逐 hunk 修改 riskyTokens——适配为完整函数替换（语义重建），保持所有行为意图但结构更清晰。
  - protectedDeleteTarget POSIX_ROOT_ALTERNATION：原始 patch 使用预编译变量——适配为 inline posixRoots Set（更简洁且避免额外全局变量）。
  - cautiousGitSubcommand → classifyGit：原始 patch 删除 cautiousGitSubcommand 并在 classifyTokens 中内联 git 逻辑——适配为独立 classifyGit 函数（更清晰且可测试）。
- Source behavior preserved：预编译正则模块加载时编译一次、dangerousRaw 新增 7 类 dangerous 模式、cautiousRaw 新增 6 类 cautious 模式、classifyTokens 15+ 威胁类别分类、isShellActive 引号感知、POSIX 单引号反斜杠字面量、cmd /c 空格拼接、bun x cautious、protectedDeleteTarget POSIX 系统根扩展、isSensitiveKeyFile 上下文感知 .pem/.key 检测、safeTokens bun false、BANNED_AUTO_ALLOW npx/pipx/uvx。
- Upstream behavior preserved：#197/#202/#203/#209/#217 既有注释和逻辑不变（仅在被替换的函数中更新）；SENSITIVE_PATH_ARGUMENT_PATTERN 结构不变（仅拆分出 CORE）；gitSafe/gitBranchSafe/unsafeRipgrepFlag/versionSafe/readsSensitivePath 不变；normalizeCommandName/joinShellTokens/decodePowerShell 不变。

## 7. Verification
- `git apply --check --index` against state 0217 working tree：PASS（exit 0）。
- Materialize：`bun .temp/patches/src/apply-cumulative.ts --materialize 220` → passed 220/220。
- Effective changed lines `E`（non-blank added lines，含代码与英文注释，排除纯空行）：E=532。
- Qualifying distributed Chinese comments `C`（`#220`-tagged 且含 CJK 的 added lines）：97。
- Required minimum `max(1, ceil(E * 0.15))`：max(1, ceil(532 * 0.15)) = max(1, ceil(79.8)) = max(1, 80) = 80。
- Gate：97 ≥ 80 → PASS（margin 17）。
- 注释分布：precheck.ts 77 条（跨头部设计哲学、新常量、敏感路径拆分、预编译正则、dangerousRaw/cautiousRaw 重写、rawExecutableMatch/isShellActive、splitCommands/tokenize POSIX 修复、unwrap cmd /c + bun x、classifyTokens 15 个威胁类别、classifyGit、safeTokens bun、hasSensitivePath/isSensitiveKeyFile、protectedDeleteTarget 等决策点）、precheck.test.ts 20 条（15 个新 test block 的验证推理 + bun x flip + canAlwaysAllowPrefix 扩展）。
- Typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 220` → 全部 30 个 workspace passed（含 packages/opencode）。

## 8. Contract Check
- 单 commit：是。
- 完整 source diff 已读：是（2 files，1968 行）。
- 完整 upstream 实现已读：是（2 个目标文件相关区段，897 行 precheck.ts + 268 行 precheck.test.ts）。
- 文件缺失后 owner 追踪：N/A（2 文件均存在于目标）。
- 无 grep-only 证据：是。
- 无架构式省略：是（全部 precheck.ts 重写 + test 追加完整移植）。
- 无删除或延迟行为：是。
- 无未请求的备用成功路径或 fallback：是。
- 每条可达路径有 owner：是。
- 无残留行为 bug：是。
- 每 patch 中文注释 gate 通过：是（C=97 ≥ 80）。

## 9. Independent Audit
- Pending（累计 audit 待后续批次）。

## 10. Final Verdict

`PASS`（build）— patch built，`git apply --check` PASS on state 0217，materialize PASS 220/220，typecheck PASS 全部 30 workspace，E/C gate PASS（E=532，C=97，need=80，margin 17）。

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T08:04:17Z
- FORWARD: BATCH PASS (ses_fwd_216_220_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_220_216_v2) - no open B/G
- Tip: states/0220-0436a82b95aa
- Typecheck: typecheck-0220-2026-07-27T07-43-08-935Z.md
- Install: 6caf0c192b005ee2771bebc192462cc82c634b63eda1d115a6fa95da3a14fddb
- Non-blocking N only (do not reopen seal)
