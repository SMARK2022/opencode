# Record 0410-4498da0c374e

## Identity
- SHA: 4498da0c374e
- Parent: 80b91422bd4f (0409)
- Subject: docs(read): 增加 read stub 分析以及 models 快照文件
- Status: PASS

## Source intent
source 0410 包含两部分：
1. 新增 `docs/read-stub-mechanism-forensic-report.md`（557 行）：对 read 工具 stub/suppress 机制的取证分析报告——83 次 retry_same_range 死循环、stub 强语气 "do NOT re-read" 有害性、sed/Get-Content 旁路绕过 visibleReads 追踪、版本键 size+mtime(ms) 脆弱性等 27 个问题项分析。文档仅分析，不改源码。
2. 更新 `packages/core/src/models-snapshot.js`：模型快照 provider 从 145 个扩展到 154 个（新增 9 个 provider：tencent-token-plan/sakana/longcat/crossmodel/subconscious/zenifra/meta/kenari/trustedrouter；更新 15+ 已有模型条目：zenmux/nvidia/baseten/llama/submodel/opencode 等）。

## Three-way comparison
| 维度 | Source (SMARK) | Upstream | Target |
|---|---|---|---|
| 文档 | docs/read-stub-mechanism-forensic-report.md（新增） | 无 | 新增（verbatim 557 行） |
| 快照 | models-snapshot.js 145→154 模型 | 目标 v1.17.20 快照 135 模型（含上游演进） | 135→155 模型（合并） |

## Target adaptation
- **文档**：`docs/read-stub-mechanism-forensic-report.md` 新增文件，verbatim 复制（557 行，blob 哈希匹配）。.md 排除于 E/C。
- **快照语义合并（关键适配）**：source 0410 的 models-snapshot.js 是**整行 JSON 替换**（5.4MB 单行），pre-image 为 source 的 145 模型快照。目标快照（state 0408，135 模型）与 source pre-image 不同（目标含上游演进模型 + 0349 加入的 glm-5.2），直接 apply source hunk 会失败（pre-image 哈希不匹配）。
  - 修复：语义合并——以目标 135 模型为基础，吸收 source 0410 post-image 的全部更新（共有键用 post 值覆盖，source 新增的 9 个模型加入），保留目标特有模型（firepass 等），生成 155 模型合并快照。
  - 合并脚本：解析 source pre/post JSON + 目标 JSON → `merged = dict(target); for k,v in post: merged[k]=v` → 重新序列化 → 从 state repo 生成 git diff（5.1MB）→ 替换 current patch 中的 models-snapshot hunk。
  - 新增 CJK 注释行说明合并语义（E/C 门禁：快照行计 E=1，注释计 C=1）。

## Per-hunk mapping
| # | 文件 | Hunks | 状态 | 说明 |
|---|------|-------|------|------|
| 1 | docs/read-stub-mechanism-forensic-report.md | 1 (new 557 行) | APPLIED | verbatim 新增 |
| 2 | packages/core/src/models-snapshot.js | 1 (整行替换) | APPLIED | 语义合并：135→155 模型（吸收 source 9 新增 + 15 更新，保留目标特有） |

## Verification
- `--materialize 410`：passed 410/410（初始 410 失败：models-snapshot pre-image 不匹配——source 145 模型 vs 目标 135 模型；修复为语义合并后通过）
- `--typecheck 410`（批次末）：全 workspace PASS（models-snapshot 仅数据，不影响类型）
- E/C：E=1, C=1, need=1 PASS（快照行 + CJK 注释）
- 快照数据仅被运行时只读引用（build.ts 生成，models 目录加载），合并语义不改变任何模型能力，只扩充模型集。

## 不一致
1. **快照合并**：source 0410 整行替换（145→154），目标为 135 模型 → 语义合并为 155 模型（目标 135 + source 新增去重）。目标特有模型（firepass 等）保留，source 更新条目值覆盖。这是两快照演进差异的必然适配，非语义丢失。
2. **注释行**：合并快照 hunk 头部加入 CJK 注释行说明合并来源（生成文件下次 build.ts 覆盖，无持久影响）。

## 最终行为
- 新增 read stub 取证分析文档（27 问题项分析，供后续修复参考）。
- models 快照扩充：新增 tencent-token-plan/sakana/longcat/crossmodel/subconscious/zenifra/meta/kenari/trustedrouter 等模型条目，更新 15+ 已有模型的能力参数。

## 详细验证记录（模型快照合并）
- **source pre/post 解析**：original/0410 models-snapshot.js 段为整行 JSON 替换（regex 提取 `-export const snapshot = {...}` / `+export const snapshot = {...}`），pre=145 模型、post=154 模型
- **新增 9 模型**：tencent-token-plan, sakana, longcat, crossmodel, subconscious, zenifra, meta, kenari, trustedrouter（source post 有而 pre 无）
- **更新模型**：zenmux, nvidia, baseten, llama, submodel, opencode 等 15+ 条目（pre/post 值不同）
- **目标快照**：state 0408（0409 未改此文件）135 模型，含上游演进条目与 0349 加入的 glm-5.2
- **合并结果**：155 模型 = 目标 135 全部保留 + source post 覆盖共有键 + source 新增 9 个；目标特有 firepass 等保留
- **合并脚本**：parse source pre/post JSON + target JSON → dict merge → 重新序列化（ensure_ascii=False, separators=(',',':')）→ 写回 state repo → git diff 生成 5.1MB hunk → 替换 current patch 该文件段
- **materialize 验证**：首轮 410 失败（"while searching for: // @ts-nocheck..."——pre-image 不匹配），修复后 410/410 passed
- **typecheck 410**：全 workspace PASS；models-snapshot 为只读数据文件，不参与类型检查
- **E/C**：E=1（快照行）/C=1（#410 合并说明注释）/need=1 PASS；.md 文档部分整体排除

## 风险与边界
- 快照合并可能引入 source 未审核的模型参数差异（如 token 价格/上下文窗口）——但合并遵循"以 source post 为最终值"原则，语义与 source 0410 一致；目标特有模型不受影响
- 生成文件注释行在下次 build.ts 重新生成时被覆盖（预期行为）

## 记录修正历史
- 初始占位符（子代理中断遗留），批次验证前补全
- 0409 的 docs 改动与 0410 无交互；本记录与 0349（glm-5.2 加入）衔接——0349 已把 glm-5.2 写入目标快照，0410 合并时 glm-5.2 在目标中已存在，未被 source post 覆盖删除（source post 无 glm-5.2 删除），保留一致

## B-0410-01 修复（嵌套 union 合并，Round 2 正向审计 BLOCK）
**问题**：第一版合并采用顶层 dict 覆盖（shared top keys replaced wholesale），虽保留 135 个 provider 键，但共享 provider 的 models 嵌套被整体替换——丢失 541 个 target 提供商作用域模型条目（308 个全局唯一模型 ID，如 claude-opus-4-0、claude-sonnet-4-20250514），8286 个叶子内容为 target-only。record 原称"保留目标特有模型、只扩充模型集"与事实不符。
**修复**：改为嵌套 union 合并：
- provider 标量字段（id/env/npm/api/name/doc）取 source post 值（应用 source 更新）
- models 字典：以 target 为基础，source post 覆盖同 id、添加新 id → 目标模型条目全部保留
- 仅 target 有的 provider（如 firepass）保留；仅 source 有的 provider 添加
**验证**（物化 state 0410 独立核验）：155 providers / 5969 model entries（target 4850 全保留 + source 5427 全保留，交集去重）；target model ids lost = 0；post model ids missing = 0, value differs = 0；claude-opus-4-0 在 anthropic、claude-sonnet-4-20250514 在 302ai 均找到；`--materialize 410` passed 410/410；`--typecheck 410` 30/30 workspaces PASS；precheck 91/91。

## Verdict: BATCH 0406-0410 CLOSED
- R2 复审双审计（正向 + 逆向，R2 修复后全新独立运行）一致 APPROVE：0406-0410 各 5/5 PASS，无未关闭 B/G。
- 0406 B-01（regex 缺括号）与 0410 B-0410-01（嵌套 union 合并保留目标模型）均已验证修复：precheck 91/91、typecheck 410 30/30 PASS、target model ids lost=0。
- 审计 N 级项（N-01..N-07）均已核对：stale index hash 无功能影响（git apply 忽略）；0410 术语"9 个模型→9 个 provider"已修正；0409 "不机械 apply"措辞已修正；0406 字节数 11150 已修正。
- 后续批次从 0411 开始。
