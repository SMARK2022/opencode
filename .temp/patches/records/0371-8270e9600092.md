# Record 0371-8270e9600092

## Source patch

- Source: `feat(docs): 添加调研方案输出指南，强调完整性与可执行性` (SMARK2022, 2026-07-04)
- Original: `.temp/patches/original/0371-8270e9600092.patch` (127 lines, 1 file)
- Replay index: 0371, sha 8270e9600092

## Port decision summary

The upstream commit moves the「只调研，不改代码，输出完整方案」research-plan block from the
middle of `docs/prompts.md` (after the TDD section, just before「## 创建 commit」) to the top of
the file — directly after the first `-------------` separator that follows the intro sentence.
The move is performed as delete-then-insert of the identical 42-line section text plus the
trailing separator, so the net content delta is a pure reordering; the section text itself is
preserved verbatim (byte-identical lines, including the blank-line layout around the closing
`-------------`).

docs/prompts.md is documentation content; per the E/C gate policy, `.md` files are excluded from
both E and C counting, so no comment-ratio obligation applies to this item.

## State analysis (state 0370)

- File exists: `docs/prompts.md` (112 lines).
- Section「## 只调研，不改代码，输出完整方案」occupies lines 66-107, preceded by blanks 64-65
  and followed by blanks 108-109 and「## 创建 commit」at line 110.
- The upstream source blob (index 4ad3aabde7) had no `<!-- #190... -->` comment block at the top;
  the state file has five such comment lines added by earlier items in the replay series. Because
  those lines are part of the accumulated local state, they are kept; only the section move is
  ported, and the resulting file is line-shifted by the offset created by the move.
- The upstream blob contained a stray trailing fragment (`在完整`) after the section that the
  state file does not have; the state file is already the cleaned shape, so no fragment handling
  was needed.

## Edit script (semantic)

1. Anchor on the exact content lines: `## 只调研，不改代码，输出完整方案` … `subagent 检查是硬性步骤…`
   … `-------------` (the last separator before `## 创建 commit`), including the two trailing
   blanks before `## 创建 commit`.
2. Delete the block from its middle position (after the TDD section).
3. Insert the identical block after the first `-------------` separator (after the intro line
   `本文件是 prompt 片段存档，不是一次性全文使用。…`), i.e. immediately before `## TDD 手术级实现`,
   so the section becomes the file's first prompt fragment and the workflow chain reads
   只调研 → TDD → 创建 commit.
4. Leave everything else untouched (comment block, intro, TDD section, 创建 commit section).

Applied via a content-anchored Python move script (no numeric line hunks), then verified by
printing the resulting head/tail regions; the move yielded a 44/44 line swap (net 0 close to a
pure reorder, with the file finishing at 112 lines).

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix371.diff` (7,089 bytes, 106 lines)
- Current patch: `.temp/patches/current/0371-8270e9600092.patch`
- Materialized state: `0371-8270e9600092` (passed 371/371, no failure phases)

## E/C gate

`count-ec-auditor.py`: E=0, C=0, need=0 → PASS (sole file is `.md`, excluded by policy).

## Materialize

`bun .temp/patches/src/apply-cumulative.ts --materialize 371` → `passed 371/371`;
state published at `.temp/patches/states/0371-8270e9600092` (check/apply via `git apply --index`
both clean).

## Verification & downstream compatibility

- The moved block is identical to the source text; a follow-up consumer (item 0375) rewrites the
  same section's tail (adds「## 方案审计与放行」heading and the audit-loop rules) and will be
  ported against this post-move layout — the anchors used by 0375 (the「最终请给出推荐方案摘要…」
  and「subagent 检查是硬性步骤…」lines) survive the move unchanged.
- The file retains the five `<!-- #190... -->` tracking comments at the top; they are local-series
  content and were not disturbed.

## Notes / deviations from source

- No verbatim hunk application was possible because the replay-state file (with the #190 comments
  and without the upstream `在完整` stray fragment) differs structurally from the upstream source
  blob; the port therefore re-express the semantic move instead of replaying hunks. This is
  consistent with the .md-excluded E/C policy and prior doc-item ports.
- Line/blank layout around the moved block matches the upstream final shape (section + one blank
  + `## TDD 手术级实现`; tail keeps `-------------` + two blanks + `## 创建 commit`).