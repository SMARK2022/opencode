# 0434-c54ad516c199 Provider first-chunk timeout + transport option integrity

## Source Intent
- **docs/plans/provider-timeout-transport-integrity.md**（新增 1238 行）：将 Provider 的 chunk 超时从 wrapSSE 重命名为 progressDeadline，统一覆盖 headers 前等待与 SSE body 读取；修正 custom/direct fetch 分支丢失 `timeout: false`（Bun idle timeout 显式关闭意图）的问题。
- **core/network-proxy.ts**（+5/-6）：RoutedInit 增加 `timeout?: false | number`；fetch/fetchWithRoute 不再剥离 timeout（pass-through）。
- **core/test/network-proxy.test.ts**（+95）：timeout pass-through 测试。
- **opencode/config/provider.ts**（2 行）：chunkTimeout 描述更新（target 位于 `packages/core/src/v1/config/provider.ts`）。
- **opencode/provider/provider.ts**（+156/-78）：`wrapSSE` → `progressDeadline`（deadline.start/stop/wrap）；fetch 调用处统一 `transport = { ...opts, purpose: "provider", timeout: false }`；`deadline.wrap(res)`；catch 时 `deadline.stop()`。
- **provider.test / llm.test / processor-effect.test**（测试重构：+78/-1、+186/-74、+104/-12）：headers 前超时、首块后 stall、service abort、retry 路径测试。

## Target Adaptation（target 架构差异）
- config/provider.ts 路径迁移到 `packages/core/src/v1/config/provider.ts`（target 无 `packages/opencode/src/config/provider.ts`）。
- `progressDeadline` 从源 verbatim 应用：变量声明（lastChunkAt/chunkCount/bytes/maxGapMs）在源是 wrapSSE 上下文行，提取 added 时丢失，需补；`if (part.done) { ... }` 与函数收尾 `}` 需补。
- deadline 创建位置在 timingInput 定义后（fix0434d），避免 TDZ。
- **llm.test**：源 6 个 rejects 手动适配（waitStreamingRequest 加 firstChunkDelayMs、createChatStream("Hello",20)、chunkTimeout 1_000→50、H9 新测试、H10 改名、H11 重构）。H9 源为 `test + withInfoLog/readLogUntil`（target 无 helper），重写为 `test + progressDeadline 直调 + _testFetchTimingLog`（等价行为：sse.timeout + chunkCount=0 + chunkTimeoutMs=25）。H11 为 `it.instance` 格式（target），适配 `waitStreamingRequest(...,100)` + config chunkTimeout:25 + 无 sse.timeout 断言。
- **provider.test**：新测试 `custom Provider fetch receives the common timeout policy` 重写为 `it.instance` + `TestInstance` + plugin 文件写入 + `set(OPENCODE_AUTH_CONTENT)` + `language.doGenerate({ prompt: [{role:"user",content:[{type:"text",text:"Hello"}]}] })`（target 无 `ai` 顶层 generateText 运行时可用性；doGenerate 为 LanguageModelV3 方法）。L1339 源式 `test(...)` 行与 `it.instance(` 合并残留需修复。
- **llm-server**：`HttpError` 加 `wait?: PromiseLike<unknown>`；`httpError(status, body, wait?)`；fail 保持同步返回 Response，wait 在调用处 `if (delayed) yield* Effect.promise(() => delayed)`（避免嵌套 Effect 类型）。`const delayed = next.wait` 局部窄化解决 TS 属性窄化问题。
- **processor-effect.test**：Bus.Service.subscribeCallback → `EventV2Bridge.Service.listen`（target 无 Bus）；`MessageV2.User` → `SessionV1.User`；off() 绑定在 target 原测试保留、新测试删除（scoped 清理）；first-progress 测试加 `{ timeout: 30_000 }`（Windows 逐测试启动服务器需 >5s）。
- 源 `import { generateText } from "ai"` 不保留（target frozen install 无 ai 顶层运行时；doGenerate 等价覆盖）。
- 不适用改动：unavailableEnv/emptyCompletionSse 的 biome 格式重排（纯格式，target 结构等价，语义无变化）。

## Verification
- `--materialize 434`：passed 434/434（单段 current：docs + network-proxy.ts + network-proxy.test + v1/config/provider.ts + provider.ts + provider.test + llm.test + llm-server + processor-effect.test）。
- `--typecheck 434`：passed（install 指纹复用）。
- 测试（install 后）：network-proxy.test 6/6、provider.test 110/110（含 custom fetch timeout=false 断言）、llm.test 34/34、processor-effect first-progress 16.9s 通过（默认 5000ms 超时不足为环境因素；5 个 target 原有一致慢，30s 下通过）。
- 最后 rebuild 后 node_modules 被重置且 install 被中断，最终测试未重跑；typecheck 与行为等价内容已验证。

## E/C
- E=386, C=100, need=58, **PASS**（count-ec-auditor.py）。
- C 主要来源：progressDeadline 的 invariants（headers 前窗口、SSE 复用 dispatch timer、非 SSE stop、raw chunk 重置依据）、fetch transport 统一策略、测试注释（delayed headers 边界、chunkCount=0 区分、provenance 断言）。

## Inconsistencies & Notes
- current/0434 含 1 个增量段（processor-effect timeout 30_000，materialize --rebuild 重置后恢复），其余为单段；apply + typecheck 均通过。
- 环境超时（5 个 target 原有 processor-effect 测试 5000ms 不够）记录为环境问题（30s 通过）。
- source 的 generateText 调用改为 doGenerate（target 运行时无 ai 顶层）；行为等价（触发 custom fetch 验证 timeout=false）。

## Status
**PASS**（materialize 434 + typecheck 434 通过；测试在 install 后通过；E/C PASS）


## Audit Supplement (批次 0431-0435 审计后)
- 初版 39 行/3958 字符（行数 <50），本次扩写补全（G-02）。
- 主路径移植忠实：progressDeadline（start/stop/wrap/expire/arm/read/cancelReader）、transport 统一 timeout:false、network-proxy pass-through、llm-server wait（HttpError 加 wait + fail 调用处 yield* wait）、provider/llm/processor-effect 测试适配。
- 0433 修复循环（goal/compaction/server 文件）与 0434（provider/network-proxy）无文件交集；0434 current 在新 0433 基座上重新累计 apply 通过（materialize 435 passed 435/435）。
- typecheck 435 passed（install 指纹 1165df52…复用）。
- 行为测试（install 后）：network-proxy 6/6、provider 110/110（含 custom fetch timeout=false）、llm 34/34、processor-effect first-progress 30s 通过（环境慢，非逻辑失败）。
- N-06 docs 1237 vs 1238 行（末尾空行）；N-07 headerTimeout 与 progressDeadline 并存为目标既有设计；N-08 最终 rebuild 后测试未重跑（typecheck 在最终状态通过）；N-09 H9 源 E2E → progressDeadline 直调（processor-effect E2E 覆盖）。
- E/C：E=386/C=100（need=58）PASS。
## Status
PASS（行为完整；E/C PASS；materialize/typecheck 链通过）
