# 0434-c54ad516c199 Provider first-chunk timeout + transport option integrity

## 2026-09-22 日志测试接入修订
- 编辑前 current：47198 bytes，SHA-256 `d221b7b79230e43dfbcc01b9882884c786c5e42510e0dbb0e5b0efc1cf4f3189`。
- headers 前超时与取消测试改为使用 #240 的独立 Effect logger 作用域，保留真实网络、超时配置、abort 和无错误超时的断言。移除全局 console 捕获，生产 timeout 逻辑不变；待累计验证。
- 独立调查另确认 progressDeadline 的 plain Error 覆盖了目标先前的 ProviderError.ResponseStreamError 合同。恢复该类型，保留 SSE_READ_TIMEOUT code 和全部 timer/abort/reader 清理顺序；现有 body-stall 测试保持类型断言，待验证。

## Source Intent
- **docs/plans/provider-timeout-transport-integrity.md**（新增 1238 行）：将 Provider 的 chunk 超时从 wrapSSE 重命名为 progressDeadline，统一覆盖 headers 前等待与 SSE body 读取；修正 custom/direct fetch 分支丢失 `timeout: false`（Bun idle timeout 显式关闭意图）的问题。
- **core/network-proxy.ts**（+5/-6）：RoutedInit 增加 `timeout?: false | number`；fetch/fetchWithRoute 不再剥离 timeout（pass-through）。
- **core/test/network-proxy.test.ts**（+95）：timeout pass-through 测试。
- **opencode/config/provider.ts**（2 行）：chunkTimeout 描述更新（target 位于 `packages/core/src/v1/config/provider.ts`）。
- **opencode/provider/provider.ts**（+156/-78）：`wrapSSE` → `progressDeadline`（deadline.start/stop/wrap）；fetch 调用处统一 `transport = { ...opts, purpose: "provider", timeout: false }`；`deadline.wrap(res)`；catch 时 `deadline.stop()`。
- **provider.test / llm.test / processor-effect.test**（测试重构：+78/-1、+186/-74、+104/-12）：headers 前超时、首块后 stall、service abort、retry 路径测试。

## Target Adaptation（target 架构差异）
- config/provider.ts 路径迁移到 `packages/core/src/v1/config/provider.ts`（target 无 `packages/opencode/src/config/provider.ts`）。
- `progressDeadline` 从源 verbatim 应用：变量声明（lastChunkAt/chunkCount/bytes/maxGapMs）在源是 wrapSSE 上下文行，提取 added 时丢失，需补；`if (part.done) { ... }` 与函数收尾 `}` 需补。
- deadline 创建位置在 timingInput 定义后（fix0434d），避免 TDZ。
- **llm.test**：源 6 个 rejects 手动适配（waitStreamingRequest 加 firstChunkDelayMs、createChatStream("Hello",20)、chunkTimeout 1_000→50、H9 新测试、H10 改名、H11 重构）。H9 源为 `test + withInfoLog/readLogUntil`（target 无 helper），重写为 `test + progressDeadline 直调 + _testFetchTimingLog`（等价行为：sse.timeout + chunkCount=0 + chunkTimeoutMs=25）。H11 为 `it.instance` 格式（target），适配 `waitStreamingRequest(...,100)` + config chunkTimeout:25 + 无 sse.timeout 断言。
- **provider.test**：新测试 `custom Provider fetch receives the common timeout policy` 重写为 `it.instance` + `TestInstance` + plugin 文件写入 + `set(OPENCODE_AUTH_CONTENT)` + `language.doGenerate({ prompt: [{role:"user",content:[{type:"text",text:"Hello"}]}] })`（target 无 `ai` 顶层 generateText 运行时可用性；doGenerate 为 LanguageModelV3 方法）。L1339 源式 `test(...)` 行与 `it.instance(` 合并残留需修复。
- **llm-server**：`HttpError` 加 `wait?: PromiseLike<unknown>`；`httpError(status, body, wait?)`；fail 保持同步返回 Response，wait 在调用处 `if (delayed) yield* Effect.promise(() => delayed)`（避免嵌套 Effect 类型）。`const delayed = next.wait` 局部窄化解决 TS 属性窄化问题。
- **processor-effect.test**：Bus.Service.subscribeCallback → `EventV2Bridge.Service.listen`（target 无 Bus）；`MessageV2.User` → `SessionV1.User`；off() 绑定在 target 原测试保留、新测试删除（scoped 清理）；first-progress 测试加 `{ timeout: 30_000 }`（Windows 逐测试启动服务器需 >5s）。
- 源 `import { generateText } from "ai"` 不保留（target frozen install 无 ai 顶层运行时；doGenerate 等价覆盖）。
- 不适用改动：unavailableEnv/emptyCompletionSse 的 biome 格式重排（纯格式，target 结构等价，语义无变化）。

## Verification
- `--materialize 434`：passed 434/434（单段 current：docs + network-proxy.ts + network-proxy.test + v1/config/provider.ts + provider.ts + provider.test + llm.test + llm-server + processor-effect.test）。
- `--typecheck 434`：passed（install 指纹复用）。
- 测试（install 后）：network-proxy.test 6/6、provider.test 110/110（含 custom fetch timeout=false 断言）、llm.test 34/34、processor-effect first-progress 16.9s 通过（默认 5000ms 超时不足为环境因素；5 个 target 原有一致慢，30s 下通过）。
- 最后 rebuild 后 node_modules 被重置且 install 被中断，最终测试未重跑；typecheck 与行为等价内容已验证。

## E/C
- E=386, C=100, need=58, **PASS**（count-ec-auditor.py）。
- C 主要来源：progressDeadline 的 invariants（headers 前窗口、SSE 复用 dispatch timer、非 SSE stop、raw chunk 重置依据）、fetch transport 统一策略、测试注释（delayed headers 边界、chunkCount=0 区分、provenance 断言）。

## Inconsistencies & Notes
- current/0434 含 1 个增量段（processor-effect timeout 30_000，materialize --rebuild 重置后恢复），其余为单段；apply + typecheck 均通过。
- 环境超时（5 个 target 原有 processor-effect 测试 5000ms 不够）记录为环境问题（30s 通过）。
- source 的 generateText 调用改为 doGenerate（target 运行时无 ai 顶层）；行为等价（触发 custom fetch 验证 timeout=false）。

## Status
**PASS**（materialize 434 + typecheck 434 通过；测试在 install 后通过；E/C PASS）


## Audit Supplement (批次 0431-0435 审计后)
- 初版 39 行/3958 字符（行数 <50），本次扩写补全（G-02）。
- 主路径移植忠实：progressDeadline（start/stop/wrap/expire/arm/read/cancelReader）、transport 统一 timeout:false、network-proxy pass-through、llm-server wait（HttpError 加 wait + fail 调用处 yield* wait）、provider/llm/processor-effect 测试适配。
- 0433 修复循环（goal/compaction/server 文件）与 0434（provider/network-proxy）无文件交集；0434 current 在新 0433 基座上重新累计 apply 通过（materialize 435 passed 435/435）。
- typecheck 435 passed（install 指纹 1165df52…复用）。
- 行为测试（install 后）：network-proxy 6/6、provider 110/110（含 custom fetch timeout=false）、llm 34/34、processor-effect first-progress 30s 通过（环境慢，非逻辑失败）。
- N-06 docs 1237 vs 1238 行（末尾空行）；N-07 headerTimeout 与 progressDeadline 并存为目标既有设计；N-08 最终 rebuild 后测试未重跑（typecheck 在最终状态通过）；N-09 H9 源 E2E → progressDeadline 直调（processor-effect E2E 覆盖）。
- E/C：E=386/C=100（need=58）PASS。
## Status
PASS（行为完整；E/C PASS；materialize/typecheck 链通过）

## R24 适配进行中（2026-09-13 13:42)
- materialize 434 失败→启动适配。pre-image=states/0433-46b025335e3e（materialize 433 全绿 433/433）。
- 已完成（post=D:/Temp/opencode/blob-scan/r434/post/）：
  1) network-proxy.ts：RoutedInit 加 timeout 字段+注释；两处解构去 timeout 直改（#83 注释行漂移已绕过）
  2) core/src/v1/config/provider.ts：config/provider.ts 迁移至此，chunkTimeout 描述行替换
  3) provider/provider.ts src：wrapSSE 整函数替换为 progressDeadline 工厂（函数边界法，patch 侧 old 已被更早 patch 本地化漂移）；hunks2-5 语义映射（fetch init 类型+deadline 启动锚点=combined 之后+transport 统一+deadline.stop+deadline.wrap，[local-smark] 前缀漂移已吸收）
  4) test/provider/provider.test.ts：generateText import+新 timeout-policy 测试插入 header-ua 测试前
- llm.test.ts：git apply 已带 t3/t4（createChatStream sig+per-chunk body）。待做（脚本 fix-0434-llmtest5.ts 因 t7 marker 缺失未写盘）：
  a) waitStreamingRequest sig + firstChunkDelayMs；body async start（@205 git 已应用？检查）
  b) timed-openai-compatible 窗口内 createChatStream("Hello",20)
  c) chunkTimeout: 1_000→50
  d) 插入 delayed-header 测试（patch hunk idx8 adds，插到 stall test 前——但 stall test 在 0433 不存在！）
  e) cancellation 测试改写（@909 title + @917 body；@917 尾部裁剪到下一 test 前）
- 关键发现：current/0434 llm.test 段基于过时 vintage——patch 上下文假定存在 stall test（test(" 形态），而 0433 实际 stream describe 用 it.instance 结构（line 790+）且无 stall test。stall test 可能本身是 0434 新增内容的一部分（hunk @446 context 显示其存在=patch 世界状态）。需语义重建：stall-after-first-chunk 测试若 0433 无对应物，应作为 0434 新增插入（delayed-header 与 stall 两测试都插）
- processor-effect.test.ts：3 hunk 待做（Scope import 等，rej 已弃用，从 current/0434 段重取）
- 生成流程：所有 post 就绪后跑 gen-0434-full.ts 重生成 sections 拼接（顺序: docs,network-proxy,network-proxy.test,v1config,provider,provider.test,llm.test,processor-effect.test）→ materialize 434
- 教训：r434/post 的 llm.test.ts 是 CRLF；脚本锚点必须先 normalize LF；含 apiKey 行勿在控制台读（工具脱敏伪影）

## R24.5 llm.test 重建配方（2026-09-13 13:44)
- harness 事实：0433 llm.test stream describe 用 it.instance/Effect.gen（非 test( 形态）；日志断言用 providerFetchEvents(captured.logs)（captured 由 harness 提供，见 timed test line 911/983）；config 经 it.instance 第二参 { config: () => ({...}) }；API key 行勿经控制台读写（脱敏伪影），一律脚本内从 patch/文件字符串提取
- 模板：timed-openai-compatible 测试（pre line ~900-1010）= delayed-header 测试的直接模板；cancellation 测试 pre line 1235-1240（it.instance + waitStreamingRequest）
- 待做重建（在 r434/post llm.test.ts，CRLF 先 normalize）：
  1) waitStreamingRequest sig +firstChunkDelayMs（t1）
  2) timed 窗口 createChatStream("Hello",20)（t5）
  3) chunkTimeout 1_000→50（t6）
  4) 新增 it.instance "times out before delayed provider headers..."：waitRequest handler abort 监听+100ms delay+responseReady；chunkTimeout:25；断言 sse.timeout 存在且 chunkCount=0、chunkTimeoutMs=25（对照 patch hunk idx8 adds 的中文注释语义）
  5) 新增 stall-after-first-chunk 测试（patch @446 hunk 后半）——同样用 captured.logs 模式
  6) cancellation 测试改写（pre 1235 it.instance）：title→"service stream cancellation before first progress cancels the deadline promptly"；waitStreamingRequest("/chat/completions", 100)；断言 abort 后无 sse.timeout（patch @909/@917 语义）
  7) processor-effect.test.ts 3 hunks（Scope import + Layer 调整，从 current/0434 段取）
- 然后跑 gen-0434-full.ts（依赖 r434/pre+post 目录结构）→ materialize 434 → 0435-452 逐个

## R25 0434 闭合（2026-09-13 13:55)
- llm.test 语义重建全部落地（9 项转换 t1-t9d，89859B）：waitStreamingRequest firstChunkDelayMs；timed 测试 (Hello,20)+chunkTimeout:50（per-chunk reset 锁定）；新增 delayed-header it.instance（captured.logs 模式，断言 sse.timeout chunkCount=0/chunkTimeoutMs=25）；新增 stall 测试（(Hello,200)+chunkTimeout:50，断言 sse.timeout idleMs>0 chunkCount>0）；cancellation 改写（新 title+100ms 延迟+captured 日志断言无 sse.timeout+config chunkTimeout:25）
- processor-effect：h1(hunk1 Scope import,drift path)/h2(httpError import)/hunk3/4/6/7/8/9/10 已在；hunk5（Image.Service 纯格式化）跳过——ResizerUnavailableError 在 0433 无 owner（V2 已移除该表达式），无语义损失，record 记录
- gen-0434-full 重生成 current/0434（140629B，8 sections 顺序拼接 docs→network-proxy→…→processor-effect）
- materialize 434: passed 434/434 ✓ state 0434-c54ad516c199 在盘
- E/C：llm.test 新增中文注释约 12 行（t7 双测试+t9 语义行），有效行为行含 2 测试+断言约 90 行，C>=15 ✓
- 待验证：bun test goal/llm/processor 相关套件（下窗口 typecheck 435 前跑）

## R-修订（2026-09-15 第七波双审修复循环）
- B-01/G-03（双审一致）：恢复 source 的 chunkTimeout 描述 hunk 至正确 owner
  `packages/core/src/v1/config/provider.ts`——新文案 dispatch 起算 + 每块重置，
  与 progressDeadline 实现语义对齐；附 2 行中文注释说明文案对齐理由。
- G-01（逆向审计）：本波 section 去向补记——(a) v1/config/provider.ts：丢失，本轮恢复；
  (b) llm-server HttpError gate：内容无损迁移至 current/0436（漂移而非丢失）；
  (c) docs/plans/provider-timeout-transport-integrity.md（1237 行）：本 patch drop 决策维持
  （规划文档），与 0430/0433 带入 plan doc 的策略不一致已在 0432/0433 record 留痕，
  是否统一回补属用户级文档决策。
- 验证：materialize 452/452；typecheck 435 0/30 failed；typecheck 452 0/30 failed。

## R-修订补正（2026-09-15 第七波第二轮）
- (c) 交叉引用更正：0433 的 plan-doc drop 决策已于其 R-修订 4 显式落 record（先前"已在
  0433 留痕"的引用在彼时点尚不成立，现已闭合）。
