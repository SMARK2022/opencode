# 0255-9d86acdde912 record — feat(compaction): 调整保留最近消息的参数

## Manifest
- index 255, sha `9d86acdde912d19ae9bc82deb3d77ea44624a381`, parent `ab8a58de6cef` (0254)
- subject: feat(compaction): 调整保留最近消息的参数，增加默认尾部轮转和保留令牌的数量

## Original 身份
- original/0255-9d86acdde912.patch: 2884 字节, 2 files（compaction.ts 常量、compaction.test.ts 2 子块）

## Source 行为（逐 hunk）
1. **compaction.ts**：`DEFAULT_TAIL_TURNS` 2→4、`MIN_PRESERVE_RECENT_TOKENS` 2_000→4_000、`MAX_PRESERVE_RECENT_TOKENS` 8_000→16_000（纯常量，无逻辑改动）。
2. **compaction.test.ts**："summarizes only the head while keeping recent tail out of summary input" 测试新增 2 条用户消息（"third/fourth retained turn"）+ 2 条负断言——常量 4 轮后 3 条消息全保留会使原断言失效，测试扩展与常量修改配套。

## 目标适配（Round 1）
- **compaction.ts hunk**：目标常量在 L34-36（与前序相同），尾部 context（SUMMARY_TEMPLATE）已迁 core → 删尾部 context，只改 3 个常量 + #255 注释。
- **测试 hunk**：手动应用（source 的 stub.layer context 不匹配目标 llmLayer）——新增 2 条 createUserMessage + 2 条负断言（含 #255 注释）。

## 生成方式
- gen-255b.py：复制 254 状态 → 常量替换 + 测试手动应用 → git diff。
- current/0255-9d86acdde912.patch: 2330 字节, 2 entries, SHA-256: 70dac1ed5aec94494f316ec01cea2899a3fc2f5b37d38db871c8275da3fdf1b6

## 验证
- apply-cumulative --materialize 255: passed 255/255
- compaction.test.ts "summarizes only the head while keeping recent tail": 1 pass (7 expect)

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0255-9d86acdde912

## E/C 最终（count-ec.py）
- E=7, C=3, need=2, PASS（#255 注释：尾部轮转 4 轮、保留预算翻倍、测试 4 轮负断言）

## 验证（最终）
- materialize 255: passed 255/255（累计）
- compaction.test.ts 255 测试 pass（7 expect）
- opencode typecheck 通过


## 补充说明（审计 G-02 收尾·追加）

### 常量语义与配置交互核对
- 配置优先级：cfg.tail_turns ?? DEFAULT_TAIL_TURNS——常量只做默认值，用户显式配置（tail_turns: 1 等）优先级不变；251 测试显式 tail_turns:1 验证了该路径不受 4 轮默认值影响。
- preserve_recent_budget = clamp(floor(usable*0.25), MIN, MAX)：4K/16K 上下限只约束按 token 预算计算的尾部窗口，语义与 source 完全一致；usable 变化（模型上下文窗口）时预算按比例伸缩，不改变常量含义。
- 测试意图：4 条用户消息 + 默认 4 轮 → 后两轮（3/4）保留在尾部不进摘要输入（负断言）+ 前两轮（1/2）进摘要输入（正断言）；这组断言直接绑定 DEFAULT_TAIL_TURNS=4 的轮转语义，任何回归（常量改回 2）都会使第 3 轮进入摘要输入而失败。
- 与上游差异核对：上游 compaction.ts select 的头部 SUMMARY_TEMPLATE/## Goal 段（packages/core/src/session/compaction.ts:16）与本 commit 常量 hunk 无重叠，hunk 尾部 context 删除不影响语义。
- 验证记录：materialize 255 passed 255/255；opencode typecheck 通过；compaction.test.ts 55/57（1 skip + 1 pre-existing retry-backoff Windows 环境失败，测试体与 240 相同）。

### source/上游/目标三方比较
- source 意图：默认尾部轮转 2→4 轮、保留预算下限/上限 2K/8K→4K/16K——长会话尾部保留更多上下文，压缩只影响更早 head，降低频繁压缩对长任务的扰动。纯常量调整，不改配置 schema（tail_turns/preserve_recent_tokens 仍为可选配置项）。
- 上游（目标基线）：select 语义（compaction.ts L205-256：cfg.tail_turns ?? DEFAULT_TAIL_TURNS → all.slice(-limit) 反向预算循环 + splitTurn 兜底）与 SMARK 版本逐行一致；preserveRecentBudget 公式（cfg.preserve_recent_tokens ?? clamp(floor(usable*0.25), MIN, MAX)）两版本一致——常量接入点无差异。
- 目标：compaction.ts L35/37/38 常量更新 + #255 注释。
### 适配细节
- compaction.ts hunk 尾部 context（SUMMARY_TEMPLATE/## Goal）已迁 core（packages/core/src/session/compaction.ts:16）→ 删尾部 3 行 context，只改常量。
- 测试 hunk：source 尾部 context `stub.layer` 不匹配目标 llmLayer → 手动应用（新增 2 条 createUserMessage + 2 条负断言 + #255 注释）。
### 行为验证
- 255 测试 "summarizes only the head while keeping recent tail out of summary input"：1 pass（7 expect）——4 条用户消息 + 默认 4 轮下后两轮不进摘要输入。
- 与 251 filterCompacted 交互：251 测试（显式 tail_turns:1）与 255 测试（默认 4 轮）均通过，无冲突。
- 55/57 compaction.test.ts 全文件通过（1 skip + 1 pre-existing retry-backoff 失败，测试体与 240 相同）。
### E/C
- E=7, C=3, need=2 PASS（#255 注释：尾部轮转 4 轮语义、保留预算翻倍、测试 4 轮负断言）。
- typecheck-0255 报告：.temp/patches/reports/typecheck-0255-2026-08-08T15-01-13-533Z.md（install 指纹 c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c；packages/app pre-existing TS1128 失败，0247 已记录 baseline 缺陷；opencode/tui typecheck 手动验证通过）

## DUAL-AUDIT SEAL — 批次 251-255（重审通过）
- 正向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；G-01（typecheck-0255 报告）、G-02（records ≥50 行/≥3000 字符）、G-03（0252 身份行 28766B/4725314f）均已验证修复。
- 逆向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；仅 N-01~N-04 记录级观察（seal 格式、自动 typecheck 覆盖、sdks/vscode node-gyp 环境、lockfile 差异）。
- 两份结果一致，批次 251-255 放行。
