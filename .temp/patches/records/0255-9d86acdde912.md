# Record 0255-9d86acdde912

## Status
PASS

## 修改内容
feat(compaction): 调整保留最近消息的参数，增加默认尾部轮转和保留令牌的数量。

### Source 语义（2 文件 3 hunk）
- 常量翻倍：DEFAULT_TAIL_TURNS 2→4、MIN_PRESERVE_RECENT_TOKENS 2_000→4_000、MAX_PRESERVE_RECENT_TOKENS 8_000→16_000（源树尾 ctx 为 SUMMARY_TEMPLATE 三行）。
- 测试 "summarizes only the head while keeping recent tail out of summary input"：tail 轮数翻倍后补两轮消息（third/fourth retained turn）保证 head 非空 + 两条 not.toContain 断言。

### 目标适配（0254 树）
- **H1 重锚**：源树尾 ctx SUMMARY_TEMPLATE 三行在目标不存在（#0017 谱系已将 SUMMARY_TEMPLATE 移至 core `buildPrompt`，opencode 侧经 import 消费）→ 尾 ctx 替换为 254 树实际后继行 `type Turn = {`；前置 3 行（PRUNE_PROTECT/TOOL_OUTPUT_MAX_CHARS/PRUNE_PROTECTED_TOOLS）逐字一致保留。头 `-32,7 +32,10`（git apply 自动吸收 -9 偏移）。
- **H2/H3**：锚点 L1440/L1455 逐字一致（git 偏移 -87 自动解析）；H3 尾 ctx `stub.layer` → `stub.llmLayer`（251 先例：目标 llm() 暴露 llmLayer）。
- 常量消费处（select L210 的 tail_turns 默认、preserveRecentBudget L86）不触及，值变化即全语义。

### E/C
- E = 12（plus 9 + minus 3，含全部新增行）
- C = 2（H1 双行翻倍理由注释 + H2 测试意图注释，均 #255 标注、位于变更决策旁）
- need = ceil(12 * 0.15) = 2 → C = 2 达标 PASS（另一口径：纯代码行 E = 10，need = 2，C = 2 亦达标）

## Source
- original/0255-9d86acdde912.patch: 52 行 2 文件。

## Current
- current/0255-9d86acdde912.patch: 1959 bytes, 3 hunk / 2 文件（compaction.ts 常量块 + compaction.test.ts 补轮）
- SHA-256: d784469e14d5809ce202dbbe9221d64b7dc7d8e2989a53a18bcb58edd85cfba1

## 逐 hunk 映射（original → current）

| original hunk | current 对应 | 适配说明 |
|---|---|---|
| H1 `@@ -32,7` DEFAULT_TAIL_TURNS 2→4 | 同位重锚 | 源尾 ctx SUMMARY_TEMPLATE 三行 → 目标实际后继行 `type Turn = {`；前置 3 行逐字一致 |
| H2 `@@ -1440` MIN 2_000→4_000 | 同位 | 锚点逐字一致，git 偏移 -87 自动解析 |
| H3 `@@ -1455` MAX 8_000→16_000 + 测试补轮 | 同位 | 尾 ctx `stub.layer` → `stub.llmLayer`（目标 llm() 暴露 llmLayer） |

## 安装
- materialize 255 --rebuild: passed 255/255（全量重放）

## 测试
- typecheck 255: 全部 workspace passed
- `bun test -t "summarizes only the head while keeping recent tail"`（packages/opencode）: 1 pass / 0 fail
- 全文件回归：52 pass / 2 pre-existing fail（keeps recent 与 abort timing，0249/0251/0254 基线同败，record 0251 已论证）——数值翻倍不改变这两者的失败机制
- 测试意图：源用例补轮的目的是在 tail 翻倍后仍保证 head 至少含一条可摘要消息；断言锁定 third/fourth retained turn 不进 summary 输入。current 逐字保留两断言与补轮结构，仅锚点适配。

## 修订历史
1. 初版 H1 尾 ctx 用 SUMMARY_TEMPLATE（源树）→ 254 树无此常量 → 替换为 `type Turn = {`。
2. H3 尾 ctx `stub.layer` → `stub.llmLayer`。
3. H2 补 #255 测试意图注释（初版误用 `#` 注释——.ts 文件改 `//`）。
4. 注释定稿后再次编辑 current（L44 注释行措辞与 0256 preimage 锚定对齐），记录随之更新为最终指纹 d784469e…/1959 bytes；materialize 255 重建 passed 255/255。
5. 最终：materialize OK、typecheck OK、目标测试 1/0、C=2=need → PASS。

## 三方比较
- Source：常量翻倍 + 测试补轮。
- Upstream v1.17.20：常量块在 opencode compaction.ts（SUMMARY_TEMPLATE 已外移 core）。
- Target：数值语义原样（4/4_000/16_000）+ 测试适配 llmLayer。无行为丢失、消费处唯一、无重复 owner。

## V1/V2 判定
- compaction 尾部保留预算在目标与上游均由 V1 谱系 `src/session/compaction.ts` 常量承担；V2 session core 无重叠能力。
- 翻倍为纯数值语义：消费处唯一（select 尾轮 + preserveRecentUserBudget），无迁移/无 fallback/无双 owner。

## 最终行为
- 未显式配置 compaction.tail_turns/preserve_recent_tokens 时，默认保留最近 4 轮、令牌预算 4_000-16_000。
- 回归测试锁定 tail 轮数翻倍后 head 仍非空、tail 消息不进 summary 输入。
