# Record: Patch 34 — 90b112f8bbf3

## 1. Manifest Entry
- **Index**: 34
- **SHA**: 90b112f8bbf3df2adac64cd488d4b8065ce02f65
- **Parent**: 0033-6dd9afdea412
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: fix: 修复计算最终令牌时的输入和总输入逻辑

## 2. Source Behavior (original patch, 1 file)
1. `token-estimate.ts`: 修复 `computeFinalTokens` 函数
   - 移除 `promptInputEstimate = requestConfirmed.input === 0 ? bootstrapInputTokens : 0`
   - 新增 `currentStepTotalInput = currentStepInputConfirmed > 0 ? currentStepInputConfirmed : bootstrapInputTokens`
   - 修改 `totalInput = requestConfirmed.input + currentStepTotalInput + pendingInputTokens`（原来用 promptInputEstimate）

### 行为目的
- **totalInput 修复 invariant**: totalInput 应该始终包含当前步骤的完整上下文，而非仅在 requestConfirmed.input===0 时补充
- **修复原因**: 原逻辑中 promptInputEstimate 仅在首轮（input===0）时补充 bootstrap token，但后续轮次也需要包含当前步骤的输入

## 3. 上游 (v1.17.20) 行为
- V2 路径: `packages/tui/src/util/token-estimate.ts`（从 `packages/opencode/src/cli/cmd/tui/util/` 迁移）
- V2 有 patch 21 添加的中文注释（`// requestConfirmed.input 为 0 时...` 和 `// currentInput 取...`）
- V2 使用 `promptInputEstimate` 旧逻辑

## 4. 目标适配 (current patch, 1 file)
### 路径迁移
- `packages/opencode/src/cli/cmd/tui/util/token-estimate.ts` → `packages/tui/src/util/token-estimate.ts`

### 适配的文件
1. `token-estimate.ts`: 移除 V2 的 patch 21 中文注释 + promptInputEstimate，添加 SMARK 修复逻辑 + 3 条中文注释

### 保留的 SMARK 语义
- currentStepTotalInput 逻辑完整保留
- totalInput 修复完整保留

### 保留的上游优势
- V2 的 token-estimate.ts 路径和结构
- V2 的 computeFinalTokens 函数框架

## 5. E/C 计算
- **E = 6**（新增代码行：currentStepTotalInput + totalInput + currentInput 注释移除后的调整）
- **C = 3**（中文注释）
- **最低 C = max(1, ceil(6 * 0.15)) = 1**
- **3 ≥ 1，通过**

### 注释位置
1. `// 修复：currentInput 应该反映当前步骤的真实输入（包括 bootstrap 或确认值）`
2. `// 修复：totalInput 应该始终包含当前步骤的完整上下文`
3. `// 如果当前步骤已确认（有 step-finish），使用确认值；否则使用 bootstrap 估算`

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 35 --rebuild`: passed 35/35
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/session-ui:passed, packages/tui:passed, packages/ui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`
- 物化状态验证: `currentStepTotalInput` 在 state 35 line 192 存在

## 7. 失败修订历史
1. **首次 apply 失败**: 路径 `packages/opencode/src/cli/cmd/tui/util/token-estimate.ts` 不存在
   - 修复: 迁移路径到 `packages/tui/src/util/token-estimate.ts`

## 8. 最终行为
- computeFinalTokens 使用 currentStepTotalInput 替代 promptInputEstimate
- totalInput 始终包含当前步骤的完整上下文
- V2 路径和结构保留
