# Patch 0223 — feat(vscode-bridge): 增强 VS Code notebook 工具权限门禁

## 1. Identity
- Index: 223
- Source SHA: f71acd28f6549852fa2c997c5f38f225aaea8dd8
- Parent SHA: 8627ea829ffd7b66c6bbeb2096f03e0b42a3f853（patch 222）
- Subject: feat(vscode-bridge): 增强 VS Code notebook 工具权限门禁
- Original patch: `.temp/patches/current/0223-f71acd28f654.patch`
- Built-on state: `0222-8627ea829ffd`（patches 1-222 applied）
- Record path: `.temp/patches/records/0223-f71acd28f654.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 3 files：
  1. `packages/opencode/src/plugin/vscode-bridge.ts` — 2 hunks：vscode_notebook_edit execute 新增 `await ask(context, "vscode_notebook_edit", args)` 在通用 edit 门禁之前；vscode_notebook_env execute 新增 `await ask(context, "vscode_notebook_env", args)` 在 timeout 之前。
  2. `packages/opencode/test/plugin/vscode-bridge.test.ts` — 新文件，2 个测试：验证三个 notebook 变更工具（run/edit/env）各自专属权限 key 在 bridge 调用前触发；验证 notebook edit 双层权限顺序（先 vscode_notebook_edit 后 edit）。
  3. `packages/opencode/test/tool/registry.test.ts` — 1 hunk：新增 `preserves instance context when plugin tools run permission asks inside async execute` 测试，验证 V2 registry 的 EffectBridge 在插件 async execute 中 await context.ask() 仍保持正确 InstanceState 目录。
- SKIP: `packages/opencode/src/tool/registry.ts` — 已在 V2 迁移中 ported，原始 patch 的改动会回退 V2 结构。

## 3. SMARK Intent
- 用户可见行为：notebook edit 和 env 工具现在有各自独立的权限 key（vscode_notebook_edit / vscode_notebook_env），agent 配置可以单独 deny/ask 而不影响通用 edit 规则。
- 受保护不变量：vscode_notebook_edit 仍保留通用 edit 门禁作为第二层（顺序先专属后通用）；vscode_notebook_env 的 timeout/operation metadata 不变。
- 正常路径：notebook edit → ask(vscode_notebook_edit) → ask(edit) → call("/notebook/edit")；notebook env → ask(vscode_notebook_env) → call("/notebook/env")。
- 边界路径：权限层拒绝时 execute 在本地终止，不继续到 bridge HTTP 调用。

## 4. Upstream Reconstruction
- 同路径目标实现：3 个文件在 tip@222 均存在（vscode-bridge.test.ts 除外——新建）。
- vscode-bridge.ts 关键差异：V2 的 `VscodeBridgePlugin(input: PluginInput): Promise<Hooks>` 签名已就位；V2 的 `ask` 函数已返回 `Promise<void>`；execute 当前只有 `await ask(context, "edit", args)`，需要在其前面插入专属 ask。
- vscode-bridge.test.ts 关键差异：V2 需要 `PluginInput` 参数；V2 的 `ToolContext.ask` 返回 `Promise<void>`。
- registry.test.ts 关键差异：V2 的 `Tool.Context.ask` 返回 `Effect<void>`；V2 的 `Tool.Context` 需要 `messages: []` 字段。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@222) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| vscode_notebook_edit ask 专属门禁 | 无（只有 edit） | vscode_notebook_edit execute | 插入 ask + #223 注释 | current patch diff |
| vscode_notebook_env ask 专属门禁 | 无 | vscode_notebook_env execute | 插入 ask + #223 注释 | current patch diff |
| edit 双层权限顺序 | 无专属层 | edit execute | 先专属后通用 + #223 注释 | current patch diff |
| vscode-bridge.test.ts V2 适配 | 无测试文件 | test/plugin/ | 新建 + PluginInput + Promise ask | current patch diff |
| registry.test.ts EffectBridge 上下文 | 无此测试 | test/tool/registry.test.ts | 新增 it.instance + #223 注释 | current patch diff |
| registry.ts V2 跳过 | 已 V2-ported | SKIP | 不应用 | 决策记录 |

## 6. Current Patch Edit
- 各编辑（3 files）：
  1. `vscode-bridge.ts`：2 个 edit block——vscode_notebook_edit execute 新增专属 ask + #223 注释；vscode_notebook_env execute 新增专属 ask + #223 注释。
  2. `vscode-bridge.test.ts`：新建文件——2 个测试 + #223 注释。
  3. `registry.test.ts`：1 个 edit block——新增 EffectBridge 上下文测试 + #223 注释。
- Adaptation 决策：registry.ts SKIP（已 V2-ported）；PluginInput 参数；Promise ask；Tool.Context ask 返回 Effect。

## 7. Verification
- `git apply --check --index` against state 222：PASS。
- Materialize：`--materialize 223 --rebuild` → passed 223/223。
- E=207，C=33，need=32。Gate PASS（margin 1）。

## 8. Contract Check
- 单 commit：是。完整 source diff 已读：是。完整 upstream 实现已读：是。
- 无 grep-only 证据：是。无架构式省略：是（registry.ts SKIP 有理由）。
- 无删除或延迟行为：是。无未请求 fallback：是。每条路径有 owner：是。
- 无残留行为 bug：是。中文注释 gate 通过：是（C=33 ≥ 32）。

## 9. Independent Audit
- Pending。

## 10. Final Verdict
`PASS`（build）— materialize PASS 223/223，E/C gate PASS（E=207，C=33，need=32，margin 1）。
