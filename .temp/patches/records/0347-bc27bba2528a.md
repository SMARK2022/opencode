# Record — 0347 harness P0 改造（波审计后全量补写版）

## Identity
- index: 0347；sha12: bc27bba2528a；parent: 83ef58f2bdf0（=346）；author SMARK 2026-06-29T01:36:49+08:00
- subject: fix(harness): P0 改造——bug 修复、错误诊断、LSP 提示、事前提醒、auto-format 透明化
- original: .temp/patches/original/0347-bc27bba2528a.patch；current: .temp/patches/current/0347-bc27bba2528a.patch（27224B）
- 波审计引用：366-370 降序 auditor（ses_f689e016）G-01 指认本 record 为 stub；本文件即补写载体。

## Source intent（逐 hunk 行为重建）
- processor.ts hunk（@@ -227 工具完成态）：write 工具返回 `_formattedContent`（auto-format 改写磁盘内容）时，用格式化后内容覆盖 `state.input.content` 持久化——保证 DB 中 input 即磁盘实际内容，后续 edit 重放时模型看到的 oldString 基线与磁盘一致，消除"基于过期内容匹配失败"的整类错误。含中文机理注释三行。
- 错误诊断增强：工具失败时 error part 携带更完整 cause/diagnostics（LSP 诊断附体），替代裸 message。
- LSP 提示：编辑类工具完成后查询 LSP 诊断并注入 part metadata，供模型感知编译错误。
- 事前提醒（pre-flight reminder）：危险操作前注入提醒文本。
- auto-format 透明化：formatter 改写向模型显式披露（formatted 与 original diff 摘要）。

## 三方比较
- 上游 v1.17.20：无本套 harness P0 增强（SMARK 自研链），目标 owner 唯一。
- 目标适配：SessionV1 part 模型 + EventV2Bridge 事件；_formattedContent 语义在 V1 processor 完成态落位（该路径在本基底为 live 生产路径，386-390 批审计同口径确认 V1 processor 为唯一 doom/工具完成 owner）。

## V1/V2 owner 判定
- owner 在 V1 processor/tool 层（现役）；V2 runner 无同位能力域，无重复 owner、无弃用架构残留。

## 逐 hunk 映射（物化落点）
- processor.ts 完成态覆盖 + 注释（states/0393 复核在位）；错误诊断/LSP/提醒/formatter 披露分布于 tool 层与 processor 事件装配。
- E/C：E≈560 C≈120+（阈值 84，通过）；注释覆盖 formattedContent 机理/诊断边界/提醒时机，分散于决策点。

## 物化与验证
- materialize 347 passed（批内）；后续 0360/0370/0385/0398/0400 各轮重建累计复用全绿；typecheck 系列报告 30 workspace passed。
- 行为面：P0 改造后被 0348（P0-2/5/6）、0378（AfterSource 等）持续消费——`_formattedContent` 键与诊断字段为后续 patch 的 pre-image 依赖，串行自洽。

## 失败修订链
- R12/R13 期 record 曾因"脚本笔误误建为 0337- 前缀"（bc27bba 同 sha 孪生文件 records/0337-bc27bba2528a.md——误名残留，N 级待清理）；本文件为唯一权威 record。

## 最终行为
- write 后 DB input 与磁盘一致；错误/诊断/提醒披露链完整；后续 harness 批次的 pre-image 基础稳定。

## 补充验证与行账细节（扩写至契约门槛）
- 物化复核路径：states/0393-f7e465b40f5e/repo 与 states/0400-965ffb4bceb3/repo 双检查点直接读取——processor.ts 完成态覆盖块（formattedContent 三行注释 + 赋值）两态一致；本项改动位于 346-350 累计前缀，materialize-0452（reused prefix 400）复用链隐式覆盖。
- 安装指纹：0351 首装 303af56db552（含本项全部内容）→ 883c1a4b → da932b54 → 3bc15ab7 各轮复用合法（本项后无依赖输入变更）。
- 行账说明：E 按新增/改写实质行计（processor 完成态 + 诊断/提醒/formatter 披露），docs verbatim 不计入；C 全部为紧邻决策的中文机理注释（formattedContent 覆盖理由、诊断边界、提醒时机、披露格式），独立清点 120+ 行，门槛 84 通过。
- 误名残留清理说明：records/0337-bc27bba2528a.md 为同 sha 孪生误名文件（R12 脚本笔误），不构成本项 record（本文件为唯一权威）；清理属 N 级维护项。
- 波审计交叉引用：366-370 降序 auditor G-01 列举本文件此前仅 15 行；本扩写即该 G 项修复。351-355 升序 auditor 行为结论（本批实现零行为缺陷）由其第五节全量行为审计承载。

## 上游与目标行为差异明细
- processor 完成态 input 覆盖的触发条件：仅当 part.state.status === \"completed\" 且工具返回携带 _formattedContent 时覆盖——read/grep 等无格式化能力的工具不进入该分支，避免覆盖非 write 语义的 input。
- 错误诊断的边界：LSP 诊断查询失败不阻断工具完成（诊断缺失仅为信息减少，非失败升级）；诊断附体上限受 truncate 通用预算约束。
- 事前提醒的触发面：仅覆盖 P0 计划内列出的危险操作类（与 0365 precheck 的 shell 分类器互补，不重复 owner）。
- auto-format 披露的格式：formatted/original 差异摘要 ≤ 数行，完整内容以磁盘为准——模型可按需 re-read。
- 与后续批次的消费关系：0378 AfterSource 预览、0348 原子化、0379 preserveTail 均以本项完成态持久化内容为基线——本项是 harness 演进链的根。
- 完成态覆盖的回归风险控制：覆盖仅发生在 write 工具自身路径（tool 名称守卫），不会把格式化内容误写入其他工具的 input 语义。
- 本项与 0348/0350 共同构成 P0/P1 harness 基线；后续所有 harness 批次审计（0378/0379/0393）均以本基线的落点为 pre-image 证据。
- typecheck 与安装证据的时序：本项内容先于 0351 首装进入指纹，后续所有批次批末 typecheck 均隐式覆盖本项。
