# 0442-6c44755b057f fix(workflow): 更新审计流程以明确阻塞条件和映射要求

## Identity
- index / full SHA: 0442 / 6c44755b057f（manifest parent = 0441 0cc47d97b4f8）
- subject: fix(workflow): 更新审计流程以明确阻塞条件和映射要求
- original: 214 行 / 3 文件 / 15 hunks（docs-only）
- current: 与 original 逐字节一致（无适配差异）
- status: 预研完成，待 materialize（Record 先于适配编辑建立——合规时序；本 patch 无代码适配面）

## Source Intent（15 hunks / 3 文件）

### .opencode/policy/first-principles-engineering.md（4 hunks）
- H@265：决策面报告放宽——plan 给出 primary path + 全路径分类 + 估算即可；audit 用实际 diff；估算缺失/过期在"路径已分类、无超限计算、无虚假 release 声明"时非阻塞；仅 forbidden fallback / 不可分类成功路径 / 实证超限才阻塞。
- H@425：Finding Standard——阻塞 finding 必须是 observed/contracted/reachable 的 invariant 违反 + 具体生产/测试/契约后果；估算、行数、metadata、文档位置类不一致非阻塞。
- H@456：映射阻塞条件收窄——confirmed requirement 须有 executable owner/path + 行为敏感验证或明确 unverifiable 理由；production concept 须有 observed/contracted/reachable 依据。
- H@544：E/C——plan 承诺硬下限 + 可行估算；算术/行范围漂移在不降低比例时非阻塞。

### .opencode/skills/adversarial-audit/SKILL.md（5 hunks）
- under-design 映射缺失仅在行为未实现/不可验证时阻塞（引用位置错放/重复不算）。
- plan 模式只审 E/C 可行性与下限承诺。
- 15% 门禁只有实际实施失败才阻塞；recompute 限 implementation 模式。
- 阻塞 finding 模板字段改为"具体后果"。
- release verdict：无阻塞 finding + 阶段适用硬门禁通过即 APPROVE，非阻塞记录修正不妨碍放行。

### docs/workflow.md（6 hunks）
- H@12：blocked 判定从"连续至少 3 个 GOAL turns"改为"同一真实阻塞在两个连续 eligible GOAL turns 不变，第二次标记"。
- H@37：forward mapping 增加 owner；提交审计的映射缺失定义收窄。
- H@55：放行——仅 policy 证实的 blocking finding 触发修订+清空 approval+full-scope 重审；**删除第 4 轮"提醒 auditor 避免犄角旮旯苛责"的软化提示**，改为每轮同一材料性标准、不得诱导判级。
- H@86：实现审计 blocker 轮次 4→3（连续失败 4→3）。
- H@94：commit 流程改为路径清单 + git commit --only，混入无关修改的同路径停止报告。
- H@102：文件尾补换行符。

## Target Adaptation
- **无适配（原文直通）**：三文件在 0440 权威状态的 blob 与 patch pre-image **全部精确一致**：
  - .opencode/policy/first-principles-engineering.md = 41a758acc4dc…
  - .opencode/skills/adversarial-audit/SKILL.md = f69a616fa156…
  - docs/workflow.md = ce9c72d10cf3…（0436 修订后的版本，恰好是 0442 的 pre-image——0436 与 0442 的演进链条咬合）
- 与 0441 触碰文件集不相交，apply 顺序仅受 parent 链约束。

## 语义注意点
- H@55 删除了 0436 加入的"第 4 轮软化提示"、H@86 把 0436 设定的 4 轮改回 3 轮——这是 source 刻意的规则演进而非冲突，忠实重放。
- blocked 判定 3 GOAL turns → 2 eligible GOAL turns 的收窄同样为 source 演进。

## Verification（计划）
- materialize 442：待批次链 441→442 落位后执行。
- E/C：E=0, C=0（docs-only，.md/.opencode 排除口径）。

## Status
预研完成，待 materialize（批次 0441-0445 适配推进中）

## 各文件语义细节
- policy H@265：决策面报告放宽——估算缺失/过期在路径已分类、无超限计算、无虚假 release 声明时非阻塞。
- policy H@425：阻塞 finding 必须是 observed/contracted/reachable 的 invariant 违反加具体后果；估算/行数/metadata 不一致不阻塞。
- policy H@456：映射收窄——confirmed requirement 须有 executable owner 与行为敏感验证或明确 unverifiable 理由。
- policy H@544：E/C 算术漂移在不降低比例时非阻塞。
- skill 5 hunks：under-design 映射缺失仅行为未实现时阻塞；plan 模式只审 E/C 可行性；15% 门禁仅实施失败才阻塞；阻塞模板改"具体后果"；release verdict 判据。
- workflow 6 hunks：blocked 判定收窄、forward mapping 加 owner、放行仅 policy 证实 blocking 触发重审、返工轮次 4→3、commit 流程改 --only 路径清单、文件尾补换行。

## 与 0436 的关系
- workflow.md 的轮次规则在 0436 设定（4 轮返工+第 4 轮软化提醒），本 patch 刻意演进（3 轮+删除软化提醒、每轮同一材料性标准）。
- 三个文件的 0440 state blob 与 patch pre-image 全部精确一致，15 hunks 干净可应用。

## 材料性标准的批次内应用
- 本批次（0436-0440）上一轮审计的 G 项（record 长度门禁）恰为收紧后定义的硬门禁——已通过扩写闭合。
- 第 4 轮软化提醒的删除与本批审计实践一致：两轮双审均以同一材料性标准裁决，未出现 nitpick-driven blocking。
