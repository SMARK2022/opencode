# Record 0430-91939ad13058

## Identity
- SHA: 91939ad13058
- Parent: 28e9edf9b052 (0429)
- Subject: fix(tool): preserve unique substring patch matches
- Status: PASS

## Source intent（10 文件）
1. `docs/plans/edit-apply-patch-match-recovery.md`（新 2172 行）：patch 匹配恢复方案——exact-only 合同、唯一 substring、closest 诊断只解释不 fallback、tie 抑制、context 校验。
2. `src/patch/match.ts`（新 157 行）：`locateExact`（exact-line 优先 → 唯一 substring；ambiguous 拒绝；EOF 优先）、`lineOffset`/`nextLineOffset`（行起点累计）、`closestWindow`（有序 bigram multiset 相似度；<0.5 或 tie 抑制）、`truncateExcerpt`（带 omission marker 的首尾保留）。
3. `src/patch/index.ts`：`applyChunks` 替代 `computeReplacements`/`applyReplacements`——逐 chunk 先验证 context（locateExact）、pure insertion 延后追加、exact-line splice 或 substring slice、工作副本原子性、`withCandidate`（失败时 closest 诊断）、`workingText`（canonical lines + terminal 分离）。
4. `src/tool/apply_patch.ts`：移除 `extractActualExcerpt`（50 行字符重叠 scorer）——Patch owner（applyChunks 的 withCandidate）持有失败身份；Tool 只聚合错误。
5. `src/tool/edit.ts`：移除 Replacer 体系（Replacer/SimpleReplacer/LineTrimmedReplacer/BlockAnchorReplacer/WhitespaceNormalizedReplacer/IndentationFlexibleReplacer/EscapeNormalizedReplacer/TrimmedBoundaryReplacer/ContextAwareReplacer/MultiOccurrenceReplacer + levenshtein + isDisproportionateMatch + findClosestMatch/charOverlap/formatClosestMatchDiff，共 608 行）→ `replace` 改为 exact-literal（indexOf → not-found 用 closestWindow 诊断 → replaceAll 显式授权 → 第二 occurrence 拒绝）。
6. `edit.txt`/`apply_patch.txt`：文档更新（exact-only、唯一 substring、closest 只报告）。
7. 测试：`patch.test.ts`（257 行新增——locateExact/substring/ambiguous/context）、`apply_patch.test.ts`（unicode 语义重写——Unicode 归一化不再匹配成功 + suppresses unrelated + reports reliable candidate for failed chunk）、`edit.test.ts`（decoy/charmatch actual candidate/suppresses mismatch/suppresses tied）。

## Target 适配
- `match.ts`/docs：target 无同名 → verbatim newfile（157/2172 行）。
- `index.ts`：target 与 source pre 结构一致（computeReplacements/applyReplacements/seekSequence/deriveNewContents）→ 移植 applyChunks + workingText + withCandidate；deriveNewContents 改 applyChunks + normalized 写回；**parseUpdateFileChunks 加"无前缀行→old_lines"分支**（source 宽松 parser 语义——chunk 测试依赖：无 `-`/`+`/空格前缀的 expected 行必须参与 exact 匹配，否则 pure-insertion 吞掉失败）。
- `apply_patch.ts`：删 target 的 extractActualExcerpt（简化版）+ processSingleHunk catch 改直接传 error（不重扫 matcher）。
- `edit.ts`：移除 Replacer 体系（target 的 L275-883）→ 新 replace（indexOf + closestWindow + ambiguity）；import `closestWindow` from `@/patch/match`（两处修复——refactor anchor 因 `// #131` 注释行未匹配，导致 import 缺失）。
- `edit.txt`/`apply_patch.txt`：git apply source hunk（target 同 pre 文本）。
- 测试：
  - `patch.test.ts`：git apply source hunk 全成功。
  - `apply_patch.test.ts`：2 hunk 成功 + 1 rej（unicode 测试重写为 exact-only 失败语义 + suppresses + chunk 测试）。
  - `edit.test.ts`：rej（imports + branded ctxWithPriorRead + decoy/charmatch/suppress/tied 测试重写）。
  - **并发测试修复**（target 既有）：`preserves concurrent edits` 超时——#348 blind edit 检查（messages 空 → "File has not been read" throw → ask 不触发 → Deferred 永不）→ delayedCtx 补 `messages: ctxWithPriorRead(filepath).messages`（不改变并发语义，仅过 read gate）。

## 关键决策记录
- source 语义"exact-only + 唯一 substring + closest 仅诊断"完整移植；fuzzy replacer（rstrip/trim/normalized/whitespace/indentation）全部移除——不保留任何隐式 replacement fallback。
- `withCandidate`/`closestWindow` 只解释失败（错误消息），绝不转成 replacement success——即使分数高。
- parseUpdateFileChunks 无前缀行处理：验证 source chunk 测试（`candidate.replace(...)` 无前缀行作为 old_lines）需要——否则测试中的 expected 被当作 pure insertion 吞掉。

## Verification
- materialize 430 --rebuild：passed 430/430（retained [0430, 0426, 0425, 0416, 0405]）。
- typecheck 430：passed（全部 21 workspace；install 指纹 1165df52… frozen reuse）。
- 行为测试：patch.test 36/36、apply_patch.test 41/41、edit.test 41/41（共 118/118 全绿）。
- E/C = 666/121（need 100）PASS。

## 过程中失败与修复
1. current/0430 的 edit.ts/index.ts 段多次重建（0428/0429 state 被 materialize 清理）→ no-index diff（0426-pre vs 0430-post）重新生成。
2. closestWindow import 缺失（refactor anchor `...line-ending"\nconst locks` 因 target 的 `// #131` 注释行不匹配）→ 补 import + 重新生成 section。
3. chunk 测试失败（`Received: false`——patch 意外成功）→ 根因 parseUpdateFileChunks 忽略无前缀行（chunk2 变 pure insertion）→ 加 else 分支。
4. edit 并发测试超时（5000ms）→ 根因 #348 blind edit 检查（delayedCtx messages 空）→ 补 prior read。
5. typecheck report outcomes 空 / "automatic install/typecheck failed" 多为 .test-workspace 未同步（materialize 不更新 .test-workspace；--typecheck 才同步）→ 用 --typecheck 同步后验证。

## 批次 0426-0430 结语
本批次五 commit（0426 workflow/0427 goal-docs/0428 stats-R6/0429 goal-integrity/0430 patch-match）全部实施并验证：materialize 430 --rebuild passed、typecheck 430 passed、行为测试全绿（goal 52 + migration 2 + patch/apply_patch/edit 118 + attention 21 + notifications 9 + malformed-sgr 7 + config 42 + session-preview 12 + stats 19）。

## 双审计修复记录
- B-01（正向）/N-0430-1（逆向）：target index.ts 残留死代码（normalizeUnicode/Comparator/tryMatch/seekSequence，~67 行）——source 0430 删除、target 保留（无调用者）→ 删除（index.ts L422-488 区域），用 no-index（0426-pre vs 0430-post）重新生成 current/0430 的 index.ts 段。
- N-0430-2：parseUpdateFileChunks 无前缀行→old_lines 分支为 target-only 新增（source 未触碰 parser）——chunk 测试需要（否则 pure-insertion 吞掉失败）；行为与 exact-only 方向一致，无回归；双 auditor 确认。
- N-0430-4：edit.test.ts 残留 #354 旧注释（提及已删除的 BlockAnchorReplacer）——不影响测试，记录。
- N-05：edit.test.ts ctxWithPriorRead 保留 as any（source 用 branded）——测试行为覆盖不变，记录。

## BATCH 0426-0430 VERDICT: PASS（CLOSED）
- 双独立审计（正向 0426→0430 + 逆向 0430→0426）第二轮：两份一致 APPROVE，五 commit 全部 PASS，无开放 B/G。
- Round 1 发现并修复：G-0426-1（.gitignore `//`→`#` 合法注释）、G-0426-2/G-0427-1/G-0428-1/G-0430-1（records 扩至 ≥50 行/≥3000 字符 + 清占位）、B-01（0430 index.ts 死代码删除）、B-02（0429 goal.txt 两行 guardrail 恢复）。
- Round 2 双 auditor 独立验证全部修复 + 全范围重审：5/5 PASS。
- 最终链：materialize 430 --rebuild passed 430/430（retained [0430,0426,0425,0416,0405] ≤5）；typecheck 430 30/30 workspace passed；install 指纹 1165df52…（bun 1.3.14 frozen reuse）。
- 行为回归（修复后复跑）：goal 52/52、migration 2/2、patch/apply_patch/edit 118/118、stats 19/19、attention 21/21、notifications 9/9、malformed-sgr 7/7、config 42/42、session-preview 12/12 全绿。
- E/C：0426 1/1、0427 0/0、0428 84/25、0429 1108/213、0430 664/121 全 PASS。
- N 项记录（非阻塞，建议后续清理）：0430 goal.ts 残留旧 accountUsage 注释（N-3）、records 措辞 21→30 workspace（N-4）、0240 孤儿文件（N-1/N-6）。
