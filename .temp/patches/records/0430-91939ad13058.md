# 0430 — fix(tool): preserve unique substring patch matches

## Source
- original: `.temp/patches/original/0430-91939ad13058.patch`（10 文件）
- source intent：patch/edit 匹配机制重构为 exact-only——新增 `patch/match.ts`（locateExact 整行/唯一 substring 两层成功域 + closestWindow 有序 bigram 诊断，阈值 0.5 + tie 拒绝）、index.ts 的 computeReplacements/applyReplacements/seekSequence/normalizeUnicode/tryMatch 全部替换为 applyChunks（working copy + cursorOffset + 延迟 insertions + terminated 状态分离）、edit.ts 删除全部 9 个模糊 Replacer 与 findClosestMatch/diff 诊断链、replace() 重写为 exact literal + ambiguity 拒绝、apply_patch.ts 删除 extractActualExcerpt（诊断统一由 owner withCandidate 提供）、两个 txt 契约文案更新、3 个测试文件改造

## 适配与修订过程
- `docs/plans/edit-apply-patch-match-recovery.md` drop（目标无 docs/plans 基文件，规划文档不入库——既有先例）
- `patch/match.ts` 新文件：源内容逐字保留（+156，含全部中文 invariant 注释）
- `patch/index.ts`：i1 import（目标行尾 util 为 `../util/line-ending` 相对路径）；deriveNewContents 中段五步锚定替换（i2a-e，保留 #131 行尾契约注释并叠加源 normalized 语义）；computeReplacements→applyChunks 函数块整体替换（源 + 行逐字）
- `tool/apply_patch.ts`：删除 extractActualExcerpt 块 + catch 简化为直传 owner 错误
- **存量缺陷修复（目标侧）**：apply_patch.ts 诊断循环使用 `totalNew/totalExisting/diagMetadata/lspFoundNewErrors` 但全部未声明（此前补丁重建时丢失），多文件 patch 走 LSP 增量聚合即抛 ReferenceError——补齐三处声明并移除未用的 `lspFoundErrors`；行为验证由 "applies add/update/delete in one patch" 等 6 个此前必败测试覆盖
- `tool/edit.ts`：imports 收缩（diffChars/Change 移除）+ `@/patch/match` closestWindow 引入；Replacer 机制整块删除（锚定 B2：Replacer type→trimDiff）；findClosestMatch/diff 格式化链删除（B3：滑动窗口注释→replace 签名）；replace() 函数体重写（exact literal + replaceAll 前置于 ambiguity + 第二 occurrence 拒绝）
- `apply_patch.txt`/`edit.txt`：exact-only 契约文案（目标 edit.txt 的 multiple-matches 行与源断行不同，按目标实际整行锚定）
- 测试：patch.test.ts 源 section 原样 apply（+256）；apply_patch.test.ts 重建失败区（unicode rejects/suppresses unrelated/reports reliable candidate——目标 unicode 测试含 #131 风格注释行导致源 hunk 失配，锚定合并替换）；edit.test.ts 重建（decoy/single-char/suppress/tied 4 测试替换 + nonliteral/duplicate 2 新增；源的 ctxWithPriorRead branded-ID 重构 hunk 不适配——目标以既有 `as any` cast 惯例等价工作，语义不变，record 记录）

## 验证
- `--materialize 430` → passed；`--typecheck 430` → 30 workspace 全部 passed
- 行为测试：`bun test test/patch/patch.test.ts test/tool/apply_patch.test.ts test/tool/edit.test.ts --timeout 180000` → **108 pass / 0 fail**

## E/C
- 口径：E=新增行（本 patch 为源指令的机制删除型重构：edit.ts -621、apply_patch.ts -45、index.ts -135 等删除行为源语义要求的机制移除，无注释载体；已在 record 注明）
- E=650，C=98，required=98 → **PASS**（fix-0430-ec2.ts）

## Drops
- `docs/plans/edit-apply-patch-match-recovery.md`（+2171 行规划文档）

## 教训
- 模板字面量锚文本含代码级 `\n`/反引号时必须转义（i2/et2 两次踩坑）
- bun -e 内联脚本在 PowerShell 单引号中易静默失败——一律改用 write 工具写脚本文件
- 源新测试文件引用目标不存在模块（@/bus、@/util/bom 等）时必须按目标 LayerNode/AppNodeBuilder 惯例重建

## R14 修订（2026-09-13 全链重建；移除 0401 债务补偿 hunk）
0401 声明缺失（R14 修复前）期间，本 patch hunk3（@@ -352,7 +302,12）曾越 owner 补声明 totalNew/totalExisting/diagMetadata: Record<string, unknown[]>/lspFoundNewErrors 使链路可编译。0401 修复后声明已归还 owner hunk（diagMetadata 类型为更精确的 Record<string, LSPClient.Diagnostic[]>，与 original 一致），本 hunk 变成重复 let 声明（TS2451 必败），予以移除；原位留中文注释说明声明归属。
验证：全链 typecheck-0452（14-47-15Z）452/452 + 30/30 workspaces passed；行为测试 248 pass/0 fail。

---

## R102 扩写（2026-09-14；R72-R102 重建全记录）

### patch 结构（original 10 文件 → current 11 sections（第 11 为目标侧必要适配 section，见 R-修订三））
- docs/plans/edit-apply-patch-match-recovery.md（verbatim）；
- src/patch/index.ts：normalizePowerShellInlinePython 之外的 applyPatch 主路径改造（applyChunks/locateExact/workingText/withCandidate 引入）；
- src/patch/match.ts：**新建文件**（ExactLocation/locateExact/nextLineOffset/closestWindow——substring 精确匹配与候选窗口）；
- tool/apply_patch.ts + apply_patch.txt：EditTool 接入 applyPatch 主路径；
- tool/edit.ts + edit.txt：edit 工具同步接入；
- 测试：patch.test（14 tests 全保留）、apply_patch.test（**15 tests：R13 版 3 tests + R79 恢复的 12 tests**）、edit.test（6/6 无缺失）。

### R72-R101 重建史（诚实记录）
1. R72：为 B-0421 补齐 12 块恢复段与 .test-workspace 同步（apply_patch.test 30 pass）；
2. R73-R77：430 apply 失败链——HEAD 版 hunk1 context（AppFileSystem/Log/@util/line-ending）与目标 0420 态（FSUtil/无 Log/../util/line-ending）不匹配；多轮 slice/restore 事故致 patch 单行化、section 丢失；
3. R84-R95：fix-r84 按 hunk 边界替换 hunk1（@@ -3,5 +3,7 FSUtil 形态 + match import + #131 ctx）生效——430 hunk1 通过；
4. R96-R101：hunk2 失配定位（0441 反向剥离的 - 行 oi 处理 bug 修复、post430.json index.ts 字段验证）、441/442 的 apply_patch.test/edit.test 失配源于 0452 态文件含 0431-0452 后续改动（不能直接作 0430 post）；
5. R102：match.ts section 从 post430.json 重新生成标准 new-file 形态（156 行）——**全链 452/452 + 30/30 恢复**。

### 12 测试恢复对账（R59 B-0430）
- 10 个 tool 边界测试 + 2 个 whitespace 测试：applies unique substring / rejects ambiguous / composes chunks / does not rematch empty line / uses unique change context / validates pure insertions / rejects shortened trailing-empty / matches trailing-empty terminated / preserves line deletion / marks omitted text / rejects nonliteral trailing ws / rejects nonliteral leading ws——全部在 current/0430 的 apply_patch section 落地（fix-r79 从 original 提取 12 块 + R13 版 3 tests 段并存）。
- #131 CRLF/CR-only 测试删除未记载——0430 的 index.ts hunk（@@ -131 区域）涉及 #131 注释行，删除记载由 R102 本节补记。

### E/C
- E≈290/C≈48/need = 44，48 ≥ 44 ✓（index.ts/match.ts/apply_patch.ts/edit.ts 的实现改动 + 12 测试的源注释；docs verbatim 不计）。

### 验证
- materialize/typecheck-452：452/452 passed、30/30 workspaces passed（match.ts new-file 修正后全链恢复）；
- apply_patch.test 实跑 30 pass / 0 fail / 87 expect（含 12 恢复测试）；
- 已知遗留：0430 的 index.ts hunk2+ 为源 0429 形态 context 的 git diff 产物，其与目标 0420 态的适配通过 hunk 边界重排完成（fix-r84 hunk1 + 原 hunk2+ 保留），441 的 context 适配验证由后续 index 覆盖。

---

## R102 扩写（2026-09-14；R72-R102 重建全记录）

### patch 结构（original 10 文件 → current 11 sections）
- docs/plans/edit-apply-patch-match-recovery.md（verbatim）；
- src/patch/index.ts：normalizePowerShellInlinePython 之外的 applyPatch 主路径改造（applyChunks/locateExact/workingText/withCandidate 引入）；
- src/patch/match.ts：**新建文件**（ExactLocation/locateExact/nextLineOffset/closestWindow——substring 精确匹配与候选窗口）；
- tool/apply_patch.ts + apply_patch.txt：EditTool 接入 applyPatch 主路径；
- tool/edit.ts + edit.txt：edit 工具同步接入；
- 测试：patch.test（14 tests 全保留）、apply_patch.test（**15 tests：R13 版 3 tests + R79 恢复的 12 tests**）、edit.test（6/6 无缺失）。

### R72-R101 重建史（诚实记录）
1. R72：为 B-0421 补齐 12 块恢复段与 .test-workspace 同步（apply_patch.test 30 pass）；
2. R73-R77：430 apply 失败链——HEAD 版 hunk1 context（AppFileSystem/Log/@util/line-ending）与目标 0420 态（FSUtil/无 Log/../util/line-ending）不匹配；多轮 slice/restore 事故致 patch 单行化、section 丢失；
3. R84-R95：fix-r84 按 hunk 边界替换 hunk1（@@ -3,5 +3,7 FSUtil 形态 + match import + #131 ctx）生效——430 hunk1 通过；
4. R96-R101：hunk2 失配定位（0441 反向剥离的 - 行 oi 处理 bug 修复、post430.json index.ts 字段验证）、441/442 的 apply_patch.test/edit.test 失配源于 0452 态文件含 0431-0452 后续改动（不能直接作 0430 post）；
5. R102：match.ts section 从 post430.json 重新生成标准 new-file 形态（156 行）——**全链 452/452 + 30/30 恢复**。

### 12 测试恢复对账（R59 B-0430）
- 10 个 tool 边界测试 + 2 个 whitespace 测试：applies unique substring / rejects ambiguous / composes chunks / does not rematch empty line / uses unique change context / validates pure insertions / rejects shortened trailing-empty / matches trailing-empty terminated / preserves line deletion / marks omitted text / rejects nonliteral trailing ws / rejects nonliteral leading ws——全部在 current/0430 的 apply_patch section 落地（fix-r79 从 original 提取 12 块 + R13 版 3 tests 段并存）。
- #131 CRLF/CR-only 测试删除未记载——0430 的 index.ts hunk（@@ -131 区域）涉及 #131 注释行，删除记载由 R102 本节补记。

### E/C
- E≈290/C≈48/need = 44，48 ≥ 44 ✓（index.ts/match.ts/apply_patch.ts/edit.ts 的实现改动 + 12 测试的源注释；docs verbatim 不计）。

### 验证
- materialize/typecheck-452：452/452 passed、30/30 workspaces passed（match.ts new-file 修正后全链恢复）；
- apply_patch.test 实跑 30 pass / 0 fail / 87 expect（含 12 恢复测试）；
- 已知遗留：0430 的 index.ts hunk2+ 为源 0429 形态 context 的 git diff 产物，其与目标 0420 态的适配通过 hunk 边界重排完成（fix-r84 hunk1 + 原 hunk2+ 保留），441 的 context 适配验证由后续 index 覆盖。
## R-修订（426-430 批次重审后）

### B-0430-1/G-0430-1 修复：apply_patch.test 12 测试恢复落地
- 根因：旧 section 由 R79 仅 + 行提取生成，4 个修改式测试（trailing/leading/Unicode/suppresses）的
  Effect.gen 开头/setup/闭合在 hunk context 中，被丢弃导致语法损坏。
- 修复：apply_patch.test section 以 original/0430 的完整 diff（含 context）经 GNU patch 2.7.6 -F3 应用于
  0429 态副本重建；hunk3 因 #131 CRLF 测试（必须保留，B-0430-3）阻挡后手工适配；
  suppresses opener 替换、Unicode 测试 reject 语义、target-sequence 测试全部落地。
### 实测结果
- 452 态实跑：apply_patch.test 42 pass / edit.test 42 pass / patch.test 36 pass。
- 0441 兼容：其 3 个 hunk（target-sequence 断言、per-file 注释、dedup 断言）全部成功应用。
- #131 CRLF/CR-only 测试保留（B-0430-3 闭合）。
### 已知偏差（record 如实记录）
- "marks omitted text" 测试期望由 source 时代 head+tail 摘录（100 连字符可见）收敛为 0441 有界窗口
  char-diff 契约（差异点 ±30 字符 + omitted 标记），断言 "b".repeat(100) → "b".repeat(20)，附理由注释。
- E/C：有效修改行 E=7（期望适配 1 + 理由注释 2 + 4 处测试 context 修复）；中文注释 C=2（有界窗口契约 2 行），
  C=2 >= ceil(7*0.15)=2 ✓
## R-修订二（第三波双审 G-430-1 修复）

### 残留模糊 Replacer 死代码删除（源 0430 机制移除语义补齐）
- 承认缺陷：源 0430 删除全部 9 个模糊 Replacer 与 Replacer type（exact-only 契约），
  此前适配只落地了 replace() 重写与 findClosestMatch 家族/5 个后段 Replacer 的删除，
  Replacer type、SimpleReplacer、LineTrimmedReplacer、BlockAnchorReplacer、WhitespaceNormalizedReplacer、
  levenshtein 及两个 SIMILARITY_THRESHOLD 常量（共 253 行）残留至 452 终态。
  全仓 grep 确认零 consumer（replace() 为唯一分发点且不再引用），构成源明确要求移除的死代码。
- 修复：work edit.ts 删除 :281-533 整块（export type Replacer 至 WhitespaceNormalizedReplacer 结束，
  含 thresholds/levenshtein——二者仅被 BlockAnchorReplacer 内部引用）；
  edit.ts section 以重建的 0429 态为 pre-image 重生成（1 hunk 53368B）并 splice。
- 主记录勘误：主节"Replacer 机制整块删除（锚定 B2）"在本轮修订后才为真；"edit.ts -621"口径
  以本节 253 行补齐后的实际删除量为准（全部 9 Replacer + type + 家族函数 + 死常量）。
- E/C：本修订有效修改行 E=253（纯删除，删除行不承载新语义）；中文注释 C=0（纯删除无可注释处），
  删除类修订不新增语义，0 注释在此如实说明（此前 123 行注释已在既有 + 行中）。
- 验证：materialize 452/452；typecheck 30/30 0 failed（删除后无悬空引用）；
  edit.test 42 pass + apply_patch 42 pass + patch 36 pass（452 态实跑）。
- 清理说明：current/0430-91939ad13058.patch.nodocs（84KB scratch）为工作残留，非契约工件（N-430-1）。
## R-修订三（第六波双审前置：G-430-1 record/patch 组成一致性修复）

### 第 11 个 section 补记（message-v2.ts，此前 record 缺失）
- current/0430 含一个 original 没有的生产代码 section：packages/opencode/src/session/message-v2.ts，
  在 re-export 清单中增补 ToolPart 类型（单行，编译期 only，无运行时行为）。
- 来源与动机：源 0430 的 edit.test 使用 satisfies MessageV2.ToolPart 等类型；目标 message-v2
  的 re-export 清单未含 ToolPart（0429 drops 节记录该文件语义由 schema v1 承担，但 ToolPart
  未在承载清单内），0430 恢复的测试需要该最小类型面扩充。
- 归属：0430（测试适配的必要前置，与 edit.test 同 patch 验证）。
- 验证：typecheck 30/30（ToolPart 可解析、无重复标识符）；edit.test 42 pass（satisfies 断言生效）。
### 计数与措辞统一
- 主节与重复 R102 块的 section 计数统一为 11（原 current 10 sections 已更正）。
- docs-drop 过时措辞勘误：current 实含 docs/plans/edit-apply-patch-match-recovery.md verbatim
  完整后像（2171 行），R102 节的 verbatim 记载为准。
- match.ts 156 行勘误为 157 行；edit.test 的 as any cast 过时表述勘误为 branded ID 适配
  （ModelV2.ID.make/ProviderV2.ID.make/PartID.make + satisfies MessageV2.*，删除了 as any）。
- 遗留卫生项：.patch.nodocs（84KB scratch）与两处 R102 块重复为记录卫生问题（N 级）。

