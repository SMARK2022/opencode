# Record 0323-018c14b9317d — feat(diff): 实现工具调用流聚合的文件修改追踪，支持并行工具的改动归因

## Identity
- index/full SHA: 0323 / 018c14b9317d6d84fe99d8a99d37b74fa2c24e95 (short 018c14b9317d)
- manifest parent: 16b5705b4f70 (0322)
- subject: feat(diff): 实现工具调用流聚合的文件修改追踪，支持并行工具的改动归因
- current SHA-256: cce5e95dd76f755c / 18833 bytes
- status: PASS

## Source intent (original/0323-018c14b9317d.patch, 392 行, 2 files)
1. `packages/opencode/src/session/summary.ts` (L14-197): 加 collectToolDiffs 函数——按工具调用流聚合文件修改；InstanceState.context 获取 worktree；合并工具 diff 和 git diff（工具覆盖的文件优先按工具归属，仅补齐工具未触及的文件）；9 个 helper 函数（toWorktreeRel, countPatchStats, inferStatus, collectToolDiffs, heaviestStatus, mapStatus, stringValue, numberValue, arrayValue）
2. `packages/opencode/test/session/summary-tool-diff.test.ts` (L198-392): 新测试文件——5 个测试用例验证工具 diff 聚合、并行工具归因、git 兜底、hidden parts 跳过

## Three-way comparison
- source/SMARK: 工具调用流聚合追踪文件修改，支持并行工具改动归因；git diff 作为兜底
- 上游 v1.17.20: 仅用 snapshot.diffFull(from,to) 整树快照，无工具流聚合
- 目标当前: summary.ts 有 computeDiff 但仅用 snapshot.diffFull；无 collectToolDiffs
- 结论: 加 collectToolDiffs + 9 helper + 新测试文件

## Target adaptation
- summary.ts: 同路径 `packages/opencode/src/session/summary.ts`
  - 加 imports: path, InstanceState
  - computeDiff 加 InstanceState.context + collectToolDiffs 调用 + 合并逻辑
  - 加 9 helper 函数（toWorktreeRel, countPatchStats, inferStatus 等）
- test: 新建 `packages/opencode/test/session/summary-tool-diff.test.ts`
  - 目标结构差异适配：SessionV1.WithParts 替代 MessageV2.WithParts；Model.ID/Provider.ID 替代 ModelID/ProviderID
  - 层装配适配：AppNodeBuilder.build + LayerNode.group([CrossSpawnSpawner.node, LayerNodePlatform.filesystem, **LayerNodePlatform.path**, InstanceStore.node, Session.node, SessionSummary.node])
  - 关键修复：CrossSpawnSpawner.node 依赖 [filesystem, path]，缺少 LayerNodePlatform.path 导致 ChildProcessSpawner 未初始化 → 测试全失败。添加 path 后 5/5 通过。
- 方法: state 0322 语义编辑（edit323.py subagent + 手动层修复）→ git diff HEAD → current/0323

## 逐文件映射
| Source file | Target file | 状态 |
|-------------|-------------|------|
| summary.ts | packages/opencode/src/session/summary.ts | ✓ 同路径，加 collectToolDiffs + helpers |
| summary-tool-diff.test.ts | packages/opencode/test/session/summary-tool-diff.test.ts | ✓ 新建，层装配适配 |

## Verification
- materialize 323/323: passed
- typecheck 323: install reused（frozen cbdc173），packages/app TS1128（环境基线），integrityFailure None
- summary tool diff test（.test-workspace synced to 323）: `bun test ./test/session/summary-tool-diff.test.ts --timeout 60000` → 5 pass / 0 fail / 13 expect() / 5.82s
- E/C: E=268 / C=51 / need=max(1,ceil(268*0.15))=41 → PASS
- source/target HEAD: 未变
- states 保留 ≤5

## 特殊适配
- 目标用 SessionV1.WithParts 替代 source 的 MessageV2.WithParts（目标 message-v2 不导出 WithParts）
- 目标用 Model.ID.make()/Provider.ID.make() 替代 source 的 ModelID.make()/ProviderID.make()（目标无 provider/schema.ts）
- 目标用 AppNodeBuilder.build + LayerNode.group 替代 source 的 Layer.mergeAll + defaultLayer（目标无 defaultLayer 导出）
- **关键修复**：CrossSpawnSpawner.node 依赖 [filesystem, path]；初始版本缺少 LayerNodePlatform.path 导致 ChildProcessSpawner 未初始化，5 测试全失败；添加 path 后全通过
- InstanceBootstrap.node 用 Layer.succeed 替换为 noop（避免拉入 Format/LSP/Plugin 重依赖）

## DUAL-AUDIT SEAL (batch 321-325)
- R1 dual audit:
  - Forward (ses_fdbcb1204ffe9iLg46Odc8wB2Z): 5/5 PASS, no open B/G
  - Backward (ses_fdbcaf978ffebcDXZdeYRtKY3s): 5/5 PASS, no open B/G
- Verified: manifest/proof hashes, original LF-SHA, current SHA+bytes, install cbdc173 reused, E/C all pass (0/0, 88/24, 268/51, 65/11, 26/6), cumulative apply 325/325, states <=5, source/target unchanged
- Environmental N: packages/app TS1128; voice transcribe uses native spawn (architectural)
- Key verified: MCP SDK 1.29.0 already has windowsHide fix (NO-OP correct); 0323 LayerNodePlatform.path critical fix; 0325 pristine original
- Batch: APPROVE (5/5 PASS, no open B/G)