# Record 0368-8fba91e68d89

## Identity
- manifest parent: df884f1416ce (0367)
- subject: fix(tui): 修复 /goal dialog 提交无响应，改用 fetch 直调 HTTP 端点
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0368-8fba91e68d89` (passed 368/368)
- current patch: `.temp/patches/current/0368-8fba91e68d89.patch` (14046 bytes)

## Source intent
SDK 未重新生成 goalSet/goalClear 方法，as any 调用运行时为 undefined
导致 TypeError 静默失败，Enter 按键无反应。改为用 sdk.fetch 直接调用
POST/DELETE /session/:id/goal，并手动设置 directory query param（与
dialog-session-list.tsx 一致），确保多实例 daemon 模式下路由正确。
sync.tsx 的 goal 读取同步改为 sdk.fetch GET（GET 由 sdk.fetch 的
rewrite 自动加 directory）。补充 httpapi-exercise scenario
（GET/POST/DELETE /session/:id/goal）解决 CI coverage missing=3。新增
两个回归测试：确认 SDK Session2 类上没有 goalSet/goalClear/goal
方法，以及 dialog-goal.tsx 使用 fetch 而非 SDK 方法。

## Source patch anatomy (4 files)
1. `cli/cmd/tui/component/dialog-goal.tsx` — useGoalApi hook 封装
   (setGoal/clearGoal 走 fetch)，三组件改用 hook。
2. `cli/cmd/tui/context/sync.tsx` — goal fetch 改 sdk.fetch GET。
3. `test/server/httpapi-exercise/index.ts` — 3 个 goal scenario。
4. `test/session/goal.test.ts` — 2 个回归测试（it.live）。

## Target mapping (state 0367)
| Source file | Target file | Status |
|---|---|---|
| `cli/cmd/tui/component/dialog-goal.tsx` | `packages/tui/src/component/dialog-goal.tsx` | ADAPTED (0367 新建的同一文件) |
| `cli/cmd/tui/context/sync.tsx` | `packages/tui/src/context/sync.tsx` | ADAPTED (目标 sdk.url/sdk.fetch 存在) |
| `test/server/httpapi-exercise/index.ts` | same | ADAPTED (插入点位于 unshare 与 append-prompt 之间) |
| `test/session/goal.test.ts` | same | ADAPTED (dialog-goal 读取路径改为 ../../tui/...) |

## Target-adaptation decisions
1. **dialog-goal 与 sync 完全复用目标 SDK 上下文**：目标 sdk.tsx 暴露
   `sdk.url`、`sdk.directory`、`sdk.fetch`（fetchWithHeaders，自动带
   attach/auth headers），useGoalApi 直接照搬 source；`sync.path.directory`
   在目标 path getter 中存在。0367 版组件（as any 调用）被整体替换。
2. **httpapi-exercise 插入点**：目标 index.ts 在
   `.delete("/session/{sessionID}/share", ...)` 与
   `.post("/tui/append-prompt", ...)` 之间插入三个 goal scenario；
   scenario 第三参（"session.goal.get" 等）只作名称过滤用，routeKey 由
   method+path 计算，与 0364 注册的端点（/session/{sessionID}/goal 的
   GET/POST/DELETE）匹配。`route()` 模板替换与 `ctx.session(...)` seed
   用法与既有 scenario 一致。
3. **goal.test.ts 回归测试路径适配**：source 读
   `src/cli/cmd/tui/component/dialog-goal.tsx`（测试 cwd = opencode 包内）；
   目标该文件位于 `packages/tui`，改为
   `path.join(process.cwd(), "../../tui/src/component/dialog-goal.tsx")`。
   `it.live` 在目标 testEffect helper 中存在（make 返回 {effect, live,
   instance}）。这两个测试在 0369 会被整体删除（CI 下 SDK build 会生成
   goal 方法导致 toBeUndefined 失败），本步只保证中间态可编译。
4. **sync.tsx 目标形态**：目标的 goal fetch 位于 session sync 的
   Promise.all 之后；替换为 `sdk.fetch(\`${sdk.url}/session/${sessionID}/goal\`)`
   并沿用 try/catch 非致命语义；`(data as any)?.goal` 保留类型断言。

## E/C audit
E=118, C=23, need=18, PASS. (C 覆盖：useGoalApi 的 URL/端点注释、
sync 的 fetch 替代注释、exercise 的 scenario 说明注释、regression guard
的删除提醒注释。)

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 368` →
  "passed 368/368"，state at `states/0368-8fba91e68d89`。
- Report: `materialize-0368-2026-08-24T21-18-00-089Z.json` — reusedPrefix
  367, baseState `0367-df884f1416ce`, stoppedAt 空, integrityFailure 空。
- Post-apply verification: useGoalApi 存在三处 setGoal/clearGoal 调用；
  exercise 三个 goal scenario 位于 append-prompt 前；goal.test.ts 含
  两个 it.live 回归测试。

## Line accounting
- dialog-goal.tsx: +43/-47（hook 化净增）。
- sync.tsx: +4/-5。
- httpapi-exercise/index.ts: +34。
- goal.test.ts: +43。

## Risks / notes
- 0368 引入的"SDK 无 goal 方法"回归测试是瞬态断言：CI turbo test:ci 依赖
  ^build 会重新生成 SDK（包含 goal 方法），toBeUndefined 必然失败 ——
  这正是 0369 删除它们的原因，本批内自洽。
- dialog-goal 的 fetch 直调依赖 workspace-routing 对 /session/preview
  （批量）与 /session/:id/goal（含 sessionID）的既有路由规则；goal 路径
  带合法 ses_ ID，不受 0366 的 preview 例外影响。
- 若 SDK build 在目标环境采用不同生成器输出，`(client.session as any)`
  形状断言在 0369 删除后不再停留于代码库。

## Per-item diff stats
- fix368.diff bytes: 13022 (raw diff, LF); patch file with header: 14046。
- 4 文件（全部修改，无新建/删除）。