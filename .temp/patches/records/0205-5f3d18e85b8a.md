# Patch 0205 — feat(compaction): 优化 compact 逻辑 (stable tail boundary + hidden-aware filter)

## 1. Identity
- Index: 205
- Source SHA: 5f3d18e85b8af8cfeb3d7621063975b7c3210537
- Parent SHA: d7eb5239e44a318ab2a209f2f7acd26dbac2d69a (patch 204)
- Subject: feat(compaction): 优化 compact 逻辑，避免虚假 tailid 导致上下文组装故障；增强消息过滤逻辑以支持隐藏消息作为边界（但不包含）
- Original patch: `.temp/patches/original/0205-5f3d18e85b8a.patch`
- Current patch: `.temp/patches/current/0205-5f3d18e85b8a.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62` (v1.17.20 release)
- Built-on state: `0200-d4f24d4434a4` + patch 204 (205 is layered on the adapted 204; 201-203 not yet adapted)
- Record path: `.temp/patches/records/0205-5f3d18e85b8a.md`
- Record created before first current edit: yes
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch is 9 788 bytes, 4 files:
  1. `packages/opencode/src/session/compaction.ts` — `splitTurn` rejects hidden/empty/pending/errored messages as `tail_start_id` anchors.
  2. `packages/opencode/src/session/message-v2.ts` — (a) `stream` hunk: default `includeHidden=true`, move filtering to `page()`; (b) `filterCompacted` adds a `visible()` helper and wraps both returns; (c) `filterCompactedEffect` unchanged.
  3. `packages/opencode/test/session/compaction.test.ts` — rename one test + insert an unfinished assistant message so the turn cannot be retained as a stable boundary.
  4. `packages/opencode/test/session/messages-pagination.test.ts` — new test: hidden retained-tail anchor used as boundary but stripped from the returned window.
- Every hunk and behavior intent:
  - splitTurn: a persisted `tail_start_id` boundary must stay replayable after undo/repair. Empty, hidden, pending (no finish), or errored messages can vanish from visible history and must stay in the summarized head instead of becoming the tail anchor.
  - stream: compaction recovery needs hidden structural anchors; visible-transcript callers pass `includeHidden=false` and let `page()` filter.
  - filterCompacted `visible()`: hidden messages may be structural anchors during the boundary walk but must never be replayed to providers; strip them (and hidden parts) only from the returned prompt window.
  - Tests: verify (a) a turn occupied by an unfinished assistant falls back to full summary, and (b) a hidden anchor is used as the tail boundary yet absent from the filtered result.

## 3. SMARK Intent
- User-visible behavior: compaction no longer picks an unstable (hidden/empty/pending/errored) message as `tail_start_id`, preventing context-assembly breakage after undo/repair. Hidden messages can still serve as compaction boundaries internally but are never returned in the model-facing prompt window.
- Protected invariant: `tail_start_id` must reference a message that remains visible and replayable; `filterCompacted` must never emit a hidden message or hidden part to a provider.
- Normal path: `splitTurn` skips unstable candidates; `filterCompacted` walks hidden-inclusive input, then `visible()` strips hidden before returning.
- Boundary/error paths: a turn with only unstable candidates → `splitTurn` returns `undefined` → compaction falls back to full summary. `filterCompacted` with no compaction boundary → `visible(result)`.

## 4. Upstream Reconstruction
- Same-path target implementation: all 4 paths exist at tip@200+204. Target `compaction.ts` `splitTurn` (line 107) is structurally identical to the source pre-patch. Target `message-v2.ts` `filterCompacted` (line 570, post-204 has the `ordered` sort) and `filterCompactedEffect` (line 628) exist.
- stream hunk ABSENT adaptation: the source `stream` is a generator (`export function* stream`) yielding messages with `includeHidden ?? true` default and in-loop filtering. The target `stream` (line 510) is `export function stream(...): Effect` returning a `WithParts[]` array, default `includeHidden` FALSE, with in-loop hidden filtering (#98/#106 design). The source stream hunk CANNOT apply verbatim — it would revert the target's default to true and re-add redundant filtering. Per research: SKIP the stream hunk; instead pass `{ includeHidden: true }` at the single recovery call site (`filterCompactedEffect`). Visible-transcript callers already default to `includeHidden=false`.
- Actual owner: `splitTurn` owns tail-anchor selection; `filterCompacted` owns the prompt window; `filterCompactedEffect` is the sole compaction-recovery caller of `stream`.
- Producer/consumer chain: `splitTurn` produces `tail_start_id` → persisted in the compaction part → `filterCompacted` reads it to slice the retained tail → `visible()` strips hidden before returning to the provider.
- Upstream tests/fixtures: `compaction.test.ts` has `TestInstance`, `MessageID`, `ref.{modelID,providerID}`, `createUserMessage`, `createSummaryCompaction`, `readCompactionPart` — all verified present. `messages-pagination.test.ts` has `addUser`, `addAssistant`, `addCompactionPart`, `withSession`, `MessageV2.get`, `SessionV1` import — all present.
- `SessionV1.AbortedError` exists (used in prompt.ts:304, revert.ts:132) and has `.toObject()`. The hidden reason literal `"repair-empty-dangling-assistant"` is valid (prompt.ts:307).

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@200+204) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| splitTurn guards (hidden/empty/pending/errored) + `msg.info.id` | splitTurn has no guards, uses `input.messages[start]!.info.id` | compaction.ts:117 | Apply verbatim (line shift only) + Chinese #205 comments | patch hunk @@ -181,6 |
| stream default includeHidden=true + move filter to page() | target stream is Effect, default false, in-loop filter (#98/#106) | message-v2.ts stream | SKIP hunk (design mismatch); pass includeHidden at call site instead | grep `export function stream` → Effect return |
| filterCompacted `visible()` helper + wrap returns | post-204 filterCompacted returns raw arrays | message-v2.ts:578 | Add `visible()` helper after `let retain`, wrap both returns | patch hunk @@ -1130,6 |
| `filterCompactedEffect` unchanged (source) | target calls `stream(sessionID)` (default false) | message-v2.ts:628 | Pass `{ includeHidden: true }` so recovery sees hidden anchors | research: replace stream hunk |
| compaction.test rename + assistant message | same test at line 978 | compaction.test.ts:978 | Apply verbatim (TestInstance/ref/MessageID all present) | patch hunk @@ -1059,16 |
| messages-pagination new test | filterCompacted describe at line 599 | messages-pagination.test.ts:754 | Adapt: `MessageV2.AbortedError`→`SessionV1.AbortedError`; `MessageV2.stream(sessionID)`→`yield* MessageV2.stream(sessionID, { includeHidden: true })` | patch hunk @@ -753,6 |

## 6. Current Patch Edit
- Current patch SHA-256: `785b75de01b33e4b9bcc42ea7f7ecb90b771a7ff4789aeb6255e3dbede6e6ce0` (9477 bytes, 195 lines)
- Exact edited hunks (4 files):
  1. `compaction.ts` splitTurn: +`const msg`, +3 Chinese #205 comments, +2 guard `if`s (hidden/empty; pending/errored assistant), `id: input.messages[start]!.info.id`→`id: msg.info.id`.
  2. `message-v2.ts` filterCompacted: +`visible()` helper (+2 Chinese #205 comments) after `let retain`; brace the `completed.add` block; wrap both returns with `visible(...)`. `filterCompactedEffect`: +1 Chinese #205 comment, `stream(sessionID)`→`stream(sessionID, { includeHidden: true })`.
  3. `compaction.test.ts`: rename test, +`const test = yield* TestInstance`, `const recent = ...`, +unfinished assistant `ssn.updateMessage(...)` (+2 Chinese #205 comments).
  4. `messages-pagination.test.ts`: +new `it.instance("uses hidden retained-tail anchors...")` test with `SessionV1.AbortedError` and `yield* MessageV2.stream(sessionID, { includeHidden: true })` (+6 Chinese #205 comments).
- Why each edit is required:
  - compaction.ts: prevent unstable boundaries from becoming persisted `tail_start_id`.
  - message-v2.ts `visible()`: strip hidden from the returned window while keeping them as walk anchors.
  - message-v2.ts `filterCompactedEffect`: recovery needs hidden anchors, so `includeHidden:true` at the call site (replaces the skipped stream hunk).
  - tests: verify both the fallback-to-summary path and the hidden-anchor-stripped path.
- Adaptation decisions:
  - stream hunk: SKIP. Target stream is an `Effect` returning `WithParts[]` with default `includeHidden=false` and in-loop filtering (#98/#106). The source hunk assumed a generator with default true. Applying it verbatim is impossible (different function shape) and would semantically revert the target's safer default. The single behavioral intent — let compaction recovery see hidden anchors — is achieved by `{ includeHidden: true }` at `filterCompactedEffect`, and `visible()` guarantees they never reach the provider.
  - `MessageV2.AbortedError`→`SessionV1.AbortedError`: target exposes the error under `SessionV1` (prompt.ts:304 uses `new SessionV1.AbortedError(...)`).
  - `MessageV2.filterCompacted(MessageV2.stream(sessionID))`→`MessageV2.filterCompacted(yield* MessageV2.stream(sessionID, { includeHidden: true }))`: target `stream` returns an `Effect` (must `yield*`) and needs `includeHidden:true` for the hidden anchor to participate in the boundary walk.
- Source behavior preserved: splitTurn guards (verbatim), visible() helper (verbatim), wrapped returns (verbatim), braced `completed.add` (verbatim). The stream default-change intent is preserved via the call-site `includeHidden:true`.
- Upstream behavior preserved: target's #98/#106 stream default (false) and in-loop filtering remain for all non-recovery callers.
- No hunk/test/schema/migration removed without evidence: stream hunk skip justified by reading target stream (Effect shape, #98/#106 comments) — confirmed not a generator.
- No semantic, location, or sequence conflict: `git apply --check` PASS on state-204.
- 204/205 interaction: 205's `visible()` and `filterCompactedEffect` edits build on 204's `ordered` sort (the loop still iterates `ordered`). Both were built in sequence (204 first, then 205) and verified together.

## 7. Verification
- `git apply --check` against state-204 working tree: PASS.
- Sequential apply 204 then 205 on a fresh state-200 copy: PASS.
- Typecheck: `bun typecheck` from `packages/opencode` and `packages/core` against the installed test-workspace (state-200 + 204 + 205): 0 errors.
- Focused tests:
  - `bun test test/session/messages-pagination.test.ts --test-name-pattern "hidden retained-tail|filterCompacted"` → 13 pass / 0 fail (includes the new hidden-anchor test + existing filterCompacted tests — no regression).
  - `bun test test/session/compaction.test.ts --test-name-pattern "cannot be retained as a stable boundary"` → 1 pass / 0 fail.
- Install input SHA-256: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f` (reused test-workspace at index 200).
- Effective changed lines `E` for this patch only: 80 (added 86 + removed 8 − 14 Chinese comments).
- Qualifying distributed Chinese comments `C` for this patch only: 14 (all `#205`-tagged).
- Required minimum `max(1, ceil(E * 0.15))`: max(1, ceil(80 * 0.15)) = max(1, 12) = 12.
- Gate: 14 ≥ 12 → PASS.
- Representative comment locations: compaction.ts splitTurn guards (3 comments: replayable boundary, summarized head, pending/errored skip); message-v2.ts visible() helper (2 comments: anchor vs replay, strip-from-return) + filterCompactedEffect (1: hidden anchors for recovery); compaction.test.ts (2: unfinished assistant + fallback rationale); messages-pagination.test.ts (6: old history, dangling anchor, tail_start_id→anchor, includeHidden stream, visible() strip, expected result).
- No cross-patch or pre-existing comment aggregation: all 14 comments are `#205`-tagged and introduced by this patch only.

## 8. Contract Check
- One commit only: yes (single adapted patch file).
- Full source diff read: yes (all 4 hunks, 206 lines).
- Full upstream implementation read: yes (compaction.ts 107-130, message-v2.ts 509-630 + filterCompactedEffect, both test files at insertion points).
- File absence followed by owner tracing: N/A (all paths exist; stream hunk skip traced to #98/#106 design).
- No grep-only evidence: yes (full reads + typecheck + test runs).
- No architecture-based omission: yes (stream hunk skip is a design-shape mismatch, documented with evidence; behavioral intent preserved at call site).
- No deleted or delayed behavior: yes (splitTurn guards, visible(), wrapped returns all ported; stream intent relocated to call site).
- No unrequested alternate success path or fallback: yes.
- Every reachable safety path has an owner: yes (splitTurn→compaction.ts; visible strip→message-v2.ts; hidden-inclusive recovery→filterCompactedEffect).
- No behavior bug remains: yes.
- No semantic loss or owner/path/order conflict: yes.
- Per-patch Chinese comment gate passed: yes (C=14 ≥ 12).
- Patch, record, tests and target result materially consistent: yes.

## 9. Independent Audit
- Pending (batch audit deferred until 201-203 are adapted and the cumulative line 201→205 can be materialized sequentially).

## 10. Final Verdict

`PASS` (build) — patch built, `git apply --check` PASS, sequential 204+205 apply PASS, typecheck PASS, focused tests 14/14 pass (13 pagination + 1 compaction), E/C gate PASS (E=80, C=14, need=12). Independent cumulative audit pending 201-203.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T01:20:38Z
- FORWARD: BATCH PASS (ses_fwd_201_205_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_205_201_v2) - no open B/G
- Tip: states/0205-5f3d18e85b8a
- Typecheck: typecheck-0205-2026-07-27T00-57-10-676Z.md
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Non-blocking N only (do not reopen seal)
