# Patch 0224 — feat: add notebook editing capabilities and metadata handling

## 1. Identity
- Index: 224
- Source SHA: 1b8b8b9dae13973fba192e5f463d2fc96e051295
- Parent SHA: f71acd28f6549852fa2c997c5f38f225aaea8dd8（patch 223）
- Subject: feat: add notebook editing capabilities and metadata handling
- Original patch: `.temp/patches/original/0224-1b8b8b9dae13.patch`
- Built-on state: `0223-f71acd28f654`（patches 1-223 applied）
- Record path: `.temp/patches/records/0224-1b8b8b9dae13.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 7 files（adapted to 6，DROP 1）：
  1. `packages/tui/src/routes/session/index.tsx` — GenericTool rework：`<Show>` → `<Switch>/<Match>` 合并 notebook inline/block 分支 + 保留 #94/#144 折叠逻辑；新增 `TOOL_INPUT_VALUE_MAX_LENGTH` 和 `inputValue()` 截断。
  2. `packages/tui/src/routes/session/notebook-tool.tsx`（NEW）— `useVscodeNotebookToolView` hook：从 metadata.vscodeNotebook 派生 inline/block 视图描述；`@/lsp/language` 适配为 `../../util/filetype`（TUI 包已有）。
  3. `packages/tui/src/routes/session/pending-tool-input.ts` — 新增 `cellId`/`operation` 到 PendingToolInputStats；新增 `createNotebookEditInputParser`；scanner 支持 string[] 数组字段。
  4. `packages/opencode/src/plugin/vscode-bridge.ts` — `call` → `callRaw`（返回原始 bridge value）；新增 `notebookResult`/`notebookView`/`notebookEditView`/`notebookEnvView` + compact helpers；6 个 execute 函数改用 `notebookResult`；保留 223 的 ask 门禁。
  5. `packages/opencode/test/plugin/vscode-bridge.test.ts` — 新增 4 个测试（edit diff metadata、output compact、insert label、oversized diff omit）+ `allowContext` helper（V2 Promise ask）+ `spyOn(VscodeBridge, "callBridge")` mock。
  6. `sdks/vscode/src/notebook/edit.ts` — 5 个 handler 新增 `beforeSource`/`afterSource`；`compactEditResult` 类型+data 新增 `deletedCellIndex`/`beforeSource`/`afterSource`。
  7. DROP: `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` — 无 TUI 测试 harness（#209 先例）。

## 3. SMARK Intent
- 用户可见行为：notebook edit 完成态显示 diff card（而非 raw generic output）；pending 态显示 file/cell/+N/-N 摘要；summary/run/output/env 各有专属 block 视图。
- 受保护不变量：metadata 只保留 compact 字段（artifact text 不复制进 session part）；diff 超 16KB 只保留统计 + diffOmitted 标记；223 的 ask 门禁不变。
- 正常路径：bridge 返回 data → `notebookResult` 派生 `vscodeNotebook` metadata → TUI `useVscodeNotebookToolView` 选择 inline/block 视图 → GenericTool Switch/Match 渲染。
- 边界路径：pending 阶段无 metadata → `inlineView` 从 streaming raw parser 派生摘要；diff 超 16KB → `diffOmitted: "too-large"`。

## 4. Upstream Reconstruction
- TUI 路径适配：原始 patch 用 `packages/opencode/src/cli/cmd/tui/routes/session/`，V2 在 `packages/tui/src/routes/session/`。
- `@/lsp/language` 适配：TUI 包无 `@/lsp/language`，但 `packages/tui/src/util/filetype.ts` 已有 `LANGUAGE_EXTENSIONS` + `filetype()` 函数，直接 import。
- GenericTool rework：V2 的 GenericTool 有 #94/#144 折叠逻辑（expanded/setExpanded/collapsed/limited/onClick/maxChars），原始 patch 的 SimpleShow→Switch 不兼容。适配方案：Switch/Match 的 notebook 分支 + 通用 output 分支保留完整折叠逻辑 + fallback inline 分支。
- vscode-bridge.ts：V2 已有 223 的 ask 门禁，`call` 函数和 execute 结构与原始 patch 的 V1 不同（V2 用 `VscodeBridgePlugin(input: PluginInput): Promise<Hooks>`）。适配：在 V2 结构上重命名 `call`→`callRaw` + 添加 `notebookResult` + 更新 6 个 execute。
- vscode-bridge.test.ts：V2 的 `ToolContext.ask` 返回 `Promise<void>`；`Hooks.tool` 是可选属性，测试用 `plugin.tool!` 断言。`allowContext` 的 `ask` 用 `async ask(input)` 返回 `Promise<void>`（非 V1 的 `Effect.void`）。
- edit.ts：V2 的 handler 结构与原始 patch 相同，直接添加 `beforeSource`/`afterSource` 字段。
- session-message-render.test.tsx DROP：V2 无 TUI render 测试 harness（#209 已先例 DROP），保留 vscode-bridge.test.ts 的 4 个测试覆盖 notebookResult 输出。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@223) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| notebook-tool.tsx LANGUAGE_EXTENSIONS | TUI 包无 @/lsp/language | notebook-tool.tsx import | 改用 ../../util/filetype | current patch diff |
| GenericTool Switch/Match | V2 有 #94/#144 折叠 | GenericTool | 合并 notebook 分支 + 保留折叠 | current patch diff |
| call→callRaw + notebookResult | V2 call 返回 summaryOnly | vscode-bridge.ts | 重命名 + 新增 helpers | current patch diff |
| 6 execute → notebookResult | V2 execute 返回 {output, metadata} | 6 execute 函数 | 改为 notebookResult | current patch diff |
| pending cellId/operation + array scanner | V2 无 cellId/operation | pending-tool-input.ts | 新增字段 + parser + scanner | current patch diff |
| edit.ts beforeSource/afterSource | V2 无 before/after | 5 handlers + compactEditResult | 新增字段 | current patch diff |
| test allowContext Effect.void | V2 Promise<void> | vscode-bridge.test.ts | 改为 async ask | current patch diff |
| session-message-render.test.tsx | 无 harness | DROP | #209 先例 | 决策记录 |

## 6. Current Patch Edit
- 各编辑（6 files）：
  1. `index.tsx`：新增 import + `TOOL_INPUT_VALUE_MAX_LENGTH` + GenericTool Switch/Match rework + `input()`/`inputValue()` 截断。
  2. `notebook-tool.tsx`：新建——`useVscodeNotebookToolView` + 6 个 block view 函数 + 5 个 component 函数 + helper 函数。
  3. `pending-tool-input.ts`：新增 `cellId`/`operation` + `createNotebookEditInputParser` + scanner 数组支持。
  4. `vscode-bridge.ts`：`call`→`callRaw` + `notebookResult`/`notebookView`/`notebookEditView`/`notebookEnvView` + compact helpers + 6 execute 更新。
  5. `vscode-bridge.test.ts`：新增 4 测试 + `allowContext` + `spyOn` mock + `plugin.tool!` 断言。
  6. `edit.ts`：5 handler 新增 before/after + `compactEditResult` 类型+data 扩展。

## 7. Verification
- `git apply --check --index` against state 223：PASS。
- Materialize：`--materialize 224 --rebuild` → passed 224/224。
- Typecheck：`--typecheck 224` → 全部 30 个 workspace passed（install: reused）。
- E=1025，C=154，need=154。Gate PASS（margin 0）。

## 8. Contract Check
- 单 commit：是。完整 source diff 已读：是（7 files，1533 行原始）。完整 upstream 实现已读：是。
- 无 grep-only 证据：是。无架构式省略：是（session-message-render.test.tsx DROP 有 #209 先例）。
- 无删除或延迟行为：是。无未请求 fallback：是。每条路径有 owner：是。
- 无残留行为 bug：是。中文注释 gate 通过：是（C=154 ≥ 154）。

## 9. Independent Audit
- v1 forward/reverse: BLOCK (B-01 corrupted edit.ts handleTypeChange/handleStringEdit — #224 comments spliced into code identifiers producing invalid TypeScript)
- v1 fix: Rewrote corrupted hunks to clean beforeSource insertion matching original patch structure; fixed hunk header counts and newStart values; fixed lost indentation on sourceRaw context line
- v2 pending: re-audit after B-01 fix
- Current SHA-256: 5bc0eca1d16c056b, bytes: 69551
- E/C after fix: E=852 C=167 need=128 gate=PASS

## 10. Final Verdict
`PASS`（build）— materialize PASS 225/225，typecheck PASS 全部 30 workspace，E/C gate PASS（E=852，C=167，need=128）。
B-01 fixed: edit.ts handleTypeChange/handleStringEdit hunks rewritten to clean beforeSource insertion.
