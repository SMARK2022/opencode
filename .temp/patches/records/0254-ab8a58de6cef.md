# 0254-ab8a58de6cef record — feat(session-export): 从守护进程读取完整会话记录

## Manifest
- index 254, sha `ab8a58de6cef3aa41a740d8fbf865c55e33090c2`, parent `f29622f1047b` (0253)
- subject: feat(session-export): 更新会话导出逻辑，确保从守护进程读取完整的会话记录而非本地渲染窗口

## Original 身份
- original/0254-ab8a58de6cef.patch: 14151 字节, 2 files（tui routes/session/index.tsx 2 hunk、session-export.test.tsx 新文件 298 行）

## Source 行为（逐 hunk）
1. **export 命令 hunk 1**：删 `const sessionMessages = messages()`（TUI 本地渲染窗口快照，sync 只保留最近 100 条）。
2. **hunk 2**：`options === null` 后加 daemon 读取：`sdk.client.session.messages({ sessionID }, { throwOnError: true })` + `if (!sessionMessages.data) throw`；formatTranscript 第二参改 `sessionMessages.data`。
3. **测试**：3 用例（daemon 全量 vs 渲染窗口、cancel 不读不写、daemon 500 不写文件）+ withExportApp harness（keymap 注册命令触发 export）。

## 目标适配（Round 1）
- **生产 hunk**：路径重写 `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx`（export 命令 L1108-1136，上下文逐字匹配，offset ~44）。copy transcript 命令（L1082）的 `sessionMessages` 保持不变（source 只改 export）。
- **测试重写**（目标 harness 差异）：
  - Provider 链：TestTuiContexts（paths.state + **cwd**——exportDir 用 paths.cwd）+ EpilogueProvider（Session 需要）+ ClipboardProvider + PluginRuntimeProvider（createPluginRuntime）+ SDKProvider testTransport（目标已有此 prop）+ PermissionProvider/LocationProvider（Session 组件依赖）。
  - `CommandPaletteProvider` 目标不存在 → 移除（命令经 useBindings 注册到 keymap）。
  - `VscodeBridgePlugin` 类问题：`Global.Path.state` → TestTuiContexts paths.state + KVProvider。
  - keymap 命令查找：`visibility: "registered"` → **"reachable"**（目标 palette 用法；debug 实证 "registered"/"reachable" 无 namespace 过滤时空，带 namespace:"palette" 时 reachable 才返回命令）。
  - `openEditor` mock：目标 openEditor 是函数非对象 → spyOn bind 无效（openEditor 调用被真实执行但成功返回 undefined，不阻塞测试）。
  - **测试 3 语义适配**：目标 export 命令有 try/catch 吞掉 daemon 读取错误走失败 toast（目标既有结构）→ source 的 `rejects.toThrow()` 改 `await runSessionExport(context)` + 断言无文件写出（行为契约：绝不写出部分/空 transcript，失败可见性由 toast 承担）。
  - 类型断言：`as SessionInfo/UserMessage/Part` → `as unknown as X`（目标 SDK v2 类型不重叠）。
  - 初始 fetch 的 `/session?start=...` 等请求由 createFetch 兜底（session 端点匹配 pathname）。

## 生成方式
- gen-254.py + 多轮修正（Provider 补全、cwd、visibility、测试 3、类型断言、debug 清理）→ gen-254final.py。
- current/0254-ab8a58de6cef.patch: 18284 字节, 2 entries, SHA-256: 4e460eb5a646dc0ebeeaf62bba1188d63f77f382e4eac7b37a20c01a8af30245

## 验证
- apply-cumulative --materialize 254: passed 254/254
- session-export.test.tsx: 3 pass 0 fail（7 expect）
- tui typecheck 通过

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0254-ab8a58de6cef

## E/C 最终（count-ec.py）
- E=281, C=43, need=43, PASS（#254 注释 43 条：daemon 全量、窗口区分、cancel 零请求、500 不写文件、Provider 链、harness、断言意图）

## 验证（最终）
- materialize 255: passed 255/255（累计）
- session-export.test.tsx 3 pass（7 expect）
- tui typecheck 通过


## 补充说明（审计 G-02 收尾）
### source/上游/目标三方比较
- source 意图：export 命令不再用 TUI 本地渲染窗口快照（sync 只保留最近 100 条，sync.tsx:597/618-619），改为 sdk.client.session.messages({sessionID}, {throwOnError:true}) 无 limit 直读 daemon 全量；!data 抛错走失败 toast；formatTranscript 第二参传 sessionMessages.data（{info, parts}[]）。
- 上游（目标基线）：copy transcript 命令（L1082）保持本地窗口不变（source 只改 export）；SDK v2 Session2.messages 返回 {data}，SessionMessagesResponses[200] 是 Array<{info, parts}>，与 formatTranscript 的 MessageWithParts[] 匹配。
- 目标：packages/tui/src/routes/session/index.tsx L1127-1128 daemon 读取 + throw；L1129 formatTranscript(sessionData, sessionMessages.data)。
### 测试适配细节（目标 harness 差异）
- Provider 链：TestTuiContexts paths={{state, cwd}}（exportDir = paths.cwd 必须指向 tmp——默认 /tmp/opencode/packages/tui 会把文件写到仓库）；EpilogueProvider/ClipboardProvider/PluginRuntimeProvider（Session 组件依赖，缺失即崩溃）；SDKProvider testTransport（目标已有此 prop）；PermissionProvider/LocationProvider。
- CommandPaletteProvider 目标不存在 → 移除（命令经 useBindings 注册到 keymap，无需 palette Provider）。
- keymap 命令查找：visibility "registered" → "reachable"（debug 实证带 namespace:"palette" 时 reachable 才返回命令）。
- openEditor spy：目标 openEditor 是函数非对象 → spyOn bind 无效但 openEditor 调用成功返回 undefined，不阻塞。
- 测试 3 语义适配：目标 export 命令 try/catch 吞掉 daemon 错误走失败 toast（目标既有结构）→ 断言"不写部分文件"契约保留（cwd 与系统 tmpdir 双路径断言），source 的 rejects 语义由 toast 失败可见性承担。
- 类型断言 as SessionInfo/UserMessage/Part → as unknown as（目标 SDK v2 类型不重叠）。
- JSX 注释：return 首节点位置不能放 {/* */} 注释（Orphan text 错误）——移到代码层。
### 行为验证
- 3 测试全绿（7 expect）：全量 vs 窗口（old 消息只来自 daemon 响应）、cancel 零请求零文件（dialog 在 fetch 前 return）、500 不写部分文件（cwd 与 OS tmpdir 双断言）。
### E/C
- E=281, C=43, need=43 PASS（#254 注释 43 条：daemon 全量语义、窗口区分、cancel 零请求、500 失败路径、Provider 链、keymap reachable、断言意图）。
- typecheck-0255 报告：.temp/patches/reports/typecheck-0255-2026-08-08T15-01-13-533Z.md（install 指纹 c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c；packages/app pre-existing TS1128 失败，0247 已记录 baseline 缺陷；opencode/tui typecheck 手动验证通过）

## DUAL-AUDIT SEAL — 批次 251-255（重审通过）
- 正向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；G-01（typecheck-0255 报告）、G-02（records ≥50 行/≥3000 字符）、G-03（0252 身份行 28766B/4725314f）均已验证修复。
- 逆向 auditor（independent adversarial-patch-auditor）：251-255 五项全部 PASS，无未关闭 B/G；仅 N-01~N-04 记录级观察（seal 格式、自动 typecheck 覆盖、sdks/vscode node-gyp 环境、lockfile 差异）。
- 两份结果一致，批次 251-255 放行。
