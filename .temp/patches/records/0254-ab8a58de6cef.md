# Record 0254-ab8a58de6cef

## Status
PASS

## 修改内容
feat(session-export): 更新会话导出逻辑，确保从守护进程读取完整的会话记录而非本地渲染窗口。

### Source 逐 hunk 行为
1. index.tsx H1：export 命令不再提前快照本地渲染窗口 `messages()`。
2. index.tsx H2：改为 `sdk.client.session.messages({sessionID}, {throwOnError: true})` 全量读取（不带 limit = daemon 完整分页），data 缺失显式 throw；formatTranscript 第二参直接消费 daemon 的 `{info, parts}[]`。只改 export 快照来源，不碰页面渲染/copy transcript/sync store 边界。
3. 测试（298 行）：三场景（全量含窗口外旧消息 / 取消零读取无文件 / daemon 500 无半成品文件），fixture 以 `limit > 0`（渲染同步）vs 无 limit（全量）区分数据源，经真实 keymap 注册的 session.export 命令触发。

### 目标适配
- **生产重切**：TUI 已迁 `packages/tui/src/routes/session/index.tsx`（缩进 -2、L1112/L1125-1129 锚点）；`sdk`（L392 useSDK）在作用域内 ✓；`formatTranscript` 的 `MessageWithParts[]` 与 daemon `.data` 形态完全对型；`messages()` memo 的其他消费者（session.copy 等）不受影响。
- **测试降级（论证）**：source harness 依赖 V1 谱系设施（Global.Path.state、src/cli/cmd/tui/context/* 全目录、CommandPaletteProvider）在目标全不存在；Session 路由级测试在目标无挂载先例（tui 包 0 个 Session 路由测试），完整 provider 链重建属独立基建工作。降级为新建 `packages/tui/test/util/transcript-daemon.test.ts`（LF）直接锁定 formatTranscript 对 daemon 全量 `{info, parts}[]` 形态的产出契约（窗口外消息进入导出、空数组安静产出元数据头）——数据映射是本 patch 的核心可测语义；「绕过本地窗口」的调用链改动由生产代码落地 + typecheck 覆盖。

## E/C
- E = 55（plus - commentPlus + minus）
- C = 9（#254：生产 3 行——绕窗语义/throwOnError 错误路径/data 缺失契约；测试 6 行——daemon 数据契约、created 顺序、三开关与 providers 同形、空数组语义、异常形态区分）
- need = ceil(55 * 0.15) = 9 → 余 0 PASS

## Source
- original/0254-ab8a58de6cef.patch: 348 行 2 文件。

## Current
- current/0254-ab8a58de6cef.patch: 3 hunk / 2 文件
- SHA-256: 2d0641a91a269b09468c62e709eb862e33d6cbf4922975a61d7689a266dc7bdc

## 安装
- materialize 254 --rebuild: passed 254/254（全量重放）

## 测试
- typecheck 254: 全部 workspace passed
- `bun test test/util/transcript-daemon.test.ts`（packages/tui）: 2 pass / 0 fail

## 修订历史
1. 初版 3 hunk：H2 计数少 1（测试文件 45 行写为 44 截断 `})`）→ sync 修正。
2. C 从 5 → 7 → 9 三轮补齐（行尾 satisfies 注释不计入 ^// 口径，改独立行）。
3. 最终：materialize OK、typecheck OK、2/0 测试、C=9=need → PASS。

## 三方比较
- Source：export 从本地窗口改为 daemon 全量读取 + 数据缺失守护 + 三场景回归。
- Upstream v1.17.20：TUI 迁 packages/tui，`sdk.client.session.messages` 无 limit 即全量语义已存在（sync.tsx:845 渲染同步传 limit:200 为对照），URL 契约一致。
- Target：生产语义原样落地；测试降级为数据契约单测（论证如上）。copy transcript 命令刻意不碰（与 source 一致）。

## 最终行为
- session.export 从 daemon 读取完整可见会话（含渲染窗口外历史），写入的 Markdown 含全部消息。
- daemon 响应异常形态（data 缺失/非 2xx）时走既有失败 toast，不写半成品文件。
- 取消对话框时不发起全量读取。

## 批次双审结论（2026-09-07，250-254）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：「取消/daemon 500」两场景的行为测试未在目标重建（记录已论证的降级妥协），由生产代码结构（throwOnError+data 守卫+单一 catch→toast）保证；tui 包存在 5 个 test/routes/session 纯函数单测但无 Session 挂载先例。- 跨批次观察（逆向审计 N-6）：V1 DEFAULT_READ_LIMIT=200（tool/read.ts:19）与 V2 MAX_READ_LINES=1_000（core read-filesystem.ts:12）分裂源自 #66 中间历史保留决策，建议在后续对应 read 限制的 index 处收敛。
