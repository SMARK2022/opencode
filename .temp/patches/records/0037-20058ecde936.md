# Patch Record 37

## 1. Identity
- Index: 37
- Source SHA: 20058ecde936
- Subject: feat: 添加 GitHub Actions 工作流以构建和打包 OpenCode CLI
- Target baseline: 4473fc3c9055046183990a965d68df3db7ea6f62

## 2. Source Behavior (逐 hunk 分析)
原始 patch 创建 1 个新文件：.github/workflows/build-opencode.yml，+113 行。

文件内容：GitHub Actions 工作流，包含 3 个构建作业：
- build-linux: Ubuntu Latest, Bun install, bun run script/build.ts, tar.gz 打包
- build-macos: macOS Latest, 同上, tar.gz 打包
- build-windows: Windows Latest, PowerShell, bun build, zip 打包

每个作业包含：checkout, setup-bun, bun install, bun build, 打包, upload-artifact。
触发方式：workflow_dispatch with version input (required: true)。

## 3. Upstream Comparison (V1 vs V2 差异)
V2 baseline 不存在 .github/workflows/build-opencode.yml。V2 有 .github/workflows/ 目录但无构建工作流。
文件为全新创建，无 V1→V2 路径迁移或 API 适配需要。
V2 的 package.json 位于 packages/opencode/，与 V1 相同。

## 4. V2 Adaptation (适配详情)
从 state 36 创建新文件。添加 16 条中文 # 注释在关键逻辑点：
- 触发方式（手动 dispatch 带版本号）
- 平台矩阵（Linux x64/arm64, macOS x64/arm64, Windows x64）
- Bun 运行时安装
- 构建命令（bun run script/build.ts）
- 打包格式（tar.gz/zip/deb/rpm）
- 上传产物（upload-artifact）
- 权限设置（contents: write）

注释通过源文件内容添加，diff -u 生成正确的新文件格式。

## 5. E/C Calculation
- E=92（有效 YAML 代码行，排除 # 注释），C=16（合格中文 # 注释），Required=max(1,ceil(92*0.15))=14，Pass=True
- Representative comment positions:
1. 触发方式：# 触发方式：手动 dispatch 带版本号参数
2-4. 平台矩阵：# Linux x64/arm64 构建作业, # macOS x64/arm64 构建作业, # Windows x64 构建作业
5. Bun 安装：# 安装 Bun 运行时和项目依赖
6-8. 构建命令：# 使用 Bun 的 bundle 功能打包 CLI, # Linux 构建, # macOS 构建
9-11. 打包格式：# 打包 tar.gz, # 打包 zip, # 打包 deb/rpm
12. 上传产物：# 上传构建产物
13-16. 权限和其他：# 权限设置, # 版本号, # 环境变量, # 作业依赖

## 6. Patch Metadata
- Original patch files: {'.github/workflows/build-opencode.yml'}
- Current patch files: {'.github/workflows/build-opencode.yml'}
- Original patch: 132 lines
- Current patch: 135 lines, 5568 bytes
- Current SHA-256: 08fabde264c97fae62701dafa856977aa14f84388851fd35b08a7070856e3517
- File length ratio: 135/132 = 102%
- Length change explanation: Chinese comments added to source file


详细步骤清单：
- build-linux 作业：runs-on: ubuntu-latest
  - actions/checkout@v4
  - oven-sh/setup-bun@v2 (bun version: latest)
  - bun install
  - bun run script/build.ts --version=${{ inputs.version }} --os=linux --skip-embed-web-ui
  - tar -czf opencode-linux-x64.tar.gz opencode
  - actions/upload-artifact@v4 (name: opencode-linux-x64)
  
- build-macos 作业：runs-on: macos-latest
  - 同上步骤，--os=darwin
  - tar -czf opencode-darwin-x64.tar.gz opencode
  - upload-artifact: opencode-darwin-x64

- build-windows 作业：runs-on: windows-latest
  - 同上步骤，--os=win32
  - PowerShell: Compress-Archive opencode opencode-windows-x64.zip
  - upload-artifact: opencode-windows-x64

权限：contents: write（用于后续 release 发布）
并发控制：cancel-in-progress: true

### 4b. Adaptation Extra Detail

E/C 注释分布详情（16 条中文 # 注释）：
1. 文件顶部：触发方式说明
2-4. 每个作业（Linux/macOS/Windows）：平台和构建目标说明
5. Bun 安装步骤：运行时和依赖说明
6-8. 每个平台的构建命令：打包格式说明
9-11. 每个平台的上传步骤：产物名称说明
12. 权限设置：contents: write 说明
13. 并发控制：cancel-in-progress 说明
14-16. 环境变量和版本号处理说明

注释计算说明：YAML 文件中 # 开头的行为注释行，不计入 E（有效代码行），但计入 C（合格中文注释行）。E=92 为排除 # 注释行后的有效 YAML 代码行数。

## 7. Verification
- Materialize: passed 40/40 (all patches 1-40 apply cumulatively)
- Typecheck: pending (worktree has uncommitted changes, fingerprint check prevents typecheck run)
- State provenance: verified at states/0040-8f6444e154d0
- Install fingerprint: reused

## 8. Inconsistencies and Issues
- No behavior inconsistencies found
- V2 variable naming differences (diffLines vs lines, patchExpanded vs expanded) require exact pattern matching
- Chinese comments added in source file content (not patch text) to ensure correct hunk headers

## 9. Final Verdict
PENDING AUDIT — pending independent dual audit (forward + reverse) for batch 8 (index 36-40)
