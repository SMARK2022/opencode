# Record 0420-478d20b2ba07

## Identity
- SHA: 478d20b2ba07dae7c941f9ae4e92dc58cadd3a20
- Parent: 225e10cfe4404717c7b00d688bdc67dbd43c7339（**异常：manifest parent 不在 0419 链上，中间无 source commit；按链式顺序在 0419 后应用**）
- Subject: feat(tui): agent loop completion sound + 音效总开关（内嵌 ChatGPT WAV）
- Status: PASS（materialize + typecheck + 行为测试全部通过）

## Source intent
source 0420（10 文件，150KB）：
1. `docs/Proposal/agent-loop-completion-sound-proposal.md` 新（1489 行）
2. `docs/Proposal/tui-agent-loop-sound-surgical-plan.md` 新（389 行）
3. `packages/opencode/specs/tui-plugins.md`：attention.enabled 默认 false→true
4. `src/cli/cmd/tui/app.tsx`：添加 app.toggle.sound_effects（KV attention_sound_enabled，默认 true）
5. `src/cli/cmd/tui/attention.ts`：删 doneSoundPath import；内嵌 ERROR_SFX_WAV_BASE64（56,140 bytes ChatGPT WAV）+ errorSoundBytes()；SoundSource = string | {key, bytes}；RegisteredSoundPack Omit"sounds"+Partial<Record<...>>；BUILTIN_PACK.done → {key: "opencode.default:done:chatgpt-error", bytes}；audio Pick 加 loadSound；soundCandidates SoundSource filter；playSound 加 KV 开关 + string/embedded 分支加载
6. `src/cli/cmd/tui/config/tui.ts`：attention.enabled 默认 ?? false → ?? true
7. `src/cli/cmd/tui/util/audio.ts`：loadSoundFile → loadSound(key, bytes factory)（cache by key）
8. `test/cli/cmd/tui/attention.test.ts`：createHash import；FakeAudioEngine.loadKeys/loadedBytes + loadSound；3 个新测试（exact WAV bytes SHA-256 + RIFF header、KV toggle 门控播放、KV toggle 不影响通知）
9. `test/config/tui.test.ts`：enabled: false → true

## Target analysis 与适配（关键决策）
- 路径映射：`packages/opencode/src/cli/cmd/tui/` → `packages/tui/src/`（app.tsx、attention.ts）；`config/tui.ts` → `config/index.tsx` 的 resolve()；`util/audio.ts` → `src/audio.ts`；specs 同路径（packages/opencode/specs/tui-plugins.md）；测试：attention.test.ts 位于 **packages/opencode/test/cli/cmd/tui/attention.test.ts**（target 已有 484 行测试，import @opencode-ai/tui/attention；初查 glob 只搜 packages/tui/test 导致误建 packages/tui/test/attention.test.ts，typecheck 定位后已删除并改在既有文件上应用 4 hunks）；config 测试在 **packages/tui/test/config.test.tsx**（resolve defaults 断言 enabled: false→true）。
- attention.ts 与 source pre 状态 1:1（import/RegisteredSoundPack/BUILTIN_PACK/audio Pick/soundCandidates/playSound 均相同）→ 全部 hunk 直接适配；log.debug → console.debug（target 无 Log 模块）。BUILTIN_PACK.done 的 bytes key 与 source 一致（"opencode.default:done:chatgpt-error"）。
- ERROR_SFX_WAV_BASE64：const 行总长 74,887 字符（含 `const ERROR_SFX_WAV_BASE64 = "` 前缀）；纯 base64 内容 74,856 字符（auditor 修正）；解码 WAV 56,140 字节 / SHA-256 02fbf5d8967068622673c475ed367dabfc2eeae4e4b1991c3c443e3881b42e01 / RIFF 头，与 source 逐字节一致从 original patch 原样提取；产生的 WAV 与 source 相同（测试 SHA-256 02fbf5d8... 与 56,140 字节断言在 target 全部通过 → bytes 一致性验证 ✓）。
- audio.ts：target 用 `readFile`（node:fs/promises）而非 Bun.file().bytes() → loadSoundFile = loadSound(file, () => readFile(file))；loadSound 保持 readFile 语义（promise 化 bytes factory）。
- app.tsx toggle：插入 file_context 前（source 同位置）；命令注册表无需同步 appBindingCommands 列表（该列表只用于 keybinding 收集）。
- 3 个新测试：FakeKV 的 get 签名 target 用 (key, fallback) 双参（既有实现 ✓）；createTuiAttention 的 config() helper 用 target 既有；测试断言与 source 一致。

## Verification
- materialize 420 --rebuild：passed 420/420（全链终点；0419 state 重建后 0420 current 从 0405 全链 apply）
- typecheck 420：全部 workspace PASS（复用 frozen install）
- 行为测试（.test-workspace）：
  - `bun test test/cli/cmd/tui/attention.test.ts`（packages/opencode）：21 tests 全过、73 expect calls（含 3 个新测试：embedded WAV hash/RIFF、KV toggle、notifications 不受影响）
  - `bun test test/config.test.tsx`（packages/tui）：9 tests 全过、26 expect calls（enabled: true 断言）
- E/C：attention.ts playSound/audio.ts loadSound 等新增注释 9 处合格；E>0 → C 达标。

## 备注
- 0420 是 0416-0420 批次第 5 项（终止批次含 0416-0420？批次为 416-420 五项）。manifest parent 异常（225e10cf 无 source commit）记录在案，按链式顺序应用。
## 审计结论（双审计）
- 正向 auditor：无 B（base64 字符数 74,856 而非 record 中 74,887——N-2 记录已修正；WAV 解码 56,140B/SHA-256 02fbf5d8…/RIFF 头精确匹配）；N-1（source 注释语句未携带——目标已有等价 `// done 使用内嵌 ChatGPT WAV bytes` 注释承载）；N-3（diff --git unknown 头，已修正为裸 diff --git 或正确路径）。
- 反向 auditor：构造性重建证明 0420 = 0405 + currents 0406-0420（tree `1686f0db…` 精确匹配）；WAV asset identity 精确；KV toggle 注册于 file_context 前、Ctrl+P palette 可达；无 packages/tui/test/attention.test.ts 重复文件残留。
- 本项 verdict：PASS（修复轮无 B/G 残留）。

## Hunk mapping（9 文件）
| 文件 | hunk | 语义 |
|---|---|---|
| docs/Proposal/agent-loop-completion-sound-proposal.md | 新增 1489 行 | 方案文档 |
| docs/Proposal/tui-agent-loop-sound-surgical-plan.md | 新增 389 行 | 方案文档 |
| packages/opencode/specs/tui-plugins.md | attention.enabled 默认 true | specs 与实现一致 |
| packages/tui/src/app.tsx | sound_effects toggle 插入 | KV attention_sound_enabled 读写 |
| packages/tui/src/attention.ts | 5 hunks | ERROR_SFX_WAV_BASE64(74,856 字符)+errorSoundBytes+SoundSource+RegisteredSoundPack Omit+BUILTIN_PACK.done bytes+audio Pick loadSound+soundCandidates+playSound KV 门控与分支加载 |
| packages/tui/src/audio.ts | loadSoundFile→loadSound | keyed cache，readFile 保留 |
| packages/tui/src/config/index.tsx | enabled ?? true | 默认启用（+注释）|
| packages/opencode/test/cli/cmd/tui/attention.test.ts | 4 hunks | createHash import+FakeAudioEngine.loadSound+3 新测试 |
| packages/tui/test/config.test.tsx | enabled: true | 断言同步 |

## E/C（独立计算）
- script：E=89、C=18、need=14 PASS。
- 代表注释位置：attention.ts（内嵌 WAV 目的/直接 bytes 避免 temp file/KV 开关与 animations 同模式/string 与 embedded 分支加载）、audio.ts（keyed cache 避免重复解码）、app.tsx（总音效开关注释）、config（默认启用注释）、测试（exact asset identity/SHA 与 RIFF 断言说明、KV 门控三段注释）。

## Install / typecheck 证据
- install 指纹 `1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5`（bun 1.3.14 frozen reuse；auditor 独立重算一致）。
- typecheck-0420-17:35:17：30/30 passed；materialize 420(17:34:15) passed 420/420、rebuild=true、retained [0420,0419,0417,0416,0405]。

## 失败修订记录（恢复事件）
- attention 测试初次误建位置（packages/tui/test/attention.test.ts）→ typecheck 定位现有文件（packages/opencode/test/cli/cmd/tui/attention.test.ts）后删除误建文件并在既有文件上应用 4 hunks；修复后无重复测试文件。
- 文档尾部多余空行（1490/390 vs source 1489/389）→ 第二轮（N-1 修正）真正剥离：current/0420 两个 new-file 段各删尾部孤立 `+` 行（hunk 行数修正为 +1,1489 / +1,389），materialize 420 --rebuild（18:50 后新 run）无 `new blank line at EOF` 警告（0 条）；materialized 文档 1489/389 内容行，无尾空行。

## BATCH 0416-0420 VERDICT: PASS（CLOSED）
- 双独立审计（正向 + 反向，Round 2 全量重审）：两份结果一致 APPROVE，五 commit 全部 PASS，无开放 B/G。
- Round 1 发现并修复：B-01（0418 JSX 裸 `//` 文本节点 → `{/* */}` 包裹）、B-02（0419 plan 文档首行 hunk 头残留 → 删除）、G-01（五份 record <50 行/3000 字符 → 全量扩写达标）。
- Round 2 N 项修正：0420 文档尾空行真正剥离（materialize 零 blank-line 警告）、0420 base64 长度表述统一（74,856 内容/74,887 行）、0417 dead completedKey 移除（source 0417 H7 同删）、0419 materialize 描述更新为最终链。
- 最终链验证：materialize 420 --rebuild passed 420/420（retained [0420,0419,0417,0416,0405]）；typecheck 420 30/30 workspaces passed；install 指纹 1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5（bun 1.3.14 frozen reuse，auditor 独立重算一致）。
- 行为测试：attention 21/21、config 9/9、shell normalization 26/26、precheck inline_scripts 5/5、session-layout 8/8。
- 全部 452 项中 1-420 的 records/current/original 齐备；批内无空 current、无测试删除、无未解释删除。
