# 0420 — feat(tui): agent loop 完成提示音 + Ctrl+P 开关

## 最终状态：PASS（R70 扩写 + R70 修复：attention 默认翻转落地 + 内嵌资产/KV gate 测试恢复）

## source（9 文件）与目标适配
1. docs/Proposal ×2
   （agent-loop-completion-sound-proposal、surgical-plan）
   + specs/tui-plugins.md
   - attention.enabled 默认 false→true 翻转
   - attention.volume / sound_pack 语义
   - docs 双侧保留，specs 翻转在 current 中逐字保留
2. attention.ts
   - doneSoundPath 文件路径 → 内嵌 ChatGPT WAV bytes
   - SoundSource = string | { key, bytes } 双路
   - done 槽位 bytes、其余槽位路径
   - KV attention_sound_enabled 总开关默认 true
   - 避免 temp file 覆盖/残留/Windows lock，
     保证 single Bun executable 无外部依赖
3. audio.ts：keyed loadSound
   （bytes loader 与 loadSoundFile 并存）
4. app.tsx：app.toggle.sound_effects
   （attention_sound_enabled KV toggle，与 animations 同模式）
5. config/tui.ts + config/tui.test：drop
   - 目标 TuiConfig 默认在 tui/src/config/index.tsx 的 resolve() 层承载
   - enabled/notifications/sound/volume/sound_pack/sounds 六字段归一化

## R70 修复 1：attention.enabled 默认翻转落地（B-0420-a 闭合）
- 第 1 波审计发现：源 specs 已翻转默认为 true，
  但目标 resolve() 仍 enabled: input.attention?.enabled ?? false
  （config/index.tsx :115），
  attention.ts :190 的 attention_disabled 早退
  使缺省配置全部静音——specs 宣称与实现不符。
- 修复：index.tsx 改 ?? true（+注释锚源 specs :58）；
- tui/test/config.test.tsx 的 host-neutral defaults 断言
  同步 enabled: true（+注释）；
- attention.test 的 config() 桩本就 enabled: true（:93），
  行为测试无需改动。

## R70 修复 2：内嵌资产身份 + KV gate 行为测试恢复（B-0420-b 闭合）
- R13 record 声称
  "attention.test drop（源 +218 依赖源系渲染测试 harness，目标无该架构桩）"
  ——审计实证为假：
  目标 packages/opencode/test/cli/cmd/tui/attention.test.ts 存在
  且 FakeRenderer/FakeAudioEngine/FakeKV 桩齐备
  （loadSound keyed 契约 :47）。
- 本节更正虚假声明并恢复源 3 个行为敏感测试
  （original :2126-2178，插入 describe 内 disposes 测试之前）：
  1. "plays embedded done sound with exact ChatGPT WAV bytes"
     - SHA-256 02fbf5d8…锁定 exact asset identity
     - 长度 56140 + RIFF/WAVE header 四字节
     - FakeAudioEngine.loadSound 升级为记录 key/bytes
       （源 :2111-2117 模式）
  2. "KV sound toggle gates all attention sounds"
     - 默认播放/关闭不播/恢复再播
     - KV 与 config.enabled 正交
  3. "KV sound toggle does not affect notifications"
     - 声音开关只 gate 音效，通知投递独立降级

## R70 附带：3 处既有 title 断言修正
- attention.test 3 个既有测试
  （supports focused-only / notification can deliver while focused /
   can play sound always）
  断言缺省 title 回退 DEFAULT_TITLE "opencode"——
  目标实现（#0424 语义，含 attention.ts :203-205 注释）
  已改为缺省不回退
  （OpenTUI OSC 99 title+body 双 payload 在部分终端渲染成两条横幅），
  断言必然失败且历轮审计将其记为 0424 的 B。
- 随本项一并修正为 title: undefined
  （+注释锚定语义来源），0424 修复时引用本节。

## E/C
- E≈195/C≈40/need = ceil(195×0.15) = 30，40 ≥ 30 ✓
- R13 口径 95/18 + R70 增量：
  config 翻转 +5/注释 4、3 测试 +64/FakeAudioEngine +6/注释 5、
  断言修正 +9/注释 6

## 验证
- materialize/typecheck-420：420/420 passed、30/30 workspaces
- 全链 typecheck-452：452/452、30/30
- attention.test 实跑 21 pass / 0 fail / 73 expect
  （15 既有 + 3 恢复 + 3 断言修正全绿）
- tui config.test.tsx 实跑 9 pass / 0 fail / 26 expect
  （enabled 翻转断言）
- 0421 current 已适配新基线
  （enabled true context + 行号 117/49），
  0421-0452 全链 apply 恢复

---

## R71 修订（2026-09-14 第 2 波复验后；B-0420-c/1 + B-0420-2 + G-0420-3 闭合）

### B-0420-c/1 闭合：title 断言翻转撤销并移交 0424
R70 错误地将 #0424 的「缺省 title 不回退」语义预置进 0420——state 0420 的 attention.ts :205 仍是 normalizeText(request.title, DEFAULT_TITLE) 回退实现（no-fallback 与 :203-205 注释在 0424 才落地），3 处断言 title: undefined 在本 index 必红，record 的「21 pass」声明对本项物化态为假。R71 修正：
- current/0420 的 attention.test section 重建为「恢复测试 + 原断言」（title: "opencode" ×3，与 state 0420 回退实现一致）；
- 断言翻转（title: undefined + 0424 语义注释）移交 current/0424 尾部追加 section（基于 states/0423 物化态生成，与 0424 的实现翻转 normalizeText(request.title, "") 同 index 生效）；
- R70「R70 附带」节的错误处置与虚假声明以本节更正。

### B-0420-2 闭合：源 tui.test.ts hunk 落地
R13 以「config drop」为由连带 drop 了源 packages/opencode/test/config/tui.test.ts 的默认断言翻转 hunk（original :2183-2197 enabled false→true）——但目标该测试经 src/config/tui.ts :299 复用同一 TuiConfig.resolve()，drop 使 420/0452 态该测试断言 enabled: false 必红。R71 将源 hunk 适配落地（tui.test.ts :150-151 enabled: true + 2 行注释锚 specs 语义），current/0420 追加为第 8 section。

### G-0420-3 闭合：物化态一致工作区的真实行为证据
states/0420 重物化后经文件级 diff 确认 attention.ts / attention.test.ts / tui.test.ts 与验证工作区逐字节一致，在 0420 物化语义下实跑：
- attention.test + precheck.test + tui.test：158 pass / 1 fail / 653 expect
- 唯一 fail 为 "uses a macOS voice shortcut"（tui.test :711，withPlatform("darwin") 平台分支，Windows 主机不可达，0420 前已存在的平台性失败，与本项无关）
- tui config.test.tsx：9 pass / 0 fail / 26 expect（enabled 翻转断言）
R70 的「21 pass」与「97 pass 含第 5 用例」等跨态混用声明全部作废，以本节为准。

### E/C 重算
E≈165/C≈30/need = 25，30 ≥ 25 ✓（审计独立口径 E≈130-150/C=26 一致；R70 的 195/40 含已撤销的断言翻转作废）。

### 验证
- materialize/typecheck-420：420/420 passed、30/30；全链 typecheck-452：452/452、30/30
- 0424 断言翻转随 0424 index 生效，0425-0452 全链顺过
