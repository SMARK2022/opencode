# Record 0264-3798c7696f55 — reviewer 失败默认回退人工审批（fallback=deny 显式 fail-closed）

## Source（original/0264-3798c7696f55.patch，7 文件）

1. `permission/auto.ts`：`reviewerDisabled?: boolean` → `reviewerFailureFallback?: "deny" | "user"`；无 reviewer 路径默认 ask、fallback=deny → deny（native-auto 特例移除）；`onFailure` 改按 `fallbackToUser` 分支（忽略 Disabled/FallbackToUser tag），新增 `Effect.catchDefect` 把 SDK defect 送入失败通道。
2. `permission/index.ts`：layer 计算 `reviewerDisabled` + `reviewerFailureFallback ?? "user"`；`reviewerDisabled` 时不传 reviewer。
3. `permission/reviewer/service.ts`：`fallbackToUser = fallback !== "deny"`；`markToolReviewFailed` 加 catch/catchDefect 防御（metadata 写入失败不产生第二失败模式）；默认路径发 `ReviewerFallbackToUser`，非 fallback 路径 `isReviewerError` 包装为类型化 `ReviewerRunError`；展示路径补 catchDefect。
4. `core/v1/config/permission.ts`：fallback 描述 "Defaults to deny." → "after retry… Defaults to user."（纯描述，无字段变化，无需 SDK regen）。
5. `docs/opencode-sandbox-windows-design.md`：路由框与硬约束清单改写（retry 先行→默认人工 ask；deny 为显式开关；合约矛盾仍 deny）。
6. `auto.test.ts`：3 用例 deny→ask 翻转 + SDK defect（Effect.die）用例 + 新 "fail closed when fallback deny" 用例（超时/FallbackToUser tag 双断言）。
7. `next.test.ts`：新增 `failingReviewedEnv`/`failingReviewedConfigEnv`（AppNodeBuilder/LayerNode.group 形态）+ 2 实例用例（失败→待审批用户请求；deny 配置→AutoDeniedError 且 0 pending）。

## 目标适配

- 路径迁移：config 描述 hunk 落 `packages/core/src/v1/config/permission.ts`（V1 配置 owner，含 #196 注释 ctx）；`MessageID` brand → `tool.messageID` plain string；`Permission.AutoDeniedError` → `PermissionV1.AutoDeniedError`；next.test 测试基建从 `Layer.mergeAll` 重写为 `AppNodeBuilder`/`LayerNode.group`。
- auto.ts `isShell` 删除 hunk 正确落（#232 已移除该行，hunk 合并跳过）；service.ts 两处 comment-only hunk 未重放（state 中对应位置措辞已被 #209/#227 重构改写，且 #264 注释已在真实失败处理点补充了 fallback 语义说明；此处置为双审 N 级认定，不影响行为）。
- 遗留 N：auto.ts state 顶部 `#203：…fail closed…` 过时注释与新 #264 默认 ask 行为并存，断言以行为为准；留待后续 patch 触及时修正。
- **node-deps 处置（双审后措辞修正）**：original/0264 并不含 node-deps hunk（全文无 deps/LayerNode 变更）；该义务是本 port 0261 S6 引入的目标架构义务（LayerNode 依赖图）。stale current 曾含一个 node-deps 草稿 hunk，因 0261 已将 node deps 更新为含 `Plugin.node`，重放会触发重复声明，故整体删除。处置后 state-0264 中 `export const node` 恰好一次，`Plugin.node` 恰好出现一次，无重复声明。语义无丢失，owner 正确（0261 是 plugin-hook 复用的引入 patch）。
- 格式修复：dup `#264` 注释去重、2 处列 0 `).resolves` 缩进、`isReviewerError` 注释缩进对齐。

## current 修改内容（sha256=4a208c076527d89ec2adc0936a049cc2ccdecc998c128e134744b2fba72cdae0，25111 bytes）

- 生产：auto.ts 3 hunk、index.ts 2 hunk、service.ts 3 hunk、config 描述 1 hunk、docs 2 hunk。
- 测试：auto.test.ts 6 hunk、next.test.ts 3 hunk。
- 无 source hunk 语义删除（node-deps 已由 0261 承载）；无测试删除。

## E/C

- E = 266（plus 228 + minus 38）；C = 41 ≥ need = ceil(266×0.15) = 40。
- C 分布：auto.ts 8、index.ts 5、service.ts 9、auto.test.ts 11、next.test.ts 8——覆盖 fallback 语义翻转、defect 桥接、markToolReviewFailed 防御、tag 信号语义、测试隔离与断言意图。

## 验证

- materialize 264 --rebuild: passed 264/264。
- typecheck 264: 全部 workspace passed（install reused 549c6966）。
- 行为测试（.test-workspace@264）：`bun test test/permission/auto.test.ts --timeout 120000` → 22 pass / 0 fail；`bun test test/permission/next.test.ts --timeout 180000` → 100 pass / 0 fail；reviewer-service 4 pass / 0 fail（双审复现 126 pass / 0 fail / 262 expect）。
- E/C 复核：C=41 ≥ 40 达标。

## 修订失败史

1. 预研发现 node-deps hunk preimage 与 261 后树双漂移（依赖顺序 Auth 位置 + #198/#250 注释措辞）→ 语义已被 0261 承载，hunk 整体删除并在此记录处置。
2. `).resolves` 缩进修复误产生 `)..resolves` 双点语法错误（auto.test 1 error）→ 正则修正后复验。
3. C 缺口分三轮补注释（29→34→37→41），每轮 materialize --rebuild + typecheck 复验。

## V1/V2 与 owner 判定

- 权限路由在目标与上游同为 `permission/` V1 谱系；config 描述落 `core/src/v1/config`（#196 迁移后的真实 owner）。无 V2 重叠能力，无双 owner。
- fallback 语义单一来源：`auto.ts` 的 `fallbackToUser` 分支是唯一裁决点；service 层只发信号（ReviewerFallbackToUser/typed error），不重复裁决——无隐藏成功路径。
- 测试可失败性：deny 用例断言 AutoDeniedError 实例与 list() 为空；默认用例断言 ask+reviewer_unavailable 来源——生产翻转任一分支即失败。

## 最终行为

- reviewer 基础设施失败（超时/provider/schema/defect）经既有 retry 耗尽后默认回到人工审批；`fallback: "deny"` 是显式 fail-closed 开关；策略 deny 与合约矛盾永不降级。SDK defect 经 catchDefect 桥接进同一失败通道，不绕过权限边界。
- 批次 260-264 状态：260/261/262/263/264 全部闭合，待批次末验证与双审。
