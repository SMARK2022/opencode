# Record 0264-3798c7696f55 — fix(auto-review): 失败后回退人工审批

## Manifest
- index: 0264, sha: 3798c7696f55
- original: original/0264-3798c7696f55.patch（26954B，7 文件）
- current: current/0264-3798c7696f55.patch（复杂适配（~18KB，SHA d57233e8e5dd））

## Source 行为
reviewer 失败后默认回退用户 ask（fallback=user）；保留 fallback=deny 的 fail-closed；catchDefect 捕获 SDK defect；测试验证 fallback 行为

## 目标适配
config/permission.ts 路径迁移到 core/v1/config；auto.ts reviewerDisabled→reviewerFailureFallback + #225 注释适配 + Effect.catchDefect 添加 + errorTag 简化；index.ts 新增 reviewerDisabled/reviewerFailureFallback；service.ts catch 块重构 + catchDefect + fallbackToUser + isReviewerError + Plugin.node 加入 deps；测试用 AppNodeBuilder.build + LayerNode.group 替代 Layer.mergeAll + Permission.layer；PermissionV1.AutoDeniedError 替代 Permission.AutoDeniedError

## E/C
- E=172, C=27, need=26, PASS

## 验证
- materialize 264: passed 264/264；opencode typecheck exit 0；core typecheck exit 0

## DUAL-AUDIT SEAL（批次 261-265）
- 待审计

## Source 证据（original/0264，27489B，535 行，7 文件）
### docs/opencode-sandbox-windows-design.md（2 hunk）
- cautious/strict 路径: deny -> ask (reviewer unavailable/fails after retry)
- 硬约束: reviewer 不调用 tool -> retry -> fallback; timeout/error -> retry -> fallback; fallback=deny 显式 fail-closed

### config/permission.ts（1 hunk）
- fallback description: "Defaults to deny" -> "Defaults to user"

### auto.ts（3 hunk）
1. reviewerDisabled?: boolean -> reviewerFailureFallback?: "deny" | "user"
2. if (!reviewer): fallbackToUser 逻辑替代 reviewerDisabled + metadata.agent 分支
3. Effect.catchDefect + onFailure 简化: fallbackToUser -> ask, else deny

### index.ts（2 hunk）
1. 新增 reviewerDisabled + reviewerFailureFallback 变量
2. evaluate 调用: reviewerDisabled ? undefined : reviewer

### service.ts（4 hunk）
1. fallbackToUser = autoReview?.fallback !== "deny"
2. model comment 更新
3. timeout comment 更新
4. catch 块重构: catchDefect + handleReviewerFailure + markToolReviewFailed catch/catchDefect + isReviewerError

### auto.test.ts（5 hunk）
- fail closed -> ask; reviewerDisabled -> reviewerFailureFallback; 新增 fallback=deny 测试 + defect 测试

### next.test.ts（4 hunk）
- failingReviewedCalls + failingReviewerLayer + failingReviewedEnv + failingReviewedConfigEnv + 2 新 instance 测试

## 上游 owner 分析
- config/permission.ts: 目标迁移到 packages/core/src/v1/config/permission.ts
- auto.ts: 目标有 #198/#203/#225 注释（source 无）
- index.ts: 目标有 #199 注释
- service.ts: 261 已修改（Plugin import/yield/hook/defaultLayer）
- test: 目标用 AppNodeBuilder.build + LayerNode.group（source 用 Layer.mergeAll + Permission.layer）

## 目标适配
1. config: 路径迁移到 core/v1/config/permission.ts
2. auto.ts: #225 注释适配 + fallbackToUser 替代 reviewerDisabled + catchDefect 添加 + errorTag 简化
3. index.ts: reviewerFailureFallback + reviewerDisabled ? undefined : reviewer
4. service.ts: catch 块重构 + catchDefect + fallbackToUser + isReviewerError + Plugin.node 加入 deps
5. test: AppNodeBuilder.build + LayerNode.group + PermissionV1.AutoDeniedError
6. docs: 零适配（context 匹配）

## Hunk 映射
- 7 文件全适配, 行号偏移因目标注释和 261 修改

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- opencode exit 0, core exit 0（独立验证）
- packages/app TS1128 pre-existing

## E/C 计算细节
- added non-blank: 199, Chinese comments: 27
- E = 172, need = 26, C = 27 PASS

## 修订历程
- v1: adapt-264.py 直接编辑 263 状态文件 + git diff
- v2: fix auto.ts if (!reviewer) 块（#225 注释适配）
- v3: fix Effect.catchDefect 添加（缩进适配）
- v4: fix catch 块尾随逗号
- v5: fix test file AppNodeBuilder.build + PermissionV1.AutoDeniedError
- v6: fix service.ts Plugin.node 加入 deps
- final: SHA d57233e8e5dd

## 验证（最终）
- materialize 265: passed 265/265
- opencode typecheck exit 0, core typecheck exit 0

## B-02 修复（第二轮审计反馈）
- 问题：auto.test.ts 两个测试断言未更新（deny→ask），defect 测试缺失
- 修复：(1) native auto 测试 deny→ask (2) TimedOut 测试 deny→ask (3) 新增 Effect.die defect 测试
- 验证：auto.test.ts 2 pass / 2 expect + 1 pass / 3 expect（含 defect 测试）
- E/C 修复后：E=179, C=28, need=27, PASS

## B-01 修复（第四轮审计反馈——显示路径 catchDefect）
- 问题：service.ts 显示路径（markToolReviewed/updateToolAutoReview）缺少 Effect.catchDefect
- 修复：添加 Effect.catchDefect(() => Effect.void) 到显示路径 pipe，更新注释
- 验证：opencode typecheck exit 0；auto.test.ts 22 pass
- E/C 修复后：E=180, C=29, need=27, PASS

## DUAL-AUDIT SEAL（批次 261-265，第七轮）
- 正向审计：ses_019fa5b6bffegepxAh7SoPSDi2 — 5/5 PASS，无 B/G
- 逆向审计：ses_019fa44acffeOcZHDIE4EK801T — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前六轮 BLOCK 修复：(1) G-01 records 过短→扩充;(2) B-01 265 测试丢弃→4个v1搜索测试;(3) B-02 264 测试断言→deny→ask+defect;(4) B-01 264 显示路径catchDefect→添加;(5) B-01 265 测试嵌套→移到describe顶层
- materialize 265: passed 265/265
- opencode/core typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
- 已知限制: 265 v2 SessionMessageTable 测试未适配（Database API 差异），v2 搜索实现完整
