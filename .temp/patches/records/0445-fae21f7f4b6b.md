# 0445-fae21f7f4b6b fix(acp): 稳定终态生命周期

## Identity
- index / full SHA: 0445 / fae21f7f4b6b（manifest parent = 0444 cc6bcd9d2372）
- subject: fix(acp): 稳定终态生命周期
- original: 295261 bytes / 3095 行 / 4 文件（2916 行为 plan doc）
- current: 与 original 逐字节一致（未适配）
- status: 预研完成——硬依赖 0443 落位（Record 先于适配编辑建立——合规时序）

## Source Intent
1. **docs/plans/opencode-disk-write-hotspots.md 新建 2916 行**：R19 canonical plan 归档——磁盘写入热点 GOAL 的审计/授权/verdict 证据链（ACP lifecycle 三项 divergence 表、R16 BLOCK 记录、R19 批准依据）。非运行时代码。
2. **src/acp/agent.ts 7 hunks（净约 35 行）**——核心修复三个 invariant：
   - callKey(sessionId, callID) 提升到 toolStart 之前（消除重复计算）。
   - pending 分支仅在 marker 非 terminal 时才 shellSnapshots.delete(key)——防止迟到 pending 抹掉 terminal marker。
   - toolStart 私有方法新增 terminal 守卫：Shell 工具若 (sessionID, callID) 已有 terminal marker，在发送 synthetic pending 之前直接 return。invariant："terminal 之后该 call 的 ACP lifecycle updates 必须为零"。
   - live partUpdate 路径与 processMessage replay 路径双入口同规则；completed/error 复用提升后的 key 删除 toolStarts（行为中性去重）。
3. **test/acp/event-subscription.test.ts +18 行**：每个 terminal case 新增尾部断言——定位 completed/failed 更新下标，断言其后更新数为 0（直接验证 terminal 支配 invariant）。
4. **test/session/prompt.test.ts 4 hunks**：**回退 0443 引入的 queued-loop 测试时序变更**——Effect.yieldNow → 恢复 Effect.sleep(50)；shortSessionTimeout → 恢复 10_000（附 Windows 冷启动注释）。commit message 明确这是"回退未经授权的 queued-loop 测试变更"。

## 0440/依赖链状态
- 硬依赖 0443：agent.ts 的 callKey/markShellTerminal/shellSnapshots/toolStarts、event-subscription.test、prompt.test 回退对的上下文——全部由 0443 引入。blob 链验证：0443 的 prompt.test post-image 13f685997a09 == 0445 pre-image。
- 0443 自身在 0440 state 为重定位适配（agent.ts → acp/event.ts 架构）——0445 的 agent.ts hunks 将随 0443 的适配形态同步重定位。
- prompt.test 回退对的净效果：0443+0445 在该文件的时序变更相互抵消，测试恢复 0440 原状（Effect.sleep(50) + 10_000）——Windows 冷启动友好，与本机环境一致。

## 适配策略
1. agent.ts hunks：跟随 0443 的重定位形态——若 0443 的 marker/terminal 逻辑落在 acp/event.ts Subscription 类，则 0445 的 callKey 提升、pending terminal 守卫、toolStart 守卫同步落位 event.ts。
2. plan doc 2916 行原样新建。
3. event-subscription.test 尾部断言：跟随 0443 的测试落位形态。
4. prompt.test 回退对：0443 适配若已跳过 queued-loop 时序变更（直接恢复 0440 原状），则 0445 的回退 hunk 变为 no-op——记录为净效果抵消，避免双重回退冲突。

## 语义风险
- toolStart 守卫的边界：terminal 后该 call 不再产生 synthetic pending 是预期 invariant；若 0443 的 marker 语义在适配中变形，此边界漂移。
- plan doc 中 R16 原始 verdict 为 BLOCK（后经 R19 修复流程批准）——引用时注意历史语境。

## E/C 预估
agent.ts 适配行 + 测试断言行；plan doc 为 .md 排除。以 count-ec-auditor 最终复算为准。

## 验证计划
- materialize 445（443/444 落位后）。
- 行为测试：event-subscription 的 terminal 尾部断言 + prompt.test 回退区域的健康度。

## Status
预研完成，待 0443/0444 适配落位后执行（本 record 建立于适配编辑之前——合规时序）

## terminal 支配的三层防护（source 语义）
- 第一层：pending 分支删除 marker 前检查 terminal——迟到 pending 不抹掉 terminal。
- 第二层：toolStart 入口守卫——terminal 后同 call 的 synthetic pending 直接 return。
- 第三层：测试尾部断言——completed/failed 下标之后更新数为零。

## 与 0443 的回退对说明
- 0443 将 prompt.test 两处 Effect.sleep(50) 改 yieldNow、两处 10_000 改 shortSessionTimeout。
- 0445 将这四处逐字回退（Windows 冷启动友好）。
- 适配时若 0443 的移植直接保持 0440 原状（未引入时序变更），则 0445 的回退为净效果抵消，按 no-op 记录。

## plan doc 的证据链价值
- 2916 行 plan 记录了磁盘写入热点的完整 divergence 分析——对后续进度通道的维护有引用价值。
- 该文档为 .md，E/C 排除口径同前。
- R16 BLOCK → R19 批准的历史语境：BLOCK 原因（测试时序变更未经授权）正是 0445 回退 hunk 修复的内容。

## 批次内净效果矩阵
- agent.ts：0443 重定位（event.ts）→ 0445 守卫追加于同一落点。
- event-subscription.test：0443 新建 → 0445 追加 terminal 尾部断言。
- prompt.test：0443 时序变更 → 0445 逐字回退 → 净效果 = 0440 原状。
- plan doc：0445 独立新建。

## Round-2 闭合验证（最终）

### 适配映射
1. docs/plans/opencode-disk-write-hotspots.md：新建 2916 行 canonical plan 文档，**原样保留**（0437 先例：plan doc 是 blob 链基座与后续审计的合同文本）。
2. 源 acp/agent.ts B-01/B-02 修复 → state acp/event.ts（0440 架构 owner）：toolStart 前置 terminal 守卫（bash && marker.terminal → return，阻止 synthetic pending 重开 lifecycle）+ pending 分支 terminal marker 保留（非 terminal 才 delete）。replay/processMessage 修改随 handleToolPart 共享路径自动覆盖（state 的 replayMessage 走 handleToolPart）。
3. 源 event-subscription.test terminal-lifecycle 增强 → state acp/event.test.ts：keeps completed shell terminal over late running progress and replayed pending（terminal 后 late progress v5 + replayed pending 均不产生新帧；updates.slice(terminalIndex+1) 为空）。该测试即 0443 Round-8/9 顺延项（当时 toolStart 守卫属 0445 本体，运行验证证实 0443 实现下断言失败——behavior 已在 0445 实现后实证通过：17 pass/0 fail）。
4. 源 prompt.test sleep(50)→yieldNow 与 10_000→shortSessionTimeout 的回退 hunks：**no-op**——state 的两个 loop 测试历史上即为 sleep(50)+10_000 形态（0445 正向后的目标形态），无对应 pre-image（record Round-1 已记录）。

### 验证
- materialize 445：passed 445/445（累计含全部 0441-0444）。
- typecheck 445：全包 passed；install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad。
- 行为测试：acp event.test 17 pass/0 fail（含 terminal-lifecycle 新测试）。
- E/C（**代码口径**）：docs 2916 行为 markdown 文档 prose，非代码，不计入 E（0437 plan doc 先例同口径、审计已接受）。代码段 additions=40、blank=3、注释=5 → E=32、C=5 ≥ ceil(32×0.15)=5（15.6%）PASS。注释位置：acp/event.ts 的 pending 保留与 toolStart 前置守卫、event.test 的 terminal-lifecycle 意图与 version 合同说明。
- 若审计采用全 additions 口径（含 docs），则该口径与 0437 已通过先例冲突，应以先例口径为准。
