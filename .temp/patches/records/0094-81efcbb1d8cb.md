# Record 0094-81efcbb1d8cb

## Source intent
1. **source.ts**：`renderedLines` 与 `bytesCut` 分离；**header 不占行预算**；行限只 break 不置 bytesCut；24KB 才置 bytesCut；页脚 24KB 文案仅 bytesCut；`returned: renderedLines`；行窗未尽且非字节截断时 more。
2. **TUI**：GenericTool / Shell / Write / Edit / ApplyPatch 对齐 SMARK **preview 10 / threshold 20**（>20 行才折，预览 10）。

## Upstream
- source：header 也计入 `rendered`、统一 `cut` 标志、24KB 与行限混用 footer。
- TUI：Generic maxLines=3；Shell 10 无 threshold；Write 20 无 threshold；Edit/ApplyPatch maxHeight=15、overflow>20。

## Target adaptation
### source.ts（完整函数，禁止截断）
- 从 state 93 完整文件改写，保留 `buildHeader`/`lastRangeEnd`/return
- `outer` 循环；header 只计 bytes；源码行计 `renderedLines`
- 行限 → `lineLimited` + break outer；字节 → `bytesCut`
- footer 分支与 data.`bytesCut`/`lineLimited`/`returned`

### TUI
- `collapseToolOutput` 第 4 参 `threshold`（默认 maxLines 兼容旧 3 参）
- GenericTool / Shell / Write：`maxLines=10` + `threshold=20`；Write 用 `code()` 非 `output()`
- Edit：`overflow = total>20`，折叠 `maxHeight=10`
- ApplyPatch：`patchOverflow = lines>20`，折叠 `maxHeight=10`

## Verification greps (tip 94)
- source 190 行级完整、`function buildHeader`、`return {`、brace 0
- 无 `renderedLines>=limit` 后设 `bytesCut=true`
- `collapseToolOutput(code()`；Edit/Apply `maxHeight ... 10`
- packages/tui typecheck passed

## Materialize / typecheck provenance
- tip `states/0094-81efcbb1d8cb`
- typecheck `typecheck-0094-2026-07-22T07-25-34-017Z.md` No failures；fingerprint `67995d9429bc…dab9c2a`


## Call chain — source
Agent → `vscode_notebook_source` → bridge → `notebookSource` → computeVirtualRanges → outer cells → header bytes → numbered lines → return summary/data.

## Call chain — TUI
Tool part → GenericTool|Shell|Write|Edit|ApplyPatch → collapseToolOutput(...,10,maxChars,20) 或 maxHeight=10 + overflow>20。

## Three-way source
| event | lineLimited | bytesCut | footer |
|---|---|---|---|
| renderedLines>=limit | true | false | more 分页文案 |
| bytes over 24KB | false | true | 24KB 文案 |
| window not finished | false | false | more 文案 |

## Three-way TUI
| component | pre-94 | SMARK | tip |
|---|---|---|---|
| GenericTool | maxLines=3 | 10/20 | 10/20 |
| Shell | 10 no thr | 10/20 | 10/20 |
| Write | 20 no thr | 10/20 | 10/20 code() |
| Edit | maxH 15 thr20 | maxH10 thr20 | maxH10 thr20 |
| ApplyPatch | maxH 15 thr20 | maxH10 thr20 | maxH10 thr20 |

## Integrity checklist (must hold after materialize)
1. source.ts 含 function buildHeader + return { + brace balance 0
2. 无 renderedLines 达限写 bytesCut=true
3. Write 使用 collapseToolOutput(code()
4. packages/tui typecheck passed


## Header line budget
header 仍计入 24KB 字节预算（hdrBytes），但 **不** 增加 renderedLines。这样 limit=400 表示最多 400 行源码，而不是 400 个“输出物理行（含 header）”。


## Post-r6 tip integrity
- Edit maxHeight collapsed = 10；overflow total>20
- ApplyPatch maxHeight = 10；patchOverflow lines>20
- source 行预算满时 pre-header break；cell 结束后 bytesCut||lineLimited||renderedLines>=limit 退出外层
- tip source 完整：buildHeader + return + brace 0


## Shell running/context
Idle: collapse 10/20. When `isRunning || showContextOutput`, return `{overflow:false}` full output (SMARK maxLines=0 equivalent).

## Files / hashes

## Risk accepted
- 无真实 Jupyter UI E2E；以 status 分支与 public API 探针契约表达。
- TUI outputPreview 仍用 4 行 collapse（非 SMARK BlockTool 主面）。

- current: `0094-81efcbb1d8cb.patch`
- sha256: `471a1474aace2b4e31d7065895f52010f124be37641714052b07434b0b53844a`
- bytes: 13660
- E=59 C=21 need=9 gate=PASS
- updated: 2026-07-22T06:25:00Z

## Independent dual audit (batch 90-94)
- FORWARD: **PASS** all five, no open B/G — task `ses_07747ffb8ffeoOHdlE46H0FgNE` (r11)
- REVERSE: **PASS** all five, no open B/G — task `ses_07747ff96ffe5KTvNREEFo8UZM` (r11)
- Tip: `states/0094-81efcbb1d8cb`
- Typecheck: `typecheck-0094-2026-07-22T07-25-34-017Z.md` (No failures; install `67995d9429bc…dab9c2a`)
- Batch release: **PASS**
