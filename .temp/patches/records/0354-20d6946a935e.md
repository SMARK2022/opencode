# Record: 0354-20d6946a935e（351-400 波审计后扩写版）

## Identity
- index: 0354；sha12: 20d6946a935e；parent: 70bf89d5f574（=353）；subject: feat(harness): overlap suppress 引导式文案 + edit 字符级 diff + range 聚合修复
- original: .temp/patches/original/0354-20d6946a935e.patch（7 文件 407 行 docs + 生产 + 测试）；current: .temp/patches/current/0354-20d6946a935e.patch（52349B；sha256=01a9c56f1a4eb2a3afbf7fedd675e09b070ea7b1ca8c5eb7cfc9703b2ad823b8）

## Source intent（逐 hunk 行为重建）
1. docs/p1-overlap-edit-diff-plan.md 新建 407 行（verbatim）。
2. edit.ts：import diffChars + type Change；findClosestMatch 窗口按 oldString 行数缩放；formatClosestMatchDiff 字符级 diff（相似度 <60% 回退 head+tail 摘要）；formatCharDiffLine 前后 30/20 字符 context——edit 失败时给 agent 精确字符级差异反馈。
3. read.ts：import mergeRanges；suppress 注释更新；computeUnreadRanges 改为同版本 mergeRanges 后区间减法；overlap note 引导文案；suppress 块加 contentLength>=300 门——不足 300 行时不 suppress 而给引导式 message 并 fall through。
4. read.txt：stub 说明替换（offset 引导）。
5. task.ts：stub 注释 [local-smark] 化。
6. edit.test：2 个字符级 diff/回退测试；read.test：overlap note 断言更新 + readMessage 助手 + 5 个 suppress it.live 测试。
- invariant：suppress 只对"足够大且已读位置明确"的重叠生效；抑制时必须给出精确的未读范围引导；字符级 diff 保证 edit 重试命中率。

## 三方比较
- 上游 v1.17.20：无 suppress/字符级 diff 能力（SMARK harness 增强）。
- 目标差异（关键必要适配）：目标 `findOverlapNote` 返回**逗号连接的多区间串**（read.ts:307），source 解析单区间——verbatim 复制会对多区间产生 NaN。current 改为按逗号 split 后 `ranges.reduce` 求和（read.ts:981-999），与目标 `findOverlapNote` 既有 lines 聚合口径一致；suppress 后精确引导由 computeUnreadRanges（先 merge 再减法，read.ts:316-340）给出。最坏情形（请求区间全被抑制）得到 "no new content" stub，行为正确。
- computeUnreadRanges 类型用目标 ReadRangeMeta；renderContentLines 签名保持 raw 形态；readMessage 助手为目标新建（source 预置形态）。

## V1/V2 owner 判定
- owner 在 V1 工具层 read.ts/edit.ts（唯一实现点，现役；无 V2 重复 owner）。SMARK 语义落在正确 owner。

## 逐 hunk 映射（物化落点，states/0360+ 实测）
- edit.ts:774/839（findClosestMatch 窗口/formatClosestMatchDiff）；read.ts:316/981-999（computeUnreadRanges/多区间求和）；read.txt:12；task.ts stub 注释；2 edit 测试 + 5 suppress it.live + readMessage 助手全部在位。

## E/C 与注释位置
- E≈280-307；C≈53-80（红线/suppress 门 300 行理由/字符级 diff 回退 60% 阈值/多区间求和必要适配注释/各测试意图，分散于 edit.ts、read.ts、两测试文件）。门禁 C≥ceil(E×0.15)=47 ✓（双向审计独立复算通过）。

## 物化与验证
- materialize 354 passed；typecheck-0354 全 workspace passed（指纹复用 303af56d 合法）。
- 行为测试：read.test + edit.test 103 pass / 0 fail（含 5 个新 suppress it.live + 2 个字符级 diff 测试）。

## 失败修订链
- ReadMetadata→ReadRangeMeta 类型适配；renderContentLines 签名还原；new-file/多区间解析/尾空行三处 corrupt 预防——均 R12/R13 期闭合，全绿收口。

## 最终行为
- read 对大重叠输出 suppress 并给出精确未读引导；edit 失败反馈字符级差异；103 测试锁定。无行为面损失。

## 审计残留（N 级）
- `// #142：渲染内容行` 注释因插入 computeUnreadRanges 悬空在错误函数上方（双向审计 N-02，纯注释错位，行为无影响）——下次触碰 read.ts 的 patch 修订时顺带归位；readMessage 助手返回 any（N-03，测试 fixture，行为无影响）。
