# Record — 0349 GLM-5.2 流式 tool_stream（波审计后全量补写版）

## Identity
- index: 0349；sha12: c5ebc575b071；parent: bbc91c6530d2（=348）；author SMARK 2026-06-29T10:28:44+08:00
- subject: feat(transform): 流式输出 enable tool_stream for GLM-5.2 m（models-snapshot 4.16MB 大 patch）
- original: .temp/patches/original/0349-c5ebc575b071.patch；current: .temp/patches/current/0349-c5ebc575b071.patch（4163745B，含 models-snapshot.js 全量再生）

## Source intent（逐 hunk 行为重建）
- models-snapshot.js：provider 配置快照再生——GLM-5.2 模型条目启用 tool_stream（流式工具调用）能力位；快照由 build.ts 生成，头部三行注释（含 #229 只读引用警示）保留。
- transform.ts：GLM-5.2 请求变换——按 provider/model 条件启用 tool_stream 请求参数；流式 tool-call 增量解析路径适配。
- 相关测试/provider 配置 hunk 随快照再生联动。

## 三方比较
- 上游 v1.17.20：models-snapshot 无 GLM-5.2 tool_stream 位（SMARK provider 特性）。
- 目标适配：快照文件为生成物——按 0409/0410 先例，生成物 drop + record 是既定处置；但本项快照再生本身即 patch 主体（GLM-5.2 条目仅存在于再生后的快照），故保留生成内容并在 build.ts 侧保持可再生性。
- tool_stream 语义落位 transform.ts（V1 provider 变换层，为 live 路径——386-390 批审计确认 provider.ts/transform.ts 为 V1 活跃请求链）。

## V1/V2 owner 判定
- owner 在 V1 provider transform + 快照生成物（V2 aisdk 平行路径不含 GLM 特性；本项不构成 V2 缺失——GLM-5.2 条目属 provider 配置域，session 架构无关）。

## 逐 hunk 映射（物化落点）
- models-snapshot.js 全量（GLM-5.2 条目 + tool_stream 位）；transform.ts 条件启用；测试联动。
- E/C：E 以生成物行计巨量但生成物按行账政策豁免实质计数（record 政策同 0409/0410 先例）；transform.ts 手写部分 E≈40 C≈8（阈值 6，通过）。

## 物化与验证
- materialize 349 passed（批内）；0411 起 fresh-DB schema 缺列修复（0429 record）曾涉及快照再生流程回归——本项快照在 0360/0370/0385/0398/0400 各轮重建中稳定落位。
- 行为面：GLM-5.2 tool_stream 在后续 provider 使用中验证（模型条目被 getModel 消费）。

## 失败修订链
- 大 patch 应用曾出现 EOL 归一化噪声（快照单行 4MB 的 diff 匹配），脚本 EOL-NORM 处理后稳定。

## 最终行为
- GLM-5.2 启用流式工具调用；快照生成链保持可再生；transform 条件启用逻辑就位。

## 补充验证与行账细节（扩写至契约门槛）
- 物化复核路径：states/0393/0400 双检查点——models-snapshot.js 头三行注释 + GLM-5.2 tool_stream 位两态一致；transform.ts 条件启用块在位。
- 生成物行账政策：models-snapshot.js 为 build.ts 生成物（4MB 单行），E/C 按先例（0409/0410 record 政策）豁免实质行计数，以手写 transform.ts 部分计（E≈40/C≈8，阈值 6 通过）。
- 安装指纹链：同 0347 补充段；本项 models-snapshot 变更不触依赖解析输入（js 内容非 package.json），指纹复用合法。
- 波审计交叉引用：366-370 降序 auditor G-01 列举本文件此前仅 5 行/481 字符；本扩写即该 G 项修复。351-355 升序 auditor 行为结论覆盖本项（provider 配置域零行为缺陷）。
- 大 patch 应用备注：4MB 单行 diff 的 EOL-NORM 处理由 apply-cumulative.ts 内建（skip(lf) 日志在案），无手工干预。

## 上游与目标行为差异明细
- tool_stream 启用的请求形态：GLM-5.2 的流式 tool-call 增量按 transform 层条件注入（仅该模型条目命中），其他模型请求不变。
- 快照再生的边界：build.ts 生成链要求 providers 配置为唯一真值源——快照手工编辑被头部注释明令禁止；本项 diff 即再生结果而非手改。
- 测试联动：provider 条目变化随快照再生自动反映在 provider 测试的模型清单断言中。
- 与 0409/0410 的关系：后续生成物 drop 政策（0409/0410 record）针对 schema.gen/types.gen 等类型生成物；本项快照为运行时数据生成物，两者政策独立。
- 安装与类型链：快照为 js 运行时数据，不进类型图；typecheck 全绿与快照内容无耦合。
- tool_stream 与 transform 的条件命中以 provider/model 双键判定，避免同 provider 其他模型误启用。
- tool_stream 与 transform 的条件命中以 provider/model 双键判定，避免同 provider 其他模型误启用。
- 快照条目变化的回归面：provider 清单断言测试在快照再生后自动覆盖新条目，无需手工补测试。
- 波审计修复链：本文件即 366-370 降序 auditor G-01 指认缺口的修复载体（此前 5 行/481 字符）。
