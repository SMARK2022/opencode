# Record 0075 — `4141d5e979ff`

**Subject:** feat: 延迟测试，增加API URL的默认配置，优化模型API的获取逻辑  
**Parent:** `4d55a2c0f4a3` (#74)  
**Index:** 75  
**Current path:** `current/0075-4141d5e979ff.patch`  
**Original path:** `original/0075-4141d5e979ff.patch`  
**Current SHA-256:** `1b6c8f55e1f798d8ef2419f73ee3443773d310f96b77a98accdceadf30403c19`  
**Current bytes:** 10407  
**Author (manifest):** SMARK `<3222301373@qq.com>` · 2026-05-04T19:58:19+08:00  

## 1. Source intent (from original patch only)

Source touches two owners:

### 1.1 TUI sidebar latency probe

Original path: `packages/opencode/src/cli/cmd/tui/feature-plugins/sidebar/context.tsx`  
Intent:

1. Resolve selected model API **origin** only (not full path) so HEAD measures network RTT without 404 noise.
2. Ping every **1s** via **serial** `setTimeout` (not overlapping interval): first ping immediate, then reschedule after completion.
3. Use `NetworkProxy.resolveProxyRoute` + `NetworkProxy.routedFetch` with `purpose: "provider"`, method HEAD, `AbortSignal.timeout(3000)`.
4. Track `active` flag; on URL change / cleanup set inactive and clear timer so late responses cannot `setState`.
5. Offline only when status is **502/503/504** or fetch throws; other statuses still show RTT.
6. Display proxy icon 🌐 vs direct ⚡; color RTT: `<300` success, `<800` warning, else error; init shows muted `...`.
7. Render latency **only when** a resolvable API URL exists (gate on `targetApiUrl()`).
8. Layout: join spent + latency on the cost line (SMARK also moved percent onto tokens line — layout detail).

### 1.2 Provider default API URLs

Original path: `packages/opencode/src/provider/provider.ts`  
Intent:

1. `SDK_DEFAULT_API_URL` map for `@ai-sdk/anthropic`, `@ai-sdk/google`, `@ai-sdk/openai`.
2. `apiUrl({ npm, api })` → `api ?? map[npm] ?? ""`.
3. Apply at three sites: `fromModelsDevModel`, provider inheritance clone of models, config model parse.

## 2. Upstream / target pre-state (after #74)

### Upstream design

- **TUI:** SolidJS sidebar plugin under package split `packages/tui/...`. Theme tokens `success` / `warning` / `error` / `textMuted` already exist. Cost line shows `money.format(cost()) spent` only. No probe.
- **Provider:** Effect-based provider layer; model `api.url` already threaded through models.dev and config. When api missing, empty string was used — callers must supply endpoints.
- **Network:** After #71/#73, `NetworkProxy` lives in `@opencode-ai/core/network-proxy` with `resolveProxyRoute` / `routedFetch` / purpose-aware routing. This is the correct fetch owner for provider pings (not raw `fetch`).

### Target owners chosen

| Concern | Source path | Target owner |
|---|---|---|
| Sidebar probe | opencode TUI sidebar | `packages/tui/src/feature-plugins/sidebar/context.tsx` |
| apiUrl defaults | `provider.ts` | `packages/opencode/src/provider/provider.ts` |
| Fetch | NetworkProxy | `@opencode-ai/core/network-proxy` |
| Model selection | `useLocal().model` | same + last assistant fallback when local empty |

## 3. Three-way comparison and adaptation

| Invariant | SMARK source | Upstream/pre | Current decision |
|---|---|---|---|
| Probe scheduling | serial timeout + active | none | Keep SMARK serial + active |
| Offline set | 502/503/504 only | n/a | Keep SMARK (not all 5xx) |
| RTT colors | 300/800 thresholds | theme exists | Keep SMARK colors |
| URL gate | only when baseURL known | n/a | Keep; empty/illegal URL → null via try/catch |
| Model pick | local.model only | local + session msgs | **local first, last assistant fallback** (V2 UX; primary path still local) |
| Percent line | co-line with tokens | separate `% used` | **Keep upstream layout** for percent; append latency to spent line only |
| apiUrl | three call sites | empty string | Full SMARK map + three sites |
| JSX comments | none in spent children | n/a | Use `{/* … */}` **outside** text children (fix after audit B-01) |

### First divergence repaired

Earlier current used `setInterval` + weaker offline (`status < 500`) + always-visible `...` + no colors (comment lied). Rebuilt to match source probe hygiene.  
Later audit found `//` comment as **JSX text child** after `spent` (user-visible garbage). Fixed: block comment above `<text>`, no `//` between children.

## 4. Current patch content

Files:

1. `packages/tui/src/feature-plugins/sidebar/context.tsx` — probe + display  
2. `packages/opencode/src/provider/provider.ts` — SDK defaults + apiUrl  

No tests in source; none added (cannot invent tests without source harness intent beyond provider defaults).

## 5. E / C (this patch only)

- **E ≈ 109** effective code add lines  
- **C ≈ 23** qualifying Chinese explanatory comments (decision-adjacent: PING interval, origin resolve, active cancel, gateway offline, color thresholds, apiUrl sites, URL gate)  
- Need `ceil(109 * 0.15) = 17` → **pass**  
- Non-qualifying: none counted as pure identifier translations  

Comment locations (examples):

- `PING_INTERVAL` — serial schedule rationale  
- `targetApiUrl` — origin-only + empty/illegal guard  
- `active` — cancel in-flight  
- 502/503/504 branch — offline semantics  
- `latencyDisplay` — color thresholds  
- `SDK_DEFAULT_API_URL` / `apiUrl` / three call sites  

## 6. Verification

| Step | Result |
|---|---|
| Cumulative materialize through 75 (via batch end 79) | OK |
| `git apply` of current on post-#74 | clean |
| State-79: no JSX `//` after spent | verified |
| Batch typecheck `--typecheck 79` | all listed workspaces passed |
| Install fingerprint | `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea` |
| Typecheck report | `reports/typecheck-0079-*.md` (latest after rematerialize) |

## 7. Failures and revisions

1. Initial diluted TUI (interval/colors) → rebuilt full source probe semantics.  
2. Dual audit B-01: JSXTEXT comment leak → moved to `{/* */}`.  
3. Record length G → this expanded record (source/upstream/target/E-C/verify/revisions).  

## 8. Final behavior

- Sidebar cost line: `spent · 🌐/⚡ Nms` with RTT colors when API origin known; offline on gateway errors; no comment garbage in UI.  
- Models without explicit `api` resolve official anthropic/google/openai endpoints by npm package.  

## 9. Provenance

- Baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`  
- Source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`  
- No `original/` edits; no empty current; no temp `_*.patch`.

## Dual audit closure (batch 75–79)

- This index current SHA-256: `1b6c8f55e1f798d8ef2419f73ee3443773d310f96b77a98accdceadf30403c19` (10407 bytes)

- FORWARD: **PASS** (no open B/G) — task session independent auditor
- REVERSE: **PASS** (no open B/G) — task session independent auditor
- Typecheck: `reports/typecheck-0079-2026-07-21T13-22-01-724Z.md`
- Install: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Materialized: `states/0079-8812fd3d588b`
- Official currents only; no `_*.patch`

## Verdict

**PASS** (independent dual audit closed for batch 75–79)
