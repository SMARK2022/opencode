# Record 0377-5429c941c9c1

## Source patch

- Source: `feat(tui): 将 generic 工具输出的默认可见性设置为 true，以保持与内置工具一致` (SMARK2022, 2026-07-05)
- Original: `.temp/patches/original/0377-5429c941c9c1.patch` (28 lines, 1 file)
- Replay index: 0377, sha 5429c941c9c1

## Port decision summary

The upstream commit flips the default of the `generic_tool_output_visibility` kv signal from
`false` to `true` so that generic tools (goal, MCP, ChatGPT, …) render their returned content
as blocks by default, consistent with built-in tools like bash. Existing users who already
persisted `false` in their `kv.json` keep their choice (the kv signal only applies the default
when no stored value exists). The port is verbatim: the two Chinese comment lines and the
`false` → `true` flip are carried over unchanged.

## State analysis (state 0376)

- Target file `packages/tui/src/routes/session/index.tsx` EXISTS at the expected migrated path
  (upstream `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/`).
- The signal declaration sits at line 346 of the state-0376 file, immediately after
  `const [_animationsEnabled, _setAnimationsEnabled] = kv.signal("animations_enabled", true)`
  and before `const wide = createMemo(() => dimensions().width > 120)`.
- Local formatting deviation: in this series the `showScrollbar` signal above is wrapped across
  three lines with a duplicated scrollbar comment (a local quirk from an earlier item), so the
  upstream hunk context (`showScrollbar` on a single line) does not match exactly. The edit was
  re-anchored on the unique `generic_tool_output_visibility` line itself, which appears exactly
  once in the file (verified by grep).
- `kv.signal` semantics in this series: the second argument is the default used only when no
  persisted value exists, so flipping the default cannot clobber stored user preferences — the
  upstream comment's claim holds against the local implementation.

## Edit script (semantic)

1. In `packages/tui/src/routes/session/index.tsx`, replace the single line
   `const [showGenericToolOutput, setShowGenericToolOutput] = kv.signal("generic_tool_output_visibility", false)`
   (line 346) with:
   - two comment lines documenting the default-true rationale (generic tools shown as blocks,
     consistent with built-in tools; persisted `false` unaffected);
   - the same declaration with default `true`.
2. No other lines change; the surrounding `animations_enabled` / `wide` lines stay untouched.

## E/C gate

`count-ec-auditor.py`: E=1 (the single `true` default flip in the declaration line), C=2 (the two
Chinese comment lines, each ≥8 meaningful chars), need=1 → PASS. Comments are substantive
(consistency rationale + persisted-value caveat), not placeholder padding.

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix377.diff` (1 file, 1 hunk, +3/-1; 28 lines in the original
  maps to the same single-hunk shape)
- Current patch: `.temp/patches/current/0377-5429c941c9c1.patch`
- Materialized state: `0377-5429c941c9c1`

## Materialize

`--materialize 377` → `passed 377/377`, state published at
`.temp/patches/states/0377-5429c941c9c1`. The replay applied 0376 then 0377 cumulatively; the
`git apply --check --index` pass confirms the hunk header matches the state-0376 file (which
includes the 0376 dialog-prompt change in a disjoint file).

## Verification

- Scratch mirror: `git apply --check --index` + `git apply --index` against the state-0376 index
  passed, byte-identical to the materialize replay path.
- Type safety: the change only alters a boolean default argument; `showGenericToolOutput` keeps
  the same `boolean` signal type, so all downstream consumers (block rendering for generic tool
  parts) compile unchanged. Full batch-end typecheck is deferred to `--typecheck 380`.

## Notes / deviations from source

- Hunk context re-anchored on the single signal line because the local `showScrollbar`
  declaration is formatted differently (multi-line with a duplicated comment from an earlier
  item); the semantic change is identical.
- Comments ported verbatim (UTF-8).
- Nothing skipped; single-file change applied in full.
