# Record 0270-f9dec59534d4 — feat(slots): useRenderer + instruction remote test

## Manifest
- index: 270, parent: 0269-39f6ad23cb23, sha: f9dec59534d41bc7f56a189ca3e46f6062157e9b
- original: original/0270-f9dec59534d4.patch（6837B，144 行，2 文件）
- current: current/0270-f9dec59534d4.patch（3774B，SHA 1cbd392659a8）

## Source 行为（original 全文 144 行，2 文件）
1. **slots.tsx**（3 hunk）：
   - Hunk 1: import 添加 `useRenderer` from `@opentui/solid`
   - Hunk 2: Slot 组件添加 renderer identity check（`if (renderer && current !== renderer) return empty(props)`），防止跨 renderer 泄漏
   - Hunk 3: setupSlots 添加 `renderer = api.renderer` 捕获和 dispose 清理（`renderer = undefined`）
2. **instruction.test.ts**（3 hunk）：
   - Hunk 1: import 移除 `test`（test.todo -> it.live）
   - Hunk 2: instructionLayer/provideInstruction 添加 config 参数
   - Hunk 3: `test.todo("fetches remote instructions...")` 替换为 `it.live` 测试（Bun.serve + HttpClient.get + 验证远程指令进入 system prompt）

## 上游 owner 分析
- 目标 v1.17.20 slots.tsx 已用 factory pattern + createSignal（比 source singleton + module-level view 更强）
  - 目标的 `createSlots()` 工厂函数创建独立的 signal-scoped view，每个实例隔离
  - source 的 module-level `view` + `renderer` 是单例，需要 `useRenderer` check 防止跨实例泄漏
  - 目标的 factory pattern 已防止跨 renderer 泄漏（不需要 useRenderer）
- 目标 instruction.test.ts 用 AppNodeBuilder pattern（source 用 Layer.provide pattern）
- 目标 Config 导出 `Interface`（source 用 `Info`）
- 目标用 `TestConfig.make(config)` 接受 overrides（source 用 `TestConfig.layer({ get: () => ... })`）

## 目标适配
1. slots.tsx: 不修改（目标 factory pattern + createSignal 已是更强实现，useRenderer check 不需要）
   - 目标的 `createSlots()` 返回独立的 `{ Slot, setup, clear }` 对象
   - 每个 setup 调用创建独立的 registry 和 signal-scoped view
   - dispose 通过 `setView(() => empty)` 清理，signal 自动通知
   - 这比 source 的 module-level singleton + useRenderer check 更安全
2. instruction.test.ts: 适配 AppNodeBuilder pattern + Config.Interface 类型
   - 移除 `test` import（test.todo -> it.live）
   - instructionLayer 添加 config 参数（`TestConfig.make(config)` 替代 `TestConfig.make()`）
   - provideInstruction 添加 config 参数
   - test.todo 替换为 it.live 远程指令测试
   - 修复 `Config.Info` -> `Config.Interface` 类型

## Hunk 映射
- slots.tsx: 0 hunk（目标更强，无需修改）
- instruction.test.ts: 3 hunk 适配（import/layer/test）

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- opencode typecheck exit 0, core typecheck exit 0

## E/C 计算细节
- added non-blank: 44, Chinese comments: 6
- E = 38, need = 6, C = 6 PASS

## 修订历程
- v1: original 拷贝 -> slots.tsx 路径不存在 + instruction.test.ts context 不匹配
- v2: slots.tsx 不修改（目标更强），instruction.test.ts 适配
- v3: 修复 Config.Info -> Config.Interface 类型
- final: materialize 通过，typecheck exit 0

## 验证（完整）
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0
- packages/app TS1128 pre-existing（自 0247）

## 不一致性分析
- 目标 slots.tsx 的 factory pattern 是 upstream v1.17.20 的改进，比 source 的 singleton + useRenderer 更强
- 适配方式：不修改 slots.tsx（保留上游更强实现），仅适配 instruction.test.ts
- 目标的 Config.Interface 类型与 source 的 Config.Info 不同（v1.17.20 命名变更）

## DUAL-AUDIT SEAL（批次 266-270）
## DUAL-AUDIT SEAL（批次 266-270，第四轮）
- 正向审计 (ses_013f2f0a1ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013f2cbaeffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 266=0/0, 267=6/2, 268=66/10, 269=60/12, 270=38/6 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
