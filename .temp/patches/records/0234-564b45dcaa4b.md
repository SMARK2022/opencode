# Record 0234-564b45dcaa4b

## Status
PASS（E=494, C=75, need=75；typecheck 30/30；行为测试 3+9+8+19 全通过；materialize 234 成功）

## 修改内容
feat(notebook): 完整修改——notebook 工具 summary 统一 Notebook:/<Label>: 头部契约、cellRef 稳定引用、strict cellId 语义、insert 结果标识。source（v1.15.3→v1.15.4 目标线）对 sdks/vscode/src/notebook/* 与 packages/opencode 新增测试的完整行为重建。

### 文件清单（8 个）
- `sdks/vscode/src/notebook/format.ts` — 新增导出 `notebookPath`（posix 化路径）、`notebookHeader`（两行头部：`Notebook: <path>` + `<Label>: k=v ...`，fields 过滤 undefined）、`cellRef`（`cN id=#VSC-xxx kind/lang` 双标识）；c1/copilotLikeCellId 保留并加注释。
- `sdks/vscode/src/notebook/edit.ts` — 插入捕获 `insertedCell`（insert 后立即取 cellAt）；summary 用 `...notebookHeader(notebook, "Edit", [...])` + target 五形态（after anchor/at inserted/deleted/anchor/index 回退）；`Inserted:` 行（cN+id+kind/lang）、`Deleted:` 行、`Shifted:` 行、`Anchor:` 行 + type-change `NOTE` 行；data 加 `insertedCellIndex`/`insertedCellId`。
- `sdks/vscode/src/notebook/env.ts` — 新增 `envSummaryHeader(notebook, operation, status?, dirty=isDirty, runtime=runtimeLabel)`（notebookHeader "Env" + operation/target=notebook/status/dirty/runtime）；info 分支首行替换为 header；configureResult 加 notebook 参数（3 调用点传参）且 summary 用 header；restart 4 分支 + save 2 分支均 header 化。
- `sdks/vscode/src/notebook/output.ts` — serializeNotebookCellOutput 的 summary 用 notebookHeader "Output"（target=cellRef/dirty/runtime/outs/artifacts 计数）+ ArtifactsRoot 行；导入调整（移除 c1/copilotLikeCellId 未用项，加 notebookHeader/cellRef）。
- `sdks/vscode/src/notebook/run.ts` — describeRunTarget 用 cellRef（单 cell / `cN..cN` range，保留 "all"）；runSummaryText 用 notebookHeader "Run"（target/completed/dirty/runtime）+ Cells 列表 + ArtifactsRoot 行。
- `sdks/vscode/src/notebook/source.ts` — strict cellId：`const cell = cellId ? resolveNotebookCell(...) : undefined`（stale id 直接抛错，去掉 try/catch 静默回退）；warning 文案改简短；输出统一 notebookHeader "Source"（target/range/offset/limit/returned/total/truncated/dirty/runtime + Warning 行）；data.target 改 1-based（`cell ${targetCellIndex + 1}`）。
- `sdks/vscode/src/notebook/summary.ts` — notebookSummaryText 用 notebookHeader "Summary"（type/dirty/cells/runtime/status）；Conventions 长行替换为简短 Fields 行（详细规则留在工具描述）。
- `packages/opencode/test/plugin/vscode-notebook-tool-summary.test.ts` — 新行为测试（311 行）：mock vscode 模块（NotebookEdit/WorkspaceEdit/Uri 等稳定 API）+ 6 个 SDK 模块动态导入（`sdkNotebookUrl` 相对路径到 0234 状态源码）；3 个测试：头部不变量（Summary/Source/Edit/Run/Output/Env 六工具 line0=Notebook: path、line1=<Label>: 前缀、Env target=notebook）、source stale id 硬失败、insert 结果 insertedCellId + Inserted 先于 Shifted。

### 目标适配决策
1. **env.ts 导入补充**：source 的 envSummaryHeader 依赖 `quoteForSummary`（util）与 `notebookHeader/runtimeLabel`（format）；目标 env.ts 原先未导入这些 → 补齐两个 import 行。
2. **configureResult 形状差异**：source 是 `summary: [header, summary, guidance, reason]` 数组 join；目标 configureResult 原本单行 `Status: ...` 模板 → 改为 source 的 header 数组结构（保留 guidance/reason 过滤）。
3. **infoNotebook 首行**：目标首行是合并的 `Notebook runtime: ...; Python/Jupyter extensions: ...` 长行；source 是 `Operation: info` 单行被 header 替换 → 在目标合并行前插入 header 行（保留原 runtime 行作为 detail，header 的 runtime 参数用 activeRuntime 探测结果）。
4. **source.ts warning 文案**：目标警告行含 "ALWAYS use GLOBAL document line numbers" 长文本；source 234 改为简短句 → 采用 source 新文案。
5. **summary.ts Fields 行**：source 用 Fields 行替换目标长 Conventions 行（其详细规则在工具描述维护）。
6. **233 遗留 typecheck 缺口修复**（vscode-bridge.test.ts 3 处）：233 新增 3 个测试用 `VscodeBridgePlugin()` 无参调用，但目标 `VscodeBridgePlugin` 签名要求 PluginInput（目标架构差异，source 该签名无参数）→ 补 `{} as unknown as PluginInput`（与文件内既有调用点一致），附中文注释。该修复落在 234 patch（累计状态生效）。
7. **regen 脚本 new-file mode**：regen-fixed.py 的 difflib 输出缺 `new file mode 100644` 标记，git apply --index 报 "does not exist in index" → 脚本增加 new/deleted file mode 头（仅脚本自身，不改 workflow 语义）。
8. **注释语言**：E/C 门禁需要 75 条中文说明注释；SDK 文件与测试文件注释全部以 `// #234：` 前缀分散于关键逻辑（header 不变量、stale id 硬失败、Inserted/Shifted 区分、configure 五态入 header、cN 1-based、#VSC 句柄与 type change、16KB 预算、artifact 根路径等）；测试注释计入 C（用户明确许可）。

## Source 行为意图（逐 hunk 重建）
- format.ts：notebookHeader 是 234 核心——所有工具 summary 前两行固定 `Notebook: <path>` + `<Label>: k=v`，fields 允许 undefined（可选字段不产生空位）；cellRef 是 cN+#VSC 双标识稳定引用；notebookPath 统一 posix。
- edit.ts：insert 完成后立即捕获 insertedCell（cellAt(insertIndex)），Inserted 行与 data.insertedCellIndex/Id 同源；target 行区分 after anchor（type change 后 anchor id 可能失效）与 at inserted；Shifted 行独立命名避免与 Inserted 混淆；Deleted 行用 1-based 索引。
- env.ts：envSummaryHeader 统一 Env 头部，operation/target=notebook/status/dirty/runtime 五字段；info 的 runtime 参数 = activeRuntime 探测结果或 runtimeLabel fallback；configure 五态 status 直接进 header；restart 失败/请求/成功 4 分支全部 header；save 三态（saved/error/not-confirmed/skipped）。
- output.ts：Output 头部 target=cellRef（读哪个 cell 的输出）、dirty、runtime、outs 列表、artifacts 计数；ArtifactsRoot 单行成列。
- run.ts：Run 头部 target=描述（all/cellRef/range cN..cN）、completed、dirty、runtime；Cells 每行 cN id exec outs；Artifacts 按 cell 平铺。
- source.ts：strict cellId——传入 stale/错误 id 必须硬失败（resolveNotebookCell 抛错透传），与 edit/run/output 失败语义一致；窗口收敛到目标 cell 虚拟行，offset 越界重置 + Warning；16KB 字节预算先于行数检查，首行超预算也保留；data.target 1-based。
- summary.ts：Summary 头部 type/dirty/cells/runtime/status（failed 优先、全成功/部分执行三态）；Fields 行简短保留 cN 1-based、id 稳定、range 全局虚拟行三条不变量。
- 新测试：3 个行为断言 = 头部不变量契约、stale id 硬失败、insert 结果 id 优先级；mock vscode 只实现工具用到的稳定 API（NotebookEdit/WorkspaceEdit/Uri/NotebookRange/NotebookCellData/workspace/events 等）。

## 上游（v1.17.20）对比
- 目标 233 状态已有部分基础：resolveNotebookCell 的 normalizeCellId 路径（#105）、16KB 预算、虚拟行号、edit data.deletedCellIndex 等——234 在其上扩展头部契约与 strict cellId，不重复实现。
- 目标 configureResult 无 Operation 行（source 有）；适配采用 source header 结构 + 保留目标 guidance 逻辑。
- 目标 run.ts describeRunTarget 有 source 没有的 "all" 分支（全量执行显式标注）——保留（上游更强实现），cellRef 仅用于非全量目标。
- 目标 edit.ts 的 Anchor 行/type-change NOTE 行为 233 已存在，234 只加注释说明，不重复。

## E/C
- E=494（a=552 d=62 blank=33 comment=87），C=75（#234 前缀中文说明注释，len>12 且含中文），need=75 → OK。
- 注释位置：format.ts（notebookHeader 3+label 1+fields 1+c1 1+copilotLikeCellId 1+cellRef 2+notebookPath 1）、edit.ts（target 1+inserted 2+Deleted 1+Anchor 1+NOTE 1+Inserted 1+Shifted 1）、env.ts（envSummaryHeader 2+operation 1+info 1+configure 1+save 3 态 2+restart 1）、run.ts（cellRef 2+all 1+startCell 2+cell 行 2+artifact 2）、source.ts（stale id 1+range 2+窗口 1+钳制 1+16KB 2+首行 1+分隔 1）、output.ts（头部 2+flatMap 1+根路径 1）、summary.ts（头部 1+status 1+虚拟行 1+cell 行 1）、测试文件（头部不变量 2+stale 2+insert 2+applyEdit 2+fake 1）。

## 安装/类型检查
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 234`：passed 234/234；install=reused，installInputSha256=a4aae9d1b49e6fae9980e4f2364d9c6f5323ea6c5df8b5034b440950fa673d2b（首次 install 失败因网络 ConnectionRefused，第二次网络恢复后 installed 并固化指纹）；typechecks 30/30 全部 passed（packages/app … packages/tui）。
- 期间修复 233 遗留：packages/opencode typecheck 曾报 vscode-bridge.test.ts 3 处 TS2554（VscodeBridgePlugin 缺参）→ 修复后 30/30。

## 行为测试（packages/opencode 与 packages/tui，工作目录 .test-workspace/repo）
| 命令 | 结果 |
|---|---|
| `bun test test/plugin/vscode-notebook-tool-summary.test.ts` | 3 pass / 0 fail（25 expect）|
| `bun test test/plugin/vscode-bridge.test.ts` | 9 pass / 0 fail（50 expect）|
| `bun test preview-diff.test.ts`（packages/tui）| 8 pass / 0 fail（27 expect）|
| `bun test ./test/server/session-list.test.ts --timeout 30000` | 19 pass / 0 fail（48 expect）|

## 失败修订记录
1. **materialize 234 失败（file-not-found）**：新测试文件缺 `new file mode 100644` 头 → 修 regen-fixed.py（new/deleted file mode）→ 重生成通过。
2. **materialize 覆盖 state 修复**：先在 state 修 env.ts 后未 regen 就 materialize → 修复丢失 → 顺序修正：state 修改 → regen → materialize → typecheck。
3. **测试失败 1（header 不变量）**：env info 分支首行仍是旧 `Notebook runtime: ...` 行 → 补 envSummaryHeader 调用；configureResult 缺 notebook 参数/header → 补签名 + 3 调用点 + summary 结构。
4. **测试失败 2（@opencode-ai/core/observability 找不到）**：在 state repo（无 node_modules）跑测试导致 → 改为在 .test-workspace/repo 跑（install 环境）。
5. **session-list 5s 超时（环境性）**：19/19 在 `--timeout 30000` 下全过；5s 默认超时是 Windows 慢 I/O + git 子进程导致（0230 record 已记录同类现象）。
6. **E/C 不足**：从 CN=19 逐轮补到 CN=75（多轮 #234 注释，经 count-ec.py 校验）。

## 一致性说明
- source original 0234（v1.15.3→v1.15.4 目标线）全部 hunk 已重建：env.ts（envSummaryHeader/imports/configureResult/info/restart/save）、edit.ts、format.ts、run.ts、source.ts、summary.ts、output.ts、新测试文件。
- 233 遗留的 VscodeBridgePlugin 缺参修复并入 234 patch（累计状态生效；record 0233 状态为 PASS 但 typecheck 缺口在此闭合）。
- 未删除任何 source 测试或行为；未空化任何 hunk；current patch 非空（47430 bytes）。
- materialized state 0234 可累计 apply（passed 234/234）；recent success states ≤ 5（0228/0229/0231/0232/0233/0234 中 0229 被清理，实际 5 个：0228/0231/0232/0233/0234——0229 由脚本保留策略清理，见 states 列表）。

## DUAL-AUDIT SEAL
批次 231-235 第三轮双独立审计封印（2026-08-06）：
- 正向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 逆向审计（round 3）：5 项全部 PASS，无未关闭 B/G
- 三轮修复验证：G-01（231 record 长度）✓、B-01（233 filetype none 哨兵）✓、B-01 r2（235 gracefulShutdown 3s 总超时）✓
- 测试：daemon 8/8（×2稳定）、filetype 2/2、exit 2/2、notebook 12/12、preview-diff 8/8、session-list 19/19、typecheck 30/30
- install 指纹 a4aae9d1（reused）、source HEAD 未变、integrity 无失败
- BATCH PASS
