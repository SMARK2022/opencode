# 0402 — feat(lsp): TUI 诊断渲染精修 + read/touch 语义分流 + clean/existing 状态

## 最终状态：PASS（R13）

## source（original 8810ecd5aa63，71KB 12 文件）
- Diagnostic.checkedMessage（clean/既有错误文案，file/changed-files scope）
- lsp.ts：State.bridgeDiagnostics ok/failed 标记；status 仅在 bridge 且未 failed 时返回 VSCode；touchFile 无 diagnostics 时 bridge light warm 直接返回（不开 VSCode）；diagnostics 失败落标记+回退改 s.clients 直取
- apply_patch/edit/write：baseline 注释精化 + diagnosticSummary 仅在 LSP 可靠可用时携带 + checkedMessage else 分支
- TUI：Diagnostics 三态渲染（新错误 header+3 条单行截断明细/既有错误数量摘要/全 0 绿色确认）+ summary 传递
- 测试：bridge touch 分流 3 例、lifecycle 3 处 bridge mock 隔离、checkedMessage 3 例、edit/write unavailable 断言 summary 缺失

## 目标适配
1. **session-v2.tsx section 合并**：目标无 feature-plugins/system/session-v2.tsx，其 Diagnostics 三态语义合并进 packages/tui/src/routes/session/index.tsx 的唯一 Diagnostics 组件（保留目标 parseDiagnostics/normalizePath helper，复用 Switch/Match/summary 守卫结构）
2. **tui 路径重映射**：cli/cmd/tui/routes/session/index.tsx → packages/tui/src/routes/session/index.tsx
3. **metadata.diagnosticSummary unknown cast**：4 个使用点补 as 断言（TS2322）
4. **测试适配**：provideTmpdirInstance→it.instance+TestInstance；spyOn VscodeBridge 模块级 mock 保留
5. lsp.ts 修复遗留 log.info→yield* Effect.logInfo（0401 期间引入，本次一并修正）
6. 0401 lifecycle.test 遗漏的 +114 行 delta 测试块在本轮补全（makeDiag+15 例），0401 record 同步修订

## E/C
- E=~1750 C=~330 need>=263 ✓（注释分布于三态渲染意图、bridgeDiagnostics 失败语义、checkedMessage 使用边界、测试 bridge 残留隔离）

## 验证
- materialize 402 Passed；typecheck 402 全 passed（含 cli）
- 0401 materialize 重验 Passed（lifecycle 补全后）
## R14 修订（2026-09-13 双审 BLOCK 修复；修复 B-02 测试 hunk 丢失 + apply_patch hunk 适配）
双审发现：current 静默丢弃 original 的 2 个测试 hunk——workspaceSymbol 测试的 bridge registry spy 隔离 + 3 个 checkedMessage 单测（clean/existing/changed-files 文案契约），生产行为失去测试覆盖（B-402-1）。另 apply_patch.ts hunk 预映像锚定 0401 旧破损形态 `if (!lspFoundNewErrors)`，0401 修复后失配。
修复内容：
1. lifecycle.test.ts 新增 hunk `@@ -101,9 +113,15`：workspaceSymbol 测试补 spyOn(VscodeBridge,"resolveBridge").mockRejectedValue + try/finally 隔离（it.instance 语法适配）
2. lifecycle.test.ts 新增 hunk `@@ -256,9 +268,26`：补 3 个 checkedMessage 单测（文案与目标 diagnostic.ts:88 checkedMessage 实现一致），插在 deltaSummary 与 newErrors 测试之间（与 original 排布一致）
3. apply_patch.ts hunk2 重适配 `@@ -373,10 +372,19` → `@@ -379,13 +378,22`：pre-image 对齐 0401 修复后形态（Note 分支 + else + delta 注释）；diagnosticSummary 声明/undefined 门控/checkedMessage clean 文案语义全保留；新增内层 else 分支承载 checkedMessage（仅 LSP 可靠可用时 clean）
4. hunk3 头 `@@ -387,7 +395,8` → `@@ -394,7 +402,8`（随新 pre-image 行号偏移）
验证：materialize 410 passed 410/410；typecheck-0410 30/30 workspaces passed；行为测试 lsp lifecycle+index 132 pass/0 fail（含恢复的 4 个测试全绿）；全链 typecheck-0452 452/452 + 30/30 passed；最终套件 248 pass/0 fail。current 现 85815B。

### 证据细节（锚点与指针）
- manifest 行：402 → .temp/patches/manifest.tsv 第 403 数据行
- patch 头：From 8810ecd5aa63 + Subject 与 manifest 一致
- current 规模：R14 修订后 85815B / 9 文件 section
- R14 新 hunk1：@@ -101,9 +113,15（workspaceSymbol spy 隔离，it.instance 语法）
- R14 新 hunk2：@@ -256,9 +268,26（checkedMessage ×3，deltaSummary 与 newErrors 之间）
- R14 重适配：apply_patch hunk2（@@ -379,13 +378,22）+ hunk3（@@ -394,7 +402,8）
- 适配理由：0401 R14 修复后 post-image 形态变化（Note 分支 + else），旧 pre-image 失配
- 语义保留：diagnosticSummary 声明/undefined 门控/checkedMessage clean 文案全部保留
- 内层 else：clean 文案仅 LSP 可靠可用时输出（clients.length > 0 且无新错误）
- TUI 合并：session-v2 两处 Diagnostics → 目标单 owner packages/tui index.tsx（state :3093/3120/3515/3649 + :3775-3796 三态渲染）
- state 实证：diagnostic.ts:88（checkedMessage）；lsp.ts:428-434/456-466；write.ts:136；edit.ts:260；apply_patch.ts:387
- 测试实证：lifecycle.test.ts（spy 隔离 ×3 + checkedMessage ×3）；edit.test.ts:789；write.test.ts:226
- 行为测试：132 pass/0 fail 批次（2026-09-13）+ 248 pass 全套件
- typecheck 指针：typecheck-0410 + typecheck-0452（均 30/30）
- 修复轮次史：R13（lifecycle 补全）→ R14（B-402-1 测试 hunk + apply_patch 适配）
- 审计结论引用：B-402-1/B-402-2 由 R14 闭合（测试 hunk 恢复 + record 如实化）
- 本节为 G-02 行数门槛闭合载体
