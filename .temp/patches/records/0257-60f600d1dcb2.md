# Record 0257-60f600d1dcb2 — memento 改从 rawHistory 采集 + hidden 过滤

## Source（original/0257-60f600d1dcb2.patch，SHA d0ceb469 谱系）

1. `collectRecentUserMessages`：
   - 消息级 `if (msg.hidden) continue`；
   - part 级 `if (part.hidden) return []`（撤销/隐藏内容不进 recent-user memento）。
2. `process` 内 memento 采集源改为 raw 全量历史（上游 filterCompacted 只保留可见窗口后，压缩 head 中的原始最近用户指令会永久丢失；source 要求 memento 仍从完整历史中采集并做有界截断）。
3. 测试：
   - 新增 "preserves raw recent user instructions across repeated compactions"：两次压缩后 memento 仍含首次压缩前的 raw 指令、超长指令截断至预算内、尾部不再出现；
   - 原 keeps-synthetic 用例更名 "keeps synthetic, ignored, and hidden..." 并补 hidden 消息整体排除断言、撤销二次更新打 hidden 标记的用例。

## 目标（0256 落地后）不一致与适配

- **锚点漂移**：source 测试 hunk 以 `  )\n\n  itCompaction.instance(` 为 preimage；目标 0256 插入块后该处为 `    { git: true },\n  )\n\n  itCompaction.instance(\n    // #256：synthetic/ignored 文本不得污染 memento——过滤按 part 粒度生效`（源谱系 bounds 测试以 `)]` 收尾而目标为 `)`，且 0256 在 itCompaction 与名行间插了注释）。current T1 已按目标真实结构重写 preimage + 行号 1183。
- 生产两 hunk 语义无漂移：目标 compaction.ts 中 `collectRecentUserMessages`/`process` 与 source 同构，hidden 字段（消息级+part 级）在目标 Schema 中已存在（V2 Part/Info 均有 hidden），直接落位。
- 上游（dev-smark tip）在 `formatRecentUserMemento` 调用侧同为 `visibleHistory` 采集（0256 已同步其可见窗口语义）；source 的 rawHistory 采集是对上游可见窗口方案的补强：可见窗口保证重放侧不回放已压缩内容，rawHistory 保证交接侧不丢失原始指令，两者正交，融合后保留两侧优势（当前 current 与 0256 落地一致：memento 采集用 rawHistory，select 重放用 visibleHistory）。

## current 修改内容（sha256=66263d2f9c22e5ebc100b8743a04d3f6a8ee955e851bacf45918a65ba9f38d72，8100 bytes）

- H1 `collectRecentUserMessages`（compaction.ts）：`msg.info.hidden` continue + `part.hidden` return []。
- H2 `process`（compaction.ts）：memento 采集源 `visibleHistory` → `rawHistory`。
- T1 测试：插入 preserves-raw 用例（两次压缩/截断≤400/尾部不出现）；keeps-synthetic 更名并补 hidden 用例（撤销标记 {time, reason} 结构、二次更新路径、part 级排除）。
- 未删除任何 source hunk；T1 中 source 新用例全部保留（更名+断言扩充按 source 原意）。

## 逐 hunk 映射（original → current）

| original hunk | current hunk | 适配说明 |
|---|---|---|
| H1 `@@ -111,12` msg.info.hidden continue | 同位 | 目标 collectRecentUserMessages 与源同构，直接落位；msg.hidden 与 part.hidden 两级排除均保留 |
| H1 内 `@@ -111` 块 part.hidden return [] | 同位 | hidden part 检查置于 synthetic/ignored 之前——撤销粒度到 part 级 |
| H2 `@@ -450,8` visibleHistory → rawHistory | 同位 | 采集源切换；select 重放侧 visibleHistory 语义不变（与 0256 可见窗口方案互补） |
| T1 新增 preserves-raw 用例 | 保留（锚点重写） | 源 preimage `  )\n\nitCompaction...` 在目标不存在（见下） |
| T2 keeps-synthetic 更名 + hidden 扩充 | 保留 | 更名 "keeps synthetic, ignored, and hidden..." + 撤销标记用例 |

## 修订历程（T1 preimage 三轮修正）

1. 源 preimage 以 `  )` + 空行 + `itCompaction.instance(` 锚定——目标 0256 插入块后该处实际为 `    { git: true },` + `  )`（源谱系 bounds 测试以 `)]` 收尾而目标为 `)`），且 0256 在 itCompaction 与名行间插了 `// #256：synthetic/ignored...` 注释行。
2. 第一轮修正 preimage 结构后仍失败——`--unidiff-zero` 模式下行号必须精确：经 `--keep-failure` staging 逐行比对定位 keeps-synthetic 位于目标 L1183-1188（前序 header 1253/1181/1223 均错位，或错在行号或错在 `)]` vs `)`）。
3. 最终 header `@@ -1183,9 +1183,61` + preimage 五行（锚行/闭括/空行/itCompaction/#256 注释 ctx）+ del 名行 → passed。
4. 注释插入改变 b 计数 → corrupt patch → sync-hunks.ps1 修正 hunk 头后通过。

## E/C

- E = 86（plus 82 + minus 4）；C = 13 ≥ ceil(86×0.15)=13。
- C 分布：H1 撤销/隐藏排除语义 2 条、H2 rawHistory 采集理由 3 条、测试两次压缩边界/预算上限/核心不变量/hidden 结构/二次更新路径/synthetic-ignored-hidden 粒度 8 条。

## 测试与 V1/V2 判定

- compaction 职责在目标与上游 v1.17.20 均由 V1 谱系 `src/session/compaction.ts` 承担；V2 session core 无重叠能力，不存在双 owner 分裂或需要迁移的语义。
- 上游 tip 的 memento 采集同为可见窗口（0256 已同步）；257 的 rawHistory 采集为补强而非冲突：可见窗口保证重放侧不回放已压缩内容，rawHistory 保证交接侧不丢失原始指令，两者正交且同一调用点内并存。
- materialize 257 --rebuild: passed 257/257（修订历程见上）。
- typecheck 257: 全部 workspace passed（install reused 549c6966）。
- 行为测试（.test-workspace@257，`bun test test/session/compaction.test.ts --timeout 120000`）：56 pass / 1 skip / 2 fail。2 fail 均为已记录 pre-existing：`stops quickly when aborted during retry backoff`（0242/0251 记录，Windows 慢盘 timing）；`keeps recent pre-compaction turns across repeated compactions`（0251 记录 0249 基线实测复现，失败点为 filterCompacted 流过滤，255-257 均不触碰该路径）。257 新增 2 用例均通过。
- E/C 复核：C=13 达标。

## 最终行为

- 撤销/隐藏产生的消息与 part 一律不进 recent-user memento；memento 采集改为从 raw 全量历史出发（保留 0256 的 20%/20K 预算与倒序优先级），压缩 head 内的原始最近用户指令在后续压缩的交接中仍有一次有界出现机会；重放侧 visibleHistory 语义不变。
- 批次 255-259 状态：255/256/257 PASS；258/259 预研完成待物化。
