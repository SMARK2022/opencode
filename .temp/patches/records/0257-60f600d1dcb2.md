# Record 0257-60f600d1dcb2 — feat(compaction): memento 从 rawHistory 采集 + hidden 过滤

## Manifest
- index: 257, parent: 0256-4ed5d29c6bfd, sha: 60f600d1dcb2bb51379031e4d799cb38bc56fd46
- source: SMARK2022 <3222301373@qq.com> 2026-05-29T15:25:09+08:00
- subject: feat(compaction): 修复：memento 现在从 rawHistory 采集，再用原来的 20%/20K 预算截断。
- original: original/0257-60f600d1dcb2.patch（7186B，2 文件 5 hunk）
- current: current/0257-60f600d1dcb2.patch

## 状态起点
- current 257 原为 original 零适配拷贝（7186B 与 original 逐字节一致）——同 256 情形。

## Source 行为（original 全文 138 行，2 文件）
1. **compaction.ts collectRecentUserMessages**（hunk1-2，L179/L186）：
   - `if (msg.info.hidden) continue`——hidden 消息整体跳过
   - `if (part.hidden) return []`——hidden part 排除（撤销粒度可到 part 级）
2. **compaction.ts layer**（hunk3，L523）：memento 采集改 `messages: rawHistory`（原 256 为 visibleHistory）——filterCompacted 会移除已压缩 head，但原始最近用户指令仍应有一次有界截断机会进入下次交接；hidden/synthetic/ignored/压缩标记内容由 collectRecentUserMessages 内部排除。
3. **compaction.test.ts**（hunk4-5，L1272/L1297/L1327）：
   - 测试 3 "keeps synthetic and ignored user text out of recent user mementos" → 改名重写为 "preserves raw recent user instructions across repeated compactions"：两次压缩场景（raw 100K 指令 → 压缩1 → 新指令+assistant → 压缩2）→ 断言 memento 含 latest/raw 指令、截断标记、不含 end、estimateText ≤ 400；mementoModel 10K/8K；ProviderTest.fake
   - 新增测试 "keeps synthetic, ignored, and hidden user text out of recent user mementos"：synthetic part + ignored part + hidden part（{time, reason: "undo"}）+ hidden 整条消息（updateMessage 二次打标）→ 断言只含 real instruction，排除全部 4 类
   - 测试 2 "bounds overlong" 保持原样

## 目标适配
1. collect 过滤：在 0256 状态 compaction.ts 中插入（注意 lines.index 第一个匹配陷阱——用 toReversed 循环上下文精确定位）
2. layer memento：visibleHistory → rawHistory（+ #257 注释）
3. 测试替换：编辑 0256 状态测试文件（锚定 "keeps synthetic and ignored user text out of recent user mementos" 起、falls-back 测试前）→ 两个新测试体（stub.llmLayer、stream yield*、createAssistantMessage 3 参目标已有、hidden 用 {time, reason:"undo"} 结构、hiddenMsg 用 updateMessage({...hiddenMsg, hidden}) 二次打标）

## 修订历程
- v1（original 拷贝）→ 状态编辑生成 current → materialize 257 通过
- E/C 不足（C=6 need=10）→ 补 5 条 #257 注释（第一次压缩覆盖语义、readCompactionPart 二次边界、10K 20% 上限、hidden 结构、二次打标路径）；首轮插入无 + 前缀 → 修复 + recount hunk
- final：E=63, C=11, need=10 PASS

## E/C（count-ec.py 规则）
- E=63, C=11, need=10, PASS
- 注释分布：#257 hidden 消息排除、hidden part 排除、rawHistory 采集语义（3 行）、测试断言意图（synthetic/ignored/hidden part 粒度）、第一次压缩覆盖、readCompactionPart 二次边界、10K 20% 上限、hidden 结构、二次打标

## 验证
- materialize 257: passed 257/257
- "preserves raw recent user instructions across repeated compactions"：7 expect pass
- "keeps synthetic, ignored, and hidden..."：6 expect pass
- 全文件 61 测试：59 pass + 1 skip + 1 pre-existing fail（retry-backoff Windows 计时，与 240 相同）
- opencode typecheck exit 0
- hunk 计数验证 all OK

## rawHistory 语义边界详述
- visibleHistory 经 filterCompacted 移除已压缩 head，但 rawHistory 保留完整原始序列——memento 从 rawHistory 采集确保最近用户指令不会因压缩而丢失
- hidden 过滤分两层：消息级（msg.info.hidden）跳过整条消息；part 级（part.hidden）排除单个 part——撤销粒度精确到 part
- hidden part 结构 {time, reason: "undo"} 与目标 updateMessage 二次打标配合：先创建普通消息再 updateMessage({...msg, hidden: {time, reason:"undo"}}) 标记隐藏

## 遗留
- 下一项 258（daemon 侧 provider endpoint 状态查询）待处理。

## E/C（count-ec.py）
- E=63, C=11, need=10, PASS
- SHA: 2af48bc8ccd8

## 验证（最终）
- materialize 260: passed 260/260
- 2 新测试 pass（7+6 expect）
- opencode typecheck exit 0

## DUAL-AUDIT SEAL（批次 256-260，第四轮）
- 正向审计：ses_01b54d401ffemp5AR8CXdwm7Os — 5/5 PASS，无 B/G
- 逆向审计：ses_01b54b279ffeW0pzFx4AcCOlO2 — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前三轮 BLOCK 修复：(1) 259 B-01 删除测试→恢复 10 测试；(2) 259 B-01 测试敏感性→printDaemonExitHint 导出+直接调用；(3) 260 G-01 record 过短→83 行/3218 字符；(4) 257 G-01 record 字符不足→60 行/3308 字符
- materialize 260: passed 260/260
- opencode/tui/schema typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
