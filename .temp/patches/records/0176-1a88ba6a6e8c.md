# Record 0176-1a88ba6a6e8c

- Index: 176
- Source SHA12: 1a88ba6a6e8c
- Batch: 176-180
- Updated: 2026-07-25T21:08:03Z
- Verdict: **PASS** (dual independent audit sealed) (dual auditor not yet stamped)

## 1. Source intent (immutable original)

Original `original/0176-1a88ba6a6e8c.patch` (~156KB, 9 paths) is not a cosmetic rename. It:

1. Moves stats modules under `stats/{charts,data,insights,render}.ts` **and** expands the data contract.
2. Adds **detail filters** on the stats CLI: `session|model|provider|source|agent|status|tool`.
3. Adds **`StatsBreakdownCommand`** (`breakdown [dimension]` / `by`) with dimension set `model|provider|agent|source|project|tool|status`.
4. Enriches `StatsReport` with agents/sources/statuses/projects series, rich `ToolUsage` (`count|inputChars|outputChars|contextTokens`), and tool-context attribution helpers (`toolCharsByName`, `toolEventsFromAssistant`).
5. Implements **CJK display width** (`isWide`/`charWidth`/`visibleLength`/`truncateVisible`) plus width tests.
6. Adds session-list footer pointing users to `opencode stats sessions`.
7. Ignores scratch dirs (including `.temp/`) in `.gitignore`.

## 2. Upstream / tip@175 baseline (v1.17.20 + sealed 171-175)

Tip already has a **stronger multi-command Effect stats surface** than early SMARK trees:

- Flat owners: `stats.ts`, `stats-data.ts`, `stats-render.ts`, `stats-charts.ts`, `stats-insights.ts`.
- Commands: overview/models/providers/timeline/sessions/dashboard/charts/heatmap/costs/tokens/insights/forecast.
- Aggregation via `Database.Service` + `RequestUsageTable` / `RequestUsageAssistantTable`, not a second ad-hoc DB client.
- Compact #174 charts/render (heatmap matrix, rank bars, etc.).

Therefore path rename to `stats/` is **optional**; behavior is mandatory on flat owners.

## 3. First divergence and primary-path repair

| Source behavior | Tip@175 gap | Tip repair owner |
|---|---|---|
| Detail filters | `StatsFilter` only days/project | `stats.ts` options + `loadReport` → `stats-data` filters |
| Breakdown command | absent | `StatsBreakdownCommand` + `renderBreakdown` |
| Rich tool usage + context tokens | `toolUsage: {id,count}` only | `ToolUsage`, `toolEvents`, attribution helpers |
| Agent/source/status/project groups | model/provider only | extra `UsageGroup`/`UsageSeries` maps in `aggregateStats` |
| CJK width | ANSI-strip length only | `stats-charts.ts` width primitives + `truncateVisible` by columns |
| Session footer | absent | `session.ts` list footer |
| `.temp/` gitignore | absent | root `.gitignore` |

No second parser / fallback success path was introduced. Filters and attribution stay on the single `aggregateStats` write path.

## 4. Current patch summary

- Path: `.temp/patches/current/0176-1a88ba6a6e8c.patch`
- Bytes: 37147; SHA-256: `e5fd4e20109fc5e7e47be75c0876cf7029df8ba8d14fef580615ebc51cffec5a`
- Independent E/C: E=532, C=83, need=80, gate=PASS
- Files: `.gitignore`, `session.ts`, `stats.ts`, `stats-data.ts`, `stats-render.ts`, `stats-charts.ts`, `test/cli/stats-render-width.test.ts`

### Important implementation notes

- Tool context tokens on tip lack SMARK `inputBreakdown`; attribution uses assistant `tokens` pools weighted by tool IO char shares (same formula structure as source when breakdown absent).
- `RequestUsageTable` already has `source|agent|status`; events are back-filled for breakdown filters.
- `truncateVisible` cuts by **display columns**, reserving one column for `…` (fixes string-length truncation bug).

## 5. Verification

- `bun .temp/patches/src/apply-cumulative.ts --materialize 180` → passed 180/180 (includes 176).
- Typecheck batch-end report `typecheck-0180-2026-07-25T21-04-49-902Z.md`: install reused `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`; all listed workspaces passed.
- Focused tests in `.test-workspace/packages/opencode`:
  - `test/cli/stats-render-width.test.ts` + `test/tool/truncation.test.ts` → **23 pass / 0 fail**.

## 6. Inconsistencies resolved during rebuild

- Earlier residual-only #176 (CJK/footer only) failed dual audit (semantic omission). Full filter/breakdown/tool-context port restored.
- Comment injection must not eat identifiers; helpers placed after `normalizeTokens` to avoid TDZ.
- Width test uses `as unknown as StatsReport` for fixture partials.

## 7. Deferred / non-goals

- Exact `stats/` directory layout not forced.
- Full original insights multi-card gallery not re-copied where tip #174 insights already own forecast/concentration; breakdown + filters + tool context are the missing SMARK product surface.

## 8. Post-BLOCK repair (2026-07-25T21:38:14Z)

- Restored `componentsFromStep` / `componentsFromAssistant` on tip `step-finish.inputBreakdown` (schema+processor present).
- `toolEventsFromAssistant` now allocates `contextTokens` from `components.toolCalls` / `components.toolResults` weighted by tool IO chars (SMARK formula), not whole assistant token pools.
- CJK `truncateVisible` uses display columns; width tests pass (24/0 with truncation).
- Materialize/typecheck@180 green after rebuild.

## LIVE IDENTITY (authoritative post-repair)

- Updated: 2026-07-25T21:52:12Z
- Current path: `.temp/patches/current/0176-1a88ba6a6e8c.patch`
- Bytes: 44254
- SHA-256: `f268a7bcf912ba9f506913712b14e11e8e61393640fb0817702edff5f20e423a`
- Independent E=624, C=97, need=94, gate=PASS
- Batch typecheck report: `typecheck-0180-2026-07-25T21-50-38-944Z.md`
- Install fingerprint: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Tip state: `states/0180-201e910402a2`
- Any earlier SHA/bytes/E/C lines in this record are superseded by this block.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-25T22:01:23Z
- Batch: 176-180
- FORWARD auditor: PASS on 176,177,178,179,180 — no open B/G
- REVERSE auditor: PASS on 180,179,178,177,176 — no open B/G
- Tip: states/0180-201e910402a2
- Typecheck: typecheck-0180-2026-07-25T21-50-38-944Z (all workspaces passed)
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Materialize: materialize-0180-2026-07-25T21-50-16-624Z (reusedPrefix 175, applied 176-180)
- Verdict: **PASS** (independent dual audit)

