# 0249-75a4cc2bbb4a record — feat(render): 移除面板背景色 ANSI 代码

## Manifest
- index 249, sha `75a4cc2bbb4ae8dc10899a34449440ff00438764`, parent `425c3b9b73f9` (0248)
- subject: feat(render): 移除面板背景色 ANSI 代码，确保在终端中输出更协调的颜色 test(stats-render-width): 添加回归测试

## Original 身份
- original/0249-75a4cc2bbb4a.patch: 7923 字节, 3 files（stats/charts.ts、stats/render.ts、stats-render-width.test.ts）

## Source 行为（逐 hunk）
1. **stats/charts.ts**：删 `RESET = "\x1b[0m"`，新增 `BACKGROUND_RESET = "\x1b[49m"` + 中文注释；删 `bg(r,g,b)` 函数；`ChartColor` 联合类型删 `"panel"`；`palette` 删 `panel: bg(19,6,24)`；`renderPanel` 用 BACKGROUND_RESET 包裹行。
2. **stats/render.ts**：同款——删 RESET、新增 BACKGROUND_RESET、删 bg()、theme 删 bg、panel 函数改用 49m。
3. **测试**：`concreteBackgroundAnsi` helper（解析 SGR 参数检测具体背景色 40-47/100-107/48;2/48;5）+ 新测试 `never emits terminal background ANSI`（5 个 renderer color:always 无具体背景色 + 每行以 49m 起止；never 模式无 49m）。

## 目标适配（Round 1）
- **生产代码 hunk 全部无对应行**：#174 重构已将 stats 扁平化为 `stats-charts.ts`/`stats-render.ts`/`stats-insights.ts`，目标渲染**从不输出背景色 ANSI**（无 bg()/palette.panel/RESET 三连；renderPanel 为纯文本边框；颜色仅 paint 前景色）。source 的"移除背景色"语义在目标已天然满足（甚至更强——连 49m 都不输出）。无生产改动。
- **测试保留**：回归价值保留——目标版断言：
  - import renderSessions/renderTimeline（stats-render）+ renderInsights（stats-insights，平铺路径）
  - concreteBackgroundAnsi helper 照搬（source 语义：解析完整 SGR 参数列表，只允许 49m）
  - 断言改为 `concreteBackgroundAnsi(output) == []`（去掉"每行以 49m 起止"——目标不输出 49m，该断言在目标恒假）
  - never 模式断言 `not.toContain("\x1b[49m")` 保留
  - withColumns 是 #181 describe 的局部 helper → 顶层测试内联临时改 process.stdout.columns（try/finally 恢复）
  - 测试内联构造最小 StatsReport（目标测试无全局 report() helper，复用 #183 测试的结构）

## 生成方式
- 首次 gen-249.py（import 适配 + helper + 测试），后经两轮修正（withColumns 作用域、renderSessions import）后从 248 状态 git diff 重新生成。
- current/0249-75a4cc2bbb4a.patch: 3990 字节, 1 entry, SHA-256: de2dbf02750b6090605e5b142dab92794b7109f8b70e0205c5e9305e28104ad3

## 验证
- apply-cumulative --materialize 249: passed 249/249
- stats-render-width.test.ts: 15/15 pass（含新测试 7 expect）

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0249-75a4cc2bbb4a

## E/C 最终（count-ec.py）
- E=43, C=7, need=7, PASS（#249 注释：concreteBackgroundAnsi SGR 解析 2 + 五渲染器覆盖 + never 模式零 SGR + withColumns 内联 + import 说明）

## 验证（最终）
- apply-cumulative --materialize 250: passed 250/250（累计）
- stats-render-width.test.ts 15 pass（含新测试）
- opencode typecheck 通过
