# Record 0249-75a4cc2bbb4a

## Status
PASS

## 修改内容
feat(render): 移除面板背景色 ANSI 代码，确保在终端中输出更协调的颜色；test(stats-render-width) 添加回归测试，确保输出不包含终端背景 ANSI。

### Source 逐 hunk 行为
- src hunks（charts/render）：删 RESET→BACKGROUND_RESET(49m)、删 truecolor bg 助手、palette 删 panel 深紫背景、renderPanel/panel() 行首行尾 49m 包装。
- 测试 hunks：concreteBackgroundAnsi 解析器（SGR 参数表含组合序列处理）+ never emits terminal background ANSI 回归（5 条渲染路径 color:"always" 无背景 ANSI、49m 首尾包装、color:"never" 无 49m）。

### 目标适配关键判定
- 0244 树 stats 渲染链（#174-#183 适配谱系）已重写为 box-drawing 面板 + 8 色 fg-only ANSI：全链无背景色发射点（grep 48;2/48;5 仅 cli/ui.ts wordmark，超出 stats 范围）——**src 删除语义在目标已天然满足，无可删对象**。
- 强行套用会破坏 paint() 的 fg+RESET 配对。语义等价物 = 回归防护测试。
- 49m 首尾包装断言与 color:"always"/"never" 差异断言编码的是原谱系实现细节，与目标行为矛盾（目标不发任何 49m）——降级为「无具体背景 ANSI + 不含 49m」双断言，保留测试的可观察意图。

### 测试适配
- source 宿主与 stats-render-width.test.ts 在目标为 CRLF blob（git apply 无法匹配，同 247/248 因）→ 新建 `packages/opencode/test/cli/stats-background-ansi.test.ts`（LF）。
- 覆盖 5 条渲染路径（dashboard/breakdown by:tool/sessions/timeline/insights）color:"always" 下 concreteBackgroundAnsi 为空且不含 49m。
- concreteBackgroundAnsi 解析逻辑原样保留（组合 SGR 拆参、48;2/48;5 命中、38 前景跳过）。

## E/C
- E = 54（plus - commentPlus + minus）
- C = 9（#249：fixture 边界 2、SGR 解析 3、五路径覆盖意图 2、无 49m 契约 1、参数可选性 1）
- need = ceil(54 * 0.15) = 9 → 余 0 PASS

## Source
- original/0249-75a4cc2bbb4a.patch: 161 行 3 文件。

## Current
- current/0249-75a4cc2bbb4a.patch: 4075 bytes, 1 节（新建测试文件）
- SHA-256: 6ac1c4eee5f833be204369712cda1c80052a89f91e5ea684479591e43bbfc440

## 安装
- materialize 249 --rebuild: passed 249/249

## 测试
- typecheck 249: 全部 workspace passed
- `bun test --timeout 120000 test/cli/stats-background-ansi.test.ts`（packages/opencode）: 连续 3 轮全 pass（首次间歇失败为 bunfig 全局 hook 超默认 5s，非断言问题；加 --timeout 后稳定）

## 修订历史
1. 初版：C=4 → 补至 7 → 9（编辑后统一 hunk sync）。
2. materialize EPERM/EBUSY 链：states/0244 目录被常驻 watcher 持久锁定，publish 搬移/恢复/删除全部连锁失败 → 脚本精准适配：eviction 失败（非同 index）跳过不阻塞发布、catch 恢复路径加退避+copy+rm 容错、backup 清理容忍失败。
3. 间歇测试失败定位：bunfig 全局 hook 默认 5s 超时（慢盘）——--timeout 120000 后 3 轮稳定。

## 三方比较
- Source：删除 stats 面板背景色 + 回归测试锁定无背景 ANSI。
- Upstream v1.17.20：stats 渲染已重构（无背景色、无 49m）。
- Target：src 语义天然满足（记录论证），测试重建为 5 路径回归防护。无行为丢失（无 src 可删对象）、测试意图完整保留。

## 最终行为
- stats 五条渲染路径（dashboard/breakdown/sessions/timeline/insights）在 color:"always" 下保证不发射具体背景 ANSI（40-47/100-107/48;2/48;5）且不依赖 49m 重置。
- 回归测试锁定该性质，防止未来渲染路径重新拼入背景色。

## 批次双审结论（2026-09-06，245-249）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：N-02 concreteBackgroundAnsi 的 38;5 前景跳步 +=3 与 source 的 +=2 有偏差（当前 stats 链不可达，下次触碰对齐）；record「Upstream 重构」措辞应为「目标 #174-#183 迁移谱系成果」。