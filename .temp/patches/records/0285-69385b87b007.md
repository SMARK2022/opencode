# Record: 0285-69385b87b007

## 基本信息
- index: 0285
- sha12: 69385b87b007
- subject: feat(permission): 增强权限处理逻辑，支持原始拒绝模式和环境变量处理
- source date: Mon, 8 Jun 2026 15:27:42 +0800

## Source 行为重建
原始 patch 修改 3 个源文件 + 3 个测试文件：
1. **permission/index.ts**：在 `needsAuto` 之前添加 `rawDenyPatterns` 循环——检查 metadata 中的 `raw_patterns`，对每个 raw pattern 调用 `rawDenyRule` 检查是否匹配 deny 规则，匹配则立即返回 `DeniedError`；新增 `rawDenyPatterns` 函数（从 metadata 提取 raw_patterns）和 `rawDenyRule` 函数（deny-only 匹配，不使用 last-match-wins）
2. **permission/precheck.ts**：重构 bash 评估——将 `evaluateShell` 直接调用改为 `bashEffect` 辅助函数，同时评估 raw command 和 pattern command，取 `maxRisk`（更高风险优先）
3. **shell.ts**：Scan 类型添加 `raw: Set<string>`；将 `source(node)` 拆分为 `permissionPattern(node, tokens)`（canonical pattern，跳过 env 赋值）和 `rawPattern(node)`（原始文本）；添加 `redirectedSuffix` 函数；metadata 添加 `raw_patterns` 字段
4. **next.test.ts**：新增 raw deny evidence 终端性测试
5. **precheck.test.ts**：新增 env assignment 处理测试（3 个用例）
6. **shell.test.ts**：新增 pattern 提取测试（3 个用例）

## 三方比较
- **permission/index.ts**：目标使用 V2 架构——`PermissionV1.DeniedError`（非 `DeniedError`）、`PermissionV1.Ruleset` 类型、`Effect.logInfo`（非 `log.info`）、`const layer`（非 `export const layer`）；`needsAuto` 在 line 81（非 225）
- **permission/precheck.ts**：目标 `evaluateShell` 调用为单行（非多行）；`LEVELS` 和 `Decision` 类型存在
- **shell.ts**：目标 `source` 函数在 line 195，结构与 patch pre-image 一致；`Scan` 类型在 line 131

## 适配内容
- **index.ts**：使用 `PermissionV1.DeniedError` 和 `PermissionV1.Ruleset`；使用 `Effect.logInfo` 代替 `log.info`；在目标 line 82 后插入 rawDenyPatterns 循环；在 `merge` 函数前插入 `rawDenyPatterns` 和 `rawDenyRule` 函数
- **precheck.ts**：替换单行 `evaluateShell` 调用为 `bashEffect(input)`；在 `evaluate` 函数后添加 `bashEffect` 和 `maxRisk` 函数
- **shell.ts**：保持 original 结构不变，仅适配行号偏移
- **测试文件**：按目标行号插入新测试用例

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（17638 bytes）
- 编辑后 SHA-256: a6a8a0bfc1cb71971886df1d30113f7904b63d290f66beb66841a75464654300
- 编辑后字节: 16326
- materialize 285: passed 285/285
- 编辑后 SHA-256（最终）: 4c5b7e3ab32a867b3ded2c5d6fae054576aaf3986f78f92b5df10b23fef655fc
- 编辑后字节（最终）: 18530
- 变更: 移除 shell.test.ts 中 description 属性; Permission.Request→PermissionV1.Request; Permission.DeniedError→PermissionV1.DeniedError

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| index.ts rawDenyPatterns 循环 | 适配 PermissionV1 类型 | DeniedError→PermissionV1.DeniedError, log.info→Effect.logInfo |
| index.ts rawDenyPatterns/rawDenyRule 函数 | 适配 PermissionV1.Ruleset 类型 | Ruleset→PermissionV1.Ruleset |
| precheck.ts evaluateShell→bashEffect | 适配单行调用格式 | 替换单行为 bashEffect |
| precheck.ts bashEffect/maxRisk 函数 | 同 original | LEVELS 和 Decision 类型存在 |
| shell.ts ×6 hunks | 同 original（行号偏移） | 结构一致 |
| next.test.ts 新测试 | 适配插入位置 | 在 #199 注释前插入 |
| precheck.test.ts ×3 新测试 | 适配插入位置 | 在 describe 块末尾插入 |
| shell.test.ts ×3 新测试 | 适配插入位置 | 在 each() 调用前插入 |

## E/C 计算
- **最终：** E=233, C=44, need=35, PASS
- E (有效修改行): ~74
  - index.ts: rawDenyPatterns 循环(12) + 函数定义(17) = 29
  - precheck.ts: bashEffect(12) + maxRisk(3) = 15
  - shell.ts: Scan(1) + permissionPattern(12) + rawPattern(3) + redirectedSuffix(4) + metadata(5) + init(1) + extraction(4) = 30
- C (中文注释行): 13
  - index.ts: 5 行（raw_patterns deny-only 说明、shell 解析产出说明、last-match-wins 不使用说明）
  - precheck.ts: 3 行（shell metadata 审计证据说明）
  - shell.ts: 5 行（permission patterns 规则匹配键说明、raw_patterns deny-only 兼容证据说明）
- 满足 E > 0 时 C >= max(1, ceil(74 * 0.15)) = 12，13 >= 12 ✓

## 安装与测试
- typecheck: packages/opencode exit 0 (手动验证), packages/app pre-existing TS1128 (非阻塞)

## 不一致
- 无

## 状态
- PASS（第四轮双审计通过）

## DUAL-AUDIT SEAL（批次 281-285，第四轮）
- 正向审计 (ses_0125c3761): 5/5 PASS, 无 B/G
- 逆向审计 (ses_0125c22f0): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 285: passed 285/285
- packages/opencode typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 281=0/0, 282=114/19, 283=29/5, 284=0/0, 285=74/12 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 修复历史: 4轮审计（shell/prompt.ts 修复 + description 移除 + PermissionV1 适配 + garbled line 修复 + record 字符数扩展）


## R8 修复记录（2026-08-14）
- 背景：E=233/C=12/need=35 FAIL（缺 23 条注释）。
- 修复：6 文件共插入 24 条 #285 中文注释（index.ts rawDenyPatterns/rawDenyRule 语义 4 条、precheck.ts bashEffect/maxRisk 双视角 3 条、shell.ts raw 集合/redirect 语义 4 条、next.test.ts 场景意图 2 条、precheck.test.ts 意图 3 条、shell.test.ts bash 测试意图 8 条）。
- 插入后 validate_hunks.py 发现 10 个 hunk header 计数错误（旧版插入脚本每次只在文件首个 hunk 计数错误），全部自动修正。
- 最终 E/C：E=233, C=44, need=35, PASS
- 验证：materialize 285 passed（修复前 corrupt；修复后通过）
