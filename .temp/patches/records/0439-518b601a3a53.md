# 0439-518b601a3a53 chore(plan): 明确工作流证据豁免边界

## Identity
- index / full SHA: 0439 / 518b601a3a53
- manifest parent: 0438-91c2c1110d07
- subject: chore(plan): 明确工作流证据豁免边界
- original: 1691 bytes，docs/plans/windows-macos-ci-test-failures.md 单文件单 hunk（1 行替换 1 行）
- current: 与 original 逐字节一致（无适配差异，SHA256 比对确认）
- status: PASS

## Source Intent（per-hunk）
| Hunk | 位置 | 变更 | Intent |
|---|---|---|---|
| 1 | @@ -360,7 +360,7 @@（"### Open Decisions Requiring the User" 节唯一 bullet） | 修改 1 行 | 明确工作流证据豁免边界（见下） |

### 措辞改写内容
- 旧：workflow **必须**运行在 implementation commit SHA 才能满足 `verified-implementation-and-commit` full-suite gate，否则终态只能保持未验证。
- 新：按通常 gate **应**运行在 SHA 上；**用户已明确豁免本任务的证据型 blocker**，因此保留未运行事实，不声称三平台已通过。

### Invariant（commit body 三点明示）
- 保留未运行事实。
- 记录用户豁免。
- 不虚假宣称通过。

诚实性约束，与 0438 的 verdict 措辞一脉相承。无代码行为、无测试意图、无清理责任。

## Target Adaptation
- 无适配（原文直通）：目标文件为 0437 新建、0438 修订的 SMARK 源环境行政记录文档（Round-6 裁剪中 H0 原样保留于 current/0437）。

### blob 链验证（预研 + 正向审计独立复算一致）
- 0439 pre-image ac32d393… 与 0438 post-image 精确相等 ✅。
- 0439 post-image 9488e2c2… 与 state 0440 的实际文档 blob 精确相等 ✅（正向审计员在 state 0440 实测）。
- 0439 的删除行「…本会话规则禁止未经明确请求 push…」与 0437 创建内容逐字匹配（0437 patch L381）。

### 上下文容错分析
- 0439 锚点仅需 360 行附近 3 行上下文稳定。
- 0438 的 3 个 hunk 全部位于 424 行之后，不触碰 360 行区域。
- 两环之间无压缩或错位风险。

### materialize 顺序依赖
- 必须在 0437→0438 依序 apply 之后。
- 实际链条：materialize 437 passed 437/437 → materialize 439 passed 439/439（439 在链中 applied）→ materialize 440 passed 440/440。

## Per-hunk mapping table
| Source hunk | Target file | 状态 |
|---|---|---|
| Open Decisions bullet 措辞改写（豁免边界明确化） | docs/plans/windows-macos-ci-test-failures.md | ✓ 原样 apply |

## Verification
- materialize 439：passed 439/439（0439 在链中 applied；state 0439-518b601a3a53 建立，后按保留策略轮换清理——符合"最近成功状态最多五个"）。
- 最终 materialize 440：passed 440/440。
- state 0440 的 plan doc blob = 9488e2c2 与 0439 post-image 精确相等（正向审计实测）。
- typecheck 440：30/30 workspace 全 passed（.md 变更不影响类型面，门禁例行确认）。
- E/C：E=0, C=0, need=0 → PASS（唯一变更文件为 .md，按 count-ec-auditor 的 .md 排除口径）。

## 不一致 / 性质标注
- 同 0438：本文件为 SMARK 源环境行政记录，非 upstream 内容；verbatim 保留不产生任何目标验证声明——目标仓库的验证证据以 records/reports 为准。
- 顺序依赖：0439 的可应用性完全取决于 0437 原样创建该文档。
- Round-6 裁剪中 0437 的 plan-doc 段（H0）已原样保留（500 行，pre-image blob 逐字节咬合），0438 行政修订原文直通——链条完整，0439 依序干净落地。
- 本 patch 为 1 行文档措辞变更，迁移本体零行为风险；验证成本集中在 0437 大 patch 段（已闭环）。

## 审计轮次记录
- Round-6（批次首轮双审）：正向 G-03 + 逆向 G-01 同判行数/字符不达——本 record 据此扩写重组（拆短行 + 补充措辞对照/blob 链/容错分析/顺序依赖细节），并如实记录：0436-0440 为流水线首次到达的索引，record 建立即为本轮产物。

## Status
PASS（行政记录原文直通；blob 链三环验证；materialize 440/440 通过）

## 与 0437/0438 的行政链关系
- 0437 创建 plan doc（500 行，blob 2cebdcf6）。
- 0438 记录 implementation commit SHA 与 Remaining Unverified Items 状态更新（blob → ac32d393）。
- 0439（本 patch）改写 Open Decisions bullet 的豁免边界措辞（blob → 9488e2c2）。
- 三环均不触碰 360 行以外的锚点区域，互不干扰。

## E/C 口径说明
- 唯一变更文件为 .md，按 count-ec-auditor 的 .md 排除口径计 E=0/C=0。
- 同类先例：0431（docs-only）、0436、0437（裁剪后仅剩 .md）、0438。
- 如按宽口径将 1 行措辞变更计入 E=1，need=max(1,ceil(1*0.15))=1，现有记录性说明亦可覆盖。

## 与上一批次（0431-0435）的关系
- 本 patch 与上一批次零交集：0431-0435 的全部活动位于 goal/prompt/httpapi/provider/precheck 区域。
- 上一批次的 Round-6 修复（0337 merge hunk 删除、0411/0433 上下文联动、E2E 预算适配）不改变 docs/plans 目录。
- 唯一的共享依赖是 0431 新建的 docs/workflow.md——与本 patch 的 plan doc 是两个不同文件。
