# Record 0268-0baf112723c2 — feat(network-proxy): HttpClient seam + infrastructureHttpClientLayer

## Manifest
- index: 268, parent: 0267-f9a6e97e3c2b, sha: 0baf112723c2662b73b2d4e6edc9ce79ee381b32
- original: original/0268-0baf112723c2.patch（8722B，184 行，3 文件）
- current: current/0268-0baf112723c2.patch（7428B，SHA 4c4bc9d8fa02）

## Source 行为（original 全文 184 行，3 文件）
1. **models.ts**（3 hunk）：
   - Hunk 1: 移除 FetchHttpClient import，保留 HttpClient/HttpClientRequest
   - Hunk 2: layer 类型加 HttpClient.HttpClient，添加 `const http = HttpClient.filterStatusOk(yield* HttpClient.HttpClient)`
   - Hunk 3: fetchApi 从 NetworkProxy.routedFetch 改为 `http.execute(HttpClientRequest.get(...))`；defaultLayer 添加 `Layer.provide(NetworkProxy.infrastructureHttpClientLayer)`
2. **network-proxy.ts**（2 hunk）：
   - Hunk 1: 添加 Effect/HttpClient/HttpClientError/HttpClientRequest/HttpClientResponse imports
   - Hunk 2: 新增 `infrastructureHttpClientLayer`：HttpClient.layerMergedContext 桥接 Effect HttpClient 到 NetworkProxy fetch
3. **network-proxy.test.ts**（2 hunk）：
   - Hunk 1: 添加 Effect/HttpClient imports
   - Hunk 2: 新增测试验证 Effect HTTP 调用不走 globalThis.fetch mocks

## 上游 owner 分析
- 目标 v1.17.20 将 models.ts 重命名为 models-dev.ts（路径迁移）
- 目标用 FSUtil.Service 替代 AppFileSystem.Service
- 目标用 makeGlobalNode 替代 Layer.effect + defaultLayer（LayerNode pattern）
- 目标用 EventV2.Service（source 无）
- 目标有 #71/#150 注释（source 有 [local-smark] 注释）
- 目标 #150 注释明确禁止 HttpClient（"禁止回退 globalThis.fetch / HttpClient"）
- 目标 network-proxy.ts 有 #83 注释（source 无）

## 目标适配
1. models-dev.ts（路径迁移 models.ts -> models-dev.ts）：
   - 添加 `import { HttpClient, HttpClientRequest } from "effect/unstable/http"`
   - 更新 #150 注释为 #268（反映 HttpClient seam 的引入）
   - 添加 `const http = HttpClient.filterStatusOk(yield* HttpClient.HttpClient)`
   - 替换 routedFetch 为 `http.execute(HttpClientRequest.get(...))`
   - node 定义改为 `layer.pipe(Layer.provide(NetworkProxy.infrastructureHttpClientLayer))`
2. network-proxy.ts：
   - 添加 imports（Effect/HttpClient/HttpClientError/HttpClientRequest/HttpClientResponse）
   - 添加 infrastructureHttpClientLayer 函数（#268 注释）
3. test：适配目标 test 结构（originalFetch.preconnect pattern 已有）

## Hunk 映射
- models-dev.ts: 3 hunk 全适配（路径迁移 + API 适配 + #150 注释更新）
- network-proxy.ts: 2 hunk 全适配（imports + infrastructureHttpClientLayer）
- test: 2 hunk 适配（imports + 新测试）

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- opencode typecheck exit 0, core typecheck exit 0

## E/C 计算细节
- added non-blank: 76, Chinese comments: 10
- E = 66, need = 10, C = 10 PASS

## 修订历程
- v1: original 拷贝 -> models.ts 路径不存在 + network-proxy.ts context 不匹配
- v2: 文件编辑方式（models-dev.ts 路径迁移 + network-proxy.ts context 适配）
- v3: 修复 infrastructureHttpClientLayerNode（Layer 不是 Node）-> layer.pipe(Layer.provide)
- final: typecheck exit 0

## 验证（完整）
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0

## 不一致性分析
- 目标 #150 注释禁止 HttpClient，但 source 268 引入 HttpClient seam
- 适配方式：更新 #150 注释为 #268，说明 HttpClient seam 用于测试注入，生产路径仍由 NetworkProxy 提供
- 目标的 makeGlobalNode pattern 与 source 的 defaultLayer pattern 不同
- 适配方式：使用 layer.pipe(Layer.provide(infrastructureHttpClientLayer)) 而非 defaultLayer

## DUAL-AUDIT SEAL（批次 266-270）
## DUAL-AUDIT SEAL（批次 266-270，第四轮）
- 正向审计 (ses_013f2f0a1ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013f2cbaeffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 266=0/0, 267=6/2, 268=66/10, 269=60/12, 270=38/6 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
