# Record 0105-e8675ce41599

## Source intent
#105 强化 notebook 工具契约与 edit 鲁棒性：
1. **descriptions**：summary/edit 文案——ID 在 session 内稳定；type change 后读 edit 响应新 id；edit 响应含 shifted 邻居。
2. **format.normalizeCellId**：兼容 `#VSC-`/`VSC-`/`#V-`/`vscode-`/裸 8 hex。
3. **resolve**：用 normalizeCellId；not found 提示 type change。
4. **source**：cellId 走 `resolveNotebookCell`（共享 normalize），失败则 undefined。
5. **summary**：Conventions 文案改为 session 稳定 + type change 可能换 id。
6. **edit**：
   - cellId 失败 + 有 oldCode → 扫描候选 **Did you mean**（不自动改写）
   - match 多命中（exact/border/line-trimmed）硬失败
   - 非 exact 策略 **indent migrate**
   - type change 保留 metadata；compact 摘要 NOTE 新 id

## 上游 / tip@104 设计
- tip 已有完整 notebook 桥（#84–#101）：TOP/BOTTOM insert、type=cell range 门禁、matchAndReplace 三阶、compactEditResult、handleTypeChange。
- **缺** normalizeCellId、Did-you-mean、indent migrate、line-trimmed 多命中、metadata 保留、描述/Conventions 更新。
- 上游更强点：applyNotebookEditAndWait、TextEdit 路径、type/range run——全部保留。

## 三方比较
| 语义 | source | tip | 决策 |
|------|--------|-----|------|
| normalizeCellId | 有 | 无 | 新增并接入 resolve/source |
| Did you mean | 有 | 无 | handleEdit try/catch 提示 |
| multi-match | 有 | 部分（仅 exact 后置） | 前置 exact + border + lineTrimmed |
| indent migrate | 有 | 无 | non-exact 写入前 |
| metadata on type change | 有 | 无 | newCell.metadata 拷贝 |
| descriptions/summary 文案 | 有 | 旧「blind refresh」 | 更新 |

First divergence：cellId 容错与匹配唯一性不足；文案仍暗示 ID 永久稳定。

## 目标适配
- 在 tip edit/format/resolve/source/summary/descriptions **主路径**增量修改；不替换整模块。
- Did-you-mean **只抛错提示**，禁止自动改 cellId 后成功。
- lineTrimmedMatch 整函数按 source 语义重写（多命中 throw）。
- 保留 tip compact 结构/字段，仅在 return 前插入 type-change NOTE。

## 失败修订
- compact 摘要曾错误插入 `summary: if (...)` 语法破坏 → 改为 return 前 push NOTE。
- 临时 ---/+++ 路径污染（历史）→ 生成时固定 a/b 相对路径。

## 调用链 / owner 验证

### owner
- descriptions：`packages/opencode/src/plugin/vscode-bridge-descriptions.ts`（agent 可见契约）。
- format/resolve/source/summary/edit：`sdks/vscode/src/notebook/*`（桥接实现）。
### 调用链
- tool call cellId → bridge schema → HTTP → resolveNotebookCell → normalizeCellId → cell URI id。
- edit oldCode → matchAndReplace（exact→line-trimmed→border）→ 唯一性门禁 → indent migrate → TextEdit/replaceCells。
- type change → new NotebookCellData + metadata copy → compact NOTE 新 id。
### 验证点
- tip 存在 `normalizeCellId`、`Did you mean`、`migrateIndent`、`scanNotebookCellsForOldCode`。
- tip descriptions 含 “cell IDs may change on type changes” 与 “edit response includes the updated cell ID”。
- tip summary Conventions 含 session 稳定表述。
### 禁止
- cellId 猜错后自动改写成功；第二 parser；删除 tip 的 apply*AndWait 同步。


## E/C（本 patch 独立，非跨批累计）
- E=112 C=17 need=17 gate=PASS
- 合格中文解释性注释位置：
- L29: // #105：edit 响应自带新 id，summary 文案改为引导读响应而非盲刷
- L48: // #105：cellId 失效时用 oldCode 扫描候选，只提示不自动改写
- L72: // #105：type change 保留原 metadata，避免丢失自定义键
- L81: // #105：exact 多命中立即失败，要求更多上下文
- L97: // #105：空 trim 或与 old 相同则跳过，避免假匹配
- L115: // #105：line/border 策略携带源缩进到 newCode
- L135: // #105：行 trim 匹配；多命中抛错而非返回首个
- L159: // #105：line-trimmed 也必须唯一
- L179: // #105：type change 后强制提示新 cellId
- L193: // #105：cellId 失效时用 oldCode 扫描候选，提示正确 #VSC id（不自动改写）
- L194: // #105：仅扫描 contains(oldCode) 的 cell，供 Did-you-mean 提示
- L201: // #105：非 exact 策略时把 newCode 缩进迁移到源块缩进，避免错位写入
- L202: // #105：取非空行公共前导空白作为块缩进
- L217: // #105：把 newCode 相对缩进映射到 matched 源块缩进
- L286: // #105：先 normalize 再剥离 #，避免只 strip 导致漏匹配
- L319: // #105：source 与 resolve 共用 normalizeCellId 路径，避免手写 # 剥离分叉
- L334: // #105：session 内稳定——type change 会换 URI/ID，文案与 edit 对齐

## current 身份（必须与磁盘一致）
- path: `current/0105-e8675ce41599.patch`
- sha256: `ec9805ef694eab034b0477c87ad3bfc1a3b23e32bc35375a015294cb4f28d728`
- bytes: 18319
- non-empty: yes

## 物化与 typecheck
- materialize 累计至 105：passed 105/105
- tip state: `states/0105-e8675ce41599`
- typecheck report: `typecheck-0105-2026-07-22T10-19-57-539Z`
- Install fingerprint: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- Source/target repository unchanged: true（该次报告）
- 无测试删除；original 未改。

## 最终行为（可观察）
见上文「目标适配」与 tip grep 结果；本 record 关闭前 current 可独立从上一项状态 apply。


## Independent dual audit (batch 101-105)
- FORWARD: **PASS** all five, no open B/G — task `ses_0769ee162ffeET0gNBckYycMsB`
- REVERSE: **PASS** all five, no open B/G — task `ses_0769ee119ffeIvgL0xG4nw5jJ1`
- Tip: `states/0105-e8675ce41599`
- Typecheck: `typecheck-0105-2026-07-22T10-29-22-204Z` (No failures)
- Install: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- #104 repair verified: package.json version authority + setup-bun path; no DEFAULT_VERSION/input
- Batch release: **PASS**
