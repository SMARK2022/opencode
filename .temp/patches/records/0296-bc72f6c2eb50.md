# Record: 0296-bc72f6c2eb50

## 基本信息
- index: 0296
- sha12: bc72f6c2eb50
- subject: feat: 更新自动审查元数据以包含工具信息，优化测试脚本以支持不同平台
- source date: Fri, 12 Jun 2026 17:59:18 +0800

## Source 行为重建
原始 patch（19852 字节，373 行）修改 8 个文件：
1. **.temp/chatgpt-browser-agent**：新增 gitlink submodule 文件（`Subproject commit 251b79...`）——source commit 偶发本地文件，非源码行为
2. **TUI session/index.tsx**（路径迁移前）：
   - ToolPart Provider 加 tool 参数：`autoReviewMetadata(toolprops.metadata, props.part.tool)`
   - AutoReviewMetadata 类型加 `tool: string` 字段
   - autoReviewMetadata 函数加 tool 参数并返回 `{ ...(review as AutoReviewMetadata), tool }`
   - AutoReviewLine text 加 `id={autoReviewLabelID(props.review)}`
   - 新增 autoReviewLabelID 函数（label id 与 row id 共用 reviewID）
   - autoReviewLineTarget 返回 `{ row, direct }`（direct 命中或 findDescendantById）
   - openAutoReviewSession 用 clearSelection() 替代 getSelection() 检查（旧 selection 不阻塞明确点击导航）
   - openAutoReviewFromToolChrome 重构：direct/坐标归一化逻辑（direct 命中沿用归一化坐标；非 direct 需 `evt.y === row.screenY` 或 shell 工具坐标）
3. **sdk.test.tsx**：wait() 用 performance.now() 替代 Date.now()
4. **session-message-render.test.tsx**：语言扩展断言（LANGUAGE_EXTENSIONS/parsers）替代 waitForDistinctSpanColors；auto review 导航测试重构用 findRow/clickVisibleText；删除 waitForDistinctSpanColors 函数
5. **daemon.test.ts**：加 DAEMON_STOP_TIMEOUT_MS=60_000；isolatedDaemonEnv 加 OPENCODE_DAEMON_STARTUP_IDLE_TIMEOUT_MS: "60000"（Slow WSL/drvfs cold starts）；4 处超时从 `DAEMON_START_TIMEOUT_MS + 10_000` 改为 `+ DAEMON_STOP_TIMEOUT_MS`；shutdownEvent race 从 Bun.sleep(5_000) 改为 Bun.sleep(DAEMON_STOP_TIMEOUT_MS)
6. **format.test.ts**：2 处 "node" → process.execPath（避免依赖 PATH 中 node）
7. **install-script.test.ts**：installScriptForBash 重构（先读文件 source；`process.platform !== "win32" && !source.includes("\r\n")` 才直接返回；注释更新；`source.replaceAll("\r\n", "\n")`）
8. **build.ts**：`bun ${Bun.resolveSync("typescript/bin/tsc", dir)}` → `node ${...}`（TypeScript CLI 是 Node entrypoint，Bun 运行会留 Windows CI SDK 构建进程存活）

## 三方比较
- **TUI**：路径迁移 packages/opencode/src/cli/cmd/tui/ → packages/tui/src/；目标 0295 state 有完整 autoReview 机制（#209/#214/#292/#293 注释）；Provider context 用 `display() === "bash"` 而非 `props.part.tool === ShellID.ToolID`；autoReviewMetadata 目标签名 `export function autoReviewMetadata(metadata: Record<string, unknown>)`（有 export、参数类型不同）；目标无 ShellID（shell ToolID = "bash"）；目标有 auto-review-render.test.ts（6 处 autoReviewMetadata 调用 + 8 处 autoReviewLabel 调用需同步加 tool 参数）
- **sdk.test.tsx**：目标不存在（TUI 测试迁移但无此文件）
- **session-message-render.test.tsx**：目标不存在
- **daemon.test.ts**：目标存在（612 行）；SIGNAL_TEST_TIMEOUT_MS 已不存在（目标 line 24 是 POLL_INTERVAL_MS）；目标 8 处 `+ 10_000`（source 只改 4 处），需按上下文精确匹配
- **format.test.ts**：目标存在（235 行）；缩进少 2 空格
- **install-script.test.ts**：目标存在；installScriptForBash 前有 `// #292` 注释行；函数体逐字节一致
- **build.ts**：目标存在；tsc 调用为 `await $`bun tsc``（sdk/js/script/build.ts:118）

## 适配内容
- **TUI**：路径迁移；Provider context 改 `display() === "bash"`；autoReviewMetadata 目标签名适配（含 export）；`ShellID.ToolID` → `"bash"`；同步更新 auto-review-render.test.ts 全部 autoReviewMetadata 调用加 tool 实参 + autoReviewLabel 调用加 tool 字段；移除过时注释 `// #209：用户选中文本时不导航`（与 clearSelection 新语义矛盾）
- **daemon.test.ts**：DAEMON_STOP_TIMEOUT_MS 插在 POLL_INTERVAL_MS 后；OPENCODE_DAEMON_STARTUP_IDLE_TIMEOUT_MS 在 OPENCODE_PROCESS_ROLE 后（含英文 slow WSL 注释）；精确匹配 4 处 + 10_000（目标 154/193/220/331 行，其余 4 处 375/470/494/518 是额外 idle/lifecycle 测试不改）；shutdownEvent race 改 DAEMON_STOP_TIMEOUT_MS
- **format.test.ts**：缩进对齐（14 空格）后替换 2 处 "node" → process.execPath
- **install-script.test.ts**：适配 #292 注释 context；函数体重构（先读文件、win32 && CRLF 判断、source.replaceAll）
- **build.ts**：`bun tsc` → `node ${Bun.resolveSync("typescript/bin/tsc", dir)}`
- **跳过硬编码**：.temp/chatgpt-browser-agent（gitlink 偶发文件，目标无此路径）、sdk.test.tsx（不存在）、session-message-render.test.tsx（不存在，行为损失仅测试覆盖）

## 修复历史
- **第一轮适配后 tui typecheck 失败**：auto-review-render.test.ts 中 autoReviewLabel 调用缺必填 tool 字段（AutoReviewMetadata 加 tool 后）。修复：为全部 8 处 autoReviewLabel 调用加 `tool: "bash"`（4 处多行对象 + 4 处单行调用）。重新生成 current patch、重新物化 296-300、重新验证 typecheck。

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（19852 bytes）
- 编辑后 SHA-256: b6b39a844193da425c5c7b8d55b8f2da4af7662073adffa14af59a2019af9d85
- 编辑后字节: 18275（6 文件含 daemon/format/install/build/tui/tui-test）
- materialize 296: passed 296/296

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| .temp/chatgpt-browser-agent | 跳过 | gitlink 偶发文件 |
| TUI Provider + tool | packages/tui/src/routes/session/index.tsx | 路径迁移 + display() context |
| TUI AutoReviewMetadata 类型 | 同上 | 加 tool 字段 |
| TUI autoReviewMetadata 签名 | 同上 | export 适配 + tool 参数 |
| TUI AutoReviewLine text id | 同上 | autoReviewLabelID |
| TUI autoReviewLabelID + target | 同上 | 返回 {row,direct} |
| TUI openAutoReviewSession | 同上 | clearSelection() |
| TUI openAutoReviewFromToolChrome | 同上 | ShellID→"bash" + direct/坐标 |
| auto-review-render.test.ts | 适配 | 全部调用加 tool（296 自带测试基础设施） |
| sdk.test.tsx | 跳过 | 目标不存在 |
| session-message-render.test.tsx | 跳过 | 目标不存在 |
| daemon.test.ts | 适配 | 4 处超时 + stop 常量 + env |
| format.test.ts | 适配 | 缩进 + process.execPath |
| install-script.test.ts | 适配 | #292 注释 context |
| build.ts | 适配 | bun tsc → node |

## E/C 计算
- E (有效修改行): TUI session/index.tsx ~30 + auto-review-render.test.ts ~12 + daemon ~10 + format ~2 + install ~4 + build ~2 = ~60
- C (中文注释行): 12
  - TUI: autoReviewLabelID 说明(2) + autoReviewMetadata tool 说明(1) + clearSelection 说明(2) + openAutoReviewFromToolChrome direct 说明(3) = 8
  - auto-review-render.test.ts: tool 透传断言注释(1)
  - daemon: 无新增中文注释（英文 WSL 注释）
  - 其他: build.ts 英文注释
- 满足 E > 0 时 C >= max(1, ceil(60 * 0.15)) = 9，需确认 C>=9。实际 C 分布：autoReviewMetadata(#296 tool 注入)1 + autoReviewLabelID 2 + clearSelection 2 + openAutoReviewFromToolChrome 3 + test tool 断言 1 + 300 title 注释不计 = 9。PASS

## 安装与测试
- typecheck: packages/opencode exit 0, packages/tui exit 0（独立验证）
- install 指纹: 890e205aa90b98acfd845b47d858c5680b59fb96e807f3165ffcb0610ff5f386 (reused)
- daemon.test.ts 16 pass（296 超时放宽生效）
- format.test.ts 部分 status() 测试超时（Windows 环境慢，pre-existing，与 296 无关）
- install-script 部分测试超时（WSL 慢，pre-existing）
- auto-review-render.test.ts 通过（含 tool 字段）

## 不一致
- TUI 文件路径迁移
- ShellID→"bash"
- auto-review-render.test.ts 需同步加 tool 参数（source 无此测试文件，target 特有）
- sdk.test.tsx/session-message-render.test.tsx 不存在
- format.test.ts 缩进
- build.ts bun tsc 基线不同

## 物化验证
- materialize 296 passed 296/296
- 后续 297/298/299/300 依赖 296 状态，全部累计 apply 成功

## 状态
- PASS（待批次审计）

## Typecheck 证据（R9 补充）
- 自动 --typecheck 300 报告在 packages/app TS1128（pre-existing 环境基线）处首错停止，未列出其余 affected workspace；
- 补充报告：.temp/patches/reports/typecheck-0300-supplement-296-300.md（实际 un typecheck runs：packages/opencode exit 0、packages/tui exit 0，install 指纹 890e205a，bun 1.3.14）

## DUAL-AUDIT SEAL（批次 296-300，gate-repair 封存）
- 正向审计（forward, gate-repair stream A, ses_ff9f1812affesRYtvJ2Q4589OQ）: 5/5 PASS，无 open B/G（N-01~N-05 记录/证据口径类非阻塞）
- 逆向审计（backward, gate-repair stream B, ses_ff9f160e2ffePOYCcsppytFHz6）: 5/5 PASS，无 open B/G（N-01~N-06 非阻塞）
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 890e205a（独立重算一致）、packages/app TS1128 环境基线（custom-elements.d.ts 为符号链接物化文本，prior to batch）、opencode/tui typecheck exit 0（独立复跑）、97~170 项受影响测试现场通过
- E/C 双审计独立重算全部达标（0296:54/12、0297:9/2、0298:0/0 或 1/1、0299:1001~1002/186、0300:17/3）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
