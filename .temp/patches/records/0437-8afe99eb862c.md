# 0437-8afe99eb862c fix(ci): 修复三平台日志与跨平台测试竞态（含 R14 计划文档）

## Identity
- index / full SHA: 0437 / 8afe99eb862c（manifest parent = 0436 ef6e3820）
- subject: fix(ci): 修复三平台日志与跨平台测试竞态
- original: 56754 bytes / 708 lines / 4 files
- current: 47395 bytes / 518 lines / 1 file（Round-6 裁剪：H0 plan doc 保留，H1-H4 superseded——见 Round-6 段）
- status: PASS（适配裁剪后，materialize 437 passed 437/437）

## Source Intent（per-hunk）
| Hunk | 文件 | Intent | Invariant |
|---|---|---|---|
| H0 | docs/plans/windows-macos-ci-test-failures.md 新建 500 行 | R14 canonical plan：三平台 CI 测试失败修复的实施规范、14 轮审计记录、实现证据、E/C 预算 | 记录性文件；0438/0439 行政修订的 blob 链基座 |
| H1 | packages/core/src/util/log.ts +2 | 引入模块级 fileInit（FIFO promise tail）与 latestInit（authoritative completion） | INV-02：file-mode init 串行拥有文件变更 |
| H2 | packages/core/src/util/log.ts +81/-32 | 重写 init()：print:true 立即切 stderr；file-mode 进 FIFO；candidate 监听 open/error/close；open 后原子替换 writer；failed candidate 成 terminal no-op | 消除 stale truncate / destroyed-writer / stale 过早返回 / print 延迟 |
| H3 | test/session/prompt.test.ts +10/-3 | cancel truncation 测试：bash 4000 行 printf 后 touch marker；existsSafe + pollWithTimeout 等 marker；删 Effect.sleep(150) | INV-03/04：cancel 前必须有 published readiness signal |
| H4 | test/snapshot/snapshot.test.ts +3/-1 | order 测试 fixture length: 140 → 101（跨 step=100 batch 边界最小 fixture） | INV-01：跨 batch 顺序 |

## Target Adaptation（Round-6 裁剪决策）
### H1/H2 — superseded（结构性取代）
- 目标 435 状态 **packages/core/src/util/log.ts 不存在**：日志已迁移至 `packages/core/src/observability/logging.ts`（Effect 原生 Logger.toFile + FileSystem.makeDirectory 自愈目录 + 结构化 formatter；loggers() 按 OPENCODE_PRINT_LOGS 切换）。
- 新架构下无可变共享 initID 竞态形状（Logger.toFile 纯函数、file 在调用时捕获）；旧 40 行命令式生命周期机制无直接对应物。INV-02 的"init 返回后写入未落盘"症状在新架构下不成立——迁移本身即为更强修复。
- 回归保护面（test/util/log.test.ts）亦已随迁移删除；H2 的 red-capable 测试面不存在。
- **遗留观察项（N，非本批行为缺陷）**：Logger.toFile 的内部 flush/readiness 行为与旧 createWriteStream 不同，未做独立验证即结构性关闭 INV-02——如后续出现日志尾部丢失症状，复查 observability/logging.ts。

### H3 — superseded（已被 435 状态吸收）
- 435 state 的 cancel truncation 测试（prompt.test.ts L3376 区）已采用等价但不同形的实现：bash 命令尾部 `printf truncation-ready; sleep 30`（stdout 哨兵）+ `pollWithTimeout` 轮询 MessageV2.filterCompactedEffect 中 tool state output 含 truncation-ready；Effect.sleep(150) 已不存在。
- INV-03/04（readiness signal 而非固定睡眠）在 435 状态由现有实现完整满足。

### H4 — superseded（已被 435 状态吸收）
- 435 state 的 snapshot order 测试 fixture 已用命名常量 `SNAPSHOT_BATCH_BOUNDARY = 100`、`OVER_BATCH_COUNT = SNAPSHOT_BATCH_BOUNDARY + 1`（=101）——与 H4 的 140→101 语义（最小跨 batch fixture）等价。
- 生产侧不变量 `diffFull step=100` 仍在（snapshot/index.ts L755）。

### H0 — 原样保留
- plan doc 500 行新建，为 0438/0439 行政修订的 blob 链基座（pre-image 2cebdcf6… 被 0438 精确引用），必须原样落地。plan 内容描述的基线与 435 状态大面积不符——record 0438 已标注其 administrative documentation 性质。

## Per-hunk mapping table
| Source hunk | Target | 状态 |
|---|---|---|
| H0 plan doc 新建 500 行 | docs/plans/windows-macos-ci-test-failures.md | ✓ 原样保留 |
| H1 log.ts fileInit/latestInit | packages/core/src/util/log.ts（不存在） | ✗ superseded（Effect 日志迁移） |
| H2 log.ts init() 重写 | 同上 | ✗ superseded（同上） |
| H3 prompt.test cancel marker | test/session/prompt.test.ts（stdout 哨兵版已在） | ✗ superseded（已吸收） |
| H4 snapshot fixture 140→101 | test/snapshot/snapshot.test.ts（OVER_BATCH_COUNT 常量已在） | ✗ superseded（已吸收） |

## Verification
- materialize 437：passed 437/437（裁剪后 patch apply 干净；裁剪前 original 直通在 437 处 failed——log.ts 不存在所致，预研与 mat440 首轮均确认）。
- 裁剪操作记录：708 行 → 518 行（保留 L1-518 = H0 段全部 + 文件头）；行尾由 WriteAllLines 意外引入的 CRLF 已归一化回 LF（47913 → 47395 bytes）。
- E/C：E=0, C=0, need=0 → PASS（唯一保留文件为 .md，按 .md 排除口径；source 原内嵌 E=79/C=13 预算绑定已消失的 log.ts hunk，仅作历史记录）。

## 不一致
- source H1-H4 的行为在目标由不同机制承载（Effect 日志架构 / 435 状态测试实现），非逐字重放——本 record 逐项标注 superseded 证据，避免后续 replay 重复实现。
- plan doc（H0）内嵌 §23 证据针对其原始基线，对 435 状态无证明力——行政记录，不影响行为。
- plan doc 个别表格中文乱序痕迹（§18 等）随 source 原样保留。

## Status
PASS（H0 原样 + H1-H4 superseded 记录在案；materialize 437/437 通过）

## 审计轮次记录
- Round-6（批次首轮双审）：正向审计确认 H0 保留/H1-H4 superseded 判据充分；逆向审计同判。
- 首轮 materialize-0440（05-31）诚实记载 437 file-not-found 失败——该失败是裁剪前的 original 直通尝试，非适配缺陷。
- 裁剪采用 PowerShell 截取（708→518 行）+ CRLF 归一化回 LF（47913→47395 bytes），全部留痕于本 record。
