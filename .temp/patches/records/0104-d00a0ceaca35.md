# Record 0104-d00a0ceaca35

## Source intent（original 完整语义）
#104 将 CI 从「散落 version + upload-artifact」升级为「**package.json 唯一版本源 → ensure-release → 构建 → gh release upload**」：

1. **删除** `workflow_dispatch.inputs.version` 与 `env.DEFAULT_VERSION`（禁止常量/手动双源）。
2. **version job**：`actions/checkout` + `node -p "require('./packages/opencode/package.json').version"`；空/`undefined` → `exit 1`；输出 `version` 与 `tag=v${VERSION}`。
3. **ensure-release**：`gh release view` 存在则跳过，否则 `gh release create`（target SHA + notes）。
4. 三平台 `needs: [version, ensure-release]`，`permissions.contents: write`。
5. 构建与打包一律 `needs.version.outputs.version`。
6. **删除** `actions/upload-artifact`；改为 `gh release upload --clobber`。
7. Windows：zip → 裸 `.exe` 复制后上传。
8. push paths **增加** `.github/actions/setup-bun/action.yml`。

## 上游 / tip@103 设计
- tip@103 已有：`archFilter` 调用、`--arch=x64`、`../../` 打包路径、三平台 if-no-files-found artifact、Windows native 工具链。
- 版本仍 `inputs.version || env.DEFAULT_VERSION`（1.14.31-smark）。
- 无 version/ensure-release；artifact 上传。

## 三方比较
| 语义 | source | tip@103 | 决策 |
|------|--------|---------|------|
| package.json 版本 | 唯一源 | 常量/input | **恢复 source 主路径** |
| fail-closed 读版本 | 有 | 无 | 实现 |
| 删 DEFAULT_VERSION/inputs | 有 | 有常量 | 删除 |
| ensure-release + upload | 有 | artifact | 实现 |
| win exe | 有 | zip | 对齐 |
| setup-bun path | 有 | 无 | 补齐 |
| #103 arch/path | 在前序 | 有 | **保留** |

## First divergence（审计 B-01/B-02 根因）
首版 current 错误保留 DEFAULT_VERSION/input，未读 package.json，亦缺 setup-bun path。双审 BLOCK 后已按 original 主路径重写。

## 目标适配
- owner：仅 `.github/workflows/build-opencode.yml`。
- primary path：package.json → version outputs → build/package/release。
- 无第二版本源、无 catch-and-success。
- 融合：保留 tip Windows setup-node/python/msbuild 与 #103 arch/path。

## 调用链
1. push/dispatch → version(checkout+node) → outputs
2. ensure-release → create if missing
3. build-* → package → gh release upload

## 错误语义
- 读不到 version：job fail。
- Windows 无 exe：throw。
- bash `set -euo pipefail`。

## E/C
- E=102 C=16 need=16 gate=PASS
- 注释：
- L15: # #104：版本从 packages/opencode/package.json 自动提取；GitHub Release 直传资产
- L20: # #104：去掉 version input，避免与 package.json 双源
- L28: # push 自动触发：dev-smark + 路径过滤（含 setup-bun 变更）
- L40: # #104：setup-bun 变更必须触发重建，避免工具链静默陈旧
- L46: # #104：唯一版本源 = packages/opencode/package.json，读失败 fail-closed
- L58: # #104：node 同步读 package.json.version，禁止默认常量回退
- L60: # #104：空/undefined 立即失败，避免错误 tag 静默发布
- L70: # #104：若不存在对应 tag 的 release 则创建
- L88: # #104：新建 release 锚定当前 SHA
- L114: # #104：--version 仅来自 package.json 解析结果
- L123: # #103：相对 bin 的 ../../ 落到 dist/
- L173: # #104：gh release upload 替代 upload-artifact
- L180: # #104：macOS 双架构 tar.gz 直传 release
- L230: # Windows x64：#103 arch；#104 裸 exe 上传 release
- L273: # #104：发布裸 exe（相对 zip）
- L292: # #104：无 exe 硬失败，禁止空 release

## current 身份
- path: `current/0104-d00a0ceaca35.patch`
- sha256: `a0b5721d49120ccbb1e8466c6dde784238e42b5080bcda5341477d647cef8cb8`
- bytes: 12774

## 物化 / typecheck
- typecheck-0105-2026-07-22T10-29-22-204Z：No failures；passed 105/105
- Install: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- tip greps：无 DEFAULT_VERSION；有 node -p；有 setup-bun/action.yml；无 upload-artifact uses；package version 1.17.20-smark

## 失败修订
- r1：DEFAULT_VERSION/input 残留 → 双审 B-01/B-02 BLOCK。
- r2：按 original 重写 version 权威 + setup-bun path；rematerialize 104–105 + typecheck 绿。

## 最终行为
- 自动构建版本 = `packages/opencode/package.json` 的 `1.17.20-smark`（随包变化）。
- Release tag `v1.17.20-smark`；资产与 `--version` 一致。
- setup-bun 变更触发本 workflow。


## Independent dual audit (batch 101-105)
- FORWARD: **PASS** all five, no open B/G — task `ses_0769ee162ffeET0gNBckYycMsB`
- REVERSE: **PASS** all five, no open B/G — task `ses_0769ee119ffeIvgL0xG4nw5jJ1`
- Tip: `states/0105-e8675ce41599`
- Typecheck: `typecheck-0105-2026-07-22T10-29-22-204Z` (No failures)
- Install: `22f7ffd65e5f771ef5f4da26e2adb6a4239744c2a6061ab6870d8c43fb1ce6d5`
- #104 repair verified: package.json version authority + setup-bun path; no DEFAULT_VERSION/input
- Batch release: **PASS**
