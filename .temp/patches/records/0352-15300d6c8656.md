# Record: 0352-15300d6c8656（351-400 波审计后扩写版）

## Identity
- index: 0352；sha12: 15300d6c8656；parent: 382f91d238a7（=351）；subject: fix(tui): 修正 Voice 提示宽度门与流式期转录禁用
- original: .temp/patches/original/0352-15300d6c8656.patch（3 文件）；current: .temp/patches/current/0352-15300d6c8656.patch（5335B；sha256=1916ee5ce5658d1af440839e2b153d07a10d56f1809837ea326dc4aa5c3b4d7a）

## Source intent（逐 hunk 行为重建）
1. prompt-voice-input.ts：新增 `VOICE_HINT_MIN_PROMPT_WIDTH=140` 常量与纯函数 `voiceHintVisible(transcriber, promptWidth)`——voice footer 提示仅在"有转写器且提示宽度足够"时显示，解决窄终端下提示溢出换行。
2. component/prompt/index.tsx：录音 useBindings `enabled` 去除 `isRunning()` 限制——流式期间允许发起/停止/取消录音；并发提交竞争由既有 `voiceInputBusy()` 提交保护拦截（转录进行中禁止发送消息）。footer voice 提示的 `Show` 条件改为 `voiceHintVisible(...)`（promptWidth 信号驱动）。
3. 测试：宽度阈值边界测试（undefined/100/140/141/200 五档），锁定 140 门与 undefined 安全回退。
- invariant：显示判定与绑定可用性分离——"能不能按 alt+v"由 bindings enabled 决定，"要不要显示提示"由 voiceHintVisible 决定；流式期录入不产生误提交。

## 三方比较
- 上游 v1.17.20：packages/tui voice 体系为本分支 SMARK 特性（上游无 voiceInput），无上游实现可融合；目标 owner 即唯一实现点。
- 目标：TUI 已整体外迁 packages/tui（源路径 packages/opencode/src/cli/cmd/tui/* → packages/tui/src/*），映射后语义 1:1。
- 目标 enabled 形态差异：目标 useBindings enabled 为 `!dialog.stack.length && !isRunning()` 风格组合，适配保留 dialog 门、去除 running 门；Show 注释转为 JSX 注释块。

## V1/V2 owner 判定
- owner 在 packages/tui（V2 期 TUI 唯一存活地；packages/opencode/src/cli/cmd/tui 已 0 文件）。SMARK 语音特性落在现役唯一 owner，无弃用架构残留、无 V2 对应物缺失。

## 逐 hunk 映射（物化落点，states/0360+ 实测）
- hunk1 常量+函数 → packages/tui/src/component/prompt/prompt-voice-input.ts:216（voiceHintVisible）及常量定义处。
- hunk2a enabled → packages/tui/src/component/prompt/index.tsx:1090-1094（去除 running 限制）；hunk2b Show → index.tsx:1948（JSX 条件）。
- hunk2c voiceInputBusy 既有保护确认在位：index.tsx:383、:1141；promptWidth 信号 :186。
- hunk3 测试 → packages/tui/test/prompt/prompt-voice-input.test.ts:96-100（五档宽度断言）。

## E/C 与注释位置
- E≈22-30（常量/函数/enabled/Show/测试行）；C≈14（宽度阈值边界注释、显示 vs 绑定职责区分注释、流式期提交保护注释、测试意图注释，分散于 prompt-voice-input.ts、index.tsx 决策点与测试文件）。门禁 C≥max(1,ceil(E×0.15))=5 ✓（双向审计独立复算通过）。

## 物化与验证
- materialize 352 passed 352/352；波内 0360-0400 检查点链全绿。
- typecheck-0352 全 workspace passed（install 复用 0351 指纹 303af56d，合法——本项未触依赖输入）。
- 行为测试：voice 测试 17 pass / 0 fail（含 5 个新宽度门测试）。

## 失败修订链
- 无功能性失败；R12 重建期仅路径映射锚点修正（cli/cmd/tui → packages/tui）。

## 最终行为
- 窄终端（<140 列）不再显示 voice 提示；流式期间可发起/停止录音且 voiceInputBusy 拦截误提交；五档边界测试锁定。无行为面损失。

## 审计残留（N 级，不阻塞）
- 双向审计确认无 B/G；record 旧版低于契约长度（G），本扩写即修复。
