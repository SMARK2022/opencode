# Record: Patch 24 — 5a7f139b02d1

## 1. Manifest Entry

- **Index**: 24
- **SHA**: 5a7f139b02d19f1853d788ca59220bdee00ba37e
- **Parent**: 0023-b2b12b0abfcd
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 优化输入文本处理和系统块合并逻辑，确保正确插入和保留原有字段

## 2. Source Behavior (original patch)

### 修改文件
- `packages/opencode/src/provider/claudecode.ts`
  - 重新启用系统块合并逻辑：将 `buildSystemBlocks` 的注释代码恢复为活跃代码
  - `buildBillingHeaderBlock(fp)` 生成计费头块
  - 合并原始 system 内容到 newSystem（支持数组和字符串两种形式）
  - 在 `patchedBody` 中恢复 `system: newSystem` 赋值

- TUI 相关文件（`prompt/index.tsx` 等）
  - SMARK 原始 patch 包含 `Buffer.from(...).subarray()` 切片修复
  - 修复虚拟文本删除时的坐标计算问题

### 行为目的
- **系统块合并 invariant**: 每个请求注入计费头块 + 保留原始 system 内容
- **计费头 invariant**: `buildBillingHeaderBlock(fp)` 使用消息指纹生成唯一计费标识
- **TUI 切片修复 invariant**: `Buffer.subarray` 确保字节级切片正确

## 3. 上游 (v1.17.20) 行为

- claudecode.ts 的系统块合并逻辑被 patch 20 注释禁用
- patch 20 的注释说明："系统块合并已禁用：不再注入伪造的系统块"
- V2 TUI 使用 `displaySlice`/`expandTrackedPastedText` 处理显示宽度切片
- V2 的 `promptOffsetWidth` 使用 `Intl.Segmenter` + `Bun.stringWidth` 正确处理 grapheme

## 4. 目标适配 (current patch)

### 保留的 SMARK 语义
- 系统块合并逻辑重新启用：`buildBillingHeaderBlock(fp)` 注入计费头块
- 原始 system 内容合并到 newSystem（数组和字符串两种形式）
- `patchedBody.system = newSystem` 恢复

### 跳过的 SMARK 语义（有正当理由）
- TUI `Buffer.subarray` 切片修复：V2 上游已使用 `displaySlice`/`expandTrackedPastedText` 正确处理显示宽度切片
  - `packages/tui/src/prompt/display.ts` 导出 `displaySlice` 和 `promptOffsetWidth`
  - `packages/tui/src/prompt/part.ts:24-28` `expandTrackedPastedText` 使用 `displaySlice`
  - 上游实现使用 `Intl.Segmenter` (grapheme) + `Bun.stringWidth`，比 SMARK 的 `Buffer.subarray` (字节级) 更正确
  - 按"上游更强时，必须把上游完整实现保留为不可删除基线"规则跳过

### 仅恢复 `buildBillingHeaderBlock`
- 不恢复完整的 `buildSystemBlocks`（与 patch 20 的禁用意图一致——patch 20 禁用了系统块注释，patch 24 仅恢复计费头块）
- `buildBillingHeaderBlock` 只生成计费头，不生成系统注释块

## 5. E/C 计算

### 有效修改行 E
- claudecode.ts:
  - 注释恢复为代码: 8 行（`let newSystem` + `if` 块）
  - 注释更新: 1 行
  - system 赋值恢复: 1 行
  - 小计: ~10 行
- **总 E ≈ 10**

### 合格中文注释 C
1. `// 2. 合并 System Blocks：仅注入计费头块，保留原始 system` — 修改的注释，解释合并策略

- **总 C = 1**
- **最低 C = max(1, ceil(10 * 0.15)) = 2**

**注意**: C=1 < 2，需要补充注释。但该 patch 主要是恢复已有代码（从注释恢复为活跃代码），新增的实质代码行较少。注释 `// 2. 合并 System Blocks：仅注入计费头块，保留原始 system` 已经解释了核心 invariant。如果审计要求更多注释，可以在 `buildBillingHeaderBlock(fp)` 和 `if (originalBody.system)` 处添加。

### 补充注释
在修订中添加以下注释以满足门禁：
- `// 计费头块使用消息指纹生成唯一计费标识`
- `// 原始 system 可能是数组或字符串，两种形式都需合并`

修订后 C = 3 ≥ 2，通过。

## 6. 验证

### Apply
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 25 --rebuild`
- 结果: passed 25/25

### Typecheck
- packages/core: passed
- packages/opencode: passed
- packages/tui: passed

### Install 指纹
- `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史

无行为缺陷。首轮审计 PASS。记录补充：从 15 行占位记录扩展为完整记录。

## 8. 最终行为

- 系统块合并逻辑重新启用，仅注入 `buildBillingHeaderBlock` 计费头块
- 原始 system 内容（数组或字符串形式）合并到 newSystem
- `patchedBody.system = newSystem` 恢复
- TUI 切片修复由上游 `displaySlice`/`expandTrackedPastedText` 处理（比 SMARK 的 `Buffer.subarray` 更正确）
- V2 路径和 API 保留
