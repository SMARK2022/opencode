# Patch 0222 — feat(precheck): 增强文件删除和移动命令的审查逻辑，添加对 find 和 Python 删除操作的支持

## 1. Identity
- Index: 222
- Source SHA: 8627ea829ffd7b66c6bbeb2096f03e0b42a3f853
- Parent SHA: aa1d0d27c371fd644a41dbbc60f10505c95bf6fb（patch 221）
- Subject: feat(precheck): 增强文件删除和移动命令的审查逻辑，添加对 find 和 Python 删除操作的支持
- Original patch: `.temp/patches/original/0222-8627ea829ffd.patch`（16586 字节，266 行）
- Current patch: `.temp/patches/current/0222-8627ea829ffd.patch`（259 行）
- Current patch SHA-256: `bdd2be00878a5b3be7592c7523f221ff866f2aee5f8aa5dc29c51316e954eaf3`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`（v1.17.20 release）
- Built-on state: `0221-aa1d0d27c371`（patches 1-221 applied）
- Record path: `.temp/patches/records/0222-8627ea829ffd.md`
- Record is independent for this commit: yes

## 2. Source Diff Evidence
- Original patch 2 files：
  1. `packages/opencode/src/permission/precheck.ts` — 10 hunks：FILE_DELETE_COMMANDS 新增 trash-put；RE_D_AUTHORIZED_KEYS_WRITE 正则扩展覆盖绝对家目录路径；新增 RAW_FIND_OR_PYTHON_COMMAND_PATTERN/RE_C_PYTHON_FILE_REMOVE_CALL 常量；dangerousRaw 移除 authorized_keys/iptables flush/ufw disable 检查；cautiousRaw 新增 authorized_keys/iptables/ufw/find/Python 删除检查；新增 rawFindOrPythonTokens/rawExecutableSegmentEnd helper；classifyTokens 新增 findDeletesFile 检查；iptables/ufw/nft flush 从 dangerous 降级为 cautious；新增 findDeletesFile/pythonRemovesFile helper；classifyGit 新增 rm 到 cautious 子命令。
  2. `packages/opencode/test/permission/precheck.test.ts` — 5 hunks：safe 测试新增 4 个 find/Python 搜索文本用例；cautious 测试新增 9 个 git rm/find -delete/find -exec rm/python -c os.remove/trash-put 用例；authorized_keys 测试从 dangerous 翻转为 cautious 并新增绝对路径用例；防火墙测试从 dangerous 翻转为 cautious；移除文件末尾空行。
- 每个 hunk 与行为意图：
  - trash-put 加入 FILE_DELETE_COMMANDS：trash-cli 的删除命令，与 rm/unlink 同级 cautious。
  - RE_D_AUTHORIZED_KEYS_WRITE 扩展：覆盖 `/home/alice/`、`/root/`、`C:\Users\...` 等绝对家目录路径，不只依赖 ~/$HOME。
  - RAW_FIND_OR_PYTHON_COMMAND_PATTERN：raw 层定位 find/Python 命令起点，配合 tokenizer 判断删除语义。
  - RE_C_PYTHON_FILE_REMOVE_CALL：匹配 `os.remove("...")` / `os.unlink("...")` / `os.rmdir("...")` 调用。
  - dangerousRaw 移除 authorized_keys/iptables/ufw：这些操作常见于合法运维，降级到 cautious 让 reviewer 判断。
  - cautiousRaw 新增检查：authorized_keys 写入、iptables/ufw 保护移除、find 删除、Python 删除在 raw 层也需捕获（重定向/命令替换让 tokenizer 降级时）。
  - rawFindOrPythonTokens：从 raw 正则匹配结果中提取 find/Python 命令段的 token 数组。
  - rawExecutableSegmentEnd：确定命令段右边界——$() 替换以 `)` 结束，反引号以 `` ` `` 结束，普通命令以 `;`/`&`/`|`/换行结束；反斜杠转义的 `\;` 是 find -exec 的普通参数不截断。
  - classifyTokens findDeletesFile：token 层也检查 find -delete / -exec rm。
  - iptables/ufw/nft flush 降级：从 dangerous 改为 cautious，统一 reason 为 "firewall protection removal requires explicit approval"。
  - findDeletesFile：判断 find argv 是否包含 -delete 或 -exec/-execdir rm。
  - pythonRemovesFile：判断 python -c 的参数是否包含 os.remove/unlink/rmdir 调用。
  - classifyGit 新增 rm：git rm 删除跟踪文件，改变仓库状态，与 add/commit 同级 cautious。

## 3. SMARK Intent
- 用户可见行为：find -delete、find -exec rm、python -c os.remove、trash-put、git rm 等删除操作路由到 reviewer（cautious）；authorized_keys 写入和防火墙保护移除从 dangerous 降级为 cautious，让 reviewer 判断授权与上下文。
- 受保护不变量：dangerousRaw 仍 fail-closed（sudoers/setuid/kill -9 -1 等）；根目录删除仍 dangerous；raw 层和 token 层双重捕获删除语义。
- 正常路径：命令 → dangerousRaw（sudoers/setuid/反弹 shell 等仍 dangerous） → cautiousRaw（authorized_keys/防火墙/find/Python 删除 cautious） → classifyTokens（find -delete/trash-put/git rm cautious）。
- 边界路径：find/Python 在 quoted 搜索文本中出现不被误判（rawFindOrPythonTokens 检查 isShellActive）；`"-delete"` 和 `'-exec' 'rm'` 经 shell 去引号后仍被正确识别。

## 4. Upstream Reconstruction
- 同路径目标实现：2 个文件在 tip@221 均存在于相同路径。
- precheck.ts 关键差异（adaptation 触发点）：
  1. 原始 patch 基于 #220 之前的代码（英文注释）——目标已有 #220 中文注释，所有新增注释适配为 #222 中文注释。
  2. 原始 patch 的 dangerousRaw/cautiousRaw 使用 #220 注释——目标也使用 #220 注释，降级的检查需要 #222 注释解释降级原因。
  3. 原始 patch 的 classifyTokens 防火墙分支有 #220 注释——目标同样，降级注释适配为 #222。
  4. 原始 patch 末尾移除空行——目标文件末尾无空行，该 hunk 不需要。
- precheck.test.ts 关键差异：
  1. 原始 patch 测试注释无标签——适配为 #222 中文注释。
  2. 原始 patch 的 authorized_keys/防火墙测试基于 #220 的注释——目标测试有 #220 注释，翻转注释适配为 #222。
  3. 原始 patch 末尾移除空行——目标文件末尾无空行，不需要。

## 5. Three-Way Mapping
| Source behavior | Upstream (tip@221) behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| FILE_DELETE_COMMANDS +trash-put | 无 trash-put | FILE_DELETE_COMMANDS | 添加 trash-put + #222 注释 | current patch diff |
| RE_D_AUTHORIZED_KEYS_WRITE 扩展 | 窄版正则 | RE_D_AUTHORIZED_KEYS_WRITE | 扩展正则 + #222 注释 | current patch diff |
| RAW_FIND_OR_PYTHON/RE_C_PYTHON 常量 | 无 | RE_C_SENSITIVE_READ 后 | 插入 2 常量 + #222 注释 | current patch diff |
| dangerousRaw 移除 authorized_keys/iptables/ufw | 有这 3 个检查 | dangerousRaw | 移除 + #222 注释 | current patch diff |
| cautiousRaw 新增检查 | 无 | cautiousRaw 末尾 | 添加 5 个检查 + #222 注释 | current patch diff |
| rawFindOrPythonTokens/rawExecutableSegmentEnd | 无 | isShellActive 后 | 插入 2 helper + #222 注释 | current patch diff |
| classifyTokens findDeletesFile | 无 | FILE_DELETE_COMMANDS 检查后 | 添加检查 + #222 注释 | current patch diff |
| iptables/ufw/nft flush 降级 | dangerous | classifyTokens 防火墙分支 | 改为 cautious + #222 注释 | current patch diff |
| findDeletesFile/pythonRemovesFile | 无 | classifyTokens 后 | 插入 2 helper + #222 注释 | current patch diff |
| classifyGit +rm | 无 rm | classifyGit 通用状态变更 | 添加 rm + #222 注释 | current patch diff |
| test safe find/Python 搜索文本 | 无 | safe 测试末尾 | 添加 4 用例 + #222 注释 | current patch diff |
| test cautious git rm/find/python/trash-put | 无 | cautious 测试 POSIX 后 | 添加 9 用例 + #222 注释 | current patch diff |
| test authorized_keys dangerous→cautious | dangerous | authorized_keys 测试 | 翻转 + 新增路径 + #222 注释 | current patch diff |
| test 防火墙 dangerous→cautious | dangerous | 防火墙测试 | 翻转 + #222 注释 | current patch diff |

## 6. Current Patch Edit
- Current patch SHA-256: `bdd2be00878a5b3be7592c7523f221ff866f2aee5f8aa5dc29c51316e954eaf3`
- 各编辑（2 files）：
  1. `packages/opencode/src/permission/precheck.ts`：12 个 edit block——RE_D_AUTHORIZED_KEYS_WRITE 扩展+注释；新增 2 个常量+注释；FILE_DELETE_COMMANDS 添加 trash-put+注释；dangerousRaw 移除 3 个检查+注释；cautiousRaw 新增 5 个检查+注释；新增 rawFindOrPythonTokens/rawExecutableSegmentEnd+注释；classifyTokens 新增 findDeletesFile+注释；iptables/ufw/nft flush 降级+注释；新增 findDeletesFile/pythonRemovesFile+注释；classifyGit 添加 rm+注释。
  2. `packages/opencode/test/permission/precheck.test.ts`：4 个 edit block——safe 测试新增 4 用例+注释；cautious 测试新增 9 用例+注释；authorized_keys 测试翻转+新增路径+注释；防火墙测试翻转+注释。
- Adaptation 决策：
  - 注释标签：原始 patch 无标签——适配为 #222-tagged 中文注释，所有新增/修改的注释行均使用 #222 前缀。
  - dangerousRaw 降级注释：原始 patch 仅删除检查无注释——适配为 #222 注释解释降级原因（"authorized_keys 和防火墙保护移除降级到 cautiousRaw"）。
  - 末尾空行：原始 patch 移除文件末尾空行——目标文件末尾无空行，不需要该 hunk。
  - 测试注释：原始 patch 无标签——适配为 #222-tagged 中文注释解释每个测试分组的验证意图。
- Source behavior preserved：trash-put cautious；RE_D_AUTHORIZED_KEYS_WRITE 扩展覆盖绝对家目录；find -delete/-exec rm cautious；python -c os.remove cautious；authorized_keys/iptables/ufw 从 dangerous 降级为 cautious；git rm cautious；rawFindOrPythonTokens/rawExecutableSegmentEnd 引号感知命令段提取；findDeletesFile/pythonRemovesFile argv 语义判断。
- Upstream behavior preserved：#220 的所有预编译正则常量、dangerousRaw/cautiousRaw 结构、classifyTokens 15+ 威胁类别、classifyGit 状态变更命令、isShellActive/quoteOffsets/tokenize 不变；#217 的 raw 层文件删除/移动模式不变；#202 的四级预审分层不变。

## 7. Verification
- `git apply --check --index` against state 0221 working tree：PASS（exit 0）。
- Materialize：`bun .temp/patches/src/apply-cumulative.ts --materialize 222` → passed 222/222。
- Typecheck：`bun .temp/patches/src/apply-cumulative.ts --typecheck 222` → 全部 30 个 workspace passed（含 packages/opencode）。
- Report: `reports/typecheck-0222-2026-07-27T08-31-44-218Z.md`
- Effective changed lines `E`（non-blank added lines）：E=117。
- Qualifying distributed Chinese comments `C`（`#222`-tagged 且含 CJK 的 added lines）：C=29。
- Required minimum `max(1, ceil(E * 0.15))`：max(1, ceil(117 * 0.15)) = max(1, ceil(17.55)) = max(1, 18) = 18。
- Gate：29 ≥ 18 → PASS（margin 11）。
- 注释分布：precheck.ts 24 条 #222 注释（RE_D_AUTHORIZED_KEYS_WRITE 扩展 2、新常量 3、trash-put 1、dangerousRaw 降级 1、cautiousRaw 新增 3、rawFindOrPythonTokens 3、rawExecutableSegmentEnd 2、classifyTokens findDeletesFile 1、iptables/ufw/nft 降级 3、findDeletesFile 2、pythonRemovesFile 1、classifyGit rm 2），precheck.test.ts 5 条 #222 注释（safe find/Python 1、cautious git rm/find/python/trash-put 3、authorized_keys 1）。

## 8. Contract Check
- 单 commit：是。
- 完整 source diff 已读：是（2 files，266 行）。
- 完整 upstream 实现已读：是（2 个目标文件相关区段）。
- 无 grep-only 证据：是。
- 无架构式省略：是。
- 无删除或延迟行为：是（authorized_keys/防火墙降级是显式行为变更，非删除）。
- 无未请求的备用成功路径或 fallback：是。
- 每条可达路径有 owner：是。
- 无残留行为 bug：是。
- 每 patch 中文注释 gate 通过：是（C=29 ≥ 18）。

## 9. Independent Audit
- Pending（累计 audit 待后续批次）。

## 10. Final Verdict

`PASS`（build）— patch built，`git apply --check` PASS on state 0221，materialize PASS 222/222，typecheck PASS 全部 30 workspace，E/C gate PASS（E=117，C=29，need=18，margin 11）。
