# Record 0133-9897db4108a7

## Identity
- Index: **133**
- Source SHA: `9897db4108a7…`
- Current: `.temp/patches/current/0133-9897db4108a7.patch`
- Current SHA-256: `d4da44a192d08e2fb34945c511e3f391f32325938c78aab9948dde4b7d0c7811`
- Bytes: **11233**
- Subject: feat(version): 沿 tip 1.17.x 线协同升号（禁止回写 1.14.40）
- E/C: **E=24 C=4 need>=4**

## Source intent
source 将多包 version 从 1.14.39 升到 1.14.40，并同步 docs/VSIX/vscode 产物名。合同语义是“发布协同 +1”，不是把目标分支钉死到 1.14.40 字符串。

## 上游 / tip@132 基线
- tip monorepo 已运行在 1.17.x-smark 线；opencode 在 #118 后为 1.17.22-smark。
- baseline 是 upstream v1.17.20；把 package 写回 1.14.x 会制造假版本线，破坏 install 指纹与发布声明。
- vscode scripts.vsix 产物文件名嵌入 version，必须与 package.json 同号。

## 三方比较
| 项 | Source | tip 前 | Current |
|---|---|---|---|
| 主版本线 | 1.14.x | 1.17.x-smark | 保持 1.17.x-smark |
| opencode | 1.14.40-smark | 1.17.22-smark | 1.17.23-smark |
| 多数 packages | 1.14.40 | 1.17.20 | 1.17.21 |
| vscode | 1.14.40.0 / vsix | 1.17.22 | 1.17.23 + vsix 名同步 |
| docs | 写 1.14.40 | 写 1.17.22 | 写 1.17.23 |

## 适配算法
对 original 文件列表中每个 package.json：读取 tip 当前 version，patch 位 +1；替换 vsix 文件名中的旧号；docs 内同步替换 1.17.22→1.17.23 与 1.17.20→1.17.21 的文档引用。绝不插入 1.14 字符串。

## Current 文件
docs/VSIX-Packaging.md；packages/{app,console/*,core,desktop,enterprise,function,opencode,plugin,sdk/js,slack,ui,web}/package.json；sdks/vscode/{package.json,README.md}；packages/core/src/installation/version.ts 注释。

## 注释（C）
installation/version.ts 说明：以 opencode package 为准、禁止 1.14 回写、vsix 必须同号、InstallationVersion 读编译期 OPENCODE_VERSION。

## 验证
- rebuild materialize 到 135 成功
- typecheck-0135-2026-07-23T16-38-21-386Z：30/30
- install fingerprint：b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- tip packages/opencode/package.json version 实为 1.17.23-smark

## 失败修订
1. 全局字符串 replace 曾破坏根 package.json 的 JSON 语法 → 改为只替换顶层 "version" 字段。
2. E/C 中 C 不足 → 充实 version.ts 中文决策注释。
3. dual audit 指出 record 垫片 → 本文件为完整有效信息，无重复块。

## 与相邻项
#118 建立 1.17 线规则；#133 再 +1。不改运行时行为，只改发布元数据与文档。

## 最终
版本协同升号完成，无 1.14 回归，E/C 达标，指纹已更新。


## 发布不变量
1. packages/opencode/package.json 的 version 是用户可见主版本。
2. sdks/vscode package.json 与 scripts.vsce 输出文件名必须同号，否则安装包与市场版本声明不一致。
3. InstallationVersion 在编译期注入；源码注释记录期望号，防止文档漂移。
4. 不得出现 1.14.x 字面量，避免与 tip baseline 混线。

## 文件级变更摘要
每个 package.json 仅改动顶层 version 字段及嵌入 version 的 vsix 文件名。docs 替换的是文档中的版本引用，不是代码逻辑。console/mail 等包随 original 列表同步 +1，即使运行时较少直接暴露，也保持 monorepo 版本集合一致。

## 风险与非目标
- 不修改依赖 semver 范围（除非 version 字段本身）。
- 不重新生成 lockfile 内容以外的业务代码。
- install fingerprint 变化是预期结果，不是失败。


## 逐包决策说明
opencode 使用 -smark 后缀版本，反映 SMARK 发行通道。app/ui/web/desktop 等前端包与 core 同步 +1，避免 workspace 协议解析时出现“主包新、依赖包旧”的文档混乱。console/* 与 function 虽偏内部服务，仍在 original 133 列表中，故一并升号。sdk/js 版本影响客户端生成物声明，需同步。plugin/slack/enterprise 同理。

## 文档同步策略
VSIX-Packaging.md 与 vscode README 中的版本号是人类发布检查清单的一部分。只改数字，不改打包步骤命令结构，除非命令内嵌旧版本文件名。

## 批 131-135 共用验证指纹
- tip state: states/0135-3bed2cfd5377
- typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- prior 126-130 dual PASS

## Dual independent audit (batch 131-135)
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Residual closed: #132 write SessionPath.relative; #131 CR/CRLF tests; records substance; viewportCulling top-level; version 1.17.23-smark
- Typecheck: typecheck-0135-2026-07-23T16-38-21-386Z 30/30
- Install: b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977
- Tip: states/0135-3bed2cfd5377
- Batch verdict: **PASS** — may advance to 136-140
