# Record 0081 — `85442a1f1e68`

**Subject:** feat(notebook): 虚拟行号与 source 渲染  
**Index:** 81  
**Parent:** see `manifest.tsv`  
**Current:** `current/0081-85442a1f1e68.patch`  
**Original:** `original/0081-85442a1f1e68.patch`  
**Current SHA-256:** `feb0fea9a86c8ca056c7e058820e5a68d5b8a7b4ba22eb65d1fc52d0d54be8bb`  
**Current bytes:** 6744  
**Author:** SMARK (manifest)

## 1. Source intent (from original patch)

1. Virtual ranges: separators unnumbered; `next += Math.max(1, count)`.
2. Source render via `document.lineAt(i)` instead of `getText().split`.
3. Drop blank separator numbered lines and EOF footer.
4. Summary conventions mention unnumbered headers/separators.
5. Remove residual skip-path `globalLineNum = cellRange.end` after dropping accumulator.

## 2. Upstream / pre-state (post previous index)

Post-#80 required filePath on source/summary; virtual ranges still counted blank separators as numbered lines.

## 3. Three-way adaptation

Surgical format/source/summary edits on state-80. Keep for-loop braces balanced; no reintroduction of outer `let globalLineNum`.

### Files in current

- `sdks/vscode/src/notebook/format.ts`
- `sdks/vscode/src/notebook/source.ts`
- `sdks/vscode/src/notebook/summary.ts`


## 3b. Hunk map (current)

1. `format.ts` — `computeVirtualRanges` step becomes `Math.max(1, count)`; docs state separators unnumbered.
2. `source.ts` — remove outer `globalLineNum` accumulator; per-line `globalLineNum = cellRange.start + i`; render via `lineAt`; drop blank separator emission and residual skip-path write to undeclared identifier; lastRenderedLine uses `Math.max(lastRenderedLine, min(globalEnd, totalLines))`.
3. `summary.ts` — conventions string documents unnumbered headers/separators.

## 3c. Invariants checked in state-84

- `next += Math.max(1, count)` present in format.
- No `globalLineNum = cellRange.end` residual.
- Brace balance in source.ts: equal open/close.
- `lineAt` used for cell source lines.

## 4. Final behavior in state-84

Virtual document line numbers track source lines only; empty cells keep a point range; source export uses lineAt; skip path only continues.

## 5. E / C (this current only)

- **E = 11**
- **C = 5** (unique ≈ 5)
- **need ≥ 2**
- **Gate:** PASS


## 6. Verification

- Cumulative materialize through **84**: `states/0084-7759649fd14d`
- Typecheck report: `reports/typecheck-0084-2026-07-22T00-40-33-686Z.md`
- Install fingerprint: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Prior batch 75–79 dual PASS closed
- No empty current; `original/` untouched; no `current/_*.patch`

## 7. Failures and revisions

- Rebuilt against post-#79 notebook + V2 plugin/provider layout when early currents failed apply.
- Closed known defects: resolve double-export (#80), source residual globalLineNum (#81), vscode import + registry (#82), NetworkProxy call-site rewires (#83), routeRequest arity + fileLocks + endCellId order + URI waiter + summary wording (#84).
- Record rewritten with numeric E/C (no provenance padding spam).

## 8. Verdict

**PASS (pending independent dual audit batch 80–84)**

## Fix note (dual-audit B)

- Agent-facing Conventions string restored to clean English ending at `unnumbered.`
- Chinese rationale moved to code comment **outside** the template string (not in payload).

## Dual independent audit (batch 80-84)

- **FORWARD:** PASS (all five, no open B/G) — ses_078a2a765ffexXTi63g11fBQJa
- **REVERSE:** PASS (all five, no open B/G) — ses_078a2a74effes7VUOaxpmbOGtW
- **State:** `states/0084-7759649fd14d`
- **Typecheck:** `typecheck-0084-2026-07-22T01-06-33-597Z.md` monorepo packages passed
- **Install:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Batch verdict:** PASS
