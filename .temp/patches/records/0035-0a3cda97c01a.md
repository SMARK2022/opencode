# Record: Patch 35 — 0a3cda97c01a

## 1. Manifest Entry
- **Index**: 35
- **SHA**: 0a3cda97c01a91b6da6ed3bca4b64cb4059de9ee
- **Parent**: 0034-90b112f8bbf3
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 更新滚动视图组件，增强可视化和交互性，支持动态展开和收缩

## 2. Source Behavior (original patch, 6 files)
1. `routes/session/index.tsx`: 滚动条配置（scrollbar_visible→scrollbar_enabled, false→true），padding/color 调整，Write/Edit/ApplyPatch 展开收缩
2. `message-part.tsx`: toolDefaultOpen 添加 return undefined
3. `scroll-view.css`: thumb 样式更新（right/cursor/width）
4. `scroll-view.tsx`: data-visible 字符串化，z-index→pointer-events
5. `session-turn.css`: ScrollView/非 ScrollView 滚动行为分离
6. `session-turn.tsx`: div→ScrollView 迁移

### 行为目的
- **滚动条默认启用 invariant**: scrollbar_enabled=true 改善长输出可读性
- **Write 展开收缩 invariant**: 超过 10 行时默认收缩，点击展开
- **滚动条交互 invariant**: pointer-events=auto 允许拖拽，data-visible 字符串匹配 CSS 选择器
- **滚动行为分离 invariant**: ScrollView 使用自身滚动，非 ScrollView 保留原生 overflow-y: auto

## 3. 上游 (v1.17.20) 行为
- V2 路径迁移: `cli/cmd/tui/` → `tui/src/`，`ui/` → `session-ui/`
- V2 Shell(Bash) 组件已有展开收缩（patch 28 添加）
- V2 Write/Edit/ApplyPatch 组件无展开收缩
- V2 session-turn.tsx 使用 `<div>` 而非 `<ScrollView>`
- V2 session-turn.css 有 `overflow-y: auto`

## 4. 目标适配 (current patch, 5 files)
### 路径迁移
- `packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx`
- `packages/ui/src/components/message-part.tsx` → `packages/session-ui/src/components/message-part.tsx`
- `packages/ui/src/components/session-turn.css` → `packages/session-ui/src/components/session-turn.css`

### 适配的文件
1. `index.tsx`: 滚动条配置 + Write 展开收缩（V2 Write 无此功能，需从零构建）
2. `message-part.tsx`: toolDefaultOpen return undefined
3. `scroll-view.css`: thumb 样式更新 + 2 条中文注释
4. `scroll-view.tsx`: data-visible 字符串化 + pointer-events + 2 条中文注释
5. `session-turn.css`: overflow-y: auto（修复 B-01: 原错误使用 overflow: hidden）+ 1 条中文注释

### 跳过的文件
- `session-turn.tsx`: V2 使用 `<div>` 而非 `<ScrollView>`，div→ScrollView 迁移需要深度重构 V2 的 autoScroll 机制。通过 CSS 的 `:not(.scroll-view)` 选择器保留非 ScrollView 的原生滚动行为，不需要迁移 TSX。

### 修复 B-01
- 原错误: `:not(.scroll-view)[data-slot="session-turn-content"] { overflow: hidden; }` 覆盖了 V2 的 `overflow-y: auto`，导致滚动失效
- 修复: 改为 `overflow-y: auto; scrollbar-width: none;`，保留原生滚动

### 修复 B-02
- V2 Write 组件无展开收缩，添加 expanded 信号 + collapsed memo + limited memo + onClick + "Click to expand/collapse" 提示

## 5. E/C 计算
- **E = 50**（代码行）
- **C = 8**（中文注释）
- **最低 C = max(1, ceil(50 * 0.15)) = 8**
- **8 ≥ 8，通过**

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 35 --rebuild`: passed 35/35
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/session-ui:passed, packages/tui:passed, packages/ui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`

## 7. 失败修订历史
1. **首次 apply 失败**: 路径 `packages/opencode/src/cli/cmd/tui/` 不存在
   - 修复: 迁移路径
2. **首轮审计 B-01**: session-turn.css `overflow: hidden` 破坏滚动
   - 修复: 改为 `overflow-y: auto; scrollbar-width: none;`
3. **首轮审计 B-02**: Write 展开收缩被丢弃
   - 修复: 为 V2 Write 组件添加 expanded/collapsed/limited 逻辑
4. **首轮审计 G-03**: C=1, 需要 C>=3
   - 修复: 添加 8 条有意义的中文注释
5. **typecheck 失败**: JSX 属性内联注释破坏语法
   - 修复: 注释移到属性行之前

## 8. 最终行为
- 滚动条默认启用，样式更新（right/cursor/width/padding/color）
- Write 组件支持展开收缩（超过 10 行默认收缩）
- scroll-view.tsx 的 data-visible 字符串化 + pointer-events 启用
- session-turn.css 保留原生滚动行为（overflow-y: auto）
- V2 路径和结构保留
