# Record 0186 — 89c4f7d15a48

- Index: 186
- Source SHA12: 89c4f7d15a48
- Batch: 186-190
- Updated: 2026-07-26T18:20:02Z
- Builder self-check: READY-FOR-AUDIT

## 1. Source intent

Optimize TUI message rendering under high-frequency streaming. The classic SMARK CLI TUI applied each `message.part.delta` event directly to the Solid store, causing per-fragment reactive churn (tokenAccounting/context memos recomputing thousands of times per tool call).

Source: `packages/opencode/src/cli/cmd/tui/context/sync.tsx`

## 2. Tip architecture (different owner)

Tip moved the CLI TUI to `packages/tui`. The sync context at `packages/tui/src/context/sync.tsx` already handles `message.part.delta` with a `#157` pending-tool raw path (raw lives under `part.state`, not top-level `part[field]`). The event handler uses a local `search()` binary search and `touchPart()` for active-part tracking.

EventMessagePartDelta type is exported from `@opencode-ai/sdk/v2` (verified in `packages/sdk/js/src/v2/gen/types.gen.ts`).

## 3. Adaptation (source → tip mapping)

| Source | Tip |
|---|---|
| `packages/opencode/src/cli/cmd/tui/context/sync.tsx` | `packages/tui/src/context/sync.tsx` |
| `Binary.search` | local `search()` |
| `packages/opencode/test/cli/cmd/tui/sync.test.tsx` | `packages/tui/test/cli/cmd/tui/sync.test.tsx` |
| `Global.Path.state` test isolation | tip `tmpdir()` + `kv.json` (tip fixture pattern) |
| English comments | Chinese `#186` decision comments |

## 4. Implementation (hunk map)

1. Import `EventMessagePartDelta` + `onCleanup`.
2. After `connectedOnce`, add `pendingPartDeltas` queue + `pendingPartDeltaTimer`.
3. `targetsSamePartDelta`: same session/message/part/field — safe to merge.
4. `coalescePartDeltas`: linear scan, merge adjacent same-key deltas, preserve first id, concatenate delta text.
5. `applyPartDelta`: extract the `#157` raw-path logic from the inline case into a reusable function; preserves `touchPart` + pending-tool `state.raw` + top-level `part[field]`.
6. `flushPartDeltas`: clear timer, coalesce, `batch()` apply all.
7. `enqueuePartDelta`: push + 16ms timer (one frame).
8. In `event.subscribe`: `if (event.type !== "message.part.delta") flushPartDeltas()` before switch — non-delta boundaries see complete text.
9. `case "message.part.delta"`: replace inline body with `enqueuePartDelta(event)`.
10. `onCleanup`: clear timer + drop queue (no flush — component torn down).

## 5. Tests (sync.test.tsx)

- `applies queued streaming part deltas`: 3 deltas "hel"+"lo"+" world" → `state.raw === "hello world"`.
- `flushes queued part deltas before non-delta event boundaries`: 2 parts, alternating deltas, vcs.branch event forces flush → part_1 raw "ac", part_2 raw "b", branch "flush".

Tests use tip fixture (`mount`, `emit`, `wait`) with `directory`/`worktree` from `sync-fixture`.

## 6. Verification

- materialize `passed 190/190`; tip `states/0190-8d2352918cad`.
- typecheck `typecheck-0190-2026-07-26T18-17-46-010Z.md`: all workspaces passed; install `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`.
- `bun test sync.test.tsx` → 4 pass (2 pre-existing + 2 new).

## 7. LIVE IDENTITY

- Current: `.temp/patches/current/0186-89c4f7d15a48.patch`
- Bytes: 13289
- SHA-256: `bf4153b7ec92e89f6f77ab8cfeb357203f49e19e4d16c04089d22b6437e8917f`
- E=185, C=29, need=28, gate=PASS
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Tip: `states/0190-8d2352918cad`
- Focused tests: sync 4 pass / 0 fail

## DUAL-AUDIT SEAL

- Sealed: 2026-07-26T18:23:37Z
- FORWARD: BATCH PASS (ses_060825b0fffed22nrlffLRoNo5) — no open B/G
- REVERSE: BATCH PASS (ses_060825af9ffe2U4eGLGAoe5q9a) — no open B/G
- Tip: `states/0190-8d2352918cad`
- Typecheck: `typecheck-0190-2026-07-26T18-17-46-010Z.md`
- Install: `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Focused tests: sync 4 pass; pending-tool-input 8 pass; session layout+spacing 6 pass; total 18/0
- Non-blocking N only (do not reopen seal): N-01 createPendingToolInputStats comment drift; N-02 frame harness adapted to headline contract; N-03 read.txt bullet placement; N-04 #190 E/C borderline; models-snapshot omission justified
