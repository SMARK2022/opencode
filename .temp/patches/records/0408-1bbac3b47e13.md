# Record 0408-1bbac3b47e13

## Identity
- manifest parent: 6713d0bbac13 (0407)
- subject: chore(vscode): 升级扩展版本并同步公开文档
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0408-1bbac3b47e13` (passed 408/408)
- current patch: `.temp/patches/current/0408-1bbac3b47e13.patch` (26011 bytes LF, 4 files applied / 21 skipped)

## Source intent
SMARK 提交把 VS Code 扩展从 1.15.5 升级到 1.15.10（追赶 CLI 1.15.10-smark），
并同步全部公开文档以描述新增的 VS Code 语言服务能力（LSP bridge）：
- `sdks/vscode/package.json`：description 增加 "VS Code-backed language
  intelligence"；version 1.15.5→1.15.10；keywords 增加 `lsp`、`language-server`。
- `README.md`：VS Code Notebook 段升级为"VS Code 语言服务与 Notebook 集成"，
  版本引用 1.15.5→1.15.10、CLI 1.15.5-smark→1.15.10-smark，新增"扩展不会捆绑
  language server"段落与回退语义；`vscode_notebook_env` 行增加 stop/create/save。
- `docs/VSIX-Packaging.md`：版本表重构（Repository extension version /
  Recommended CLI / Default version source / Local VSIX name / CI VSIX name）、
  `bun install --frozen-lockfile`、`vsce@^3`、build-vsix.yml 说明、smoke test
  增加语言服务步骤、Publishing/Versioning 章节重写（本地 vs CI 发布路径差异、
  独立版本化）、Source Layout 增加 lsp.ts/source.ts。
- 21 个 `docs/readme/README.*.md` 翻译文件：版本徽章 1.15.7→1.15.10、dev-smark
  分支说明、安装命令版本、VS Code 段落同步。
- `sdks/vscode/README.md`：完整同步语言智能能力表、Language Intelligence 章节、
  `/lsp/*` HTTP 端点表、Security/Limits/Troubleshooting 增补、Source Layout。

## Source patch anatomy (25 files)
- README.md: 3 hunks（分支说明 / 对比表行 / VS Code 段落 + env 行）。
- docs/VSIX-Packaging.md: 10 hunks（版本表、frozen-lockfile、build-vsix 说明、
  vsce@^3、smoke test 4-8、Security 两行、Publishing、GitHub prerelease 说明、
  Versioning、Source Layout）。
- 21 × docs/readme/README.*.md: 每个 4 hunks（badge、分支说明、安装命令、
  VS Code 段落 + env 行），内容为 SMARK 品牌/版本同步。
- sdks/vscode/README.md: 11 hunks（intro+版本表、What It Adds、capability 表、
  Prerequisites、Quick Start 示例、env 行、registry 段落、capabilities 行 +
  Language Intelligence 新章节、HTTP 端点表、Security、Limits、Source Layout、
  Troubleshooting）。
- sdks/vscode/package.json: 2 hunks（description+version、keywords）。

## Target mapping (state 0407)
目标仓库在 0407 状态下的差异：
1. **README.md**：目标根 README 是上游 anomalyco 版（无 SMARK 分支说明、无对比
   表），但 0267 已把"VS Code Notebook 集成"段落（103-118 行）作为新内容插入
   ——该段落正是 0408 源补丁要修改的目标。因此 README 的 3 个 hunk 中：
   - 分支说明 hunk（源 @@ -44）与对比表行 hunk（源 @@ -184）在目标中无对应
     内容 → SKIP（0287/0363 先例：SMARK 品牌段从未移植）。
   - VS Code 段落 hunk 在目标中存在 → 应用，按目标版本线改写。
2. **docs/VSIX-Packaging.md**：目标为 0267 重写版（179 行），与源 pre-image
   结构同源（版本表、Clean Build、Publishing、Versioning 等章节一一对应），
   10 个 hunk 全部可映射，逐 hunk 语义应用。
3. **21 个 docs/readme/README.*.md**：目标全部为上游 anomalyco 品牌（opencode.ai
   徽章、anomalyco/opencode 链接、无 SMARK badge/dev-smark/1.15.x 字符串），
   源补丁的翻译变更全部是 SMARK 版本与品牌内容 → 全部 SKIP（0287/0363 先例
   明文：SMARK 品牌/版本段落从未移植到目标翻译文件）。
4. **sdks/vscode/README.md**：目标为 0267 重写版（237 行），与源 pre-image 同源
   ——11 个 hunk 全部可映射，逐字应用（除版本号）。
5. **sdks/vscode/package.json**：目标 version=1.17.27（0253 协同升号线）、
   description 为 0218 适配版（"Local HTTP bridge extension..."）、keywords
   顺序不同（jupyter/kernel/opencode/bridge/notebook/ide）。

## Key port decisions
1. **版本映射（0408 决策）**：源扩展 1.15.5→1.15.10 追赶 CLI 1.15.10-smark；
   目标 CLI 为 1.17.31-smark（0396 协同升号），目标扩展为 1.17.27。按源提交的
   语义（扩展版本 == CLI 版本，两者独立版本化），目标扩展 1.17.27→**1.17.31**
   （追赶目标 CLI 1.17.31-smark），README/VSIX-Packaging/vsc README 的
   "Marketplace 可能仍显示" 旧版本引用写为 1.17.27。0253 的"vscode 滞后
   opencode 一个 minor"关系在此被源提交的追赶语义取代，记录在案。
2. **README.md 应用而非跳过**：0287/0363 的"README 跳过"先例针对的是目标
   README 无 SMARK 内容的情形；0267 已把 VS Code 段落插入目标 README，因此
   0408 对该段落执行语义更新（非跳过）。分支说明/对比表两 hunk仍按先例跳过。
3. **package.json description 适配**：源 description 是 SMARK 英文版（"SMARK
   OpenCode IDE bridge for VS Code..."），目标 description 是 0218 适配版
   （"Local HTTP bridge extension for OpenCode IDE..."）——按源补丁的语义增量
   （在 notebook operations 前插入 "VS Code-backed language intelligence"）
   应用到目标现有 description 文本，而非整句替换。
4. **keywords 插入位置**：源在 notebook 前插入 lsp/language-server；目标
   keywords 顺序不同，按"lsp、language-server 置于 notebook 之前"语义插入到
   目标序列（jupyter/kernel/opencode/bridge/lsp/language-server/notebook/ide）。
5. **版本注释更新**：目标 package.json 第 6 行 `//` JSONC 注释记录 #218 版本
   说明；0408 升级后追加 "#408 扩展版本追赶 CLI 1.17.31-smark，1.17.27→
   1.17.31"，与 0284/0346/0363/0396 的 release-note 注释惯例一致。
6. **VSIX-Packaging.md 的 Publishing/Versioning 重写**：源补丁把 `bun run
   script/publish` 与 `script/release` 标记为 legacy 并禁止使用（独立版本化
   扩展）；目标文档同源结构，逐段应用；`build-vsix.yml` 工作流在目标 0382 已
   存在（记录在案），文档描述与目标工作流一致。

## E/C audit
E=4, C=1, need=1, PASS。E 全部来自 package.json（description 行、version 行、
2 个 keyword 行）；C 为 `//` 版本注释行（CJK ≥8 字符）。21 个 .md 翻译与 3 个
.md 文档均被 E/C 排除。

## Materialization
- `--materialize 408` → "passed 408/408"，state 发布到 states/0408-1bbac3b47e13。
- 复用前缀 407，仅应用本 patch（4 文件 93+/38-）；git apply --check 与
  --index 一次通过。
- package.json JSON 合法性由 materialize 的 JSON.parse 路径验证（版本号
  1.17.31 与 README/VSIX 文档一致）。

## Line accounting
- README.md: +6/-3；docs/VSIX-Packaging.md: +36/-21；sdks/vscode/README.md:
  +44/-14；sdks/vscode/package.json: +5/-3。合计 93 insertions, 38 deletions。
- fix408.diff 字节 26011（LF）。源补丁 209556 字节：差异主要来自 21 个翻译
  文件（源 ~1400 行）被跳过，以及源补头 metadata。

## Risks / notes
- 版本 1.17.31 是"追赶 CLI"语义的映射决策，与 0253 的"滞后一个 minor"规则
  不同——若后续源提交再 bump 扩展，目标按 +1 继续（1.17.31→1.17.32），
  一致性由 package.json `//` 注释承载。
- 21 个翻译文件跳过意味着目标翻译 README 仍显示上游版本内容（无 SMARK 版本
  徽章），与 0287/0363/0396 的既定目标形态一致，非语义丢失。
- sdks/vscode/README.md 的 Language Intelligence 章节描述了 `/lsp/*` 端点
  与回退语义，对应目标 0378/0400 系列已实现的 bridge LSP 能力（lsp.ts 在
  目标 sdks/vscode/src/ 已存在，文档与实现一致）。

## Verification summary
- E/C gate: PASS (4/1, need 1)。
- Materialize: passed 408/408，无 stoppedAt，integrityFailure 未触发。
- 语义不变量：package.json version=1.17.31 且 `//` 注释更新；keywords 含
  lsp/language-server；README/VSIX/vsc README 三处版本引用一致（1.17.31 /
  1.17.31-smark / Marketplace 旧版 1.17.27）；VSIX-Packaging 与 vsc README
  的 Source Layout 均含 lsp.ts 与 source.ts；env 行 stop/create/save 语义
  三处（README 中文、VSIX-Packaging、vsc README）一致。
- 批量 typecheck 在 0410 结束时统一执行（见 0410 记录）。