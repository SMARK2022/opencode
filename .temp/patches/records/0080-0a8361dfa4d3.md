# Record 0080-0a8361dfa4d3

## 1. Identity

- **Index:** 80
- **Source SHA:** `0a8361dfa4d3d4b5c7cac3f744743c711fcb8cba`
- **Subject:** feat(notebook): 强化笔记本命令和环境解析，优化错误处理和参数验证
- **Parent:** prior notebook batch (79 string-match edit)
- **Current path:** `current/0080-0a8361dfa4d3.patch`
- **Current SHA-256:** `514fb0a45c70aa17585e1a4b76a7b35ac4b6835395d4cd834a1936f11f872e84`
- **Current bytes:** 28725
- **Baseline:** `4473fc3c9055046183990a965d68df3db7ea6f62` (v1.17.20)
- **Source tip:** `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- **Typecheck report:** `typecheck-0084-2026-07-22T00-40-33-686Z.md`
- **Install fingerprint:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`

## 2. Three-way design

### Upstream (post-#79 target notebook stack)

Upstream already has VS Code notebook extension handlers under `sdks/vscode/src/notebook/*` with resolve-by-filePath, run/edit/source/summary/output/env, and cell identity via `#VSC-*` ids after earlier SMARK notebook work. Run historically accepted broader targets (`all`, nested payload shapes) and could fall back to the active notebook when `filePath` was missing. Wait semantics in early target paths were weaker (timing-only or success-optional depending on intermediate currents).

### Source / SMARK (original #80)

Original removes run `all`, flattens CLI/plugin payload to top-level `type|cellId|endCellId`, makes `filePath` required on every notebook tool entry, deletes `notebookRange` helper, hardens edit to apply+wait, and makes run wait complete only when `executionSummary.success` is a boolean. Critically, **`endCellId` is the range signal**: if present and distinct from `cellId`, the run expands to an inclusive cell range; reverse order throws. `type` is documentation/validation, not the sole range gate.

### Target adaptation (this current)

Rebuild on post-#79 owners without inventing a second HTTP API:

1. **commands.ts** — quick-pick only `cell|range` (no `all`); messages match flat args.
2. **run.ts** — `resolveRunTarget(notebook, type?, cellId, endCellId)`:
   - normalize ids by stripping leading `#`;
   - if `endCellId` present and `norm(endCellId) !== norm(cellId)` → inclusive range;
   - if caller also set `type === "cell"` with a different end → throw (SMARK #84 guard layered early so cumulative state never silently ignores mismatch);
   - reverse range (`end.index < start.index`) throws;
   - wait: pre-register `waitForSingleCell` → execute → `wait.promise`; complete only when `typeof success === "boolean"`; timeout writes SMARK timed-out `exec` text and `failedIndex`.
3. **resolve/env/output/source/summary** — required `filePath`; no active-notebook fallback.
4. **edit.ts** — `applyNotebookEditAndWait` / `applyTextEditAndWait` primary path.
5. **resolve.ts** — drop `notebookRange` export (orphaned JSDoc may remain; no callers).

URI identity for waiters is retained as a target strengthening on the same success-boolean path (not a timing-only alternate).

## 3. Files in current

- `sdks/vscode/src/notebook/commands.ts`
- `sdks/vscode/src/notebook/edit.ts`
- `sdks/vscode/src/notebook/env.ts`
- `sdks/vscode/src/notebook/output.ts`
- `sdks/vscode/src/notebook/resolve.ts`
- `sdks/vscode/src/notebook/run.ts` (**range signal + wait + type=cell mismatch throw**)
- `sdks/vscode/src/notebook/source.ts`
- `sdks/vscode/src/notebook/summary.ts`

## 4. Final behavior in state-84

- Range without explicit `type:"range"` works when `endCellId` differs from `cellId`.
- Explicit `type:"cell"` + different `endCellId` throws.
- Reverse range throws; success-boolean wait + timeout exec text + `failedIndex`.
- Required `filePath` on notebook tools; edit apply+wait; no `all` run target.

## 5. E / C (this current only)

- **E = 169**
- **C = 29** (unique ≈ 27)
- **need ≥ 26**
- **Gate:** PASS


## 6. Verification

- Materialize cumulative through 84 after this current: OK
- Typecheck `typecheck-0084-2026-07-22T00-40-33-686Z.md`: monorepo packages passed
- Install reused: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Independent dual audit: pending this revision

## 7. Inconsistencies closed

- Double `export async` on resolveNotebook (earlier): fixed.
- Type-gated-only range that ignored bare `endCellId`: fixed to endCellId-as-signal.
- Timing-only wait without success boolean: fixed.

## Dual independent audit (batch 80-84)

- **FORWARD:** PASS (all five, no open B/G) — ses_078a2a765ffexXTi63g11fBQJa
- **REVERSE:** PASS (all five, no open B/G) — ses_078a2a74effes7VUOaxpmbOGtW
- **State:** `states/0084-7759649fd14d`
- **Typecheck:** `typecheck-0084-2026-07-22T01-06-33-597Z.md` monorepo packages passed
- **Install:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Batch verdict:** PASS
