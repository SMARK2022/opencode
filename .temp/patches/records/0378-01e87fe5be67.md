# Record 0378-01e87fe5be67

## Source patch

- Source: `fix(vscode-notebook): 修复 Notebook 工具 harness 工程缺陷，降低 edit 错误率` (SMARK2022, 2026-07-05)
- Original: `.temp/patches/original/0378-01e87fe5be67.patch` (561 lines, 7 files)
- Replay index: 0378, sha 01e87fe5be67

## Port decision summary

Six surgical improvements to the vscode-notebook tool harness, derived from a full forensics
pass over `opencode.db` (893 tool calls / 89 errors). The goal is a lower edit error rate:

1. **edit response after-source preview** — the edit summary now appends the first 10 lines of the
   post-edit cell source (capped at 4 KB) so the agent can copy an exact `oldCode` anchor for the
   next edit instead of reconstructing text from memory (43% of edit errors traced to oldCode
   drift).
2. **client global request queue removed** — `callBridge` no longer serializes every bridge HTTP
   request through a global Promise queue; the server-side `withFileLock` already serializes per
   filePath, and the old queue let one long `run` (up to 50 min) block all subsequent
   summary/source/edit/env calls across all notebooks.
3. **source auto-expands limit** — when `cellId` is given without an explicit `limit`, the cell
   window expands to the whole cell (max 1000 lines) so a paged read cannot straddle a page
   boundary and break the next edit's oldCode match.
4. **non-string array detection** — `edit` rejects `oldCode`/`newCode` arrays containing
   non-string items before any cell mutation, preventing `sourceProp` from silently returning
   `undefined` (misleading "newCode is required") or a silent full-cell replacement.
5. **configure selectKernel timeout** — the server races `notebook.selectKernel` against a 15 s
   timeout and reports `selection-requested` instead of blocking until the client's 120 s timeout
   aborts with no useful information.
6. **cell_not_found message generalized** — the error text no longer mentions "edit response"
   (the error is shared by edit/run/source/output) and now recommends `vscode_notebook_summary`
   to obtain the current cell map.

All six changes are ported with their accompanying tests (one new concurrency test in
`vscode-bridge.test.ts`, ten new tests in `vscode-notebook-tool-summary.test.ts`).

## State analysis (state 0377)

All seven target files EXIST at the expected paths (no migration needed):

- `packages/opencode/src/ide/vscode-bridge.ts` — the queue machinery is present: the
  `bridgeRequestQueue` declaration (line 101), `callBridge` delegating through
  `enqueueBridgeRequest` (line 249), and the `enqueueBridgeRequest` scheduler (lines 303-315).
  Local comments differ from upstream (this series carries extra #219-era comments), so the
  removal hunks were re-anchored on the code lines themselves.
- `packages/opencode/test/ide/vscode-bridge.test.ts` — structure matches upstream: the describe
  block ends at line 157, `writeEntry` (line 166) writes `active: {}` (line 183), and
  `bridgeServer` (line 189) is the only HTTP helper.
- `packages/opencode/test/plugin/vscode-notebook-tool-summary.test.ts` — imports `bun:test`
  without `spyOn`; `executeCommand` returns hardcoded `true` for `notebook.selectKernel`
  (line 108); the insert test ends at line 290 before the `expectHeader` helper.
- `sdks/vscode/src/notebook/edit.ts` — `EDIT_SYNC_TIMEOUT_MS` at line 32; the two
  `rejectEmptySourceArray` calls at lines 55-56; `rejectEmptySourceArray` helper at line 523;
  the `Preview:` push at line 674 inside `compactEditResult`.
- `sdks/vscode/src/notebook/env.ts` — `PROBE_TIMEOUT_MS` at line 68; the selectKernel
  `executeCommand` block at lines 308-316; the `selection-requested` statusSummary at lines
  446-450.
- `sdks/vscode/src/notebook/resolve.ts` — the cell-not-found throw at line 54.
- `sdks/vscode/src/notebook/source.ts` — `limit` default at line 36; the target-cell window
  block at lines 55-67.

## Edit script (semantic)

1. `vscode-bridge.ts`:
   - Delete the two queue comments + `let bridgeRequestQueue = Promise.resolve()`.
   - Replace `callBridge`'s queue comments + `return await enqueueBridgeRequest(() =>
     callBridgeOnce(input))` with the removal rationale comment + `return await callBridgeOnce(input)`.
   - Delete the whole `enqueueBridgeRequest` function.
2. `vscode-bridge.test.ts`:
   - Insert the "does not serialize concurrent callBridge requests for different files" test
     before the describe's closing brace. It registers two bridge servers bound to different
     active notebooks (via the new `active` writeEntry parameter), blocks serverA's first POST on
     a deferred release, and asserts callB completes while callA is still in flight.
   - Extend `writeEntry` with `active?: { notebook?: string }` and emit `active: active ?? {}`.
   - Add the `postBridgeServer` helper (blocks the first POST whose body filePath contains
     `a.ipynb` until `release()`).
3. `vscode-notebook-tool-summary.test.ts`:
   - Add `spyOn` to the `bun:test` import.
   - Add `let selectKernelResult: unknown = true` (overridable per test) after `executedCommands`.
   - `executeCommand` returns `selectKernelResult` for `notebook.selectKernel`.
   - Reset `selectKernelResult = true` in `beforeEach`.
   - Append ten tests: four after-source preview tests, three non-string array rejection tests
     (including the empty-string oldCode compatibility guard), one source auto-expand test, and
     two configure tests (timeout → selection-requested via a `setTimeout` spy; immediate accept →
     configured via a fake active kernel).
4. `edit.ts`:
   - Add `AFTER_SOURCE_PREVIEW_LINES = 10` and `AFTER_SOURCE_PREVIEW_MAX_CHARS = 4 * 1024`.
   - Add `rejectNonStringSourceArray(input, …)` calls after the empty-array guards.
   - Add the `rejectNonStringSourceArray` and `afterSourcePreview` helpers (CRLF→LF normalized).
   - Append the `AfterSource` block to `compactEditResult` (skipped for delete; distinct
     remaining-lines vs char-truncation messages).
5. `env.ts`:
   - Add `SELECT_KERNEL_TIMEOUT_MS = 15_000`.
   - Wrap the selectKernel `executeCommand` in `Promise.race` with the timeout promise (cleared
     in `finally`).
   - Update the `selection-requested` statusSummary to describe the open-picker case and manual
     recovery.
6. `resolve.ts`: replace the cell-not-found message with the generalized text recommending
   `vscode_notebook_summary`.
7. `source.ts`: introduce `requestedLimit`; when `cellId` is set and `requestedLimit ===
   undefined`, expand `limit` to the full cell (max 1000 lines).

## E/C gate

`count-ec-auditor.py`: E=215, C=80, need=33 → PASS. Substantive Chinese comments document the
forensics-derived rationale at every behavior point (queue removal, after-source anchor, timeout
semantics, the empty-string compatibility guard), well above the 15% need.

## Artifacts

- Scratch diff: `D:\Temp\opencode\fix378.diff` (29,247 bytes, 7 files)
- Current patch: `.temp/patches/current/0378-01e87fe5be67.patch`
- Materialized state: `0378-01e87fe5be67`

## Materialize

`--materialize 378` → `passed 378/378`, state published at
`.temp/patches/states/0378-01e87fe5be67`. The replay applied 0376→0378 cumulatively; every hunk
was verified with `git apply --check --index` before `git apply --index` in the scratch mirror,
byte-identical to the materialize path.

## Verification

- The scratch mirror applied the patch cleanly against the state-0377 index (which already
  includes 0376/0377), so the published state is exactly state-0377 + this item.
- Type safety concerns checked against local contracts: `spyOn` is exported by `bun:test` in this
  repo's Bun version (used by prior items); `using` declarations and `Symbol.dispose` already
  appear in the local test file; `Promise.race` + `clearTimeout` typing matches the existing
  `waitFor*` helpers; `let limit` reassignment is valid. Full batch-end typecheck is deferred to
  `--typecheck 380`.
- The `setTimeout` spy test uses `mock.restore()` after the await, matching the upstream ordering.

## Notes / deviations from source

- Hunk contexts re-anchored where local comments differ (vscode-bridge.ts queue block,
  env.ts const ordering, source.ts #234 comments) — semantic changes identical.
- One comment typo introduced during porting (`// irst request…`) was corrected to the upstream
  Chinese text before diff generation.
- Nothing skipped; all 7 files ported in full.

## B-01/B-02 修复（Round 1 双审计 BLOCK）
**B-01**：`sdks/vscode/src/notebook/env.ts` 最终 `configureResult({...})` 调用缺 `notebook` 字段（前 3 个调用点都有；source 4 个调用点全传）→ `envSummaryHeader(input.notebook, ...)` 对 undefined 取 `.isDirty` 抛 TypeError → configure 15s 超时路径崩溃。修复：最终调用加 `notebook,`（与 source 逐字一致）。
**B-02**：`sdks/vscode/src/notebook/source.ts` 页脚/截断语义漂移（源自 index-70 时代移植）：
- `if (!bytesCut && !lineLimited)` → `if (!bytesCut && renderedLines < limit)`（source 语义）
- `const more = !bytesCut && lastRenderedLine < globalEnd` → `const more = lastRenderedLine < globalEnd`
- `} else if (more || lineLimited)` → `} else if (more)`（页脚仅在 more 时提示）
- `truncated: bytesCut || more || lineLimited` → `truncated: bytesCut || more`
修复后：auto-expand 返回完整 cell 时不产生误导性 "Use offset=454 to continue"（total=453），header `truncated=false` 与 footer 一致。
**验证**：重新物化 378-380 后 `bun test test/plugin/vscode-notebook-tool-summary.test.ts` 应 20/20；`--typecheck 380` 全绿。

## B-01/B-02 修复验证（补充）
- `bun test test/plugin/vscode-notebook-tool-summary.test.ts`：**20 pass / 1 fail**——B-01/B-02 两个核心测试（"source returns full cell content when cellId is specified without explicit limit"、"configure returns selection-requested when selectKernel does not resolve in time"）均通过；剩余 1 个 (unnamed) hook timeout 为 bun 1.3.14 文件结构级 artifact——仅运行 pre-existing 测试（不含 0378 新增 spy 测试）也复现同款 5.1s hook TimeoutError（R2 逆向审计员 N-04 验证），非本批引入
- `bun test test/ide/vscode-bridge.test.ts`：单独运行 **7/7 pass**（完整进程内与 summary 套件串联时 1 个 (unnamed) hook timeout 为跨套件 spy 泄漏污染，单独运行无）
- `--typecheck 380`：全部 workspace PASS
- E/C：E=220, C=80, need=33 PASS（修复后）

## R13 闭合（本 session，批次 376-380）
- 4 文件 section 重建：vscode-bridge（移除 bridgeRequestQueue/enqueueBridgeRequest + callBridge 直调 + 新语义注释，目标多注释行适配）；edit.ts 常量插入（目标 L31 多一行注释，锚 EDIT_SYNC_TIMEOUT_MS）；env.ts（SELECT_KERNEL_TIMEOUT_MS 常量 + Promise.race 超时（目标用 SELECT_KERNEL_CMD 常量而非字面量）+ resolveConfigureStatus 文案）；source.ts（requestedLimit + cellId 自动扩展）。E=313 C=64
- final：26669B sha256=88685a20bd330cbd49656a1a339bc9c3172d3735717a9a88f9a81b11db086f79；E=313 C=64 阈值 47 通过
- 验证：typecheck 380 全 workspace passed

## R14 修复（2026-09-13 351-400 波审计 B-01/B-02/B-03/G-01/G-03 闭合）
- B-01（双向审计 observed）：R13 重建丢失 edit.ts 三个实现 hunk（死常量 AFTER_SOURCE_PREVIEW_*，测试保留但消费代码缺失，7 用例必败）。修复：从 original/0378 字节级提取三个 added 块（rejectNonStringSourceArray 调用对、两 helper 函数、compactEditResult AfterSource summary 块），程序化重锚至目标 0378 物化内容（锚点：rejectEmptySourceArray(newCode) 后空行 / Apply+wait banner 前 / sourcePreview Preview 块后）
- B-02：source.ts 四处页脚判断修复（R1 声明于 record、R13 重建丢失）回归——!lineLimited → renderedLines < limit；more 去 !bytesCut；else if (more||lineLimited) → else if (more)；truncated 去 || lineLimited。auto-expand 完整 cell 时不再产生误导 "Use offset=" 页脚
- B-03：env.ts :399 最终 configureResult 补 notebook, 字段（envSummaryHeader 对 undefined 取 isDirty 的 configure 路径 TypeError 消除）
- 生成方式：pre=states/0377 实物（LF 归一化）→ post=0378 物化树+三修复 → git diff --no-index 字节精确生成三文件新段 → 程序化拼接替换 current 对应 section（vscode-bridge/两测试段原样保留）
- final：32211B sha256=df7246697e841baf61ecfc92b7c22d8fa999eb2821f93d38392ba617f6eec7a6；E/C 重算：E≈470 C≈92（阈值 71，通过——回补实现同时带来注释载体）
- 验证：materialize 380 passed 380/380（reused 377）；--typecheck 380 全 30 workspace passed（install 883c1a4b reused）；行为测试 tw：vscode-notebook-tool-summary 20 pass / 0 fail（R1 修复后的核心用例全部转绿且 hook timeout 未复现）、vscode-bridge 单独运行通过
