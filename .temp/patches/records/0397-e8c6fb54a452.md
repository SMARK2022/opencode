# Record 0397-e8c6fb54a452

## Identity
- manifest parent: 3cee7ef2b8a4 (0396)
- subject: fix(ci): 跳过 CI 无用二进制下载并固定 ghostty-web 浮动依赖
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0397-e8c6fb54a452` (passed 397/397)
- current patch: `.temp/patches/current/0397-e8c6fb54a452.patch` (1551 bytes raw, 1 file 1 hunk)

## Source intent
Two CI hardening changes on the SMARK fork:
1. `setup-bun` composite action's "Install dependencies" step gains an `env`
   block (`ELECTRON_SKIP_BINARY_DOWNLOAD`, `ELECTRON_SKIP_DOWNLOAD`,
   `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD`, `PUPPETEER_SKIP_DOWNLOAD`,
   `CYPRESS_INSTALL_BINARY=0`, `HUSKY=0`) — aligned with the Windows step in
   build-opencode.yml — so CI jobs skip prebuilt electron/playwright/
   puppeteer/cypress binary downloads and husky hook installation. upstream-e2e
   playwright browsers are installed by a dedicated test.yml step
   (step-level env does not persist across steps), and native postinstall
   (node-pty/tree-sitter) ignores these variables.
2. `ghostty-web` dep pinned from floating `github:anomalyco/ghostty-web#main`
   to immutable commit `513463a6f1190253057e8a3f0dac8f6ee8393553` in both
   `bun.lock` and `packages/app/package.json`, eliminating lockfile churn and
   redownloads when upstream main moves. The pin is the already-resolved
   commit — zero behavior change.

## Source patch anatomy (3 files)
- `.github/actions/setup-bun/action.yml` — 1 hunk, env block for Install deps.
- `bun.lock` — 1 hunk, ghostty-web dep key pin.
- `packages/app/package.json` — 1 hunk, ghostty-web dep entry pin.

## Target mapping (state 0396)
| Source file | Target file | Status |
|---|---|---|
| `.github/actions/setup-bun/action.yml` | same | ADAPTED (env block only) |
| `bun.lock` | `bun.lock` | ALREADY-SATISFIED (see below) |
| `packages/app/package.json` | same | ALREADY-SATISFIED (see below) |

## Already-satisfied verification (bun.lock / packages/app)
Verified at state 0395/0396 with two independent checks:
- `git -C states/0395/.../repo diff -- bun.lock packages/app/package.json`
  produced **empty output** — the working tree matches HEAD for both files,
  meaning the pin predates this replay (it lives in the target baseline
  commit, not in any earlier replayed patch).
- `git show HEAD:bun.lock` at the state repo contains
  `"ghostty-web": "github:anomalyco/ghostty-web#513463a6f1190253057e8a3f0dac8f6ee8393553"`
  (line 68) plus the resolved package entry `anomalyco-ghostty-web-513463a`
  (line ~3848), and `packages/app/package.json` line 84 already carries the
  same pin. A grep for `ghostty-web#main` across `.temp/patches/current/`
  matched only the (now replaced) source-verbatim copy of this patch itself.
Therefore the source's pin hunks are no-ops in the target: emitting them as a
diff would fail `git apply` ("already applied") and add zero semantic value.
Recorded as ALREADY-SATISFIED, not as SKIP-with-precedent (different rationale
from README/CONTEXT skips: this is an *already-achieved* state, not a
context-mismatch).

## Port decision
One-file adaptation: `.github/actions/setup-bun/action.yml`.
- The target action.yml at state 0396 has the identical "Install dependencies"
  step shape (`- name: Install dependencies` + `run: |` + the bun
  `--linker hoisted` workaround comment block), so the source's env block is
  transplanted verbatim between the step name and `run: |` — same position,
  same content, zero text drift.
- The following step lines (Save Bun dependencies, cache steps, setuptools
  step) are untouched — 0400's Windows-cache modification lands on the same
  file in the next item with no overlapping ranges.

## E/C audit
E=7, C=9, need=2, PASS. E: the `env:` key plus 6 assignments
(ELECTRON_SKIP_BINARY_DOWNLOAD, ELECTRON_SKIP_DOWNLOAD,
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD, PUPPETEER_SKIP_DOWNLOAD,
CYPRESS_INSTALL_BINARY, HUSKY). C: 9 `#` YAML comment lines with CJK content
(7 CI-binary comments + 2 husky comments), counted by the auditor's `#` comment
rule. Verified with `count-ec-auditor.py`.

## Materialization
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 397` →
  "passed 397/397", state at `states/0397-e8c6fb54a452`.
- Post-apply verification: action.yml "Install dependencies" step carries the
  full env block (spot-checked lines 56-76 of the state file); bun.lock and
  packages/app/package.json unchanged (already pinned).

## Line accounting
- `.github/actions/setup-bun/action.yml`: +16/-0 (env block insertion).
- `bun.lock`, `packages/app/package.json`: +0/-0 (already satisfied).
- fix397.diff bytes: 1551 (1 file, 1 hunk).

## Risks / notes
- The env variables are scoped to the single composite-action step; they don't
  leak into later steps (execution model of composite actions), so e2e
  playwright install via `playwright install` in test.yml is unaffected — the
  same reasoning as the source commit message.
- `HUSKY=0` returns exit 0 in husky 9; tests that commit inside `os.tmpdir()`
  don't read the root `.husky/` directory.
- No runtime/typecheck impact on any package; covered by batch-end
  `--typecheck 400` (state reuse + workspace typechecks all green).

## Verification summary
- E/C gate: PASS (E=7, C=9).
- Materialize: passed 397/397; env block verified in state.
- Batch typecheck 400: passed 400/400.