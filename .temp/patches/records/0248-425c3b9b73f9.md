# Record 0248-425c3b9b73f9

## Status
PASS

## 修改内容
feat(apply_patch): 更新删除文件处理逻辑，确保在有 patch 数据时显示行数统计，兼容 legacy 数据。

### Source 逐 hunk 行为
1. title() 守卫 `file.type === "delete" || !file.patch` → `!file.patch`：删除文件带 patch 时标题追加 +added -removed（删除场景 +0 -N）。
2. BlockTool preview `when={file.type !== "delete"}` → `when={!!file.patch}`：删除带 patch 走 DiffPreview，legacy 无 patch 保留 -N lines 摘要。
3. 展开态同改：删除带 patch 可完整 DiffView 渲染。
4. 测试：删除+有 patch（标题含 +0/-3、diff 可见）与删除+无 patch（legacy，-N lines）两场景。

### 目标适配（packages/tui/src/routes/session/index.tsx）
- L3416 title 守卫改 `!file.patch`（内联计数已正确排除 +++/---，删除 patch 得 +0 -N）。
- L3454 #144 注释更新为 #248 语义。
- L3461-3462 preview 守卫 `when={!!file.patch}`（JSX 属性区跨行结构：`<Show` 换行 + when 属性行）。
- L3477-3478 展开态同改。
- parseApplyPatchFiles 放宽：仅 delete 允许 patch 缺失（`patch: patch ?? ""` 空串表示 legacy），其余类型无 patch 依旧丢弃；保证 `!!file.patch` 分流 legacy 与真实数据。

### V1/V2 职责说明
- metadata.files 的 legacy 形状由 V1 apply_patch.ts 产出（delete 分支恒含 patch）；V2 core 输出 FileDiff.Info 且删除条目恒含 patch——legacy 分支仅对历史 V1 会话生效，不产生重复 owner 或行为分裂。

### 测试适配
- source 测试宿主 session-message-render.test.tsx 不存在；目标 inline-tool-wrap-snapshot.test.tsx 为 CRLF blob（git apply 无法匹配，与 247 display.test.ts 同因）。
- 新建 `packages/tui/test/cli/tui/apply-patch-files.test.ts`（LF）：parseApplyPatchFiles 三分支（delete 无 patch 保留为空串 / delete 带 patch 保留 / add 无 patch 丢弃）。
- 渲染分支 `!!file.patch` 由 typecheck 覆盖类型正确性；既有 inline-tool-wrap-snapshot 快照 17 项全绿证明 parser 行为无回归。

## E/C
- E = 45（plus - commentPlus + minus）
- C = 7（#248：title 守卫 2、preview 守卫 1、展开守卫 1、parser 放宽 2、测试 1——均落在真实分流契约处）
- need = ceil(45 * 0.15) = 7 → 余 0 PASS

## Source
- original/0248-425c3b9b73f9.patch: 145 行 2 文件。

## Current
- current/0248-425c3b9b73f9.patch: 4117 bytes, 6 hunk / 2 文件
- SHA-256: 9f2e7f20621b7f2684ee29247f5b7c16dfbfc614b7db5d81df9be671abc00632

## 安装
- materialize 248 --rebuild: passed 248/248

## 测试
- typecheck 248: 全部 workspace passed
- `bun test test/cli/tui/apply-patch-files.test.ts`（packages/tui）: 1 pass / 0 fail
- `bun test test/cli/tui/inline-tool-wrap-snapshot.test.tsx`: 17 pass / 0 fail（既有回归全绿）

## 修订历史
1. 初版：测试节误插 parseDiagnostics 调用中间 + hunk 头/缩进多处错位 → 全量重写。
2. JSX 跨行结构认知修正三次：L3453 `<BlockTool`（无 >，属性跨行）、L3461 `<Show`、L3477 `<Show`——read 显示方括号包装造成误判，最终以字节级检查为准。
3. A5 尾部 ctx 笔误（`}]`/` }]` vs 文件 `})`/`}`）修复；起点 3669→3668。
4. 测试宿主 CRLF → 新建 LF 文件；JSX 属性区 {/* */} 注释非法（TS1005）→ 改 // 行注释。
5. 最终：materialize OK、typecheck OK、18/0 测试、C=7=need → PASS。

## 三方比较
- Source：删除文件不再被 type 硬切断路，有 patch 显示统计、无 patch 保留 legacy 摘要。
- Upstream v1.17.20：ApplyPatch 在 packages/tui 且结构不同（内联计数、parseApplyPatchFiles 严格模式）。
- Target：守卫改 !!file.patch + parser 放宽 delete，语义等价落地；无重复 owner、无行为分裂。

## 最终行为
- 删除文件带 patch：标题 +0 -N、preview/展开渲染真实 diff。
- 删除文件无 patch（legacy V1）：标题纯文本、-N lines 摘要。
- 其他类型缺 patch 条目依旧被解析器丢弃。

## 批次双审结论（2026-09-06，245-249）
- 双独立 auditor（升序/降序）一致：本项 PASS，无未关闭 B/G，批次一致放行。
- N 级备注（不阻塞）：N-03 同 0247 的 CRLF 归因措辞；测试 fixture 的 \\n 为字面反斜杠序列（同形比较，敏感度不受损）。