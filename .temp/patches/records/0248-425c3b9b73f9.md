# 0248-425c3b9b73f9 record — feat(apply_patch): 更新删除文件处理逻辑

## Manifest
- index 248, sha `425c3b9b73f9f22b96a861686cccc9c8086a44b0`, parent `9c4cf37be9e4` (0247)
- subject: feat(apply_patch): 更新删除文件处理逻辑，确保在有 patch 数据时显示行数统计，兼容 legacy

## Original 身份
- original/0248-425c3b9b73f9.patch: 2 文件（tui routes/session/index.tsx ApplyPatch 3 处条件、session-message-render.test.tsx 2 个测试）

## Source 行为（逐 hunk）
1. **title()**：`if (file.type === "delete" || !file.patch) return baseTitle` → `if (!file.patch) return baseTitle`。删除文件的 patch 字段（apply-patch 生成的完整 unified diff）不再被短路，有 patch 时标题显示 `+added -removed` 统计。
2. **preview 分支**：`<Show when={file.type !== "delete"}>` → `<Show when={!!file.patch}>`。删除文件有 patch 时 DiffPreview 折叠预览；legacy 无 patch 退回 `-N lines` 摘要。
3. **正文分支**：同款 `<Show when={!!file.patch}>`。有 patch 渲染完整 DiffView；legacy 退回 `-N lines`。
4. **测试**：2 个用例——delete 带完整 patch 断言标题 `+0 -3` + KEEP_THIS diff 真实渲染；legacy 无 patch 断言标题不含统计 + `-5 lines` 摘要。

## 目标适配（Round 1）
- **路径迁移**：`packages/opencode/src/cli/cmd/tui/routes/session/index.tsx` → `packages/tui/src/routes/session/index.tsx`（ApplyPatch @ 3360）。三处改动对应目标 3401/3447/3463。
- **目标 title()** 用内联计数（split/startsWith 统计 added/removed，L3404-3409），无 diffLineStats——只改短路条件，统计逻辑保留目标现有实现。
- **legacy 可达性**：目标 parseApplyPatchFiles（L3648-3661）要求 `patch !== undefined`，无 patch 条目被过滤——但 `patch: ""`（stringValue 返回空串）条目保留，`!!"" = false` → legacy `-N lines` 分支可达。source 的 legacy 兼容语义在目标保持有效。
- **测试 hunk 跳过**：按 0245 先例——目标无 `session-message-render.test.tsx` 及 withRenderedSession 渲染 harness（0245 record 已记录该测试文件目标不存在）。行为由 title() 条件 + 目标既有渲染路径覆盖。

## 生成方式
- gen-248.py：复制 247 状态 repo → 3 处替换 + 1 注释 → git diff。
- current/0248-425c3b9b73f9.patch: 1564 字节, 1 entry, SHA-256: a79442a588fa4e41dac102e0a26cbd457f34785882180029017523267f5e15bf

## 验证
- apply-cumulative --materialize 248: passed 248/248
- tui typecheck 待批次验证

## E/C
- E/C 以 count-ec.py 输出为准（待批次验证时统一确认）

## 状态
- materialized: states/0248-425c3b9b73f9

## E/C 最终（count-ec.py）
- E=3, C=1, need=1, PASS（#248 title 注释）

## 验证（最终）
- apply-cumulative --materialize 250: passed 250/250（累计）
- opencode/tui typecheck 通过


## 补充说明（审计 N-01 收尾）
### 与 0245 先例的一致性
- 0245 record 明确记录：session-message-render.test.tsx 在目标仓库不存在（TUI 测试按 util/component 组织），源渲染测试 hunk 一律跳过；本项沿用同一先例，行为由目标既有渲染路径（DiffPreview/DiffView/BlockTool）承载。
### legacy 兼容语义
- source 的 "有 patch 显示统计、无 patch（legacy）回退 -N lines" 在目标保持：parseApplyPatchFiles 保留 patch:"" 条目（stringValue 对空串返回 ""，仅过滤 undefined），!!"" === false 使 fallback 分支可达；title 的统计逻辑（L3405-3415 内联 split/startsWith 计数）对 delete unified diff 计算 +0 -3 与 source diffLineStats 结果一致。
### 三处条件改动的行为矩阵
| 条件 | source 前 | source 后（目标一致） | 效果 |
|---|---|---|---|
| title 短路 | type==="delete" \|\| !patch | !patch | delete 有 patch 时显示 +added -removed |
| preview Show | type!=="delete" | !!patch | delete 有 patch 走 DiffPreview 折叠预览 |
| 正文 Show | type!=="delete" | !!patch | delete 有 patch 走 DiffView 完整 diff |
- legacy 无 patch/空 patch 时三处均回退 -N lines 摘要（theme.textMuted / theme.diffRemoved fallback 保留目标原样）。
### typecheck 覆盖
- 三处条件改动经 packages/tui typecheck（tsgo --noEmit）验证；ApplyPatch 组件 props 类型 file.patch?: string 兼容 !!file.patch 判定。
