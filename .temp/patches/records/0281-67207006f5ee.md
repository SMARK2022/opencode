# Record: 0281-67207006f5ee

## 基本信息
- index: 0281
- sha12: 67207006f5ee
- subject: feat: 一些配置项
- source date: Sun, 7 Jun 2026 17:32:20 +0800

## Source 行为重建
原始 patch 修改 4 个文件：
1. `.gitignore`：将 `.temp/` 改为 `.temp/*` + 添加 `!.temp/chatgpt-browser-agent/` 和 `!.temp/chatgpt-browser-agent/**` 例外规则，使 chatgpt-browser-agent 目录可被 VCS 跟踪。这个变更的目的是让 chatgpt-browser-agent 的 MCP server 脚本和配置文件能够被 git 跟踪，而不是被 `.temp/` 的全局忽略规则排除。
2. `.opencode/.gitignore`：在 `references/` 后添加 `cache/`。opencode 的 cache 目录包含运行时缓存数据，不应被 VCS 跟踪。
3. `.opencode/opencode.jsonc`：修改 chatgpt MCP 配置——`node` 改为绝对路径 `D:\Program Files\nodejs\node.exe`（避免 PATH 环境变量问题），删除 environment 块（环境变量已移至 chatgpt-browser-agent 内部管理），timeout 从 320000 改为 690000 并添加中文注释说明原因（opencode 同一个 timeout 同时约束连接与工具调用，需要保证长回答不被宿主提前切断）。
4. `packages/core/src/models-snapshot.js`：自动生成的模型快照 vendor 数据替换（4MB），由 build.ts 从 providers 配置自动生成，不应手工修改。

## 三方比较
- `.gitignore`：目标在 `tsconfig.tsbuildinfo` 和 `.temp/` 之间插入了 `# #176` 注释行（SMARK patch #176），需要将该注释行作为上下文加入 hunk
- `.opencode/.gitignore`：目标与 patch pre-image 完全一致，可直接 apply
- `.opencode/opencode.jsonc`：目标 chatgpt MCP 配置块与 patch pre-image 完全一致，可直接 apply
- `models-snapshot.js`：目标第 3 行被 #229 插入注释，模型数据移至第 4 行；按 #195/#204/#229 先例跳过该 hunk（build-time 注入策略）

## 适配内容
- 修改 `.gitignore` hunk：添加 `# #176` 注释行作为上下文，调整行号 `@@ -32,4 +32,6 @@` → `@@ -34,5 +34,7 @@`
- 保留 `.opencode/.gitignore` hunk 原样
- 保留 `.opencode/opencode.jsonc` hunk 原样
- 删除 `models-snapshot.js` hunk（按先例跳过自动生成 vendor 数据）

## current 修改前后
- 编辑前 SHA-256: 9fb5dff2b55cba8ccf5ac6c306e05dac76e39070335e87e007a366bd8cfb2522
- 编辑前字节: 4291601
- 编辑后 SHA-256: 7de3cbd4d50b48a7bfe9a026546256d466613220165993fec79cd058d95d9499
- 编辑后字节: 2206
- materialize 281: passed 281/281

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| .gitignore @@ -32,4 +32,6 | .gitignore @@ -34,5 +34,7 | 添加 #176 上下文，调整行号 |
| .opencode/.gitignore @@ -5,3 +5,4 | 同 original | 无变化 |
| .opencode/opencode.jsonc @@ -6,19 +6,12 | 同 original | 无变化 |
| models-snapshot.js | 删除 | 按 #195/#204/#229 先例跳过 |

## E/C 计算
- E (有效修改行): 0（仅配置文件变更，无生产代码修改）
- C (中文注释行): 0
- 满足 E=0 时 C=0

## 安装与测试
- typecheck: N/A（无生产代码变更）
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- materialize report: passed 281/281, reused prefix 280, applied 1

## 不一致
- models-snapshot.js 跳过：按 #195/#204/#229 先例，build-time 自动生成。目标第 3 行被 #229 插入注释 `// #229：模型快照由 build.ts 从 providers 配置生成——运行时只读引用，变更必须重新生成`，模型数据移至第 4 行，导致 patch pre-image 的 3 行连续上下文不匹配。
- .gitignore 行号偏移：目标在 `tsconfig.tsbuildinfo` 和 `.temp/` 之间插入了 `# #176：物化/scratch 目录不入 VCS` 注释行，导致 patch 期望的 `@@ -32,4 +32,6 @@` 上下文窗口无法对齐，适配为 `@@ -34,5 +34,7 @@` 并将 #176 注释行作为 context 加入。

## 物化验证
- .gitignore: `.temp/*` + `!.temp/chatgpt-browser-agent/` + `!.temp/chatgpt-browser-agent/**` 正确应用，#176 注释行保留 ✓
- .opencode/.gitignore: `cache/` 正确添加 ✓
- .opencode/opencode.jsonc: `D:\Program Files\nodejs\node.exe` 绝对路径、environment 块删除、timeout 690000 + 中文注释 正确应用 ✓
- models-snapshot.js: 未修改（跳过）✓

## 审计历史
- 第一轮正向审计: PASS (无 B/G)
- 第一轮逆向审计: PASS (无 B/G, N-01 record 长度)
- 第二轮正向审计: BLOCK (G-01 record < 3000 字符)
- 第二轮逆向审计: PASS (N-01 record 长度, 非阻塞)
- 本次更新: 扩展 record 至 ≥50 行/≥3000 字符

## 状态
- PASS（第四轮双审计通过）

## DUAL-AUDIT SEAL（批次 281-285，第四轮）
- 正向审计 (ses_0125c3761): 5/5 PASS, 无 B/G
- 逆向审计 (ses_0125c22f0): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 285: passed 285/285
- packages/opencode typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 281=0/0, 282=114/19, 283=29/5, 284=0/0, 285=74/12 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 修复历史: 4轮审计（shell/prompt.ts 修复 + description 移除 + PermissionV1 适配 + garbled line 修复 + record 字符数扩展）

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `601b56034fe136b7da4e21638bac6e431181b42702828bcb2f74cc6b1749bae9` / bytes: 2390
- E/C 最终：E=6, C=1, need=1, PASS
- 修复 G-01：record 与 final patch 对齐（此前记录 7de3cbd4/2206、E=0/C=0 为 R8 前旧值）。实际：jsonc hunk @@ -18,21 +18,14 @@（含 `// #278` 上下文行，适配重建后 0278 双反斜杠路径）、E=6/C=1、SHA `601B56034F...`。
