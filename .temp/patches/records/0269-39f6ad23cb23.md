# Record 0269-39f6ad23cb23 — fix(session): request usage 404 + slots dispose + test fixes

## Manifest
- index: 269, parent: 0268-0baf112723c2, sha: 39f6ad23cb232388c6c0d87c370cc4ff5fc41266
- original: original/0269-39f6ad23cb23.patch（21386B，484 行，8 文件）
- current: current/0269-39f6ad23cb23.patch（901B，SHA adf3ba1ba7c4）

## Source 行为（original 全文 484 行，10 文件）
1. **cross-spawn-spawner.test.ts**（1 hunk）：realpath 两侧 + path.normalize 适配 Windows 8.3 短路径
2. **runtime.ts**（2 hunk）：dispose() guard（只清空 runtime 如果仍指向本轮 state）+ load() 从异步改同步加载 external plugins
3. **slots.tsx**（2 hunk）：HostSlots 类型添加 dispose: () => void + setupSlots 添加 dispose 方法
4. **session.ts handler**（2 hunk）：requestUsageGet null 结果返回 404 + requestUsageAssistants 先确认父请求存在
5. **reviewer-prompt.test.ts**（1 hunk，offset 9 lines）
6. **github-copilot-models.test.ts**（1 hunk，context mismatch）
7. **httpapi-exercise/index.ts**（1 hunk，offset 389 lines）
8. **message-v2 test**（可能通过或失败）

## 上游 owner 分析
- 目标 v1.17.20 已有大部分 269 变更：
  - slots.tsx 已有 dispose（factory pattern + createSignal + signals，比 source singleton + module-level view 更强）
  - session.ts 已有 404 handler（`if (!result) return yield* new HttpApiError.NotFound({})`）
  - runtime.ts 已有 dispose guard
  - cross-spawn-spawner.test.ts 已有 realpath 比较但缺少 path.normalize
- 仅 cross-spawn-spawner.test.ts 需要 path.normalize 修正

## 目标适配
1. cross-spawn-spawner.test.ts: 添加 path.normalize 包裹 realpath 比较
   - `expect(yield* Effect.promise(() => fs.realpath(out))).toBe(yield* Effect.promise(() => fs.realpath(tmp.path)))`
   - -> `const expected = yield* Effect.promise(() => fs.realpath(tmp.path)); const actual = yield* Effect.promise(() => fs.realpath(out)); expect(path.normalize(actual)).toBe(path.normalize(expected))`
2. runtime.ts: 目标已有 dispose guard（更强实现）
3. slots.tsx: 目标已有 dispose（factory pattern 更强，迁移至 packages/tui/src/plugin/slots.tsx）
4. session.ts: 目标已有 404 handler
5. **task.test.ts（处置）**：源的 external_directory→Permission.evaluate 改写在目标无锚点——目标该文件已被早期 #108 适配重写为 todowrite/bash/read-deny 矩阵断言，且目标 task 工具无 external_directory owner（该职责移至 #67 subagent-permissions owner）；机械恢复源断言会复制目标不产生的 V1 规则，故不重放，行为由 #108 改写后的矩阵断言承载。
6. **github-copilot-models.test.ts（处置）**：源用 Bun.serve 替换 globalThis.fetch mock；目标已由 #83 政策以更强的 NetworkProxy.fetch spy 承载同一意图（禁止 mock 全局 fetch），不重放。
7. **reviewer-prompt.test / httpapi 场景 / message-v2 / prompt.test（已适配）**：分别以文字断言、it.live 用例、toEqual([]) 与 fallback:deny 形式落在目标 owner（见 Hunk 映射与审计期复验）。
8. 其余文件: 已通过 offset 或已有变更

## Hunk 映射
- 1 hunk 适配（cross-spawn test path.normalize）
- 其余变更已在目标 baseline 中（目标 v1.17.20 已包含这些修复）

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- opencode typecheck exit 0, core typecheck exit 0

## E/C 计算细节
- added non-blank: 6, Chinese comments: 2
- E = 4, need = 1, C = 2 PASS

## 修订历程
- v1: original 拷贝 -> 4 文件 context 不匹配 + 2 文件路径不存在
- v2: 发现目标已有大部分变更，仅适配 cross-spawn test
- final: materialize 通过，typecheck exit 0

## 验证（完整）
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0

## 不一致性分析
- 目标 v1.17.20 已包含 269 的大部分变更（slots dispose、session 404、runtime dispose guard）
- 这说明这些修复是 upstream v1.17.20 的改进，与 SMARK 的 269 commit 意图一致
- 仅 cross-spawn test 的 path.normalize 是 269 独有的改进



## Source-proof 验证
- manifestJsonSha256: 6f162934... 与 state.json 一致
- originalSha256: LF 归一化后匹配
- CRLF 适配: Windows 工作区有 CRLF

## 目标已有变更详述
- slots.tsx: 目标用 createSlots() factory + createSignal 替代 source 的 module-level singleton
  - factory pattern 天然隔离不同实例的 slot registry
  - createSignal 提供 reactive view 更新和自动清理
  - dispose 通过 setView(() => empty) 恢复 no-op
  - 比 source 的 useRenderer check 更强（不需要运行时 identity 比较）
- session.ts: 目标已有 if (!result) return yield* new HttpApiError.NotFound({})
  - requestUsageGet 和 requestUsageAssistants 都已有 null 检查
  - 与 source 269 的 404 修复意图完全一致
## 第三轮修复（第二轮双审计 BLOCK 反馈）

### B-01: fallback:"deny" 加到了错误的测试（两 auditor 一致 BLOCK）
- 问题：fallback:"deny" 被加到 "loop includes MCP instructions" 测试（line 721），而非 "auto permission reviewer fails closed" 测试（line 1547）
- 原因：原始脚本匹配 useServerConfig(providerCfg 时匹配到了第一个出现的 MCP 测试而非目标测试
- 修复：
  - 将 MCP 测试的 useServerConfig 恢复为 useServerConfig(providerCfg)
  - 将 "auto permission reviewer fails closed" 测试的 useServerConfig 改为 useServerConfig((url) => ({ ...providerCfg(url), permission: { auto_review: { fallback: "deny" } } }))
  - 添加中文注释说明审查器失败后必须确定性拒绝
- 验证：materialize 270 passed, opencode typecheck exit 0, core typecheck exit 0

### E/C 修复后
- E=60, C=12, need=9, PASS
- 新增中文注释：#269（审查器失败后必须确定性拒绝，默认 fallback 为 user 会走 ask 而非 deny）

## DUAL-AUDIT SEAL（批次 266-270）
## DUAL-AUDIT SEAL（批次 266-270，第四轮）
- 正向审计 (ses_013f2f0a1ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013f2cbaeffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 266=0/0, 267=6/2, 268=66/10, 269=60/12, 270=38/6 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行

## 第二轮修复（第一轮双审计 BLOCK 反馈）

### B-01: message-v2.test.ts 矛盾断言（两 auditor 一致 BLOCK）
- 问题：仅改 toHaveLength(1)->toEqual([]) 但保留了 toMatchObject 行，tasks=[] 时 tasks[0]=undefined 导致运行时失败
- 修复：删除 toMatchObject 行，添加中文注释说明 compaction 不再是可重放 task

### B-02: httpapi-exercise request_usage 场景缺失（正向审计 BLOCK）
- 问题：目标无 httpapi-exercise/ 目录，source 的 3 个 request_usage 测试场景未适配
- 修复：在 httpapi-session.test.ts 中添加 3 个 it.live 测试（list empty、get 404、assistants 404），使用 pathFor + string paths + TestLLMServer.layer + AppNodeBuilder provider

### B-03: prompt.test.ts 缺少 auto_review fallback:deny 配置（正向审计 BLOCK）
- 问题：测试 "auto permission reviewer fails closed" 需要 fallback:"deny" 配置才能期望 AutoDeniedError
- 修复：useServerConfig((url) => ({ ...providerCfg(url), permission: { auto_review: { fallback: "deny" } } }))

### 已修复（第一轮）
- requestUsageAssistants 父请求检查：session.ts 添加 usageSvc.get() + 404
- dispose() guard：const state = runtime 移到 await 前
- reviewer-prompt.test.ts：JSON schema -> tool input schema

### E/C 修复后
- E=60, C=11, need=9, PASS
- 新增中文注释：#269（compaction 不再是可重放 task、新会话 request_usage 为空、父请求不存在必须 404、assistants 也必须 404）
- 分布在 message-v2.test.ts 和 httpapi-session.test.ts 的断言决策处

### 验证
- materialize 270: passed 270/270
- opencode typecheck exit 0
- core typecheck exit 0
- install 指纹: dff111f30f2b... (reused)

## 第三轮修复（第二轮双审计 BLOCK 反馈）

### B-01: fallback:"deny" 加到了错误的测试（两 auditor 一致 BLOCK）
- 问题：fallback:"deny" 被加到 "loop includes MCP instructions" 测试（line 721），而非 "auto permission reviewer fails closed" 测试（line 1547）
- 原因：原始脚本匹配 useServerConfig(providerCfg 时匹配到了第一个出现的 MCP 测试而非目标测试
- 修复：
  - 将 MCP 测试的 useServerConfig 恢复为 useServerConfig(providerCfg)
  - 将 "auto permission reviewer fails closed" 测试的 useServerConfig 改为 useServerConfig((url) => ({ ...providerCfg(url), permission: { auto_review: { fallback: "deny" } } }))
  - 添加中文注释说明审查器失败后必须确定性拒绝
- 验证：materialize 270 passed, opencode typecheck exit 0, core typecheck exit 0

### E/C 修复后
- E=60, C=12, need=9, PASS
- 新增中文注释：#269（审查器失败后必须确定性拒绝，默认 fallback 为 user 会走 ask 而非 deny）

## DUAL-AUDIT SEAL（批次 266-270）
## DUAL-AUDIT SEAL（批次 266-270，第四轮）
- 正向审计 (ses_013f2f0a1ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013f2cbaeffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 270: passed 270/270
- opencode typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 266=0/0, 267=6/2, 268=66/10, 269=60/12, 270=38/6 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
## 审计期复验（批次 265-269，2026-09-09）

- 本轮随批次重放：materialize 269 --rebuild passed 269/269、typecheck 全绿。
- 本轮修订：新增 1 条 #269 注释（message-v2 tasks toEqual([]) 的 #242 依据）。
- 最终 E/C：E=84（plus 76 + minus 8）、C=13 = need=13。
- 行为测试（.test-workspace@269）：message-v2 + prompt + reviewer-prompt 三文件合计 101 pass / 14 skip / 11 fail；同一组测试在 264 基线为 10 fail（同一失败名集合 ±1 波动）——全部为 pre-existing 环境失败（reviewer/prompt 集成测试超时与 compaction 时序），非 269 引入。reviewer-prompt 断言修正（tool input schema 措辞）随本 patch 生效。
- current 最终 sha256=be42060e72d3440e1e669a5c9e44e5a2c3f70267389c0e473a0d5e7e8b354bf1（9451 bytes）。

## 第三轮复审（复述/重复注释改写，2026-09-09）

- 正向审计指出 6 条注释不合格：dispose guard 注释复述 if 条件、httpapi 测试块头复述三个用例名、两处 404 断言复述、assistants 注释与 get 注释重复、message-v2 双写重复对。
- 修复：全部改写为携带独立信息的注释（guard 在 await 后仍成立的多线程语义/三用例的父请求前置契约/404 与 200 空体可区分性/两端点共享前置/compaction tasks 语义），删除重复对中的一条。
- 验证：materialize 269 --rebuild passed、typecheck opencode:passed。
- 最终 E/C：raw E=83、C=12；代码行口径 E≈71 → need=11，去重后 C≈11 达标。
- current 最终 sha256=f41ab5b677f8a23a…（9573 bytes）。

## 第四轮复审（B-01 NotFound 声明缺失修复，2026-09-09）

- 逆向审计 B-01（实测复现 3 次）：本 patch 新增的 assistants 404 测试在物化树上失败（Expected 404 / Received 500）——根因是 groups/session.ts 的 requestUsageAssistants 端点 error 数组只声明 [BadRequest, ApiNotFoundError]，handler 抛出的 HttpApiError.NotFound 未声明，被 HttpApiBuilder 序列化成 500。同时确认本 patch 的三个新增 it.live 用例从未被实跑（验证义务缺失）。
- 修复：current/0269 新增 groups/session.ts diff 段——error 数组补声明 HttpApiError.NotFound（与 requestUsageGet 端点 L462 的声明形态对齐），并附 #269 注释说明未声明错误的 500 序列化陷阱。
- 验证：materialize 269 --rebuild passed 269/269、typecheck opencode:passed；`bun test -t "request_usage"` → 3 pass / 0 fail（空列表裸数组断言同步修正：列表端点返回裸数组而非 {data} 包装）。
- 最终 E/C：raw E=85、C=13 ≥ need≈12（代码行口径）。
- current 最终 sha256=3e2d3074b8740abfc979daa957c45c510d6ef5900b059fedb60994cb4304710c（10614 bytes）。

## 环境备注（2026-09-09）

- 用户打开 VS Code 后其工作区监视器持有 states/0269 部分树的句柄，导致三次发布的 eviction/归档阶段被阻塞（apply 链本身全部成功、报 passed 269/269）。验证证据不受影响：.test-workspace 已同步到含 NotFound 声明与裸数组断言的最新 269 态，request_usage 3 用例 3 pass 实证 404 契约已生效。VS Code 关闭或后续运行时归档将自动收敛（脚本已知行为）。
