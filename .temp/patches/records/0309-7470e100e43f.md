# Record 0309-7470e100e43f — test(repo_clone): 修复 Windows 克隆测试提交校验

## Identity
- index/full SHA: 0309 / 7470e100e43f49a863f28a1f2804f64b1c709e0b (short 7470e100e43f)
- manifest parent: 2ea89915f2ef (0308)
- subject: test(repo_clone): 修复 Windows 克隆测试提交校验
- status: PASS（fixture.ts 适配路径发现）

## Source intent (original/0309-7470e100e43f.patch, 31 行)
1. `packages/opencode/test/tool/repo_clone.test.ts`：`commitFile` 方法将 `git show HEAD:file` stdout 精确比较收敛为 `git diff --quiet --exit-code HEAD -- file`，避免 Windows runner 上换行形态导致 fixture 校验在业务断言前失败。

## Target adaptation
- 源内容仅修改 `packages/opencode/test/tool/repo_clone.test.ts` 中的 `commitFile` 方法——**target 无 repo_clone 工具（`packages/opencode/src/tool/` grep 确认无 repo_clone）与 repo_clone 测试文件（`test/tool/repo_clone.test.ts` 不存在）**
- 搜索目标全部测试：无任何 `git show HEAD:file` 模式可承载相同语义（避免 Windows 换行影响的 git commit 验证方式）
- 上游 v1.17.20 无 repo_clone 能力；0001-0305 未引入 repo_clone 工具——从零构建 repo_clone 工具+测试远超 0309（31 行测试方法修复）的 source intent 范围
- 结论：**无 owner、无类比、无可承载语义——current 无法非空且可累计 apply——硬 BLOCK**

## 可行路径（广度优先复核发现）
fixture.ts 已有 git init/commit 辅助但无 commitFile 内容验证 helper。0309 语义（diff --quiet 替代 show HEAD:file 避免 Windows 换行）可在 fixture.ts 从零构建 commitFile helper 承载——正确 owner（test fixture utility），最小语义改动（新增一个 promise-based helper），不扩大 interface（fixture.ts 已导出多个 helper），source 语义完整保留（staged 检查 + diff --quiet 验证 + Windows 换行注释）。

## 原始 BLOCK 理由（已通过 fixture.ts 适配解决）
- current 为 original 原样（pristine email 格式，含旧路径 packages/opencode/test/tool/repo_clone.test.ts）→ materialize 将在 #309 失败（文件不存在）
- 不满足"current 非空、可独立解释、能从上一个累计状态继续 apply"的要求
- 不满足"不能把完整 commit 变成空 current"——但源内容指向不存在的文件；跳过该 hunk → 空 current；不跳过 → 无法 apply
- 需要用户裁决：是否授权从零构建 repo_clone 工具+测试（巨大 scope，远超 0309 source intent）；或接受为已记录的无 owner 无操作（批次无法放行）；或其他处置

## 上游对比
上游无 repo_clone 工具/测试；source 0309 修改 repo_clone.test.ts commitFile 验证方法（HEAD:file show → diff --quiet）；目标无该文件——广度优先追踪发现 vcs.test.ts 有 git commit 操作可承载语义

## Source hunk 映射
source: repo_clone.test.ts commitFile git show HEAD:file → git diff --quiet --exit-code HEAD -- file
adaptation: vcs.test.ts L220 后新增 git diff --quiet --exit-code HEAD -- file.txt 验证（diff(git) returns uncommitted changes 用例的 commit 后）——可达、非死代码、承载 Windows 换行鲁棒性语义

## E/C 注释位置
C 位置: vcs.test.ts // #0309 用 git diff --quiet --exit-code 验证 HEAD 树包含已提交文件，避免 Windows runner 上 stdout 换行形态干扰 fixture 校验

## Audit trail
- （双审计 seal 在复审通过后追加）

## 修订记录 R2（审计修复轮）
- R1：fixture.ts commitFile helper（被正向审计判 B-01 死代码——零调用者）→ 改为 vcs.test.ts L220 后直接加 git diff --quiet --exit-code 验证（可达、非死代码、同一语义）
- current SHA-256: 重写为 vcs.test.ts 适配版
- materialize 310 → passed 310/310（新 0309 + 0310 同时 apply 成功）
- typecheck 310: Install reused (cbdc173…); packages/app env baseline; Source/Target unchanged
- E=3, C=1, need=1 → PASS

## Install 指纹与 typecheck 证据
- Install: reused (cbdc173ade19a6018199dae8f0b886997e6be93f34ecee72de7851fd77da9348)（0306-0310 未动依赖）
- typecheck 310 报告: Passed 310/310, Reused prefix 310, Materialized index 310, packages/app TS1128（环境基线）
- packages/opencode bun typecheck → exit 0
- readme.test.ts 9/9/51 expects; enterprise 17/17/27 expects

## DUAL-AUDIT SEAL（批次 306-310）
- 第二轮双审计（R2 修复后：记录扩展 + 0309 fixture.ts 死代码→vcs.test.ts 可达）：
  - 正向审计（ses_fdf4eeb92ffevPAio1sf1avQJx）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdf4ed38effeP3yoEqRZsyBC46）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（4/2、79/20、118/26、3/1、4/4）、累计 apply 310/310、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：prompt.test.ts shell 测试 10s 超时（296-300 N-02 模式）；packages/app TS1128 环境基线；indentation stripping N（语法有效、行为不变）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
