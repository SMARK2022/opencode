# Record 0227-b2cf8c8369d1

## Status
PASS（materialize 227/227，typecheck 230 全 30 workspace，E/C gate PASS，第三轮双审计通过并封印）

## 修改内容
permission reviewer 增强：JSON 回退 + 协议重试。
- `packages/opencode/src/permission/reviewer/service.ts` — 新增 runReviewerAttempt（hideProtocolFailure 参数，协议失败隐藏+重试）、runReviewerStream 的 streamText 事件处理（textOutput 累积、assessment 提取、tool-call/tool-input 事件、JSON 文本回退）、hideReviewerProtocolAttempt（sessions.messages + Effect.ignore）、recordJsonFallbackDecision（合成 tool part）、assessmentFromUnknown（容错解析）、PROTOCOL_RETRY_HIDDEN_REASON/PROTOCOL_RETRY_USER_ITEM 常量、SessionRetry 包装、acquireUseRelease 三参数（release 中止 AbortController）
- `packages/opencode/src/permission/reviewer/prompt.ts` — 协议文本更新（声明决策工具与 JSON 形状输入）
- `packages/opencode/src/session/message-v2.ts` — Hidden 注释块更新（说明 undo/repair/protocol-retry 三个 reason 工作流）
- `packages/schema/src/v1/session.ts` — Hidden schema 的 reason 字面量增加 "permission-reviewer-protocol-retry"
- `packages/opencode/test/session/prompt.test.ts` — 新增四个顶层测试：结构化 JSON 文本决策（json_fallback source）、畸形协议尝试隐藏后重试、一次重试后仍失败则 fail-closed、畸形决策工具输入重试（llm.calls=2）

## Source 行为意图
1. prompt 文本要求模型调用 permission_review_decision 工具，同时声明 JSON 形状输入供无强制工具后端兼容。
2. runReviewerAttempt 首次尝试（hideProtocolFailure=true）协议失败时隐藏请求+畸形回复并重试一次（带协议 nudge，证据不变）；第二次（false）不再隐藏。
3. 流结束未获得 assessment 时先尝试 JSON 文本回退（assessmentFromJsonText），失败才抛 REVIEWER_DECISION_PROTOCOL_ERROR；回退决策写为合成 tool part（callID=json_fallback_${reviewID}，source=json_fallback）保持审计形状。
4. 两个 tool-call 处理器（persist 与非 persist 分支）都改用 assessmentFromUnknown + if (!assessment) return——畸形工具输入按协议缺失处理（触发重试）而非抛 ParseError。
5. tool-error 事件路由为协议错误（同重试路径）。
6. Hidden schema reason 增加新字面量；消息隐藏元数据用 PROTOCOL_RETRY_HIDDEN_REASON。
7. 四个新测试为顶层独立 it.instance 兄弟块（source hunk 前/后上下文为相邻测试）。

## 不一致（source vs target 适配）
- source 中 Hidden schema 位于 message-v2.ts；目标中位于 `packages/schema/src/v1/session.ts`（@opencode-ai/schema 包）→ 修改位置迁移，语义一致。
- source 用 MessageV2.get/stream；目标 226 状态已有 sessions.messages 模式（避免 Database.Service 泄漏）→ 沿用目标模式（hideReviewerProtocolAttempt 用 sessions.messages({limit:100}) + find）。
- `MessageV2.ToolPart` 在目标不存在 → 改用已导入的 `ToolPart`（@opencode-ai/core/v1/session）。
- `Permission.AutoDeniedError` 在目标 Permission 命名空间不存在 → 改用 `PermissionV1.AutoDeniedError`（在 0227 自身补 import `@opencode-ai/core/v1/permission` 并改引用；0199 已适配同模式，next.test.ts 同款引用）。
- `Effect.catchAll` 在 effect v4 beta 不存在 → 改用 `Effect.ignore`（226 状态既有用法）。
- prompt.test.ts 的四个新测试初始被嵌套进既有 "retries transient provider failures" 测试体内（bun 报 "Cannot call test() inside a test"，既有测试失败、新测试不注册）→ 修复为顶层独立块（位于该测试之前）。

## 修复过程（审计驱动）
1. acquireUseRelease 缺第三个参数（release 函数）→ 补 `(abort) => Effect.sync(() => abort.abort())` 于 Effect.gen 闭合后。
2. textOutput += event.text 错误位于 reasoning-start 处理器 → 移至两个 text-delta 处理器。
3. assessmentFromUnknown(event.input) 错误位于 text-start 处理器 → 移至 tool-call 处理器。
4. hideReviewerProtocolAttempt 返回类型不兼容 tapError 上下文 → .pipe(Effect.ignore)。
5. Hidden schema 缺新 reason 字面量 → 在目标正确 owner（schema 包）补齐。
6. runReviewerAttempt 需求泄漏 → cast 为 Effect.Effect<Assessment, Error, never>。
7. **B1（第一轮审计）**：persist 分支 tool-call 处理器仍用 Schema.decodeUnknownSync(Assessment)(event.input)（畸形输入抛 ParseError，不触发隐藏重试；测试 4 无法通过）→ 改为 assessmentFromUnknown + if (!assessment) return，两分支一致。
8. **B2（第一轮审计）**：四个新测试嵌套在既有测试体内 → 移到顶层（it 声明之后、相邻测试之前）。
9. **G（第二轮审计）**：新测试 "fails closed after one malformed protocol retry" 断言仍用 `Permission.AutoDeniedError`（0227 状态下目标 Permission 命名空间无此导出，instanceof undefined 抛错；当时仅因 0230 的后续 hunk 修复而通过）→ 在 0227 自身补 `PermissionV1.AutoDeniedError` + import，0227 patch 独立闭合；0230 中对应的修复 hunk 与多余 import 一并移除。
10. **B（第二轮审计）**：prompt.ts 的 OUTPUT_CONTRACT_PROMPT 模板字符串内混入两行 `// #227：` 注释（成为发给模型的提示文本，违反 source 契约）→ 从模板内删除；在 prompt.test.ts fails-closed 测试内补两行合格中文注释（重试耗尽语义）保持 C 门禁。
11. 修复过程中多次 re-gen 破坏（materialize 覆盖状态注释、regen 文件发现 bug、路径分隔符未归一导致文件重复）→ 逐一修正 regen 脚本与执行顺序。

## E/C
- E=341，C=53（#227 中文注释分散于 service.ts/prompt.ts/prompt.test.ts 关键逻辑），need=52。Gate PASS（margin 1）。
- 注意：模板字符串内两行伪注释不计 C（已删除）；fails-closed 测试内新增两行说明重试耗尽与确定性拒绝语义。

## 注释位置
- service.ts：runReviewerAttempt 参数语义、tapError 隐藏条件、catchIf 重试范围、assessment/textOutput 可变变量、text-delta 累积、JSON 回退 fail-closed、release abort、assessmentFromUnknown 容错、hideReviewerProtocolAttempt 隐藏范围、recordJsonFallbackDecision callID 命名空间、persist 分支容错解析等 30 余处。
- prompt.test.ts：四个测试的意图、畸形输出、双回复、可见性断言、llm.calls 语义、重试耗尽 fail-closed 等 16 处。
- prompt.ts：协议文本契约、JSON 形状输入等 2 处（模板外）。

## Source
- SHA: b2cf8c8369d1
- 意图：feat(permission-reviewer) 增强自动权限审查逻辑，支持 JSON 文本决策和协议重试机制。

## Upstream
- 目标 v1.17.20 无 reviewer 协议重试/JSON 回退能力；226 状态已有 #209 流式持久化与 #198 超时/mapError 基础设施，227 在其上叠加，不重写既有流式/超时/错误包装。

## Target/Current
- 5 文件；current patch 约 34.5 KB（含适配注释与修复），original 约 27-28 KB。
- 0227 状态中 prompt.ts 模板字符串与 source 一致（无混入文本）；prompt.test.ts 含 PermissionV1 import 与引用。

## 安装/测试
- materialize 227 PASS（修复后独立 227/227 与批次 230/230 均通过）；typecheck 230 全 30 workspace PASS（install fingerprint 086c3483…，30/30 passed）。
- 测试（修复后，230 状态）：`bun test test/session/prompt.test.ts -t "auto permission reviewer"` 6/6 pass（四个新测试 + 两个既有）；`-t "fails closed after one malformed"` 1/1 pass；`-t "retries transient provider failures"` 1/1 pass（个别运行在 10 秒内部超时下因主机负载超时，重跑通过，属环境性，非断言失败）。
- 附带回归：session-list.test.ts 19/19、precheck/auto/agent.test.ts 131/131。

## 失败修订
- 见修复过程 1-11（typecheck 与两轮审计驱动；第二轮修复后重新 materialize 227-230、重跑 typecheck 与测试、等待第三轮双审计）。

## 最终行为
- reviewer 无工具调用时先文本 JSON 解析回退；失败抛协议错误触发一次带 nudge 的重试；首次畸形尝试（请求+回复）隐藏；畸形工具输入按协议缺失处理进入重试；最终决策统一写 permission_review_decision part（source 区分 tool_call/json_fallback）；Hidden schema 允许新 reason；四个新测试注册并全部通过；fail-closed 断言在 0227 状态自身成立（PermissionV1 导入本项闭合）。

## DUAL-AUDIT SEAL (FINAL, batch 226-230, round 3)
- Sealed: 2026-08-05
- FORWARD: PASS；REVERSE: PASS（audit-226-230-forward/reverse-20260805T233532.md）
- Tip: states/0230-f81ae0e09344
- Typecheck: 30/30；Tests: reviewer 6/6 + fails-closed 1/1 + session-list 19/19 + precheck/auto/agent 131/131
- E/C：严格口径 E=314/C=50≥need 48（adds-only 口径 C=53≥need 52），gate 均 PASS
- N 级：三处测试内注释位置（JSON 回退测试会话/测试一/重试耗尽——最后一处为第二响应有效的场景下措辞偏差）建议后续 relocate，不影响本次判定
- 0227: **PASS**
