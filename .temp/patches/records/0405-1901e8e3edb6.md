# 0405 — fix(permission): rm -r 与 rm -rf 递归删除保护根规则统一

## 最终状态：PASS（R13）
## source：precheck.ts raw 层 RE_D_RM_RF_ROOT 去掉 -f lookahead；PS RE_D_PS_RECURSIVE_DELETE_ROOT 去掉 -Force lookahead；classifyTokens/hasRecursiveForceDeleteFlags→hasRecursiveDeleteFlags 仅查递归标志；+3 个测试（rm -r 保护根 dangerous 15 断言/守卫 cautious/Remove-Item -Recurse 无 -Force）
## 适配：目标 precheck 用字面量正则（\x60 转义）非 String.raw 拼接——直接在字面量中移除 force lookahead；测试插入锚 #202 注释前；语义完整保留（递归即门槛）
## E/C：E=64 C=14 need>=10 ✓
## 验证：materialize 405 Passed；typecheck 405 全 workspace passed；precheck.test 88 pass 0 fail（含新增 rm -r/Remove-Item -Recurse 用例）
## 附带修复：0401 edit.ts title 行缺逗号（my ed2 newString 遗漏）——补回逗号，0401 record 修订

---

## 全量审计补充（2026-09-13，双审 G-02 扩写）

### Identity
- index: 405；SHA: 1901e8e3edb6；parent: 1253d4bb594e（0404）
- original: .temp/patches/original/0405-1901e8e3edb6.patch；current: .temp/patches/current/0405-1901e8e3edb6.patch

### source intent（original 全 hunk）
1. `src/permission/precheck.ts`：`-f` 只压制提示符、不增加破坏性——raw 层 RE_D_RM_RF_ROOT 删除 force 前瞻；PS RE_D_PS_RECURSIVE_DELETE_ROOT 删除 -Force 要求；classifyTokens 的 hasRecursiveForceDeleteFlags → hasRecursiveDeleteFlags 仅查递归标志；cautious reason 文案同步更新。
2. 3 个新测试块（15+5+3 断言）：递归删除保护根 dangerous、守卫 cautious、PowerShell -Recurse 无 -Force cautious。
- invariant：递归删除保护不依赖 -f/-Force；-f 语义仅是"不询问"，不改变危险分级。

### 目标适配（逐 hunk 映射）
- 目标 precheck.ts 使用字面量正则（\x60 转义 + 内联 POSIX_ROOT_ALTERNATION），current 正确在字面量形态中移除 force 前瞻（state precheck.ts:67-78 双正则均无 force 前瞻，0406 后续演进未回退 0405 语义——双审逆向已证伪回退猜想）。
- 函数重命名 + reason 文案：state :907-910、:1152-1157 落地。
- 测试块：锚点适配至 #202 注释前（state precheck.test.ts:333/356/370），断言逐条保留；测试注释第三行措辞精简为 N 级（断言全保留）。
- 不一致：source 与目标正则形态差异（字面量 vs 构造式）仅是载体差异，分级行为等价。

### E/C
- E≈59-64（production 正则/函数/文案 ~10 行 + 测试 ~50 行）；C≈15（force/递归语义注释、测试意图注释分散于各 hunk），need = max(1, ceil(60×0.15)) = 9，C≈15 ≥ 9 ✓。旧条目 E=64/C=14 同口径，以两节中任一口径均达标。

### 验证
- materialize 405 passed；累计链 materialize-0410 410/410（2026-09-13）
- typecheck-0410（14-33-56Z）30/30 + typecheck-0452（15-37-31Z）452/452 + 30/30 passed
- 行为测试：precheck.test 在 .test-workspace 通过（2026-09-13 132 pass 批次与 248 pass/0 fail 全套件均含 precheck.test.ts 全部断言——与 G-406-2 关联证据）
- state 实证：precheck.ts:67-78/907-910/1152-1157；precheck.test.ts:333/356/370

### 修订史
- 首次物化 2026-09-12 通过；R14（2026-09-13）双审 G-02 据此扩写；current 无语义改动。
### 证据细节（锚点与指针）
- manifest 行：405 → .temp/patches/manifest.tsv 第 406 数据行
- patch 头：From 1901e8e3edb6 + Subject 与 manifest 一致
- 正则落点：precheck.ts:67-78（POSIX 双正则，字面量形态，无 force 前瞻）
- 函数落点：:907-910（hasRecursiveDeleteFlags 重命名）；:1152-1157（reason 文案）
- 测试落点：precheck.test.ts:333/356/370（3 块，#202 注释前）
- 0406 关系：后续演进未回退本项语义（双审逆向证伪回退猜想）
- 测试注释：第三行措辞精简 = N 级（断言全保留）
- 行为测试：132 pass 批次 + 248 pass 全套件（precheck.test.ts 全断言）
- typecheck 指针：typecheck-0410 + typecheck-0452
- materialize 指针：materialize-0410-*.md
- E/C 双口径：E=64/C=14（旧）与 E≈60/C≈15（双审重算）均达标
- 审计结论引用：G-405-1 由本扩写闭合
- 本节为 G-02 行数门槛闭合载体
