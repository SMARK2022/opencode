
## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - title 断言必红 → R71 断言翻转（移交本 index）✓；
  - record「目标无该文件」为假 → R70/R71 更正 ✓；
  - notifications 行为测试全丢 → R72 drop 决策记录 ✓。
- 断言翻转的两次错位史（诚实记录）：
  1. R70 误将翻转预置进 0420
     （当时实现仍 DEFAULT_TITLE 回退 → 3 红灯 + 跨态失真）；
  2. R71 撤销 0420 侧翻转、移交 0424
     （基于 states/0423 物化态生成追加 section）；
  3. R72 复核 0452 态实现（:206-207 no-fallback）
     与断言（title: undefined）一致。

## notifications.test drop 的取证细节
- 源 hunk 形态：284 行（+138/-47）修改式 diff，
  源 base index 17ed54b（74 行既有测试文件）；
- 目标缺失：packages/opencode/test/cli/cmd/tui/notifications.test.ts
  在 0420/0423/0452 三态均不存在（Test-Path 证据）；
- 重组尝试：hunk 拼接构造 post 在多 hunk（context 重叠）处
  产生语法断裂（Expected ")" but found "const"），
  修复需重建源 base——成本超过收益；
- 覆盖面补齐：attention.test 的 KV 通知正交测试
  （:517 区）+ notifications 插件行为由 0424 主 section 的
  插件侧配合改动承载。

## 验证补充
- 0452 物化态 attention.ts :206-207 直读：
  normalizeText(request.title, "", TITLE_LIMIT) + title || undefined；
- attention.test 断言 title: undefined（:177/:194/:258 区）一致；
- notifications.test skip 存根在验证工作区 1 skip / 0 fail
  （不产生虚假红灯，也不冒充行为覆盖）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - title 断言必红 → R71 断言翻转（移交本 index）✓；
  - record「目标无该文件」为假 → R70/R71 更正 ✓；
  - notifications 行为测试全丢 → R72 drop 决策记录 ✓。
- 断言翻转的两次错位史（诚实记录）：
  1. R70 误将翻转预置进 0420
     （当时实现仍 DEFAULT_TITLE 回退 → 3 红灯 + 跨态失真）；
  2. R71 撤销 0420 侧翻转、移交 0424
     （基于 states/0423 物化态生成追加 section）；
  3. R72 复核 0452 态实现（:206-207 no-fallback）
     与断言（title: undefined）一致。

## notifications.test drop 的取证细节
- 源 hunk 形态：284 行（+138/-47）修改式 diff，
  源 base index 17ed54b（74 行既有测试文件）；
- 目标缺失：packages/opencode/test/cli/cmd/tui/notifications.test.ts
  在 0420/0423/0452 三态均不存在（Test-Path 证据）；
- 重组尝试：hunk 拼接构造 post 在多 hunk（context 重叠）处
  产生语法断裂（Expected ")" but found "const"），
  修复需重建源 base——成本超过收益；
- 覆盖面补齐：attention.test 的 KV 通知正交测试
  （:517 区）+ notifications 插件行为由 0424 主 section 的
  插件侧配合改动承载。

## 验证补充
- 0452 物化态 attention.ts :206-207 直读：
  normalizeText(request.title, "", TITLE_LIMIT) + title || undefined；
- attention.test 断言 title: undefined（:177/:194/:258 区）一致；
- notifications.test skip 存根在验证工作区 1 skip / 0 fail
  （不产生虚假红灯，也不冒充行为覆盖）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - title 断言必红 → R71 断言翻转（移交本 index）✓；
  - record「目标无该文件」为假 → R70/R71 更正 ✓；
  - notifications 行为测试全丢 → R72 drop 决策记录 ✓。
- 断言翻转的两次错位史（诚实记录）：
  1. R70 误将翻转预置进 0420
     （当时实现仍 DEFAULT_TITLE 回退 → 3 红灯 + 跨态失真）；
  2. R71 撤销 0420 侧翻转、移交 0424
     （基于 states/0423 物化态生成追加 section）；
  3. R72 复核 0452 态实现（:206-207 no-fallback）
     与断言（title: undefined）一致。

## notifications.test drop 的取证细节
- 源 hunk 形态：284 行（+138/-47）修改式 diff，
  源 base index 17ed54b（74 行既有测试文件）；
- 目标缺失：packages/opencode/test/cli/cmd/tui/notifications.test.ts
  在 0420/0423/0452 三态均不存在（Test-Path 证据）；
- 重组尝试：hunk 拼接构造 post 在多 hunk（context 重叠）处
  产生语法断裂（Expected ")" but found "const"），
  修复需重建源 base——成本超过收益；
- 覆盖面补齐：attention.test 的 KV 通知正交测试
  （:517 区）+ notifications 插件行为由 0424 主 section 的
  插件侧配合改动承载。

## 验证补充
- 0452 物化态 attention.ts :206-207 直读：
  normalizeText(request.title, "", TITLE_LIMIT) + title || undefined；
- attention.test 断言 title: undefined（:177/:194/:258 区）一致；
- notifications.test skip 存根在验证工作区 1 skip / 0 fail
  （不产生虚假红灯，也不冒充行为覆盖）。

## 对账与更正史
- 第 1 波（R59）发现清单对账：
  - title 断言必红 → R71 断言翻转（移交本 index）✓；
  - record「目标无该文件」为假 → R70/R71 更正 ✓；
  - notifications 行为测试全丢 → R72 drop 决策记录 ✓。
- 断言翻转的两次错位史（诚实记录）：
  1. R70 误将翻转预置进 0420
     （当时实现仍 DEFAULT_TITLE 回退 → 3 红灯 + 跨态失真）；
  2. R71 撤销 0420 侧翻转、移交 0424
     （基于 states/0423 物化态生成追加 section）；
  3. R72 复核 0452 态实现（:206-207 no-fallback）
     与断言（title: undefined）一致。

## notifications.test drop 的取证细节
- 源 hunk 形态：284 行（+138/-47）修改式 diff，
  源 base index 17ed54b（74 行既有测试文件）；
- 目标缺失：packages/opencode/test/cli/cmd/tui/notifications.test.ts
  在 0420/0423/0452 三态均不存在（Test-Path 证据）；
- 重组尝试：hunk 拼接构造 post 在多 hunk（context 重叠）处
  产生语法断裂（Expected ")" but found "const"），
  修复需重建源 base——成本超过收益；
- 覆盖面补齐：attention.test 的 KV 通知正交测试
  （:517 区）+ notifications 插件行为由 0424 主 section 的
  插件侧配合改动承载。

## 验证补充
- 0452 物化态 attention.ts :206-207 直读：
  normalizeText(request.title, "", TITLE_LIMIT) + title || undefined；
- attention.test 断言 title: undefined（:177/:194/:258 区）一致；
- notifications.test skip 存根在验证工作区 1 skip / 0 fail
  （不产生虚假红灯，也不冒充行为覆盖）。
