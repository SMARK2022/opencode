# Record 0314-9b0b83e23bc0 — feat(tests): 增加输出截断指导信息，优化模型恢复策略

## Identity
- index/full SHA: 0314 / 9b0b83e23bc093b561954e09de547d676fb7b33e (short 9b0b83e23bc0)
- manifest parent: bba9c227dfcf (0313)
- subject: feat(tests): 增加输出截断指导信息，优化模型恢复策略
- current SHA-256: d735e841ffc1e5d1 / 3847 bytes
- status: PASS

## Source intent (original/0314-9b0b83e23bc0.patch, 82 行)
### Hunk 1: `packages/opencode/src/util/output-notice.ts` (L21-33)
- 新增: `OUTPUT_TRUNCATED_GUIDANCE` 常量
  - 4 行中文注释: "这段文本是模型可见的截断恢复策略，而不是 UI 文案：它必须足够短，同时明确先用保存文件做 grep 定位，再用 read offset/limit 读取局部上下文。保持为单个 attribute 可以复用现有 XML-like notice 解析和展示路径，避免为了增强提示性而改变 shell/tool 截断输出的前后位置不变量。"
  - 字符串值: "Before making decisions that depend on omitted output, use grep on path first, then read relevant ranges with read offset/limit. Avoid reading the full file unless necessary."
- 行为目的: 模型可见的截断恢复策略，引导模型先 grep 定位再 read offset/limit 局部读取

### Hunk 2: `packages/opencode/src/util/output-notice.ts` (L39-46)
- 新增: `formatOutputTruncatedNotice` 返回对象加 `guidance: OUTPUT_TRUNCATED_GUIDANCE` 字段
- 行为目的: 将 guidance 常量注入截断 notice，复用现有 XML-like notice 解析路径

### Hunk 3: `packages/opencode/test/tool/shell.test.ts` (L2065-2070)
- 新增: 3 行 expect 断言（grep, read offset/limit, Avoid reading the full file）+ 2 行中文注释
- 注释内容: "Shell 日志通常很长，notice 必须引导模型先搜索保存文件、再按行段读取，否则截断恢复容易退化成读取完整日志并浪费上下文。"
- 行为目的: 验证 shell 截断输出包含 guidance 指导信息

### Hunk 4: `packages/opencode/test/tool/truncation.test.ts` (L66-88)
- 新增: `it.live("guides targeted recovery from saved output when truncated")` 测试用例
  - 2 行中文注释: "Notice 是模型可见的恢复入口；这里验证它给出先 grep 定位、再 read 局部读取的低成本路径，避免调用方为了找隐藏日志而直接读取完整文件。"
  - 4 行 expect: path outputPath, grep, read offset/limit, Avoid reading the full file
- 行为目的: 验证 Truncate 服务输出包含 guidance 指导信息

## Three-way comparison
- source/SMARK: 截断 notice 加 guidance 字段，引导模型先 grep 再 read offset/limit，避免直接读完整文件
- 上游 v1.17.20: 截断 notice 无 guidance 字段，仅包含 type/source/total/shown/path
- 目标当前: output-notice.ts 有 formatOutputTruncatedNotice 但无 guidance 字段；shell.test.ts/truncation.test.ts 有截断测试但无 guidance 断言
- 结论: 加 OUTPUT_TRUNCATED_GUIDANCE 常量 + guidance 字段 + 测试验证

## Target adaptation
- output-notice.ts: L24 后加常量（4 行中文注释 + 字符串），L45 后加 `guidance: OUTPUT_TRUNCATED_GUIDANCE`
  - Notice 类型 `Record<string, string | number | undefined>` 接受 string 类型的 guidance
- shell.test.ts: L1689 后加 3 行 expect + 2 行中文注释
- truncation.test.ts: L199 后加 `it.live("guides targeted recovery...")` 测试用例 + 2 行中文注释 + 4 行 expect
- 方法: state 0313 语义编辑（edit314.py patch_window）→ git diff → current/0314

## 逐 hunk 映射
| Source hunk | Source file | Target file | Target line | 状态 |
|-------------|-------------|-------------|-------------|------|
| Hunk 1 (constant) | output-notice.ts L21-27 | output-notice.ts L24-31 | L26-31 新增 | ✓ 适配 |
| Hunk 2 (guidance field) | output-notice.ts L39-46 | output-notice.ts L45-53 | L53 新增 | ✓ 适配 |
| Hunk 3 (shell expects) | shell.test.ts L2065-2070 | shell.test.ts L1689-1694 | L1690-1694 新增 | ✓ 适配 |
| Hunk 4 (truncation test) | truncation.test.ts L66-88 | truncation.test.ts L198-216 | L201-216 新增 | ✓ 适配 |

## Verification
- materialize 314/314: passed（从 state 0313 继续，应用 0314 current）
- typecheck 314: install reused（frozen install cbdc173），packages/app TS1128（环境基线）
- truncation test（.test-workspace synced to 314）: `bun test ./test/tool/truncation.test.ts --timeout 60000` → 20 pass / 1 fail / 66 expect() calls / 8.82s
  - 1 fail = "cleanup > deletes files older than 7 days" — pre-existing 环境性失败（7 天旧文件时间戳依赖），非 0314 引入
  - 新增 "guides targeted recovery" test 单独运行: `bun test ./test/tool/truncation.test.ts -t "guides targeted" --timeout 60000` → 1 pass / 0 fail / 5 expect() calls / 6.15s
- shell truncation test（.test-workspace）: `bun test ./test/tool/shell.test.ts -t "truncation" --timeout 60000` → 5 pass / 0 fail / 22 expect() calls / 22.42s（含 grep/read offset/Avoid 断言）
- E/C: E=19 / C=8 / need=max(1,ceil(19*0.15))=3 → PASS
  - 注释位置: output-notice.ts L26-29（截断恢复策略设计原因）、shell.test.ts L1690-1691（notice 引导原因）、truncation.test.ts L209-210（恢复入口验证原因）
- source/target HEAD: 未变（integrityFailure None）
- states 保留 ≤5

## 不一致
- 无。三处修改均在目标文件同路径、同函数、同测试 describe 内
- truncation cleanup 测试失败为 pre-existing（7 天旧文件时间戳依赖），非 0314 引入
- guidance 字符串通过 formatNotice → escapeAttribute 路径输出，无需额外转义（无特殊 XML 字符）

## DUAL-AUDIT SEAL（批次 311-315）
- R1 双审计（记录扩展后：四份记录从 <50L/3000C 扩展至 ≥55L/3787C，0315 manifest parent 更正为 cf597333）：
  - 正向审计（ses_fdefde82fffeDVRJftO2F6brVb）: 5/5 PASS，无 open B/G
  - 逆向审计（ses_fdefdcbf6ffeU0neKZIAxyCnKO）: 5/5 PASS，无 open B/G
- 独立复核: manifest/proof 哈希、original LF-SHA、current SHA+字节、install 指纹 cbdc173 reused、E/C 五项全达标（9/2、12/6、13/3、19/8、6/4）、累计 apply 315/315、states 保留 ≤5、source/target 未改
- 环境性注记（非 B/G）：packages/app TS1128 环境基线（custom-elements.d.ts 符号链接物化文本）；truncation cleanup 测试 7天旧文件时间戳依赖；typecheck script packages/app 首失败阻断后续 workspace（tsgo -b 模式覆盖 project references）
- 批次放行: APPROVE（5/5 PASS，无未关闭 B/G）
