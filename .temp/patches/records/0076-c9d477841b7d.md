# Record 0076 — `c9d477841b7d`

**Subject:** feat: 优化代理路由逻辑，调整TTL常量，增强插件模块的插件导出处理  
**Parent:** `4141d5e979ff` (#75)  
**Index:** 76  
**Current path:** `current/0076-c9d477841b7d.patch`  
**Original path:** `original/0076-c9d477841b7d.patch`  
**Current SHA-256:** `51350c11e54772425280300d77f3ca2d2ea6a4219defb8e3c88a0cd2fbe61bae`  
**Current bytes:** 7952  
**Author (manifest):** SMARK `<3222301373@qq.com>` · 2026-05-04T20:52:52+08:00  

## 1. Source intent (original)

### 1.1 `packages/core/src/network-proxy.ts`

1. Rename cache TTL `15_000` → **`PROXY_TTL = 10_000`**.  
2. First `resolveProxyRoute` always **`refresh=false`** (drop force-refresh on first hop for provider/npm).  
3. Destructure and **strip** caller `proxy` from init (`proxy: _incomingProxy`) so route owns proxy selection.  
4. One-shot Bun env clamp: `NO_PROXY=*` + `__OPENCODE_NO_PROXY_SET=1` so plain fetch ignores startup-cached proxy env; explicit `proxy:` param still wins.  
5. `fetchDirect(overrideSignal?)` helper; on proxy failure + **signal aborted** → direct with **`AbortSignal.timeout(10_000)`**.  
6. Direct path still: if `signal?.aborted` after failure → rethrow (no blind retry).  
7. `savedOriginal` + **`originalFetch()`** export; `installGlobalFetch` captures pre-wrap fetch into `savedOriginal`.  

### 1.2 `packages/opencode/src/plugin/index.ts` — `getLegacyPlugins`

1. Prefer `mod.default` if server plugin function.  
2. Else named exports ending in `Plugin` that are functions.  
3. Else all non-default function exports; if **>1** without Plugin suffix → `TypeError` ambiguity.  
4. Dedupe by plugin identity after `getServerPlugin`.  

## 2. Upstream / pre-state (post-#73)

#73 rewrote `fetchWithRoute` to a `withSignal` form:

- First resolve used `forceRefresh = purpose === "provider" || purpose === "npm"`.  
- Proxy abort path retried direct with **no signal** (`rest`).  
- `TTL = 15_000`; no `originalFetch`; no NO_PROXY clamp.  
- `installGlobalFetch` flag-before-wrap with local `originalFetch` binding (not exported).  

Plugin pre-image: `Object.values(mod)` all entries must be plugin functions; multi-export modules without discipline throw generically.

**Critical apply failure:** earlier `current/0076` still patched pre-#73 `direct()`/`proxy()` helper shape → `git apply` conflict. Must adapt onto post-#73 file literally.

## 3. Three-way comparison and adaptation

| SMARK #76 intent | Pre-#76 (#73) | Current adaptation |
|---|---|---|
| First resolve cache-first | forceRefresh provider/npm | `resolveProxyRoute(..., false)` |
| Strip caller proxy | left in rest | `_incomingProxy` strip |
| NO_PROXY clamp | absent | once via guard flag |
| Abort after proxy | direct without signal | `fetchDirect(AbortSignal.timeout(10_000))` |
| TTL 10s | 15s | `PROXY_TTL = 10_000` |
| originalFetch | private only | export + savedOriginal |
| Plugin selection | all values | default → *Plugin → ambiguity |

Design choice: **fold into #73 structure** (`fetchDirect`/`fetchProxy` on rest), do not reintroduce obsolete helper names unless needed for clarity. Keep single owner `fetchWithRoute`. Failover remains one recovery family (cache-first then refresh-on-failure), not a second success path.

Plugin hunk applied as source intent on `getLegacyPlugins`; `readV1Plugin` / shared V1 object path left alone.

## 4. Current patch files

1. `packages/core/src/network-proxy.ts`  
2. `packages/opencode/src/plugin/index.ts`  

No separate tests in source for this commit.

## 5. E / C

- **E ≈ 49**, **C ≈ 12**, need ≥ 8 → **pass**  
- Comments: PROXY_TTL meaning, NO_PROXY Bun interaction, strip proxy, 10s abort fallback, savedOriginal/originalFetch, plugin default/*Plugin/ambiguity.  

## 6. Verification

| Step | Result |
|---|---|
| Apply check on post-#75 tree | clean |
| Materialize cumulative through 79 | OK (includes 76) |
| State-79: `PROXY_TTL`, `originalFetch`, `NO_PROXY` clamp, `getLegacyPlugins` *Plugin | present |
| Typecheck batch-end 79 | all workspaces passed |
| Install | `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea` |

## 7. Failures and revisions

1. Stale current against pre-#73 shape → full rewrite of network-proxy hunks against state-75.  
2. Dual audit G on record length → expanded this record with three-way table and provenance.  

## 8. Final behavior

- Provider/npm still refresh proxy **on failure**, but no longer force-refresh every first hop.  
- Bun plain fetch after clamp goes direct; explicit proxy still works.  
- Aborted proxy path gets 10s deadline direct.  
- Ambiguous multi-export legacy plugins fail with clear TypeError.  
- Callers can bypass wrap via `originalFetch()`.  

## 9. Provenance

- Baseline `4473fc3c…`; source tip `d0ceb469…`  
- Depends on #71/#73 NetworkProxy existence  
- No original/ edits; official current only

## Dual audit closure (batch 75–79)

- This index current SHA-256: `51350c11e54772425280300d77f3ca2d2ea6a4219defb8e3c88a0cd2fbe61bae` (7952 bytes)

- FORWARD: **PASS** (no open B/G) — task session independent auditor
- REVERSE: **PASS** (no open B/G) — task session independent auditor
- Typecheck: `reports/typecheck-0079-2026-07-21T13-22-01-724Z.md`
- Install: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Materialized: `states/0079-8812fd3d588b`
- Official currents only; no `_*.patch`

## Verdict

**PASS** (independent dual audit closed for batch 75–79)
