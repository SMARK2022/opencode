# Record 0079 — `8812fd3d588b`

**Subject:** feat(notebook): 优化编辑操作文档，增强字符串匹配逻辑及上下文构建  
**Parent:** `3bab13f27c2f` (#78)  
**Index:** 79  
**Current path:** `current/0079-8812fd3d588b.patch`  
**Original path:** `original/0079-8812fd3d588b.patch`  
**Current SHA-256:** `8c3a26fcabec0238820f28839196faa07bc94a02c8ee848d22c0564664d4df79`  
**Current bytes:** 19382  
**Author (manifest):** SMARK `<3222301373@qq.com>` · 2026-05-05T05:56:32+08:00  

## 1. Source intent (original)

Single-file follow-up on **`sdks/vscode/src/notebook/edit.ts`** after #78:

### 1.1 Docs

- File header documents `oldCode` / `newCode` string match (like opencode edit tool).  
- Strategies: line-trimmed → border-trimmed fallback after exact.  
- No line numbers in match surface — immune to display index shift.  

### 1.2 String match (replaces range path)

- Remove `readLineRange` / `applyRangeEdit` / range-oriented `handleTextEditSource`.  
- Add `matchAndReplace`, `lineTrimmedMatch`, `handleStringEdit`.  
- Order: **exact → line-trimmed → border-trimmed**.  
- Multi-match error **only for exact** strategy.  
- Type-change path may still use string match when `oldCode` set; full-cell replace with `newCode` only remains.  

### 1.3 Context building

- `buildContext`: above / match / below lines with virtual global offsets via `computeVirtualRanges`.  
- Strategy tag on match label; `contextSummary` into `compactEditResult`.  
- Preview uses `newCode` (not full cell source).  

### 1.4 Cleanup

- Unify Affected for `delete || insert`.  
- Drop unused imports (`isRecord` / `numberProp` style).  

## 2. Upstream / dependency

- **Hard depends on #78:** editType handlers, `computeVirtualRanges`, compact result helpers, cell id helpers.  
- No upstream opencode notebook equivalent.  
- Pre-#79 state still has range helpers; post-#79 they must be gone.  

## 3. Three-way adaptation

| Intent | Current |
|---|---|
| Remove range edit path | `readLineRange` / `applyRangeEdit` absent in state-79 |
| Three-tier match | present; same order as source |
| Exact multi-match throw | present |
| buildContext + contextSummary | present |
| Comments | decision-adjacent Chinese on match ladder, uniqueness, virtual coords, no silent success |

### Known shared nits (source + target)

- Unused `eol` local possible in match path.  
- CRLF documents: match on LF-normalized text may mis-index raw content — **also in original**; not introduced as migration-only alternate success path.  

Do **not** treat exact→trimmed→border as forbidden “multi-parser fallback family”: it **is** the SMARK primary algorithm (same as edit tool).

## 4. Current patch

- Only `sdks/vscode/src/notebook/edit.ts`.  
- Built on fixed #78 post-image (async env intact).  
- Stripped mass `// 常量` noise; kept/added qualifying rationale comments until C ≥ need.  

## 5. E / C

- **E ≈ 124**, qualifying **C ≈ 19**, need ≥ 19 → **pass**  
- Comments examples: three-tier match, line-trim, border-trim, exact uniqueness, virtual range context, no silent no-op, compact summary.  

## 6. Verification

| Step | Result |
|---|---|
| Cumulative `--materialize 79` | OK |
| State-79: matchAndReplace present; readLineRange gone | verified |
| State-79: #78 async env still valid | verified |
| `--typecheck 79` | all listed packages passed; report `typecheck-0079-2026-07-21T13-22-01-724Z.md` (or later rematerialize sibling) |
| Install | `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea` |
| Provider tests from #77 still green in workspace | 2 pass (claudecode / extends) |

## 7. Failures and revisions

1. Dual audit G-02: C overcounted non-qualifying lines → strip noise, add decision comments, recount.  
2. Dual audit G-01 record length → this expanded record.  
3. Must rematerialize after #78 async fix so #79 applies on correct base.  

## 8. Final behavior

- Notebook edit is string-match based with three ordered strategies and virtual-range context summary.  
- Full-cell replace / type change with optional oldCode still work.  
- Failures throw; no silent success on miss.  

## 9. Batch closure context

This is batch-end index for **75–79**. Dual independent auditors must both PASS all five with no open B/G before advancing to 80–84.



## Post-repair fingerprint (authoritative)

- Current: `current/0079-8812fd3d588b.patch`
- SHA-256: `8c3a26fcabec0238820f28839196faa07bc94a02c8ee848d22c0564664d4df79`
- Bytes: 19382
- Typecheck: `reports/typecheck-0079-2026-07-21T13-22-01-724Z.md`
- Install: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Materialized state: `states/0079-8812fd3d588b`
- E/C note: decision comments on match ladder, uniqueness, Anchor oldKind/oldLang; dead range headers removed


## Type-change Anchor fix

- `handleTypeChange` now returns `kind: cellTypeLabel(newKind)`, `language: newLang`, plus `oldKind`/`oldLang` fields so `compactEditResult` can print `old->new` Anchor.
- Removed dead range-edit comments; qualifying Chinese comments on match ladder and uniqueness.
- Verified: no `async//`, no `return//` ASI, `async function handleStringEdit`, `readLineRange` absent.

## Dual audit closure (batch 75–79)

- This index current SHA-256: `8c3a26fcabec0238820f28839196faa07bc94a02c8ee848d22c0564664d4df79` (19382 bytes)

- FORWARD: **PASS** (no open B/G) — task session independent auditor
- REVERSE: **PASS** (no open B/G) — task session independent auditor
- Typecheck: `reports/typecheck-0079-2026-07-21T13-22-01-724Z.md`
- Install: `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- Materialized: `states/0079-8812fd3d588b`
- Official currents only; no `_*.patch`

## Verdict

**PASS** (independent dual audit closed for batch 75–79)
