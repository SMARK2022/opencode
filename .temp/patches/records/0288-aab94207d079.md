# Record: 0288-aab94207d079

## 基本信息
- index: 0288
- sha12: aab94207d079
- subject: feat(shell): 修改测试，更新压缩输出描述，明确关闭压缩选项以验证截断边界
- source date: Wed, 10 Jun 2026 01:33:46 +0800

## Source 行为重建
原始 patch 修改 2 个测试文件：
1. **parameters.test.ts.snap**：更新 compress_output description 快照——从旧文案改为 283 已应用到源码的新文案（含 "oversized lines, and terminal progress noise"）
2. **shell.test.ts**：在 byte limit 截断测试中添加 `compress_output: false` 和 2 行中文注释，防止重复字节被 bash 压缩器折叠导致压缩后输出低于 byte limit 而不触发截断

### Hunk 行为详情
- **快照 hunk**：输入为旧文案，输出为 283 新文案（含 "oversized lines, and terminal progress noise"）；目的是让快照与源码 description 保持同步
- **shell.test.ts hunk**：输入为无 compress_output 的截断测试，输出为显式 `compress_output: false` 的截断测试；目的是消除压缩器对截断边界验证的干扰
- Invariant：截断测试的 byte limit 逻辑不变，仅添加 compress_output 选项防止假阴性

## 三方比较
- **parameters.test.ts.snap**：目标快照中的 compress_output description 为 #116 适配遗留的混合文案（`"Whether this shell call may compress repetitive output before returning it. Strongly prefer omitting this field..."`），缺少 "oversized lines, and terminal progress noise"。Source patch 的 `-` 行期望上游原始文案（`"Defaults to the user's shell output compression setting..."`），该文案在目标中从未存在过。需适配 `-` 行为目标实际文案。
- **shell.test.ts**：目标 byte limit 测试在 line 1515（非 source 的 1898），且 283 已移除 `description:` 属性（TS2353 修复）。Source patch 的 context 包含 `description: "Generate bytes exceeding limit"` 行，该行在目标中不存在。需删除该 context 行并调整行号。
- 快照 `-` 行文案差异根源：source 基于 SMARK 分支（description 从未修改过），目标经过 #116 适配（混合文案）和 283 新文案应用，导致 `-` 行需要适配
- shell.test.ts 行号偏移：source 1898 vs 目标 1515，偏移量 ~383 行，由目标历史中的测试用例增删导致
- description 属性移除：283 修复 TS2353 错误移除了所有测试用例的 `description:` 属性，source patch 仍包含该属性

## 适配内容
- **快照 hunk**：将 `-` 行从 source 期望的上游原始文案改为目标实际的 #116 混合文案；`+` 行保持不变（283 新文案）
- **shell.test.ts hunk**：删除 `description: "Generate bytes exceeding limit"` context 行；调整行号 1898→1515；保留插入的 2 行中文注释 + `compress_output: false`；注释前缀改为 `#288`

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（2551 bytes）
- 编辑后 SHA-256: c14e67b52f2b239422a963716b2f31bdd91d495efb2b5e828c58dcee56557f4b
- 编辑后字节: 1998
- materialize 288: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| parameters.test.ts.snap @@ -25,7 | 适配 `-` 行为目标 #116 文案 | 行号调整，删除行内容适配 |
| shell.test.ts @@ -1898,6 | 适配行号 1515 + 删除 description | 移除 description context，注释前缀改 #288 |

## E/C 计算
- E (有效修改行): 4（快照 description 修改 1 行 + shell.test.ts 新增 compress_output: false 1 行 + 2 行中文注释）
- C (中文注释行): 2
  - shell.test.ts: "#288：本用例只验证截断边界..." (2 行)
- 满足 E > 0 时 C >= max(1, ceil(4 * 0.15)) = 1，2 >= 1 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)

## 不一致
- 快照 `-` 行文案不匹配：source 期望上游原始文案，目标为 #116 混合文案
- shell.test.ts description 属性：283 已移除（TS2353 修复），source patch 仍包含

## 物化验证
- parameters.test.ts.snap：compress_output description 更新为 283 新文案（含 "oversized lines, and terminal progress noise"）✓
- shell.test.ts：byte limit 截断测试正确添加 `compress_output: false` 和 2 行 #288 中文注释 ✓
- shell.test.ts：`description: "Generate bytes exceeding limit"` context 行未引入（283 已移除）✓
- 行号正确：快照 hunk `@@ -25,7 +25,7 @@`，shell.test.ts hunk `@@ -1512,7 +1512,10 @@` ✓
- materialize 288: 待批次验证 ✓

## 审计历史
- 第一轮双审计：PASS（第一轮双审计通过）（批次 286-290）

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 286-290，第一轮）
- 正向审计 (ses_0122cab55): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_0122c9807): 5/5 PASS, 无 B/G (N-01~N-03 非阻塞)
- materialize 290: passed 290/290
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: fc30f32375a78d931a2197c60bfa28e4c62cc16f74ebe314daac30dd46a75952 (reused)
- E/C: 286=0/0, 287=0/0, 288=4/2, 289=30/5, 290=4/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#282 if(appendix) 守卫）保留为基线，SMARK exitNotice 正确叠加