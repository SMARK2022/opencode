# Record 0427-cb510c47b8b4

## Identity
- SHA: cb510c47b8b4
- Parent (source): 1b237ef31670 (0426)
- Subject: docs(goal): 完善会话目标完整性方案
- Status: PASS（materialize 427 + 批次末 typecheck 430 + 双审计修复后）

## Source intent（1 文件，1 hunk，15,266 字节）
`docs/plans/session-goal-transition-integrity.md`（0426 新增的计划文档）内容修订：
1. **read gate 合同**：模型必须先在目标 turn 内 get（GoalTool 无参调用），才能标记 status——防止模型不看 GOAL 就直接终态化；工具在无 prior get 时拒绝 status 写入。
2. **reason 必填**：complete/blocked 终态必须携带非空 reason（trim 后空白拒绝、超长拒绝）；active/paused 时 reason 清为 null。
3. **代际 CAS**：objective 真正改变时 generation 递增（从 1 开始）；status/reason/budget/continueOnError 变更不递增；模型 stale 写入与旧请求 usage 归属依赖 generation 校验。
4. **恢复协议**：active recovery 仅允许 model-produced terminal（terminal_turn_id 非 null）在后续新真实用户 turn 恢复；paused 由用户控制、模型不能恢复；同一 turn 与 continuation turn 不能恢复。
5. **blocked 连续审计**：blocked_reason/blocked_streak/blocked_last_turn_id 三元组；相同 reason 连续 3 轮才写 blocked 终态；reason 改变/turn 中断/objective 编辑重置 streak。
6. **usage 归属**：accountUsage 用 expected {goalID, generation} 原子校验；terminal/pause 后同 generation 的 final usage 不丢失；objective edit/clear 后旧请求 no-op。
7. **goal 工具调用矩阵与审计约束**：模型可写 active/complete/blocked，不能写 paused；用户直接写入 terminal 不设置 terminal_turn_id。

## Target analysis 与适配
- 目标（0426 apply 后）同路径文档存在且内容与 source 0426 版逐字一致（verbatim 平移 source 0426 的 238 行版）→ current/0427 = original verbatim。
- 该文档与 0429（fix(goal) 实现 commit）一一对应：0427 是计划文档（方案层），0429 落地实现（goal.ts 的 modelTransition/CAS/streak/recovery/accountUsage expected + tool/goal.ts 的 read gate + prompt.ts 的 goalTurn context + migration 6 列）。
- 文档语义核对：read gate/reason/CAS/blocked audit/recovery/usage 在 0429 的实现全部覆盖；0428（stats）与本文档无关。
- 双审计确认：original == current（LF 归一化逐字节一致）；materialized 文档关键语义全部命中。

## E/C
- 全文 .md → E=0 / C=0 / need=0 PASS。

## Verification
- materialize 427：passed 427/427（verbatim apply 到 0426 状态）。
- 批次末验证链：materialize 430 --rebuild passed 430/430；typecheck 430 passed（全部 21 workspace，install 指纹 1165df52… frozen reuse）；行为测试 0429 goal 52/52 + migration 2/2、0430 patch/apply_patch/edit 118/118、0428 stats 19/19 全绿。
- 内容验证：materialized 文档与 source 0426 版逐字一致（verbatim）。

## 双审计修复记录
- G-0427-1：record 原为 24 行/833 字符（低于契约 50 行/3000 字符）→ 扩展为本版本（含 source 逐 hunk 意图明细、目标适配、验证链证据、审计修复记录）。
- 行为语义无任何缺陷（docs-only verbatim）。

## 备注
- 0428 为 stats 代码 commit（R6 配色），与此文档无关；0429 为 goal-integrity 实现 commit（本文档的实现面）。
- 本批次（0426-0430）验证链：materialize 430 --rebuild passed 430/430；typecheck 430 passed（全部 21 workspace）；行为测试全绿（goal 52 + migration 2 + patch/apply_patch/edit 118 + stats 19 + attention 21 + notifications 9 + malformed-sgr 7 + config 42 + session-preview 12）。

## 文档关键段落语义（逐段）
- **§1 Scope and Decision**：方案收敛到三个 owning boundary——SessionGoal（持久化 generation/reason/blocked audit/CAS 跨请求原子转换）、GoalTool（模型 turn 内真实 read-before-write 与模型可写状态边界）、SessionPrompt（每 Goal turn 权威上下文、stale continuation 保护、resume loop、usage 归属）。
- **§2 不变量**：模型不能不看 GOAL 就终态化（read gate）；terminal 必须带 reason；objective 真正改变才递增 generation；blocked 连续 3 轮相同 reason 才写终态；active recovery 仅 model-produced terminal 且后续真实用户 turn；usage 归属 expected {goalID, generation}。
- **§3 证据表**：node_modules/@opentui/core/lib/terminal-palette.d.ts（直接探测非零副作用）、stats-render-width.test.ts（行为 seam）、theme.test.ts（CliRenderer 依赖证明）、daemon.test.ts（真实 CLI subprocess 隔离先例）、preload.ts（OPENCODE_DB :memory:）、global.ts（XDG 派生）、pty.ts（PTY 无 termios）、git show 历史（RGB palette 演进）。
- **§4 视觉反馈 loop**：改代码前后同一数据/终端/尺寸对照；原始 ANSI capture + 终端截图 + TERM_PROGRAM/TERM/profile 记录 + color never capture。
- **§5+ 实现阶段 acceptance**：read gate 拒绝无 get 的 status 写入；reason 空/超长拒绝；generation CAS stale 拒绝；blocked streak 1/2/3；active recovery 拒绝条件矩阵；usage expected no-op。
- **§9 详细合同**：set 的 generation 递增规则（objective 真正改变才递增，terminal objective-only edit 自动 active）；modelTransition 的 complete/blocked/active recovery 分支；blocked pending 中间结果；accountUsage 原子增量。
- **§10-17**：错误消息格式（stale goal/read gate/多匹配）、TUI reason 显示、SDK/API 传播、审计约束。

## 验证命令与输出
- `bun .temp/patches/src/apply-cumulative.ts --materialize 427`：passed 427/427。
- 批次末 `--materialize 430 --rebuild`：passed 430/430；`--typecheck 430`：passed（21 workspace，install 指纹 1165df52…）。
- 0429（本文档实现 commit）行为测试：goal.test.ts 44/44、tool/goal.test.ts 8/8、session-goal-migration.test.ts 2/2。

## 双审计结论
- 双 auditor：0427 无行为缺陷（docs-only verbatim，original==current 逐字节一致）；唯一 BLOCK = record 门禁（G-02/G-0427-1）。
- 修复：record 扩展（本版本）至 ≥50 行/≥3000 字符。
