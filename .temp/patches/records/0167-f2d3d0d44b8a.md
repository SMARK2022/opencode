# Patch 0167 — f2d3d0d44b8a

## Identity
- Index: **167**
- Full SHA: `f2d3d0d44b8a52c0dd92f55851734c350530ece9`
- SHA12: `f2d3d0d44b8a`
- Parent: `442fd61eabf4` (#166)
- Subject: feat(install): enhance installation script with multi-profile PATH, sudo gate, reinstall policy, and script tests
- Authored: 2026-05-18T21:36:39+08:00
- Current path: `.temp/patches/current/0167-f2d3d0d44b8a.patch`
- Current bytes: **30560**
- Current SHA-256: `98d93c6c5e8e75893c43a4f67e4e036bc537e73f38bd4f18f2585e7c41615ac6`
- Original path: `.temp/patches/original/0167-f2d3d0d44b8a.patch`
- Original bytes: **57006**
- Record updated: 2026-07-25T08:22:35.133549+00:00
- Batch: **166–170**
- Tip base before this patch: `states/0166-442fd61eabf4`
- Tip state after this patch: `states/0167-f2d3d0d44b8a` (later consumed into cumulative 170)

## Source intent (from immutable original)
Original touches three paths:
1. **`install`** — primary behavioral owner
2. **`README.md`** — full SMARK Chinese README rewrite (behavior tables + flags)
3. **`packages/opencode/test/installation/install-script.test.ts`** — new four-case shell contract tests

Installer behavioral contracts that must survive tip adaptation:
1. New flags: `--path-profile` (repeatable), `--all-shell-profiles`, `--interactive`, `--allow-sudo`
2. Default PATH policy: update **all existing** supported shell profiles (bash/zsh/fish/sh), not only the first profile of `$SHELL`
3. Sudo gate: if `uid==0` **and** `SUDO_USER` set, refuse unless `--allow-sudo`; with allow and no explicit `--path-profile`, force `no_modify_path=true` and warn
4. Install dir: `mkdir -p` failure → exit 1; not writable → exit 1 with guidance
5. `check_version` rewrite:
   - Target dir same version → **reinstall** (“reinstalling to refresh it”), never `exit 0`
   - Binary elsewhere on PATH → **warn only**, still install to target
   - Old tip behavior (same version anywhere → skip) is the regression this commit fixes
6. Profile write: dedupe literal / `$HOME/...` / `~/...`; create missing profile when possible; quoted PATH / `fish_add_path`; zsh includes `.zprofile`; ash/sh **drop** `/etc/profile`
7. Interactive uses `/dev/tty`; no TTY → fall back to default multi-profile
8. Priority: explicit `--path-profile` → all-shell → interactive → default-all-existing

## Three-way comparison

### Upstream / tip@166 strengths (must keep)
- Root `install` already carries SMARK `#156` stack: `OPENCODE_REPO`, multi-candidate release assets, checksum soft-warn, quarantine, `--binary`, progress download, `GITHUB_ACTIONS` → `GITHUB_PATH`, fail-closed download
- English multi-locale README hub (`README.md` + many locales) is tip structure; wholesale Chinese replace would destroy tip docs architecture
- `packages/opencode/test/installation/installation.test.ts` is a **different** owner (Effect `Installation` package channel) — not the shell script under test

### SMARK original deltas
- Large install rewrite for multi-profile + sudo + reinstall
- New `install-script.test.ts`
- Nearly full README replacement with SMARK installer narrative

### Target adaptation (current)
| Original path | Tip owner | Action |
|---|---|---|
| `install` | root `install` | Surgical merge **on top of** tip `#156` base — do not replace whole file blindly |
| `README.md` full rewrite | tip English hub | **Do not apply**; port install docs to **`README.zh.md`** surgically |
| `install-script.test.ts` | same path (new) | Port four tests almost as-is; fixture import `../fixture/fixture` |

## Owners and current hunk map
Current `diff --git` paths:
1. `install` — flags, `check_sudo`, writable mkdir, `check_version`, multi-profile helpers (`profile_has_*`, `append_*`, `select_*`, `path_command_for_profile`, `normalize_profile_path`), PATH write loop
2. `README.zh.md` — SMARK install URL, env-pipe gotcha, behavior table, flag table; keep package-manager list / upstream links
3. `packages/opencode/test/installation/install-script.test.ts` — four cases:
   - PATH has same version elsewhere → still installs to `OPENCODE_INSTALL_DIR`
   - target already same version → reinstalls; content refreshed
   - multi-profile default without duplicate PATH lines
   - sudo without allow → exit 1

## What was deliberately not applied
- Full SMARK `README.md` wipe of tip English multi-locale structure (record: docs semantic preserved via `README.zh.md` + install usage text)
- Replacing tip `#156` checksum/asset/REPO logic (would be semantic loss of tip strength)

## Independent E/C (current patch only)
- **E** (non-blank non-comment added lines): **458** (approx; install + test + zh docs)
- **C** (Chinese `#167` decision comments): **79**
- Required: `max(1, ceil(E*0.15))` = **69**
- Gate: **PASS**
- Representative C sites (must stay near decisions, not stacked):
  - sudo refuse-by-default + force `no_modify_path` under sudo without explicit profile
  - target same version reinstall vs PATH-elsewhere warn
  - multi-profile default / dedupe `$HOME` and `~/`
  - ash/sh drop `/etc/profile`
  - interactive TTY fallback
  - test intents for reinstall / sudo / multi-profile / PATH conflict

## Materialize / install / typecheck
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 167` → passed 167/167 after current rewrite
- Command: `bun .temp/patches/src/apply-cumulative.ts --typecheck 167` → packages passed; install reused fingerprint `bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f`
- Report family: `typecheck-0167-*` then later batch-end `typecheck-0170-2026-07-25T07-56-46-317Z.md` (30 packages)
- `bash -n install` OK after adaptation
- Materialize provenance: baseline `4473fc3c…`; source tip `d0ceb469…`; serial after #166

## Tests (behavior)
From test-workspace `packages/opencode` (after frozen install):
```text
bun test test/installation/install-script.test.ts
→ 4 pass, 0 fail, 13 expect()
```
Cases lock: PATH conflict still installs; target reinstall; multi-profile dedupe; sudo refuse.

## Inconsistencies found during adaptation
1. Original current ≡ original (byte-identical) would not apply cleanly on tip `#156` install context → hand-authored residual required
2. English README wipe rejected as tip-architecture conflict
3. Early E/C fail (C=0 on raw original) → added distributed `#167` Chinese decision comments until C≥need

## Final tip behavior after #167
Installer no longer skips when PATH already has the same version; reinstalls the target binary; defaults to multi-profile PATH writes with dedupe; refuses sudo unless explicitly allowed; script tests lock the four contracts; Chinese docs document SMARK URL, env-pipe gotcha, and flags without destroying tip English README hub.

## Dual independent audit (batch 166–170)
- FORWARD: **PASS** (all five; no open B/G) — task after record expansion re-audit
- REVERSE: **PASS** (all five; no open B/G) — independent reverse re-audit
- Prior cycle: BLOCK only on record size G for 167–170; records expanded ≥50 lines & ≥3000 chars; behavior had no open B
- Materialize: `materialize-0170-2026-07-25T07-56-36-492Z`
- Typecheck: `typecheck-0170-2026-07-25T07-56-46-317Z` (30/30; install `bfd9217687…`)
- Sealed: 2026-07-25T08:32:23.632271+00:00
- Batch verdict: **PASS** — may advance to 171–175
