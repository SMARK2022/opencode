# 0441-0cc47d97b4f8 fix(patch): 精简匹配失败诊断

## Identity
- index / full SHA: 0441 / 0cc47d97b4f8（manifest parent = 0440 319ce18c1064）
- subject: fix(patch): 精简匹配失败诊断
- original: 1033 行 / 5 文件（docs plan 644 行 + patch/index.ts 2 hunks + patch/match.ts 3 hunks + apply_patch.test 12 hunks + edit.test 2 hunks）
- current: 70595 bytes / 5 文件适配版（docs plan 原样 + 4 文件语义适配，Round-6 生成）
- status: PASS（Round-6 语义适配 + materialize 441/441 + typecheck 441 30/30 + apply_patch 43/43 + edit 41/41）

## Source Intent
1. **docs/plans/tool-match-diagnostic-information-density.md 新建 644 行**：R6 canonical plan，status verified、双审计 APPROVE。D01-D06 六个诊断决策面 = 100%（超出 10% 默认上限，有用户明示授权）。E/C 记录 E=176、C=27（15.34%）。
2. **patch/index.ts 2 hunks（+6/-3）**：D06——context 失败错误去掉 change_context 回显 → `Failed to find context in <path>.`；D05/D03——ambiguity 错误去掉 old_lines 回显 → `Found multiple matches ... Provide more context to make the match unique.`；not-found 去掉 expected 回显 → `Failed to find expected lines in <path>.`。拒绝语义不变，只精简诊断密度。
3. **patch/match.ts 3 hunks（+63/-1）**：新增 diffChars import；共享 renderer formatClosestDifference（逐差异行 actual JSON 窗口 + requested columns 对比，前 30/后 20 字符窗口）；closestWindow 尾部重写（persisted candidate 完整时不回显文本 D04；否则 diff renderer 再 fallback truncateExcerpt D02；500 字符预算保留）。
4. **apply_patch.test 12 hunks（+114/-12）**：四类失败（ambiguity/context/not-found/无候选）改 Effect.exit+Cause.squash 断言 INV-03（请求文本在组合输出恰好一次且 error 不含请求文本）；新增 2 个多 chunk 测试；长候选测试断言 difference: requested columns 301-301；partial-success 输出继承去重不变量。
5. **edit.test 2 hunks（+5/-3）**：单字符差异测试断言 line 2 actual JSON + difference columns + not.toContain(expected)。

## 预研锚点核查（state 0440）
- match.ts：pre-image blob a4e595c1 与 state **精确一致**，3 hunks FULL-MATCH。
- index.ts：blob 漂移（d0658d08 vs 8044d6f5）。H@355 FULL-MATCH（state L354）；**H@373 PARTIAL**——state 在 `}` 与 `const result = locateExact` 之间有 5 行 insertions.push 逻辑（源基线没有），上下文错位需语义适配（消息改造落在 state L372-379 的实际位置）。
- apply_patch.test：blob 漂移。H@231/407 FULL-MATCH；H@222/289/306/321/389/747/765/796/807/838 共 10 个 PARTIAL/NOT-FOUND——需按 state 形态适配（细节见适配计划）。
- edit.test：H@308 PARTIAL（closest match 测试 state 形态不同）；H@324 PARTIAL（断言改造锚点偏移）。

## 适配实施（Round-6）
1. index.ts H@355：原样（FULL-MATCH）。
2. index.ts H@373：语义适配——在 state L372-379 实际位置做两处消息精简（ambiguity 长版→短版+注释；not-found withCandidate 长版→短版+注释）。
3. match.ts 3 hunks：原样（FULL-MATCH）。
4. apply_patch.test 12 hunks：逐 hunk 按 state 0440 测试形态适配——[222] 插入 ambiguity 注释行（state 格式 `),` 结尾）；[231] 原样；[289] +72 行新测试按 state helper 形态（execute 缩进/ctx 形态）重写插入位置；[306][321][389][407][747] 逐个核对；[765][796] decoy/可靠候选断言改造对齐 state 现有断言形态；[807][838] partial-success：state 现有 #348 测试为文件级失败场景，source 的 block 级失败场景需改写适配（INV-03 组合输出去重不变量保持）。
5. edit.test 2 hunks：按 state closest match 测试形态适配断言。
6. E/C 预估：以 count-ec-auditor 最终复算为准。

## 验证（已完成）
- materialize 441：passed 441/441（Round-6 适配后，gamma 断言修正二次重物化确认）。
- typecheck 441：30/30 workspace 全 passed（typecheck-0441-2026-08-30T07-59-21 report）。
- 行为测试：apply_patch.test 43/43（含新组合去重断言 + Closest match at line 3 + difference 列号断言）；edit.test 41/41（含 line 2 actual JSON + requested columns 17-17 列号断言）。

## 审计轮次记录
- Round-6（批次首轮）：record 先于适配编辑建立（合规时序）；适配后回写本最终态。gamma/gammo 断言修正过程已留痕（第一版误对 actual 串断言，实测后改为 requested 串）。

## Status
PASS（Round-6 语义适配完成并验证；materialize/typecheck/行为测试全绿）

## INV-03 去重不变量的语义映射（source → target）
- source H9（not-reliable-candidate 测试）：请求文本 "totally different expected block" → target 同名测试同串，组合 split 计数 == 1。
- source H10（reliable-candidate 测试）：requested "target sequence alpha beta gamma" 留在 patchText → target 同串（文件侧 actual 为 gammo 变体，由 renderer 以 actual 窗口+列区间展示）。
- source H12（multi-file partial-success）：失败文件 missing.txt 的 "-old" 请求文本 → target 同场景（文件级失败），组合 split 计数 == 1 且 output 不含 "-old+new"。
- source H11：部分成功的注释行（output-available 不复制失败 old block）→ target 同位置同注释。

## renderer 行为说明（match.ts）
- formatClosestDifference：逐差异行输出 actual JSON 窗口（前 30/后 20 字符，显式省略计数）+ requested/actual 列区间。
- closestWindow：persisted candidate 含完整 expected 时只报告位置（D04），否则走 renderer，最后 fallback truncateExcerpt 500 字符（D02）。
- 该 renderer 是 Edit 与 Patch 两个工具的共享实现，保证同一 candidate 不会在两个 Tool result 里形成漂移副本。

## 与 0442/0443 的关系
- 0442（workflow/policy/skill docs）与本 patch 零文件交集。
- 0443 的 acp/session 重构不触碰 patch 工具链（src/patch/*）。
- 本 patch 的 apply_patch.test 断言改造独立于 0443 的进度通道。

## 遗留观察项
- closestWindow 的 500 字符 truncateExcerpt fallback 仅在 renderer 返回空串时触发（理论上 renderer 对每个差异行都有输出，fallback 为防御性保留）。
- bounded window 的 30/20 字符常量为 source 实测值，Windows 终端折行宽度下仍可读。

## 风险与缓解
- diffChars 依赖 "diff" 包：0440 package.json 已含该依赖（无新增），manifest 链无 lockfile 变更。
- apply_patch.test 的 partial-success 适配将 block 级失败场景改为文件级失败场景（0440 状态无 block 级多文件测试宿主），INV-03 语义以文件级错误不回显 old block 等价承载，已在 record 标注。
