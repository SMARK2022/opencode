# Record: Patch 32 — b815a5764d5b

## 1. Manifest Entry
- **Index**: 32
- **SHA**: b815a5764d5bba724b7fe7245cacf3e4f9b85f56
- **Parent**: 0031-6285c0741261
- **Source tip**: d0ceb469011412b4ac5058a12d5fe4f247bdac79
- **Target baseline**: 4473fc3c9055046183990a965d68df3db7ea6f62
- **Subject**: feat: 添加高熵行压缩和增强的诊断上下文收集功能

## 2. Source Behavior (original patch, 1 file)
1. `bash-compress.ts`: 增强 patch 28 添加的压缩模块
   - 新增 `CommandAdapter` 接口和 `CommandAdapterContext` 类型
   - 新增 `CompressionConfig` 字段: `minHighEntropyLineLength`, `minEntropy`, `maxWhitespaceRatio`, `minTemplateRepeats`, `templateNormalizationLevel`, `enableCommandAdapters`
   - 新增 `CompressionStats` 字段: `templateGroups`, `highEntropyLines`, `secretsRedacted`
   - 新增 `DiagnosticContext.priority` (`'first' | 'fatal' | 'recent'`) 和 `DiagnosticSnapshot` 三队列字段
   - 新增密钥脱敏 (`SECRET_PATTERNS`, `redactSecrets`)
   - 新增高熵行检测和压缩 (`compressHighEntropyLines`)
   - 新增模板近重复压缩
   - 新增命令适配器 (`applyCommandAdapter`)
   - 增强 `compressVisibleOutput` 调用链: redactSecrets → highEntropy → template → blocks → same → inline

### 行为目的
- **高熵行压缩 invariant**: 检测并压缩高熵长行（如 minified JS、base64），减少 token 消耗
- **密钥脱敏 invariant**: 在压缩前脱敏 API key、token 等敏感信息
- **三队列诊断 invariant**: firstErrorContexts/fatalContexts/recentErrorContexts 分别保留首批、致命和最近错误上下文
- **命令适配器 invariant**: 对特定命令模式（如 git log、ls）应用专用压缩策略

## 3. 上游 (v1.17.20) 行为
- V2 有 patch 28 添加的 bash-compress.ts 基础版本（949 行）
- V2 无高熵检测、密钥脱敏、命令适配器
- V2 诊断上下文为单队列

## 4. 目标适配 (current patch, 1 file)
### 适配的文件
1. `bash-compress.ts`: 原始 patch 与 current byte-identical，直接应用

### 保留的 SMARK 语义
- 高熵行压缩完整保留
- 密钥脱敏完整保留
- 三队列诊断完整保留
- 命令适配器完整保留
- 增强的 compressVisibleOutput 调用链完整保留

### 保留的上游优势
- V2 的 bash-compress.ts 基础结构保留
- patch 28 的压缩管道保留

## 5. E/C 计算
- **E ≈ 997**（新增代码行，排除注释和空行）
- **C ≈ 167**（中文注释行）
- **最低 C = max(1, ceil(997 * 0.15)) = 150**
- **167 ≥ 150，通过**

## 6. 验证
- `bun .temp/patches/src/apply-cumulative.ts --typecheck 35 --rebuild`: passed 35/35
- Typecheck: packages/core:passed, packages/opencode:passed, packages/sdk/js:passed, packages/session-ui:passed, packages/tui:passed, packages/ui:passed
- Install fingerprint: `f0084b64d44fb33093f3fc7bc99df1bda58be07025687f8e74879a27878319dc`
- 增强函数调用链验证: redactSecrets → compressHighEntropyLines → template/blocks/same/inline → applyCommandAdapter

## 7. 失败修订历史
无。Patch 32 直接 apply 成功。

## 8. 最终行为
- bash-compress.ts 增强为高熵行压缩 + 密钥脱敏 + 三队列诊断 + 命令适配器版本
- 压缩管道: redactSecrets → highEntropy → template → blocks → same → inline → commandAdapter
- V2 路径和结构保留
