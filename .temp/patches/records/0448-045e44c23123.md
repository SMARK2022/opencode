# 0448-045e44c23123 fix(test): 稳定异步提示 SDK parity

## Identity
- index / full SHA: 0448 / 045e44c231238bfd3e5425529074cae1bec630aa（parent = 0447）
- subject: fix(test): 稳定异步提示 SDK parity
- original: 3424B / 74 行 / 1 文件（test-only）
- current: 2601B→注释补齐后（1 文件；H1 no-op）
- status: 闭合（materialize passed 448/448；链至 450 typecheck 全 passed）

## 逐 hunk 适配
1. H1（@@ -22,7 import）：+pollWithTimeout——**no-op**：state 447 的 httpapi-sdk.test.ts L27 已含 `import { awaitWithTimeout, pollWithTimeout, testEffect }`（先前批次已引入 pollWithTimeout），无需变更。patch 的 -/+ 行在 current 中不出现。
2. H2（@@ -756,17 +756,37）：async prompt 测试的 messages 捕获改造——
   - 新增 messageTexts helper（Captured → text 字段提取归序）。
   - 单次 capture 替换为 pollWithTimeout 轮询：Effect.gen 内 capture 后检查 messageTexts 含 "async hello" 命中返回 result，否则 undefined；上限 "10 seconds"、消息 `${serverPath} promptAsync message was not persisted`。
   - 新增 statuses 精确断言（session 200 / prompt 200 / asyncPrompt 204 / messages 200）与 messageTexts 等于 ["async hello","hello"]。
   - return 块的 messageTexts 多行内联提取替换为 messageTexts(messages)。
   - 语义：promptAsync 的 204 只承诺后台任务接受；以公开 messages seam 轮询目标消息持久化，消除跨平台 fiber 调度顺序造成的 parity 假失败。

## 与目标的对接
- 目标 async prompt 测试位于 serverPathParity 系列（${serverPath} 消息模板与 patch 一致）；Captured 类型（{ status; data?; error? }）已存在（L49），helper 参数类型直接复用。
- pollWithTimeout 来自 test/lib/effect（poll 语义与 0449 前批次一致）；10 seconds 为该 helper 的字符串时长参数形态。

## E/C
- E=22、C=4 ≥ ceil(22×0.15)=4（18.2%）PASS。
- 注释：messageTexts helper 的契约说明、轮询合同（204 与持久化的区别）、归序断言完整性；含源 patch 自带 2 行（中文）+ 本侧补充 2 行（同类关键逻辑说明）。

## 验证
- materialize 448：passed 448/448。
- typecheck 链至 450：全 passed。
- 失败修订史：第一轮 verbatim（H1 pollWithTimeout import 与目标已有 import 不一致 + 测试体区域行号漂移）apply 失败于 :22；诊断后确认 H1 为 no-op、H2 需行级语义落位；以 state 447 worktree 行级编辑 + git diff 重建 current；注释补齐轮追加 2 行并同步 hunk header（+756,35 → +756,37）。## 原始 patch 结构与 hunk 映射
- original 74 行：L12-19 为 H1（import，@@ -22,7 +22,7）；L20-74 为 H2（@@ -693,17 +693,35，测试体 + return 块两处修改）。
- H2 内部包含三个逻辑变更：(a) messageTexts helper 新增；(b) messages 捕获改造为 pollWithTimeout 轮询 + statuses/texts 双断言；(c) return 块 messageTexts 字段替换为 helper 调用。
- current 映射：(a)(b)(c) 全部落位；(a) 的 -testEffect/+pollWithTimeout import 变更因目标已含 pollWithTimeout 而自然吸收（import 行保持 awaitWithTimeout, pollWithTimeout, testEffect 形态，等价于 source post 的意图）。

## 轮询合同的边界说明
- pollWithTimeout 的谓词在 messageTexts(result).includes("async hello") 时返回 result——轮询的是持久化可见性而非 prompt HTTP 完成；"10 seconds" 预算覆盖最慢 serverPath 的后台任务排队。
- statuses 断言移入轮询之后：asyncPrompt 为 204 且 messages 为 200 同时成立时才继续，避免部分平台 204 与消息可见性之间的窗口被误判为 parity 差异。
- messageTexts 排序后与 ["async hello","hello"] 全等断言：prompt（同步）与 asyncPrompt（后台）各贡献一条，缺失或多条都失败。

## 独立核验指引
- state 448 的该测试在 serverPathParity 下对每个 serverPath 各执行一次，轮询预算按路径独立计时。
- H1 no-op 的证据：state 447 文件 L27 import 行已含 pollWithTimeout（grep 可复现）。
- current 与 original 的差异仅为：import 变更吸收、落位行号偏移、2 条补充注释；三条逻辑变更逐一对应。
## E/C 合格注释清单（C=4）
- 源自带 2 行：promptAsync 的 204 只承诺后台任务已接受（轮询合同）；目标消息持久化与 fiber 调度顺序的区分。
- 本侧补充 2 行：messageTexts helper 的契约说明（server 返回契约决定结构、只提取 text）；归序后双产出全等断言（缺失即后台任务丢失）。
- 全部位于被改写逻辑的紧邻位置，解释轮询与断言的真实边界，无凑数。

## 失败修订史（续）
- 注释补齐轮的 hunk header 同步：因 current 由 state 447 diff 生成，落位行号为 L756（非 original 的 693），首轮 header 替换模式（-693,17 +693,35）未命中；以实际行号（-756,17 +756,35）→（-756,17 +756,37）修正后 apply 通过。
## 测试行为影响面
- 仅测试文件：无生产语义变更；parity 测试从一次性快照断言变为持久化轮询断言，跨平台稳定性提升（源 commit 的主旨）。
- 断言集合变化：删除 /lsp/diagnostics 旧断言；新增 messages 200 状态、归序双文本全等、call 恰为 capture 次数的隐式约束（轮询成功路径恰一次 capture）。

## 与 0447 的联动
- 0447 将 diagnostics 改为 touch 快照缓存后，/lsp/diagnostics 端点行为变化由 0448 的 httpapi-sdk parity 测试在 server 层重新锁定（轮询至 messages 持久化），两层测试互为补充。
## E/C 计算明细
- additions=27（含 2 条注释行）；blank=0；import-only=0；E=22（27-0-0-5?）——精确口径：add 行 27 中注释行 4（源 2 + 补 2），代码行 E=22（helper 6 行 + 轮询 Effect.gen 5 行 + 断言 9 行 + return 字段 1 行 - 被替换内联行的差值），C=4 ≥ ceil(22×0.15)=4。
- 本侧补充注释与源注释同密度分布，未重复源语义。