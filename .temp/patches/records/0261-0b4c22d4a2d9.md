# Record 0261-0b4c22d4a2d9 — fix(auto-review): 复用聊天 provider hook，兼容 Codex 审查请求

## Manifest
- index: 261, parent: 0260-a0714815cb0c, sha: 0b4c22d4a2d98339c49d155bac06d80b9f1c0d3a
- source: SMARK2022 <3222301373@qq.com> 2026-05-29T18:01:54+08:00
- subject: fix(auto-review): 复用聊天 provider hook，兼容 Codex 审查请求
- original: original/0261-0b4c22d4a2d9.patch（13074B，2 文件）
- current: current/0261-0b4c22d4a2d9.patch（12973B，SHA 6e89cc785ad4）

## Source 行为
1. **service.ts**：reviewer 请求复用主聊天路径的 provider hook（chat.params + chat.headers）
   - 新增 `import { Plugin } from "@/plugin"` 和 `const plugin = yield* Plugin.Service`
   - 在 streamText 前构造 hookSessionID/hookMessage/hookInput，调用 `plugin.trigger("chat.params", ...)` 和 `plugin.trigger("chat.headers", ...)`
   - streamText 的 providerOptions/maxOutputTokens 改从 params 取，新增 headers 参数
   - defaultLayer 新增 `Layer.provide(Plugin.defaultLayer)`
2. **reviewer-service.test.ts**：新增 plugin hook 测试
   - 扩展 ReviewerRequestBody 加 max_output_tokens 字段
   - 新增 CapturedReviewerRequest 类型和 REVIEWER_HOOK_HEADER 哨兵
   - 新增 oauthHookFixture（带 plugin hook + rejectMaxOutputTokens）
   - 新增测试 "applies chat provider hooks before sending OpenAI OAuth reviewer requests"
   - 新增 pluginLayer 辅助函数

## 目标适配
1. **service.ts 路径迁移**：目标 import 行 1-2 是 LayerNode/ConfigPermissionV1（source 没有）→ 在 Auth 后插入 Plugin import
2. **service yield**：目标 auth yield 在 line 104（source 89）→ 在 auth 后插入 plugin yield
3. **defaultLayer**：目标用 `LayerNode.compile(Auth.node)` 而非 `Auth.defaultLayer` → 用 `LayerNode.compile(Plugin.node)`
4. **defaultLayer 注释**：目标有 "提供 Auth，否则生产默认 wiring 会缺少这个 provider 判定边界" → 改为 "提供 Auth。" + #261 注释
5. **测试文件**：目标用 ProviderV2.ID/ModelV2.ID（source 用 ProviderID/ModelID）→ 适配命名
6. 生成方式：直接编辑 0260 状态文件 + git diff 生成，确保 context 完全匹配

## E/C
- E=91, C=22, need=14, PASS
- 注释分布：hook 复用语义、合成消息安全边界、params/options 来源、plugin 层提供、max_output_tokens 捕获、CapturedReviewerRequest、REVIEWER_HOOK_HEADER、oauthHookFixture、plugin hook 验证测试、options 扩展、requests 捕获、headers 验证、rejectMaxOutputTokens、plugin 层默认值、pluginLayer hook 行为

## 验证
- materialize 261: passed 261/261
- materialize 265: passed 262/265, stopped at 263（263 需适配）
- 待 typecheck + 测试验证

## Source 证据（original/0261，13074B，255 行，2 文件）
### service.ts（5 hunk）
1. Hunk 1: 新增 Plugin import 在 Auth 后
2. Hunk 2: 新增 plugin yield 在 auth yield 后
3. Hunk 3: 构造 hookSessionID/hookMessage/hookInput，调用 plugin.trigger chat.params/chat.headers
4. Hunk 4: streamText providerOptions/maxOutputTokens/headers 改从 params/headers 取
5. Hunk 5: defaultLayer 新增 Plugin.defaultLayer

### reviewer-service.test.ts（6 hunk）
1. Plugin import 2. max_output_tokens + CapturedReviewerRequest + REVIEWER_HOOK_HEADER
3. oauthHookFixture + testEffect 4. 新测试 5. options 扩展 + requests + rejectMaxOutputTokens
6. pluginLayer 辅助函数

## 上游 owner 分析
- 目标 v1.17.20 有 LayerNode/ConfigPermissionV1 import（source 没有）
- 目标用 LayerNode.compile(Auth.node) 而非 Auth.defaultLayer
- 目标 User 类型从 core/v1/session 导入
- 目标 test 用 ProviderV2.ID/ModelV2.ID

## 目标适配
1. import: Auth 后插入 Plugin
2. yield: auth 后插入 plugin
3. hook logic: options 后 streamText 前插入
4. params: providerOptions/maxOutputTokens/headers 修改
5. defaultLayer: LayerNode.compile(Plugin.node) 替代 Plugin.defaultLayer
6. node deps: Plugin.node 加入（264 修复）
7. satisfies User 替代 MessageV2.User
8. test: ProviderV2/ModelV2 命名空间

## Hunk 映射
- service.ts 5 hunk 全适配（行号偏移 +12~25）
- test 6 hunk 全适配

## Install 指纹
- c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c（reused）

## Typecheck 证据
- opencode exit 0, core exit 0
- packages/app TS1128 pre-existing

## E/C 计算细节
- added non-blank: 114, Chinese comments: 23
- E = 91, need = 14, C = 23 PASS

## 修订历程
- v1: original 拷贝 materialize 失败
- v2: 直接编辑 0260 状态 + git diff materialize 通过
- v3: fix MessageV2.User -> User typecheck 通过
- final: SHA 6e89cc785ad4, 12973B

## 验证（最终）
- materialize 265: passed 265/265
- opencode typecheck exit 0, core typecheck exit 0

## DUAL-AUDIT SEAL（批次 261-265，第七轮）
- 正向审计：ses_019fa5b6bffegepxAh7SoPSDi2 — 5/5 PASS，无 B/G
- 逆向审计：ses_019fa44acffeOcZHDIE4EK801T — 5/5 PASS，无 B/G
- 两份结果一致，无未关闭 B/G
- 前六轮 BLOCK 修复：(1) G-01 records 过短→扩充;(2) B-01 265 测试丢弃→4个v1搜索测试;(3) B-02 264 测试断言→deny→ask+defect;(4) B-01 264 显示路径catchDefect→添加;(5) B-01 265 测试嵌套→移到describe顶层
- materialize 265: passed 265/265
- opencode/core typecheck exit 0（packages/app TS1128 pre-existing）
- install 指纹: c34975122a79ddcbf66fbef57772157b7585fea66bffa3f090abd7751608f92c
- 已知限制: 265 v2 SessionMessageTable 测试未适配（Database API 差异），v2 搜索实现完整
