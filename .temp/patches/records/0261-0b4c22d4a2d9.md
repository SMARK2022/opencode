# Record 0261-0b4c22d4a2d9 — Reviewer 复用聊天 provider hook（Codex 审查兼容）

## Source（original/0261-0b4c22d4a2d9.patch）

1. `service.ts`：layer 生成器 `yield* Plugin.Service`；`runReviewerStream` 在 streamText 前合成 `User` hook 消息（agent: "permission-reviewer"、新 SessionID/MessageID、不暴露 reviewer prompt/证据）并触发 `plugin.trigger("chat.params")`（temperature/topP/topK/maxOutputTokens/options）与 `plugin.trigger("chat.headers")`；streamText 改用 hook 后的 `params.options`/`params.maxOutputTokens`/`headers.headers`；defaultLayer 提供 Plugin。
2. `reviewer-service.test.ts`：`ReviewerRequestBody` 增 `max_output_tokens`；`CapturedReviewerRequest`（body+headers）；哨兵 `REVIEWER_HOOK_HEADER`；新 fixture（rejectMaxOutputTokens + plugin 层）与新用例（Codex 400 `Unsupported parameter: max_output_tokens` 语义下 hook 清理 caps + 注入 header + instructions 保持 string + shell 证据保留）；`pluginLayer()` stub（trigger/list/init）。

## 目标适配与 259 树冲突修订（本项为审计期全量重适配）

stale current 按 upstream-250 后像文本锚定，与本 port 的 #250 落位（Auth 导入位置、#250 注释措辞、无 Auth.node 默认层）全面漂移，materialize 首跑 conflict。逐 hunk 修订：

| hunk | 修订 |
|---|---|
| S1 导入 | preimage 重写为 state 实际导入序（MessageV2/Auth/alias/SessionRetry/MessageID），Plugin 插入 Auth 后 |
| S2 yield | preimage 补 state L104 `#250：auth.get...` 注释行，plugin 插入 auth 之后 |
| S3 hook 块 | 删除不存在的 `#250：仅 Codex 路径附加 instructions` ctx；锚定 state L404-406（reviewerMessages/options/#209 注释） |
| S4 streamText | 原样（state 437-442 逐字一致） |
| S5 defaultLayer | 重写：state L809-815（#235 注释 + Config.node/Provider.node），插入 Plugin.node provide（+2 条 #261 注释） |
| S6（新增） | 既有 `export const node` 声明修改：deps 追加 `Plugin.node`，前置 #261 注释——LayerNode 编译期依赖图完整性（stale 版完全遗漏，会导致 app-runtime 组装缺依赖） |
| T1 测试导入 | 重写为 state 实际序（ModelV2/ProviderV2/Auth/Config/PermissionReviewer/Session），Plugin 插 PermissionReviewer 后 |
| T2 类型/哨兵 | 删除不存在的 `#250：openai 基类...` 与 `#250：reviewer 测试不触及...` ctx；补 state 实际 `#250：三个 provider/凭据组合...` 两行 ctx |
| T3 fixture 块 | 补 preimage 前置 `oauthFixture`/`apiKeyFixture` 两行 ctx；移除尾部不存在的 `#250：OAuth 路径` ctx（state L52 直接是 oauth.effect） |
| T4-T6 | 原样（+3 偏移自动吸收） |
| T7 fixture 尾 | 补 `bodies,` 后 state 实际两行 `#250：Layer.mock(Session)...` ctx；删除不存在的 `#250：gpt-5 模型固定` ctx/del；起始行 137→142 |

语义保真：`MessageV2.User`→core/v1/session `User`（satisfies）、hook trigger 形状与主聊天路径 `session/llm/request.ts` 逐字一致、Codex 400 复现边界保留——源全部有效语义无删减。

## current 修改内容（sha256=6b2a4409fb654db4fc3f7a5e4d4ca3374f1b47474a6c3eafdf87e2a629729381，13216 bytes）

- 生产：service.ts 6 hunk（导入/yield/hook 块/streamText/defaultLayer/node deps）。
- 测试：reviewer-service.test.ts 7 hunk（导入/类型/fixture/hook 用例/headers 捕获/400 fixture/requests+pluginLayer）。
- 无 source hunk 删除、无测试删除；C 侧全部 #261 中文契约注释保留。

## E/C

- E = 122（plus 118 + minus 4）；C = 22 ≥ need = ceil(122×0.15) = 19。
- C 分布：S3 hook 块 2、S5 默认层 2、S6 node deps 1、测试类型/哨兵/fixture/hook 用例/pluginLayer 17——全部位于安全边界（不暴露证据）、wiring 完整性、测试意图处。

## 修订失败史（本项记录）

1. stale current conflict（导入/注释/默认层/测试锚全面漂移）→ 上表逐 hunk 重写。
2. T2 误将 state 已有注释写成 del+add → 改回 ctx。
3. T3 前置 ctx 行缩进丢失（1 空格 vs 2 空格）→ 按行号补齐。
4. T3 尾部 `#250：OAuth 路径` ctx 不存在 → 移除。
5. T7 `#250：gpt-5` 行误保留为 del → 彻底删除；起始行 137→142。
6. S5 误新增重复 `export const node`（"node has already been declared"）→ 移除并改为修改既有声明的独立 hunk；其 #261 注释误置 ctx → 移至 add 侧；del 前缀误转 ctx → 翻回。
7. 最终：materialize 261 --rebuild passed 261/261。

## 验证

- typecheck 261: 全部 workspace passed（install reused 549c6966）。
- 行为测试（.test-workspace@261）：`bun test test/permission/reviewer-service.test.ts --timeout 120000` → 4 pass / 0 fail / 43 expect（含新 hook 契约用例：max_output_tokens 清理 + 哨兵 header 注入 + instructions string + shell 证据保留）。
- E/C 复核：C=22 ≥ 19 达标。

## V1/V2 与 owner 判定

- reviewer 流在目标与上游同为 `permission/reviewer/service.ts`（V1 谱系权限路径）；Plugin hook 机制与主聊天路径共享同一 owner（plugin/index.ts），未新建平行 hook 通道——reviewer 与业务请求用同一套 chat.params/chat.headers 修正在一个既有入口。
- 测试可失败性：hook 契约用例经真实 fetch stub 捕获 wire 请求——若生产移除 plugin.trigger 或 streamText 不消费 params/headers，断言即失败（非同义反复）。

## 最终行为

- reviewer 流与主聊天共享 provider 兼容 hook：Codex 路径自动清理不支持的 output caps、注入插件要求的传输 header；默认层与 node deps 补齐 Plugin 依赖，隐藏 reviewer 流不再有 wiring 缺口。
- 批次 260-264 状态：260/261/262/263/264 全部闭合，双审一致 PASS 放行（260-264 批次报告，无未关闭 B/G）。
