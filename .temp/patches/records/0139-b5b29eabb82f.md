# Record 0139-b5b29eabb82f

## Identity
- Current SHA-256: `99d492f68e0b89bc12df2e9b3815908dfb7ab03fdb62eacf64a22a8fb349ce31`
- Bytes: **26877**
- E/C: **E=279 C=45 need>=42**
- Typecheck: `typecheck-0140-2026-07-23T21-35-57-482Z.json` 30/30
- Install: `b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`

## 修复：discoverOrSpawn Unix detached；reconnect 重绑 SIGUSR2/fwdPid；daemon-detach 契约测试

## Source intent（original）
1. ConnectionError 分类（Abort/ECONN*/fetch failed/cause）。
2. SSE 心跳默认 15s；可配置 heartbeatTimeout。
3. reconnect?: () => Promise<string>；流结束后 rebind URL 并重建 client。
4. 会话路由连接错误不 navigate home。
5. processor：非 running 仍 settle deferred；abort 时 cleanup 跳过慢 snapshot。
6. handleSubtask：agent 校验在 running 前；before+execute 保护；终态后再 after best-effort。
7. 集成测试：SDKProvider rebind + stall heartbeat。

## 上游 / tip@138
- SDK：`packages/tui/src/context/sdk.tsx`（非 cli/cmd/tui）。
- 启动：`cli/cmd/tui.ts` 的 `discoverOrSpawnDaemon` 已是选举/spawn owner。
- processor/prompt 为 Effect 会话路径。

## first divergence
无 rebind → 死端口粘滞；abort cleanup 仍跑 git snapshot；running tool 后 setup 失败粘滞；测试缺失。

## 目标适配
### 文件
- connection-error.ts + connection-error.test.ts
- sdk.tsx（mutable url, reconnectDaemon, 15s default）
- app.tsx（透传 reconnect/heartbeatTimeout）
- cli/cmd/tui.ts（reconnect → discoverOrSpawnDaemon）
- session/index.tsx（isConnectionError stay）
- processor.ts（settle + cleanup `snapshot && !aborted` + 始终 clear）
- prompt.ts（agent 先；Effect.gen before+execute；after 在终态后）
- sdk-reconnect.test.tsx（testRender + 真实 SDKProvider）

### 不做
- 不复制第二套 daemon.ts 与 tip discover 双 owner。
- 不把 after 放回终态前。

## 三方对照
| 点 | Source | tip@138 | Current |
|---|---|---|---|
| rebind | reconnect prop | 无 | 有 |
| 心跳 | 15s | 无/曾 45s 错 | 15s |
| cleanup snapshot | !aborted | 无条件 patch | cleanup 内 !aborted |
| agent 顺序 | 先校验 | 后校验 | 先校验 |
| after 顺序 | 终态后 | 终态前/无 | 终态后 |
| 测试 | SDKProvider 集成 | 无 | testRender 集成 |

## 注释位置
reconnect/rebind；15s 心跳；cleanup !aborted；agent 先；before+execute 保护；after 终态后；测试断言生产 sdk.url。

## 验证
- materialize 140 PASS
- typecheck 30/30
- bun test sdk-reconnect + connection-error → 7 pass（生产敏感）

## 审计轮次
- R1 BLOCK：缺 rebind/session stay
- R2 BLOCK：agent 顺序 / after 顺序 / hollow tests
- R3：cleanup 权威路径 + 真实 SDKProvider 测试

## 最终
连接恢复主路径完整；abort 不拖慢 snapshot；setup 失败不粘滞 running；E/C 达标。


## 调用链
TUI start → discoverOrSpawnDaemon → run({reconnect}) → SDKProvider SSE loop
→ 失败/心跳超时 → reconnectDaemon → discoverOrSpawnDaemon 新 URL → createSDK
→ session route catch：isConnectionError 则 stay；server.connected 强制 sync。

Session loop → handleSubtask：agent → running part → protected before+execute
→ catchCause terminalize → persist completed/error → after best-effort。

Processor process → onInterrupt aborted=true → ensuring cleanup：
snapshot 仅 !aborted 时 patch，然后始终清空 snapshot 并 settle tools。

## 测试意图
sdk-reconnect.test.tsx 挂载真实 SDKProvider：
- old 端口 fetch failed → 必须出现 new/global/event 且 sdk.url===new
- stalled SSE + heartbeatTimeout=50 → cancel 旧流并 rebind

若删除 reconnectDaemon/心跳，这些测试必须失败（非本地伪实现）。

## 与 #137/#140 边界
- #137 手动 compact break 不改动 reconnect。
- #140 estimatedInput overflow 不改动 handleSubtask 生命周期。
- 本项独占连接恢复与 subtask setup/after 顺序。


## Dual independent audit
- FORWARD: **PASS** (all five; no open B/G)
- REVERSE: **PASS** (all five; no open B/G)
- Typecheck: `typecheck-0140-2026-07-23T21-35-57-482Z.json` 30/30
- Install: `b1965193fab202bc94ddf89ee12a7024f63620bdf86156961857675dddb79977`
- Tip: `.temp/patches/states/0140-8acb3fecbfe7`
- Closed: 2026-07-23 batch 136-140
