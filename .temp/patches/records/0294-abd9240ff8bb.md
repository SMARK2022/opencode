# Record: 0294-abd9240ff8bb

## 基本信息
- index: 0294
- sha12: abd9240ff8bb
- subject: feat(test): 将SDK事件超时设置从1秒增加至10秒，以适应Windows环境下的延迟
- source date: Wed, 11 Jun 2026 01:30:51 +0800

## Source 行为重建
原始 patch（1516 字节，27 行）修改 1 个文件：
1. **httpapi-sdk.test.ts**：SDK SSE 首事件超时从 `"1 second"` 改为 `"10 seconds"`，添加中文注释说明 Windows 首包延迟原因。这个 helper 覆盖 default/raw 与 global/instance 的 SDK SSE 首事件；Windows 上首包可能晚于 1 秒，测试只验证事件最终到达而不定义低延迟 SLA。

## 三方比较
- **httpapi-sdk.test.ts**：目标文件存在（913 行，36047 字节）；`duration: "1 second"` 在目标第 155 行（source 期望第 179 行，偏移 -24 行）；上下文完全匹配（6 行 context + 1 行删除行逐字一致）；`function firstEvent` 唯一匹配；patches 0291-0293 不修改此文件
- **git apply**：行号偏移在容差范围内，可直接 apply

## 适配内容
- 无适配 needed，current patch 与 original 完全相同
- 行号偏移 -24 行由 git apply 自动处理

## current 修改前后
- 编辑前 SHA-256: 与 original 相同（1516 bytes）
- 编辑后 SHA-256: 与 original 相同（1516 bytes，未修改）
- materialize 294: 待验证

## hunk 映射
| original hunk | current hunk | 说明 |
|---|---|---|
| httpapi-sdk.test.ts @@ -176,7 | 同 original | 行号偏移由 git apply 处理 |

## E/C 计算
- E (有效修改行): 2（duration 值修改 1 行 + 中文注释 1 行）
- C (中文注释行): 1
  - httpapi-sdk.test.ts: "这个 helper 覆盖 default/raw 与 global/instance 的 SDK SSE 首事件；Windows 上首包可能晚于 1 秒，测试只验证事件最终到达而不定义低延迟 SLA。" (1 行)
- 满足 E > 0 时 C >= max(1, ceil(2 * 0.15)) = 1，1 >= 1 ✓

## 安装与测试
- typecheck: 待批次验证
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)

## 不一致
- 无（行号偏移由 git apply 处理）

## 物化验证
- 待验证

## Source 行为重建（详细）

### Hunk 1: httpapi-sdk.test.ts SSE 首事件超时 (@@ -176,7 +176,8)
- **函数上下文**：`firstEvent` 是测试 helper，封装 SDK SSE 流的首事件等待逻辑；接收一个 `open` 回调，返回首个事件或超时失败
- **Effect 链路**：`events.stream.next()` → `Effect.timeoutOrElse({ duration, orElse })` — 超时后 `Effect.fail(new Error("timed out waiting for SDK event"))`
- **变更内容**：`duration: "1 second"` → `duration: "10 seconds"`；新增中文注释说明 Windows 首包延迟原因
- **覆盖范围**：`firstEvent` 被 default/raw 与 global/instance 两组 SDK SSE 测试共用，一次修改覆盖所有首事件等待场景
- **Windows 延迟根因**：Windows 上进程启动、IPC 握手、SSE 首包的端到端延迟可能超过 1s，尤其在 CI 环境或高负载下；1s 超时导致间歇性 false negative
- **不变量**：测试只验证事件最终到达，不定义低延迟 SLA；10s 超时足以覆盖 Windows 正常负载下的首包延迟
- **超时层次**：Effect 级 `timeoutOrElse` 10s 控制首事件等待；测试运行器级 15s 超时（`it.live(..., 15_000)`）控制整个用例执行，两层独立且不冲突

## 三方比较（详细）

### httpapi-sdk.test.ts
- **文件存在性**：target 存在（913 行，36047 字节），无需创建
- **行号偏移**：source `@@ -176,7` → target 实际行 155（偏移 -24 行），在 git apply 容差范围内
- **上下文匹配**：6 行 context（`Effect.flatMap` → `call(() => events.stream.next())` → `Effect.timeoutOrElse`）+ 1 行删除行（`duration: "1 second"`）逐字一致
- **唯一性**：`function firstEvent` 在文件中唯一匹配，无歧义
- **前置 patch 影响**：patches 0291-0293 不修改 httpapi-sdk.test.ts，无叠加冲突
- **适配结论**：current patch 与 original 完全相同，无需适配；行号偏移由 git apply 自动处理

## 物化验证（详细）
- patch apply：current patch 成功 apply，行号偏移由 git apply 自动处理
- SHA-256：current patch 与 original 完全相同（1516 bytes，未修改）
- typecheck：待批次验证
- 测试运行：待批次验证（超时变更不影响正确性，只影响等待时间上限）
- install 指纹：08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4（reused，无依赖变更）

## 审计历史

### 第一轮双审计
- 审计 A：PASS（第一轮双审计通过）
- 审计 B：PASS（第一轮双审计通过）
- 仲裁结论：待仲裁

## 状态
- PASS（PASS（第一轮双审计通过））

## DUAL-AUDIT SEAL（批次 291-295，第一轮）
- 正向审计 (ses_011d96b20): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- 逆向审计 (ses_011d9572e): 5/5 PASS, 无 B/G (N-01~N-02 非阻塞)
- materialize 295: passed 295/295
- packages/opencode typecheck exit 0, packages/tui typecheck exit 0 (独立验证)
- packages/app TS1128 pre-existing (非阻塞)
- install 指纹: 08977fbe66d47da0800767963c24c6e8f51a8585432857c89f8e573be251b5f4 (reused)
- E/C: 291=16/8, 292=60/10, 293=80/12, 294=2/1, 295=5/2 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
- 上游更强实现（#215 closeSink finalizer, #289 normalizePowerShellOutput）保留为基线