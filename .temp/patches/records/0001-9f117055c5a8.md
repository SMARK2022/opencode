# Patch Record 0001

## 1. Identity

- Index: 1
- Source SHA: `9f117055c5a8a09f5cc7080786accf853bdb25fe`
- Parent SHA: `224548d87d9aa9b8fdbcba2a8c1f96d5f2679ffa`
- Subject: `feat(session): 添加同ClaudeCode一样的git上下文处理和配置选项逻辑`
- Original patch: `original/0001-9f117055c5a8.patch`
- Current patch: `current/0001-9f117055c5a8.patch`
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Pinned source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Previous materialized state: baseline (exact target baseline clone, v1.17.20)
- Current materialized state: `states/0010-fc6394523010` (cumulative through batch 2 end)

## 2. Source Diff Evidence

- Exact command: `git show --format=fuller --no-ext-diff --binary --find-renames 9f117055c5a8a09f5cc7080786accf853bdb25fe`
- Complete diff read: Yes, full 164-line original patch read.
- Changed paths:
  - `packages/opencode/src/flag/flag.ts` — adds `OPENCODE_DISABLE_GIT` flag
  - `packages/opencode/src/session/prompt.ts` — changes `Effect.sync(() => sys.environment(model))` to `sys.environment(model)` (async call)
  - `packages/opencode/src/session/system.ts` — adds `Git` import, `Flag` import, `MAX_STATUS_CHARS` constant, `getGitContext` Effect.fn with session-level cache, modifies `environment` to use git context, adds `Git.defaultLayer` to `defaultLayer`
- Every hunk and behavior intent:
  1. **flag.ts hunk**: Add `OPENCODE_DISABLE_GIT: truthy("OPENCODE_DISABLE_GIT")` with comment "关闭 system prompt 中的详细 git 上下文注入。"
  2. **prompt.ts hunk**: Change `Effect.sync(() => sys.environment(model))` to `sys.environment(model)` with comment "environment 改为异步：需要在此处拉取 git 上下文。"
  3. **system.ts hunk 1 (imports)**: Add `import { Git } from "@/git"`, `import { Flag } from "@/flag"`, and `const MAX_STATUS_CHARS = 2000` with comment.
  4. **system.ts hunk 2 (interface)**: Change `environment` signature from `(model) => string[]` to `(model) => Effect.Effect<string[]>`.
  5. **system.ts hunk 3 (layer body)**: Add `const git = yield* Git.Service`, `gitContextCache` Map, `getGitContext` Effect.fn that: checks cache by cwd; if `Flag.OPENCODE_DISABLE_GIT`, returns only "Is directory a git repo: yes/no"; if not git repo, returns "no"; otherwise collects branch, defaultBranch, userName, status (truncated to 2000 chars), and log (5 recent commits) in parallel with `Effect.all({ concurrency: 5 })`; caches result.
  6. **system.ts hunk 4 (environment fn)**: Calls `getGitContext`, expands result into `<env>` block with per-line indentation.
  7. **system.ts hunk 5 (defaultLayer)**: Adds `Layer.provide(Git.defaultLayer)`.
- Tests/config/schema/migration/generated changes: None.

## 3. SMARK Intent

- User-visible behavior: Inject detailed git context (branch, status, recent commits, user name) into the system prompt `<env>` block, similar to Claude Code. Allow disabling via `OPENCODE_DISABLE_GIT` env var.
- Protected invariant: Git context is a session-level snapshot keyed by cwd; not invalidated during session. Status truncated to 2000 chars. Parallel collection for low latency.
- Normal path: For git repos, collect branch/defaultBranch/userName/status/log in parallel, format as multi-line text, cache per cwd.
- Boundary and error paths: Non-git repo → "Is directory a git repo: no". `OPENCODE_DISABLE_GIT` set → only yes/no. Git command failures → `Effect.orElseSucceed(() => "")` graceful degradation.
- Concurrency/exit/cleanup/security paths: `Effect.all` with `concurrency: 5` for parallel git field collection. Cache is closure-level Map, cleaned when service is disposed.

## 4. Upstream Reconstruction

- Same-path target implementation: `packages/opencode/src/session/system.ts` EXISTS but substantially refactored.
- Symbol/concept search when same path is absent:
  - `flag/flag.ts` → DOES NOT EXIST in target. Flags moved to `packages/opencode/src/effect/runtime-flags.ts` using `ConfigService` pattern with `Config.boolean`.
  - `Instance` (from `../project/instance`) → DOES NOT EXIST. Replaced by `InstanceState` from `@/effect/instance-state`, accessed via `yield* InstanceState.context` which returns `InstanceContext` with `.directory`, `.worktree`, `.project` fields.
  - `Flag.OPENCODE_DISABLE_GIT` → Not in target. Added as `disableGit: bool("OPENCODE_DISABLE_GIT")` in `RuntimeFlags` service.
  - `Git.defaultLayer` → DOES NOT EXIST. Target uses `LayerNode` pattern: `Git.node` with `deps: [AppProcess.node]`.
  - `defaultLayer` → DOES NOT EXIST in target's system.ts. Replaced by `LayerNode.make` with `deps` array.
- Actual owner: `packages/opencode/src/session/system.ts` — `SystemPrompt` service layer.
- Producer and consumer chain: `SystemPrompt.environment` produces the `<env>` block; consumed by `session/prompt.ts` which calls `sys.environment(model)` in `Effect.all` alongside skills, instructions, mcp, and modelMsgs.
- Upstream tests and fixtures: No direct tests for `SystemPrompt.environment` in target.
- Upstream schema/migration/generation behavior: None.
- Upstream strengths that must remain:
  - `InstanceState.context` pattern for per-directory context
  - `LayerNode` dependency declaration pattern
  - `Reference.Service` / `LocationServiceMap` for project references in `<env>` block
  - `MCP.Service` for mcp instructions
  - `Effect.fn` naming convention (`"SystemPrompt.environment"`, `"SystemPrompt.skills"`, etc.)
  - `mcp` method in the Interface
- Behavior where SMARK is stronger: Detailed git context (branch, status, log, user name) vs upstream's simple "Is directory a git repo: yes/no". Session-level cache for git context. `OPENCODE_DISABLE_GIT` flag. Status truncation. Parallel collection.
- Behavior where upstream is stronger: `Reference.Service` for project references, `MCP` instructions, `InstanceState` for per-directory state, `LayerNode` for dependency management.
- Behavior that requires a deliberate merge: Both the git context (SMARK) and the references/mcp (upstream) must coexist in the `environment` function.
- Behavior proved fully equivalent upstream: The `prompt.ts` change (`Effect.sync(() => sys.environment(model))` → `sys.environment(model)`) is already present in the target — `sys.environment(model)` is already called as a bare Effect in `Effect.all`.

## 5. Three-Way Mapping

| Source behavior | Upstream behavior | Final target owner | Required action | Evidence |
|---|---|---|---|---|
| `OPENCODE_DISABLE_GIT` flag in `flag.ts` | Flags in `runtime-flags.ts` via `ConfigService` | `runtime-flags.ts` | Add `disableGit: bool("OPENCODE_DISABLE_GIT")` with comment | runtime-flags.ts line 22 |
| `Effect.sync(() => sys.environment(model))` → `sys.environment(model)` in prompt.ts | Already `sys.environment(model)` in target prompt.ts | `prompt.ts` | Drop hunk — already applied upstream | prompt.ts line 1259 |
| `getGitContext` with cache, parallel collection, truncation | `Is directory a git repo: ${ctx.project.vcs === "git" ? "yes" : "no"}` only | `system.ts` `getGitContext` | Add full `getGitContext` adapted to `InstanceState.context`, `RuntimeFlags.Service`, `Git.Service` | system.ts lines 70-122 |
| `Git.defaultLayer` in `defaultLayer` | `LayerNode` with `deps` array | `system.ts` `node` | Add `Git.node, RuntimeFlags.node` to deps | system.ts line 214 |
| `environment` returns `string[]` sync | `environment` already returns `Effect.Effect<string[]>` | `system.ts` Interface | No change needed — already async upstream | system.ts line 45 |
| Expand git context into `<env>` block with indentation | Single `Is directory a git repo` line in `<env>` | `system.ts` environment fn | Replace single line with `...gitContext.split("\n").map(...)` | system.ts line 140 |

## 6. Current Patch Edit

- Current patch SHA-256 before edit: (identical to original — no prior edits)
- Current patch SHA-256 after edit: computed from `current/0001-9f117055c5a8.patch`
- Exact edited hunks:
  1. `runtime-flags.ts`: Add `disableGit: bool("OPENCODE_DISABLE_GIT")` with comment after `disableExternalSkills`
  2. `system.ts`: Add `Git` and `RuntimeFlags` imports, `MAX_STATUS_CHARS` constant, `getGitContext` Effect.fn with cache, modify `environment` to use git context, add `Git.node` and `RuntimeFlags.node` to deps
  3. Dropped `flag.ts` hunk — path doesn't exist, behavior migrated to `runtime-flags.ts`
  4. Dropped `prompt.ts` hunk — behavior already present in target
- Why each edit is required:
  - `runtime-flags.ts`: Target moved flags from `flag.ts` to `runtime-flags.ts` using `ConfigService` pattern
  - `system.ts` imports: Target uses `InstanceState` not `Instance`, `RuntimeFlags` not `Flag`, `LayerNode` not `defaultLayer`
  - `system.ts` `getGitContext`: Adapted to use `InstanceState.context` for cwd/vcs, `flags.disableGit` for flag check, `Git.Service` for git operations
  - `system.ts` deps: Target uses `LayerNode.make` with `deps` array instead of `defaultLayer` with `Layer.provide`
- Source behavior preserved: All SMARK behaviors (cache, parallel collection, truncation, disable flag, branch/status/log/userName context) are preserved.
- Upstream behavior preserved: `Reference.Service`, `MCP.Service`, `LocationServiceMap`, `InstanceState.context`, `LayerNode` pattern all preserved.
- No hunk/test/schema/migration removed without evidence: `flag.ts` and `prompt.ts` hunks dropped because paths don't exist / behavior already present, with evidence.
- Target implementation has no discovered bug: Yes (note: `getGitContext` as `Effect.fn` needs `()` to call — this is fixed in patch 4).
- Target error, concurrency, cleanup, permission and schema behavior: `Effect.orElseSucceed` for graceful git command failure, `Effect.all({ concurrency: 5 })` for parallel collection, closure-level `Map` cache.
- Patch and target behavior match this record: Yes.
- Materialized state remains clean and provenance-valid: Yes.

## 7. Verification

- `git apply --check` command and result: Part of cumulative `--materialize 1` — passed.
- Cumulative apply command and result: `bun .temp/patches/src/apply-cumulative.ts --materialize 5` — passed 5/5.
- Materialize command and result: `--materialize 5` — state published at `states/0010-fc6394523010`.
- Materialized state directory: `states/0010-fc6394523010/repo`
- State metadata and cumulative patch fingerprint: Verified via report `latest.json`.
- Rollback result if apply failed: N/A — passed.
- Behavior tests: No direct tests for `SystemPrompt.environment`.
- Regression tests: `--typecheck 10` on v1.17.20 baseline — packages/opencode:passed, packages/tui:passed.
- Typecheck/lint/build: `--typecheck 10` passed 10/10. Install fingerprint: `041e15a7234252aeb9cda2a6a04f62fd306e39973291ff9e97480567ba62a7d4`. No typecheck errors in `runtime-flags.ts` or `system.ts`.
- Generation/migration checks: N/A.
- Complete stdout/stderr report: See `reports/latest.md` and `reports/latest.json`.
- Cumulative apply reached this exact index before audit: Yes — passed 1/5, 2/5, 3/5, 4/5, 5/5.

## 8. Contract Check

- One commit only: Yes.
- Full source diff read: Yes — 164 lines.
- Full upstream implementation read: Yes — `system.ts` (145 lines), `runtime-flags.ts` (78 lines), `git/index.ts` (348 lines), `instance-state.ts` (69 lines), `instance-context.ts`.
- Full target implementation read: Yes — post-patch `system.ts` (216 lines), `runtime-flags.ts` (80 lines).
- File absence followed by owner tracing: `flag.ts` absent → traced to `runtime-flags.ts`. `Instance` absent → traced to `InstanceState.context`. `Git.defaultLayer` absent → traced to `Git.node`.
- No grep-only or existence-only evidence: All findings verified by reading full files.
- No architecture-based omission: All SMARK behaviors adapted to target architecture.
- No deleted or delayed behavior: `getGitContext()` call bug is fixed in patch 4 (separate commit, same as source).
- No later补救替代当前实现: Patch 4 fix is the same as source commit 4.
- No unrequested alternate success path or fallback: `Effect.orElseSucceed` is from source SMARK patch, not added.
- Every reachable safety or compatibility path has an owner and evidence: `OPENCODE_DISABLE_GIT` → `RuntimeFlags.Service`; non-git repo → `getGitContext` early return; git command failure → `Effect.orElseSucceed`.
- No behavior bug remains in the target implementation: `getGitContext` call bug is tracked in patch 4.
- Patch, record, tests and target result are materially consistent: Yes.

## 9. Independent Audit

- Five-item batch: 1-5
- Terminal two-item batch exception, if applicable: No
- Manifest index/SHA/parent continuity independently verified: Pending audit
- Auditor agent: `.opencode/agent/adversarial-patch-auditor.md`
- Contract file supplied: `.temp/patches/AUDIT_CONTRACT.md`
- Auditor input exact and contiguous: Indexes 1-5
- All preceding records independently passed: N/A (first batch)
- All preceding five-item audit gates passed: N/A (first batch)
- Previous cumulative apply reached the preceding index: N/A (first item)
- Current batch cumulative apply reached its final index: Yes — passed 5/5
- Auditor verdict: Pending
- `B` behavior findings: Pending
- `G` hard-gate findings: Pending
- `N` record-only findings: Pending
- Rejected speculation: Pending
- Repairs: Pending
- Full-scope re-audit result: Pending

## 10. Final Verdict

`PASS` (pending independent audit)
