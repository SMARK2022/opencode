# Record 0360-0d6b83d7319b

## Identity
- manifest parent: e7325006afac (0359)
- subject: fix(tui): SubagentFooter regex doesn't match hyphens → permission-reviewer label falls back to Subagent
- status: MATERIALIZED
- materialized state: `.temp/patches/states/0360-0d6b83d7319b` (passed 360/360)
- current patch: `.temp/patches/current/0360-0d6b83d7319b.patch` (1818 bytes)

## Source intent
`SubagentFooter` extracts the agent name from the session title with the regex
`/@(\w+) subagent/`. `\w` does not include `-`, so for the reviewer subagent
(`@permission-reviewer subagent`) the `\w+` matcher stops at "permission",
the overall regex fails to match, and the footer label falls back to the generic
"Subagent". Fix: widen the character class to `[\w-]+`. This is a strict
superset of `\w+`, so hyphen-free agent names (general/explore) behave
identically, and the existing fallback when the title has no `(@… subagent)`
marker at all is unchanged.

## Source patch anatomy (2 files)
- `packages/opencode/src/cli/cmd/tui/routes/session/subagent-footer.tsx` —
  regex change + 2 rationale comments.
- `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` — adds a
  behavior-level assertion in a large TUI flow test: after navigating into the
  reviewer child session, the footer must render "Permission-Reviewer" (not
  "Subagent"). 7-line change (comment + `frame` capture + `expect`).

## Target mapping (state 0359)
| Source file | Target file | Status |
|---|---|---|
| `packages/opencode/src/cli/cmd/tui/routes/session/subagent-footer.tsx` | `packages/tui/src/routes/session/subagent-footer.tsx` | MIGRATED (EXISTS, identical `agentMatch` line) |
| `packages/opencode/test/cli/cmd/tui/session-message-render.test.tsx` | — | SKIPPED — no migrated analog exists in `packages/tui/test` (verified: no `session-message-render*` file; the only "subagent"-matching tui tests are `config.test.tsx` and `cli/tui/inline-tool-wrap-snapshot.test.tsx`, neither exercises the reviewer footer label) |

## Target-adaptation decisions
1. **Regex line is verbatim-identical** in the target
   (`const agentMatch = s.title.match(/@(\w+) subagent/)` at L24); the curated
   patch replaces only that line, with the source's two rationale comments
   (half-width punctuation as authored) inserted above it.
2. **Test skipped per established precedent**: the source's test file is a
   heavyweight headless-TUI end-to-end flow that has no receiver in the target
   test tree (TUI layout tests in `packages/tui/test/routes/session/*` cover
   spacing/follow/layout, not the footer-label flow). Porting a synthetic
   equivalent would duplicate the render harness without upstream evidence; the
   prescription in the batch brief ("verify; may skip per precedent") applies.
   Behavioral protection for the regex now rests on the code-level guarantee
   that `[\w-]+` is a superset of `\w+`.

## E/C audit
E=1, C=2, need=1, PASS. (E: the `[\w-]+` code line; C: the two CJK rationale
comment lines.)

## Materialization
- Preflight: `git apply --check --index` on pristine 0359-state copy → exit 0.
- Command: `bun .temp/patches/src/apply-cumulative.ts --materialize 360` → "passed
  360/360", state at `states/0360-0d6b83d7319b`.
- Post-apply verification: `[@\w-]+` regex present at subagent-footer.tsx L26.

## Line accounting
- `packages/tui/src/routes/session/subagent-footer.tsx`: +3/-1.
- (test file skipped: +0/-0).

## Risks / notes
- No behavioral risk outside titles containing `@<hyphenated-name> subagent`;
  `Locale.titlecase(agentMatch[1])` receives the full hyphenated name
  (`permission-reviewer` → "Permission-Reviewer"), matching the source's
  intended label.
- Typecheck of the tsx change is covered by the batch-end `--typecheck 360`.

## Per-item diff stats
- fix0360.diff bytes: 919; patch file with header: 1820, single file, single hunk.

## DUAL-AUDIT SEAL (batch 0356-0360)
- R1 forward auditor (ses_fd0070cf9): 5/5 PASS, batch APPROVE, no open B/G
- R1 backward auditor (ses_fd006f98): 5/5 PASS, batch APPROVE, no open B/G
- N items (non-blocking): 0356 docs trailing LF; 0357 record wording; 0358 v1 gen stale (source also unchanged — upstream regeneration scope); 0359 record byte count (fixed); 0360 test assertion not ported (no harness)
- Install: c6491269 reused, bun 1.3.14; typecheck 360/360, only packages/app TS1128 environmental
- Independent typechecks: packages/core/tui/sdk-js PASS; packages/opencode 3 pre-existing TS2353
- Verdict: BATCH 0356-0360 CLOSED
