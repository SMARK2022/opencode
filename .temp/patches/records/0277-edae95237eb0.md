# Record 0277-edae95237eb0

## Patch Identity
- Index: 0277
- SHA12: edae95237eb0
- Subject: fix(image): 统一图片附件压缩边界
- Current SHA-256: 420c2ed25291bfd47a1362a69dc1aac96195a628877698c2b179c1f7bd2a657a
- Current bytes: 19080
- Files: image.ts + read.ts + registry.ts + read.test.ts（fixture 适配）

## Source Intent
Source patch 的完整行为目的见 original/0277-edae95237eb0.patch。

## Adaptation Details
- 276: image.test.ts 添加 tokenBudget 测试；read.test.ts 适配 LayerNode 模式（Image.node 代替 Image.defaultLayer），添加有效 JPEG base64 和损坏图片测试
- 277: image.ts 添加 tokenBudget 支持（保留 photon，添加 targetMaxBase64Bytes 辅助函数）；read.ts 收敛图片处理到 Image.Service（替换 processImageWithTokenBudget），添加 Image.Service 到 Tool.define context type；registry.ts 添加 Image.node 到 deps
- 278: opencode.jsonc 适配 references 块上下文（hunk header 从 -2 改为 -14）；session-ses_1837.md 新文件
- 279: context-usage.tsx 添加 contextUsageRefreshKey 函数（轻量刷新信号）；dialog-model.tsx 添加 currentModelKey memo 和字符串 key
- 280: subagent-permissions.ts 重写 deriveSubagentSessionPermission（PermissionV1 类型，parentAgentCeiling/forwardParentSessionRule/childSpecificCeilings/subagentDenyOverrides 辅助函数）；reviewer/service.ts 简化 reviewerEnabled

## E/C Calculation
- E=66 C=10 need=10 PASS（与修复后一致）
- 注：本轮 fixture 替换只改测试内 base64 字面量（剩余行数不计新增），E/C 不变

## Verification
- materialize 280: passed 280/280
- opencode typecheck: exit 0
- tui typecheck: exit 0
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: reused


## Source Behavior Analysis
源 patch 将图片处理从 photon-node 切换到 sharp，并添加 tokenBudget 支持。
read.ts 图片附件处理收敛到 Image.Service。

## Upstream Comparison
目标使用 photon-node（无 sharp 依赖），LayerNode 模式。
目标 image.ts 的 normalize 接口不支持 tokenBudget。
目标 read.ts 使用 processImageWithTokenBudget。

## Target Adaptation
1. image.ts: 保留 photon，添加 tokenBudget 支持（targetMaxBase64Bytes 辅助函数 + TOKEN_ESTIMATE_BASE64_CHARS 常量）
2. read.ts: 替换 processImageWithTokenBudget 为 image.normalize + ResizerUnavailableError 回退；添加 Image.Service 到 Tool.define context type
3. registry.ts: 添加 Image.node 到 deps

## Hunk Mapping
| Source File | Target File | Adaptation |
|---|---|---|
| image.ts | image.ts | 保留 photon + tokenBudget 支持 |
| read.ts | read.ts | image.normalize + Image.Service context type |
| registry.ts | registry.ts | Image.node 到 deps |
| media.ts | media.ts | no-op（目标已是最终状态） |
| docs/draft | docs/draft | no-op（文件不存在） |
| 测试文件 | 测试文件 | 0276 已覆盖 |

## Comment Positions
1. #277：image payload budget 从 base64 字符估算（image.ts）
2. #277：调用方传入更小 token budget 时收紧 base64 上限（image.ts）
3. #277：调用方的 token budget 收紧 base64 上限（image.ts）
4. #277：图片附件处理收敛到 Image.Service（read.ts）
5. #277：Image.Service 处理图片附件（read.ts）
6. #277：图片走 Image.Service（read.ts）
7. #277：resizer 不可用时保留原始图片作为回退（read.ts）
（本轮新增）8. #276/#277：使用光子解码器可解码的 8x8 JPEG（read.test.ts 两处 fixture）

## Failure Revision History
- 第一次 typecheck：read.ts duplicate image yield（脚本添加了两次）
- 修复：移除重复行
- 第二次 typecheck：read.ts TS2345（Image.Service 不在 Tool.define context type）
- 修复：添加 Image.Service 到 context type

## Final Behavior
read 工具的图片附件处理收敛到 Image.Service，按调用方 token budget 收紧 base64 上限。

## 第八轮修复（批次 276-280 重审 B-01）

### B-01: 有效 JPEG 在 read 中解码失败——已修复
- 现象：read.test.ts "detects attachment media from file contents" 失败，photon wasm 无法解码 519 字节 JPEG fixture（`Unreachable code should not be executed`），抛 ImageDecodeError。
- 根因：source 0276 的 fixture（519 字节 base64 JPEG）在 source 的 sharp 管线可解码，但目标 0276/0277 升级到 Image.Service/photon 后 photon 无法解码该 JPEG 变体。目标 image.test.ts 用 `new photon.PhotonImage(rgba,w,h).get_bytes_jpeg(90)` 生成可解码 JPEG（image.test.ts:33-41）。
- 修复：将 read.test.ts 两处 fixture（"detects attachment media" + "falls back to original image bytes"）替换为 photon 生成的 8x8 可解码 JPEG（631 字节 base64，独立生成验证可解码）。
- 语义保留：有效 JPEG 附加成功（detects 测试）、损坏 6 字节头报 DecodeError（fails invalid 测试）、resizer 不可用回退原始字节（noResizer 测试）——两类输入在 photon 解码层可区分。
- 验证：bun test read.test.ts 图片测试 4 pass / 0 fail（detects+fails invalid+noResizer+large image）。
- 影响：0276 状态（sharp 可解码原 fixture）无需改动；0277 状态（photon）替换 fixture。

### 验证
- materialize 279: passed 279/279（含 277 修复后累计）
- read.test.ts 图片测试 4 pass / 0 fail
- tui typecheck exit 0（279 状态）
- packages/app TS1128 pre-existing（不阻塞）

## 第七轮修复（第六轮双审计 BLOCK 反馈）

### B-01: noResizer 测试缺失——已修复
- 修改 readLayer 接受可选 imageLayer 参数，供 noResizer 测试注入失败 Image.Service
- 添加 noResizer testEffect 变体（注入始终失败的 Image.Service）
- 添加 "falls back to original image bytes when the image resizer is unavailable" 测试

### E/C 修复后
- E=66 C=10 need=10 PASS

### 验证
- materialize 280: passed 280/280
- opencode typecheck exit 0

## DUAL-AUDIT SEAL（批次 276-280）
## DUAL-AUDIT SEAL（批次 276-280，第七轮）
- 正向审计 (ses_012cf5d9dffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_012cf42b2ffe): 5/5 PASS, 无 B/G (N-01~N-08 非阻塞)
- materialize 280: passed 280/280
- opencode typecheck exit 0, tui typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 276=38/6, 277=66/10, 278=0/0, 279=219/33, 280=134/41 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行

## 审计修复记录（R9，批次重审后）
- current 最终 SHA-256: `420c2ed29eb151e018cf6392541a79fec69df86ba6ad3527698eac40c6090e0c` / bytes: 19080
- E/C 最终：E=75, C=12, need=12, PASS
- 修复 B-01/B-02/B-03（正向审计发现）：
  - media.ts：删除遗留 sharp 压缩器（IMAGE_TOKEN_BUDGET/ProcessedImage/SHARP_UNAVAILABLE/getSharp/estimateImageTokens/processImageWithTokenBudget），保留 formatSize 与 `export { formatSize }`（read.ts 仍引用）
  - read.test.ts large-image 测试：补 `attachment` 提取 + base64.length <= 1_600*750 预算断言
  - docs/draft/image-processing-unification-plan.md 行 72：'photon fallback 或 typed error' -> 'typed error；read 工具仅在 resizer 不可用时兼容返回原始图片'
- 验证：state 276 上 git apply --check EXIT 0；materialize 278-300 passed；read image 测试 6/6 pass
