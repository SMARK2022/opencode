# Patch 0203 — feat: 串联修正 auto 路由逻辑

## 1. Identity
- Index: 203
- Source SHA: 773ad40748faae9a76d1b24f9304558c1c24a33e
- Parent SHA: 8ae5a36f940752675c870aab85b165c1796a432a (patch 202)
- Subject: feat: 串联修正 auto 路由逻辑
- Original patch: `.temp/patches/original/0203-773ad40748fa.patch`
- Current patch: `.temp/patches/current/0203-773ad40748fa.patch`
- Pinned source tip: `.temp/patches/states/0202-8ae5a36f9407/repo`
- Previous materialized state: `0202-8ae5a36f9407`
- Current materialized state: `0203-773ad40748fa`
- Record path: `.temp/patches/records/0203-773ad40748fa.md`
- Record created before first current edit: yes
- Record is independent for this commit and is not merged with another index: yes

## 2. Source Diff Evidence
- Exact command: `git diff` against tip@202 state repo
- Changed paths (11 files):
  1. `packages/opencode/src/agent/agent.ts` — tighten Auto comment; add `external_directory: { "*": "auto", ...whitelisted: "allow" }` to Auto permission
  2. `packages/opencode/src/permission/auto.ts` — add `reviewerDisabled` input; rewrite routing (dangerous→deny, shell-external→allow, non-shell external→ask, safe→allow, shell general→allow, cautious→reviewer with Auto fail-closed)
  3. `packages/opencode/src/permission/index.ts` — add `reviewerDisabled` to PermissionAuto.evaluate call (capture-at-ask preserved for AppNodeBuilder compatibility; defaultLayer hunk DROPPED)
  4. `packages/opencode/src/permission/precheck.ts` — add external_directory shell branch in evaluate; expand SENSITIVE_PATH_PATTERN (SSH key names, Windows home); cautiousRaw/readsSensitivePath add ls/dir/gci; hasSensitivePath add dir/key variants
  5. `packages/opencode/src/permission/reviewer/policy/policy.md` — add "direct reads of credentials" to deny bullet
  6. `packages/opencode/src/permission/reviewer/service.ts` — reviewerEnabled helper; defaultLayerWithSession; autoReview local var
  7. `packages/opencode/src/tool/shell.ts` — 3-way merge: add `agent: ctx.agent` to external_directory + bash metadata; bash metadata gains action_kind/cwd/shell; FILES set adds get-childitem/gci
  8. `packages/opencode/test/agent/agent.test.ts` — Auto test: external_directory:auto assertion; "/some/other/path" ask→auto; comment bash:auto→shell:auto
  9. `packages/opencode/test/permission/auto.test.ts` — shellExternalDirectory helper; 6 new tests (PowerShell credentials, SSH keys, external directory, non-shell external, shell general allow, Auto fail-closed, user reviewer config); general test ask→allow
  10. `packages/opencode/test/permission/next.test.ts` — opaque bash test (echo $HOME pending→allow); non-shell general test (user approval→reviewer)
  11. `packages/opencode/test/tool/shell.test.ts` — extDirReq.metadata assertions (action_kind/command/cwd/agent/shell)
- Behavior intent: Wire the 4-level precheck into the full auto routing. Shell-origin external_directory reuses the same command evidence (no double LLM). Non-shell external_directory stays on user approval. Dev-period shell general auto-allows. Native Auto fails closed when reviewer is missing (no clickable Allow exposing sensitive output), unless explicit `approvals_reviewer: "user"` config opts back to user approval.

## 3. SMARK Intent
- User-visible behavior: Auto agent's shell commands route through precheck→reviewer without redundant external_directory prompts. Sensitive reads (SSH keys, credentials) route to reviewer. Opaque shell commands auto-allow during the dev period.
- Protected invariant: Native Auto must not degrade cautious shell to a clickable user ask when reviewer wiring is missing — fail closed (deny) to prevent sensitive shell output exposure via Allow. Explicit `approvals_reviewer: "user"` overrides this for small runtimes.
- Boundary paths: `dangerous`→deny (first); shell-external→allow (pass to bash auto); non-shell external→ask; safe→allow; shell general→allow; cautious→reviewer (or fail-closed for Auto).

## 4. Upstream Reconstruction
- The original patch targeted the old Bus/defaultLayer architecture. tip@202 uses LayerNode/AppNodeBuilder.
- CRITICAL adaptation: The original moved collaborator capture to layer-top and added a `Permission.defaultLayer` hunk consuming `PermissionReviewer.defaultLayerWithSession`. In the AppNodeBuilder pattern, `Config`/`PermissionReviewer` are NOT declared deps of `Permission.node`, so layer-top capture would yield `None` and break all reviewed tests. Adaptation: capture-at-ask preserved (runtime context has all services); only `reviewerDisabled` added to the evaluate call. The index.ts defaultLayer hunk is DROPPED (no defaultLayer in target). `defaultLayerWithSession` still added to service.ts for API parity (uses `LayerNode.compile(Session.node)` since Session has no defaultLayer).
- shell.ts 3-way merge: tip@202 already had `action_kind/command/cwd/shell` in external_directory metadata (from a prior SMARK patch); only `agent: ctx.agent` added there. bash metadata at tip@202 had only `command`; gained `action_kind/cwd/shell/agent`.
- `[local-smark]` markers → `#203` Chinese comments.
- Windows-only `it.live` PowerShell test loops in shell.test.ts SKIPPED (cannot validate on macOS; behavior covered by auto.test.ts unit tests). The cross-platform `extDirReq.metadata` assertions are kept.

## 5. Three-Way Mapping

| Source behavior | Upstream (tip@202) | Final target owner | Required action |
|---|---|---|---|
| Auto external_directory:auto | Auto bash:auto only | agent.ts:190 | Add external_directory auto+whitelist |
| auto.ts shell-external/general/cautious routing | 202 4-level routing | auto.ts:47 | Rewrite with isShell/isShellExternalDirectory/reviewerDisabled/Auto fail-closed |
| index.ts reviewerDisabled | 202 evaluate call | index.ts:129 | Add reviewerDisabled (capture-at-ask kept) |
| index.ts defaultLayer hunk | no defaultLayer (LayerNode) | — | DROPPED |
| precheck external_directory shell branch | 202 evaluate | precheck.ts:131 | Add shell-origin branch |
| precheck SENSITIVE_PATH Windows/SSH expansion | 202 expanded pattern | precheck.ts:34 | Add SSH_PRIVATE_KEY_NAME + WINDOWS_HOME patterns |
| precheck cautiousRaw/readsSensitivePath ls/dir/gci | 202 reader set | precheck.ts | Add Get-ChildItem/gci/ls/dir |
| precheck hasSensitivePath dir/key variants | 202 token paths | precheck.ts:620 | Add /.ssh, /.aws, id_dsa/ecdsa/sk variants |
| policy.md credentials deny | deny bullet | policy.md:3 | Prepend direct-reads clause |
| service.ts reviewerEnabled + autoReview | `#198` disabled check | service.ts:62 | reviewerEnabled helper + autoReview local |
| service.ts defaultLayerWithSession | no such export | service.ts:187 | Add (LayerNode.compile(Session.node)) |
| shell.ts external_directory +agent | 3-way merge (has evidence) | shell.ts:330 | Add agent: ctx.agent |
| shell.ts bash metadata +action_kind/cwd/shell/agent | only command | shell.ts:346 | Add action_kind/cwd/shell/agent |
| shell.ts FILES get-childitem/gci | get-content only | shell.ts:95 | Add get-childitem, gci |
| agent.test.ts external_directory:auto | ask | agent.test.ts:114 | ask→auto |
| auto.test.ts 6 new tests | 202 tests | auto.test.ts | Add helper + tests; general ask→allow |
| next.test.ts opaque bash allow + non-shell reviewer | 202 pending/user | next.test.ts | allow / reviewer |
| shell.test.ts extDirReq.metadata | patterns only | shell.test.ts:508 | Add metadata toMatchObject |

## 6. Current Patch Edit
- Current patch SHA-256: `3dd728ca12f8311052fdf15207b87773587928f75db6a7658d8d71b8f24bb7cb`
- Exact edited hunks: 11 files
- Why each edit is required:
  1. agent.ts: Auto needs external_directory:auto so shell path gates enter auto routing
  2. auto.ts: the routing rewrite ties 4-level precheck to shell/external/reviewer/fail-closed semantics
  3. index.ts: reviewerDisabled propagates explicit user-reviewer config to Auto fail-closed logic
  4. precheck.ts: external_directory shell branch + expanded sensitive patterns close PowerShell/SSH gaps
  5. policy.md: reviewer policy must deny direct credential reads
  6. service.ts: reviewerEnabled lets native Auto implicitly enable reviewer without global config
  7. shell.ts: agent field lets auto.ts detect Auto for fail-closed; bash metadata gains shell evidence
  8-11. Tests: lock the new routing behavior
- Source behavior preserved: all 203 routing semantics preserved; only the layer-top capture and defaultLayer hunk adapted to AppNodeBuilder.
- Minimal semantic adaptation: capture-at-ask instead of capture-at-top (AppNodeBuilder compatibility); defaultLayerWithSession uses LayerNode.compile(Session.node); Windows-only live tests skipped.
- No semantic, location, or sequence conflict: `git apply --check` PASS against tip@202.

## 7. Verification
- `git apply --check .temp/patches/current/0203-773ad40748fa.patch` against tip@202: PASSED
- Cumulative apply (201→202→203) against tip@200: PASSED (final state matches tip@203)
- Materialize: tip@203 created as self-contained fresh-git state; reverse-check PASS
- Effective changed lines `E` for this patch only: 277
- Qualifying distributed Chinese comments `C` for this patch only: 53
- Required minimum `max(1, ceil(E * 0.15))`: max(1, ceil(277 * 0.15)) = max(1, 42) = 42
- C (53) ≥ 42 → PASS
- Representative #203 comment locations:
  - agent.ts — Auto external_directory:auto rationale (shell path gate into auto routing)
  - auto.ts — shell-external allow (no double LLM), non-shell external ask, shell general allow, Auto fail-closed
  - index.ts — AppNodeBuilder capture-at-ask rationale
  - precheck.ts — external_directory shell branch, SSH/Windows pattern expansion, ls/dir/gci reader expansion
  - service.ts — reviewerEnabled (native Auto implicit enable), defaultLayerWithSession cycle boundary
  - shell.ts — FILES gci rationale, external_directory/bash agent field
  - policy.md — direct credential read deny
  - tests — shell-external, non-shell external, Auto fail-closed, dev-period shell general rationales

## 8. Contract Check
- One commit only: yes
- Full source diff read: yes (1004-line original patch read in full)
- Full target implementation read: yes (all 11 target files read at tip@202 relevant ranges)
- No deleted or delayed behavior: yes (defaultLayer hunk dropped because target has no defaultLayer; Windows-only live tests skipped, behavior covered by unit tests)
- Per-patch Chinese comment gate passed: yes (C=53 ≥ 42 required)
- Patch, record, tests and target result are materially consistent: yes

## 9. Final Verdict

`PASS` — patch built, `git apply --check` PASS, cumulative apply PASS, E/C gate PASS (E=277, C=53, required=42). Materialized as `states/0203-773ad40748fa`.

## DUAL-AUDIT SEAL

- Sealed: 2026-07-27T01:20:38Z
- FORWARD: BATCH PASS (ses_fwd_201_205_v2) - no open B/G
- REVERSE: BATCH PASS (ses_rev_205_201_v2) - no open B/G
- Tip: states/0205-5f3d18e85b8a
- Typecheck: typecheck-0205-2026-07-27T00-57-10-676Z.md
- Install: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f
- Non-blocking N only (do not reopen seal)
