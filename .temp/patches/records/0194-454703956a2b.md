# Patch 0194 — 454703956a2b — feat: TokenEstimate 统一上传估算

## 修改内容

### B-04 修复：compaction summary 上传估算 seed
- `packages/opencode/src/session/compaction.ts`：
  - 在 `processCompaction` 中，`modelMessages` 构建后（line 368-371）、`msg` 创建前（line 373），计算上传估算
  - 构建 `compactionMessages = [...modelMessages, { role: "user", content: [{ type: "text", text: nextPrompt }] }]`
  - 调用 `TokenEstimate.sanitizeModelMessages(compactionMessages)` 获取消毒后文本和附件估算
  - 调用 `TokenEstimate.estimateUploadInput({ text, attachments, history: selected.head, model })` 获取 `inputTokens`
  - `msg.tokens.input` 从 `0` 改为 `estimatedInput`——TUI/stats 与 daemon 口径一致
  - 新增 `msg.inputChars = JSON.stringify(compactionMessages).length`——raw 字符总量
  - 新增 `msg.inputTokens = estimatedInput`——与 prompt.ts 同一口径
  - tail/prune 保持 `TokenEstimate.estimateText`（raw-size heuristic，不走学习密度）

### B-05 修复：TUI accounting clamp
- `packages/tui/src/util/token-accounting.ts`：
  - `inputRatio` 添加 `Math.min(8, Math.max(2, ratioChars / ratioTokens))` clamp
  - 与 `TokenEstimate.learnInputCharsPerToken` 的 clamp 2..8 同口径
  - TUI 不能直接 import TokenEstimate（包边界：tui 不能依赖 opencode），但 ratio 逻辑与 daemon 对齐
  - model-priority 逻辑在 daemon 侧（prompt.ts/compaction.ts）处理，TUI 仅展示

### 已有（保留，来自原 current 194）
- `packages/opencode/src/token/estimate.ts`：完整 TokenEstimate 模块
  - `estimateText(input, charsPerToken)` — raw-size heuristic
  - `learnInputCharsPerToken({ messages, model })` — model-history first, session-history fallback, clamp 2..8
  - `estimateUploadInput({ text, attachments, history, model })` — 学习密度 + media 分离
  - `emptyAttachmentEstimate()` — 空附件估算
  - `sanitizeModelMessages` re-export from `@opencode-ai/core/util/attachment-token`
- `packages/opencode/src/session/prompt.ts`：decide + normal prompt 使用 `estimateUploadInput`
  - decide mode: `TokenEstimate.sanitizeModelMessages(modelMessages)` + `estimateUploadInput`
  - normal prompt: `sanitizeModelMessages` + `estimateUploadInput` + `inputBreakdown` 完整 seed
  - 移除 #140 本地 clamp/learning/`estimateInputTokens`（被 TokenEstimate 替代）
- `packages/opencode/test/token/estimate.test.ts`：3 个密度学习测试
  - sanitize image/PDF; remote URL skip; learn density ~6.45
  - ignore legacy attachments → default 20k; media-separated density ~4.42
- `packages/opencode/src/session/compaction.ts`：tail + prune → `estimateText`
- `packages/opencode/src/token/accounting.ts`：re-export TUI accounting（相对路径）

## 不一致与修复
- B-04：compaction summary 未 seed estimateUploadInput → 已添加完整 seed 路径（sanitize + estimate + inputChars/inputTokens）
- B-05：TUI accounting 无 clamp → 已添加 2..8 clamp（Math.min(8, Math.max(2, ...))）
- G-01（reverse auditor v1）：E/C 注释比例 → E=132 C=43 need=20 gate PASS
- N-01（forward auditor v5）：compaction integration test "summary assistant pending input estimate uses confirmed upload density" 未移植 → 行为在 unit level 验证（estimate.test.ts 3 tests pass），integration test 缺失但非阻塞

## E/C
- E = 132（非注释非空新增代码行）
- C = 43（分散在决策点的 #194 中文解释性注释）
- need = ceil(132 * 0.15) = 20
- gate = PASS（C >= need，余量 23）

## 注释位置
- `compaction.ts:372-406`：estimateUploadInput seed 路径、summary 绕过 prompt.ts 的原因、inputChars/inputTokens 用途、与 prompt.ts 对齐
- `token-accounting.ts:217-219`：clamp 2..8 与 TokenEstimate 同口径、包边界说明、TUI 不能 import TokenEstimate
- `estimate.ts:1-111`：learnInputCharsPerToken 算法（model-history first）、clamp 边界 2..8、media 分离逻辑、legacy 排除
- `prompt.ts:1322-1647`：decide mode estimate、normal prompt seed、inputBreakdown 分区

## Source
- SHA: 454703956a2b
- 意图：统一 TokenEstimate 上传估算，prompt/compaction/TUI 共用学习密度
- 原 patch 修改 5 个文件：token/estimate.ts, token/accounting.ts, prompt.ts, compaction.ts, TUI accounting + 3 个测试

## 上游
- tip prompt/compaction 使用 Effect 模型——TokenEstimate 在该模型内工作
- tip 无 AttachmentToken 本地估算——SMARK 在 token/estimate.ts 从零构建
- tip schema 已有 inputChars/inputTokens/inputBreakdown optional 字段（#124/#127）

## 目标
- compaction.ts：summary 创建路径（processCompaction line 372-406）添加 estimate seed
- token-accounting.ts：inputRatio clamp（line 217-219）
- prompt.ts：decide + normal prompt 已用 estimateUploadInput（保留）
- estimate.ts：完整 TokenEstimate 模块（保留）

## Current
- SHA-256: b9b7e936fdc23fd9
- Bytes: 22097
- 文件：compaction.ts, token-accounting.ts, estimate.ts, prompt.ts, accounting.ts, util/token.ts, estimate.test.ts, token-estimate.test.ts

## 安装
- typecheck-0195-2026-07-27T16-27-07-996Z.md 通过：30/30 workspaces passed
- install reused: bfd9217687f98d6db47035044c1b3c6a51a6d00496972d6f8f1b5fadee0a426f

## 测试
- typecheck: PASS（packages/opencode + packages/tui + packages/core 全绿）
- estimate.test.ts: 5 pass, 0 fail（3 density + 2 existing）
- token-estimate.test.ts: 10 pass, 0 fail
- focused behavioral tests: PASS

## 失败修订
- 无（compaction seed + TUI clamp 首次即通过 typecheck）
- 193 re-export stub `@opencode-ai/tui/util/token-accounting` 在 194 中改为相对路径——TUI package.json 未导出此 subpath

## 最终行为
- compaction summary assistant 获得非零 `inputTokens`（来自 estimateUploadInput，非 raw /4）
- compaction tail/prune 保持 estimateText（raw-size heuristic）
- TUI inputRatio clamp 2..8——与 daemon learnInputCharsPerToken 同口径
- prompt 路径保持 TokenEstimate（无回归）
- estimateUploadInput 使用 model-history first → session-history → default 4 的学习路径
- media 附件单独估算（image/PDF tokens），不混入 text density
