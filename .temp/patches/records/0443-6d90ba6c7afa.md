# 0443-6d90ba6c7afa fix(storage): 降低进度事件磁盘写入

## Identity
- index / full SHA: 0443 / 6d90ba6c7afa690555eac5c97f97e8a2e186866a（manifest parent = 0442 6c44755b057f）
- subject: fix(storage): 降低进度事件磁盘写入
- original: 120106 bytes / 3347 行 / 18 文件
- current: 与 original 逐字节一致（未适配）
- status: 预研完成——**本批次风险中心**，需大面积语义重定位适配（Record 先于适配编辑建立——合规时序）

## Source Intent（核心不变量）
1. **live progress 与 durable 持久化分离**：工具执行中的 running 快照走 ephemeral publish（不进 SQLite/EventSequence），durable 更新保持 1s 节流；迟到 durable 不得回退展示，terminal 无条件支配。
2. **version 单调**：progress 事件带 version，running 只接受严格更高 version；缺失/NaN/小数/负数归一为 legacy v0；terminal 无条件支配（markShellTerminal）。
3. **跨 Session callID 隔离**：marker 按 Session+call 复合键；close_session 按 session 前缀清理。
4. **LLM 诊断日志体积限制**：tag 走 utf8Prefix（128/256/32B）、streamErrorSummary 只落 statusCode/字节数摘要，provider 控制的 body/message 不落盘。
5. **SDK**：Event/GlobalEvent union 增 EventMessagePartProgress。

## 18 文件清单与 0440 state 锚点状态
| 文件 | 0440 状态 | 适配难度 |
|---|---|---|
| src/acp/agent.ts | 95 行薄包装类（委托 ACPService）；shellSnapshots 逻辑实际在 **src/acp/event.ts**（Subscription 类） | 高——需重定位到 event.ts 架构 |
| src/cli/cmd/tui/context/sync.tsx | 不存在；实际在 **packages/tui/src/context/sync.tsx**（全部锚点在：pendingPartDeltas L188、mergeLivePart L330、case L745） | 中——换路径近净应用 |
| src/session/llm.ts | 部分（onError/repairToolCall 锚在；hunk1 失配） | 中——需补 helper 或改写 |
| src/session/message-v2.ts | 事件自 SessionV1 re-export，无本地 Schema 块 | 中——需本地新增 PartProgressEventSchema |
| src/session/processor.ts | updateToolCall L65/L273、updatePart L279 ✅ | 低——近净应用 |
| src/session/prompt.ts | spawner.spawn L893 但 metadata 写法漂移（{ output } vs { output, description: "" }） | 中——语义适配 |
| src/session/session.ts | updatePart 类型 SessionV1.Part（非 MessageV2.Part） | 中 |
| src/sync/index.ts | **不存在**（事件机器在 packages/core/src/event.ts publishEvent L369 + server httpapi handlers/sync.ts） | 高——project/publish 拆分需在 core 重做 |
| src/tool/progress.ts | 新文件 113 行 | 低——新建 |
| src/tool/shell.ts | onChunk L1183 在但为 bash-compress 结构 | 高——需大幅重写适配 |
| src/tool/tool.ts | metadata L46 精确匹配 | 低 |
| test/acp/event-subscription.test.ts | 不存在 | 中——新建或寄宿 |
| test/cli/cmd/tui/sync.test.tsx | 实际在 packages/tui/test/cli/cmd/tui/sync.test.tsx（partEvent L618、force sync L465/499） | 中——换路径 |
| test/session/llm.test.ts | waitRequest L582 ✓；withInfoLog 全仓不存在 | 中——需补 helper |
| test/session/prompt.test.ts | shortSessionTimeout L82 ✓、unix( L278 ✓、Effect.sleep(50) ×4 | 低-中 |
| test/sync/index.test.ts | 不存在；packages/core/test/event.test.ts 可作新家 | 中——重定位 |
| test/tool/shell.test.ts | import/run/it.live 锚全在 | 低 |
| sdk/js/src/v2/gen/types.gen.ts | ToolPart 已存在；缺 EventMessagePartProgress；union 顺序不同 | **禁整 hunk**：手动补类型 + 跑 script/build.ts 再生成 |

## 适配策略（Round-6 预研结论）
1. **delivery 通道核心**：processor.updateToolCall 增 delivery 参数（ephemeral→publishPartProgress、durable 缺省保持既有语义）；session.ts 增 publishPartProgress（structuredClone + bus.publish）；tool/progress.ts 新建协调器（live 50ms/durable 1s 双 worker、sliding queue、按 delivery 独立 version ack、uninterruptible 提交、幂等 close+flush）。
2. **acp 重定位**：shellSnapshots 按 Session+call 键控的 marker/terminal 支配/version 单调逻辑落位到 src/acp/event.ts 的 Subscription 类（0440 架构的真实 owner）。
3. **sync/projectEvent 拆分**：在 core event.ts 按 0440 架构重做 projectEvent（事务内）+ publishEvent（提交后）拆分，保持"consumer 永不见未提交状态、terminal 不越过 trailing running publication"不变量。
4. **SDK**：手动在 types.gen.ts 的 Event 与 GlobalEvent union 追加 EventMessagePartProgress（照 0443 的类型定义），随后按 AGENTS.md 跑 script/build.ts 验证再生成一致性。
5. **prompt.ts**：delivery 透传 + direct shell progressVersion: 0 基线 + ToolProgress.make 协调器接入（按 0440 的 metadata { output } 形态适配）。
6. 测试六处按 0440 架构落位/适配。

## E/C 预估
源内嵌 E/C 未知（record 层无基线）；适配后以 count-ec-auditor 复算为准。预计 E 密集（新文件 progress.ts 113 行 + 多处 delivery 通道）。

## 验证计划
- materialize 443（在 441/442 落位后）。
- 行为测试：acp event-subscription（新建落位后）、tui sync、llm 日志体积、prompt direct shell 合并、sync 发布顺序、shell 合并、ToolProgress 单元测试。

## Status
预研完成，适配进行中（本 record 建立于适配编辑之前——合规时序）

## 行为测试落位计划
- test/acp/event-subscription.test.ts：新建（或在 0440 acp 测试面寄宿），覆盖 live progress、stale durable 拒绝、terminal 支配（live+replay 双入口）。
- test/cli/cmd/tui/sync.test.tsx：落位到 packages/tui/test/cli/cmd/tui/sync.test.tsx（legacy v0、force sync 单调、terminal 支配）。
- test/session/llm.test.ts：新增日志体积 sentinel（≤8192B/行、marker 不落盘、error event 保留）。
- test/session/prompt.test.ts：direct shell 合并测试（40 CR 帧、durable running ≤3、首帧 v0、终态 output 完整）。
- test/sync/index.test.ts：落位到 packages/core/test/event.test.ts（发布顺序：published == running→terminal）。
- test/tool/shell.test.ts：合并测试 + ToolProgress close/publication 竞态单元测试。

## Round-6 深化预研：V2 EventSourcing 架构鸿沟（关键发现）

- **架构级差异**：0442 state 的 session.ts 已是 V2 EventSourcing 架构——updatePart 通过 `events.publish(SessionV1.Event.PartUpdated, ...)` 走事件溯源（L642-650），part 类型为 SessionV1.Part；0443 源基线则是 MessageV2 直写架构。
- **delivery 通道的设计缺口**：0443 的 durable/ephemeral 分离依赖"bus.publish 不进 SQLite"的旁路。V2 架构中 events.publish 即 EventLog append + projector——**不存在天然旁路**。适配需先设计 ephemeral 事件通道（V2 事件系统新增 ephemeral 事件类型，或 PartUpdated 发布方增加 delivery 维度跳过 EventLog），且不得破坏"consumer 永不见未提交状态"等 V2 不变量。
- **处理器层可行**：processor.ts 的 updateToolCall 锚点在 0442 state 成立（预研 ✅），delivery 参数适配可落位；但 ephemeral 分支的最终落点取决于上述通道设计。
- **结论**：0443 适配 = 先设计 V2 ephemeral 事件通道（架构决策）→ 再逐文件落位。这是设计级工作，需以完整上下文独立执行。
- **其余 17 文件**的预研锚点结论不变（见上文清单），但 prompt.ts/session.ts 的 ToolProgress/publishPartProgress 落点依赖通道设计定稿。

## Round-7 适配实施进度（V2 架构等价落位）

### 已完成（全部落位在 V2 EventSourcing 架构内）
1. schema/src/v1/session.ts：PartProgress = define({type:"message.part.progress", schema:{sessionID,part,time:NonNegativeInt}})，无 durable 字段 → 纯 ephemeral（core publishEvent L378-393 分支仅 notify 不落盘）；Event + Definitions 注册。
2. opencode/session/message-v2.ts：Event.PartProgress = SessionV1.Event.PartProgress。
3. opencode/session/session.ts：publishPartProgress（updatePart 的镜像，Interface + 实现 + Service.of）。
4. opencode/session/processor.ts：updateToolCall delivery 参数（durable 缺省/ephemeral → publishPartProgress）。
5. opencode/src/tool/progress.ts：新建 113 行 ToolProgress（LIVE 50ms/DURABLE 1s 双 worker、latest/version 唯一持有、uninterruptible 确认区、幂等 close + finalizer）；Scope 导入按仓库惯例改值导入。
6. opencode/src/tool/tool.ts：Context.metadata 类型加 delivery；wrap 的 ...input 已天然透传。
7. opencode/src/tool/shell.ts：initial metadata progressVersion:0；Effect.scoped 内 ToolProgress.make（live→ephemeral/durable→durable）；onChunk 拆 spill + lastVisible 去重 + progress.update；旧 onChunk 删除。
8. opencode/src/session/prompt.ts：metadata 回调 delivery 分流（剔除 delivery 防污染 ToolState）；direct shell 初始 progressVersion:0 + ToolProgress coordinator + stream 循环改 progress.update({output})；import ToolProgress。
9. opencode/src/session/llm.ts：APICallError import；stream 标识符 utf8Prefix(128/256/128/128/32)；onError(event) + streamErrorSummary；helper 追加 hasToolCalls 后；repair 日志 state 已无（superseded）。
10. opencode/src/acp/event.ts（0440 架构的 agent.ts 逻辑 owner）：EventMessagePartProgress import + Hash；shellSnapshots 改 marker 对象；handle progress fallthrough；progress 跳过 recordPartMetadata；handleToolPart/runningTool/toolStart/closeToolCall 全部 callKey(sessionId,callID) 化；runningTool 的 version 排序 + terminal 支配 + hash 去重；closeToolCall 的 terminal marker（version 0 + terminal:true）；shellProgressVersion 归一化。close_session 前缀清理 superseded（Subscription per-session 生命周期即清理）。
11. packages/tui/src/context/sync.tsx：mergeLivePart 注释更新 + tool 分支 terminal 支配 + bash version 排序 + case progress fallthrough + orphan 注释更新。
12. packages/core/src/event.ts sync 拆分：**superseded**——commitDurableEvent 已把 projector 放在 SQLite 事务内、committed falsy 不 notify，"projector 接受才 publish"不变量结构性成立。
13. packages/sdk/js/src/v2/gen/types.gen.ts：EventMessagePartProgress type + Event union + GlobalEvent payload union 注册。
14. schema/test/event-manifest.test.ts：85→86、Definitions 数组 + PartProgress、slice(40,44)。
15. opencode/test/event-manifest.test.ts：88→89。
16. opencode/test/tool/shell.test.ts：Deferred/Fiber import、ToolProgress import、3 个 ToolProgress 行为测试（coalesces/close overlap/live while durable delayed）。

### 验证状态
- materialize 443：passed 443/443（含测试 hunk 的中间版本）
- typecheck 443：全包 passed（install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad）
- 首轮 typecheck 失败（schema manifest 测试断言过期）已通过合同伴随更新修复

### 待办（本 index 闭合前必须完成）
- 剩余 4 个测试文件适配：test/session/llm.test.ts、test/cli/cmd/tui/sync.test.tsx、test/sync/index.test.ts、test/session/prompt.test.ts、test/acp/event-subscription.test.ts（注意 prompt.test 的部分 hunk 属于 0445 的 sleep/timeout 变更，勿混入 0443）
- 重新 materialize 443 + typecheck
- 行为测试矩阵（shell.test 新测试 + manifest 测试 + 0441 apply_patch/edit 回归）
- record 定稿 + E/C 复核
- 0444 materialize（current 已预构建）→ 0445 适配 → 双审计

## Round-8 重做与当前验证状态

### 事故与恢复（审计须知）
- Round-7 完成的适配曾因两次 materialize 链式清理丢失：materialize 443 时 state 442 被删；materialize 442 时 state 443 被删；一次 PowerShell 非终止异常导致 current/0443 被截断为 1368B。
- 恢复：在重建的 state 442 worktree 上按 Round-7 记录完整重做 12 个生产文件适配（edit 调用历史在案），重建 current 后追加 4 个测试文件段。重建后的生产代码段字节数与丢失前完全一致（39295B），证明重放忠实。
- 教训落实：测试适配修改必须在提取 diff 进 current 之后才允许 materialize。

### 测试适配完成情况
1. schema/test/event-manifest.test.ts：85→86、Definitions 数组插入 PartProgress、slice(40,44)。完成。
2. opencode/test/event-manifest.test.ts：88→89（manifest 是 schema re-export）。完成。
3. opencode/test/tool/shell.test.ts：Deferred/Fiber import、ToolProgress import、3 个 ToolProgress 行为测试（coalesces ≤3 durable 快照+ephemeral 存在+v0 基线+output 保真；close 与 publication 重叠不重复 durable version；live 50ms cadence 不被 delayed durable 阻塞）。完成。
4. opencode/test/session/prompt.test.ts：新增 coalesces direct shell progress 测试。事件观察用 GlobalBus 同步回调（EventV2Bridge.listen 的泛型 R 在 it.instance 选项重载下泛化为 unknown 导致 TS2345，GlobalBus 为 bridge 下游同步总线等价观察）；宿主 it.instance + 30_000（unixNoLLMServer 的层不含 EventV2Bridge；806 类似 it.instance 亦无 options）。完成。
5. opencode/test/session/llm.test.ts：**未适配**。bounds stream error logs 测试需 state 侧日志基建映射（source 用 withInfoLog/readLogUntil 轮询文件日志；state 的 #240 先例用内存数组捕获）。待补。
6. packages/tui（test/cli/cmd/tui/sync.test.tsx 源路径→state packages/tui 测试）：**未适配**。待补。
7. opencode/test/sync/index.test.ts：**superseded**——测试对象 SyncEvent.use.run 的 publish 顺序合同在 0440 架构由 commitDurableEvent 的事务内 projector + committed 检查结构性承担，测试目标架构不存在。
8. opencode/test/acp/event-subscription.test.ts（源 +435 行）：**未适配**。ACP marker/version/terminal-dominance 合同测试。待补。

### 当前验证（最新一轮）
- materialize 443：passed 443/443
- typecheck 443：全包 passed（含 app/cli/client/codemode/console/core/opencode/tui/sdk/schema 等）；install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad
- current/0443：51946B→（description 修复后约 51900B）、16 文件、LF 无 CR

### 闭合 0443 前剩余
- 补 5/6/8 三个测试文件适配（llm.test、tui sync.test、acp event-subscription.test）
- E/C 复核（当前生产+测试有效行估算 E>500，中文注释行需 >= ceil(E*0.15)，待精确复算）
- 行为测试矩阵（shell.test 新增 3 测试、两 manifest 测试、0441 apply_patch/edit 回归）
- 0444 materialize + 0445 适配 + 批次双审计

## Round-9 测试适配完成与闭合验证

### 剩余 3 测试文件适配（全部完成）
5. packages/tui/test/cli/cmd/tui/sync.test.tsx：progressEvent/runningShellPart/completedShellPart 工厂 + keeps shell progress monotonic 测试。遵循既有 mount(handler, state) 适配合同（源 Global.Path.state 模式已被更早批次替换为第二参数传递，见文件 L439 适配说明）。
6. packages/opencode/test/acp/event.test.ts（源 event-subscription.test 的 0440 harness 对应物）：progressEvent/versionedRunning 工厂；dedupe 测试按 0443 version 合同改写（相同 v0 重复 snapshot 拒绝——源同款语义改写）；新增 routes live shell progress and rejects stale durable snapshots（v1 content → v2 duplicate 帧 → v3 content → stale v2 拒绝）；terminal-lifecycle 测试**移入 0445**（其 toolStart terminal 守卫是 0445 修复本体，0443 阶段实现尚无该守卫——运行验证确认过早断言会失败，record Round-9 已存证）。
7. packages/opencode/test/session/llm.test.ts：bounds stream error logs 测试。适配决策：llm.ts 的 streamErrorSummary 加 export（测试 seam，非 production path）；日志行字节级断言降级为对 streamErrorSummary 净化合同的直接单元断言（state 无 daemon 文件日志轮询基建，#240 先例同款降级）+ error-event 保真走真实 mock 500 响应集成验证（LLM.Service.use + runCollect，event.type=provider-error）。

### 二次事故与恢复
- materialize 链式清理再次删除 state 443/442（各一次），一次 current 被部分覆盖（27123B 测试段版本）。恢复：备份 7 个修正测试文件至 D:\Temp；从 .test-workspace（保有 443 mirror）恢复 12 个生产文件；在 442 worktree 重新生成完整 19 文件 diff。
- 最终 current/0443：65759B、19 文件、LF、累计 apply 通过。

### 最终验证
- materialize 443：passed 443/443
- typecheck 443：全包 passed；install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad
- E/C：additions=767、blank=38、import-only=11、注释=103 → E=615、C=103 ≥ ceil(615×0.15)=93（16.7%）PASS。注释分散于 schema 事件定义、ToolProgress cadence 确认区、session.ts publishPartProgress、processor delivery 分支、shell onChunk latest-wins、prompt delivery 分流/GlobalBus 观察、acp marker/terminal、llm utf8Prefix/summary、各测试断言处。
- 行为测试：ToolProgress 3 测试 8 pass/0 fail（-t progress）；acp event.test 16 pass/0 fail（含 3 新测试 + dedupe 改写）。修复记录：bun -e CR 转义跨 shell 断裂改用既有 JSON.stringify 传输模式（L205 先例）；prompt.test 事件观察从 EventV2Bridge.listen 泛型降级改用 GlobalBus 同步回调（bridge listen 的 R 在 it.instance options 重载下泛化为 unknown）——两者均有相应中文注释。
- 遗留：#179 WSL 测试 2 例环境性失败（grep 守卫消息与宿主差异，非 0443 引入——该失败在 0442 基线同样存在）；terminal-lifecycle 测试顺延至 0445。

## Round-10 manifest 基数权威修正（最终定稿）

- **遗留偏差发现与修正**：manifest 测试的计数断言在 0442 基线即为过期（先前批次的 schema 事件增加未同步测试——schema ServerDefinitions 55→运行时 60、Durable 32→35、Defs/Latest 85→95 均为历史遗留 +9；PartProgress 仅贡献 +1）。0443 适配将断言完全对齐运行时真实基数：Server=60、Defs=95、Latest=95、Durable=35、slice(42,46)=[PartDelta,PartProgress,Diff,Error]（PartProgress 位于 Definitions index 43，bun -e 运行时探针证实）。opencode test 88→95 同理（其 manifest 为 schema re-export）。
- **最终 current/0443**：66121B、19 文件、LF、累计 apply 链 441→445 全过。
- **E/C（最终）**：additions=769、blank=38、import-only=11、注释=103 → E=617、C=103 ≥ ceil(617×0.15)=93（16.7%）PASS。
- **权威验证（typecheck 445 报告 typecheck-0445-2026-08-30T15-45-29-480Z）**：passed 445/445、30 包 typecheck 全 passed、install reused 04420627f0d26ecf9a0810ddc8f7cf8203c7ff3ca0e36b999069f68ece90d1ad、No failures。
- **行为测试（权威 state 445 代码 + junction 共享依赖）**：schema manifest 2 pass/0 fail；opencode manifest 1 pass/0 fail；acp event.test 17 pass/0 fail（Round-9 时 16 pass 系 0443 实现无 toolStart 守卫时期的 terminal 测试移出后计数）；shell ToolProgress -t progress 8 pass/0 fail。
- **事故追溯（供审计）**：Round-8 的 19 文件备份曾混入 0445 守卫（备份取自 445 物化产物），导致链式重建时 0443/0445 双重应用冲突；已通过行级回退（pending 恢复单行 delete+return、toolStart 移除前置守卫）与 acp-event-fixed.test.ts（0443 正确版）恢复，重建后的 0443 diff 经 grep 证实无 0445 内容泄漏。
