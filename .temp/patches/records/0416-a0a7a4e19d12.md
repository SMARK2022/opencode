# Record 0416-a0a7a4e19d12

## Identity
- SHA: a0a7a4e19d12（manifest sha12）
- Parent: 74966f40c64f (0415)
- Subject: docs: update prompts for research and auditing process; add session cancellation follow-up plan
- Status: PASS（materialize + typecheck + 双审计修复后批次验证全部通过）

## Source intent（original/0416，1360 行，3 文件）
1. `docs/prompts.md` 3 处语义收紧：
   - ① 首段调研指引用语改为"直接进行跳步"（禁止跳步）；
   - ② "穷尽式探索" → "完整探索"；
   - ③ 调研阶段尾部追加"请注意当前并没有允许进行相应的完整修改，你需要完整进行相应的汇报。"（修改前必须完整汇报）。
2. 新增 `docs/session-cancellation-follow-up-plan.md`（388 行）：Session 取消时序与工具终止设计（single-flight、cancellation gate、Tool Abort Bridge、Processor terminal ownership；明确"未获放行，不允许据此实施"；是 0414/0415 实现后的跟随方案文档）。
3. 新增 `docs/Proposal/tui-reasoning-run-rendering-proposal.md`（923 行）：TUI 多 Reasoning Part 聚合折叠完整设计（22 节）——0417 的实现依据文档。

## Target adaptation（current/0416）
- current/0416 与 original/0416 逐字节一致（verbatim；docs-only 无目标适配需求）。
- prompts.md 三处 hunk 在目标（0415 状态）上下文逐字匹配（#190 注释头 +5 偏移演进后锚点仍一致），materialize 420 实测 apply 成功（offset 命中）。
- 两个新增文档：纯新增无冲突（docs/Proposal/ 与 docs/ 目标目录均存在）。
- 文档内容保留 verbatim（proposal 引用的旧 TUI 路径属文档性质，不改写路径语义）。

## Hunk mapping（3 文件）
| 文件 | 变更 | 说明 |
|---|---|---|
| docs/prompts.md | 3 处文本替换 | verbatim，锚点逐字匹配 |
| docs/session-cancellation-follow-up-plan.md | 新增 388 行 | verbatim new-file |
| docs/Proposal/tui-reasoning-run-rendering-proposal.md | 新增 923 行 | verbatim new-file |

## E/C（独立计算）
- 全部为 .md 文档 → E=0 / C=0 / need=0 → PASS（script 与 auditor 语义一致）。

## Install / typecheck 证据
- install 指纹 `1165df52b90ae85411eb2a8598d30f19e8da24f2bc9c6a6e60cddbff8d11f9d5`（bun 1.3.14 frozen reuse，auditor 独立重算一致）。
- 批次末 typecheck 报告 `typecheck-0420-2026-08-27T17-35-17-523Z.json`：reusedPrefix=420，30/30 workspaces passed（packages/opencode、packages/tui 均在内），docs-only 无代码影响。

## Verification（验证证据链）
- materialize-0416 报告（17:24:33）：index 416 passed (apply applied)，prefix 405 链 [406..416] 全 passed。
- 重建后 materialize-0420-2026-08-27T17-34-15-738Z：passed 420/420、rebuild=true、reusedPrefix=419、retained [0420,0419,0417,0416,0405]、sourceTip d0ceb469011412b4ac5058a12d5fe4f247bdac79、targetBaseline 4473fc3c9055046183990a965d68df3db7ea6f62。
- 状态树验证（auditor 构造性重建）：state 0416 tree hash `abd5466c…` 与 0405 + currents 0406-0416 精确匹配；0419/0420 文件内 prompts.md 三处文本实测命中。
- typecheck：批次末 30/30 workspaces passed（含 packages/opencode、packages/tui）。

## 审计结论（双审计）
- 正向 auditor：无 B；G-02（record 门禁）→ 修复后重审。
- 反向 auditor：无 B（record 门禁 G-01 同）。
- 本 record 于 G-01 修复轮次扩写至 ≥50 行 / ≥3000 字符（原版 28 行/1462 字符不达标）。
- N-6（正向）：record 原以 0415 批次报告代理 0416 的 typecheck 证据 → 已改为批次末 typecheck-0420 报告。

## 最终行为
- docs/prompts.md 调研工作流语义收紧（不跳步、完整探索、修改前完整汇报）。
- 新增两份设计文档（cancellation follow-up plan + reasoning-run rendering proposal），作为 0417/0418 的文档依据；本项不实施代码改动。

## 失败修订记录（本批次恢复事件相关）
- 0416 本身 verbatim，无修订；但批次 0417-0420 的 addheads/addcomments/fixhunkcounts 脚本事故导致 0417/0418/0419/0420 current 的 hunk body 被清空，物理文件被破坏。
- 恢复策略：materialize 416/417/418/419 按序重建 states；0417 = diff(0416 state, D:\Temp\opencode\idx-0418post.tsx 减去 0418 变化)；0418 = diff(0417 state, idx-0418post + 0417 注释)；0419 = gen0419c（0418 state vs 0419-post）；0420 = gen0420v2（0419 state vs 0420-post）。
- 恢复后重新 materialize 420 --rebuild（passed 420/420）与 typecheck 420（30/30）全部验证通过；state 树与 0405+currents 重建精确匹配（auditor 构造性证据 `abd5466c…`/`f495d15d…`/`f80aec21…`/`1686f0db…`）。
- 审计发现与修复：0418 JSX 裸 `//` 行（B-01）已包裹为 `{/* … */}`；0419 plan 文档首行 hunk 头残留（B-02）已删除；五份 record 门禁（G-01）本轮全部扩写达标。

## 放行依据
- 五 commit 全部通过 materialize（passed 420/420）、typecheck（30/30）、行为测试（attention 21/21、config 9/9、normalization 26/26、precheck inline_scripts 5/5、session-layout 8/8）。
- 双审计（正向+反向）修复轮重审：本项无 B/G 残留，verdict PASS。