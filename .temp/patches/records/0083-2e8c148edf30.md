# Record 0083-2e8c148edf30

## 1. Identity

- **Typecheck report:** `typecheck-0084-2026-07-22T01-06-33-597Z.md`


- **Index:** 83
- **Source SHA12:** `2e8c148edf30`
- **Current path:** `current/0083-2e8c148edf30.patch`
- **Current SHA-256:** `5175f7f1ea59ce13cf1b77b4d653060756b7fb3376c6d7ea6c9ea60507ef52f0`
- **Current bytes:** 18187
- **Baseline:** `4473fc3c9055046183990a965d68df3db7ea6f62`
- **Source tip:** `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- **Install fingerprint:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`

## 2. Upstream design (state-82 baseline)

Target already had a strong `fetchWithRoute(fetchFn, …)` spine: resolve proxy route, NO_PROXY=* for Bun env cache, proxy-fail → refresh alternate proxy → direct recovery, and `installGlobalFetch` wrapping global fetch. That recovery chain is **stronger** than SMARK original's simple direct/proxy dispatch without fail-open.

## 3. Source / SMARK #83 intent

Original restructures NetworkProxy around:

1. `nativeFetch` captured at module load (anti-recursion).
2. **Primary** `export async function fetch` — route then `directFetch`/`proxyFetch` via native.
3. **`fetchWithRoute` becomes passthrough** for custom auth adapters that must call `NetworkProxy.fetch` internally for the real hop.
4. Provider ternary: `customFetch ? fetchWithRoute(customFetch) : NetworkProxy.fetch`.
5. Call-site rewires to `NetworkProxy.fetch` + purpose tags.
6. `normalizeProxy` localhost → 127.0.0.1.
7. Original also **removes** multi-step recovery; three-way policy keeps recovery **inside** primary `fetch` (upstream strength) while adopting SMARK public API shape.

## 4. Target adaptation (this current)

### `packages/core/src/network-proxy.ts`

- Add `const nativeFetch = globalThis.fetch.bind(globalThis)`.
- `export async function fetch` is the sole routing primary path using `nativeFetch`, **retaining upstream recovery** (refresh proxy / alternate proxy / direct after abort).
- `export async function fetchWithRoute` is SMARK passthrough (`return fetchFn(input, rest)`), no second proxy injection.
- `routedFetch` aliases to `fetch`.
- `installGlobalFetch` installs `fetch(...)` with `as any` for preconnect.
- `originalFetch()` returns `nativeFetch`.
- `normalizeProxy` maps localhost host to 127.0.0.1.

### Provider

- Replace `fetchFn = customFetch ?? fetch` + always `fetchWithRoute(fetchFn)` with ternary:
  - customFetch → `NetworkProxy.fetchWithRoute(customFetch, …)` (passthrough)
  - else → `NetworkProxy.fetch(…)` (primary routed path)

### Plugins

- Rewire free call sites (copilot, models, codex, claudecode) to `NetworkProxy.fetch` with purpose (from prior #83 adaptation, retained).

## 5. E / C (this current only)

- **E = 71**
- **C = 16**
- **need ≥ 11**
- **Gate:** PASS


## 6. Final behavior in state-84

- Public SMARK entry is `NetworkProxy.fetch` with routing + native + recovery.
- Custom adapters use passthrough `fetchWithRoute` and are expected to call `NetworkProxy.fetch` for outbound.
- Provider default path no longer double-routes customFetch through the full proxy injector.
- Call sites use NetworkProxy.fetch; monorepo typecheck must pass.

## 7. Why recovery stays inside `fetch`

Migration contract: keep stronger upstream capabilities as baseline; apply SMARK semantics on top. SMARK's public split (primary `fetch` vs passthrough `fetchWithRoute` + provider ternary + nativeFetch + call sites) is fully applied. Deleting recovery would drop upstream strength without adding a unique SMARK capability that callers depend on as a contract beyond the simple route decision.

## Dual independent audit (batch 80-84)

- **FORWARD:** PASS (all five, no open B/G) — ses_078a2a765ffexXTi63g11fBQJa
- **REVERSE:** PASS (all five, no open B/G) — ses_078a2a74effes7VUOaxpmbOGtW
- **State:** `states/0084-7759649fd14d`
- **Typecheck:** `typecheck-0084-2026-07-22T01-06-33-597Z.md` monorepo packages passed
- **Install:** `550ca932999a73cc76626eec6fe28a2bc7dcdcb6764cb494d32aee7bff50c9ea`
- **Batch verdict:** PASS

## tip@145/150 full-suite repair (2026-07-24)

### 问题
Copilot models 测试 mock `globalThis.fetch`，但 #83 生产改用 `NetworkProxy.fetch`→`nativeFetch`，mock 无效，真实请求返回 400。

### 改动
`github-copilot-models.test.ts` 改为 `spyOn(NetworkProxy, "fetch")`。

### current seal
- bytes: 23473
- sha256: `efd945a205757124c218225b7314115fd6dbc4015a0cba251b0929c2adf7ca3b`
