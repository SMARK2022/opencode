# Record 0230-f81ae0e09344

## Status
PASS（materialize 230/230，typecheck 230 全 30 workspace，E/C gate PASS，第三轮双审计通过并封印）

## 修改内容
- `docs/opencode-sandbox-windows-design.md` — 两处 hunk 按 source 锚点应用：auto agent bullet 更新（bash、workspace edit gate 与 shell/tool-originated external_directory 接入 auto）、edit/write/apply_patch 行更新（auto through the shared edit permission gate）、说明段落更新（共享 edit permission、同一 auto admission seam、其他 build-like 权限暂不扩大）
- `packages/opencode/src/agent/agent.ts` — auto agent 的 edit 权限接入 auto admission（`edit: "auto"`）
- `packages/opencode/src/permission/precheck.ts` — 新增 structuredFileEffect（在 evaluate 通用规则前调用）：edit 权限 files 数组含 delete 项 → cautious（file deletion requires explicit approval）；external_directory + apply_patch + operation=delete + patchText 为 string → cautious；新增 fileEffectType 辅助（读 .type 字段）
- `packages/opencode/src/session/tools.ts` — **primary ask 站点**（context().ask）合并 `metadata: { ...req.metadata, agent: input.agent.name }`——permission reviewer 以 metadata.agent 为稳定信号，集中写入使 apply_patch/write/edit/插件共享同一安全路径
- `packages/opencode/src/session/prompt.ts` — task path ask 站点写入 `metadata: { ...req.metadata, agent: task.agent }`（与 primary 一致）
- `packages/opencode/test/agent/agent.test.ts` — edit auto 权限测试适配（改名 + 断言翻转 + source 中文注释）
- `packages/opencode/test/permission/auto.test.ts` — auto 相关测试
- `packages/opencode/test/permission/precheck.test.ts` — structuredFileEffect 测试
- `packages/opencode/test/session/prompt.test.ts` — 新增 waitForPermission 辅助（waitForBusy 之后）+ "auto agent tool permission requests carry auto agent evidence before approval" 测试（it 声明之后顶层）

## Source 行为意图
1. auto agent 的 edit 权限进入 auto admission——apply_patch/write/edit 实际执行前都请求 edit 权限，必须统一接入而非按工具名散列规则。
2. structuredFileEffect 提升文件删除为 cautious：删除是不可逆效果，必须在 auto reviewer 边界前被提升；仅提升 delete（update/add 走既有路径）；限定 permission=edit 与 external_directory+apply_patch 组合，避免误归类。
3. prompt 适配层统一写 agent 标识到权限 metadata——工具实现无需各自复制 ctx.agent；auto reviewer 对所有非 shell 工具一致启用（reviewerEnabled 依赖 metadata.agent === "auto"）。
4. 测试验证：agent 元数据传递（request.metadata.agent === "auto"）、precheck 分类、auto reviewer 路由、JSON 回退审计形状。

## 不一致（source vs target 适配）
- source 的 primary ask hunk 位于 source prompt.ts:731（`agent: input.agent.name`，带 tool: {messageID, callID} 上下文）；目标中该 primary ask 站点位于 `session/tools.ts:98-105`（context().ask → permission.ask）→ 适配写入 tools.ts，并加入 230 patch 文件列表。
- source 的 task path hunk（prompt.ts:970，`agent: task.agent`）→ 目标等价位置 prompt.ts（适配时已写入）。
- prompt.test.ts 的 waitForPermission 与 it.instance 块初始被构建脚本错误放在文件顶部（import 之间、it 声明之前，`it used before declaration`）→ 按 original 两个 hunk（@@ -323 @@ waitForPermission 于 waitForBusy 后；@@ -1189 @@ it.instance 块于相邻测试前）重建到正确位置。
- `Permission.AutoDeniedError` 与 `PermissionV1` import 的修复 hunk 原在 0230 中（为 0227 测试兜底）→ 0227 第三轮修复已在本项自身闭合，0230 移除该 hunk 与多余 import（保持每项独立可累计应用）。
- docs 两个 hunk 初始被放在错误锚点（文件顶部/title 后/ASCII 图内），旧文本未替换 → 按 source 锚点（~86 行 bullet、~96 行代码块行与段落）重建，旧矛盾文本替换。
- docs hunk 2 的说明段落（3 行：shell risk gate 与 workspace edit gate 重点、同一 auto admission seam、read/task/notebook 不扩大）在第二轮审计前被丢失（旧行删除但未加新段落）→ 从 original 精确恢复 8 ctx + 2 rem + 4 add 的完整 hunk（含两行空 ctx 行），段落语义完整。
- agent.test.ts 六处既有 #201/#203 中文注释（source 未触碰）曾被误替换为重复英文模板行 → 从 227 状态恢复原文（hunk 整体移除或还原为 context），source 的新中文注释（apply_patch/write/edit 在 runtime 都通过 edit permission 约束…）按 source 原样应用。

## 修复过程（审计驱动）
1. prompt.test.ts 顶部误置块导致 `it used before declaration` → 从 original patch 提取两个 hunk 的添加行，重建到正确位置（waitForPermission 于 waitForBusy 后；it.instance 块于 it 声明后顶层）。
2. **B1（第一轮审计）**：primary ask 站点（目标 tools.ts:98-105）未写 metadata.agent——source hunk 1 语义丢失，新测试断言 request.metadata.agent==="auto" 静态不可能通过 → 在 tools.ts ask 包装 `...req` 后插入 `metadata: { ...req.metadata, agent: input.agent.name }`，并将 tools.ts 加入 230 patch。
3. **B2（第一轮审计）**：docs 两个 hunk 错误锚点导致文档自相矛盾（新 bullet 在文件顶、段落进 ASCII 图、旧文本残留）→ 移除误放内容，在 source 锚点（~86/~96 行）应用替换。
4. 验证：permission/index.ts:219 `metadata: request.metadata` 确认 ask 请求存储合并后的 metadata；静态链路 todowrite(ctx.ask metadata:{}) → tools.ts 合并 → permission.ask 存储 {agent:"auto"}。
5. 运行时验证：metadata 测试与相邻既有 LLM-server 测试在此 Windows 环境超时（环境性，auditor 确认），静态证据闭合。
6. **B1（第二轮审计）**：docs hunk 2 的说明段落缺失（record 声称已应用但 target 文档无段落）→ 从 original 恢复完整 hunk（含 3 行新段落与两行空 ctx 行）；同时修正 doc hunk 行数（-96,10 +97,12）。
7. **B2（第二轮审计）**：agent.test.ts 六处 #201/#203 注释被替换为英文模板行（source 未触碰）→ 恢复原文；source 新中文注释按原样应用；测试改名/断言翻转保持 source 语义。
8. prompt.test.ts 的 PermissionV1 相关 hunks 从 0230 移除（0227 已独立闭合）→ 避免重复 import/重复 hunk。
9. 手工编辑 hunk 后逐个用 git apply --check 校验行数（首次 doc hunk 缺两行空 ctx、hunk C/D 内容行数不足导致 corrupt patch）→ 用 git diff --no-index 从 227 状态重建 agent.test.ts 精确 hunks（@@ -92,7 +92,7 @@、@@ -100,11 +100,12 @@）替换，全部行数校验通过。

## E/C
- E=158，C=28（#230/#201/#203 中文注释），need=24。Gate PASS（margin 4）。

## 注释位置
- precheck.ts：structuredFileEffect 优先级、仅两类权限携带效果 metadata、edit delete 提升条件、apply_patch 项目外删除、patchText 存在性要求、type 字段判定。
- agent.ts：edit 权限接入 auto admission 的原因（source 中文注释 4 行）。
- prompt.ts / tools.ts：prompt 适配层统一写 agent 标识的原因。
- agent.test.ts：恢复的 #201/#203 中文注释 + source 新中文注释（apply_patch/write/edit 运行时统一 edit gate）。

## Source
- SHA: f81ae0e09344
- 意图：feat(permission) 增强 auto agent 权限审查逻辑，支持工作区编辑和文件删除的谨慎处理。

## Upstream
- 目标 v1.17.20 有 precheck 基础但无 structuredFileEffect；edit 权限无 auto admission；primary/task ask 站点无统一 agent metadata 写入。230 在既有 precheck evaluate 主路径中插入 structuredFileEffect 调用（通用规则之前），在既有 ask 包装中合并 metadata，不重写既有逻辑。

## Target/Current
- 9 文件（含 tools.ts 新增）；current patch 约 17.2 KB（修复后）。
- 状态 0230 中：doc 段落 3 行存在、agent.test.ts 注释恢复（9 处 #201 出现）、English 模板行零命中、edit auto 断言存在。

## 安装/测试
- materialize 230 PASS（修复后 230/230）；typecheck 230 全 30 workspace PASS（install fingerprint 086c3483…，30/30 passed）。
- 测试（修复后）：precheck/agent/auto 131/131 pass；session-list 19/19 pass；prompt "auto permission reviewer" 6/6 pass、"fails closed" 1/1 pass；LLM-server 类测试个别运行受 10 秒内部超时影响（环境性，重跑通过）。

## 失败修订
- 见修复过程 1-9（两轮审计驱动；修复后重新 materialize 230、重跑 typecheck 与全部测试、等待第三轮双审计）。

## 最终行为
- auto agent 的 edit 权限进入 auto admission；precheck 对结构化文件删除效果（edit files[] delete、apply_patch 项目外删除）提升 cautious；primary 与 task 两个 ask 站点统一写入 metadata.agent（reviewer 稳定信号）；文档 hunk 按 source 锚点应用且说明段落完整；agent.test.ts 既有中文注释保留、source 新注释按原样；新增测试验证 agent 元数据传递与 structuredFileEffect 分类。

## DUAL-AUDIT SEAL (FINAL, batch 226-230, round 3)
- Sealed: 2026-08-05
- FORWARD: BATCH PASS (audit-226-230-forward-20260805T233532.md)
- REVERSE: BATCH PASS (audit-226-230-reverse-20260805T233532.md)
- Tip: states/0230-f81ae0e09344
- Install fingerprint: 086c3483ba3a061ae39d54741faa5d3d0521dd2abde2a6ae9a324edd45d2e359（双审计各自独立复核一致）
- Typecheck: 30/30
- Tests: session-list 19/19；precheck+auto+agent 131/131；"auto permission reviewer" 6/6；fails-closed 1/1；metadata 行为经扩展 poll 运行时证明
- Per-item: 226-230 all PASS
- Round-2 blockers closed: 0227 PermissionV1 自包含（0230 无残留 hunk）；prompt 模板纯净；0230 文档三段恢复；agent.test.ts #201/#203 注释恢复；records 全部修正
- 仅 N 级：227 三处注释位置（C 严格口径 50≥48 仍过）、228 listGlobal 空白 hunk、226 record 行数 metadata、230 测试放置/hunk offset/注释位置/格式
- Batch 226-230: **PASS**
