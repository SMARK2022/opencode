# Record 0273-3282685c3aa1

## Patch Identity
- Index: 0273
- SHA12: 3282685c3aa1
- Subject: fix(daemon): 安全处理 stop 后 TUI 退出
- Original SHA-256: 0ddacbb6e7e56fe41a3d4548f7fb9b047338e62f5db6c7460ecd918f00978209
- Original bytes: 10106
- Current SHA-256: 68f9d7a9e77edecb19690fc032a0b5f7a8202395a328e567ecaa3f65a7883b00
- Current bytes: 7980
- Files: event.ts + worker.ts + sync.tsx + daemon.test.ts (4文件，global.ts 为 no-op drop)

## Source Intent
源 patch 的完整行为目的和 invariant 见 original/0273-3282685c3aa1.patch。

## Adaptation Details
4文件跨包适配：

1. event.ts (packages/opencode/src/server/event.ts):
- 目标仅 10 行，re-export ServerEvent from @opencode-ai/schema/server-event
- 添加 DisposedReason = { DaemonStop: "daemon-stop" } as const
- 不修改 schema 包的 Event.Disposed（保持 schema: {}），reason 通过 properties 运行时传递

2. worker.ts (packages/opencode/src/cli/tui/worker.ts):
- 路径从 cli/cmd/tui/worker.ts 迁移到 cli/tui/worker.ts
- 添加 import DisposedReason, Event as ServerEvent from @/server/event
- gracefulShutdown 中：ServerLock.clearIfOwner 从 dispose 前移到 Database.close 之后
- 添加 notifyDaemonStop() 函数，通过 GlobalBus.emit 广播 global.disposed 事件
- controlServer 中 "daemon-stop" 改为 DisposedReason.DaemonStop
- 保留 3s Promise.race 超时包装（#235）

3. sync.tsx (packages/tui/src/context/sync.tsx):
- 路径从 cli/cmd/tui/context/sync.tsx 迁移到 tui/src/context/sync.tsx
- useExit() 从 event.subscribe 后移到前（供 global.disposed case 使用）
- 目标 Exit 类型为 (reason?: unknown) => void（非对象），exit.message.set 改为 void exit("message")
- 添加 daemonStopSeen 标志和 global.disposed case
- server.instance.disposed 添加 if (daemonStopSeen) break 守卫
- DisposedReason 不跨包导入，使用字符串字面量 "daemon-stop"

4. daemon.test.ts:
- 添加 readUntil 辅助函数
- 添加 "daemon stop announces shutdown to connected TUI" 测试

5. global.ts: no-op drop（目标无 BusEvent import 和 GlobalDisposedEvent）

注释位置（12个）：
- event.ts: #273 daemon stop 时需匿名广播原因
- worker.ts: #273 daemon stop 时先广播事件 / #273 lock 清理延后 / #273 daemon stop 时向已连接 TUI 广播
- sync.tsx: #273 提前获取 exit / #273 标记 daemon stop / #273 仅响应 daemon 主动关闭 / #273 daemon stop 已设置退出标志 / #273 直接调用 exit 函数
- daemon.test.ts: #273 读取 SSE 流 / #273 daemon stop 应在退出前广播 / #273 期望收到带 daemon-stop 原因

## E/C Calculation (独立计算)
- E=66 C=12 need=10 PASS
- E (独立计算): 66
- C (独立计算): 12
- 注释分布：见上方适配详情中的注释位置列表

## Three-way Comparison
- Source (SMARK): 原始 patch 的完整行为语义
- Upstream (v1.17.20): 目标基线的已有实现，作为不可删除基线
- Target (current): 适配后的实现，保留上游优势 + SMARK 全部有效语义

## Target State
- Baseline: 0270-f9dec59534d4 (batch 266-270 PASS)
- Materialized: 275/275 passed
- Typecheck: opencode exit 0, tui exit 0, core exit 0
- packages/app TS1128 pre-existing (不阻塞)

## Verification
- materialize 275: passed 275/275
- opencode typecheck: exit 0
- tui typecheck: exit 0
- core typecheck: exit 0
- install 指纹: reused

## Hunk Mapping
- Source hunks: preserved or adapted per target architecture
- No test deletions
- No source behavior omissions
- No empty current patches


## Source Behavior Analysis
源 patch（5文件）实现 daemon stop 安全处理：
1. event.ts: 添加 DisposedReason 常量，Event.Disposed schema 添加 reason 字段
2. worker.ts: notifyDaemonStop() 广播 global.disposed 事件；ServerLock.clearIfOwner 延后
3. sync.tsx: global.disposed 事件处理（检查 reason=daemon-stop -> exit）；server.instance.disposed 守卫
4. global.ts: 移除冗余 GlobalDisposedEvent 定义
5. daemon.test.ts: readUntil 辅助 + daemon stop 广播测试

## Upstream Comparison
- event.ts: 目标 re-export ServerEvent from schema 包
- worker.ts: 路径 cli/cmd/tui -> cli/tui；有 #235 的 3s Promise.race 超时包装
- sync.tsx: 路径迁移；useExit 在 event.subscribe 后；Exit 类型为 (reason?: unknown) => void
- global.ts: 目标无 BusEvent import 和 GlobalDisposedEvent（no-op drop）
- daemon.test.ts: 目标有 spawnDaemon/runDaemonStop 但无 readFirstLine

## Target Adaptation
1. event.ts: 添加 DisposedReason（不修改 schema 包）
2. worker.ts: 适配路径；notifyDaemonStop；clearIfOwner 延后；保留 Promise.race
3. sync.tsx: 适配路径；useExit 移前；exit.message.set -> void exit("msg")；字符串字面量代替 DisposedReason 导入
4. global.ts: drop（no-op）
5. daemon.test.ts: readUntil + 新测试

## Hunk Mapping
| Source File | Target File | Adaptation |
|---|---|---|
| event.ts | event.ts | 添加 DisposedReason |
| worker.ts (cli/cmd/tui) | worker.ts (cli/tui) | 路径迁移 + notifyDaemonStop |
| sync.tsx (cli/cmd/tui/context) | sync.tsx (tui/src/context) | 路径迁移 + exit 类型适配 |
| global.ts | global.ts | no-op drop |
| daemon.test.ts | daemon.test.ts | readUntil + 新测试 |

## Failure Revision History
- 第一次 typecheck：sync.tsx exit.message.set 类型错误（Exit 为函数类型非对象）
- 修复：改为 void exit("opencode daemon stopped.")
- E/C 从 67/10 调整为 66/12

## Final Behavior
daemon stop 时先广播 global.disposed 事件，让 TUI 走正常退出路径。lock 清理延后到 Database.close 之后。

## DUAL-AUDIT SEAL（批次 271-275）
## DUAL-AUDIT SEAL（批次 271-275，第一轮）
- 正向审计 (ses_013c01df5ffe): 5/5 PASS, 无 B/G
- 逆向审计 (ses_013c001adffe): 5/5 PASS, 无 B/G (N-01~N-05 非阻塞)
- materialize 275: passed 275/275
- opencode typecheck exit 0, tui typecheck exit 0, core typecheck exit 0 (手动验证)
- packages/app TS1128 pre-existing (不阻塞)
- install 指纹: dff111f30f2b... (reused)
- E/C: 271=5/2, 272=7/2, 273=66/12, 274=0/0, 275=0/0 — 全部 PASS
- 两份结果一致，无未关闭 B/G，批次放行
