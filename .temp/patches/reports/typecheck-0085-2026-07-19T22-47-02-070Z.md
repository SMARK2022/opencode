# Patch Dry Run typecheck-0085-2026-07-19T22-47-02-070Z

- Source: `dev-smark`
- Source tip: `d0ceb469011412b4ac5058a12d5fe4f247bdac79`
- Mode: typecheck
- Target baseline: `4473fc3c9055046183990a965d68df3db7ea6f62`
- Manifest JSON SHA-256: `6f162934b5a1313c0df474a3c73db05bf0453a337d659cce38243b85dcaf2f5e`
- Manifest TSV SHA-256: `788f6cfd9b9b948311c00b7ce96e0c0a94748c56d56db7129daacca7c3325572`
- Patch count: 85
- Passed: 83
- Reused prefix: 80
- Replay base: 0080-0a8361dfa4d3
- Explicit rebuild: false
- Applied in this run: 3
- Stopped at: 84
- Temporary repo: cleaned
- Materialized index: 85
- State directory: none
- Retained states: 0080-0a8361dfa4d3, 0077-9768471cf3fc, 0075-4141d5e979ff
- Source repository unchanged: true
- Target repository unchanged: true
- Simulation preflight: provenance verified at `4473fc3c9055046183990a965d68df3db7ea6f62`





| # | SHA | Status | Phase | Reason | Subject |
|---:|---|---|---|---|---|
| 81 | `85442a1f1e68` | passed | apply | applied | feat(notebook): 优化虚拟文档行范围计算，修正行号处理逻辑，增强源代码渲染效率 |
| 82 | `afccfe84a555` | passed | apply | applied | feat(vscode): 添加VS Code桥接功能，支持笔记本摘要、源代码读取、执行和编辑操作 |
| 83 | `2e8c148edf30` | passed | apply | applied | feat(network-proxy): 引入NetworkProxy以支持插件和提供者的fetch请求，增强代理处理能力 |
| 84 | `7759649fd14d` | failed | check | conflict | feat(vscode): 增强笔记本请求处理，添加文件锁机制以序列化请求，优化运行逻辑 |

## Failure Output

### #84 7759649fd14dcb2b5e0fb268dc013e2609f9f209

```text
Checking patch packages/opencode/src/ide/vscode-bridge.ts...
error: while searching for:
import fs from "fs/promises"
import os from "os"
import path from "path"
import { fileURLToPath } from "url"
import { Global } from "@opencode-ai/core/global"

const DEFAULT_HEALTH_TIMEOUT = 500
const DEFAULT_STALE_MS = 15_000
const RESOLVE_CACHE_MS = 5_000

export type BridgeEntry = {
  schema: 1
  id: string
  pid: number

error: patch failed: packages/opencode/src/ide/vscode-bridge.ts:1
error: packages/opencode/src/ide/vscode-bridge.ts: patch does not apply
Checking patch sdks/vscode/src/bridge.ts...
error: while searching for:
  const { randomUUID } = await import("node:crypto")
  const token = randomUUID()

  // 常量：httpServer
  const httpServer = http.createServer(async (request, response) => {
    const url = new URL(request.url ?? "/", `http://${BRIDGE_HOST}`)
    output.appendLine(`[bridge] ${request.method} ${url.pathname}`)

error: patch failed: sdks/vscode/src/bridge.ts:49
error: sdks/vscode/src/bridge.ts: patch does not apply
Checking patch sdks/vscode/src/notebook/run.ts...
error: while searching for:
if (type !== undefined && type !== "cell" && type !== "range") throw new Error("type must be one of: cell, range")
const cellId = stringProp(input, "cellId")
if (!cellId) throw new Error("cellId is required")

  const notebook = await resolveNotebook(filePath)
  const commands = await vscode.commands.getCommands(true)

error: patch failed: sdks/vscode/src/notebook/run.ts:37
error: sdks/vscode/src/notebook/run.ts: patch does not apply
Checking patch sdks/vscode/src/notebook/summary.ts...
error: while searching for:
  const status = failed.length
    ? `${failed.length} failed`
    : executed.length === code.length && code.length > 0
      ? "All code cells have successful session execution state."
      : `${executed.length}/${code.length} code cells executed`

  return [
    `Notebook: ${toPosixPath(notebook.uri.fsPath || notebook.uri.toString())}`,
    `Type: ${notebook.notebookType}, dirty=${notebook.isDirty}, num_cells=${notebook.cellCount}, runtime=${runtimeLabel(notebook) ?? "unknown"}. ${status}`,
    "",
    `Conventions: cell indexes (cN) are 1-based; cell IDs (#VSC-xxxxxxxx) are stable across insert/delete; line range=[A,B] is 1-based inclusive in the virtual source document; headers and visual separators are unnumbered.`,
    "",
    'Cells (format: cN id=<#VSC-xxx> <kind>/<lang> lines=<count> range=[A,B] exec="<run state>" existing_outs="<mime list>" first="<first source line>"):',
    ...cells.map((cell) => {

error: patch failed: sdks/vscode/src/notebook/summary.ts:49
error: sdks/vscode/src/notebook/summary.ts: patch does not apply

```



