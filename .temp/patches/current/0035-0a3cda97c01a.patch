diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -263,7 +263,9 @@
   const [timestamps, setTimestamps] = kv.signal<"hide" | "show">("timestamps", "hide")
   const [showDetails, setShowDetails] = kv.signal("tool_details_visibility", true)
   const [showAssistantMetadata, _setShowAssistantMetadata] = kv.signal("assistant_metadata_visibility", true)
-  const [showScrollbar, setShowScrollbar] = kv.signal("scrollbar_visible", false)
+  const [showScrollbar, setShowScrollbar] = // 滚动条默认启用：改善长输出的可读性
+        // 滚动条默认启用：改善长输出的可读性
+        kv.signal("scrollbar_enabled", true)
   const [diffWrapMode] = kv.signal<"word" | "none">("diff_wrap_mode", "word")
   const [_animationsEnabled, _setAnimationsEnabled] = kv.signal("animations_enabled", true)
   const [showGenericToolOutput, setShowGenericToolOutput] = kv.signal("generic_tool_output_visibility", false)
@@ -1171,7 +1173,7 @@
         }}
       >
         <box flexDirection="row" flexGrow={1} minHeight={0}>
-          <box flexGrow={1} minHeight={0} paddingBottom={1} paddingLeft={2} paddingRight={2} gap={1}>
+          <box flexGrow={1} minHeight={0} paddingBottom={1} paddingLeft={2} paddingRight={1} gap={1}>
             <Show when={session()}>
               <scrollbox
                 ref={(r) => (scroll = r)}
@@ -1179,11 +1181,11 @@ export function Session() {
                   paddingRight: showScrollbar() ? 1 : 0,
                 }}
                 verticalScrollbarOptions={{
-                  paddingLeft: 1,
+                  paddingLeft: 0, // 滚动条紧贴右侧，消除左侧留白
                   visible: showScrollbar(),
                   trackOptions: {
                     backgroundColor: theme.backgroundElement,
-                    foregroundColor: theme.border,
+                    foregroundColor: theme.textMuted, // 使用柔和文字色，降低滚动条视觉强度
                   },
                 }}
                 stickyScroll={true}
@@ -2144,23 +2146,40 @@
 function Write(props: ToolProps) {
   const { theme, syntax } = useTheme()
   const pathFormatter = usePathFormatter()
+  const ctx = use()
+  // 文件内容展开/收缩：超过 10 行时默认收缩，点击展开
+  const [expanded, setExpanded] = createSignal(false)
   const code = createMemo(() => {
     return stringValue(props.input.content) ?? ""
   })
+  const maxLines = 20
+  const maxChars = createMemo(() => maxLines * Math.max(20, ctx.width - 6))
+  const collapsed = createMemo(() => collapseToolOutput(code(), maxLines, maxChars()))
+  const limited = createMemo(() => {
+    if (expanded() || !collapsed().overflow) return code()
+    return collapsed().output
+  })
 
   return (
     <Switch>
       <Match when={props.metadata.diagnostics !== undefined}>
-        <BlockTool title={"# Wrote " + pathFormatter.format(stringValue(props.input.filePath))} part={props.part}>
+        <BlockTool
+          title={"# Wrote " + pathFormatter.format(stringValue(props.input.filePath))}
+          part={props.part}
+          onClick={collapsed().overflow ? () => setExpanded((prev) => !prev) : undefined}
+        >
           <line_number fg={theme.textMuted} minWidth={3} paddingRight={1}>
             <code
               conceal={false}
               fg={theme.text}
               filetype={filetype(stringValue(props.input.filePath))}
               syntaxStyle={syntax()}
-              content={code()}
+              content={limited()}
             />
           </line_number>
+          <Show when={collapsed().overflow}>
+            <text fg={theme.textMuted}>{expanded() ? "Click to collapse" : "Click to expand"}</text>
+          </Show>
           <Diagnostics diagnostics={props.metadata.diagnostics} filePath={stringValue(props.input.filePath) ?? ""} />
         </BlockTool>
       </Match>
@@ -2435,6 +2454,8 @@
   const ctx = use()
   const { theme, syntax } = useTheme()
   const pathFormatter = usePathFormatter()
+  // diff 内容展开/收缩：超过 20 行时默认收缩，避免长 diff 占满终端
+  const [expanded, setExpanded] = createSignal(false)
 
   const view = createMemo(() => {
     const diffStyle = ctx.tui.diff_style
@@ -2446,14 +2467,24 @@
   const ft = createMemo(() => filetype(stringValue(props.input.filePath)))
 
   const diffContent = createMemo(() => stringValue(props.metadata.diff) ?? "")
+  const diffLines = createMemo(() => diffContent().split("\n"))
+  const diffOverflow = createMemo(() => diffLines().length > 20)
+  const limitedDiff = createMemo(() => {
+    if (expanded() || !diffOverflow()) return diffContent()
+    return [...diffLines().slice(0, 10), "…"].join("\n")
+  })
 
   return (
     <Switch>
       <Match when={stringValue(props.metadata.diff) !== undefined}>
-        <BlockTool title={"← Edit " + pathFormatter.format(stringValue(props.input.filePath))} part={props.part}>
+        <BlockTool
+          title={"← Edit " + pathFormatter.format(stringValue(props.input.filePath))}
+          part={props.part}
+          onClick={diffOverflow() ? () => setExpanded((prev) => !prev) : undefined}  // 点击展开/收缩 diff
+        >
           <box paddingLeft={1}>
             <diff
-              diff={diffContent()}
+              diff={limitedDiff()}
               view={view()}
               filetype={ft()}
               syntaxStyle={syntax()}
@@ -2472,6 +2503,9 @@
               removedLineNumberBg={theme.diffRemovedLineNumberBg}
             />
           </box>
+          <Show when={diffOverflow()}>
+            <text fg={theme.textMuted}>{expanded() ? "Click to collapse" : "Click to expand"}</text>
+          </Show>
           <Diagnostics diagnostics={props.metadata.diagnostics} filePath={stringValue(props.input.filePath) ?? ""} />
         </BlockTool>
       </Match>
@@ -2534,8 +2568,22 @@
     <Switch>
       <Match when={files().length > 0}>
         <For each={files()}>
-          {(file) => (
-            <BlockTool title={title(file)} part={props.part}>
+          {(file) => {
+            // patch 内容展开/收缩：多文件 patch 超过 20 行时收缩，避免长 patch 占满终端
+            const [patchExpanded, setPatchExpanded] = createSignal(false)
+            const patchLines = createMemo(() => (file.patch || "").split("\n"))
+            const patchOverflow = createMemo(() => patchLines().length > 20)
+            const limitedPatch = createMemo(() => {
+              if (patchExpanded() || !patchOverflow()) return file.patch
+              return [...patchLines().slice(0, 10), "…"].join("\n")
+            })
+
+            return (
+            <BlockTool
+              title={title(file)}
+              part={props.part}
+              onClick={patchOverflow() ? () => setPatchExpanded((prev) => !prev) : undefined}  // 点击展开/收缩 patch
+            >
               <Show
                 when={file.type !== "delete"}
                 fallback={
@@ -2544,11 +2592,15 @@
                   </text>
                 }
               >
-                <Diff diff={file.patch} filePath={file.filePath} />
+                <Diff diff={limitedPatch()} filePath={file.filePath} />
+                <Show when={patchOverflow()}>
+                  <text fg={theme.textMuted}>{patchExpanded() ? "Click to collapse" : "Click to expand"}</text>
+                </Show>
                 <Diagnostics diagnostics={props.metadata.diagnostics} filePath={file.movePath ?? file.filePath} />
               </Show>
             </BlockTool>
-          )}
+            )
+          }}
         </For>
       </Match>
       <Match when={true}>


diff --git a/packages/session-ui/src/components/message-part.tsx b/packages/session-ui/src/components/message-part.tsx
--- a/packages/session-ui/src/components/message-part.tsx
+++ b/packages/session-ui/src/components/message-part.tsx
@@ -730,6 +730,8 @@
 function toolDefaultOpen(tool: string, shell = false, edit = false) {
   if (tool === "bash") return shell
   if (tool === "edit" || tool === "write" || tool === "apply_patch") return edit
+  // 非工具消息不默认展开
+  return undefined
 }
 
 export function partDefaultOpen(part: PartType, shell = false, edit = false) {
@@ -2184,9 +2186,11 @@ ToolRegistry.register({
     return (
       <div data-component="edit-tool">
         <BasicTool
-          {...props}
           icon="code-lines"
           defer={props.deferContent !== false}
+          status={props.status}
+          hideDetails={props.hideDetails}
+          defaultOpen={props.defaultOpen}
           trigger={
             <div data-component="edit-trigger">
               <div data-slot="message-part-title-area">
@@ -2251,9 +2255,11 @@ ToolRegistry.register({
     return (
       <div data-component="write-tool">
         <BasicTool
-          {...props}
           icon="code-lines"
           defer={props.deferContent !== false}
+          status={props.status}
+          hideDetails={props.hideDetails}
+          defaultOpen={props.defaultOpen}
           trigger={
             <div data-component="write-trigger">
               <div data-slot="message-part-title-area">
@@ -2334,9 +2340,11 @@ ToolRegistry.register({
         fallback={
           <div data-component="apply-patch-tool">
             <BasicTool
-              {...props}
               icon="code-lines"
               defer={props.deferContent !== false}
+              status={props.status}
+              hideDetails={props.hideDetails}
+              defaultOpen={props.defaultOpen}
               trigger={{
                 title: i18n.t("ui.tool.patch"),
                 subtitle: subtitle(),


diff --git a/packages/ui/src/components/scroll-view.css b/packages/ui/src/components/scroll-view.css
--- a/packages/ui/src/components/scroll-view.css
+++ b/packages/ui/src/components/scroll-view.css
@@ -4,6 +4,8 @@
   flex-direction: column;
   min-height: 0;
   overflow: hidden;
+  height: 100%; /* 滚动视图填满父容器高度 */
+  width: 100%; /* 滚动视图填满父容器宽度 */
 }
 
 .scroll-view__viewport {
@@ -21,13 +23,18 @@
 
 .scroll-view__thumb {
   position: absolute;
-  right: 0;
+  /* 滚动条右移 4px，避免遮挡内容 */
+  right: 4px;
   top: 0;
   width: 12px;
   transition: opacity 200ms ease;
-  cursor: default;
+  /* 指针变为手型，提示可拖拽 */
+  cursor: pointer;
   user-select: none;
+  /* 确保滚动条显示在内容之上，包括 sticky header */
+  z-index: 100;
   opacity: 0;
+  pointer-events: none; /* 默认不拦截指针事件，仅在可见时启用拖拽 */
 }
 
 .scroll-view__thumb::after {
@@ -37,30 +44,33 @@
   transform: translateX(-50%);
   top: 0;
   bottom: 0;
-  width: 4px;
+  width: 6px;  /* 加宽滚动条，改善可点击区域 */
   border-radius: 9999px;
-  background-color: var(--border-weak-base);
+  background-color: rgba(0, 0, 0, 0.3); /* 浅色模式下半透明黑色滚动条 */
   backdrop-filter: blur(4px);
-  transition: background-color 150ms ease;
+  transition: all 150ms ease; /* 宽度与颜色统一过渡，悬停时更顺滑 */
 }
 
 .scroll-view__thumb:hover::after,
 .scroll-view__thumb[data-dragging="true"]::after {
-  background-color: var(--border-strong-base);
+  width: 8px; /* 悬停/拖拽时加宽，增强可点击区域 */
+  background-color: rgba(0, 0, 0, 0.5);
 }
 
 .dark .scroll-view__thumb::after,
 [data-theme="dark"] .scroll-view__thumb::after {
-  background-color: var(--border-weak-base);
+  background-color: rgba(255, 255, 255, 0.3); /* 深色模式下半透明白色滚动条 */
 }
 
 .dark .scroll-view__thumb:hover::after,
 [data-theme="dark"] .scroll-view__thumb:hover::after,
 .dark .scroll-view__thumb[data-dragging="true"]::after,
 [data-theme="dark"] .scroll-view__thumb[data-dragging="true"]::after {
-  background-color: var(--border-strong-base);
+  width: 8px;
+  background-color: rgba(255, 255, 255, 0.5);
 }
 
 .scroll-view__thumb[data-visible="true"] {
   opacity: 1;
+  pointer-events: auto; /* 可见时启用指针事件，允许拖拽滚动条 */
 }


diff --git a/packages/ui/src/components/scroll-view.tsx b/packages/ui/src/components/scroll-view.tsx
--- a/packages/ui/src/components/scroll-view.tsx
+++ b/packages/ui/src/components/scroll-view.tsx
@@ -296,12 +296,14 @@
           ref={thumbRef}
           onPointerDown={onThumbPointerDown}
           class="scroll-view__thumb"
-          data-visible={thumbVisible()}
+          // data-visible 用字符串 "true" 而非布尔值，匹配 CSS [data-visible="true"] 选择器
+        data-visible={thumbVisible() ? "true" : undefined}
           data-dragging={isDragging()}
           style={{
             height: `${thumbHeight()}px`,
             transform: `translateY(${thumbTop()}px)`,
-            "z-index": 100, // ensure it displays over content
+            // 滚动条可交互：启用指针事件以便拖拽
+        "pointer-events": "auto", // ensure it displays over content
           }}
         />
       </Show>


diff --git a/packages/session-ui/src/components/session-turn.css b/packages/session-ui/src/components/session-turn.css
--- a/packages/session-ui/src/components/session-turn.css
+++ b/packages/session-ui/src/components/session-turn.css
@@ -7,16 +7,21 @@
   align-items: flex-start;
   justify-content: flex-start;
 
-  [data-slot="session-turn-content"] {
+  /* ScrollView 容器样式：仅设置尺寸，滚动交由 ScrollView 自身处理 */
+  .scroll-view[data-slot="session-turn-content"] {
     flex-grow: 1;
     width: 100%;
     height: 100%;
     min-width: 0;
+  }
+
+  /* 保留原有的滚动条隐藏样式（用于非 ScrollView 的情况） */
+  [data-slot="session-turn-content"]:not(.scroll-view) {
     overflow-y: auto;
     scrollbar-width: none;
   }
 
-  [data-slot="session-turn-content"]::-webkit-scrollbar {
+  [data-slot="session-turn-content"]:not(.scroll-view)::-webkit-scrollbar {
     display: none;
   }
 
@@ -239,3 +239,13 @@
 [data-slot="session-turn-list"] {
   gap: 24px;
 }
+
+/* ScrollView 容器使用自身滚动条，非 ScrollView 的 div 保留原生 overflow-y 滚动 */
+.scroll-view[data-slot="session-turn-content"] {
+  overflow: auto;
+}
+
+:not(.scroll-view)[data-slot="session-turn-content"] {
+  overflow-y: auto;
+  scrollbar-width: none;
+}


diff --git a/packages/session-ui/src/components/session-turn.tsx b/packages/session-ui/src/components/session-turn.tsx
--- a/packages/session-ui/src/components/session-turn.tsx
+++ b/packages/session-ui/src/components/session-turn.tsx
@@ -25,6 +25,7 @@
 import { createAutoScroll } from "@opencode-ai/ui/hooks"
 import { useI18n } from "@opencode-ai/ui/context/i18n"
 import { normalize } from "./session-diff"
+import { ScrollView } from "@opencode-ai/ui/scroll-view"
 
 function record(value: unknown): value is Record<string, unknown> {
   return !!value && typeof value === "object" && !Array.isArray(value)
@@ -383,8 +384,8 @@
 
   return (
     <div data-component="session-turn" class={props.classes?.root}>
-      <div
-        ref={autoScroll.scrollRef}
+      <ScrollView
+        viewportRef={autoScroll.scrollRef}
         onScroll={autoScroll.handleScroll}
         data-slot="session-turn-content"
         class={props.classes?.content}
@@ -536,7 +537,7 @@
           </Show>
           {props.children}
         </div>
-      </div>
+      </ScrollView>
     </div>
   )
 }

