From 707d679466ec973af1dca2a64f3c8fa98738a8d4 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Thu, 7 May 2026 13:13:46 +0800
Subject: [PATCH] =?UTF-8?q?feat(agent):=20=E6=B7=BB=E5=8A=A0=20VSCode=20?=
 =?UTF-8?q?=E7=AC=94=E8=AE=B0=E6=9C=AC=E7=9B=B8=E5=85=B3=E6=9D=83=E9=99=90?=
 =?UTF-8?q?=E9=80=89=E9=A1=B9=EF=BC=8C=E6=9B=B4=E6=96=B0=E8=AE=A1=E5=88=92?=

diff --git a/packages/opencode/src/agent/agent.ts b/packages/opencode/src/agent/agent.ts
index 8530e46ee3..b8cb349e65 100644
--- a/packages/opencode/src/agent/agent.ts
+++ b/packages/opencode/src/agent/agent.ts
@@ -188,6 +188,10 @@ const layer = Layer.effect(
                 edit: "ask",
                 write: "ask",
                 apply_patch: "ask",
+                // #99：build 对 notebook 变更类工具默认 ask
+                vscode_notebook_run: "ask",
+                vscode_notebook_edit: "ask",
+                vscode_notebook_env: "ask",
               }),
               user,
             ),
@@ -197,7 +201,7 @@ const layer = Layer.effect(
           },
           plan: {
             name: "plan",
-            description: "Plan mode. Disallows all edit tools.",
+            description: "Plan mode. Disallows all edit tools and notebook mutations.",
             options: {},
             permission: Permission.merge(
               defaults,
@@ -215,6 +219,10 @@ const layer = Layer.effect(
                   [path.join(".opencode", "plans", "*.md")]: "allow",
                   [path.relative(ctx.worktree, path.join(Global.Path.data, path.join("plans", "*.md")))]: "allow",
                 },
+                // #99：plan 禁止 notebook run/edit/env 变异
+                vscode_notebook_run: "deny",
+                vscode_notebook_edit: "deny",
+                vscode_notebook_env: "deny",
               }),
               user,
             ),
diff --git a/packages/tui/src/util/context-usage.ts b/packages/tui/src/util/context-usage.ts
index 6bb53c03d4..f05b1970d3 100644
--- a/packages/tui/src/util/context-usage.ts
+++ b/packages/tui/src/util/context-usage.ts
@@ -485,7 +485,8 @@ function messageTokens(messages: WithParts[], ratio = 4) {
 }
 
 // 汇总已确认用量：优先从 step-finish 部件提取，无确认时回退到助手消息自身的 token 字段
-function usageTotals(messages: WithParts[]): ContextUsageData["details"]["usage"] {
+// ratio 用于流式字符估 token（未完成 assistant 增量）
+function usageTotals(messages: WithParts[], ratio: number): ContextUsageData["details"]["usage"] {
   const usage: ContextUsageData["details"]["usage"] = {
     input: 0,
     output: 0,
@@ -495,27 +496,21 @@ function usageTotals(messages: WithParts[]): ContextUsageData["details"]["usage"
     total: 0,
     cost: 0,
   }
-  let confirmed = false
+  // 每条消息优先用 step-finish 精确账本；无则回退 assistant tokens
   for (const msg of messages) {
-    for (const part of msg.parts) {
-      if (part.type !== "step-finish") continue
-      confirmed = true
-      usage.input += part.tokens.input
-      // output 不含 reasoning：推理 token 单独累计到 usage.reasoning 避免重复计算
-      usage.output += part.tokens.output
-      usage.reasoning += part.tokens.reasoning
-      usage.cacheRead += part.tokens.cache.read
-      usage.cacheWrite += part.tokens.cache.write
-      usage.cost += part.cost
-    }
-  }
-
-  // 无 step-finish 确认时，回退到助手消息自身的 token 累计
-  if (!confirmed) {
-    for (const msg of messages) {
-      if (msg.info.role !== "assistant") continue
+    const hasStepFinish = msg.parts.some((p) => p.type === "step-finish")
+    if (hasStepFinish) {
+      for (const part of msg.parts) {
+        if (part.type !== "step-finish") continue
+        usage.input += part.tokens.input
+        usage.output += part.tokens.output
+        usage.reasoning += part.tokens.reasoning
+        usage.cacheRead += part.tokens.cache.read
+        usage.cacheWrite += part.tokens.cache.write
+        usage.cost += part.cost
+      }
+    } else if (msg.info.role === "assistant") {
       usage.input += msg.info.tokens.input
-      // 同上：output 不含 reasoning
       usage.output += msg.info.tokens.output
       usage.reasoning += msg.info.tokens.reasoning
       usage.cacheRead += msg.info.tokens.cache.read
@@ -523,7 +518,28 @@ function usageTotals(messages: WithParts[]): ContextUsageData["details"]["usage"
       usage.cost += msg.info.cost
     }
   }
-  // total 不含 cost 但含 reasoning：reasoning 单独累计避免与 output 重复
+
+  // 未完成 assistant：把 step-finish 之后的流式增量按字符估 token
+  const lastMsg = messages.findLast((m) => m.info.role === "assistant" && !m.info.time.completed)
+  if (lastMsg) {
+    const lastParts = lastMsg.parts
+    const lastSFIdx = lastParts.reduce((i, p, idx) => (p.type === "step-finish" ? idx : i), -1)
+    let outputChars = 0
+    let inputChars = 0
+    for (let i = lastSFIdx + 1; i < lastParts.length; i++) {
+      const p = lastParts[i]
+      if (p.type === "text" && !p.ignored) outputChars += (p.text ?? "").length
+      if (p.type === "reasoning") outputChars += (p.text ?? "").length
+      if (p.type === "tool") {
+        if (p.state.status === "pending") outputChars += (p.state.raw ?? "").length
+        else outputChars += JSON.stringify(p.state.input ?? {}).length
+        if (p.state.status === "completed") inputChars += (p.state.output ?? "").length
+      }
+    }
+    usage.output += Math.round(outputChars / ratio)
+    usage.input += Math.round(inputChars / ratio)
+  }
+
   usage.total = usage.input + usage.output + usage.reasoning + usage.cacheRead + usage.cacheWrite
   return usage
 }
@@ -1190,8 +1206,8 @@ export async function computeContextData(input: ComputeContextDataInput): Promis
   const bd = latestBreakdown(raw)
 
   const msg = messageTokens(compacted, ratio)
-    // 使用 raw 而非 compacted：统计全部消息的用量，不仅压缩后的尾部
-  const usage = usageTotals(raw)
+  // #99：usage 用 raw 全量消息 + ratio 估流式增量
+  const usage = usageTotals(raw, ratio)
   const instructions = await gatherInstructionFiles(input)
   let instructionDetails = instructions.map((item) => ({ path: item.path, tokens: estimate(item.content, ratio) }))
   const messageText = msg.text.join("\n")
