diff --git a/packages/core/src/session/compaction.ts b/packages/core/src/session/compaction.ts
--- a/packages/core/src/session/compaction.ts
+++ b/packages/core/src/session/compaction.ts
@@ -11,8 +11,11 @@
 
 const DEFAULT_BUFFER = 20_000
 const DEFAULT_KEEP_TOKENS = 8_000
+// 工具输出截断限制：避免单个工具输出占用过多摘要空间
 const TOOL_OUTPUT_MAX_CHARS = 2_000
+// 摘要输出 token 上限：控制摘要长度避免占用过多上下文
 const SUMMARY_OUTPUT_TOKENS = 4_096
+// 摘要模板：恢复快照而非转录，包含文件/代码/错误/修复等关键信息
 const SUMMARY_TEMPLATE = `Output exactly the Markdown structure shown inside <template> and keep the section order unchanged. Do not include the <template> tags in your response.
 <template>
 ## Objective
@@ -35,14 +38,21 @@
 1. [immediate concrete action, or "(none)"]
 2. [next action if known, or "(none)"]
 
-## Relevant Files
-- [file or directory path: why it matters, or "(none)"]
+## Files & Code
+- [path: relevant symbols/sections and why they matter, or "(none)"]
+
+## Errors & Fixes
+- [exact error/output and fix or current status, or "(none)"]
 </template>
 
 Rules:
 - Keep every section, even when empty.
 - Use terse bullets, not prose paragraphs.
-- Preserve exact file paths, symbols, commands, error strings, URLs, and identifiers when known.
+- Preserve exact file paths, symbols, commands, error strings, URLs, identifiers, and line numbers when known.
+- Prefer facts over speculation; remove stale or resolved details to keep the snapshot accurate.
+- Files entries must include why they matter and relevant symbols.
+- Errors entries must include the fix or current status so the next model does not repeat dead ends.
+- No long file contents or large command outputs — reference them, do not reproduce them.
 - Do not mention the summary process or that context was compacted.`
 
 type Entry = {
@@ -158,10 +168,11 @@
   }
 }
 
+// 构建摘要提示词：合并前次摘要、模板和上下文，确保恢复快照的完整性
 export const buildPrompt = (input: { readonly previousSummary?: string; readonly context: readonly string[] }) =>
   [
     input.previousSummary
-      ? `Update the anchored summary below using the conversation history above.\nPreserve still-true details, remove stale details, and merge in the new facts.\n<previous-summary>\n${input.previousSummary}\n</previous-summary>`
+      ? `Update the anchored summary below using the conversation history above.\nPreserve still-true details, remove stale **or resolved** details, and merge in the new facts.\n<previous-summary>\n${input.previousSummary}\n</previous-summary>`
       : "Create a new anchored summary from the conversation history.",
     SUMMARY_TEMPLATE,
     ...input.context,
diff --git a/packages/opencode/src/agent/prompt/compaction.txt b/packages/opencode/src/agent/prompt/compaction.txt
--- a/packages/opencode/src/agent/prompt/compaction.txt
+++ b/packages/opencode/src/agent/prompt/compaction.txt
@@ -1,9 +1,20 @@
+CRITICAL: Respond with TEXT ONLY. Do NOT call tools.
+
 You are an anchored context summarization assistant for coding sessions.
 
 Summarize only the conversation history you are given. The newest turns may be kept verbatim outside your summary, so focus on the older context that still matters for continuing the work.
 
-If the prompt includes a <previous-summary> block, treat it as the current anchored summary. Update it with the new history by preserving still-true details, removing stale details, and merging in new facts.
+If the prompt includes a <previous-summary> block, treat it as the current anchored summary. Update it with the new history by preserving still-true details, removing stale **or resolved** details, and merging in new facts.
 
-Always follow the exact output structure requested by the user prompt. Keep every section, preserve exact file paths and identifiers when known, and prefer terse bullets over paragraphs.
+Produce a recovery snapshot, not a transcript. Capture:
+- Unfinished work and where it stopped
+- Constraints, feedback, and decisions
+- Files, code, and symbols that matter
+- Errors and their fixes
+- Next steps tied to the latest user request
 
+Always follow the exact output structure requested by the user prompt. Keep every section, preserve exact file paths, commands, error strings, symbols, API names, identifiers, and line numbers when known, and prefer terse bullets over paragraphs.
+
+Avoid noise: no raw long file contents, no command output dumps, no tool chatter, no generic advice.
+
 Do not answer the conversation itself. Do not mention that you are summarizing, compacting, or merging context. Respond in the same language as the conversation.
diff --git a/packages/core/test/session-compaction.test.ts b/packages/core/test/session-compaction.test.ts
--- a/packages/core/test/session-compaction.test.ts
+++ b/packages/core/test/session-compaction.test.ts
@@ -7,7 +7,8 @@
   expect(prompt).toContain("## Work State\n### Completed")
   expect(prompt).toContain("### Active")
   expect(prompt).toContain("### Blocked")
-  expect(prompt).toContain("## Relevant Files")
+  expect(prompt).toContain("## Files & Code")
+  expect(prompt).toContain("## Errors & Fixes")
 })
 
 test("compaction describes tool media without embedding base64", () => {
