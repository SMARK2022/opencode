diff --git a/packages/opencode/src/provider/claudecode.ts b/packages/opencode/src/provider/claudecode.ts
--- a/packages/opencode/src/provider/claudecode.ts
+++ b/packages/opencode/src/provider/claudecode.ts
@@ -1,5 +1,4 @@
 import { createHash, randomUUID } from "crypto"
-import { xxh64 } from "@node-rs/xxhash"
 
 // --- 常量与基础配置 ---
 // Claude Code 版本号和指纹盐值，用于计费头和请求签名
@@ -123,10 +122,117 @@
 
 // --- CCH 完整性校验计算 ---
 // xxh64 哈希取低 20 位，用于请求体完整性签名
+// 纯 TypeScript xxh64 实现，替代 @node-rs/xxhash 原生依赖
+// BigInt 运算实现 xxHash64 算法，避免原生编译依赖
+const MASK_64 = 0xffff_ffff_ffff_ffffn
+// xxHash64 规范质数常量，不可修改，修改会导致 CCH 签名校验失败
+const PRIME64_1 = 0x9e37_79b1_85eb_ca87n
+const PRIME64_2 = 0xc2b2_ae3d_27d4_eb4fn
+const PRIME64_3 = 0x1656_67b1_9e37_79f9n
+const PRIME64_4 = 0x85eb_ca77_c2b2_ae63n
+// xxHash64 算法常量：5 个质数用于混合和雪崩
+const PRIME64_5 = 0x27d4_eb2f_1656_67c5n
+
+// 64 位运算辅助：加法、乘法、左循环移位，均截断到 64 位
+const add64 = (a: bigint, b: bigint) => (a + b) & MASK_64
+const mul64 = (a: bigint, b: bigint) => (a * b) & MASK_64
+const rotl64 = (x: bigint, bits: number) => {
+  const r = BigInt(bits)
+  return ((x << r) | (x >> (64n - r))) & MASK_64
+}
+
+// 小端读取：从字节数组指定偏移读取无符号整数
+const readU32LE = (bytes: Uint8Array, offset: number) => {
+  const b0 = bytes[offset] ?? 0
+  const b1 = bytes[offset + 1] ?? 0
+  const b2 = bytes[offset + 2] ?? 0
+  const b3 = bytes[offset + 3] ?? 0
+  return BigInt((b0 + b1 * 0x100 + b2 * 0x10000 + b3 * 0x1000000) >>> 0)
+}
+
+const readU64LE = (bytes: Uint8Array, offset: number) => readU32LE(bytes, offset) | (readU32LE(bytes, offset + 4) << 32n)
+
+// round64：单轮混合，用于 32 字节块的处理
+const round64 = (acc: bigint, lane: bigint) => mul64(rotl64(add64(acc, mul64(lane, PRIME64_2)), 31), PRIME64_1)
+
+const mergeRound64 = (acc: bigint, lane: bigint) => {
+  const merged = (acc ^ round64(0n, lane)) & MASK_64
+  return add64(mul64(merged, PRIME64_1), PRIME64_4)
+}
+
+// avalanche64：最终雪崩步骤，确保输出分布均匀
+const avalanche64 = (value: bigint) => {
+  let hash = value
+  hash = (hash ^ (hash >> 33n)) & MASK_64
+  hash = mul64(hash, PRIME64_2)
+  hash = (hash ^ (hash >> 29n)) & MASK_64
+  hash = mul64(hash, PRIME64_3)
+  hash = (hash ^ (hash >> 32n)) & MASK_64
+  return hash
+}
+
+// xxh64 主函数：对字节流计算 64 位 xxHash，支持种子
+// 分为 32 字节块处理、尾部 8/4/1 字节处理、雪崩三阶段
+export function xxh64(bodyBytes: Uint8Array, seed = 0n) {
+  let offset = 0
+  const len = bodyBytes.length
+  let hash: bigint
+
+  if (len >= 32) {
+    // 4 个累加器并行处理 32 字节块
+    let v1 = add64(add64(seed, PRIME64_1), PRIME64_2)
+    let v2 = add64(seed, PRIME64_2)
+    let v3 = seed & MASK_64
+    let v4 = add64(seed, -PRIME64_1)
+
+    // 主循环：每次处理 32 字节，4 个 lane 并行混合
+  const limit = len - 32
+    while (offset <= limit) {
+      v1 = round64(v1, readU64LE(bodyBytes, offset))
+      v2 = round64(v2, readU64LE(bodyBytes, offset + 8))
+      v3 = round64(v3, readU64LE(bodyBytes, offset + 16))
+      v4 = round64(v4, readU64LE(bodyBytes, offset + 24))
+      offset += 32
+    }
+
+    hash = add64(add64(rotl64(v1, 1), rotl64(v2, 7)), add64(rotl64(v3, 12), rotl64(v4, 18)))
+    hash = mergeRound64(hash, v1)
+    hash = mergeRound64(hash, v2)
+    hash = mergeRound64(hash, v3)
+    hash = mergeRound64(hash, v4)
+  } else {
+    hash = add64(seed, PRIME64_5)
+  }
+
+  hash = add64(hash, BigInt(len))
+
+  while (offset + 8 <= len) {
+    const lane = readU64LE(bodyBytes, offset)
+    hash = (hash ^ round64(0n, lane)) & MASK_64
+    hash = add64(mul64(rotl64(hash, 27), PRIME64_1), PRIME64_4)
+    offset += 8
+  }
+
+  if (offset + 4 <= len) {
+    hash = (hash ^ mul64(readU32LE(bodyBytes, offset), PRIME64_1)) & MASK_64
+    hash = add64(mul64(rotl64(hash, 23), PRIME64_2), PRIME64_3)
+    offset += 4
+  }
+
+  while (offset < len) {
+    hash = (hash ^ mul64(BigInt(bodyBytes[offset]), PRIME64_5)) & MASK_64
+    hash = mul64(rotl64(hash, 11), PRIME64_1)
+    offset += 1
+  }
+
+  // 最终雪崩确保均匀分布
+  return avalanche64(hash)
+}
+
 // 计算 CCH：对请求体字节做 xxh64 哈希，取低 20 位十六进制
 export function computeCch(bodyBytes: Uint8Array): string {
-  const hash: bigint = xxh64(bodyBytes)
-  const low20 = hash & BigInt(0xfffff)
+  // 取低 20 位：CCH 签名为 5 位 hex，与 Claude Code 官方格式一致
+  const low20 = xxh64(bodyBytes) & 0xfffffn
   return low20.toString(16).padStart(5, "0")
 }
 
@@ -229,10 +335,23 @@
     }
 
     // 5. 构造打补丁后的最终 Body，保留 output_config
+    // 注入 Read 占位工具，确保 API 返回的工具列表包含 Read
+    // 注入 Read 占位工具，确保 API 返回的工具列表包含 Read
+    // 复制原始工具列表，后续注入 Read 占位工具
+    const patchedTools = Array.isArray(originalBody.tools) ? [...originalBody.tools] : []
+    if (!patchedTools.some((tool: { name?: string }) => tool?.name === "Read")) {
+      patchedTools.push({
+        name: "Read",
+        description: "unsupported",
+        input_schema: { type: "object", properties: {} },
+      })
+    }
+
     const patchedBody = {
       ...originalBody,
       system: newSystem,
       metadata: newMetadata,
+      tools: patchedTools,
       ...(thinking ? { thinking } : {}),
       ...(originalBody.output_config ? { output_config: originalBody.output_config } : {}),
     }


diff --git a/packages/tui/src/component/prompt/index.tsx b/packages/tui/src/component/prompt/index.tsx
--- a/packages/tui/src/component/prompt/index.tsx
+++ b/packages/tui/src/component/prompt/index.tsx
@@ -351,7 +351,9 @@
     const pendingOutputTokens = Math.round(streamingOut / 4)
     // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
     const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
-    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    // 使用 lastSFIdx 直接索引，避免 reverse() 遍历
+    const lastStepFinish =
+      lastSFIdx >= 0 && lastParts[lastSFIdx]?.type === "step-finish" ? lastParts[lastSFIdx] : undefined
     const currentStepInputConfirmed =
       !hasInFlightTail && lastStepFinish
         ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write


diff --git a/packages/tui/src/feature-plugins/sidebar/context.tsx b/packages/tui/src/feature-plugins/sidebar/context.tsx
--- a/packages/tui/src/feature-plugins/sidebar/context.tsx
+++ b/packages/tui/src/feature-plugins/sidebar/context.tsx
@@ -101,7 +101,9 @@
     const pendingOutputTokens = Math.round(streamingOut / 4)
     // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
     const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
-    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    // 使用 lastSFIdx 直接索引，避免 reverse() 遍历
+    const lastStepFinish =
+      lastSFIdx >= 0 && lastParts[lastSFIdx]?.type === "step-finish" ? lastParts[lastSFIdx] : undefined
     const currentStepInputConfirmed =
       !hasInFlightTail && lastStepFinish
         ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write


diff --git a/packages/tui/src/routes/session/subagent-footer.tsx b/packages/tui/src/routes/session/subagent-footer.tsx
--- a/packages/tui/src/routes/session/subagent-footer.tsx
+++ b/packages/tui/src/routes/session/subagent-footer.tsx
@@ -126,7 +126,9 @@
     const pendingOutputTokens = Math.round(streamingOut / 4)
     // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
     const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
-    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    // 使用 lastSFIdx 直接索引，避免 reverse() 遍历
+    const lastStepFinish =
+      lastSFIdx >= 0 && lastParts[lastSFIdx]?.type === "step-finish" ? lastParts[lastSFIdx] : undefined
     const currentStepInputConfirmed =
       !hasInFlightTail && lastStepFinish
         ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write


diff --git a/packages/opencode/package.json b/packages/opencode/package.json
--- a/packages/opencode/package.json
+++ b/packages/opencode/package.json
@@ -81,7 +81,6 @@
     "@ff-labs/fff-bun": "0.9.4",
     "@gitlab/opencode-gitlab-auth": "1.3.3",
     "@modelcontextprotocol/sdk": "1.29.0",
-    "@node-rs/xxhash": "1.7.6",
     "@octokit/graphql": "9.0.2",
     "@octokit/rest": "catalog:",
     "@openauthjs/openauth": "catalog:",

