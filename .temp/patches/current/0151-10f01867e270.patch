From 10f01867e270d778f1d325146a2e870bf4c2249d Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Fri, 15 May 2026 04:01:13 +0800
Subject: [PATCH] =?UTF-8?q?feat(interrupt):=20=E6=B7=BB=E5=8A=A0=E4=B8=AD?=
 =?UTF-8?q?=E6=96=AD=E5=A4=84=E7=90=86=E5=8A=9F=E8=83=BD=EF=BC=8C=E6=94=AF?=
 =?UTF-8?q?=E6=8C=81=E4=BC=9A=E8=AF=9D=E4=B8=AD=E6=96=AD=E8=AE=A1=E6=95=B0?=

diff --git a/packages/tui/src/component/prompt/interrupt.ts b/packages/tui/src/component/prompt/interrupt.ts
new file mode 100644
index 0000000000..97705e2384
--- /dev/null
+++ b/packages/tui/src/component/prompt/interrupt.ts
@@ -0,0 +1,17 @@
+// #151：双击 Escape 确认窗口（毫秒）
+// #151：5s 内保持 armed，窗口内可再次 abort 而无需重新武装
+export const INTERRUPT_CONFIRMATION_MS = 5000
+
+// #151：第二次 Escape 触发 abort；count 封顶 1，保持 armed 以便窗口内继续 abort
+export function advanceInterruptCount(current: number) {
+  const next = current + 1
+  return {
+    count: Math.min(next, 1),
+    abort: next >= 2,
+  }
+}
+
+// #151：有 sessionID 即可中断（status 可能 stale/idle 仍应可用）
+export function canInterruptSession(sessionID: string | undefined) {
+  return sessionID !== undefined
+}
diff --git a/packages/tui/src/component/prompt/index.tsx b/packages/tui/src/component/prompt/index.tsx
index 9f0362dc88..27fcca52fc 100644
--- a/packages/tui/src/component/prompt/index.tsx
+++ b/packages/tui/src/component/prompt/index.tsx
@@ -59,6 +59,7 @@ import { useTuiConfig } from "../../config"
 import { usePromptWorkspace } from "./workspace"
 import { usePromptMove } from "./move"
 import { readLocalAttachment } from "./local-attachment"
+import { advanceInterruptCount, canInterruptSession, INTERRUPT_CONFIRMATION_MS } from "./interrupt"
 import { useLocation } from "../../context/location"
 
 registerOpencodeSpinner()
@@ -474,7 +475,8 @@ export function Prompt(props: PromptProps) {
         name: "session.interrupt",
         category: "Session",
         hidden: true,
-        enabled: status().type !== "idle",
+        // #151：sessionID 在场即可中断，不依赖可能过期的 busy status
+        enabled: canInterruptSession(props.sessionID),
         run: () => {
           if (auto()?.visible) return
           if (!input.focused) return
@@ -485,17 +487,18 @@ export function Prompt(props: PromptProps) {
           }
           if (!props.sessionID) return
 
-          setStore("interrupt", store.interrupt + 1)
+          // #151：advance 后 count 封顶 1；abort 后不立即清零，窗口内可重复 abort
+          const interrupt = advanceInterruptCount(store.interrupt)
+          setStore("interrupt", interrupt.count)
 
           setTimeout(() => {
             setStore("interrupt", 0)
-          }, 5000)
+          }, INTERRUPT_CONFIRMATION_MS)
 
-          if (store.interrupt >= 2) {
+          if (interrupt.abort) {
             void sdk.client.session.abort({
               sessionID: props.sessionID,
             })
-            setStore("interrupt", 0)
           }
           dialog.clear()
         },
diff --git a/packages/tui/test/prompt/prompt-interrupt.test.ts b/packages/tui/test/prompt/prompt-interrupt.test.ts
new file mode 100644
index 0000000000..6a884b2f5b
--- /dev/null
+++ b/packages/tui/test/prompt/prompt-interrupt.test.ts
@@ -0,0 +1,28 @@
+import { describe, expect, test } from "bun:test"
+import {
+  advanceInterruptCount,
+  canInterruptSession,
+  INTERRUPT_CONFIRMATION_MS,
+} from "../../src/component/prompt/interrupt"
+
+describe("advanceInterruptCount", () => {
+  // #151：第一次按键武装，第二次 abort，且 count 保持 1
+  test("keeps the prompt armed after abort", () => {
+    expect(advanceInterruptCount(0)).toEqual({ count: 1, abort: false })
+    expect(advanceInterruptCount(1)).toEqual({ count: 1, abort: true })
+    expect(advanceInterruptCount(1)).toEqual({ count: 1, abort: true })
+  })
+})
+
+describe("canInterruptSession", () => {
+  test("allows interrupt when a session id is present", () => {
+    expect(canInterruptSession("ses_1")).toBe(true)
+    expect(canInterruptSession(undefined)).toBe(false)
+  })
+})
+
+describe("INTERRUPT_CONFIRMATION_MS", () => {
+  test("is five seconds", () => {
+    expect(INTERRUPT_CONFIRMATION_MS).toBe(5000)
+  })
+})
