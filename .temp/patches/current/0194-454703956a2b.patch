From 454703956a2bc17b8d7984ef8a86d0766182e1b3 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Fri, 22 May 2026 18:09:51 +0800
Subject: [PATCH] fix: align upload token estimates with confirmed usage


diff --git a/packages/opencode/src/session/compaction.ts b/packages/opencode/src/session/compaction.ts
index a3177275ac..20235fda41 100644
--- a/packages/opencode/src/session/compaction.ts
+++ b/packages/opencode/src/session/compaction.ts
@@ -5,7 +5,8 @@ import { Session } from "./session"
 import { SessionID, MessageID, PartID } from "./schema"
 import { Provider } from "@/provider/provider"
 import { MessageV2 } from "./message-v2"
-import { Token } from "@/util/token"
+// #194：改用 TokenEstimate 统一上传估算
+import { TokenEstimate } from "@/token/estimate"
 import { SessionProcessor } from "./processor"
 import { Agent } from "@/agent/agent"
 import { Plugin } from "@/plugin"
@@ -183,7 +184,8 @@ const layer = Layer.effect(
       model: Provider.Model
     }) {
       const msgs = yield* MessageV2.toModelMessagesEffect(input.messages, input.model)
-      return Token.estimate(JSON.stringify(msgs))
+      // #194：tail-retention 保持原始大小启发式
+      return TokenEstimate.estimateText(JSON.stringify(msgs))
     })
 
     const select = Effect.fn("SessionCompaction.select")(function* (input: {
@@ -267,7 +269,8 @@ const layer = Layer.effect(
           if (part.state.status !== "completed") continue
           if (PRUNE_PROTECTED_TOOLS.includes(part.tool)) continue
           if (part.state.time.compacted) break loop
-          const estimate = Token.estimate(part.state.output)
+          // #194：prune 用 estimateText 作粗略大小代理
+          const estimate = TokenEstimate.estimateText(part.state.output)
           total += estimate
           if (total <= PRUNE_PROTECT) continue
           pruned += estimate
@@ -366,6 +369,16 @@ const layer = Layer.effect(
         stripMedia: true,
         toolOutputMaxChars: TOOL_OUTPUT_MAX_CHARS,
       })
+      // #194：compaction summary 上传估算——与 prompt.ts 同一 estimateUploadInput 路径
+      // #194：summary 绕过 prompt.ts，需在此处独立 seed pending upload snapshot
+      const compactionMessages = [...modelMessages, { role: "user" as const, content: [{ type: "text" as const, text: nextPrompt }] }]
+      const compactionEstimate = TokenEstimate.sanitizeModelMessages(compactionMessages)
+      const estimatedInput = TokenEstimate.estimateUploadInput({
+        text: compactionEstimate.text,
+        attachments: compactionEstimate.attachments,
+        history: selected.head,
+        model,
+      }).inputTokens
       const ctx = yield* InstanceState.context
       const msg: SessionV1.Assistant = {
         id: MessageID.ascending(),
@@ -381,12 +394,16 @@ const layer = Layer.effect(
           root: ctx.worktree,
         },
         cost: 0,
+        // #194：input 用 estimateUploadInput 估算——TUI/stats 与 daemon 口径一致
         tokens: {
           output: 0,
-          input: 0,
+          input: estimatedInput,
           reasoning: 0,
           cache: { read: 0, write: 0 },
         },
+        // #194：inputChars/inputTokens 供 TUI pending 展示——与 prompt.ts 对齐
+        inputChars: JSON.stringify(compactionMessages).length,
+        inputTokens: estimatedInput,
         modelID: model.id,
         providerID: model.providerID,
         time: {
diff --git a/packages/opencode/src/session/prompt.ts b/packages/opencode/src/session/prompt.ts
index 16715bba2f..d5d78fb956 100644
--- a/packages/opencode/src/session/prompt.ts
+++ b/packages/opencode/src/session/prompt.ts
@@ -43,7 +43,7 @@ import { FSUtil } from "@opencode-ai/core/fs-util"
 import { Truncate } from "@/tool/truncate"
 import { Image } from "@/image/image"
 import { decodeDataUrl } from "@/util/data-url"
-import { AttachmentToken } from "@opencode-ai/core/util/attachment-token"
+// #194：AttachmentToken 已由 TokenEstimate 替代
 import { Process } from "@/util/process"
 import { Cause, Effect, Exit, Latch, Layer, Option, Scope, Context, Schema, Types , Semaphore} from "effect"
 import { InstanceState } from "@/effect/instance-state"
@@ -59,19 +59,11 @@ import { SessionTable } from "@opencode-ai/core/session/sql"
 import { SessionReminders } from "./reminders"
 import { SessionTools } from "./tools"
 import { LLMEvent } from "@opencode-ai/llm"
-// #140：无历史样本时回退 Token.estimate（chars/4）
-import { estimate as tokenEstimate } from "@/util/token"
+// #194：改用 TokenEstimate 统一上传估算
+import { TokenEstimate } from "@/token/estimate"
 // #159：decide 窗口裁剪用 usable 上限（与 overflow 同一口径）
 import { usable } from "./overflow"
 
-// #140：输入侧 chars/token 样本窗口与夹紧范围
-const INPUT_CHARS_HISTORY_LIMIT = 100_000
-const MIN_CHARS_PER_TOKEN = 3
-const MAX_CHARS_PER_TOKEN = 4
-// #140：防止异常样本把 preflight 估得过乐观/过悲观
-function clampCharsPerToken(value: number) {
-  return Math.min(MAX_CHARS_PER_TOKEN, Math.max(MIN_CHARS_PER_TOKEN, value))
-}
 
 // #159：decide 最近轮目标占用区间（相对 usable）
 // LATEST_MIN：最新一轮已≥60% 则不再向历史扩展
@@ -1304,7 +1296,7 @@ const layer = Layer.effect(
       throw new Error("Impossible")
     })
 
-    // #159：从最近 user 起点向后试窗口；ratio 相对 usable，估量用 tip #140 tokenEstimate
+    // #159：从最近 user 起点向后试窗口；ratio 相对 usable，估量用 #194 TokenEstimate
     const selectDecideMessages = Effect.fn("SessionPrompt.selectDecideMessages")(function* (input: {
       messages: SessionV1.WithParts[]
       model: Provider.Model
@@ -1327,9 +1319,15 @@ const layer = Layer.effect(
       for (const [position, start] of starts.entries()) {
         const candidate = sanitized.slice(start)
         const modelMessages = yield* MessageV2.toModelMessagesEffect(candidate, input.model)
-        // 不用 estimateInputTokens：tip 已历史校准，decide 窗口统一 Token.estimate
+        // #194：decide mode 用 estimateUploadInput——与 normal prompt 同一估算路径
+        const decideEstimate = TokenEstimate.sanitizeModelMessages(modelMessages)
         const ratio =
-          tokenEstimate([input.systemText, JSON.stringify(modelMessages)].join("\n")) / limit
+          TokenEstimate.estimateUploadInput({
+            text: [input.systemText, decideEstimate.text].join("\n"),
+            attachments: decideEstimate.attachments,
+            history: candidate,
+            model: input.model,
+          }).inputTokens / limit
         // 超满：退回上一个可容纳窗口
         if (ratio >= DECIDE_MAX) return fallback ?? candidate
 
@@ -1560,7 +1558,7 @@ const layer = Layer.effect(
             const messages = [...modelMsgs, ...(isLastStep ? [{ role: "assistant" as const, content: MAX_STEPS_PROMPT }] : [])]
             // #175：消毒后文本用于估计；raw 单独保留线长
             // #175：messagesText 不得再含 base64
-            const messageEstimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate(messages)
+            const messageEstimate = TokenEstimate.sanitizeModelMessages(messages)
             const messagesText = messageEstimate.text
             // #175：raw 仅用于 inputChars，不参与 Token.estimate 文本路径
             const rawMessagesText = JSON.stringify(messages)
@@ -1612,35 +1610,14 @@ const layer = Layer.effect(
               // #175：count>0 才写 media，兼容无附件会话
               ...(messageEstimate.attachments.count > 0 ? { media: messageEstimate.attachments } : {}),
             } as SessionV1.StepFinishPart["inputBreakdown"]
-            // #140：输入侧比率校准（历史 step-finish inputChars/tokens），夹紧到 [3,4]
-            // #140：history 校准样本来自 step-finish.inputChars
-            let historyInputChars = 0
-            let historyInputTokens = 0
-            for (let i = msgs.length - 1; i >= 0 && historyInputChars < INPUT_CHARS_HISTORY_LIMIT; i--) {
-              const m = msgs[i]
-              if (m.info.role !== "assistant") continue
-              const sf = m.parts.findLast((p) => p.type === "step-finish")
-              if (!sf) continue
-              const chars = (sf as any).inputChars
-              // 样本不足 100 字符跳过，与 TUI charsPerTokenFromHistory 对齐
-              if (!chars || chars < 100) continue
-              const tokens = (sf.tokens.input ?? 0) + (sf.tokens.cache?.read ?? 0) + (sf.tokens.cache?.write ?? 0)
-              if (typeof chars === "number" && chars > 0 && tokens > 0) {
-                // 附件不参与输入侧 chars/token 密度校准
-                const bd = (sf as any).inputBreakdown
-                // #175：history 与 live 同一 raw→text 密度公式
-                historyInputChars += bd?.media
-                  ? Math.max(0, chars - bd.media.rawChars + bd.media.textChars)
-                  : Math.max(0, chars - (bd?.messages?.attachments ?? 0))
-                historyInputTokens += tokens
-              }
-            }
-            // #140：样本不足时回退 Token.estimate；有样本则夹紧比值
-            // #175：文本估计后加 media.tokens，只加一次
-            const estimatedInput =
-              (historyInputTokens > 0 && historyInputChars > 500
-                ? Math.round(inputChars / clampCharsPerToken(historyInputChars / historyInputTokens))
-                : tokenEstimate([systemText, messagesText, toolsText].join("\n"))) + messageEstimate.attachments.tokens
+            // #194：输入侧比率校准已由 TokenEstimate.learnInputCharsPerToken 接管
+            // #194：用 estimateUploadInput 对齐 compaction 同一估算路径
+            const estimatedInput = TokenEstimate.estimateUploadInput({
+              text: [systemText, messagesText, toolsText].join("\n"),
+              attachments: messageEstimate.attachments,
+              history: requestMsgs,
+              model,
+            }).inputTokens
             // #140：用即将发送的 estimatedInput 做 overflow，而非 lastFinished 历史巨量
             // #175：overflow 使用含 media 的 estimatedInput
             if (
diff --git a/packages/opencode/src/token/accounting.ts b/packages/opencode/src/token/accounting.ts
index b2c9a8b4bc..3d5d1e85be 100644
--- a/packages/opencode/src/token/accounting.ts
+++ b/packages/opencode/src/token/accounting.ts
@@ -1,3 +1,4 @@
 // #193：token 域统一统计入口；#194 将填充完整实现
 // #193：从 TUI 包 re-export，保持现有消费端不中断
-export * from "@opencode-ai/tui/util/token-accounting"
+// #194：相对路径
+export * from "../../../tui/src/util/token-accounting"
diff --git a/packages/opencode/src/token/estimate.ts b/packages/opencode/src/token/estimate.ts
index 53baa6349f..4cd9fdb3e4 100644
--- a/packages/opencode/src/token/estimate.ts
+++ b/packages/opencode/src/token/estimate.ts
@@ -1,3 +1,111 @@
 // #193：token 域估算入口；#194 将填充完整实现
 // #193：从 core 包 re-export，保持现有消费端不中断
-export * from "@opencode-ai/core/util/attachment-token"
+// #194：re-export 旧 API
+export {
+  estimateAttachment,
+  parseDataUrl,
+  type AttachmentInput,
+  type AttachmentTokenEstimate,
+  type SanitizedTextEstimate,
+} from "@opencode-ai/core/util/attachment-token"
+// #194：别名
+export { sanitizeModelMessagesForTokenEstimate as sanitizeModelMessages } from "@opencode-ai/core/util/attachment-token"
+// #194：本地类型
+import type { AttachmentTokenEstimate } from "@opencode-ai/core/util/attachment-token"
+// #194：源模块未导出，本地定义
+export function emptyAttachmentEstimate(): AttachmentTokenEstimate {
+  return { tokens: 0, rawChars: 0, textChars: 0, count: 0, imageTokens: 0, pdfTokens: 0, otherTokens: 0 }
+}
+
+// #194：默认密度
+const DEFAULT_CHARS_PER_TOKEN = 4
+// #194：密度下限
+const MIN_CHARS_PER_TOKEN = 2
+// #194：密度上限
+const MAX_CHARS_PER_TOKEN = 8
+// #194：学习窗口
+const INPUT_CHARS_HISTORY_LIMIT = 100_000
+
+type HistoryModel = { providerID?: string; id?: string; modelID?: string }
+type HistoryMessage = { info?: { role: string; providerID?: string; modelID?: string }; role?: string; providerID?: string; modelID?: string; parts: ReadonlyArray<HistoryPart> }
+type HistoryPart = { type: string; inputChars?: number; inputBreakdown?: { messages?: { attachments?: number }; media?: AttachmentTokenEstimate }; tokens?: { input: number; cache: { read: number; write: number } } }
+type LearnedInputRatio = { charsPerToken: number; source: "model-history" | "session-history" }
+export type UploadInputEstimate = { inputTokens: number; textTokens: number; attachmentTokens: number; charsPerToken: number; source: "model-history" | "session-history" | "default" }
+
+// #194：纯文本估算
+export function estimateText(input: string, charsPerToken = DEFAULT_CHARS_PER_TOKEN) { return Math.max(0, Math.round((input || "").length / charsPerToken)) }
+
+// #194：从历史学习
+export function learnInputCharsPerToken(input: { messages: ReadonlyArray<HistoryMessage>; model?: HistoryModel }): LearnedInputRatio | undefined {
+  const modelRatio = collectInputRatio(input.messages, input.model, true)
+  // #194：同 model 优先
+  if (modelRatio) return { charsPerToken: modelRatio, source: "model-history" }
+  // #194：回退 session
+  const sessionRatio = collectInputRatio(input.messages, input.model, false)
+  if (sessionRatio) return { charsPerToken: sessionRatio, source: "session-history" }
+  return undefined
+}
+
+// #194：上传估算
+export function estimateUploadInput(input: { text: string; attachments: AttachmentTokenEstimate; history: ReadonlyArray<HistoryMessage>; model?: HistoryModel }): UploadInputEstimate {
+  // #194：学习密度
+  const learned = learnInputCharsPerToken({ messages: input.history, model: input.model })
+  const charsPerToken = learned?.charsPerToken ?? DEFAULT_CHARS_PER_TOKEN
+  // #194：文本估算
+  const textTokens = estimateText(input.text, charsPerToken)
+  return { inputTokens: textTokens + input.attachments.tokens, textTokens, attachmentTokens: input.attachments.tokens, charsPerToken, source: learned?.source ?? "default" }
+}
+
+// #194：钳制
+function clampCharsPerToken(value: number) { return Math.min(MAX_CHARS_PER_TOKEN, Math.max(MIN_CHARS_PER_TOKEN, value)) }
+
+function collectInputRatio(messages: ReadonlyArray<HistoryMessage>, model: HistoryModel | undefined, modelOnly: boolean) {
+  let historyInputTokens = 0
+  let historyInputChars = 0
+  for (let i = messages.length - 1; i >= 0 && historyInputChars < INPUT_CHARS_HISTORY_LIMIT; i--) {
+    const message = messages[i]
+    if ((message.info?.role ?? message.role) !== "assistant") continue
+    if (modelOnly && !sameModel(message, model)) continue
+    for (let j = message.parts.length - 1; j >= 0; j--) {
+      const sample = inputRatioSample(message.parts[j])
+      if (!sample) continue
+      historyInputChars += sample.chars
+      historyInputTokens += sample.tokens
+    }
+  }
+  if (historyInputTokens <= 0 || historyInputChars <= 500) return undefined
+  return clampCharsPerToken(historyInputChars / historyInputTokens)
+}
+
+// #194：同 model 判定
+function sameModel(message: HistoryMessage, model: HistoryModel | undefined) {
+  // #194：无 provider 不算
+  if (!model?.providerID) return false
+  const modelID = model.id ?? model.modelID
+  if (!modelID) return false
+  return (message.info?.providerID ?? message.providerID) === model.providerID && (message.info?.modelID ?? message.modelID) === modelID
+}
+
+// #194：step-finish 样本
+function inputRatioSample(part: HistoryPart): { chars: number; tokens: number } | undefined {
+  // #194：只从 step-finish
+  if (part.type !== "step-finish") return undefined
+  const chars = part.inputChars
+  if (!chars || chars < 100) return undefined
+  const confirmedTokens = (part.tokens?.input ?? 0) + (part.tokens?.cache.read ?? 0) + (part.tokens?.cache.write ?? 0)
+  if (confirmedTokens <= 0) return undefined
+  const media = part.inputBreakdown?.media
+  if (media) {
+    // #194：media 分开
+    const textChars = Math.max(0, chars - media.rawChars + media.textChars)
+    const textTokens = Math.max(0, confirmedTokens - media.tokens)
+    return textChars > 0 && textTokens > 0 ? { chars: textChars, tokens: textTokens } : undefined
+  }
+  // #194：旧快照排除
+  if ((part.inputBreakdown?.messages?.attachments ?? 0) > 0) return undefined
+  return { chars, tokens: confirmedTokens }
+}
+
+// #194：统一导出
+export * as TokenEstimate from "./estimate"
+
diff --git a/packages/opencode/src/util/token.ts b/packages/opencode/src/util/token.ts
index d45a51346b..0573c7d343 100644
--- a/packages/opencode/src/util/token.ts
+++ b/packages/opencode/src/util/token.ts
@@ -1 +1,3 @@
 export { Token, estimate } from "@opencode-ai/core/util/token"
+// #194：re-export
+export { TokenEstimate } from "@/token/estimate"
diff --git a/packages/opencode/test/token/estimate.test.ts b/packages/opencode/test/token/estimate.test.ts
index f7b6b825c8..310c3197dc 100644
--- a/packages/opencode/test/token/estimate.test.ts
+++ b/packages/opencode/test/token/estimate.test.ts
@@ -2,12 +2,15 @@
 // #175：单测锁定消毒、远程跳过与分项 token
 import { describe, expect, test } from "bun:test"
 // #193：测试随 estimate 迁移到 token 域
-import { AttachmentToken } from "@/token/estimate"
+// #194：改用 TokenEstimate
+import { TokenEstimate } from "@/token/estimate"
 
-describe("AttachmentToken", () => {
+const stepFinish = (inputChars: number, inputTokens: number, inputBreakdown?: { messages?: { attachments?: number }; media?: { rawChars: number; textChars: number; tokens: number; count: number; imageTokens: number; pdfTokens: number; otherTokens: number } }) => ({ type: "step-finish", inputChars, inputBreakdown, tokens: { input: inputTokens, output: 0, reasoning: 0, cache: { read: 0, write: 0 } } })
+
+describe("TokenEstimate", () => {
   test("estimates image data URLs separately from sanitized text", () => {
     const payload = "a".repeat(7500)
-    const estimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate([
+    const estimate = TokenEstimate.sanitizeModelMessages([
       {
         role: "user",
         content: [
@@ -26,7 +29,7 @@ describe("AttachmentToken", () => {
   })
 
   test("does not treat remote file URLs as uploaded media payloads", () => {
-    const estimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate([
+    const estimate = TokenEstimate.sanitizeModelMessages([
       {
         role: "user",
         content: [
@@ -40,4 +43,29 @@ describe("AttachmentToken", () => {
     expect(estimate.text).toContain("https://example.com/image.png")
     expect(estimate.text).toContain("//example.com/image.png")
   })
-})
+
+  // #194：学习上传密度
+  test("learns upload input density above the legacy four-character fallback", () => {
+    const estimate = TokenEstimate.estimateUploadInput({ text: "x".repeat(827_111), attachments: TokenEstimate.emptyAttachmentEstimate(), history: [{ info: { role: "assistant", providerID: "p", modelID: "m" }, parts: [stepFinish(827_111, 128_159)] }], model: { providerID: "p", id: "m" } })
+    expect(estimate.source).toBe("model-history")
+    expect(estimate.charsPerToken).toBeCloseTo(6.45, 1)
+    expect(estimate.inputTokens).toBeGreaterThan(120_000)
+    expect(estimate.inputTokens).toBeLessThan(135_000)
+  })
+
+  // #194：旧附件排除
+  test("ignores legacy attachment samples that lack media token metadata", () => {
+    const estimate = TokenEstimate.estimateUploadInput({ text: "x".repeat(80_000), attachments: TokenEstimate.emptyAttachmentEstimate(), history: [{ info: { role: "assistant", providerID: "p", modelID: "m" }, parts: [stepFinish(269_504, 12_886, { messages: { attachments: 210_514 } })] }], model: { providerID: "p", id: "m" } })
+    expect(estimate.source).toBe("default")
+    expect(estimate.inputTokens).toBe(20_000)
+  })
+
+  // #194：media 分开
+  test("uses media metadata as separate tokens when learning text density", () => {
+    const estimate = TokenEstimate.estimateUploadInput({ text: "x".repeat(65_000), attachments: TokenEstimate.emptyAttachmentEstimate(), history: [{ info: { role: "assistant", providerID: "p", modelID: "m" }, parts: [stepFinish(3_989_772, 16_280, { messages: { attachments: 3_924_846 }, media: { rawChars: 3_924_846, textChars: 31, tokens: 1_600, count: 1, imageTokens: 1_600, pdfTokens: 0, otherTokens: 0 } })] }], model: { providerID: "p", id: "m" } })
+    expect(estimate.source).toBe("model-history")
+    expect(estimate.charsPerToken).toBeCloseTo(4.42, 1)
+    expect(estimate.inputTokens).toBeGreaterThan(14_000)
+    expect(estimate.inputTokens).toBeLessThan(16_000)
+  })
+})
\ No newline at end of file
diff --git a/packages/tui/src/util/token-accounting.ts b/packages/tui/src/util/token-accounting.ts
index 5f59f52fc4..f364f45730 100644
--- a/packages/tui/src/util/token-accounting.ts
+++ b/packages/tui/src/util/token-accounting.ts
@@ -214,8 +214,9 @@ export function tokenAccounting(
   }
 
   // ── 3. ratio ──
-  // #122：样本不足时回落 DEFAULT_RATIO，避免小样本失真
-  const inputRatio = ratioTokens > 0 && ratioChars > 500 ? ratioChars / ratioTokens : DEFAULT_RATIO
+  // #194：clamp 2..8——与 TokenEstimate.learnInputCharsPerToken 同口径，防极端样本失真
+  // #194：TUI 不能直接 import TokenEstimate（包边界），但 ratio 逻辑与 daemon 对齐
+  const inputRatio = ratioTokens > 0 && ratioChars > 500 ? Math.min(8, Math.max(2, ratioChars / ratioTokens)) : DEFAULT_RATIO
   // #122：输出字符/token 比；样本不足回落 DEFAULT_RATIO
   const outputRatio = outputTokensTotal > 0 && outputCharsTotal > 100 ? outputCharsTotal / outputTokensTotal : DEFAULT_RATIO
 
