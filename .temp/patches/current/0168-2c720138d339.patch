From 2c720138d3397839f61349a04ba4d9b5d0a43372 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Mon, 18 May 2026 22:14:06 +0800
Subject: [PATCH] feat(provider): custom HTTP headers and User-Agent (#168 tip)

diff --git a/packages/core/src/v1/config/provider.ts b/packages/core/src/v1/config/provider.ts
--- a/packages/core/src/v1/config/provider.ts
+++ b/packages/core/src/v1/config/provider.ts
@@ -90,6 +90,14 @@ export const Info = Schema.Struct({
       Schema.Struct({
         apiKey: Schema.optional(Schema.String),
         baseURL: Schema.optional(Schema.String),
+        // #168：具名 headers，进入 JSON schema / 文档，而非仅 rest 透传
+        headers: Schema.optional(Schema.Record(Schema.String, Schema.String)).annotate({
+          description: "Additional HTTP headers to send to this provider.",
+        }),
+        // #168：header-ua 快捷键折叠为 User-Agent；显式 headers UA 优先
+        "header-ua": Schema.optional(Schema.String).annotate({
+          description: "Shortcut for setting the provider request User-Agent header.",
+        }),
         // #147：自定义 provider（如 claudecode）客户端版本覆盖
         version: Schema.optional(Schema.String).annotate({
           description: "Provider-specific client version override, used by custom providers like claudecode",
diff --git a/packages/opencode/src/provider/provider.ts b/packages/opencode/src/provider/provider.ts
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
@@ -52,6 +52,35 @@ function apiUrl({ npm, api }: { npm: string; api?: string }): string {
   return api ?? SDK_DEFAULT_API_URL[npm] ?? ""
 }
 
+// #168：归一化 provider 请求头；剥离非 string / 原 UA 后再写入权威 User-Agent
+// #168：供 resolveSDK / request / native 共用，禁止复制第二套解析
+export function requestHeaders(options: Readonly<Record<string, unknown>>) {
+  const userAgent = requestUserAgent(options)
+  const headers = options.headers
+  const base =
+    headers && typeof headers === "object" && !Array.isArray(headers)
+      ? Object.fromEntries(
+          Object.entries(headers).filter(
+            (entry): entry is [string, string] => entry[0].toLowerCase() !== "user-agent" && typeof entry[1] === "string",
+          ),
+        )
+      : {}
+  if (!userAgent) return base
+  return { ...base, "User-Agent": userAgent }
+}
+
+// #168：headers 内任意大小写 User-Agent 优先于 header-ua 快捷键
+// #168：单一 UA 解析入口，大小写不敏感匹配 User-Agent 键
+export function requestUserAgent(options: Readonly<Record<string, unknown>>) {
+  const headers = options.headers
+  const headerUserAgent =
+    headers && typeof headers === "object" && !Array.isArray(headers)
+      ? Object.entries(headers).find(
+          (entry): entry is [string, string] => entry[0].toLowerCase() === "user-agent" && typeof entry[1] === "string",
+        )?.[1]
+      : undefined
+  return headerUserAgent ?? (typeof options["header-ua"] === "string" ? options["header-ua"] : undefined)
+}
 
 function wrapSSE(res: Response, ms: number, ctl: AbortController) {
   if (typeof ms !== "number" || ms <= 0) return res
@@ -1913,6 +1942,13 @@ for (const [id, provider] of configProviders) {
             ...options["headers"],
             ...model.headers,
           }
+        // #168：把 header-ua 折叠进 headers，并删除 shortcut 避免泄漏给 SDK
+        // #168：model.headers 已并入 options 后再归一化，顺序：options→model→UA 策略
+        const configuredHeaders = requestHeaders(options)
+        if (Object.keys(configuredHeaders).length > 0) options["headers"] = configuredHeaders
+        const configuredUserAgent = requestUserAgent(options)
+        // #168：shortcut 不得进入 SDK constructor options
+        delete options["header-ua"]
 
         const key = Hash.fast(
           JSON.stringify({
@@ -1946,6 +1982,18 @@ for (const [id, provider] of configProviders) {
           const combined = signals.length === 0 ? null : signals.length === 1 ? signals[0] : AbortSignal.any(signals)
           if (combined) opts.signal = combined
 
+          // #168：仅当当前 UA 为空或仍以配置 UA 为前缀时回写，避免误伤无关 UA
+          // #168：SDK 可能追加后缀（ai-sdk/...），startsWith 仍允许刷新为配置值
+          if (configuredUserAgent) {
+            const headers = new Headers(opts.headers)
+            const currentUserAgent = headers.get("user-agent") ?? ""
+            // #168：空 UA 或配置前缀才覆盖；完全无关 UA 保持不动
+            if (!currentUserAgent || currentUserAgent.startsWith(configuredUserAgent)) {
+              headers.set("User-Agent", configuredUserAgent)
+            }
+            opts.headers = headers
+          }
+
           // #83：customFetch 仅鉴权透传；默认走 NetworkProxy.fetch 主路由
           // #83：provider 请求 purpose=provider，区别于基础设施和 npm
           const res = await (
diff --git a/packages/opencode/src/session/llm/request.ts b/packages/opencode/src/session/llm/request.ts
--- a/packages/opencode/src/session/llm/request.ts
+++ b/packages/opencode/src/session/llm/request.ts
@@ -7,7 +7,7 @@ import { Permission } from "@/permission"
 import { ToolSelection } from "@/tool/selection"
 import type { Agent } from "@/agent/agent"
 import type { MessageV2 } from "../message-v2"
-import type { Provider } from "@/provider/provider"
+import { Provider } from "@/provider/provider"
 import { ProviderTransform } from "@/provider/transform"
 import { SystemPrompt } from "../system"
 import { InstallationVersion } from "@opencode-ai/core/installation/version"
@@ -205,6 +205,8 @@ export const prepare = Effect.fn("LLMRequestPrep.prepare")(function* (input: Pre
             ...(input.parentSessionID ? { "x-parent-session-id": input.parentSessionID } : {}),
             "User-Agent": USER_AGENT,
           }),
+      // #168：默认 UA 之后合并 provider 配置头，允许覆盖 User-Agent
+      ...Provider.requestHeaders(input.provider.options),
       ...input.model.headers,
       ...headers,
     },
diff --git a/packages/opencode/src/session/llm/native-runtime.ts b/packages/opencode/src/session/llm/native-runtime.ts
--- a/packages/opencode/src/session/llm/native-runtime.ts
+++ b/packages/opencode/src/session/llm/native-runtime.ts
@@ -1,5 +1,5 @@
 import type { Auth } from "@/auth"
-import type { Provider } from "@/provider/provider"
+import { Provider } from "@/provider/provider"
 import { ProviderTransform } from "@/provider/transform"
 import { errorMessage } from "@/util/error"
 import { isRecord } from "@/util/record"
@@ -103,7 +103,9 @@ export function stream(input: StreamInput): StreamResult {
     topK: input.topK,
     maxOutputTokens: input.maxOutputTokens,
     providerOptions: ProviderTransform.providerOptions(input.model, input.providerOptions ?? {}),
-    headers: { ...providerHeaders(input.provider.options.headers), ...input.headers },
+    // #168：native 与 AI-SDK 共用 requestHeaders，避免 header-ua 只在一侧生效
+    // #168：配置头在前、prepared headers 在后：模型/插件仍可覆盖，但配置 UA 不被默认 UA 无条件碾压需由 request 侧保证
+    headers: { ...Provider.requestHeaders(input.provider.options), ...input.headers },
   })
   const stream = Stream.scoped(
     Stream.unwrap(
diff --git a/packages/opencode/test/provider/provider.test.ts b/packages/opencode/test/provider/provider.test.ts
--- a/packages/opencode/test/provider/provider.test.ts
+++ b/packages/opencode/test/provider/provider.test.ts
@@ -1334,6 +1334,30 @@ it.instance(
   },
 )
 
+// #168：header-ua 快捷键映射为 User-Agent
+// #168：纯函数单测，不依赖 instance/config 加载
+test("provider header-ua maps to request headers", () => {
+  expect(Provider.requestHeaders({ "header-ua": "codex_cli_rs/0.2333" })).toEqual({
+    "User-Agent": "codex_cli_rs/0.2333",
+  })
+})
+
+// #168：显式 headers.User-Agent 覆盖 shortcut，并保留其它头
+test("provider headers User-Agent overrides header-ua shortcut", () => {
+  expect(
+    Provider.requestHeaders({
+      "header-ua": "shortcut",
+      headers: {
+        "User-Agent": "codex_cli_rs/0.2333",
+        "X-Custom": "custom-value",
+      },
+    }),
+  ).toEqual({
+    "X-Custom": "custom-value",
+    "User-Agent": "codex_cli_rs/0.2333",
+  })
+})
+
 it.instance(
   "hosted nvidia provider adds billing origin header",
   Effect.gen(function* () {
diff --git a/packages/opencode/test/session/llm.test.ts b/packages/opencode/test/session/llm.test.ts
--- a/packages/opencode/test/session/llm.test.ts
+++ b/packages/opencode/test/session/llm.test.ts
@@ -752,6 +752,82 @@ function createEventResponse(chunks: unknown[], includeDone = false) {
 }
 
 describe("session.llm.stream", () => {
+  // #168：线上抓取 User-Agent，锁定 headers 与 header-ua 两条配置路径
+  // #168：集成测覆盖 openai-compatible 线上头，防止只测纯函数
+  for (const item of [
+    { name: "options.headers.User-Agent", options: { headers: { "User-Agent": "codex_cli_rs/0.2333" } }, id: "daxiao-codex-headers" },
+    { name: "options.header-ua", options: { "header-ua": "codex_cli_rs/0.2333" }, id: "daxiao-codex-shortcut" },
+  ] as const) {
+    it.instance(
+      `uses configured provider ${item.name} for request user agent`,
+      () =>
+        Effect.gen(function* () {
+          const request = waitRequest(
+            "/chat/completions",
+            new Response(createChatStream("Hello"), {
+              status: 200,
+              headers: { "Content-Type": "text/event-stream" },
+            }),
+          )
+
+          const resolved = yield* Provider.use.getModel(ProviderV2.ID.make(item.id), ModelV2.ID.make("gpt-5.5"))
+          const sessionID = SessionID.make(`session-test-${item.id}`)
+          const agent = {
+            name: "test",
+            mode: "primary",
+            options: {},
+            permission: [{ permission: "*", pattern: "*", action: "allow" }],
+          } satisfies Agent.Info
+
+          const user = {
+            id: MessageID.make(`msg_user-${item.id}`),
+            sessionID,
+            role: "user",
+            time: { created: Date.now() },
+            agent: agent.name,
+            model: { providerID: ProviderV2.ID.make(item.id), modelID: resolved.id },
+          } satisfies SessionV1.User
+
+          yield* drain({
+            user,
+            sessionID,
+            model: resolved,
+            agent,
+            system: ["You are a helpful assistant."],
+            messages: [{ role: "user", content: "Hello" }],
+            tools: {},
+          })
+
+          const capture = yield* Effect.promise(() => request)
+          // #168：配置 UA 必须出现在线上，不被默认 opencode/version 覆盖
+          expect(capture.headers.get("user-agent")).toBe("codex_cli_rs/0.2333")
+        }),
+      {
+        config: () => ({
+          enabled_providers: [item.id],
+          provider: {
+            [item.id]: {
+              npm: "@ai-sdk/openai-compatible",
+              name: "DaXiao Codex",
+              options: {
+                apiKey: "test-key",
+                baseURL: `${state.server!.url.origin}/v1`,
+                ...item.options,
+              },
+              models: {
+                "gpt-5.5": {
+                  name: "GPT-5.5",
+                  limit: { context: 400000, input: 272000, output: 128000 },
+                  modalities: { input: ["text", "image"], output: ["text"] },
+                },
+              },
+            },
+          },
+        }),
+      },
+    )
+  }
+
   const vivgridFixture = { providerID: "vivgrid", modelID: "gemini-3.1-pro-preview" }
   it.instance(
     "sends temperature, tokens, and reasoning options for openai-compatible models",
