diff --git a/packages/tui/src/component/prompt/index.tsx b/packages/tui/src/component/prompt/index.tsx
--- a/packages/tui/src/component/prompt/index.tsx
+++ b/packages/tui/src/component/prompt/index.tsx
@@ -310,14 +310,17 @@
     const lastUser = users.at(-1)
     const requestAssistants = lastUser ? assistants.filter((item) => item.parentID === lastUser.id) : []
 
-    // input/output 默认展示"本次请求"累计；括号内显示会话总累计
+    // 本次请求尚无助手回复时显示零值
+    if (requestAssistants.length === 0) {
+      if (!isRunning) return
+      return { input: 0, output: 0, totalInput: 0, totalOutput: 0, context: undefined, cost: undefined }
+    }
+
+    // 显示规则：外面是当前 step 的估算 token；括号里是当前请求的累计 token
     const requestConfirmed = sumConfirmed(requestAssistants)
-    const globalConfirmed = sumConfirmed(assistants)
 
-    // 从最后一条（可能正在进行中的）消息添加流式估算
-    // 优先取本次请求的最后一条，无则回退到全局最后一条
-    const activeAssistants = requestAssistants.length > 0 ? requestAssistants : assistants
-    const last = activeAssistants.at(-1)!
+    // 从最后一条消息添加流式估算
+    const last = requestAssistants.at(-1)!
     const lastParts = sync.data.part[last.id] ?? []
     const lastSFIdx = lastParts.reduce((idx: number, p, i) => (p.type === "step-finish" ? i : idx), -1)
     // 流式输出：text/reasoning 字符 + pending 工具的 raw + completed 工具的 input
@@ -342,16 +345,27 @@
           return sum
         }, 0)
 
-    // 拆分为本次请求和会话总累计的输入输出
+    // 拆分为当前 step 和请求累计的输入输出
     // pendingInputTokens/pendingOutputTokens 是流式中的估算值，两边共用
     const pendingInputTokens = Math.round(pendingIn / 4)
     const pendingOutputTokens = Math.round(streamingOut / 4)
-    // currentInput/Output 来自本次请求的已确认 token + 流式估算
-    const currentInput = requestConfirmed.input + pendingInputTokens
-    const currentOutput = requestConfirmed.output + pendingOutputTokens
-    // totalInput/Output 来自全部助手消息的已确认 token + 流式估算
-    const totalInput = globalConfirmed.input + pendingInputTokens
-    const totalOutput = globalConfirmed.output + pendingOutputTokens
+    // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
+    const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
+    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    const currentStepInputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write
+        : 0
+    const currentStepOutputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.output + lastStepFinish.tokens.reasoning
+        : 0
+    // currentInput/Output 来自当前 step 的已确认 token + 流式估算
+    const currentInput = currentStepInputConfirmed + pendingInputTokens
+    const currentOutput = currentStepOutputConfirmed + pendingOutputTokens
+    // totalInput/Output 来自本次请求的累计 token + 流式估算
+    const totalInput = requestConfirmed.input + pendingInputTokens
+    const totalOutput = requestConfirmed.output + pendingOutputTokens
     // requestTokens 用于 context 百分比，totalTokens 用于括号显示
     const requestTokens = currentInput + currentOutput
     const totalTokens = totalInput + totalOutput


diff --git a/packages/tui/src/feature-plugins/sidebar/context.tsx b/packages/tui/src/feature-plugins/sidebar/context.tsx
--- a/packages/tui/src/feature-plugins/sidebar/context.tsx
+++ b/packages/tui/src/feature-plugins/sidebar/context.tsx
@@ -62,15 +62,16 @@
     const lastUser = users.at(-1)
     const requestAssistants = lastUser ? assistants.filter((item) => item.parentID === lastUser.id) : []
 
-    // 侧边栏主值显示"本次请求"，括号里显示会话总累计
+    // 本次请求尚无助手回复时返回零值，避免 at(-1)! 解引用 undefined
+    if (requestAssistants.length === 0) {
+      return { tokens: 0, totalTokens: 0, input: 0, output: 0, totalInput: 0, totalOutput: 0, percent: null }
+    }
+
+    // 显示规则：外面是当前 step 的估算 token；括号里是当前请求的累计 token
     const requestConfirmed = sumConfirmed(requestAssistants)
-    const globalConfirmed = sumConfirmed(assistants)
 
     // 从最后一条消息添加流式估算
-    // 优先取本次请求的最后一条，无则回退到全局最后一条
-    // 回退场景：流式中尚未收到 step-finish，requestAssistants 为空
-    const activeAssistants = requestAssistants.length > 0 ? requestAssistants : assistants
-    const last = activeAssistants.at(-1)!
+    const last = requestAssistants.at(-1)!
     const lastParts = props.api.state.part(last.id)
     const lastSFIdx = lastParts.reduce((idx: number, p, i) => (p.type === "step-finish" ? i : idx), -1)
     // 流式输出：text/reasoning + pending 工具 raw + completed 工具 input
@@ -94,16 +95,27 @@
           return sum
         }, 0)
 
-    // 拆分为本次请求和会话总累计的输入输出
+    // 拆分为当前 step 和请求累计的输入输出
     // pendingInputTokens/pendingOutputTokens 是流式中的估算值，两边共用
     const pendingInputTokens = Math.round(pendingIn / 4)
     const pendingOutputTokens = Math.round(streamingOut / 4)
-    // input/output 来自本次请求的已确认 token + 流式估算
-    const input = requestConfirmed.input + pendingInputTokens
-    const output = requestConfirmed.output + pendingOutputTokens
-    // totalInput/Output 来自全部助手消息的已确认 token + 流式估算
-    const totalInput = globalConfirmed.input + pendingInputTokens
-    const totalOutput = globalConfirmed.output + pendingOutputTokens
+    // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
+    const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
+    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    const currentStepInputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write
+        : 0
+    const currentStepOutputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.output + lastStepFinish.tokens.reasoning
+        : 0
+    // input/output 来自当前 step 的已确认 token + 流式估算
+    const input = currentStepInputConfirmed + pendingInputTokens
+    const output = currentStepOutputConfirmed + pendingOutputTokens
+    // totalInput/Output 来自本次请求的累计 token + 流式估算
+    const totalInput = requestConfirmed.input + pendingInputTokens
+    const totalOutput = requestConfirmed.output + pendingOutputTokens
     // tokens 用于百分比，totalTokens 用于括号显示
     const tokens = input + output
     const totalTokens = totalInput + totalOutput


diff --git a/packages/tui/src/routes/session/subagent-footer.tsx b/packages/tui/src/routes/session/subagent-footer.tsx
--- a/packages/tui/src/routes/session/subagent-footer.tsx
+++ b/packages/tui/src/routes/session/subagent-footer.tsx
@@ -85,14 +85,17 @@
     const lastUser = users.at(-1)
     const requestAssistants = lastUser ? assistants.filter((item) => item.parentID === lastUser.id) : []
 
-    // input/output 展示"本次请求"；括号为会话累计总量
+    // 本次请求尚无助手回复时显示零值
+    if (requestAssistants.length === 0) {
+      if (!isRunning) return
+      return { input: 0, output: 0, totalInput: 0, totalOutput: 0, context: undefined, cost: undefined }
+    }
+
+    // 显示规则：外面是当前 step 的估算 token；括号里是当前请求的累计 token
     const requestConfirmed = sumConfirmed(requestAssistants)
-    const globalConfirmed = sumConfirmed(assistants)
 
     // 从最后一条消息添加流式估算
-    // 优先取本次请求的最后一条，无则回退到全局最后一条
-    const activeAssistants = requestAssistants.length > 0 ? requestAssistants : assistants
-    const last = activeAssistants.at(-1)!
+    const last = requestAssistants.at(-1)!
     const lastParts = sync.data.part[last.id] ?? []
     const lastSFIdx = lastParts.reduce((idx: number, p, i) => (p.type === "step-finish" ? i : idx), -1)
     // 流式输出：text/reasoning + pending 工具 raw + completed 工具 input
@@ -117,16 +120,27 @@
           return sum
         }, 0)
 
-    // 拆分为本次请求和会话总累计的输入输出
+    // 拆分为当前 step 和请求累计的输入输出
     // pendingInputTokens/pendingOutputTokens 是流式中的估算值，两边共用
     const pendingInputTokens = Math.round(pendingIn / 4)
     const pendingOutputTokens = Math.round(streamingOut / 4)
-    // currentInput/Output 来自本次请求的已确认 token + 流式估算
-    const currentInput = requestConfirmed.input + pendingInputTokens
-    const currentOutput = requestConfirmed.output + pendingOutputTokens
-    // totalInput/Output 来自全部助手消息的已确认 token + 流式估算
-    const totalInput = globalConfirmed.input + pendingInputTokens
-    const totalOutput = globalConfirmed.output + pendingOutputTokens
+    // 当前 step：如果尾部有进行中的工具调用，则当前 step 尚未确认，显示 0
+    const hasInFlightTail = !last.time.completed && lastParts.some((_, i) => i > lastSFIdx)
+    const lastStepFinish = [...lastParts].reverse().find((p) => p.type === "step-finish")
+    const currentStepInputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.input + lastStepFinish.tokens.cache.read + lastStepFinish.tokens.cache.write
+        : 0
+    const currentStepOutputConfirmed =
+      !hasInFlightTail && lastStepFinish
+        ? lastStepFinish.tokens.output + lastStepFinish.tokens.reasoning
+        : 0
+    // currentInput/Output 来自当前 step 的已确认 token + 流式估算
+    const currentInput = currentStepInputConfirmed + pendingInputTokens
+    const currentOutput = currentStepOutputConfirmed + pendingOutputTokens
+    // totalInput/Output 来自本次请求的累计 token + 流式估算
+    const totalInput = requestConfirmed.input + pendingInputTokens
+    const totalOutput = requestConfirmed.output + pendingOutputTokens
     // requestTokens 用于 context 百分比，totalTokens 用于括号显示
     const requestTokens = currentInput + currentOutput
     const totalTokens = totalInput + totalOutput


