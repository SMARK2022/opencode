
diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -1828,6 +1828,9 @@ const PART_MAPPING = {
 
 const INLINE_TOOL_ICON_WIDTH = 2
 
+// [local-smark] 折叠预览行数：由 maxHeight/overflow 裁剪，而不是重挂更短的正文树
+const REASONING_PREVIEW_ROWS = 5
+
 function ReasoningPart(props: { last: boolean; part: ReasoningPart; message: AssistantMessage }) {
   const { theme } = useTheme()
   const ctx = use()
@@ -1881,60 +1884,29 @@ const renderedContent = createMemo(() => displayContent() || content())
             duration={isDone() ? Locale.duration(duration()) : undefined}
           />
         </box>
-        {/* 折叠推理：保留 5 行预览，避免 hide 模式只剩标题不可窥 */}
-        <Show when={inMinimal() && !expanded() && content()}>
-          <box paddingLeft={2} marginTop={1}>
-            {/* 用 content() 而非 summary().body：summary 可能只剩标题体为空 */}
-            <code
-              filetype="markdown"
-              // #137：预览流式用 styled streaming
-              drawUnstyledText={false}
-              streaming={!isDone()}
-              syntaxStyle={syntax()}
-              content={renderedContent().split(String.fromCharCode(10)).slice(0, 5).join(String.fromCharCode(10))}
-              conceal={ctx.conceal()}
-              fg={theme.textMuted}
-            />
-            {/* 超出预览行时提示可展开；展开仍走 header toggle */}
-            <Show when={renderedContent().split(String.fromCharCode(10)).length > 5}>
-              <text fg={theme.textMuted}>▼ 展开查看完整推理</text>
-            </Show>
-          </box>
-        </Show>
-        <Show when={(!inMinimal() || expanded()) && summary().body}>
-          <box paddingLeft={inMinimal() ? 2 : 0} marginTop={1}>
-            <Switch>
-              <Match when={!isDone()}>
-                <code
-                  filetype="markdown"
-                  // #135/#137：流式推理 styled streaming，避免 unstyled 闪烁
-                  drawUnstyledText={false}
-                  streaming={true}
-                  syntaxStyle={syntax()}
-                  content={summary().body}
-                  conceal={ctx.conceal()}
-                  fg={theme.textMuted}
-                />
-              </Match>
-              {/* #137：推理完成后 keyed remount */}
-              <Match when={isDone()}>
-                <Show keyed when={completedKey()}>
-                  {(_key) => (
-                    <code
-                      filetype="markdown"
-                      drawUnstyledText={false}
-                      streaming={false}
-                      syntaxStyle={syntax()}
-                      content={summary().body}
-                      conceal={ctx.conceal()}
-                      fg={theme.textMuted}
-                    />
-                  )}
-                </Show>
-              </Match>
-            </Switch>
-          </box>
-        </Show>
+        {/* [local-smark] 唯一正文树只改变布局约束（maxHeight/overflow/wrapMode），
+            展开/折叠与 streaming→done 都不重挂已高亮正文，修复展开后正文消失
+            和高度冲突；预览裁剪交给 maxHeight 而非重挂更短内容 */}
+        <box
+          paddingLeft={inMinimal() ? 2 : 0}
+          marginTop={1}
+          maxHeight={!inMinimal() || expanded() ? undefined : REASONING_PREVIEW_ROWS}
+          overflow={!inMinimal() || expanded() ? "visible" : "hidden"}
+        >
+          <code
+            filetype="markdown"
+            drawUnstyledText={false}
+            streaming={!isDone()}
+            syntaxStyle={syntax()}
+            content={renderedContent()}
+            conceal={ctx.conceal()}
+            fg={theme.textMuted}
+            wrapMode={!inMinimal() || expanded() ? "word" : "char"}
+          />
+          <Show when={inMinimal() && !expanded()}>
+            <text fg={theme.textMuted}>▼ 展开查看完整推理</text>
+          </Show>
+        </box>
       </box>
     </Show>
   )
