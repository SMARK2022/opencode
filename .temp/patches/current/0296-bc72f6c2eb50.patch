diff --git a/packages/opencode/test/cli/tui/daemon.test.ts b/packages/opencode/test/cli/tui/daemon.test.ts
index 1c5fa6d..c651fc7 100644
--- a/packages/opencode/test/cli/tui/daemon.test.ts
+++ b/packages/opencode/test/cli/tui/daemon.test.ts
@@ -22,6 +22,7 @@ const INDEX_TS = fileURLToPath(new URL("../../../src/index.ts", import.meta.url)
 
 const DAEMON_START_TIMEOUT_MS = 15_000
 const POLL_INTERVAL_MS = 100
+const DAEMON_STOP_TIMEOUT_MS = 60_000
 
 afterEach(() => {
   ServerLockModule._setLockPath(undefined)
@@ -35,6 +36,10 @@ function isolatedDaemonEnv(lockPath: string, env: Record<string, string> = {}) {
     ...process.env,
     OPENCODE_DAEMON_LAUNCHER_PID: "",
     OPENCODE_PROCESS_ROLE: "main",
+    // Slow WSL/drvfs cold starts can make the real CLI stop path take >5s before
+    // it reaches the daemon control endpoint. Startup-idle behavior tests pass
+    // explicit shorter values below; lifecycle tests need the daemon to stay up.
+    OPENCODE_DAEMON_STARTUP_IDLE_TIMEOUT_MS: "60000",
     ...env,
     OPENCODE_LOCK_PATH: lockPath,
     OPENCODE_DB: path.join(root, "opencode.db"),
@@ -146,7 +151,7 @@ describe("daemon lifecycle", () => {
         await proc.exited.catch(() => undefined)
       }
     },
-    DAEMON_START_TIMEOUT_MS + 10_000,
+    DAEMON_START_TIMEOUT_MS + DAEMON_STOP_TIMEOUT_MS,
   )
 
   // #273：daemon stop 应在退出前向已连接 TUI 广播事件
@@ -170,7 +175,10 @@ describe("daemon lifecycle", () => {
 
           const stop = runDaemonStop(lockPath)
           // #273：期望收到带 daemon-stop 原因的 global.disposed 事件
-          const shutdownEvent = await Promise.race([readUntil(reader, "daemon-stop"), Bun.sleep(5_000).then(() => "timeout")])
+          const shutdownEvent = await Promise.race([
+            readUntil(reader, "daemon-stop"),
+            Bun.sleep(DAEMON_STOP_TIMEOUT_MS).then(() => "timeout"),
+          ])
           expect(shutdownEvent).not.toBe("timeout")
           expect(String(shutdownEvent)).toContain("global.disposed")
           expect(String(shutdownEvent)).toContain("daemon-stop")
@@ -185,7 +193,7 @@ describe("daemon lifecycle", () => {
         await proc.exited.catch(() => undefined)
       }
     },
-    DAEMON_START_TIMEOUT_MS + 10_000,
+    DAEMON_START_TIMEOUT_MS + DAEMON_STOP_TIMEOUT_MS,
   )
 
   test(
@@ -212,7 +220,7 @@ describe("daemon lifecycle", () => {
         await proc.exited.catch(() => undefined)
       }
     },
-    DAEMON_START_TIMEOUT_MS + 10_000,
+    DAEMON_START_TIMEOUT_MS + DAEMON_STOP_TIMEOUT_MS,
   )
 
   // #259：无 lock 时 stop 是 no-op（便于脚本重复调用）
@@ -323,7 +331,7 @@ describe("daemon lifecycle", () => {
         await Promise.all([nonDaemon.exited.catch(() => undefined), proc.exited.catch(() => undefined)])
       }
     },
-    DAEMON_START_TIMEOUT_MS + 10_000,
+    DAEMON_START_TIMEOUT_MS + DAEMON_STOP_TIMEOUT_MS,
   )
 
   test(
diff --git a/packages/opencode/test/format/format.test.ts b/packages/opencode/test/format/format.test.ts
index 72adc84..794e879 100644
--- a/packages/opencode/test/format/format.test.ts
+++ b/packages/opencode/test/format/format.test.ts
@@ -212,7 +212,7 @@ describe("Format", () => {
         formatter: {
           first: {
             command: [
-              "node",
+              process.execPath,
               "-e",
               "const fs = require('fs'); const file = process.argv[1]; fs.writeFileSync(file, fs.readFileSync(file, 'utf8') + 'A')",
               "$FILE",
@@ -221,7 +221,7 @@ describe("Format", () => {
           },
           second: {
             command: [
-              "node",
+              process.execPath,
               "-e",
               "const fs = require('fs'); const file = process.argv[1]; fs.writeFileSync(file, fs.readFileSync(file, 'utf8') + 'B')",
               "$FILE",
diff --git a/packages/opencode/test/installation/install-script.test.ts b/packages/opencode/test/installation/install-script.test.ts
index 0acae84..652a479 100644
--- a/packages/opencode/test/installation/install-script.test.ts
+++ b/packages/opencode/test/installation/install-script.test.ts
@@ -216,13 +216,13 @@ function count(text: string, needle: string) {
 
 // #292：Windows 下将 install 脚本复制到临时位置并归一化 CRLF，供 WSL bash 执行
 async function installScriptForBash(env: Record<string, string | undefined>) {
-  if (process.platform !== "win32") return INSTALL_SCRIPT
+  const source = await Bun.file(INSTALL_SCRIPT).text()
+  if (process.platform !== "win32" && !source.includes("\r\n")) return INSTALL_SCRIPT
   const script = path.join(path.dirname(env.HOME ?? path.dirname(INSTALL_SCRIPT)), "install")
   await fs.mkdir(path.dirname(script), { recursive: true })
-  // WSL bash executes the same script bytes as a POSIX shell would see from curl.
-  // On Windows checkouts git may materialize CRLF line endings, which turns
-  // `pipefail` into `pipefail\r`; normalize only the temporary test copy.
-  await Bun.write(script, (await Bun.file(INSTALL_SCRIPT).text()).replaceAll("\r\n", "\n"))
+  // WSL may run these Linux tests from a Windows checkout where git materialized
+  // CRLF line endings. Bash then sees `pipefail\r`; normalize only the temp copy.
+  await Bun.write(script, source.replaceAll("\r\n", "\n"))
   await fs.chmod(script, 0o755)
   return script
 }
diff --git a/packages/sdk/js/script/build.ts b/packages/sdk/js/script/build.ts
index 79e0879..fbd3d8b 100644
--- a/packages/sdk/js/script/build.ts
+++ b/packages/sdk/js/script/build.ts
@@ -115,5 +115,6 @@ await Bun.write(sseTypesPath, sseTypesPatched)
 await $`bun prettier --write src/gen`
 await $`bun prettier --write src/v2`
 await $`rm -rf dist`
-await $`bun tsc`
+// TypeScript's CLI is a Node entrypoint; running it through Bun can leave the Windows CI SDK build alive.
+await $`node ${Bun.resolveSync("typescript/bin/tsc", dir)}`
 await $`rm openapi.json`
diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
index cd29a84..0112e00 100644
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -2033,7 +2033,7 @@ function ToolPart(props: { last: boolean; part: ToolPart; message: AssistantMess
   return (
     <Show when={!shouldHide()}>
       {/* #214：ToolAutoReview.Provider 提供 autoReview metadata——InlineTool/BlockTool 从 context 渲染共享审查状态行 */}
-      <ToolAutoReview.Provider value={() => autoReviewMetadata(toolprops.metadata)}>
+      <ToolAutoReview.Provider value={() => autoReviewMetadata(toolprops.metadata, props.part.tool)}>
         <Switch>
           <Match when={display() === "bash"}>
             <Shell {...toolprops} />
@@ -2220,6 +2220,7 @@ function GenericTool(props: ToolProps) {
 const ToolAutoReview = createContext<() => AutoReviewMetadata | undefined>(() => undefined)
 type AutoReviewMetadata = {
   reviewID: string
+  tool: string
   sessionID?: string
   // #216：aborted 状态——工具执行被取消时审查终止，与 reviewing/allowed/denied 等终态并列
   status?: "reviewing" | "allowed" | "denied" | "timed_out" | "failed" | "fallback_user" | "aborted"
@@ -2228,14 +2229,15 @@ type AutoReviewMetadata = {
 }
 
 // #209：从工具 metadata 提取 autoReview——非对象或缺少 reviewID 返回 undefined
-export function autoReviewMetadata(metadata: Record<string, unknown>): AutoReviewMetadata | undefined {
+export function autoReviewMetadata(metadata: Record<string, unknown>, tool: string): AutoReviewMetadata | undefined {
   const value = metadata.autoReview
   // #209：非对象或数组直接拒绝——防止 string/number 等 metadata 被强转为 review 结构
   if (!value || typeof value !== "object" || Array.isArray(value)) return
   const review = value as Record<string, unknown>
   // #209：reviewID 是 reviewer 写入的必填锚点——缺少则非有效 auto-review 注解，避免渲染空壳状态行
   if (typeof review.reviewID !== "string") return
-  return review as AutoReviewMetadata
+  // #296：tool 由调用方从 part.tool 注入，供父卡片兜底区分 shell 与 inline 工具坐标语义
+  return { ...(review as AutoReviewMetadata), tool }
 }
 
 // #209：auto-review 状态标签——两行合约：命令 + 一行紧凑审查状态
@@ -2294,7 +2296,13 @@ function AutoReviewLine(props: { review: AutoReviewMetadata }) {
       onMouseOver={() => clickable() && setHover(true)}
       onMouseOut={() => setHover(false)}
     >
-      <text selectable={false} fg={hover() ? theme.text : autoReviewColor(props.review, theme)} wrapMode="none" onMouseUp={openReview}>
+      <text
+        id={autoReviewLabelID(props.review)}
+        selectable={false}
+        fg={hover() ? theme.text : autoReviewColor(props.review, theme)}
+        wrapMode="none"
+        onMouseUp={openReview}
+      >
         {autoReviewLabel(props.review)}
       </text>
     </box>
@@ -2307,14 +2315,21 @@ function autoReviewLineID(review: AutoReviewMetadata) {
   return `auto-review-${review.reviewID}`
 }
 
+function autoReviewLabelID(review: AutoReviewMetadata) {
+  // OpenTUI 在不同平台可能把 mouse target 设为 text 内部 renderable，而不是
+  // 外层 row box；label id 与 row id 共用 reviewID，仍只服务本地 hit-test。
+  return `${autoReviewLineID(review)}-label`
+}
+
 // #293：通过 id 在事件 target 的 renderable 子树中查找真实的 review row，
 // 供 openAutoReviewFromToolChrome 做精确命中判断，避免退回 root+1 的布局猜测。
 function autoReviewLineTarget(review: AutoReviewMetadata, target: Renderable | null | undefined) {
   const id = autoReviewLineID(review)
+  const label = autoReviewLabelID(review)
   for (let item = target; item; item = item.parent) {
-    if (item.id === id) return item
+    if (item.id === id || item.id === label) return { row: item, direct: true }
     const child = item.findDescendantById(id)
-    if (child) return child
+    if (child) return { row: child, direct: false }
   }
 }
 
@@ -2333,8 +2348,9 @@ function openAutoReviewSession(
   // 继续冒泡到父 BlockTool/InlineTool 后触发展开、折叠或工具专属点击。
   evt?.preventDefault()
   evt?.stopPropagation()
-  // #209：用户选中文本时不导航——避免复制审查结论时误触发跳转
-  if (renderer.getSelection()?.getSelectedText()) return true
+  // review 行不是 transcript 正文，并且内部 text 已关闭 selection。前序 TUI
+  // 测试或真实用户留下的旧 selection 不能让这次明确点击只消费事件不导航。
+  renderer.clearSelection()
   navigate({ type: "session", sessionID: review.sessionID })
   return true
 }
@@ -2347,14 +2363,19 @@ function openAutoReviewFromToolChrome(
   evt: TuiMouseEvent | undefined,
 ) {
   // #293：OpenTUI 可能把 review 文本行的 mouseup 冒泡给父工具卡。这里不再用
-  // root + 1 这类布局猜测，因为 BlockTool 的相邻行可能是标题、body 或
-  // expand affordance；只接受事件命中的 renderable 子树里真实存在的 review row。
+  // root + 1 这类布局猜测；父卡片兜底必须再经过行坐标校验，因为标题、
+  // 命令行、body 或 expand affordance 与 review row 是兄弟区域，不能共享导航语义。
   if (!review?.sessionID || !evt?.target) return false
-  const row = autoReviewLineTarget(review, evt.target)
+  const match = autoReviewLineTarget(review, evt.target)
   // #293：mockMouse 和真实终端鼠标事件传入的是 char frame 的 0-based y；OpenTUI
-  // renderable.screenY 在当前布局树中是对应行的 1-based 屏幕坐标。先用 id
-  // 找到真实 review row，再做这个坐标归一化，避免退回 root+1 的布局猜测。
-  if (!row || evt.y !== row.screenY - 1) return false
+  // renderable.screenY 在当前布局树中是对应行的 1-based 屏幕坐标。direct 命中
+  // 说明事件 target 已在 review row 内，沿用归一化坐标。shell 工具的命令行
+  // 和 review row 紧邻，父卡片兜底必须只接受真正 status 行；非 shell inline
+  // 工具在部分 OpenTUI hit-grid 状态下会把 review 文本报告成上一行内部 text。
+  if (!match) return false
+  if (match.direct) {
+    if (evt.y !== match.row.screenY - 1) return false
+  } else if (evt.y !== match.row.screenY && (review.tool === "bash" || evt.y !== match.row.screenY - 1)) return false
   return openAutoReviewSession(review, navigate, renderer, evt)
 }
 
diff --git a/packages/tui/test/routes/session/auto-review-render.test.ts b/packages/tui/test/routes/session/auto-review-render.test.ts
index 0ec89b8..00886ac 100644
--- a/packages/tui/test/routes/session/auto-review-render.test.ts
+++ b/packages/tui/test/routes/session/auto-review-render.test.ts
@@ -6,15 +6,15 @@ describe("auto review rendering (#209)", () => {
   test("autoReviewMetadata returns undefined for missing autoReview", () => {
     // #209：无 autoReview 字段时返回 undefined
     // #209：覆盖 null/string/array 四种非法类型——确保 type guard 不被 falsy 或非对象值欺骗
-    expect(autoReviewMetadata({})).toBeUndefined()
-    expect(autoReviewMetadata({ autoReview: null })).toBeUndefined()
-    expect(autoReviewMetadata({ autoReview: "string" })).toBeUndefined()
-    expect(autoReviewMetadata({ autoReview: [] })).toBeUndefined()
+    expect(autoReviewMetadata({}, "bash")).toBeUndefined()
+    expect(autoReviewMetadata({ autoReview: null }, "bash")).toBeUndefined()
+    expect(autoReviewMetadata({ autoReview: "string" }, "bash")).toBeUndefined()
+    expect(autoReviewMetadata({ autoReview: [] }, "bash")).toBeUndefined()
   })
 
   test("autoReviewMetadata returns undefined when reviewID is missing", () => {
     // #209：缺少 reviewID 字段时返回 undefined——防止部分写入的 metadata 被误用
-    expect(autoReviewMetadata({ autoReview: { status: "reviewing" } })).toBeUndefined()
+    expect(autoReviewMetadata({ autoReview: { status: "reviewing" } }, "bash")).toBeUndefined()
   })
 
   test("autoReviewMetadata extracts valid autoReview metadata", () => {
@@ -29,9 +29,11 @@ describe("auto review rendering (#209)", () => {
         result: { risk_level: "high", user_authorization: "high", rationale: "authorized" },
       },
     }
-    const review = autoReviewMetadata(metadata)
+    const review = autoReviewMetadata(metadata, "edit")
     expect(review).toBeDefined()
     expect(review?.reviewID).toBe("review_1")
+    // #296：tool 参数由调用方注入并透传，供父卡片兜底区分 shell 与 inline 工具坐标语义
+    expect(review?.tool).toBe("edit")
     expect(review?.sessionID).toBe("ses_reviewer")
     expect(review?.status).toBe("allowed")
     expect(review?.precheck?.level).toBe("cautious")
@@ -41,6 +43,7 @@ describe("auto review rendering (#209)", () => {
   test("autoReviewLabel shows reviewing state with precheck level", () => {
     // #209：reviewing 状态展示 ◌ + precheck level + @permission-reviewer
     const label = autoReviewLabel({
+      tool: "bash",
       reviewID: "review_2",
       status: "reviewing",
       precheck: { level: "cautious", reason: "sensitive read" },
@@ -51,6 +54,7 @@ describe("auto review rendering (#209)", () => {
   test("autoReviewLabel shows allowed state with auth level", () => {
     // #209：allowed 状态展示 ✓ + auth level + @permission-reviewer
     const label = autoReviewLabel({
+      tool: "bash",
       reviewID: "review_3",
       status: "allowed",
       result: { risk_level: "high", user_authorization: "high", rationale: "authorized" },
@@ -61,6 +65,7 @@ describe("auto review rendering (#209)", () => {
   test("autoReviewLabel shows denied state with risk and auth", () => {
     // #209：denied 状态展示 ! + risk + auth + @permission-reviewer
     const label = autoReviewLabel({
+      tool: "bash",
       reviewID: "review_4",
       status: "denied",
       result: { risk_level: "high", user_authorization: "unknown", rationale: "not authorized" },
@@ -70,25 +75,25 @@ describe("auto review rendering (#209)", () => {
 
   test("autoReviewLabel shows timed_out state as failed closed", () => {
     // #209：timed_out 状态展示 ! + timed out + failed closed
-    const label = autoReviewLabel({ reviewID: "review_5", status: "timed_out" })
+    const label = autoReviewLabel({ reviewID: "review_5", tool: "bash", status: "timed_out" })
     expect(label).toBe("! auto review · timed out · failed closed · @permission-reviewer")
   })
 
   test("autoReviewLabel shows failed state as failed closed", () => {
     // #209：failed 状态展示 ! + failed + failed closed
-    const label = autoReviewLabel({ reviewID: "review_6", status: "failed" })
+    const label = autoReviewLabel({ reviewID: "review_6", tool: "bash", status: "failed" })
     expect(label).toBe("! auto review · failed · failed closed · @permission-reviewer")
   })
 
   test("autoReviewLabel shows fallback_user state as asking user", () => {
     // #209：fallback_user 状态展示 ! + unavailable + asking user
-    const label = autoReviewLabel({ reviewID: "review_7", status: "fallback_user" })
+    const label = autoReviewLabel({ reviewID: "review_7", tool: "bash", status: "fallback_user" })
     expect(label).toBe("! auto review · unavailable · asking user · @permission-reviewer")
   })
 
   test("autoReviewLabel shows aborted state", () => {
     // #216：aborted 状态展示 ! + aborted + @permission-reviewer——工具执行被取消时的审查终态
-    const label = autoReviewLabel({ reviewID: "review_9", status: "aborted" })
+    const label = autoReviewLabel({ reviewID: "review_9", tool: "bash", status: "aborted" })
     expect(label).toBe("! auto review · aborted · @permission-reviewer")
   })
 
@@ -96,6 +101,7 @@ describe("auto review rendering (#209)", () => {
     // #209：无 status 时默认展示 reviewing + precheck level
     // #209：默认 reviewing 是安全假设——metadata 写入与状态更新非原子，读取时可能尚未标记终态
     const label = autoReviewLabel({
+      tool: "bash",
       reviewID: "review_8",
       precheck: { level: "general", reason: "unknown" },
     })
