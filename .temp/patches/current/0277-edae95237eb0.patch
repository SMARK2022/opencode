From edae95237eb0eea6a997f50d9eb3aca26db36da1 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Sun, 31 May 2026 16:06:57 +0800
Subject: [PATCH] =?UTF-8?q?fix(image):=20=E7=BB=9F=E4=B8=80=E5=9B=BE?=
 =?UTF-8?q?=E7=89=87=E9=99=84=E4=BB=B6=E5=8E=8B=E7=BC=A9=E8=BE=B9=E7=95=8C?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

将 read 工具的图片附件处理收敛到 Image.Service，并按调用方 token budget 收紧 base64 上限，避免默认尺寸内的大图绕过模型预算。

保留 resizer 不可用时 read 附件回退，但损坏图片仍返回解码错误；同步测试 registry/session 依赖与图片处理草案，降低工具链回归风险。

diff --git a/docs/draft/image-processing-unification-plan.md b/docs/draft/image-processing-unification-plan.md
index 4525a39689d432e6b21c1160e8c6a87d53820228..6f3270e70ead909d59d7405283cefdbcf99790f9 100644
--- a/docs/draft/image-processing-unification-plan.md
+++ b/docs/draft/image-processing-unification-plan.md
@@ -69,7 +69,7 @@ sharp metadata fast path
         ↓
 需要处理时 sharp resize/compress
         ↓
-photon fallback 或 typed error
+typed error；read 工具仅在 resizer 不可用时兼容返回原始图片
 ```
 
 实现原则：
warning: in the working copy of 'packages/opencode/src/image/image.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'packages/opencode/test/tool/read.test.ts', LF will be replaced by CRLF the next time Git touches it
diff --git a/packages/opencode/src/image/image.ts b/packages/opencode/src/image/image.ts
index 744bd3fc9..58f0a39ba 100644
--- a/packages/opencode/src/image/image.ts
+++ b/packages/opencode/src/image/image.ts
@@ -12,6 +12,8 @@ const MAX_WIDTH = 2000
 const MAX_HEIGHT = 2000
 const AUTO_RESIZE = true
 const JPEG_QUALITIES = [80, 85, 70, 55, 40]
+// #277：token 估算以 base64 字符为单位，常数必须与 token/estimate.ts 的图片估算保持一致
+const TOKEN_ESTIMATE_BASE64_CHARS = 750
 export class ResizerUnavailableError extends Schema.TaggedErrorClass<ResizerUnavailableError>()(
   "ImageResizerUnavailableError",
   {},
@@ -51,7 +53,13 @@ export class SizeError extends Schema.TaggedErrorClass<SizeError>()("ImageSizeEr
 export type Error = ResizerUnavailableError | InvalidDataUrlError | DecodeError | SizeError
 
 export interface Interface {
-  readonly normalize: (input: SessionV1.FilePart) => Effect.Effect<SessionV1.FilePart, Error>
+  readonly normalize: (
+    input: SessionV1.FilePart,
+    // #277：调用方可传入更小的 token budget，在不放宽配置上限的前提下收紧单图 base64 上限
+    options?: {
+      readonly tokenBudget?: number
+    },
+  ) => Effect.Effect<SessionV1.FilePart, Error>
 }
 
 export class Service extends Context.Service<Service, Interface>()("@opencode/Image") {}
@@ -72,13 +80,19 @@ const layer = Layer.effect(
       ),
     )
 
-    const normalize = Effect.fn("Image.normalize")(function* (input: SessionV1.FilePart) {
+    const normalize = Effect.fn("Image.normalize")(function* (
+      input: SessionV1.FilePart,
+      options?: {
+        readonly tokenBudget?: number
+      },
+    ) {
       const image = (yield* config.get()).attachment?.image
       const info = {
         autoResize: image?.auto_resize ?? AUTO_RESIZE,
         maxWidth: image?.max_width ?? MAX_WIDTH,
         maxHeight: image?.max_height ?? MAX_HEIGHT,
-        maxBase64Bytes: image?.max_base64_bytes ?? MAX_BASE64_BYTES,
+        // #277：默认尺寸内的大图在调用方收紧预算后仍会进入压缩，避免绕过模型 token 上限
+        maxBase64Bytes: targetMaxBase64Bytes(image?.max_base64_bytes ?? MAX_BASE64_BYTES, options),
       }
       if (!input.url.startsWith("data:") || !input.url.includes(";base64,"))
         return yield* new InvalidDataUrlError({ url: input.url })
@@ -169,4 +183,15 @@ const layer = Layer.effect(
 
 export const node = LayerNode.make({ service: Service, layer: layer, deps: [Config.node] })
 
+// #277：配置上限与调用方预算取小值，budget 未提供时保持既有配置语义
+function targetMaxBase64Bytes(
+  configuredMax: number,
+  options?: {
+    readonly tokenBudget?: number
+  },
+) {
+  if (options?.tokenBudget === undefined) return configuredMax
+  return Math.min(configuredMax, Math.max(0, Math.floor(options.tokenBudget * TOKEN_ESTIMATE_BASE64_CHARS)))
+}
+
 export * as Image from "./image"
diff --git a/packages/opencode/src/tool/read.ts b/packages/opencode/src/tool/read.ts
index cbd9ce886..67300aa15 100644
--- a/packages/opencode/src/tool/read.ts
+++ b/packages/opencode/src/tool/read.ts
@@ -10,7 +10,10 @@ import { readOutline, type Outline } from "./read-outline"
 import { InstanceState } from "@/effect/instance-state"
 import { assertExternalDirectoryEffect } from "./external-directory"
 import { Instruction } from "../session/instruction"
-import { isPdfAttachment, sniffAttachmentMime, processImageWithTokenBudget, formatSize } from "@/util/media"
+import { isPdfAttachment, sniffAttachmentMime, formatSize } from "@/util/media"
+// #277：read 的图片附件处理收敛到 Image.Service，替换 read 独占的 processImageWithTokenBudget
+import { Image } from "@/image/image"
+import { PartID } from "@/session/schema"
 import { SessionV1 } from "@opencode-ai/core/v1/session"
 
 // 默认 400 行：与 notebook source limit 对齐，压低单次工具输出
@@ -418,13 +421,15 @@ type Metadata = {
 export const ReadTool = Tool.define<
   typeof Parameters,
   Metadata,
-  FSUtil.Service | Instruction.Service | LSP.Service | Scope.Scope
+  FSUtil.Service | Instruction.Service | LSP.Service | Image.Service | Scope.Scope
 >(
   "read",
   Effect.gen(function* () {
     const fs = yield* FSUtil.Service
     const instruction = yield* Instruction.Service
     const lsp = yield* LSP.Service
+    // #277：Image.Service 承担图片附件的尺寸与 token 预算边界
+    const image = yield* Image.Service
     const scope = yield* Scope.Scope
 
     const miss = Effect.fn("ReadTool.miss")(function* (filepath: string) {
@@ -721,11 +726,30 @@ export const ReadTool = Tool.define<
       if (isImage || isPdfAttachment(mime)) {
         const bytes = yield* fs.readFile(filepath)
 
-        // 图片使用三级压缩策略
-        // 先尝试 processImageWithTokenBudget（基于 sharp 的多级压缩），失败则回退到原始 base64
+        // #277：图片走 Image.Service，read、粘贴与 tool-result 共用同一套尺寸与 token 预算边界
         if (isImage) {
-          const processed = yield* Effect.promise(() => processImageWithTokenBudget(bytes, mime))
-          const msg = `Image read successfully (${formatSize(processed.originalSize)} → compressed for model)`
+          const input = {
+            id: PartID.ascending(),
+            messageID: ctx.messageID,
+            sessionID: ctx.sessionID,
+            type: "file" as const,
+            mime,
+            url: `data:${mime};base64,${Buffer.from(bytes).toString("base64")}`,
+          }
+          const attachment = yield* image
+            .normalize(
+              input,
+              // #277：read 结果立即进入模型上下文，预算对齐既有图片 token 上限
+              { tokenBudget: 1600 },
+            )
+            .pipe(
+              // #277：resizer 不可用时保留原始图片字节，维持既有 read 行为而不是让读取失败
+              Effect.catchIf(
+                (error) => error instanceof Image.ResizerUnavailableError,
+                () => Effect.succeed(input),
+              ),
+            )
+          const msg = `Image read successfully (${formatSize(bytes.length)} → compressed for model)`
           return {
             title,
             output: msg,
@@ -737,8 +761,8 @@ export const ReadTool = Tool.define<
             attachments: [
               {
                 type: "file" as const,
-                mime: processed.mime,
-                url: `data:${processed.mime};base64,${processed.data}`,
+                mime: attachment.mime,
+                url: attachment.url,
               },
             ],
           }
diff --git a/packages/opencode/src/tool/registry.ts b/packages/opencode/src/tool/registry.ts
index 436762d1c..c1139f110 100644
--- a/packages/opencode/src/tool/registry.ts
+++ b/packages/opencode/src/tool/registry.ts
@@ -55,6 +55,8 @@ import { ModelV2 } from "@opencode-ai/core/model"
 import { MCP } from "@/mcp"
 import { PermissionV1 } from "@opencode-ai/core/v1/permission"
 import { McpCatalog } from "@/mcp/catalog"
+// #277：read 工具的图片附件处理收敛到 Image.Service，registry 需要装配该依赖
+import { Image } from "@/image/image"
 
 export function webSearchEnabled(providerID: ProviderV2.ID, flags = { exa: false, parallel: false }) {
   return providerID === ProviderV2.ID.opencode || flags.exa || flags.parallel
@@ -444,6 +446,8 @@ export const node = LayerNode.make({
     Skill.node,
     Session.node,
     BackgroundJob.node,
+    // #277：Image.node 供 ReadTool 归一化图片附件；LayerNode 会传递解析其 Config 依赖
+    Image.node,
     Provider.node,
     LSP.node,
     Instruction.node,
diff --git a/packages/opencode/src/util/media.ts b/packages/opencode/src/util/media.ts
index 7f0c03380..863190c1d 100644
--- a/packages/opencode/src/util/media.ts
+++ b/packages/opencode/src/util/media.ts
@@ -25,43 +25,6 @@ export function sniffAttachmentMime(bytes: Uint8Array, fallback: string) {
   return fallback
 }
 
-// 图片 token 预算 - 约等于 1024x768 图片的 token 数，超过此值触发压缩
-const IMAGE_TOKEN_BUDGET = 1600
-
-// 图片处理结果 - 包含压缩后的 base64 数据、MIME 类型和原始大小
-export interface ProcessedImage {
-  data: string
-  mime: string
-  originalSize: number
-}
-
-// 动态导入 sharp（可选依赖） - 哨兵值模式避免无限重试
-// 使用 Symbol 哨兵区分"尚未尝试"和"已确认不可用"，避免每次调用都重新 import
-// sharp 是可选依赖：安装后启用三级压缩，未安装时回退到原始 base64
-const SHARP_UNAVAILABLE = Symbol("SHARP_UNAVAILABLE")
-let sharpCached: typeof import("sharp") | typeof SHARP_UNAVAILABLE | null = null
-
-async function getSharp(): Promise<typeof import("sharp") | null> {
-  // 哨兵值模式：失败后标记不可用，避免每次调用都重新 import
-  if (sharpCached === SHARP_UNAVAILABLE) return null  // 失败后不重试
-  if (sharpCached !== null) return sharpCached as typeof import("sharp")
-  try {
-    const mod = await import("sharp")
-    // 处理 ESM/CJS 互操作
-    // sharp 模块类型断言：确保返回的是 sharp 模块本身而非模块命名空间
-    sharpCached = ((mod as any).default ?? mod) as typeof import("sharp")
-    return sharpCached as typeof import("sharp")
-  } catch {
-    sharpCached = SHARP_UNAVAILABLE  // 标记为不可用
-    return null
-  }
-}
-
-// 估算图片 token：base64 编码后约 4/3 倍原始字节，750 token 约等于一张图
-function estimateImageTokens(byteLength: number): number {
-  return Math.ceil((byteLength * 4) / 3 / 750)
-}
-
 // 格式化文件大小 - 用于压缩结果展示
 function formatSize(bytes: number): string {
   if (bytes < 1024) return `${bytes} B`
@@ -69,95 +32,5 @@ function formatSize(bytes: number): string {
   return `${(bytes / (1024 * 1024)).toFixed(1)} MB`
 }
 
-// 三级图片压缩策略
-// Level 0: 无需压缩; Level 1: 尺寸缩放; Level 2: 质量递减; Level 3: 极限兜底
-export async function processImageWithTokenBudget(
-  bytes: Uint8Array,
-  mime: string,
-  maxTokens: number = IMAGE_TOKEN_BUDGET,
-): Promise<ProcessedImage> {
-  const originalSize = bytes.length
-  const buffer = Buffer.from(bytes)
-
-  // Level 0: 估算无需压缩的情况
-  const estimatedTokens = estimateImageTokens(buffer.length)
-  if (estimatedTokens <= maxTokens) {
-    return {
-      data: buffer.toString("base64"),
-      mime,
-      originalSize,
-    }
-  }
-
-  // 尝试使用 sharp 进行压缩
-  const sharp = await getSharp()
-  if (!sharp) {
-    // 无 sharp 时直接返回原始数据
-    return {
-      data: buffer.toString("base64"),
-      mime,
-      originalSize,
-    }
-  }
-
-  try {
-    // 读取图片元数据，计算缩放比例
-    const meta = await sharp(buffer).metadata()
-    const maxDim = 1568
-    const scale = Math.min(
-      maxDim / (meta.width ?? maxDim),
-      maxDim / (meta.height ?? maxDim),
-      1,
-    )
-
-    // Level 1: 标准尺寸缩放（最长边 1568px，符合 Claude vision 最优分辨率）
-    if (scale < 1) {
-      const resized = await sharp(buffer)
-        .resize(Math.floor((meta.width ?? maxDim) * scale), Math.floor((meta.height ?? maxDim) * scale), {
-          fit: "inside",
-          withoutEnlargement: true,
-        })
-        .jpeg({ quality: 85 })
-        .toBuffer()
-
-      const l1Tokens = estimateImageTokens(resized.length)
-      if (l1Tokens <= maxTokens) {
-        return { data: resized.toString("base64"), mime: "image/jpeg", originalSize }
-      }
-    }
-
-    // Level 2: 质量递减压缩（无论是否缩放过都尝试）
-    // 逐级降低分辨率和 JPEG 质量，直到满足 token 预算
-    for (const [width, quality] of [
-      [800, 60],
-      [800, 40],
-      [600, 20],
-    ] as [number, number][]) {
-      const compressed = await sharp(buffer)
-        .resize(width, width, { fit: "inside", withoutEnlargement: true })
-        .jpeg({ quality })
-        .toBuffer()
-      if (estimateImageTokens(compressed.length) <= maxTokens) {
-        return { data: compressed.toString("base64"), mime: "image/jpeg", originalSize }
-      }
-    }
-
-    // Level 3: 极限兜底压缩（只有前两级都超出预算才到这里）
-    // 强制缩放到 400x400 以下，质量降到 20
-    const fallback = await sharp(buffer)
-      .resize(400, 400, { fit: "inside", withoutEnlargement: true })
-      .jpeg({ quality: 20 })
-      .toBuffer()
-
-    return { data: fallback.toString("base64"), mime: "image/jpeg", originalSize }
-  } catch {
-    // 压缩失败时返回原始数据，不阻断读取流程
-    return {
-      data: buffer.toString("base64"),
-      mime,
-      originalSize,
-    }
-  }
-}
-
+// #277：read 的图片压缩职责已收敛到 Image.Service，此处只保留纯工具函数
 export { formatSize }
diff --git a/packages/opencode/test/tool/read.test.ts b/packages/opencode/test/tool/read.test.ts
index fd08941a7..b2b848264 100644
--- a/packages/opencode/test/tool/read.test.ts
+++ b/packages/opencode/test/tool/read.test.ts
@@ -47,7 +47,7 @@ const ctx = {
   ask: () => Effect.void,
 }
 
-const readLayer = (flags: Partial<RuntimeFlags.Info> = {}) =>
+const readLayer = (flags: Partial<RuntimeFlags.Info> = {}, imageNode = Image.node) =>
   LayerNode.compile(
     LayerNode.group([
       Agent.node,
@@ -57,13 +57,26 @@ const readLayer = (flags: Partial<RuntimeFlags.Info> = {}) =>
       LSP.node,
       Ripgrep.node,
       Truncate.node,
-      // #276：read 工具需 Image.Service 处理图片附件
-      Image.node,
+      // #277：imageNode 参数供 noResizer 变体注入始终失败的 Image.Service，默认仍是真实 Image.node
+      imageNode,
     ]),
   )
 
 const it = testEffect(Layer.mergeAll(readLayer(), testInstanceStoreLayer))
 
+// #277：stub 与 Image.node 同名同 service，LayerNode 编译时可安全替换；依赖为空因 normalize 立即失败
+const noResizerImageNode = LayerNode.make({
+  service: Image.Service,
+  layer: Layer.succeed(
+    Image.Service,
+    Image.Service.of({
+      normalize: () => Effect.fail(new Image.ResizerUnavailableError()),
+    }),
+  ),
+  deps: [],
+})
+const noResizer = testEffect(Layer.mergeAll(readLayer({}, noResizerImageNode), testInstanceStoreLayer))
+
 const init = Effect.fn("ReadToolTest.init")(function* () {
   const info = yield* ReadTool
   return yield* info.init()
@@ -519,9 +532,18 @@ describe("tool.read truncation", () => {
   it.live("detects attachment media from file contents", () =>
     Effect.gen(function* () {
       const dir = yield* tmpdirScoped()
-      // #276：使用可解码的有效 JPEG base64 替代裸字节头部
-      const jpeg = Buffer.from(
-        "",
-        "base64",
-      )
+      // #276/#277：运行时用 photon 生成 8x8 JPEG，保证 Image.Service 的光子解码器可解码
+      const photon = yield* Effect.promise(() => import("@silvia-odwyer/photon-node"))
+      const source = new photon.PhotonImage(
+        new Uint8Array(
+          Array.from(
+            { length: 8 * 8 * 4 },
+            (_, index) => (index % 4 === 3 ? 255 : ((index >> 2) * (index % 4 === 0 ? 1 : index % 4 === 1 ? 7 : 13)) % 251),
+          ),
+        ),
+        8,
+        8,
+      )
+      const jpeg = Buffer.from(source.get_bytes_jpeg(90))
+      source.free()
       yield* put(path.join(dir, "image.bin"), jpeg)
@@ -548,13 +570,40 @@ describe("tool.read truncation", () => {
   it.live("large image files are properly attached without error", () =>
     Effect.gen(function* () {
       const result = yield* exec(FIXTURES_DIR, { filePath: path.join(FIXTURES_DIR, "large-image.png") })
+      const attachment = result.attachments?.[0]
       expect(result.metadata.truncated).toBe(false)
       expect(result.attachments).toBeDefined()
       expect(result.attachments?.length).toBe(1)
-      expect(result.attachments?.[0].type).toBe("file")
-      expect(result.attachments?.[0]).not.toHaveProperty("id")
-      expect(result.attachments?.[0]).not.toHaveProperty("sessionID")
-      expect(result.attachments?.[0]).not.toHaveProperty("messageID")
+      expect(attachment?.type).toBe("file")
+      // #277：read 以 tokenBudget 1_600 收紧，附件 base64 长度不得超过 1_600 * 750 字符
+      if (!attachment) return
+      const base64 = attachment.url.slice(attachment.url.indexOf(";base64,") + ";base64,".length)
+      // #277：base64 长度直接反映 payload 上限，锁定它即锁定调用方 token 预算不被大图绕过
+      expect(base64.length).toBeLessThanOrEqual(1_600 * 750)
+      expect(attachment).not.toHaveProperty("id")
+      expect(attachment).not.toHaveProperty("sessionID")
+      expect(attachment).not.toHaveProperty("messageID")
+    }),
+  )
+
+  // #277：resizer 不可用时回退为附加原始图片字节，保持 read 的可用性而不是失败
+  noResizer.live("falls back to original image bytes when the image resizer is unavailable", () =>
+    Effect.gen(function* () {
+      const dir = yield* tmpdirScoped()
+      const png = Buffer.from(
+        "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8DwHwAFBQIAX8jx0gAAAABJRU5ErkJggg==",
+        "base64",
+      )
+      yield* put(path.join(dir, "image.png"), png)
+
+      const result = yield* exec(dir, { filePath: path.join(dir, "image.png") })
+      // #277：stub 使 normalize 必然失败，此时附件必须逐字节等于原始输入而非压缩产物
+      expect(result.output).toStartWith("Image read successfully")
+      expect(result.attachments?.[0]).toMatchObject({
+        type: "file",
+        mime: "image/png",
+        url: `data:image/png;base64,${png.toString("base64")}`,
+      })
     }),
   )
 
