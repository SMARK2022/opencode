From f4304858229cc5be8cf2729cd6ef4dc1b97e57b0 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Thu, 7 May 2026 02:50:34 +0800
Subject: [PATCH] =?UTF-8?q?feat(session):=20=E6=B7=BB=E5=8A=A0=E9=9A=90?=
 =?UTF-8?q?=E8=97=8F=E6=B6=88=E6=81=AF=E5=A4=84=E7=90=86=E9=80=BB=E8=BE=91?=
 =?UTF-8?q?=EF=BC=8C=E6=94=AF=E6=8C=81=E6=92=A4=E9=94=80=E6=93=8D=E4=BD=9C?=

diff --git a/packages/app/src/context/global-sync/event-reducer.ts b/packages/app/src/context/global-sync/event-reducer.ts
index 15d125df39..7e44477132 100644
--- a/packages/app/src/context/global-sync/event-reducer.ts
+++ b/packages/app/src/context/global-sync/event-reducer.ts
@@ -204,6 +204,21 @@ export function applyDirectoryEvent(input: {
       break
     }
     case "message.updated": {
+      // #98：消息被软隐藏时从 UI store 移除
+      const _info0 = clean((event.properties as { info: Message }).info)
+      if ((_info0 as unknown as { hidden?: unknown }).hidden) {
+        input.setStore(
+          produce((draft) => {
+            const messages = draft.message[_info0.sessionID]
+            if (messages) {
+              const result = Binary.search(messages, _info0.id, (m) => m.id)
+              if (result.found) messages.splice(result.index, 1)
+            }
+            delete draft.part[_info0.id]
+          }),
+        )
+        break
+      }
       const info = clean((event.properties as { info: Message }).info)
       const messages = input.store.message[info.sessionID]
       if (!messages) {
@@ -245,6 +260,25 @@ export function applyDirectoryEvent(input: {
       break
     }
     case "message.part.updated": {
+      // #98：part 软隐藏时从 UI store 移除
+      const _part0 = (event.properties as { part: Part }).part
+      if ((_part0 as unknown as { hidden?: unknown }).hidden) {
+        const parts = input.store.part[_part0.messageID]
+        if (!parts) break
+        const foundAt = Binary.search(parts, _part0.id, (p) => p.id)
+        if (!foundAt.found) break
+        input.setStore(
+          produce((draft) => {
+            const list = draft.part[_part0.messageID]
+            if (!list) return
+            const next = Binary.search(list, _part0.id, (p) => p.id)
+            if (!next.found) return
+            list.splice(next.index, 1)
+            if (list.length === 0) delete draft.part[_part0.messageID]
+          }),
+        )
+        break
+      }
       const part = (event.properties as { part: Part }).part
       if (SKIP_PARTS.has(part.type)) break
       input.setStore(
diff --git a/packages/opencode/src/session/message-v2.ts b/packages/opencode/src/session/message-v2.ts
index 1bea9f52c3..2f039a2ea0 100644
--- a/packages/opencode/src/session/message-v2.ts
+++ b/packages/opencode/src/session/message-v2.ts
@@ -43,6 +43,7 @@ interface FetchDecompressionError extends Error {
   path: string
 }
 
+
 export const SYNTHETIC_ATTACHMENT_PROMPT = "Attached media from tool result:"
 export { isMedia }
 
@@ -466,7 +467,9 @@ export const page = Effect.fn("MessageV2.page")(function* (input: {
   }
 })
 
-export function stream(sessionID: SessionID) {
+// #98：stream 默认过滤 hidden；includeHidden 供审计/恢复读取
+export function stream(sessionID: SessionID, opts?: { includeHidden?: boolean }) {
+  // includeHidden 默认 false：正常流不返回被撤销隐藏的消息和 part
   const size = 50
   return Effect.gen(function* () {
     const result = [] as WithParts[]
@@ -480,7 +483,12 @@ export function stream(sessionID: SessionID) {
       if (next.items.length === 0) break
       for (let i = next.items.length - 1; i >= 0; i--) {
         const item = next.items[i]
-        if (item) result.push(item)
+        if (!item) continue
+        // 默认过滤隐藏消息：撤销操作产生的隐藏记录不进入正常流
+        if (!opts?.includeHidden && item.info.hidden) continue
+        // part 级隐藏过滤
+        if (!opts?.includeHidden) item.parts = item.parts.filter((p) => !p.hidden)
+        result.push(item)
       }
       if (!next.more || !next.cursor) break
       before = next.cursor
diff --git a/packages/opencode/src/session/revert.ts b/packages/opencode/src/session/revert.ts
index 79fef2cda9..30719d80ed 100644
--- a/packages/opencode/src/session/revert.ts
+++ b/packages/opencode/src/session/revert.ts
@@ -117,7 +117,12 @@ const layer = Layer.effect(
         remove.push(msg)
       }
       for (const msg of remove) {
-        yield* sessions.removeMessage({ sessionID, messageID: msg.info.id })
+        // #98：软隐藏而非物理删除，保留 DB 行
+        yield* sessions.updateMessage({
+          ...msg.info,
+          // #98：cleanup 写 hidden 而非 MessageRemoved
+          hidden: { time: Date.now(), reason: "undo" },
+        })
       }
       if (session.revert.partID && target) {
         const partID = session.revert.partID
@@ -126,7 +131,11 @@ const layer = Layer.effect(
           const removeParts = target.parts.slice(idx)
           target.parts = target.parts.slice(0, idx)
           for (const part of removeParts) {
-            yield* sessions.removePart({ sessionID, messageID: target.info.id, partID: part.id })
+            // #98：part 级软隐藏
+            yield* sessions.updatePart({
+              ...part,
+              hidden: { time: Date.now(), reason: "undo" },
+            })
           }
         }
       }
diff --git a/packages/opencode/src/session/session.ts b/packages/opencode/src/session/session.ts
index de8c3dc4cb..e1bdddf8b6 100644
--- a/packages/opencode/src/session/session.ts
+++ b/packages/opencode/src/session/session.ts
@@ -831,25 +831,19 @@ const layer: Layer.Layer<
       if (input.limit) {
         return (yield* MessageV2.page({ sessionID: input.sessionID, limit: input.limit }).pipe(
           Effect.provideService(Database.Service, database),
-        )).items
-      }
-
-      const size = 50
-      const result = [] as SessionV1.WithParts[]
-      let before: string | undefined
-      while (true) {
-        const page = yield* MessageV2.page({ sessionID: input.sessionID, limit: size, before }).pipe(
-          Effect.provideService(Database.Service, database),
+        )).items.filter(
+          // 隐藏消息不返回给调用方，但仍在数据库中保留
+          (m) => !m.info.hidden,
         )
-        if (page.items.length === 0) break
-        for (let i = page.items.length - 1; i >= 0; i--) {
-          const item = page.items[i]
-          if (item) result.push(item)
-        }
-        if (!page.more || !page.cursor) break
-        before = page.cursor
       }
-      return result.reverse()
+
+      // 无 limit：走 MessageV2.stream 默认过滤 hidden
+      // #98：无 limit 列表必须过滤 hidden，禁止 page 全量回放
+      const newestFirst = yield* MessageV2.stream(input.sessionID).pipe(
+        Effect.provideService(Database.Service, database),
+      )
+      // stream 为 newest-first；Session.messages 契约为 chronological oldest-first
+      return newestFirst.slice().reverse()
     })
 
     const removeMessage = Effect.fn("Session.removeMessage")(function* (input: {
diff --git a/packages/opencode/test/session/revert-compact.test.ts b/packages/opencode/test/session/revert-compact.test.ts
index d3d7ba7aae..8c54d0e170 100644
--- a/packages/opencode/test/session/revert-compact.test.ts
+++ b/packages/opencode/test/session/revert-compact.test.ts
@@ -19,7 +19,7 @@ import { ModelV2 } from "@opencode-ai/core/model"
 
 const it = testEffect(
   LayerNode.compile(
-    LayerNode.group([Session.node, SessionRevert.node, Snapshot.node, SessionProjector.node, CrossSpawnSpawner.node]),
+    LayerNode.group([Session.node, SessionRevert.node, Snapshot.node, SessionProjector.node, CrossSpawnSpawner.node, MessageV2.node]),
   ),
 )
 
@@ -426,6 +426,44 @@ describe("revert + compact workflow", () => {
     ),
   )
 
+
+  it.live(
+    "cleanup preserves hidden messages in database",
+    provideTmpdirInstance(
+      (dir) =>
+        Effect.gen(function* () {
+          // #98：软隐藏必须 DB 留存；visible 为空且 get 得 hidden.reason=undo
+          const session = yield* Session.Service
+          const revert = yield* SessionRevert.Service
+
+          const info = yield* session.create({})
+          const sid = info.id
+
+          const u1 = yield* user(sid)
+          yield* text(sid, u1.id, "hello")
+          const a1 = yield* assistant(sid, u1.id, dir)
+          yield* text(sid, a1.id, "hi back")
+
+          yield* session.setRevert({
+            sessionID: sid,
+            revert: { messageID: u1.id },
+            summary: { additions: 0, deletions: 0, files: 0 },
+          })
+
+          const state = yield* session.get(sid)
+          yield* revert.cleanup(state)
+
+          const visible = yield* session.messages({ sessionID: sid })
+          expect(visible.length).toBe(0)
+
+          const hidden = yield* MessageV2.get({ sessionID: sid, messageID: u1.id })
+          expect(hidden.info.hidden).toBeDefined()
+          expect(hidden.info.hidden!.reason).toBe("undo")
+        }),
+      { git: true },
+    ),
+  )
+
   it.live(
     "cleanup is a no-op when session has no revert state",
     provideTmpdirInstance(
diff --git a/packages/schema/src/v1/session.ts b/packages/schema/src/v1/session.ts
index 6c0821a9e3..f0eb8df148 100644
--- a/packages/schema/src/v1/session.ts
+++ b/packages/schema/src/v1/session.ts
@@ -1,6 +1,7 @@
 export * as SessionV1 from "./session"
 
 import { Effect, Schema, Types } from "effect"
+
 import { define, inventory } from "../event"
 import { FileDiff } from "../file-diff"
 import { Project } from "../project"
@@ -78,10 +79,19 @@ export const Format = Schema.Union([OutputFormatText, OutputFormatJsonSchema]).a
 })
 export type OutputFormat = Schema.Schema.Type<typeof Format>
 
+// #98：撤销软隐藏元数据
+export const Hidden = Schema.Struct({
+  time: NonNegativeInt,
+  reason: Schema.Literal("undo"),
+})
+export type Hidden = Schema.Schema.Type<typeof Hidden>
+
 const partBase = {
   id: PartID,
   sessionID: SessionID,
   messageID: MessageID,
+  // #98：part 可选 soft-hide
+  hidden: Schema.optional(Hidden),
 }
 
 export const SnapshotPart = Schema.Struct({
@@ -347,6 +357,8 @@ export type ToolPart = Omit<Types.DeepMutable<Schema.Schema.Type<typeof ToolPart
 const messageBase = {
   id: MessageID,
   sessionID: partBase.sessionID,
+  // #98：message 可选 soft-hide
+  hidden: Schema.optional(Hidden),
 }
 
 export const User = Schema.Struct({
diff --git a/packages/tui/src/context/sync.tsx b/packages/tui/src/context/sync.tsx
index d0511c5183..9365a8abdc 100644
--- a/packages/tui/src/context/sync.tsx
+++ b/packages/tui/src/context/sync.tsx
@@ -313,6 +313,24 @@ export const {
         }
 
         case "message.updated": {
+          // #98：隐藏消息从 TUI store 删除
+          const info = event.properties.info
+          if ((info as Record<string, unknown>).hidden) {
+            const messages = store.message[info.sessionID]
+            if (messages) {
+              const result = search(messages, info.id, (m) => m.id)
+              if (result.found) {
+                setStore(
+                  "message",
+                  info.sessionID,
+                  produce((draft) => {
+                    draft.splice(result.index, 1)
+                  }),
+                )
+              }
+            }
+            break
+          }
           touchMessage(event.properties.info.sessionID, event.properties.info.id)
           const messages = store.message[event.properties.info.sessionID]
           if (!messages) {
@@ -368,6 +386,22 @@ export const {
           break
         }
         case "message.part.updated": {
+          // #98：隐藏 part 从 TUI store 删除
+          const part = event.properties.part
+          if ((part as Record<string, unknown>).hidden) {
+            const parts = store.part[part.messageID]
+            if (!parts) break
+            const foundAt = search(parts, part.id, (p) => p.id)
+            if (!foundAt.found) break
+            setStore(
+              "part",
+              part.messageID,
+              produce((draft) => {
+                draft.splice(foundAt.index, 1)
+              }),
+            )
+            break
+          }
           touchPart(event.properties.part.sessionID, event.properties.part.id)
           const parts = store.part[event.properties.part.messageID]
           if (!parts) {
