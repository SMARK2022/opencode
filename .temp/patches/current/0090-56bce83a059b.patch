From 56bce83a059b9cf93c13a048d41a256ed24f0362 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Wed, 6 May 2026 03:29:50 +0800
Subject: [PATCH] =?UTF-8?q?feat(read):=20=E6=9B=B4=E6=96=B0=E9=BB=98?=
 =?UTF-8?q?=E8=AE=A4=E8=AF=BB=E5=8F=96=E8=A1=8C=E6=95=B0=E4=B8=BA400?=
 =?UTF-8?q?=EF=BC=8C=E8=B0=83=E6=95=B4=E8=BE=93=E5=87=BA=E5=AD=97=E8=8A=82?=

diff --git a/packages/core/src/tool/read-filesystem.ts b/packages/core/src/tool/read-filesystem.ts
index 0b9ddb3887..7b5c1f03bd 100644
--- a/packages/core/src/tool/read-filesystem.ts
+++ b/packages/core/src/tool/read-filesystem.ts
@@ -13,8 +13,7 @@ export const MAX_READ_LINES = 1_000
 // V2 read 默认字节上限与 SMARK read MAX_BYTES 对齐 24KB
 export const MAX_READ_BYTES = 24 * 1024
 export const MAX_MEDIA_INGEST_BYTES = 20 * 1024 * 1024
-const MAX_LINE_LENGTH = 2_000
-const MAX_LINE_SUFFIX = `... (line truncated to ${MAX_LINE_LENGTH} chars)`
+// #90：禁止行中 2000 字截断；仅行数+总字节预算分页
 
 export class BinaryFileError extends Schema.TaggedErrorClass<BinaryFileError>()("ReadTool.BinaryFileError", {
   resource: Schema.String,
@@ -250,7 +249,7 @@ export const read = Effect.fn("ReadTool.read")(function* (
           next = line
           return false
         }
-        const text = input.length > MAX_LINE_LENGTH ? input.slice(0, MAX_LINE_LENGTH) + MAX_LINE_SUFFIX : input
+        const text = input // 完整行：不做 2000 字截断
         const size = Buffer.byteLength(text, "utf-8") + (lines.length > 0 ? 1 : 0)
         if (bytes + size > MAX_READ_BYTES) {
           next = line
@@ -267,11 +266,8 @@ export const read = Effect.fn("ReadTool.read")(function* (
           const index = text.indexOf("\n")
           if (index === -1) {
             if (!discard) {
+              // 长行完整缓冲，不在行中丢弃/截断
               pending += text
-              if (pending.length > MAX_LINE_LENGTH) {
-                pending = pending.slice(0, MAX_LINE_LENGTH + 1)
-                discard = true
-              }
             }
             break
           }
diff --git a/packages/opencode/src/tool/read.ts b/packages/opencode/src/tool/read.ts
index 6d1ef54051..c6000c109b 100644
--- a/packages/opencode/src/tool/read.ts
+++ b/packages/opencode/src/tool/read.ts
@@ -11,12 +11,9 @@ import { Instruction } from "../session/instruction"
 import { isPdfAttachment, sniffAttachmentMime, processImageWithTokenBudget, formatSize } from "@/util/media"
 import { SessionV1 } from "@opencode-ai/core/v1/session"
 
-// 默认 read 行数 400：与 notebook source 默认 limit 对齐，压低单次工具输出
-// 默认 400 行：与 notebook source limit 对齐
-// 默认 400 行：与 notebook source limit 对齐，降低单次工具输出
+// 默认 400 行：与 notebook source limit 对齐，压低单次工具输出
 const DEFAULT_READ_LIMIT = 400
-const MAX_LINE_LENGTH = 2000
-const MAX_LINE_SUFFIX = `... (line truncated to ${MAX_LINE_LENGTH} chars)`
+// 不再按行截断：超长行完整保留，仅用 MAX_BYTES 控制总输出
 // 默认最大读取字节 24KB：压低单次工具输出内存与上下文占用
 const MAX_BYTES = 24 * 1024
 const MAX_BYTES_LABEL = `${MAX_BYTES / 1024} KB`
@@ -280,7 +277,7 @@ export const ReadTool = Tool.define<
               return
             }
 
-            const line = text.length > MAX_LINE_LENGTH ? text.substring(0, MAX_LINE_LENGTH) + MAX_LINE_SUFFIX : text
+            const line = text
             const size = Buffer.byteLength(line, "utf-8") + (raw.length > 0 ? 1 : 0)
             if (flags.bytes + size <= MAX_BYTES) {
               raw.push(line)
diff --git a/packages/opencode/src/tool/read.txt b/packages/opencode/src/tool/read.txt
index 368174cc8d..f0842253cf 100644
--- a/packages/opencode/src/tool/read.txt
+++ b/packages/opencode/src/tool/read.txt
@@ -2,13 +2,12 @@ Read a file or directory from the local filesystem. If the path does not exist,
 
 Usage:
 - The filePath parameter should be an absolute path.
-- By default, this tool returns up to 2000 lines from the start of the file.
+- By default, this tool returns up to 400 lines from the start of the file.
 - The offset parameter is the line number to start from (1-indexed).
 - To read later sections, call this tool again with a larger offset.
 - Use the grep tool to find specific content in large files or files with long lines.
 - If you are unsure of the correct file path, use the glob tool to look up filenames by glob pattern.
 - Contents are returned with each line prefixed by its line number as `<line>: <content>`. For example, if a file has contents "foo\n", you will receive "1: foo\n". For directories, entries are returned one per line (without line numbers) with a trailing `/` for subdirectories.
-- Any line longer than 2000 characters is truncated.
 - Call this tool in parallel when you know there are multiple files you want to read.
 - Avoid tiny repeated slices (30 line chunks). If you need more context, read a larger window.
 - This tool can read image files and PDFs and return them as file attachments.
diff --git a/packages/opencode/test/tool/read.test.ts b/packages/opencode/test/tool/read.test.ts
index 5aba7870a5..4cf1074a1d 100644
--- a/packages/opencode/test/tool/read.test.ts
+++ b/packages/opencode/test/tool/read.test.ts
@@ -469,14 +469,18 @@ describe("tool.read truncation", () => {
     }),
   )
 
-  it.live("truncates long lines", () =>
+  it.live("keeps long lines whole until byte budget", () =>
     Effect.gen(function* () {
+      // #90：长行完整保留；禁止出现 2000 字行中截断后缀
       const dir = yield* tmpdirScoped()
-      yield* put(path.join(dir, "long-line.txt"), "x".repeat(3000))
+      const long = "x".repeat(3000)
+      yield* put(path.join(dir, "long-line.txt"), long + "\n")
 
       const result = yield* exec(dir, { filePath: path.join(dir, "long-line.txt") })
-      expect(result.output).toContain("(line truncated to 2000 chars)")
-      expect(result.output.length).toBeLessThan(3000)
+      expect(result.output).toContain(long)
+      expect(result.output).not.toContain("line truncated to 2000")
+      expect(result.output).not.toContain("(line truncated")
+      expect(result.output.length).toBeGreaterThan(3000)
     }),
   )
 
diff --git a/sdks/vscode/src/notebook/source.ts b/sdks/vscode/src/notebook/source.ts
index 2ead9b305c..e6786da5cd 100644
--- a/sdks/vscode/src/notebook/source.ts
+++ b/sdks/vscode/src/notebook/source.ts
@@ -24,7 +24,7 @@ import { resolveNotebook } from "./resolve"
 
 /**
  * Bridge endpoint handler: returns notebook source code with global line numbering.
- * Primary input: `cellId` (#VSC-xxxxxxxx). Output capped at 12 KB.
+ * Primary input: `cellId` (#VSC-xxxxxxxx). Output capped at 24 KB.
  */
 // 获取 notebook 源码：返回所有 cell 的代码、类型和虚拟行号范围
 // 获取 notebook 源码：返回所有 cell 的代码和虚拟行号范围
@@ -75,7 +75,7 @@ export async function notebookSource(input: Record<string, unknown>) {
   globalStart = Math.max(1, Math.min(globalStart, totalLines > 0 ? totalLines : 1))
 
   // Render cells one by one, emitting headers + in-range source lines
-  const maxBytes = 12 * 1024
+  const maxBytes = 24 * 1024
   let bytes = 0
   let rendered = 0
   let cut = false
@@ -123,10 +123,8 @@ export async function notebookSource(input: Record<string, unknown>) {
         break
       }
       const sourceLine = cell.document.lineAt(i).text
-      const src = sourceLine.length > 2000
-        ? sourceLine.substring(0, 2000) + "... (line truncated to 2000 chars)"
-        : sourceLine
-      const outLine = `${globalLineNum}: ${src}`
+      // 不再截断单行：字节预算只截断总输出
+      const outLine = `${globalLineNum}: ${sourceLine}`
       const outBytes = Buffer.byteLength(outLine, "utf8")
       if (bytes + outBytes > maxBytes && outputCells.length > 0) {
         cut = true
@@ -153,7 +151,7 @@ export async function notebookSource(input: Record<string, unknown>) {
   output += outputCells.join("\n")
 
   if (cut) {
-    output += `\n\n(Output capped at 12 KB. Showing lines ${globalStart}-${lastRenderedLine}. Use offset=${lastRenderedLine + 1} to continue.)`
+    output += `\n\n(Output capped at 24 KB. Showing lines ${globalStart}-${lastRenderedLine}. Use offset=${lastRenderedLine + 1} to continue.)`
   } else if (more) {
     output += `\n\n(Showing lines ${globalStart}-${lastRenderedLine} of ${totalLines}. Use offset=${lastRenderedLine + 1} to continue.)`
   }
