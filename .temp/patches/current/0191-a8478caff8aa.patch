From a8478caff8aa41f77a13206ba1c0a730e02777e2 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Fri, 22 May 2026 12:48:12 +0800
Subject: [PATCH] =?UTF-8?q?feat:=20=E5=A2=9E=E5=BC=BA=E9=94=99=E8=AF=AF?=

diff --git a/packages/opencode/src/provider/provider.ts b/packages/opencode/src/provider/provider.ts
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
@@ -92,7 +92,7 @@ function wrapSSE(res: Response, ms: number, ctl: AbortController) {
     async pull(ctrl) {
       const part = await new Promise<Awaited<ReturnType<typeof reader.read>>>((resolve, reject) => {
         const id = setTimeout(() => {
-          const err = new ProviderError.ResponseStreamError("SSE read timed out")
+          const err = Object.assign(new ProviderError.ResponseStreamError("SSE read timed out"), { code: "SSE_READ_TIMEOUT" })
           ctl.abort(err)
           void reader.cancel(err)
           reject(err)
diff --git a/packages/opencode/src/session/message-v2.ts b/packages/opencode/src/session/message-v2.ts
--- a/packages/opencode/src/session/message-v2.ts
+++ b/packages/opencode/src/session/message-v2.ts
@@ -43,6 +43,35 @@ interface FetchDecompressionError extends Error {
   path: string
 }
 
+// #191：仅限 transport 层瞬时故障；provider/status 错误和非法 payload 必须走各自非重试分支
+// #191：码集合覆盖 Node/undici/TLS 常见瞬时故障；ECONNRESET 由既有 switch case 处理故不在此列
+const TRANSIENT_TRANSPORT_CODES = new Set([
+  "ECONNREFUSED",
+  "ETIMEDOUT",
+  "ENOTFOUND",
+  "EAI_AGAIN",
+  "EPIPE",
+  "ConnectionClosed",
+  "ConnectionRefused",
+  "FailedToOpenSocket",
+  "UND_ERR_SOCKET",
+  "UND_ERR_CONNECT_TIMEOUT",
+  "UND_ERR_HEADERS_TIMEOUT",
+  "UND_ERR_BODY_TIMEOUT",
+  "UND_ERR_ABORTED",
+  "CERT_HAS_EXPIRED",
+  "DEPTH_ZERO_SELF_SIGNED_CERT",
+  "SELF_SIGNED_CERT_IN_CHAIN",
+  "UNABLE_TO_VERIFY_LEAF_SIGNATURE",
+  "UNABLE_TO_GET_ISSUER_CERT",
+  "UNABLE_TO_GET_ISSUER_CERT_LOCALLY",
+  "ERR_TLS_CERT_ALTNAME_INVALID",
+  "ERR_TLS_HANDSHAKE_TIMEOUT",
+  "SSE_READ_TIMEOUT",
+  // #191：SSE_READ_TIMEOUT 是自研超时码，与其他 Node/undici 网络码同属 transport 层重试类
+])
+  // #191：ECONNRESET 被排除在集合外，由 fromError 顶层 case 按嵌套深度单独判定，避免双重分类
+
 
 export const SYNTHETIC_ATTACHMENT_PROMPT = "Attached media from tool result:"
 export { isMedia }
@@ -722,6 +748,34 @@ export function fromError(
         },
         { cause: e },
       ).toObject()
+    case !ctx.aborted && !APICallError.isInstance(e) && (() => {
+      // #191：ctx.aborted 表示用户主动取消，不得转为可重试错误
+      // #191：在 catch-all 之前检查 transport 层瞬时故障
+      // #191：先解包 AI SDK RetryError，取 lastError 再判定
+      const retried = retryErrorCause(e)
+      if (retried) return true
+      // #191：transportError 命中时返回 true 进入本 case
+      return Boolean(transportError(e))
+    })():
+      // #191：RetryError 解包后递归调用 fromError
+      {
+        const retried = retryErrorCause(e)
+        if (retried) return fromError(retried, ctx)
+        // #191：transport 层瞬时故障转为可重试 APIError
+          // #191：isRetryable=true 是 retryable() 的入口契约，metadata 保留原始错误信息
+        const transport = transportError(e)
+        if (transport) {
+          return new APIError(
+            {
+              message: transport.message,
+              isRetryable: true,
+              metadata: transport.metadata,
+            },
+            { cause: e },
+          ).toObject()
+        }
+      }
+      return new NamedError.Unknown({ message: errorMessage(e) }, { cause: e }).toObject()
     case e instanceof Error:
       return new NamedError.Unknown({ message: errorMessage(e) }, { cause: e }).toObject()
     default:
@@ -753,5 +805,100 @@ export function fromError(
   }
 }
 
+
+// #191：errorLike 统一 Error 与裸对象两种形态，使 cause 链检查覆盖非标准错误包装
+type ErrorLike = {
+  name?: unknown
+  message?: unknown
+  code?: unknown
+  syscall?: unknown
+  cause?: unknown
+  lastError?: unknown
+  errors?: unknown
+}
+
+// #191：安全提取 Error-like 属性，兼容 Error 和普通对象
+// #191：非 Error 对象也可能有 code/cause，需兼容裸对象
+function errorLike(value: unknown): ErrorLike | undefined {
+  if (value instanceof Error) return value as ErrorLike
+  if (typeof value === "object" && value !== null) return value as ErrorLike
+  return undefined
+}
+
+// #191：stringValue 统一 code/name/syscall 为字符串比较，避免数字或符号混入判定
+// #191：防止 number/undefined 混入 code 判定
+function stringValue(value: unknown) {
+  return typeof value === "string" ? value : undefined
+}
+
+// #191：解包 AI SDK RetryError，取 lastError 或 errors 末尾
+// #191：只处理 AI_RetryError，其他包装错误不拆
+// #191：lastError 是 AI SDK 记录的最终失败，优先于 errors 数组；两者都不存在时不拆
+function retryErrorCause(value: unknown) {
+  const error = errorLike(value)
+  if (!error) return undefined
+  if (stringValue(error.name) !== "AI_RetryError") return undefined
+  if (error.lastError !== undefined && error.lastError !== value) return error.lastError
+  if (!Array.isArray(error.errors)) return undefined
+  const last = error.errors.at(-1)
+  // #191：避免自引用死循环
+  return last === value ? undefined : last
+}
+
+// #191：递归判定 transport 层瞬时故障，最多展开 2 层 cause
+// #191：ECONNRESET 顶层 switch 已有专门 case，嵌套时才走 transport
+function transportError(value: unknown, depth = 0): { message: string; metadata: Record<string, string> } | undefined {
+  const error = errorLike(value)
+  if (!error) return undefined
+
+  const code = stringValue(error.code)
+  // #191：code/name/message 三级判定：code 优先，其次 name，最后消息文本兜底
+  const name = stringValue(error.name)
+  const message = errorMessage(value)
+  const syscall = stringValue(error.syscall)
+  const retryCode = code
+  // #191：ECONNRESET 仅在嵌套 cause 时视为可重试，顶层由既有 case 处理，避免重复分类
+    ? code === "ECONNRESET" && depth > 0
+      ? code
+      : TRANSIENT_TRANSPORT_CODES.has(code)
+        // #191：transport 码集合是跨 provider 的稳定性契约，新增码需同步测试
+        ? code
+        : undefined
+    : name === "TimeoutError"
+      ? "TimeoutError"
+      : message === "SSE read timed out"
+        // #191：消息兜底用于未携带 code 的 provider 超时错误，保证重试语义一致
+        ? "SSE_READ_TIMEOUT"
+        : undefined
+  // #191：fetch failed / cannot connect 也视为可重试
+  const retryable =
+    retryCode !== undefined ||
+    message === "fetch failed" ||
+    message === "failed to fetch" ||
+    message.startsWith("Cannot connect to API:")
+    // #191：fetch failed 是 Node/undici 网络层通用包装，Cannot connect 是代理层提示，二者均属瞬时故障
+
+  if (retryable) {
+    // #191：部分 provider 用裸消息而非错误码表达网络失败，因此消息匹配不可省略
+    return {
+      message,
+      // #191：仅附加非空元数据
+        // #191：metadata.message 恒存在供下游展示，code/name/syscall 仅在有值时附加
+      metadata: {
+        message,
+        ...(retryCode ? { code: retryCode } : {}),
+        ...(name ? { name } : {}),
+        ...(syscall ? { syscall } : {}),
+      },
+    }
+  }
+
+  // #191：递归检查 cause 链，限制深度防死循环
+  // #191：cause 链可能成环（错误对象互相引用），depth 上限 2 是防死循环的硬边界
+  // #191：cause === value 直接截断自引用环，depth 上限兜底深链，保证判定必然终止
+  if (depth >= 2 || error.cause === undefined || error.cause === value) return undefined
+  return transportError(error.cause, depth + 1)
+}
+
 export * as MessageV2 from "./message-v2"
 export const node = LayerNode.group([Database.node])
diff --git a/packages/opencode/src/session/retry.ts b/packages/opencode/src/session/retry.ts
--- a/packages/opencode/src/session/retry.ts
+++ b/packages/opencode/src/session/retry.ts
@@ -73,7 +73,10 @@ export function retryable(error: Err, provider: string) {
     // 5xx errors are transient server failures and should always be retried,
     // even when the provider SDK doesn't explicitly mark them as retryable.
     // 网络异常（statusCode 为 undefined）也应重试，避免瞬时网络故障直接失败
-    if (!error.data.isRetryable && !(status !== undefined && status >= 500) && status !== undefined) return undefined
+    // #191：移除 status !== undefined 守卫——无 statusCode 的可重试错误（transport）也应重试
+    // #191：5xx 恒重试、无 statusCode 的可重试错误也重试；有 statusCode 的 4xx 仍不重试
+    if (!error.data.isRetryable && !(status !== undefined && status >= 500)) return undefined
     if (error.data.responseBody?.includes("FreeUsageLimitError")) {
+    // #191：FreeUsageLimitError 是计费门槛，优先于通用 5xx 重试判定
       return {
         message: GO_UPSELL_MESSAGE,
diff --git a/packages/opencode/test/session/retry.test.ts b/packages/opencode/test/session/retry.test.ts
--- a/packages/opencode/test/session/retry.test.ts
+++ b/packages/opencode/test/session/retry.test.ts
@@ -344,6 +344,18 @@ describe("session.retry.retryable", () => {
       "Usage limit reached. It will reset in 15 minutes. To continue using this model now, enable usage from your available balance",
     )
   })
+
+  // #191：isRetryable=false 且无 statusCode 时不重试
+  // #191：验证移除 status !== undefined 守卫后此路径仍正确
+  test("does not retry statusless API errors when isRetryable is false", () => {
+    const error = Schema.decodeUnknownSync(SessionV1.APIError.Schema)(
+      new SessionV1.APIError({
+        message: "Invalid provider output",
+        isRetryable: false,
+      }).toObject(),
+    )
+    expect(SessionRetry.retryable(error, retryProvider)).toBeUndefined()
+  })
 })
 
 describe("session.message-v2.fromError", () => {
@@ -437,4 +449,83 @@ describe("session.message-v2.fromError", () => {
       message: "An error occurred while processing your request.",
     })
   })
+
+  // #191：SSE 超时转为可重试 APIError
+  test("converts SSE read timeouts to retryable APIError", () => {
+    const result = MessageV2.fromError(Object.assign(new Error("SSE read timed out"), { code: "SSE_READ_TIMEOUT" }), { providerID })
+    expect(SessionV1.APIError.isInstance(result)).toBe(true)
+    if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+    expect(result.data.isRetryable).toBe(true)
+    expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: "SSE read timed out" })
+  })
+
+  // #191：瞬时 socket/DNS 故障转为可重试
+  test("converts transient socket and DNS failures to retryable APIError", () => {
+    for (const code of ["ECONNREFUSED", "ETIMEDOUT", "ENOTFOUND", "EAI_AGAIN", "EPIPE"]) {
+      const result = MessageV2.fromError(Object.assign(new Error(`${code} while connecting`), { code }), { providerID })
+      expect(SessionV1.APIError.isInstance(result)).toBe(true)
+      if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+      expect(result.data.isRetryable).toBe(true)
+      expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: `${code} while connecting` })
+    }
+  })
+
+  // #191：嵌套 ECONNRESET 也应可重试
+  test("converts wrapped connection resets to retryable APIError", () => {
+    const result = MessageV2.fromError(Object.assign(new TypeError("terminated"), { cause: Object.assign(new Error("socket closed"), { code: "ECONNRESET" }) }), { providerID })
+    expect(SessionV1.APIError.isInstance(result)).toBe(true)
+    if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+    expect(result.data.isRetryable).toBe(true)
+    expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: "socket closed" })
+  })
+
+  // #191：DOMException TimeoutError 也应可重试
+  test("converts provider timeout aborts to retryable APIError", () => {
+    const result = MessageV2.fromError(new DOMException("The operation timed out.", "TimeoutError"), { providerID })
+    expect(SessionV1.APIError.isInstance(result)).toBe(true)
+    if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+    expect(result.data.isRetryable).toBe(true)
+    expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: "The operation timed out." })
+  })
+
+  // #191：TLS 证书故障转为可重试
+  test("converts TLS certificate failures to retryable APIError", () => {
+    for (const error of [
+      Object.assign(new Error("certificate has expired"), { code: "CERT_HAS_EXPIRED" }),
+      Object.assign(new TypeError("fetch failed"), { cause: Object.assign(new Error("self-signed certificate"), { code: "DEPTH_ZERO_SELF_SIGNED_CERT" }) }),
+    ]) {
+      const result = MessageV2.fromError(error, { providerID })
+      expect(SessionV1.APIError.isInstance(result)).toBe(true)
+      if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+      expect(result.data.isRetryable).toBe(true)
+      expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: error.message })
+    }
+  })
+
+  // #191：用户主动 abort 不可重试
+  test("keeps user aborts non-retryable", () => {
+    const result = MessageV2.fromError(new DOMException("Aborted", "AbortError"), { providerID, aborted: true })
+    expect(SessionV1.AbortedError.isInstance(result)).toBe(true)
+    expect(SessionRetry.retryable(result, retryProvider)).toBeUndefined()
+  })
+
+  // #191：普通错误不转为可重试
+  test("does not convert ordinary errors to retryable APIError", () => {
+    const result = MessageV2.fromError(new Error("Invalid request body"), { providerID })
+    expect(SessionV1.APIError.isInstance(result)).toBe(false)
+    expect(SessionRetry.retryable(result, retryProvider)).toBeUndefined()
+  })
+
+  // #191：AI SDK RetryError 解包后取 lastError 判定
+  test("unwraps AI SDK retry errors before deciding retryability", () => {
+    const result = MessageV2.fromError(
+      Object.assign(new Error("Failed after retries"), { name: "AI_RetryError", lastError: Object.assign(new Error("fetch failed"), { code: "ETIMEDOUT" }) }),
+      { providerID },
+    )
+    expect(SessionV1.APIError.isInstance(result)).toBe(true)
+    if (!SessionV1.APIError.isInstance(result)) throw new Error("expected APIError")
+    expect(result.data.isRetryable).toBe(true)
+    expect(SessionRetry.retryable(result, retryProvider)).toEqual({ message: "fetch failed" })
+  })
+
 })
