From a79689db16e752672721a0aae9cbc0e6d186b3d9 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Sat, 16 May 2026 16:40:02 +0800
Subject: [PATCH] =?UTF-8?q?feat:=20=E5=A2=9E=E5=BC=BA=E4=BC=9A=E8=AF=9D?=
 =?UTF-8?q?=E5=92=8C=E6=B4=BB=E5=8A=A8=E7=AE=A1=E7=90=86=EF=BC=8C=E4=BC=98?=
 =?UTF-8?q?=E5=8C=96=E5=BF=83=E8=B7=B3=E6=9C=BA=E5=88=B6=E5=92=8C=E5=AE=88?=

diff --git a/packages/opencode/src/cli/cmd/tui.ts b/packages/opencode/src/cli/cmd/tui.ts
index 6eba89b86e..2421953d87 100644
--- a/packages/opencode/src/cli/cmd/tui.ts
+++ b/packages/opencode/src/cli/cmd/tui.ts
@@ -128,8 +128,9 @@ export async function discoverOrSpawnDaemon(
   // 快速路径：锁存活且 ping 成功则跳过竞选
   
   const quickCheck = await deps.read()
-  const quickAlive = quickCheck && deps.alive(quickCheck.pid) && (await deps.ping(quickCheck.port))
-  let existingUrl: string | null = quickAlive ? `http://127.0.0.1:${quickCheck!.port}` : null
+  // #152：pid 存活即复用 URL；ping 失败视为 unresponsive，禁止第二 daemon
+  let existingUrl: string | null =
+    quickCheck && deps.alive(quickCheck.pid) ? `http://127.0.0.1:${quickCheck.port}` : null
   let electionLease: Awaited<ReturnType<typeof Flock.acquire>> | undefined
   let fwdPid: number | undefined
 
@@ -145,10 +146,12 @@ export async function discoverOrSpawnDaemon(
       staleMs: deps.electionStaleMs,
     })
     const lock = await deps.read()
-    existingUrl =
-      lock && deps.alive(lock.pid) && (await deps.ping(lock.port)) ? `http://127.0.0.1:${lock.port}` : null
-    // 持锁后仍不可用则清理陈旧锁，避免阻塞新 daemon
-    if (!existingUrl && lock) await deps.clear()
+    // #152：alive 即可复用；仅 dead/orphan 清锁
+    if (lock && deps.alive(lock.pid)) {
+      existingUrl = `http://127.0.0.1:${lock.port}`
+    } else if (lock) {
+      await deps.clear()
+    }
 
     if (existingUrl) {
       await electionLease.release()
diff --git a/packages/opencode/src/cli/tui/server-lock.ts b/packages/opencode/src/cli/tui/server-lock.ts
index 071f2fc68e..725fe837c5 100644
--- a/packages/opencode/src/cli/tui/server-lock.ts
+++ b/packages/opencode/src/cli/tui/server-lock.ts
@@ -15,6 +15,14 @@ export type ServerLock = {
   externalUrl?: string
 }
 
+// #152：写锁前若已有存活 pid，拒绝覆盖
+export class ExistingLiveServerError extends Error {
+  constructor(public readonly lock: ServerLock) {
+    super(`Existing live opencode daemon is still running: pid=${lock.pid}, port=${lock.port}`)
+    this.name = "ExistingLiveServerError"
+  }
+}
+
 // 可覆盖以用于测试；undefined 表示使用默认路径
 let _lockPath: string | undefined
 export function _setLockPath(p: string | undefined) {
@@ -29,6 +37,11 @@ function getLockPath() {
 // 原子写入锁文件（tmp → rename），返回新 token 供 clearIfOwner 安全删除
 // 原子写锁：记录 pid/port/token，供 clearIfOwner 安全删除
 export async function write(port: number, externalUrl?: string): Promise<string> {
+  // #152：已有存活 owner 时拒绝写锁，防止双 daemon
+  const existing = await read()
+  if (existing && alive(existing.pid) && existing.pid !== process.pid) {
+    throw new ExistingLiveServerError(existing)
+  }
   const token = randomUUID()
   const lock: ServerLock = {
     pid: process.pid,
diff --git a/packages/opencode/src/cli/tui/worker.ts b/packages/opencode/src/cli/tui/worker.ts
index ab8046ebcc..1bf227c1fb 100644
--- a/packages/opencode/src/cli/tui/worker.ts
+++ b/packages/opencode/src/cli/tui/worker.ts
@@ -17,6 +17,7 @@ import { disposeAllInstancesAndEmitGlobalDisposed } from "@/server/global-lifecy
 import { Database } from "@opencode-ai/core/database/database"
 import { ServerLock } from "@/cli/tui/server-lock"
 import { onSseClientCountChange } from "@/server/routes/instance/httpapi/handlers/global"
+import { SessionActivity } from "@/session/activity"
 
 Heap.start()
 
@@ -205,12 +206,31 @@ if (isDaemon) {
   // ── 空闲超时 ───────────────────────────────────────────────────────────
   // 最后一个 SSE 客户端断开 30 秒后退出。计时器仅在首次有客户端连接后启动，
   // 避免启动缓慢的 TUI 与守护进程退出产生竞态。
+  // #152：SSE 全断且无活跃 session/shell 后 30s 才退出
   const IDLE_TIMEOUT_MS = 30_000
 
   let hadClient = false
+  let sseClients = 0
   let idleTimer: ReturnType<typeof setTimeout> | undefined
 
+  const armIdleTimer = () => {
+    if (idleTimer) {
+      clearTimeout(idleTimer)
+      idleTimer = undefined
+    }
+    if (!hadClient) return
+    if (sseClients > 0) return
+    if (SessionActivity.count() > 0) return
+    idleTimer = setTimeout(() => {
+      // #152：触发时再确认，避免 runner 刚启动被误杀
+      if (sseClients > 0 || SessionActivity.count() > 0) return
+      void gracefulShutdown()
+    }, IDLE_TIMEOUT_MS)
+    idleTimer.unref?.()
+  }
+
   onSseClientCountChange((count) => {
+    sseClients = count
     if (count > 0) {
       hadClient = true
       if (idleTimer) {
@@ -219,10 +239,11 @@ if (isDaemon) {
       }
       return
     }
-    // 从未有客户端连接过，不启动退出计时器
-    if (!hadClient) return
-    idleTimer = setTimeout(() => void gracefulShutdown(), IDLE_TIMEOUT_MS)
-    idleTimer.unref?.()
+    armIdleTimer()
+  })
+
+  SessionActivity.onChange(() => {
+    armIdleTimer()
   })
 
   // ── 启动任务 ───────────────────────────────────────────────────────────
diff --git a/packages/opencode/src/session/activity.ts b/packages/opencode/src/session/activity.ts
new file mode 100644
index 0000000000..60b575c22e
--- /dev/null
+++ b/packages/opencode/src/session/activity.ts
@@ -0,0 +1,33 @@
+import { EventEmitter } from "events"
+
+const emitter = new EventEmitter()
+const active = new Set<string>()
+
+// #152：begin 返回 end；进程内跟踪 session/shell 活跃 runner
+export function begin(id: string) {
+  active.add(id)
+  emitter.emit("change", active.size)
+
+  let done = false
+  return () => {
+    if (done) return
+    done = true
+    active.delete(id)
+    emitter.emit("change", active.size)
+  }
+}
+
+// #152：当前活跃 runner 数量
+export function count() {
+  return active.size
+}
+
+// #152：数量变化订阅，供 worker 重武装 idle 计时器
+export function onChange(cb: (count: number) => void) {
+  emitter.on("change", cb)
+  return () => {
+    emitter.off("change", cb)
+  }
+}
+
+export * as SessionActivity from "./activity"
diff --git a/packages/opencode/src/session/run-state.ts b/packages/opencode/src/session/run-state.ts
index 5cefdd04a3..116c7a9dee 100644
--- a/packages/opencode/src/session/run-state.ts
+++ b/packages/opencode/src/session/run-state.ts
@@ -6,6 +6,7 @@ import { BackgroundJob } from "@/background/job"
 import { Effect, Latch, Layer, Scope, Context } from "effect"
 import { Session } from "./session"
 import { SessionID } from "./schema"
+import { SessionActivity } from "@/session/activity"
 import { SessionStatus } from "./status"
 
 export interface Interface {
@@ -90,7 +91,11 @@ const layer = Layer.effect(
       onInterrupt: Effect.Effect<SessionV1.WithParts>,
       work: Effect.Effect<SessionV1.WithParts>,
     ) {
-      return yield* (yield* runner(sessionID, onInterrupt)).ensureRunning(work)
+      // #152：runner 生命周期计入 SessionActivity，供 worker 空闲判定
+      const end = SessionActivity.begin(`session:${sessionID}`)
+      return yield* (yield* runner(sessionID, onInterrupt))
+        .ensureRunning(work)
+        .pipe(Effect.ensuring(Effect.sync(end)))
     })
 
     const startShell = Effect.fn("SessionRunState.startShell")(function* (
@@ -99,9 +104,14 @@ const layer = Layer.effect(
       work: Effect.Effect<SessionV1.WithParts>,
       ready?: Latch.Latch,
     ) {
+      // #152：shell runner 同样计入 activity
+      const end = SessionActivity.begin(`shell:${sessionID}`)
       return yield* (yield* runner(sessionID, onInterrupt))
         .startShell(work, ready)
-        .pipe(Effect.catchTag("RunnerBusy", () => Effect.fail(busyError(sessionID))))
+        .pipe(
+          Effect.catchTag("RunnerBusy", () => Effect.fail(busyError(sessionID))),
+          Effect.ensuring(Effect.sync(end)),
+        )
     })
 
     return Service.of({ assertNotBusy, cancel, ensureRunning, startShell })
diff --git a/packages/opencode/src/snapshot/index.ts b/packages/opencode/src/snapshot/index.ts
index 4da9bc3ca8..1c4b009ad4 100644
--- a/packages/opencode/src/snapshot/index.ts
+++ b/packages/opencode/src/snapshot/index.ts
@@ -733,8 +733,13 @@ const layer: Layer.Layer<Service, never, FSUtil.Service | AppProcess.Service | C
               }
 
               const step = 100
-              const patch = (file: string, before: string, after: string) =>
-                formatPatch(structuredPatch(file, file, before, after, "", "", { context: Number.MAX_SAFE_INTEGER }))
+              // #152：大文件跳过同步 structuredPatch，避免阻塞心跳/SSE
+              const patchSizeLimit = 500_000
+              const patch = (file: string, before: string, after: string) => {
+                if (before.length + after.length > patchSizeLimit) return ""
+                // #152：context 收窄到 3，降低 O(n²) 与输出体积
+                return formatPatch(structuredPatch(file, file, before, after, "", "", { context: 3 }))
+              }
 
               for (let i = 0; i < rows.length; i += step) {
                 const run = rows.slice(i, i + step)
diff --git a/packages/opencode/test/cli/tui/thread.test.ts b/packages/opencode/test/cli/tui/thread.test.ts
index 2cf01c7c01..9d1453fa26 100644
--- a/packages/opencode/test/cli/tui/thread.test.ts
+++ b/packages/opencode/test/cli/tui/thread.test.ts
@@ -172,42 +172,31 @@ describe("tui thread", () => {
       expect(result.url).toBe("http://127.0.0.1:9999")
     })
 
-    test("lock exists + pid alive but HTTP ping fails: clears stale lock and spawns", async () => {
+    test("lock exists + pid alive but HTTP ping fails: reuses URL without spawn", async () => {
+      // #152：unresponsive 存活 owner 必须复用，禁止清锁再 spawn
       let daemonSpawnCount = 0
       let clearCalled = false
-      let readCount = 0
-      let pingCount = 0
       const result = await discoverOrSpawnDaemon(localNet as never, baseDeps({
-        read: async () => {
-          readCount++
-          if (readCount <= 2) {
-            return {
-              pid: process.pid,
-              port: 9999,
-              token: "stale",
-              dbPath: "/tmp/test.db",
-              channel: "local",
-              startedAt: new Date().toISOString(),
-            }
-          }
-          return fakeLock
-        },
-        ping: async () => {
-          pingCount++
-          // 前两次（quick+under-lock）失败，spawn 后成功
-          return pingCount > 2
-        },
+        read: async () => ({
+          pid: process.pid,
+          port: 9999,
+          token: "stale",
+          dbPath: "/tmp/test.db",
+          channel: "local",
+          startedAt: new Date().toISOString(),
+        }),
+        ping: async () => false,
         clear: async () => {
           clearCalled = true
         },
         spawn: ((_cmd, _opts) => {
           daemonSpawnCount++
-          return { pid: FAKE_DAEMON_PID, unref() {}, kill() {} } as ReturnType<typeof Bun.spawn>
+          return { pid: 12345, unref() {}, kill() {} } as never
         }) as DiscoverDaemonDeps["spawn"],
       }))
-      expect(daemonSpawnCount).toBe(1)
-      expect(clearCalled).toBe(true)
       expect(result.url).toBe("http://127.0.0.1:9999")
+      expect(daemonSpawnCount).toBe(0)
+      expect(clearCalled).toBe(false)
     })
 
     test("lock exists but pid dead (orphan): clears and spawns new daemon", async () => {
diff --git a/packages/tui/src/context/sdk.tsx b/packages/tui/src/context/sdk.tsx
index ddf8810d0f..3520c0e518 100644
--- a/packages/tui/src/context/sdk.tsx
+++ b/packages/tui/src/context/sdk.tsx
@@ -58,7 +58,7 @@ export const { use: useSDK, provider: SDKProvider } = createSimpleContext({
     const retryDelay = 1000
     const maxRetryDelay = 30000
     // #139：默认 15s，与 source 服务端 10s 心跳匹配
-    const heartbeatTimeout = props.heartbeatTimeout ?? 15000
+    const heartbeatTimeout = props.heartbeatTimeout ?? 90_000 /* #152：容忍长 diff/事件循环卡顿 */
 
     const flush = () => {
       if (queue.length === 0) return
