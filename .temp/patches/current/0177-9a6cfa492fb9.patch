From 9a6cfa492fb9b3ef77ce45f70614f57a4b6bc113 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Tue, 19 May 2026 17:35:59 +0800
Subject: [PATCH] =?UTF-8?q?feat:=20=E6=9B=B4=E6=96=B0=E7=BB=9F=E8=AE=A1?=

diff --git a/packages/opencode/src/cli/cmd/stats.ts b/packages/opencode/src/cli/cmd/stats.ts
--- a/packages/opencode/src/cli/cmd/stats.ts
+++ b/packages/opencode/src/cli/cmd/stats.ts
@@ -64,7 +64,8 @@ const colorChoices = ["auto", "always", "never"] as const
 // #174：metricChoices 限制取值
 const metricChoices = ["tokens", "cost"] as const
 // #174：sortChoices 限制取值
-const sortChoices = ["cost", "tokens", "calls", "updated"] as const
+// #177：默认排序优先 tokens
+const sortChoices = ["tokens", "cost", "calls", "updated"] as const
 
 // #174：nonNegativeInteger 拒浮点与负数
 const nonNegativeInteger = (value: unknown): value is number =>
@@ -145,7 +146,7 @@ const withDetailOptions = (yargs: Argv) =>
       describe: "sort order",
       type: "string",
       choices: sortChoices,
-      default: "cost",
+      default: "tokens",
     })
     // #176：明细过滤选项
     .option("session", {
@@ -221,13 +222,15 @@ const colorMode = (value?: string): ColorMode => {
 }
 
 const metricMode = (value?: string): StatsRenderOptions["metric"] => {
+  // #177：metric 与 sort 解耦，cost 必须仍可选
   if (value === "cost") return "cost"
   return "tokens"
 }
 
 const sortMode = (value?: string): StatsRenderOptions["sort"] => {
-  if (value === "tokens" || value === "calls" || value === "updated") return value
-  return "cost"
+  // #177：显式 cost 仍可选；默认 tokens
+  if (value === "cost" || value === "tokens" || value === "calls" || value === "updated") return value
+  return "tokens"
 }
 
 // #174：renderOptions 从 args 组装
diff --git a/packages/opencode/src/cli/cmd/stats-data.ts b/packages/opencode/src/cli/cmd/stats-data.ts
--- a/packages/opencode/src/cli/cmd/stats-data.ts
+++ b/packages/opencode/src/cli/cmd/stats-data.ts
@@ -137,6 +137,8 @@ export type StatsReport = {
   statusSeries: UsageSeries[]
   projectSeries: UsageSeries[]
   modelProviderTokens: { providerID: string; modelID: string; tokens: number }[]
+  // #177：工具上下文 token 时间序列
+  toolSeries: UsageSeries[]
 // #174：tokensPerSession 均摊
   tokensPerSession: number
 // #174：median 抗长尾
@@ -298,6 +300,7 @@ const addGroup = (map: Map<string, UsageGroupAccumulator>, id: string, label: st
 }
 
 // #174：finalizeGroups 定稿分组
+// #177：分组默认按 tokens 排序，cost 作次键
 const finalizeGroups = (map: Map<string, UsageGroupAccumulator>) =>
   Array.from(map.values())
     .map((group): UsageGroup => ({
@@ -311,7 +314,7 @@ const finalizeGroups = (map: Map<string, UsageGroupAccumulator>) =>
       providers: Array.from(group.providerTotals, ([id, tokens]) => ({ id, tokens })).sort((a, b) => b.tokens - a.tokens),
       models: Array.from(group.modelTotals, ([id, tokens]) => ({ id, tokens })).sort((a, b) => b.tokens - a.tokens),
     }))
-    .sort((a, b) => b.cost - a.cost || b.tokens.total - a.tokens.total)
+    .sort((a, b) => b.tokens.total - a.tokens.total || b.cost - a.cost)
 
 // #174：dayStart 对齐本地日界
 const dayStart = (time: number) => {
@@ -370,7 +373,7 @@ const finalizeSeries = (map: Map<string, Map<number, DailyUsage>>, days: DailyUs
           return series
         }, emptySeriesTotals(id)),
     )
-    .sort((a, b) => b.cost - a.cost || b.tokens.total - a.tokens.total)
+    .sort((a, b) => b.tokens.total - a.tokens.total || b.cost - a.cost)
 
 const tokenPartSeries = (daily: DailyUsage[]): TokenPartSeries[] =>
   [
@@ -936,6 +939,8 @@ export const aggregateStats = Effect.fn("Cli.stats.aggregate")(function* (input:
   const statusSeries = new Map<string, Map<number, DailyUsage>>()
   const projectSeries = new Map<string, Map<number, DailyUsage>>()
   const modelProviderTokens = new Map<string, number>()
+  // #177：按日累加工具 contextTokens
+  const toolSeries = new Map<string, Map<number, DailyUsage>>()
   const sessionsWithUsage: SessionUsage[] = []
   const sessionTokenTotals: number[] = []
   let totalMessages = 0
@@ -976,7 +981,17 @@ const filteredAggregates =
     // #176：上下文 token 由 toolEvents 叠加，避免与 count 双计字符
     aggregate.toolEvents
       .filter((event) => toolEventMatches(event, input))
-      .forEach((event) => addToolContextTokens(toolUsage, event.toolID, event.contextTokens))
+      // #177：工具事件同时更新用量与序列
+      .forEach((event) => {
+        addToolContextTokens(toolUsage, event.toolID, event.contextTokens)
+        // #177：工具事件写入日序列（tokens.total 承载 contextTokens）
+        const point = getSeriesDaily(toolSeries, event.toolID, event.time)
+        // #177：日点 tokens.total 承载 context
+        point.tokens.total += event.contextTokens
+        point.tokens.input += event.contextTokens
+        // #177：请求次数随工具事件递增
+        point.requests += 1
+      })
 
     for (const event of aggregate.totalEvents.filter((event) => eventMatches(event, input))) {
       addUsage(total, event)
@@ -1059,11 +1074,13 @@ const filteredAggregates =
     statusSeries: finalizeSeries(statusSeries, sortedDaily),
     projectSeries: finalizeSeries(projectSeries, sortedDaily),
     tokenPartSeries: tokenPartSeries(sortedDaily),
-    sessions: sessionsWithUsage.sort((a, b) => b.cost - a.cost || b.tokens.total - a.tokens.total),
+    sessions: sessionsWithUsage.sort((a, b) => b.tokens.total - a.tokens.total || b.cost - a.cost),
     // #176：默认按 contextTokens 再按 count
     // #176：工具榜优先 contextTokens 体现归因
     toolUsage: Object.values(toolUsage).sort((a, b) => b.contextTokens - a.contextTokens || b.count - a.count),
     // #176：模型×供应矩阵供热图/对比
+    // #177：输出 toolSeries 供渲染
+    toolSeries: finalizeSeries(toolSeries, sortedDaily),
     modelProviderTokens: Array.from(modelProviderTokens, ([key, tokens]) => {
       const [providerID, modelID] = key.split("\0")
       return { providerID, modelID, tokens }
diff --git a/packages/opencode/src/cli/cmd/stats-render.ts b/packages/opencode/src/cli/cmd/stats-render.ts
--- a/packages/opencode/src/cli/cmd/stats-render.ts
+++ b/packages/opencode/src/cli/cmd/stats-render.ts
@@ -115,6 +115,21 @@ export const renderBreakdown = (report: StatsReport, options: StatsRenderOptions
   ]
     // #176：tool 维附加 context/count/IO 明细
   if (by === "tool") {
+    // #177：tool breakdown 主路径展示 context 时间序列
+    if ((report.toolSeries?.length ?? 0) > 0) {
+      lines.push("")
+      lines.push(
+        ...renderRoundedLineChart({
+          // #177：固定标题供测试与 UI 契约锁定
+          title: "Tool context tokens over time",
+          series: (report.toolSeries ?? []).slice(0, options.limit ?? 5).map((s) => ({
+            id: s.id,
+            label: s.label || s.id,
+            points: s.points.map((p) => ({ x: p.label, y: p.tokens.total })),
+          })),
+        }),
+      )
+    }
     lines.push("")
     lines.push("Tool context tokens / counts")
     for (const tool of limitRows(report.toolUsage ?? [], options.limit)) {
@@ -176,8 +191,9 @@ export const renderTimeline = (report: StatsReport, options: StatsRenderOptions
   ].join("\n")
 
 // #174：sessions 按 sort 键排序再切片
+// #177：sessions 默认 tokens 排序
 export const renderSessions = (report: StatsReport, options: StatsRenderOptions = {}) => {
-  const sort = options.sort ?? "cost"
+  const sort = options.sort ?? "tokens"
   const rows = [...report.sessions].sort((a, b) => {
     if (sort === "tokens") return b.tokens.total - a.tokens.total
     if (sort === "calls") return b.requests - a.requests
@@ -195,6 +211,18 @@ export const renderSessions = (report: StatsReport, options: StatsRenderOptions
 export const renderCharts = (report: StatsReport, options: StatsRenderOptions = {}) =>
   [
     ...header(report, "stats charts"),
+    // #177：charts 主路径组合 tool context 时间序列
+    ...((report.toolSeries?.length ?? 0) > 0
+      ? renderRoundedLineChart({
+          // #177：固定标题供测试与 UI 契约锁定
+          title: "Tool context tokens over time",
+          series: (report.toolSeries ?? []).slice(0, options.limit ?? 5).map((s) => ({
+            id: s.id,
+            label: s.label || s.id,
+            points: s.points.map((p) => ({ x: p.label, y: p.tokens.total })),
+          })),
+        })
+      : []),
     ...renderRankBars({ rows: limitRows(report.models, options.limit ?? 8).map((m) => ({ label: m.label || m.id, value: m.tokens.total })) }),
     ...renderRoundedLineChart({ series: [dailySeries(report.daily, (d) => d.cost)] }),
   ].join("\n")
diff --git a/packages/opencode/test/cli/stats-render-width.test.ts b/packages/opencode/test/cli/stats-render-width.test.ts
--- a/packages/opencode/test/cli/stats-render-width.test.ts
+++ b/packages/opencode/test/cli/stats-render-width.test.ts
@@ -61,4 +61,53 @@ describe("stats render width (#176)", () => {
     // with toolSeries present, breakdown must surface the #177 chart title
   })
 
+  test("renderBreakdown tool dimension includes tool context series title", () => {
+    const report = {
+      totalSessions: 1,
+      sessionsWithUsage: 1,
+      totalMessages: 1,
+      totalTools: 1,
+      days: 1,
+      dateRange: { earliest: 0, latest: 1 },
+      total: {
+        tokens: { input: 1, output: 1, reasoning: 0, cache: { read: 0, write: 0 }, total: 2 },
+        cost: 0,
+        requests: 1,
+        assistantCalls: 1,
+      },
+      models: [],
+      providers: [],
+      agents: [],
+      sources: [],
+      statuses: [],
+      projects: [],
+      daily: [{ day: 0, label: "d0", tokens: { input: 0, output: 0, reasoning: 0, cache: { read: 0, write: 0 }, total: 0 }, cost: 0, requests: 0, assistantCalls: 0 }],
+      modelSeries: [],
+      providerSeries: [],
+      agentSeries: [],
+      sourceSeries: [],
+      statusSeries: [],
+      projectSeries: [],
+      tokenPartSeries: [],
+      sessions: [],
+      toolUsage: [{ id: "bash", count: 1, inputChars: 1, outputChars: 1, contextTokens: 3 }],
+      toolSeries: [
+        {
+          id: "bash",
+          label: "bash",
+          points: [{ day: 0, label: "d0", tokens: { input: 3, output: 0, reasoning: 0, cache: { read: 0, write: 0 }, total: 3 }, cost: 0, requests: 1, assistantCalls: 0 }],
+          tokens: { input: 3, output: 0, reasoning: 0, cache: { read: 0, write: 0 }, total: 3 },
+          cost: 0,
+          requests: 1,
+          assistantCalls: 0,
+        },
+      ],
+      modelProviderTokens: [],
+      tokensPerSession: 2,
+      medianTokensPerSession: 2,
+    } as unknown as StatsReport
+    const text = renderBreakdown(report, { by: "tool", metric: "tokens" })
+    // #177：主路径必须出现系列标题
+    expect(text).toContain("Tool context tokens over time")
+  })
 })
