From d7b11daa5ef707cfdf61d606e5d6b982c8a11607 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Thu, 21 May 2026 16:07:41 +0800
Subject: [PATCH] =?UTF-8?q?feat:=20=E6=B7=BB=E5=8A=A0=E4=BC=9A=E8=AF=9D?=

diff --git a/packages/tui/src/routes/session/layout.ts b/packages/tui/src/routes/session/layout.ts
new file mode 100644
--- /dev/null
+++ b/packages/tui/src/routes/session/layout.ts
@@ -0,0 +1,25 @@
+// #184：常量按 tip Session 主列/滚动条实测 chrome 校准
+// #184：会话消息可读宽度的唯一公式，避免终端估值漂移
+export const SESSION_SIDEBAR_WIDTH = 42
+
+// #184：tip 主列 paddingLeft2+paddingRight1=3
+const SESSION_MAIN_HORIZONTAL_PADDING = 3
+// #184：消息左边框+左 padding（assistant chrome）
+const SESSION_MESSAGE_LEFT_CHROME = 4
+// #184：tip viewport+content 各 paddingRight 1，合计 gutter 2
+const SESSION_SCROLLBAR_GUTTER = 2
+
+export const sessionMessageContentWidth = (input: {
+  terminalWidth: number
+  sidebarInLayout: boolean
+  scrollbarEnabled: boolean
+}) => {
+  // #184：overlay 侧栏不扣宽；仅 docked 时减 SIDEBAR
+  const sidebar = input.sidebarInLayout ? SESSION_SIDEBAR_WIDTH : 0
+  const scroll = input.scrollbarEnabled ? SESSION_SCROLLBAR_GUTTER : 0
+  // #184：至少 1 列，防止 Yoga 负数
+  return Math.max(
+    1,
+    input.terminalWidth - sidebar - SESSION_MAIN_HORIZONTAL_PADDING - SESSION_MESSAGE_LEFT_CHROME - scroll,
+  )
+}
diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -15,6 +15,7 @@ import {
   useContext,
 } from "solid-js"
 import { Dynamic } from "solid-js/web"
+import { sessionMessageContentWidth } from "./layout"
 import path from "node:path"
 import { mkdir, writeFile } from "node:fs/promises"
 import { useRoute, useRouteData } from "../../context/route"
@@ -311,7 +312,15 @@ setTimeout(() => {
     return false
   })
   const showTimestamps = createMemo(() => timestamps() === "show")
-  const contentWidth = createMemo(() => dimensions().width - (sidebarVisible() ? 42 : 0) - 4)
+  const sidebarInLayout = createMemo(() => sidebarVisible() && wide())
+  // #184：可读宽按 docked 侧栏与滚动条精确扣减
+  const messageContentWidth = createMemo(() =>
+    sessionMessageContentWidth({
+      terminalWidth: dimensions().width,
+      sidebarInLayout: sidebarInLayout(),
+      scrollbarEnabled: showScrollbar(),
+    }),
+  )
   const providers = createMemo(() => Model.index(sync.data.provider))
 
   const scrollAcceleration = createMemo(() => getScrollAcceleration(tuiConfig))
@@ -1254,7 +1263,7 @@ setTimeout(() => {
       <context.Provider
         value={{
           get width() {
-            return contentWidth()
+            return messageContentWidth()
           },
           sessionID: route.sessionID,
           conceal,
diff --git a/packages/tui/src/routes/session/sidebar.tsx b/packages/tui/src/routes/session/sidebar.tsx
--- a/packages/tui/src/routes/session/sidebar.tsx
+++ b/packages/tui/src/routes/session/sidebar.tsx
@@ -1,4 +1,5 @@
 import { useProject } from "../../context/project"
+import { SESSION_SIDEBAR_WIDTH } from "./layout"
 import { useSync } from "../../context/sync"
 import { createMemo, Show } from "solid-js"
 import { useTheme } from "../../context/theme"
@@ -27,7 +28,7 @@ export function Sidebar(props: { sessionID: string; overlay?: boolean }) {
     <Show when={session()}>
       <box
         backgroundColor={theme.backgroundPanel}
-        width={42}
+        width={SESSION_SIDEBAR_WIDTH}
         height="100%"
         paddingTop={1}
         paddingBottom={1}
diff --git a/packages/tui/test/routes/session/session-layout.test.ts b/packages/tui/test/routes/session/session-layout.test.ts
new file mode 100644
--- /dev/null
+++ b/packages/tui/test/routes/session/session-layout.test.ts
@@ -0,0 +1,24 @@
+import { describe, expect, test } from "bun:test"
+import { sessionMessageContentWidth, SESSION_SIDEBAR_WIDTH } from "../../../src/routes/session/layout"
+
+// #184：与 tip index 主列 paddingLeft=2 paddingRight=1、scrollbar paddingRight 0/1 对齐
+// #184：纯函数锁定 docked/overlay 与滚动条组合
+describe("sessionMessageContentWidth (#184)", () => {
+  test("does not subtract sidebar when overlay (narrow)", () => {
+    // #184：窄屏侧栏 overlay，消息区应保留全宽扣 chrome
+    const w = sessionMessageContentWidth({ terminalWidth: 80, sidebarInLayout: false, scrollbarEnabled: false })
+    expect(w).toBe(80 - 3 - 4)
+  })
+
+  test("subtracts docked sidebar on wide layout", () => {
+    // #184：宽屏 docked 侧栏必须计入
+    const w = sessionMessageContentWidth({ terminalWidth: 160, sidebarInLayout: true, scrollbarEnabled: false })
+    expect(w).toBe(160 - SESSION_SIDEBAR_WIDTH - 3 - 4)
+  })
+
+  test("subtracts scrollbar gutter when enabled", () => {
+    // #184：滚动条开启再扣 tip 双层 paddingRight 合计 2
+    const w = sessionMessageContentWidth({ terminalWidth: 160, sidebarInLayout: true, scrollbarEnabled: true })
+    expect(w).toBe(160 - SESSION_SIDEBAR_WIDTH - 3 - 4 - 2)
+  })
+})
