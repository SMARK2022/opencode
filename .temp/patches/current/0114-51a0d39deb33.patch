From 51a0d39deb33739fe9460d8d55b3476d15e9e1c2 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Fri, 8 May 2026 16:47:30 +0800
Subject: [PATCH] =?UTF-8?q?feat(provider):=20=E6=B7=BB=E5=8A=A0=E5=88=AB?=
 =?UTF-8?q?=E5=90=8D=E6=94=AF=E6=8C=81=EF=BC=8C=E5=85=81=E8=AE=B8=E5=A4=9A?=
 =?UTF-8?q?=E4=B8=AA=E6=8F=90=E4=BE=9B=E8=80=85=E7=8B=AC=E7=AB=8B=E7=AE=A1?=

diff --git a/packages/opencode/src/provider/alias.ts b/packages/opencode/src/provider/alias.ts
new file mode 100644
index 0000000000..e8845497b6
--- /dev/null
+++ b/packages/opencode/src/provider/alias.ts
@@ -0,0 +1,47 @@
+import { AsyncLocalStorage } from "node:async_hooks"
+
+// #114：别名 provider 运行时上下文（真实 alias ID vs extends 基类）
+export interface AliasContext {
+  // #114：真实配置中的别名 id（写入 auth 的目标）
+  providerID: string
+  // #114：extends 指向的基类 id（插件/hook 逻辑复用目标）
+  baseProviderID: string
+}
+
+export const aliasContext = new AsyncLocalStorage<AliasContext>()
+
+// #114：在别名 ALS 中执行 models/auth loader，隔离多账号
+export function runWithAlias<T>(providerID: string, baseProviderID: string, fn: () => T): T {
+  return aliasContext.run({ providerID, baseProviderID }, fn)
+}
+
+// #114：从 config.provider 构建 aliasID -> extends 基类映射
+export function buildBaseProviderMap(
+  configProviders: Record<string, { extends?: unknown }>,
+): Record<string, string> {
+  const map: Record<string, string> = {}
+  for (const [id, provider] of Object.entries(configProviders)) {
+    if (provider?.extends && typeof provider.extends === "string") {
+      map[id] = provider.extends
+    }
+  }
+  return map
+}
+
+// #114：查询别名对应的基类 id；未配置 extends 时返回 undefined
+export function resolveBaseProvider(providerID: string, baseProviderMap: Record<string, string>): string | undefined {
+  return baseProviderMap[providerID]
+}
+
+// #114：openai oauth 判定覆盖 extends: openai 的别名账号
+export function isOpenaiOauthProvider(
+  providerID: string,
+  baseProviderMap: Record<string, string>,
+): boolean {
+  return providerID === "openai" || baseProviderMap[providerID] === "openai"
+}
+
+// #114：无 config map 时，用 npm=@ai-sdk/openai 识别 openai 别名 oauth
+export function isOpenaiOauthByModelNpm(providerID: string, npm?: string): boolean {
+  return providerID === "openai" || npm === "@ai-sdk/openai"
+}
diff --git a/packages/opencode/src/provider/provider.ts b/packages/opencode/src/provider/provider.ts
index 909cd14a8e..425cf2a0fc 100644
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
@@ -27,6 +27,7 @@ import { NetworkProxy } from "@opencode-ai/core/network-proxy"
 import { isRecord } from "@/util/record"
 import { optional } from "@opencode-ai/core/schema"
 import { ProviderTransform } from "./transform"
+import { runWithAlias, buildBaseProviderMap } from "./alias"
 import { ProviderV2 } from "@opencode-ai/core/provider"
 import { ModelV2 } from "@opencode-ai/core/model"
 import { ModelStatus } from "./model-status"
@@ -1593,6 +1594,43 @@ url: apiUrl({ npm: apiNpm, api: model.provider?.api ?? provider?.api ?? existing
           database[providerID] = parsed
         }
 
+        // #114：config extends 落库后，对别名再跑 provider.models hook
+        // #114：必须在 extend database 之后，否则 aliasDB 尚不存在
+        const baseProviderMap = buildBaseProviderMap(cfg.provider ?? {})
+        for (const hook of plugins) {
+          const p = hook.provider
+          const models = p?.models
+          if (!p || !models) continue
+          // #114：hook.provider.id 是基类（如 openai），映射到所有 extends 它的别名
+          const baseProviderID = p.id
+          for (const [aliasID, baseType] of Object.entries(baseProviderMap)) {
+            if (baseType !== baseProviderID) continue
+            const aliasProviderID = ProviderV2.ID.make(aliasID)
+            const aliasDB = database[aliasProviderID]
+            // #114：别名未启用或未落库则跳过，避免污染主 provider
+            if (!aliasDB) continue
+            if (disabled.has(aliasProviderID)) continue
+            const pluginAuth = yield* auth.get(aliasProviderID).pipe(Effect.orDie)
+            aliasDB.models = yield* Effect.promise(async () => {
+              // #114：runWithAlias 保证 models hook 内 auth.set 写到 alias id
+              const next = await runWithAlias(aliasID, baseType, () =>
+                models(toPublicInfo(aliasDB), { auth: pluginAuth }),
+              )
+              return Object.fromEntries(
+                Object.entries(next).map(([id, model]) => [
+                  id,
+                  {
+                    ...model,
+                    id: ModelV2.ID.make(id),
+                    // #114：模型归属写回别名 providerID，禁止残留基类 id
+                    providerID: aliasProviderID,
+                  },
+                ]),
+              )
+            })
+          }
+        }
+
         // load env
         const envs = yield* env.all()
         for (const [id, provider] of Object.entries(database)) {
@@ -1640,6 +1678,45 @@ url: apiUrl({ npm: apiNpm, api: model.provider?.api ?? provider?.api ?? existing
           mergeProvider(providerID, patch)
         }
 
+        // #114：别名账号复用基类 auth.loader，fetch 保持 runWithAlias
+        // #114：与 tip customLoader extends 路径并存：这里专管 plugin.auth 注入
+        for (const plugin of plugins) {
+          if (!plugin.auth) continue
+          // #114：基类 auth.provider（如 openai）对应所有 extends 别名
+          const baseProvider = plugin.auth.provider
+          for (const [aliasID, baseType] of Object.entries(baseProviderMap)) {
+            if (baseType !== baseProvider) continue
+            const aliasProviderID = ProviderV2.ID.make(aliasID)
+            if (disabled.has(aliasProviderID)) continue
+            const aliasDB = database[aliasProviderID]
+            if (!aliasDB) continue
+            // #114：别名自身无凭证则不跑 loader，避免空 oauth 污染 options
+            const stored = yield* auth.get(aliasProviderID).pipe(Effect.orDie)
+            if (!stored) continue
+            if (!plugin.auth.loader) continue
+            const options = yield* Effect.promise(() =>
+              runWithAlias(aliasID, baseProvider, () =>
+                plugin.auth!.loader!(
+                  // #114：getAuth 读别名自己的凭证，不是基类凭证
+                  () => bridge.promise(auth.get(aliasProviderID).pipe(Effect.orDie)) as any,
+                  toPublicInfo(aliasDB),
+                ),
+              ),
+            )
+            const opts = options ?? {}
+            if (opts.fetch) {
+              const origFetch = opts.fetch
+              // #114：后续每次 fetch 仍带着 ALS，保证刷新 token 写回别名
+              opts.fetch = (...args: Parameters<typeof origFetch>): ReturnType<typeof origFetch> =>
+                runWithAlias(aliasID, baseProvider, () => origFetch(...args))
+            }
+            const patch: Partial<Info> = providers[aliasProviderID]
+              ? { options: opts }
+              : { source: "custom", options: opts }
+            mergeProvider(aliasProviderID, patch)
+          }
+        }
+
         // 封装 custom loader 为 Effect.fn，支持 configProviders.extends 复用同一 loader
         // 先物化 loader 表，供内置与 extends 两次复用
 const customLoaders = custom(dep)
diff --git a/packages/opencode/src/plugin/index.ts b/packages/opencode/src/plugin/index.ts
index aefe54a4f1..bf8410e396 100644
--- a/packages/opencode/src/plugin/index.ts
+++ b/packages/opencode/src/plugin/index.ts
@@ -7,6 +7,7 @@ import type {
   WorkspaceAdapter as PluginWorkspaceAdapter,
 } from "@opencode-ai/plugin"
 import { Config } from "@/config/config"
+import { aliasContext, buildBaseProviderMap } from "@/provider/alias"
 import { createOpencodeClient } from "@opencode-ai/sdk"
 import { ServerAuth } from "@/server/auth"
 import { CodexAuthPlugin } from "./openai/codex"
@@ -143,6 +144,101 @@ async function applyPlugin(load: PluginLoader.Loaded, input: PluginInput, hooks:
   }
 }
 
+
+// #114：auth.set 时把基类 provider id 改写为当前 ALS 中的 alias id
+// #114：只代理 auth.set；其它 SDK 方法透传，避免扩大 Proxy 面
+function wrapClientForAlias(raw: ReturnType<typeof createOpencodeClient>) {
+  return new Proxy(raw, {
+    get(target, prop, receiver) {
+      if (prop === "auth") {
+        return new Proxy(Reflect.get(target, prop, receiver), {
+          get(authTarget, authProp, authReceiver) {
+            if (authProp === "set") {
+              const origSet = Reflect.get(authTarget, authProp, authReceiver)
+              return async (params: { path: { id: string }; body: any }) => {
+                const alias = aliasContext.getStore()
+                if (alias && params.path.id === alias.baseProviderID) {
+                  return origSet.call(authTarget, {
+                    ...params,
+                    path: { ...params.path, id: alias.providerID },
+                  })
+                }
+                return origSet.call(authTarget, params)
+              }
+            }
+            return Reflect.get(authTarget, authProp, authReceiver)
+          },
+        })
+      }
+      return Reflect.get(target, prop, receiver)
+    },
+  })
+}
+
+// #114：为 extends 别名克隆基类 hook，provider/auth id 指向 alias
+// #114：chat.* 入参 model.providerID 映射回基类，复用基类插件逻辑
+function createAliasHook(baseHook: Hooks, providerID: string, baseProviderID: string): Hooks {
+  const aliasHook: Hooks = {}
+  if (baseHook.auth) {
+    aliasHook.auth = {
+      ...baseHook.auth,
+      provider: providerID,
+      loader: baseHook.auth.loader
+        ? async (getAuth, provider) => {
+            return aliasContext.run({ providerID, baseProviderID }, () =>
+              baseHook.auth!.loader!(getAuth, provider),
+            )
+          }
+        : undefined,
+    }
+  }
+  if (baseHook.provider) {
+    aliasHook.provider = {
+      ...baseHook.provider,
+      id: providerID,
+      models: baseHook.provider.models
+        ? async (provider, ctx) => {
+            return aliasContext.run({ providerID, baseProviderID }, () =>
+              baseHook.provider!.models!(provider, ctx),
+            )
+          }
+        : undefined,
+    }
+  }
+  const chatHeaders = baseHook["chat.headers"]
+  if (chatHeaders) {
+    aliasHook["chat.headers"] = async (input, output) => {
+      const mappedInput = {
+        ...input,
+        model: input.model ? { ...input.model, providerID: baseProviderID } : input.model,
+      }
+      return chatHeaders(mappedInput as any, output)
+    }
+  }
+  const chatParams = baseHook["chat.params"]
+  if (chatParams) {
+    aliasHook["chat.params"] = async (input, output) => {
+      const mappedInput = {
+        ...input,
+        model: input.model ? { ...input.model, providerID: baseProviderID } : input.model,
+      }
+      return chatParams(mappedInput as any, output)
+    }
+  }
+  const systemTransform = baseHook["experimental.chat.system.transform"]
+  if (systemTransform) {
+    aliasHook["experimental.chat.system.transform"] = async (input, output) => {
+      const mappedInput = {
+        ...input,
+        model: input.model ? { ...input.model, providerID: baseProviderID } : input.model,
+      }
+      return systemTransform(mappedInput as any, output)
+    }
+  }
+  return aliasHook
+}
+
+
 const layer = Layer.effect(
   Service,
   Effect.gen(function* () {
@@ -162,12 +258,14 @@ const layer = Layer.effect(
         const { Server } = yield* Effect.promise(() => import("../server/server"))
 
         const serverUrl = Server.url
-        const client = createOpencodeClient({
+        const baseClient = createOpencodeClient({
           baseUrl: serverUrl?.toString() ?? "http://localhost:4096",
           directory: ctx.directory,
           headers: ServerAuth.headers(),
           ...(serverUrl ? {} : { fetch: async (...args) => Server.Default().app.fetch(...args) }),
         })
+        // #114：SDK client 经 alias 代理，auth.set 写到真实 alias id
+        const client = wrapClientForAlias(baseClient)
         const cfg = yield* config.get()
         const input: PluginInput = {
           client,
@@ -260,6 +358,22 @@ const layer = Layer.effect(
           )
         }
 
+        // #114：为 extends 别名生成独立 hook（多账号隔离）
+        // #114：在 config notify 前 push，确保 alias 也收到 config hook
+        const baseProviderMap = buildBaseProviderMap(cfg.provider ?? {})
+        const aliasHooks: Hooks[] = []
+        for (const baseHook of hooks) {
+          // #114：仅对带 auth.provider 的基类 hook 生成别名（oauth 插件主路径）
+          const baseAuthProvider = baseHook.auth?.provider
+          if (!baseAuthProvider) continue
+          for (const [aliasID, baseType] of Object.entries(baseProviderMap)) {
+            if (baseType !== baseAuthProvider) continue
+            aliasHooks.push(createAliasHook(baseHook, aliasID, baseType))
+          }
+        }
+        // #114：先收集再 push，避免遍历 hooks 时修改同一数组
+        for (const aliasHook of aliasHooks) hooks.push(aliasHook)
+
         // Notify plugins of current config
         for (const hook of hooks) {
           yield* Effect.tryPromise({
diff --git a/packages/opencode/src/agent/agent.ts b/packages/opencode/src/agent/agent.ts
index 82f9b5d60a..5a1c1332ae 100644
--- a/packages/opencode/src/agent/agent.ts
+++ b/packages/opencode/src/agent/agent.ts
@@ -31,6 +31,7 @@ import { LocationServiceMap, locationServiceMapLayer } from "@opencode-ai/core/l
 import { Reference } from "@opencode-ai/core/reference"
 import { Location } from "@opencode-ai/core/location"
 import { PluginV2 } from "@opencode-ai/core/plugin"
+import { isOpenaiOauthProvider, buildBaseProviderMap } from "@/provider/alias"
 
 export const Info = Schema.Struct({
   name: Schema.String,
@@ -436,7 +437,11 @@ const layer = Layer.effect(
 
         // TODO: clean this up so provider specific logic doesnt bleed over
         const authInfo = yield* auth.get(model.providerID).pipe(Effect.orDie)
-        const isOpenaiOauth = model.providerID === "openai" && authInfo?.type === "oauth"
+        // #114：extends openai 的别名同样走 oauth 系统提示剥离
+        // #114：map 来自当前 instance cfg，避免跨项目串号
+        const oauthBaseMap = buildBaseProviderMap(cfg.provider ?? {})
+        // #114：仅 oauth 凭证触发；api key 账号保持原 system 注入
+        const isOpenaiOauth = isOpenaiOauthProvider(model.providerID, oauthBaseMap) && authInfo?.type === "oauth"
 
         const params = {
           experimental_telemetry: {
diff --git a/packages/opencode/src/session/llm/request.ts b/packages/opencode/src/session/llm/request.ts
index 799954eb41..38eda6b8f1 100644
--- a/packages/opencode/src/session/llm/request.ts
+++ b/packages/opencode/src/session/llm/request.ts
@@ -15,6 +15,7 @@ import { Effect, Record } from "effect"
 import { jsonSchema, tool as aiTool, type ModelMessage, type Tool } from "ai"
 import type { Plugin } from "@/plugin"
 import { mergeDeep } from "remeda"
+import { isOpenaiOauthByModelNpm } from "@/provider/alias"
 
 const USER_AGENT = `opencode/${InstallationVersion}`
 
@@ -55,7 +56,11 @@ const mergeOptions = (target: Record<string, any>, source: Record<string, any> |
   mergeDeep(target, source ?? {}) as Record<string, any>
 
 export const prepare = Effect.fn("LLMRequestPrep.prepare")(function* (input: PrepareInput) {
-  const isOpenaiOauth = input.provider.id === "openai" && input.auth?.type === "oauth"
+  // #114：别名 openai 用 npm 识别 oauth 指令路径（prepare 无 config map）
+  // #114：instructions 字段与 system 消息互斥，误判会重复注入
+  const isOpenaiOauth =
+    input.auth?.type === "oauth" &&
+    isOpenaiOauthByModelNpm(input.provider.id, input.model.api.npm)
   const system = [
     [
       ...(input.agent.prompt ? [input.agent.prompt] : SystemPrompt.provider(input.model)),
diff --git a/packages/opencode/src/session/llm/native-runtime.ts b/packages/opencode/src/session/llm/native-runtime.ts
index bac385c591..2965b8ed76 100644
--- a/packages/opencode/src/session/llm/native-runtime.ts
+++ b/packages/opencode/src/session/llm/native-runtime.ts
@@ -18,6 +18,7 @@ import {
 } from "@opencode-ai/llm"
 import type { LLMClientShape } from "@opencode-ai/llm/route"
 import { LLMNative } from "./native-request"
+import { isOpenaiOauthByModelNpm } from "@/provider/alias"
 
 export type RuntimeStatus =
   | { readonly type: "supported"; readonly apiKey: string; readonly baseURL?: string }
@@ -57,7 +58,11 @@ function statusWithFetch(
   const npm = input.model.api.npm
   if (npm !== "@ai-sdk/openai" && npm !== "@ai-sdk/openai-compatible" && npm !== "@ai-sdk/anthropic")
     return { type: "unsupported", reason: "provider package is not OpenAI, OpenAI-compatible, or Anthropic" }
-  if (input.auth?.type === "oauth" && !(input.provider.id === "openai" && fetch)) {
+  // #114：oauth + openai(含别名 npm) + fetch 才支持 native
+  if (
+    input.auth?.type === "oauth" &&
+    !(isOpenaiOauthByModelNpm(input.provider.id, input.model.api.npm) && fetch)
+  ) {
     return { type: "unsupported", reason: "OAuth auth requires a provider fetch override" }
   }
 
@@ -146,7 +151,12 @@ export function stream(input: StreamInput): StreamResult {
 }
 
 function providerFetch(input: Pick<StreamInput, "provider" | "auth">): typeof globalThis.fetch | undefined {
-  if (input.provider.id !== "openai" || input.auth?.type !== "oauth") return undefined
+  // #114：openai 别名 oauth：id 为 openai 或模型 npm 为 @ai-sdk/openai
+  // #114：providerFetch 无 model 参数，只能扫 provider.models 推断 npm
+  if (input.auth?.type !== "oauth") return undefined
+  const npmHit = Object.values(input.provider.models ?? {}).some((m) => m.api?.npm === "@ai-sdk/openai")
+  // #114：非 openai id 且无 openai npm 模型则不是目标别名
+  if (input.provider.id !== "openai" && !npmHit) return undefined
   const value: unknown = input.provider.options.fetch
   if (typeof value !== "function") return undefined
   return value as typeof globalThis.fetch
diff --git a/packages/opencode/test/provider/provider.test.ts b/packages/opencode/test/provider/provider.test.ts
index 2c139cc635..3d4e079073 100644
--- a/packages/opencode/test/provider/provider.test.ts
+++ b/packages/opencode/test/provider/provider.test.ts
@@ -2111,3 +2111,127 @@ it.effect("opencode loader keeps paid models when auth exists", () =>
     expect(keyedCount).toBeGreaterThan(0)
   }).pipe(provideMultiInstance),
 )
+
+// #114：别名映射与 oauth 判定（不依赖网络）
+// #114：纯函数单测，保证 buildBaseProviderMap 契约不回退
+test("provider alias map resolves extends and openai oauth", async () => {
+  const { buildBaseProviderMap, isOpenaiOauthProvider, isOpenaiOauthByModelNpm } = await import("../../src/provider/alias")
+  const map = buildBaseProviderMap({
+    OpenAIA: { extends: "openai" },
+    GeminiA: { extends: "google" },
+    plain: {},
+  })
+  expect(map.OpenAIA).toBe("openai")
+  expect(map.GeminiA).toBe("google")
+  expect(map.plain).toBeUndefined()
+  expect(isOpenaiOauthProvider("openai", map)).toBe(true)
+  expect(isOpenaiOauthProvider("OpenAIA", map)).toBe(true)
+  expect(isOpenaiOauthProvider("GeminiA", map)).toBe(false)
+  expect(isOpenaiOauthByModelNpm("OpenAIA", "@ai-sdk/openai")).toBe(true)
+  expect(isOpenaiOauthByModelNpm("other", "@ai-sdk/anthropic")).toBe(false)
+})
+
+it.instance(
+  "provider extending openai inherits models and npm",
+  Effect.gen(function* () {
+    const providers = yield* list
+    const a = ProviderV2.ID.make("OpenAIA")
+    const provider = providers[a]
+    expect(provider).not.toBeUndefined()
+    expect(provider.name).toBe("OpenAI Account A")
+    expect(provider.options.baseURL).toBe("https://custom-openai.example.com/v1")
+    const gptModel = Object.values(provider.models).find((m) => m.api.id.includes("gpt") || m.api.npm === "@ai-sdk/openai")
+    // #114：别名应继承 openai npm 与独立 providerID
+    // #114：name/baseURL 来自 alias 配置，不可回落到基类展示名
+    expect(gptModel).not.toBeUndefined()
+    expect(gptModel?.api.npm).toBe("@ai-sdk/openai")
+    expect(gptModel?.providerID).toBe(a)
+  }),
+  {
+    config: {
+      provider: {
+        OpenAIA: {
+          extends: "openai",
+          name: "OpenAI Account A",
+          options: {
+            baseURL: "https://custom-openai.example.com/v1",
+            apiKey: "test-key",
+          },
+        },
+      },
+    },
+  },
+)
+
+it.instance(
+  "alias providers appear as independent list entries",
+  Effect.gen(function* () {
+    const providers = yield* list
+    const a = ProviderV2.ID.make("OpenAIA")
+    const b = ProviderV2.ID.make("OpenAIB")
+    expect(providers[a]).not.toBeUndefined()
+    expect(providers[b]).not.toBeUndefined()
+    expect(a).not.toBe(b)
+    // #114：两个 openai 别名各自独立，不共享同一 provider 键
+    expect(Object.keys(providers).filter((k) => k.startsWith("OpenAI")).length).toBe(2)
+  }),
+  {
+    config: {
+      provider: {
+        OpenAIA: { extends: "openai", options: { apiKey: "a" } },
+        OpenAIB: { extends: "openai", options: { apiKey: "b" } },
+      },
+    },
+  },
+)
+
+// #114：google 多账号 extends 集成测（original 源测试适配 tip harness）
+// #114：验证 projectId/options 与 providerID 不串号
+it.instance(
+  "provider extending google inherits models and npm",
+  Effect.gen(function* () {
+    const providers = yield* list
+    const a = ProviderV2.ID.make("GeminiA")
+    const b = ProviderV2.ID.make("GeminiB")
+    expect(providers[a]).not.toBeUndefined()
+    expect(providers[a].name).toBe("Gemini Account A")
+    expect(providers[a].options.projectId).toBe("project-a")
+    expect(providers[b]).not.toBeUndefined()
+    expect(providers[b].name).toBe("Gemini Account B")
+    expect(providers[b].options.projectId).toBe("project-b")
+    const geminiModelA = Object.values(providers[a].models).find((m) => m.api.id.includes("gemini") || m.api.npm === "@ai-sdk/google")
+    // #114：google 别名继承 npm 且 providerID 独立
+    // #114：A/B 各自 models 归属不可落到基类 google id
+    expect(geminiModelA).not.toBeUndefined()
+    expect(geminiModelA?.api.npm).toBe("@ai-sdk/google")
+    expect(geminiModelA?.providerID).toBe(a)
+    const geminiModelB = Object.values(providers[b].models).find((m) => m.api.id.includes("gemini") || m.api.npm === "@ai-sdk/google")
+    expect(geminiModelB).not.toBeUndefined()
+    expect(geminiModelB?.api.npm).toBe("@ai-sdk/google")
+    expect(geminiModelB?.providerID).toBe(b)
+  }),
+  {
+    config: {
+      provider: {
+        GeminiA: {
+          extends: "google",
+          name: "Gemini Account A",
+          options: {
+            projectId: "project-a",
+            apiKey: "test-key-a",
+          },
+        },
+        // #114：第二 google 别名，证明列表键与 options 独立
+        GeminiB: {
+          extends: "google",
+          name: "Gemini Account B",
+          options: {
+            projectId: "project-b",
+            apiKey: "test-key-b",
+          },
+        },
+      },
+    },
+  },
+)
+
