diff --git a/packages/tui/src/parsers-config.ts b/packages/tui/src/parsers-config.ts
--- a/packages/tui/src/parsers-config.ts
+++ b/packages/tui/src/parsers-config.ts
@@ -70,6 +70,11 @@ export default {
     },
     {
       filetype: "bash",
+      // TUI 的文件路径映射沿用 VS Code/LSP 的 `shellscript` 命名，不能为了高亮
+      // 把全局 language id 改成 `bash`，否则会影响 LSP 兼容性。这里仅在
+      // OpenTUI parser 层把常见 shell info-string/扩展名别名收敛到已有 bash
+      // parser，保持行为影响范围只限终端代码高亮，不新增下载项或运行依赖。
+      aliases: ["shellscript", "shell", "sh", "zsh", "ksh"],
       wasm: "https://github.com/tree-sitter/tree-sitter-bash/releases/download/v0.25.0/tree-sitter-bash.wasm",
       queries: {
         highlights: [
diff --git a/packages/opencode/src/lsp/language.ts b/packages/opencode/src/lsp/language.ts
--- a/packages/opencode/src/lsp/language.ts
+++ b/packages/opencode/src/lsp/language.ts
@@ -114,6 +114,10 @@ export const LANGUAGE_EXTENSIONS: Record<string, string> = {
   ".mli": "ocaml",
   ".tf": "terraform",
   ".tfvars": "terraform-vars",
+  // #245：该表供 LSP 客户端按扩展名选择 language server，toml 缺席会让 TOML 文件
+  // `toml` 是 VS Code 与 OpenTUI parser 共用的标准 language id；这里只补
+  // 已注册 parser 的文件扩展名入口，不改变其他语言的 LSP 语义，也不新增依赖。
+  ".toml": "toml",
   ".hcl": "hcl",
   ".nix": "nix",
   ".typ": "typst",
diff --git a/packages/tui/src/util/filetype.ts b/packages/tui/src/util/filetype.ts
--- a/packages/tui/src/util/filetype.ts
+++ b/packages/tui/src/util/filetype.ts
@@ -117,5 +117,8 @@
   ".tf": "terraform",
   ".tfvars": "terraform-vars",
+  // #245：TUI 高亮与 LSP 是两张独立扩展名表，源树单表结构下 toml 条目隐含共享；
+  // 目标双表拆分后必须在此显式补入口，否则 edit diff 渲染 .toml 时 filetype() 返回 undefined
+  ".toml": "toml",
   ".hcl": "hcl",
   ".nix": "nix",
   ".typ": "typst",
diff --git a/packages/tui/test/filetype.test.ts b/packages/tui/test/filetype.test.ts
new file mode 100644
--- /dev/null
+++ b/packages/tui/test/filetype.test.ts
@@ -0,0 +1,32 @@
+import { expect, test } from "bun:test"
+import { filetype } from "../src/util/filetype"
+import parsersConfig from "../src/parsers-config"
+
+test("shellscript extensions resolve to the shellscript language id", () => {
+  // #245：.sh 家族在 LSP 语义上保持 shellscript 语言 id，TUI 高亮由 bash parser
+  // 的 aliases 承接；锁定扩展名→语言 id 映射，防止被改成 bash 而破坏 LSP 兼容
+  expect(filetype("scripts/install.sh")).toBe("shellscript")
+  expect(filetype("scripts/build.zsh")).toBe("shellscript")
+  expect(filetype("scripts/run.ksh")).toBe("shellscript")
+})
+
+test("toml extension resolves to the registered toml parser", () => {
+  // #245：修复前 .toml 没有扩展名映射，parser 已注册也无法被 edit diff 使用；
+  // 该断言锁定扩展名入口，防止未来只保留 parser 配置却丢失映射时测试误通过
+  expect(filetype("config/opencode.toml")).toBe("toml")
+  const toml = parsersConfig.parsers.find((parser) => parser.filetype === "toml")
+  expect(toml).toBeDefined()
+})
+
+test("bash parser aliases absorb shellscript family names", () => {
+  // #245：aliases 是 TUI info-string/语言名收敛到已有 bash parser 的唯一通道，
+  // 缺失任一别名都会让对应 shell 变体的 diff 高亮静默退化为纯文本
+  const bash = parsersConfig.parsers.find((parser) => parser.filetype === "bash")
+  expect(Boolean(bash && "aliases" in bash)).toBe(true)
+  if (!bash || !("aliases" in bash)) return
+  expect(bash.aliases).toContain("shellscript")
+  expect(bash.aliases).toContain("shell")
+  expect(bash.aliases).toContain("sh")
+  expect(bash.aliases).toContain("zsh")
+  expect(bash.aliases).toContain("ksh")
+})
