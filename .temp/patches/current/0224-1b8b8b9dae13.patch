diff --git a/packages/opencode/src/plugin/vscode-bridge.ts b/packages/opencode/src/plugin/vscode-bridge.ts
index c5d9253352..aa5dd1fe60 100644
--- a/packages/opencode/src/plugin/vscode-bridge.ts
+++ b/packages/opencode/src/plugin/vscode-bridge.ts
@@ -2,10 +2,21 @@
 // Plugin 入口：注册 notebook bridge 工具并转发到 ide/vscode-bridge 客户端
 import { Effect } from "effect"
 import { tool, type ToolContext, type PluginInput, type Hooks } from "@opencode-ai/plugin"
+import { createTwoFilesPatch } from "diff"
 import { z } from "zod"
 import * as VscodeBridge from "@/ide/vscode-bridge"
 import { VscodeNotebookDescriptions } from "./vscode-bridge-descriptions"
 
+// #224：Notebook 工具是 plugin tool，不能直接复用内置 edit/write 的专用 renderer。
+// 这个 fork-local metadata key 是 TUI 和 VS Code bridge 之间的内部展示契约：
+// 只让 generic renderer 选择性升级展示，不扩大 public plugin API，也不为每个
+// vscode_notebook_* 工具在 session/index.tsx 增加分支。
+const NOTEBOOK_METADATA_KEY = "vscodeNotebook"
+// #224：notebook edit diff 会持久化到 session part metadata；16KB 与现有工具输出
+// 默认内联上限保持同一量级，超过后只保留统计和摘要，避免大 cell/full replace
+// 把 notebook 源码整段复制进会话状态。
+const NOTEBOOK_DIFF_METADATA_MAX_CHARS = 16 * 1024
+
 const requiredFilePath = {
   filePath: z
     .string()
@@ -91,13 +102,14 @@ const editArgs = {
     .describe("Cell language. Use 'markdown' for Markdown cells; use 'python' or another language for code cells. On edit, language alone changes cell kind/language while preserving source."),
 }
 
-async function call(
+async function callRaw(
   endpoint: string,
   args: Record<string, unknown>,
   context: Pick<ToolContext, "directory" | "abort">,
   timeoutMs?: number,
 ) {
-  const value = await VscodeBridge.callBridge({
+  // #224：返回原始 bridge value，notebookResult 从中派生 summary + compact metadata
+  return await VscodeBridge.callBridge({
     cwd: context.directory,
     path: endpoint,
     body: args,
@@ -105,7 +117,213 @@ async function call(
     signal: context.abort,
     timeoutMs,
   })
-  return VscodeBridge.summaryOnly(value)
+}
+
+// #224：从 bridge 原始返回值派生 output summary + vscodeNotebook metadata；
+// metadata 只包含 TUI 展示需要的 compact 字段，不复制完整源码/artifact 文本
+function notebookResult(endpoint: string, args: Record<string, unknown>, value: unknown) {
+  const view = notebookView(endpoint, args, value)
+  return {
+    output: VscodeBridge.summaryOnly(value),
+    metadata: {
+      endpoint,
+      ...(view && { [NOTEBOOK_METADATA_KEY]: view }),
+    },
+  }
+}
+
+// #224：按 endpoint 分派到具体的 view 构建函数；每个 view 只保留 TUI 需要的字段
+function notebookView(endpoint: string, args: Record<string, unknown>, value: unknown) {
+  const data = recordValue(recordValue(value).data)
+  const path = stringValue(data.path) ?? stringValue(args.filePath)
+  // #224：summary view——cells 列表 + dirty/runtime 状态
+  if (endpoint === "/notebook/summary") {
+    return { view: "summary", path, dirty: booleanValue(data.dirty), runtime: stringValue(data.runtime), cells: compactCells(data.cells) }
+  }
+  // #224：source view——返回的行数/截断信息，不含源码文本
+  if (endpoint === "/notebook/source") {
+    return {
+      view: "source",
+      path,
+      target: stringValue(data.target),
+      cellId: stringValue(data.cellId) ?? stringValue(args.cellId),
+      returned: numberValue(data.returned),
+      totalLines: numberValue(data.totalLines),
+      truncated: booleanValue(data.truncated),
+    }
+  }
+  // #224：run view——cells 执行结果 + artifacts
+  if (endpoint === "/notebook/run") {
+    return {
+      view: "run",
+      path,
+      dirty: booleanValue(data.dirty),
+      runtime: stringValue(data.runtime),
+      target: stringValue(data.target),
+      completed: booleanValue(data.completed),
+      stoppedAt: numberValue(data.stoppedAt),
+      cells: compactRunCells(data.cells),
+    }
+  }
+  // #224：output view——单个 cell 的 artifacts 列表
+  if (endpoint === "/notebook/output") {
+    return {
+      view: "output",
+      path,
+      dirty: booleanValue(data.dirty),
+      runtime: stringValue(data.runtime),
+      cell: compactCell(data.cell),
+      artifacts: compactArtifacts(data.artifacts),
+    }
+  }
+  // #224：env view——操作状态 + guidance 文案
+  if (endpoint === "/notebook/env") {
+    return notebookEnvView(path, data)
+  }
+  // #224：edit view——从 before/after 源码生成 diff
+  if (endpoint === "/notebook/edit") {
+    return notebookEditView(path, args, data)
+  }
+}
+
+// #224：notebook edit view——从 beforeSource/afterSource 生成 unified diff，
+// #224：超过 16KB 只保留统计和 diffOmitted 标记
+function notebookEditView(path: string | undefined, args: Record<string, unknown>, data: Record<string, unknown>) {
+  const before = stringValue(data.beforeSource)
+  const after = stringValue(data.afterSource)
+  // #224：createTwoFilesPatch 生成 unified diff——两路径用 cell index + language 标记
+  const fullDiff = before !== undefined && after !== undefined && before !== after
+    ? createTwoFilesPatch(notebookDiffPath(path, data, "old"), notebookDiffPath(path, data, "new"), before, after)
+    : undefined
+  const stats = diffStats(fullDiff ?? "")
+  // #224：diff 超过 16KB 只保留统计，diff 设为 undefined，diffOmitted 标记 "too-large"
+  const diff = fullDiff && fullDiff.length <= NOTEBOOK_DIFF_METADATA_MAX_CHARS ? fullDiff : undefined
+  const cellIndex = numberValue(data.affectedCellIndex) ?? numberValue(data.anchorCellIndex) ?? numberValue(data.deletedCellIndex)
+  return {
+    view: "edit",
+    path,
+    cellLabel: cellIndex === undefined ? undefined : `c${cellIndex + 1}`,
+    cellId: stringValue(args.cellId),
+    editType: stringValue(data.editType) ?? stringValue(args.editType),
+    cellCountBefore: numberValue(data.cellCountBefore),
+    cellCountAfter: numberValue(data.cellCountAfter),
+    dirty: booleanValue(data.dirty),
+    kind: stringValue(data.kind),
+    language: stringValue(data.language),
+    diff,
+    diffOmitted: fullDiff !== undefined && diff === undefined ? "too-large" : undefined,
+    added: stats.added,
+    removed: stats.removed,
+  }
+}
+
+// #224：notebook env view——从 status 派生 guidance 文案，让 agent 知道下一步
+function notebookEnvView(path: string | undefined, data: Record<string, unknown>) {
+  const operation = stringValue(data.operation)
+  const status = stringValue(data.status)
+  // #224：guidance 从 status 派生——让 agent 知道下一步操作
+  const guidance = status === "configured"
+    ? "Notebook is ready for cell execution."
+    : status === "selected"
+      ? "Proceed to run cells — the kernel will start on first execution."
+      : status === "needs-selection"
+        ? "Select a kernel manually from the notebook toolbar, then call configure to verify."
+        : status === "selection-requested"
+          ? "Retry configure or select a kernel manually."
+          : status === "failed"
+            ? "Check Jupyter/Python extensions and workspace trust."
+            : undefined
+  return {
+    view: "env",
+    path,
+    operation,
+    status,
+    guidance,
+    runtime: recordValue(data.runtime),
+    saved: booleanValue(data.saved),
+    beforeDirty: booleanValue(data.beforeDirty),
+    afterDirty: booleanValue(data.afterDirty),
+  }
+}
+
+// #224：diff 路径标签——用 path + cell index + language 组成虚拟文件名，
+// 让 diff component 能选对语法高亮
+function notebookDiffPath(path: string | undefined, data: Record<string, unknown>, fallback: string) {
+  const cellIndex = numberValue(data.affectedCellIndex) ?? numberValue(data.anchorCellIndex) ?? numberValue(data.deletedCellIndex)
+  const language = stringValue(data.language)
+  return [path ?? "notebook", cellIndex === undefined ? undefined : `#c${cellIndex + 1}`, language ? `.${language}` : undefined]
+    .filter((item): item is string => Boolean(item))
+    .join("") || fallback
+}
+
+// #224：run cells 压缩——每个 cell 追加 artifacts 列表
+function compactRunCells(value: unknown) {
+  return arrayValue(value).map((item) => ({ ...compactCell(item), artifacts: compactArtifacts(recordValue(item).artifacts) }))
+}
+
+// #224：summary cells 压缩——只保留 TUI 需要的字段
+function compactCells(value: unknown) {
+  return arrayValue(value).map(compactCell)
+}
+
+// #224：只保留 TUI 需要的 cell 字段——i/id/kind/lang/lines/exec/existing_outs/first
+function compactCell(value: unknown) {
+  const item = recordValue(value)
+  return {
+    i: numberValue(item.i),
+    id: stringValue(item.id),
+    kind: stringValue(item.kind),
+    lang: stringValue(item.lang),
+    lines: numberValue(item.lines),
+    exec: stringValue(item.exec),
+    existing_outs: arrayValue(item.existing_outs).filter((entry): entry is string => typeof entry === "string"),
+    first: stringValue(item.first),
+  }
+}
+
+// #224：artifact 只保留 mime/bytes/preview/artifactPath——text 字段可能是大 stdout/json/blob，
+// 不复制进 session part metadata，完整内容仍在 bridge 返回的 artifact 文件中
+function compactArtifacts(value: unknown) {
+  return arrayValue(value).map((entry) => {
+    const item = recordValue(entry)
+    return {
+      mime: stringValue(item.mime),
+      bytes: numberValue(item.bytes),
+      preview: stringValue(item.preview),
+      artifactPath: stringValue(item.artifactPath),
+    }
+  })
+}
+
+// #224：从 diff 文本统计 added/removed 行数
+function diffStats(diff: string) {
+  const lines = diff.split("\n")
+  return {
+    added: lines.filter((line) => line.startsWith("+") && !line.startsWith("+++")).length,
+    removed: lines.filter((line) => line.startsWith("-") && !line.startsWith("---")).length,
+  }
+}
+
+function recordValue(value: unknown): Record<string, unknown> {
+  if (!value || typeof value !== "object" || Array.isArray(value)) return {}
+  return value as Record<string, unknown>
+}
+
+// #224：类型安全守卫——非数组返回空数组
+function arrayValue(value: unknown): unknown[] {
+  return Array.isArray(value) ? value : []
+}
+
+function stringValue(value: unknown) {
+  return typeof value === "string" ? value : undefined
+}
+
+function numberValue(value: unknown) {
+  return typeof value === "number" && Number.isFinite(value) ? value : undefined
+}
+
+function booleanValue(value: unknown) {
+  return typeof value === "boolean" ? value : undefined
 }
 
 // ask 适配：V2 的 ToolContext.ask 返回 Promise<void>，替代 V1 的 Effect.runPromise
@@ -129,18 +347,12 @@ export async function VscodeBridgePlugin(input: PluginInput): Promise<Hooks> {
     vscode_notebook_summary: tool({
       description: VscodeNotebookDescriptions.summary,
       args: requiredFilePath,
-      execute: async (args, context) => ({
-        output: await call("/notebook/summary", args, context, 10_000),
-        metadata: { endpoint: "/notebook/summary" },
-      }),
+      execute: async (args, context) => notebookResult("/notebook/summary", args, await callRaw("/notebook/summary", args, context, 10_000)),
     }),
     vscode_notebook_source: tool({
       description: VscodeNotebookDescriptions.source,
       args: sourceArgs,
-      execute: async (args, context) => ({
-        output: await call("/notebook/source", args, context, 10_000),
-        metadata: { endpoint: "/notebook/source" },
-      }),
+      execute: async (args, context) => notebookResult("/notebook/source", args, await callRaw("/notebook/source", args, context, 10_000)),
     }),
     vscode_notebook_run: tool({
       description: VscodeNotebookDescriptions.run,
@@ -148,21 +360,14 @@ export async function VscodeBridgePlugin(input: PluginInput): Promise<Hooks> {
       execute: async (args, context) => {
         await ask(context, "vscode_notebook_run", args)
         const timeoutMs = args.timeoutMs ?? 300_000
-        return {
-          // run 工具描述：endCellId 为 range 信号，type 仅提示
-          output: await call("/notebook/run", { ...args, timeoutMs }, context, timeoutMs + 5_000),
-          metadata: { endpoint: "/notebook/run" },
-        }
+        const body = { ...args, timeoutMs }
+        return notebookResult("/notebook/run", body, await callRaw("/notebook/run", body, context, timeoutMs + 5_000))
       },
     }),
     vscode_notebook_output: tool({
       description: VscodeNotebookDescriptions.output,
       args: outputArgs,
-      execute: async (args, context) => ({
-        // 30s 覆盖 activate+restart；短于通用工具会误超时
-        output: await call("/notebook/output", args, context, 30_000),
-        metadata: { endpoint: "/notebook/output" },
-      }),
+      execute: async (args, context) => notebookResult("/notebook/output", args, await callRaw("/notebook/output", args, context, 30_000)),
     }),
     vscode_notebook_edit: tool({
       description: VscodeNotebookDescriptions.edit,
@@ -176,10 +381,7 @@ export async function VscodeBridgePlugin(input: PluginInput): Promise<Hooks> {
         // notebook cell 修改也不能绕过。顺序先专属后通用，确保更精确的
         // vscode_notebook_edit deny/ask 先表达，再由 edit 维持历史兼容边界。
         await ask(context, "edit", args)
-        return {
-          output: await call("/notebook/edit", args, context, 30_000),
-          metadata: { endpoint: "/notebook/edit" },
-        }
+        return notebookResult("/notebook/edit", args, await callRaw("/notebook/edit", args, context, 30_000))
       },
     }),
     vscode_notebook_env: tool({
@@ -200,11 +402,7 @@ export async function VscodeBridgePlugin(input: PluginInput): Promise<Hooks> {
         await ask(context, "vscode_notebook_env", args)
         // SMARK 统一 120s：configure 选核 UI + restart 不可用短窗
         const timeout = 120_000
-        return {
-          output: await call("/notebook/env", args, context, timeout),
-          // metadata 带回 operation，便于 agent 对账
-          metadata: { endpoint: "/notebook/env", operation: args.operation },
-        }
+        return notebookResult("/notebook/env", args, await callRaw("/notebook/env", args, context, timeout))
       },
     }),
 
diff --git a/packages/opencode/test/plugin/vscode-bridge.test.ts b/packages/opencode/test/plugin/vscode-bridge.test.ts
index 7af6e6361f..c54a6c10cf 100644
--- a/packages/opencode/test/plugin/vscode-bridge.test.ts
+++ b/packages/opencode/test/plugin/vscode-bridge.test.ts
@@ -1,25 +1,23 @@
-import { describe, expect, test } from "bun:test"
+import { afterEach, describe, expect, mock, spyOn, test } from "bun:test"
 import type { ToolContext, PluginInput } from "@opencode-ai/plugin"
 import { VscodeBridgePlugin } from "@/plugin/vscode-bridge"
+import * as VscodeBridge from "@/ide/vscode-bridge"
+
+afterEach(() => {
+  mock.restore()
+})
 
 // #223：V2 的 ToolContext.ask 返回 Promise<void>，替代 V1 的 Effect.runPromise。
 // VscodeBridgePlugin 需要 PluginInput 参数，但函数体不使用 input，测试只验证
 // tool 定义和 execute 行为，因此传入最小 mock。
-// #223：插件在模块加载时就初始化 tool map——V2 的 tool() 工厂返回静态定义，
-// 不依赖 Effect runtime，所以顶层 await 安全
 const plugin = await VscodeBridgePlugin({} as unknown as PluginInput)
 
 describe("vscode bridge plugin", () => {
-  // #223：验证三个 notebook 变更工具在调用 bridge 前都经过各自专属权限 key；
-  // 如果权限层拒绝，execute 必须在本地终止，不能继续发起到 127.0.0.1 的 HTTP 调用。
   test("checks notebook mutation permissions before calling the VS Code bridge", async () => {
     const calls: Array<{ permission: string; patterns: string[]; always: string[]; metadata: Record<string, unknown> }> = []
     const stop = "stopped after permission check"
     const filePath = "F:\\project with spaces\\demo.ipynb"
 
-    // #223：mock context 的 ask 记录每次权限调用的 key/patterns/always/metadata，
-    // 然后立即抛错阻止 execute 继续执行——这样测试只验证权限顺序，不依赖真实 bridge。
-    // #223：directory/worktree 设为含空格的 Windows 路径，验证 patterns 不被分词
     const context = (): ToolContext => ({
       sessionID: "ses_test",
       messageID: "msg_test",
@@ -40,11 +38,8 @@ describe("vscode bridge plugin", () => {
       },
     })
 
-    // #223：run 工具先经过 vscode_notebook_run 权限——这是 #178 已有的 ask，
-    // 测试中 ask 立即抛错，所以 execute 不会到达 call("/notebook/run", ...)
-    // #223：timeoutMs 设为 1_000 确保即使 ask 意外放行，bridge 调用也快速超时
     await expect(
-      plugin.tool.vscode_notebook_run.execute(
+      plugin.tool!.vscode_notebook_run.execute(
         {
           filePath,
           cellId: "#VSC-12345678",
@@ -54,10 +49,8 @@ describe("vscode bridge plugin", () => {
       ),
     ).rejects.toThrow(stop)
 
-    // #223：edit 工具先经过 vscode_notebook_edit 专属权限，再经过通用 edit 权限；
-    // 专属 ask 抛错后不会到达 edit ask，验证专属门禁的优先级
     await expect(
-      plugin.tool.vscode_notebook_edit.execute(
+      plugin.tool!.vscode_notebook_edit.execute(
         {
           filePath,
           cellId: "#VSC-12345678",
@@ -69,10 +62,8 @@ describe("vscode bridge plugin", () => {
       ),
     ).rejects.toThrow(stop)
 
-    // #223：env 工具经过 vscode_notebook_env 专属权限——configure/restart/save
-    // 都会改变 Jupyter/VS Code 状态，不能用通用 edit 覆盖
     await expect(
-      plugin.tool.vscode_notebook_env.execute(
+      plugin.tool!.vscode_notebook_env.execute(
         {
           filePath,
           operation: "restart",
@@ -82,34 +73,25 @@ describe("vscode bridge plugin", () => {
       ),
     ).rejects.toThrow(stop)
 
-    // #223：三个工具各自只触发一个 ask（第一个 ask 就抛错），验证专属权限 key 顺序
     expect(calls.map((call) => call.permission)).toEqual([
       "vscode_notebook_run",
       "vscode_notebook_edit",
       "vscode_notebook_env",
     ])
-    // #223：patterns/always 绑定到 filePath——含空格的 Windows 路径也必须原样传递，
-    // 不能被 shell 分词或 URL 编码破坏
     for (const call of calls) {
       expect(call.patterns).toEqual([filePath])
       expect(call.always).toEqual([filePath])
-      // #223：metadata.args 保留原始参数，供权限 UI 展示操作详情
-      // #223：含空格路径不会被截断——patterns 和 metadata 同时验证
       expect(call.metadata.args).toMatchObject({ filePath })
     }
   })
 
-  // #223：验证 notebook edit 的双层权限顺序——先 vscode_notebook_edit 后 edit；
-  // 如果 agent 配置只 deny 了 edit 而允许 vscode_notebook_edit，执行仍会在 edit 门禁停止
   test("keeps notebook edits covered by the generic edit permission gate", async () => {
     const filePath = "F:\\project with spaces\\demo.ipynb"
     const stop = "generic edit denied"
     const calls: string[] = []
 
-    // #223：mock ask 只在 permission === "edit" 时抛错，vscode_notebook_edit 放行；
-    // 这样可以验证两个 ask 都被调用，且顺序是先专属后通用
     await expect(
-      plugin.tool.vscode_notebook_edit.execute(
+     plugin.tool!.vscode_notebook_edit.execute(
         {
           filePath,
           cellId: "#VSC-12345678",
@@ -125,7 +107,7 @@ describe("vscode bridge plugin", () => {
           worktree: "F:\\project with spaces",
           abort: new AbortController().signal,
           metadata: () => undefined,
-          async ask(input) {
+          async ask(input: { permission: string; patterns: string[]; always: string[]; metadata: Record<string, unknown> }) {
             calls.push(input.permission)
             // #223：vscode_notebook_edit 通过后才在 edit 门禁拒绝，验证双层权限顺序
             if (input.permission === "edit") throw new Error(stop)
@@ -134,8 +116,235 @@ describe("vscode bridge plugin", () => {
       ),
     ).rejects.toThrow(stop)
 
-    // #223：calls 必须精确匹配 ["vscode_notebook_edit", "edit"]——先专属后通用
     expect(calls).toEqual(["vscode_notebook_edit", "edit"])
   })
+
+  // #224：验证 notebook edit 从 bridge 返回的 beforeSource/afterSource 生成 diff metadata
+  // #224：notebookResult 从 bridge data 的 before/after 生成 unified diff 并写入 metadata
+  test("returns notebook edit diff metadata from bridge before and after source", async () => {
+    const filePath = "F:\\project with spaces\\analysis notebook.ipynb"
+    // #224：mock callBridge 返回包含 beforeSource/afterSource 的 data，notebookResult 从中生成 diff
+    // #224：anchorCellIndex=2 对应 c3（0-based index + 1）
+    const bridge = spyOn(VscodeBridge, "callBridge").mockResolvedValue({
+      ran: false,
+      summary: "Notebook edit: applied=true op=edit at=3 num_cells=4->4 dirty=true.",
+      data: {
+        path: filePath,
+        editType: "edit",
+        cellCountBefore: 4,
+        cellCountAfter: 4,
+        anchorCellIndex: 2,
+        dirty: true,
+        kind: "code",
+        language: "python",
+        beforeSource: "old value\n",
+        afterSource: "new value\nrendered diff line\n",
+      },
+    })
+
+    const result = await plugin.tool!.vscode_notebook_edit.execute(
+      {
+        filePath,
+        cellId: "#VSC-12345678",
+        editType: "edit",
+        newCode: "RAW_ONLY_SHOULD_NOT_RENDER",
+      },
+      allowContext(filePath),
+    )
+
+    // #224：bridge 被调用时路径和 endpoint 正确
+    // #224：summary 从 bridge 返回的 summary 字段提取，不改变
+    // #224：callBridge 的 path 参数是 /notebook/edit，filePath 是含空格的完整路径
+    expect(bridge).toHaveBeenCalledWith(expect.objectContaining({ path: "/notebook/edit", filePath }))
+    expect(result).toMatchObject({
+      output: "Notebook edit: applied=true op=edit at=3 num_cells=4->4 dirty=true.",
+      metadata: {
+        endpoint: "/notebook/edit",
+        vscodeNotebook: {
+          view: "edit",
+          path: filePath,
+          cellLabel: "c3",
+          editType: "edit",
+          cellCountBefore: 4,
+          cellCountAfter: 4,
+          dirty: true,
+          language: "python",
+          added: 2,
+          removed: 1,
+        },
+      },
+    })
+    // #224：diff 内容包含 before/after 的关键行
+    // #224：createTwoFilesPatch 生成 unified diff，- 行是 beforeSource，+ 行是 afterSource
+    if (typeof result === "object" && result && "metadata" in result) {
+      const metadata = result.metadata as Record<string, unknown>
+      const nb = metadata.vscodeNotebook as Record<string, unknown>
+      expect(nb.diff as string).toContain("-old value")
+      expect(nb.diff as string).toContain("+rendered diff line")
+    }
+  })
+
+  // #224：验证 notebook output metadata 不包含完整 artifact text
+  // #224：artifact 的 text 字段可能是大 stdout，不复制进 session part metadata
+  test("keeps notebook output metadata compact by excluding full artifact text", async () => {
+    const filePath = "F:\\project with spaces\\analysis notebook.ipynb"
+    spyOn(VscodeBridge, "callBridge").mockResolvedValue({
+      ran: false,
+      summary: "Cell c3 id=#VSC-12345678 existing outputs: text.",
+      data: {
+        path: filePath,
+        dirty: false,
+        runtime: "Python 3.11",
+        cell: { i: 3, id: "#VSC-12345678", kind: "code", lang: "python", lines: 7, exec: "succeeded", existing_outs: ["text"], first: "df.head()" },
+        artifacts: [
+          {
+            mime: "text/plain",
+            bytes: 2048,
+            preview: "short preview",
+            text: "full text should not be persisted into metadata",
+            artifactPath: ".opencode/cache/notebook-outputs/analysis-cell-2-output-0-item-0.txt",
+          },
+        ],
+      },
+    })
+
+    const result = await plugin.tool!.vscode_notebook_output.execute(
+      { filePath, cellId: "#VSC-12345678" },
+      allowContext(filePath),
+    )
+
+    // #224：artifact 只保留 mime/bytes/preview/artifactPath，不包含 text
+    // #224：compactArtifacts 过滤掉了 text 字段，只保留 TUI 展示需要的元信息
+    // #224：artifactPath 指向 bridge 缓存的 artifact 文件，TUI 只展示路径不读内容
+    expect(result).toMatchObject({
+      metadata: {
+        vscodeNotebook: {
+          view: "output",
+          path: filePath,
+          artifacts: [
+            {
+              mime: "text/plain",
+              bytes: 2048,
+              preview: "short preview",
+              artifactPath: ".opencode/cache/notebook-outputs/analysis-cell-2-output-0-item-0.txt",
+            },
+          ],
+        },
+      },
+    })
+    // #224：序列化后的 metadata 不能包含完整 artifact text
+    // #224：这验证 compactArtifacts 确实排除了 text，不只是 toMatchObject 不检查它
+    // #224：JSON.stringify 会序列化整个 metadata，如果 text 泄漏会在这里被发现
+    expect(JSON.stringify(typeof result === "object" && result ? result.metadata : result)).not.toContain("full text should not be persisted")
+  })
+
+  // #224：验证 notebook insert 的 diff 标签使用插入位置的 cell index
+  // #224：insert 的 affectedCellIndex 从 0 开始，显示为 c4
+  test("labels notebook insert diff with the inserted cell index", async () => {
+    const filePath = "F:\\project with spaces\\analysis notebook.ipynb"
+    spyOn(VscodeBridge, "callBridge").mockResolvedValue({
+      ran: false,
+      summary: "Notebook edit: applied=true op=insert at=4 num_cells=3->4 dirty=true.",
+      data: {
+        path: filePath,
+        editType: "insert",
+        cellCountBefore: 3,
+        cellCountAfter: 4,
+        anchorCellIndex: 2,
+        affectedCellIndex: 3,
+        dirty: true,
+        language: "python",
+        beforeSource: "",
+        afterSource: "print('new cell')\n",
+      },
+    })
+
+    const result = await plugin.tool!.vscode_notebook_edit.execute(
+      { filePath, cellId: "#VSC-anchor", editType: "insert", newCode: "print('new cell')" },
+      allowContext(filePath),
+    )
+
+    // #224：insert 的 cellLabel 使用 affectedCellIndex（从 0 开始），显示为 c4
+    // #224：insert 前 3 cells，插入后 4 cells，affectedCellIndex=3 对应 c4
+    // #224：diff 路径中包含 #c4 标签，供语法高亮使用
+    expect(result).toMatchObject({
+      metadata: {
+        vscodeNotebook: {
+          view: "edit",
+          cellLabel: "c4",
+          added: 1,
+          removed: 0,
+        },
+      },
+    })
+    if (typeof result === "object" && result && "metadata" in result) {
+      const metadata = result.metadata as Record<string, unknown>
+      const nb = metadata.vscodeNotebook as Record<string, unknown>
+      expect(nb.diff as string).toContain("#c4")
+    }
+  })
+
+  // #224：验证超过 16KB 的 diff 不持久化到 metadata，只保留 diffOmitted 标记
+  // #224：NOTEBOOK_DIFF_METADATA_MAX_CHARS=16KB，超过后 diff 设为 undefined
+  test("omits oversized notebook edit diffs from persisted metadata", async () => {
+    const filePath = "F:\\project with spaces\\large notebook.ipynb"
+    spyOn(VscodeBridge, "callBridge").mockResolvedValue({
+      ran: false,
+      summary: "Notebook edit: applied=true op=edit at=1 num_cells=1->1 dirty=true.",
+      data: {
+        path: filePath,
+        editType: "edit",
+        cellCountBefore: 1,
+        cellCountAfter: 1,
+        anchorCellIndex: 0,
+        dirty: true,
+        beforeSource: "old\n",
+        afterSource: `${"new line\n".repeat(3000)}`,
+      },
+    })
+
+    const result = await plugin.tool!.vscode_notebook_edit.execute(
+      { filePath, cellId: "#VSC-large", editType: "edit", newCode: "ignored in mock" },
+      allowContext(filePath),
+    )
+
+    // #224：大 diff 被省略，diffOmitted 标记为 "too-large"
+    // #224：3000 行 newCode 生成的 diff 远超 16KB 限制
+    // #224：cellLabel=c1 因为 anchorCellIndex=0 对应 c1（0-based + 1）
+    expect(result).toMatchObject({
+      metadata: {
+        vscodeNotebook: {
+          view: "edit",
+          cellLabel: "c1",
+          diffOmitted: "too-large",
+        },
+      },
+    })
+    if (typeof result === "object" && result && "metadata" in result) {
+      const metadata = result.metadata as Record<string, unknown>
+      const nb = metadata.vscodeNotebook as Record<string, unknown>
+      expect(nb.diff).toBeUndefined()
+      // #224：序列化后的 metadata 不能包含重复的大段源码
+      // #224：这验证 afterSource 的内容没有被泄漏到 diffOmitted 的 metadata 中
+      expect(JSON.stringify(metadata)).not.toContain("new line\nnew line\nnew line")
+    }
+  })
 })
-// #223：文件末尾——两个测试覆盖 notebook 变更工具的权限专属化和双层 edit 门禁顺序
+
+// #224：allowContext 放行所有权限 ask——用于测试 notebookResult 输出而非权限顺序
+// #224：V2 的 Hooks.tool 是可选属性，测试中用 ! 断言 plugin.tool 总是存在
+function allowContext(filePath: string): ToolContext {
+  return {
+    sessionID: "ses_test",
+    messageID: "msg_test",
+    agent: "build",
+    directory: "F:\\project with spaces",
+    worktree: "F:\\project with spaces",
+    abort: new AbortController().signal,
+    metadata: () => undefined,
+    // #224：V2 的 ask 返回 Promise<void>——放行后继续到 callBridge mock
+    async ask(input) {
+      expect(input.patterns).toEqual([filePath])
+    },
+  }
+}
diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
index 8a24e2adf5..4d4a6ebfdb 100644
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -65,6 +65,8 @@ import {
   PENDING_TOOL_INPUT_PROGRESS_INTERVAL,
   type PendingToolInputStats,
 } from "./pending-tool-input"
+// #224：notebook 工具的 inline/block 视图 seam——把 metadata.vscodeNotebook 翻译成 GenericTool 可消费的描述
+import { useVscodeNotebookToolView } from "./notebook-tool"
 import {
   hasStreamingAssistant,
   pendingAssistantID,
@@ -2107,6 +2109,11 @@ function previewText(input: string, maxLines: number) {
   return rows.slice(0, maxLines).join("\n") + "\n…"
 }
 
+// #224：工具标题只承担单行摘要职责，不能承载多行源码。这个阈值只影响
+// generic/plugin tool 的标题参数展示；真实内容仍由完成态的 notebook diff/source
+// card 或 generic output card 展示，避免 pending 阶段刷屏但不丢失结果可见性。
+const TOOL_INPUT_VALUE_MAX_LENGTH = 120
+
 function GenericTool(props: ToolProps) {
   const { theme } = useTheme()
   const ctx = use()
@@ -2121,37 +2128,86 @@ function GenericTool(props: ToolProps) {
     if (expanded() || !collapsed().overflow) return output()
     return collapsed().output
   })
+  // #224：notebook 工具视图 seam——完成态从 metadata.vscodeNotebook 派生 block/inline 描述；
+  // #224：pending 态从 streaming raw parser 派生 inline 摘要。非 notebook 工具返回 undefined。
+  // #224：useVscodeNotebookToolView 是纯 derived memo——不读 VS Code，不产生副作用
+  const pendingStats = createPendingToolInputStats(() => props.part)
+  const notebook = useVscodeNotebookToolView({
+    tool: () => props.tool,
+    input: () => props.input as Record<string, unknown>,
+    metadata: () => props.metadata as Record<string, unknown>,
+    output,
+    status: () => props.part.state.status,
+    pendingStats,
+    width: () => ctx.width,
+    diffWrapMode: ctx.diffWrapMode,
+  })
+  // #224：notebookInline 只在 view.mode === "inline" 时有值——控制 Switch 的第一个 Match
+  const notebookInline = createMemo(() => {
+    const view = notebook()
+    return view?.mode === "inline" ? view : undefined
+  })
+  // #224：notebookBlock 只在 view.mode === "block" 时有值——控制 Switch 的第二个 Match
+  const notebookBlock = createMemo(() => {
+    const view = notebook()
+    return view?.mode === "block" ? view : undefined
+  })
 
   return (
-    <Show
-      when={props.output && ctx.showGenericToolOutput()}
-      fallback={
+    <Switch>
+      {/* #224：notebook inline 视图——pending 态和完成态的轻量单行摘要 */}
+      <Match when={notebookInline()}>
+        {(view) => (
+          <InlineTool icon={view().icon} pending={view().pending} complete={view().complete} part={props.part}>
+            {view().children}
+          </InlineTool>
+        )}
+      </Match>
+      {/* #224：notebook block 视图——完成态的 diff/source/summary/run/output/env card */}
+      <Match when={notebookBlock()}>
+        {(view) => (
+          <BlockTool
+            title={view().title}
+            part={props.part}
+            maxLines={view().maxLines ?? 10}
+            threshold={view().threshold ?? 20}
+            totalLines={view().totalLines}
+            preview={view().preview}
+          >
+            {view().body}
+          </BlockTool>
+        )}
+      </Match>
+      {/* #94/#144：通用工具输出——保留折叠/展开逻辑，仅当有 output 且 showGenericToolOutput 时进入 */}
+      <Match when={props.output && ctx.showGenericToolOutput()}>
+        <BlockTool
+          title={`# ${props.tool} ${input(props.input)}`}
+          part={props.part}
+          onClick={collapsed().overflow ? () => setExpanded((prev) => !prev) : undefined}
+          // #144：武装 BlockTool 内置折叠以启用 preview 主路径
+          maxLines={10}
+          threshold={20}
+          totalLines={output().split(String.fromCharCode(10)).length}
+          // #144：通用工具折叠预览
+          preview={
+            collapsed().overflow ? <text fg={theme.text}>{previewText(output(), 10)}</text> : undefined
+          }
+        >
+          <box gap={1}>
+            <text fg={theme.text}>{expanded() || !collapsed().overflow ? output() : limited()}</text>
+            <Show when={collapsed().overflow}>
+              <text fg={theme.textMuted}>{expanded() ? "Click to collapse" : "Click to expand"}</text>
+            </Show>
+          </box>
+        </BlockTool>
+      </Match>
+      {/* #224：无 output 或关闭 generic 输出时回退到 inline 单行摘要 */}
+      <Match when={true}>
         <InlineTool icon="⚙" pending="Writing command..." complete={true} part={props.part}>
           {props.tool} {input(props.input)}
         </InlineTool>
-      }
-    >
-      <BlockTool
-        title={`# ${props.tool} ${input(props.input)}`}
-        part={props.part}
-        onClick={collapsed().overflow ? () => setExpanded((prev) => !prev) : undefined}
-        // #144：武装 BlockTool 内置折叠以启用 preview 主路径
-        maxLines={10}
-        threshold={20}
-        totalLines={output().split(String.fromCharCode(10)).length}
-        // #144：通用工具折叠预览
-        preview={
-          collapsed().overflow ? <text fg={theme.text}>{previewText(output(), 10)}</text> : undefined
-        }
-      >
-        <box gap={1}>
-          <text fg={theme.text}>{expanded() || !collapsed().overflow ? output() : limited()}</text>
-          <Show when={collapsed().overflow}>
-            <text fg={theme.textMuted}>{expanded() ? "Click to collapse" : "Click to expand"}</text>
-          </Show>
-        </box>
-      </BlockTool>
-    </Show>
+      </Match>
+    </Switch>
   )
 }
 
@@ -3509,7 +3565,20 @@ function input(input: Record<string, unknown>, omit?: string[]): string {
     return typeof value === "string" || typeof value === "number" || typeof value === "boolean"
   })
   if (primitives.length === 0) return ""
-  return `[${primitives.map(([key, value]) => `${key}=${value}`).join(", ")}]`
+    // #224：多行 string 参数在标题中只显示行数/字符数摘要，不刷屏
+  // #224：inputValue 对多行/超长值做截断，单行短值原样返回
+  return `[${primitives.map(([key, value]) => `${key}=${inputValue(value as string | number | boolean)}`).join(", ")}]`
+}
+
+// #224：标题参数值截断——多行 string 显示 <N lines, M chars>，
+// #224：单行超长 string 截断到 TOOL_INPUT_VALUE_MAX_LENGTH 并追加省略号
+// #224：这只影响 generic/plugin tool 的标题，不影响 notebook block card 的完整展示
+function inputValue(value: string | number | boolean) {
+  if (typeof value !== "string") return String(value)
+  const lines = value.split("\n")
+  if (lines.length > 1) return `<${lines.length} lines, ${value.length} chars>`
+  if (value.length <= TOOL_INPUT_VALUE_MAX_LENGTH) return value
+  return value.slice(0, TOOL_INPUT_VALUE_MAX_LENGTH).trimEnd() + "..."
 }
 
 function stringValue(value: unknown) {
diff --git a/packages/tui/src/routes/session/notebook-tool.tsx b/packages/tui/src/routes/session/notebook-tool.tsx
new file mode 100644
index 0000000000..9d5c814b66
--- /dev/null
+++ b/packages/tui/src/routes/session/notebook-tool.tsx
@@ -0,0 +1,422 @@
+import { createMemo, For, Show, type Accessor } from "solid-js"
+import type { JSX } from "@opentui/solid"
+import type { PendingToolInputStats } from "./pending-tool-input"
+import { useTheme } from "../../context/theme"
+import { usePathFormatter } from "../../context/path-format"
+import { previewDiff } from "../../util/preview-diff"
+import { filetype } from "../../util/filetype"
+
+// #224：这个文件只负责把 metadata.vscodeNotebook 翻译成 GenericTool 可消费的
+// inline/block 描述，不拥有 BlockTool/InlineTool 本身。这样 notebook 的差异化
+// 展示集中在一个小 seam，同时保持上游 session/index.tsx 的工具 card 框架不被复制。
+export type VscodeNotebookToolView =
+  | {
+      mode: "inline"
+      icon: string
+      complete: unknown
+      pending: JSX.Element
+      children: JSX.Element
+    }
+  | {
+      mode: "block"
+      title: string
+      body: JSX.Element
+      preview?: JSX.Element
+      totalLines?: number
+      totalChars?: number
+      maxLines?: number
+      threshold?: number
+    }
+
+export function useVscodeNotebookToolView(props: {
+  tool: Accessor<string>
+  input: Accessor<Record<string, unknown>>
+  metadata: Accessor<Record<string, unknown>>
+  output: Accessor<string>
+  status: Accessor<string>
+  pendingStats: Accessor<PendingToolInputStats | undefined>
+  width: Accessor<number>
+  diffWrapMode: Accessor<"word" | "none">
+}) {
+  const { theme, syntax } = useTheme()
+  const pathFormatter = usePathFormatter()
+  return createMemo<VscodeNotebookToolView | undefined>(() => {
+    // #224：完成态优先使用 plugin 写入的 compact metadata；pending/running 阶段 metadata
+    // 尚不存在，只能用 streaming raw parser 提供的 file/cell/+/- 摘要，避免读 VS Code。
+    const metadata = notebookMetadata(props.metadata())
+    if (metadata) return blockView(metadata, props.output())
+    if (!props.tool().startsWith("vscode_notebook_")) return undefined
+    return inlineView(props.tool(), props.input(), props.status(), props.pendingStats())
+  })
+
+  function blockView(metadata: Record<string, unknown>, output: string): VscodeNotebookToolView | undefined {
+    const view = stringValue(metadata.view)
+    // #224：view 字段分派到 edit/summary/source/run/output/env 六种 block 视图
+    // #224：未知 view 值返回 undefined，回退到 generic renderer
+    if (view === "edit") return editView(metadata)
+    if (view === "summary") return summaryView(metadata)
+    if (view === "source") return textView(metadata, output, "Notebook source")
+    if (view === "run") return runView(metadata)
+    if (view === "output") return outputView(metadata)
+    if (view === "env") return envView(metadata)
+  }
+
+  // #224：notebook inline 视图——pending 态从流式参数提取摘要，完成态回退到 inline
+  function inlineView(tool: string, input: Record<string, unknown>, status: string, stats?: PendingToolInputStats): VscodeNotebookToolView {
+    const op = stats?.operation ?? stringValue(input.editType) ?? stringValue(input.operation) ?? tool.replace("vscode_notebook_", "")
+    // #224：target 从 filePath 或 stats.filePath 派生——pathFormatter 统一格式化
+    const target = notebookTarget(stringValue(input.filePath) ?? stats?.filePath)
+    const cell = stats?.cellId ?? stringValue(input.cellId)
+    const label = tool === "vscode_notebook_edit" ? notebookEditLabel(op) : notebookToolLabel(tool)
+    // #224：lineStats 是 pending 阶段的 +N/-N 色标，只在有统计时显示
+    const lineStats = <NotebookPendingStats stats={stats} />
+    const content = (
+      <>
+        {label} <Show when={target}>{target}</Show> <Show when={cell}>{cell}</Show>{lineStats}
+      </>
+    )
+    return {
+      mode: "inline",
+    // #224：icon 用 ← 表示 edit（写入），⚙ 表示其他 notebook 工具
+      icon: tool === "vscode_notebook_edit" ? "←" : "⚙",
+      complete: status !== "pending",
+      pending: (
+        <Show when={stats || target !== "notebook" || cell} fallback={<>Preparing {label.toLowerCase()}...</>}>
+          {content}
+        </Show>
+      ),
+      children: content,
+    }
+  }
+
+  // #224：notebook edit block 视图——从 metadata.diff 渲染 diff card，无 diff 时显示 summary
+  function editView(metadata: Record<string, unknown>): VscodeNotebookToolView {
+    const diff = stringValue(metadata.diff) ?? ""
+    // #224：stats 从 metadata.added/removed 派生——如果 diff 被省略，stats 仍保留行数
+    const stats = { added: numberValue(metadata.added) ?? 0, removed: numberValue(metadata.removed) ?? 0 }
+    const title = [
+      "←",
+      notebookEditLabel(stringValue(metadata.editType)),
+      notebookTarget(stringValue(metadata.path)),
+      stringValue(metadata.cellLabel) ?? stringValue(metadata.cellId),
+        stats.added > 0 || stats.removed > 0 ? `+${stats.added} -${stats.removed}` : undefined,
+    ].filter(Boolean).join(" ")
+    const filePath = notebookDiffPath(metadata)
+    // #224：preview 用 previewDiff 截断到 10 行，body 用完整 diff
+    return {
+      mode: "block",
+      title,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: diff.split("\n").length,
+      totalChars: diff.length,
+      preview: diff ? <NotebookDiff diff={previewDiff(diff, 10)} filePath={filePath} maxLines={10} /> : undefined,
+      // #224：body 在 diff 和 summary 之间用 Show 切换；diffOmitted 时显示提示
+      body: (
+        <box gap={1} flexDirection="column">
+          <Show when={diff} fallback={<text fg={theme.text}>{summaryLine(metadata)}</text>}>
+            <NotebookDiff diff={diff} filePath={filePath} />
+          </Show>
+          <Show when={stringValue(metadata.diffOmitted)}>
+            <text fg={theme.textMuted}>Diff omitted from metadata because it exceeds the notebook inline display budget.</text>
+          </Show>
+          <text fg={theme.textMuted}>{summaryLine(metadata)}</text>
+        </box>
+      ),
+    }
+  }
+
+  // #224：notebook summary block 视图——渲染 cell 列表和 runtime/dirty 状态
+  function summaryView(metadata: Record<string, unknown>): VscodeNotebookToolView {
+    const cells = arrayRecords(metadata.cells)
+    const title = `# Notebook summary ${notebookTarget(stringValue(metadata.path))}${cells.length ? ` · ${cells.length} cells` : ""}`
+    const lines = cells.length + 2
+    return {
+      mode: "block",
+      title,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: lines,
+      totalChars: JSON.stringify(metadata).length,
+      preview: <NotebookCells cells={cells.slice(0, 8)} runtime={stringValue(metadata.runtime)} dirty={booleanValue(metadata.dirty)} />,
+      body: <NotebookCells cells={cells} runtime={stringValue(metadata.runtime)} dirty={booleanValue(metadata.dirty)} />,
+    }
+  }
+
+  // #224：notebook source block 视图——从 output 渲染源码文本
+  function textView(metadata: Record<string, unknown>, output: string, label: string): VscodeNotebookToolView {
+    const title = `→ ${label} ${notebookTarget(stringValue(metadata.path))}`
+    return {
+      mode: "block",
+      title,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: output.split("\n").length,
+      totalChars: output.length,
+      preview: <text fg={theme.text}>{previewText(output, 10)}</text>,
+      body: <text fg={theme.text}>{output}</text>,
+    }
+  }
+
+  // #224：notebook run block 视图——渲染执行结果 cells 和 artifacts
+  function runView(metadata: Record<string, unknown>): VscodeNotebookToolView {
+    const cells = arrayRecords(metadata.cells)
+    const completed = booleanValue(metadata.completed)
+    return {
+      mode: "block",
+      title: `▶ Notebook run ${notebookTarget(stringValue(metadata.path))} ${stringValue(metadata.target) ?? ""} · ${completed === false ? "failed" : "completed"}`,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: cells.length + artifactRows(cells).length + 2,
+      totalChars: JSON.stringify(metadata).length,
+      preview: <NotebookRun cells={cells.slice(0, 6)} />,
+      body: <NotebookRun cells={cells} />,
+    }
+  }
+
+  // #224：notebook output block 视图——渲染 cell artifacts 和 cell 元信息
+  function outputView(metadata: Record<string, unknown>): VscodeNotebookToolView {
+    const artifacts = arrayRecords(metadata.artifacts)
+    const cell = recordValue(metadata.cell)
+    return {
+      mode: "block",
+      title: `→ Notebook output ${notebookTarget(stringValue(metadata.path))} ${cellLabel(cell)} · ${artifacts.length ? `${artifacts.length} artifacts` : "no outputs"}`,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: artifacts.length + 2,
+      totalChars: JSON.stringify(metadata).length,
+      preview: <NotebookArtifacts artifacts={artifacts.slice(0, 6)} />,
+      body: (
+        <box flexDirection="column">
+          <text fg={theme.textMuted}>Cell: {cellLabel(cell)} {stringValue(cell.kind)}/{stringValue(cell.lang)}</text>
+          <NotebookArtifacts artifacts={artifacts} />
+        </box>
+      ),
+    }
+  }
+
+  // #224：notebook env block 视图——渲染操作/状态/guidance/dirty 变化
+  function envView(metadata: Record<string, unknown>): VscodeNotebookToolView {
+    const status = stringValue(metadata.status) ?? envStatus(metadata)
+    return {
+      mode: "block",
+      title: `# Notebook env ${notebookTarget(stringValue(metadata.path))} · ${status ?? stringValue(metadata.operation) ?? "info"}`,
+      maxLines: 10,
+      threshold: 20,
+      totalLines: 4,
+      totalChars: JSON.stringify(metadata).length,
+      body: (
+        <box flexDirection="column">
+          <Show when={stringValue(metadata.operation)}>{(operation) => <text fg={theme.textMuted}>Operation: {operation()}</text>}</Show>
+          <Show when={status}>{(item) => <text fg={statusColor(item())}>Status: {item()}</text>}</Show>
+          <Show when={stringValue(metadata.guidance)}>{(guidance) => <text fg={theme.text}>{guidance()}</text>}</Show>
+          <Show when={metadata.beforeDirty !== undefined || metadata.afterDirty !== undefined}>
+            <text fg={theme.textMuted}>Dirty: {String(booleanValue(metadata.beforeDirty))} -&gt; {String(booleanValue(metadata.afterDirty))}</text>
+          </Show>
+        </box>
+      ),
+    }
+  }
+
+  // #224：NotebookDiff 组件——用 opentui diff 渲染 unified/split diff，根据宽度自动选 view
+  function NotebookDiff(diffProps: { diff: string; filePath?: string; maxLines?: number }) {
+    return (
+      <box paddingLeft={1} maxHeight={diffProps.maxLines} overflow={diffProps.maxLines ? "hidden" : undefined}>
+        <diff
+          diff={diffProps.diff}
+          view={props.width() > 120 ? "split" : "unified"}
+          filetype={filetype(diffProps.filePath)}
+          syntaxStyle={syntax()}
+          showLineNumbers={true}
+          width="100%"
+          wrapMode={props.diffWrapMode()}
+          fg={theme.text}
+          addedBg={theme.diffAddedBg}
+          removedBg={theme.diffRemovedBg}
+          contextBg={theme.diffContextBg}
+          addedSignColor={theme.diffHighlightAdded}
+          removedSignColor={theme.diffHighlightRemoved}
+          lineNumberFg={theme.diffLineNumber}
+          lineNumberBg={theme.diffContextBg}
+          addedLineNumberBg={theme.diffAddedLineNumberBg}
+          removedLineNumberBg={theme.diffRemovedLineNumberBg}
+        />
+      </box>
+    )
+  }
+
+  // #224：pending 阶段的 +N/-N 行数标签
+  function NotebookPendingStats(statProps: { stats?: PendingToolInputStats }) {
+    return (
+      <>
+        <Show when={statProps.stats?.added}><span style={{ fg: theme.diffAdded }}> +{statProps.stats!.added}</span></Show>
+        <Show when={statProps.stats?.removed}><span style={{ fg: theme.diffRemoved }}> -{statProps.stats!.removed}</span></Show>
+      </>
+    )
+  }
+
+  // #224：summary/run 的 cell 列表渲染
+  function NotebookCells(cellProps: { cells: Record<string, unknown>[]; runtime?: string; dirty?: boolean }) {
+    return (
+      <box flexDirection="column">
+        <text fg={theme.textMuted}>runtime={cellProps.runtime ?? "unknown"} dirty={String(cellProps.dirty ?? "unknown")}</text>
+        <For each={cellProps.cells}>{(cell) => <text fg={execColor(stringValue(cell.exec))}>{cellLine(cell)}</text>}</For>
+      </box>
+    )
+  }
+
+  // #224：run 结果的 cell + artifacts 渲染
+  function NotebookRun(runProps: { cells: Record<string, unknown>[] }) {
+    const artifacts = artifactRows(runProps.cells)
+    return (
+      <box flexDirection="column">
+        <For each={runProps.cells}>{(cell) => <text fg={execColor(stringValue(cell.exec))}>{cellLine(cell)}</text>}</For>
+        <Show when={artifacts.length}>
+          <text fg={theme.textMuted}>Artifacts:</text>
+          <NotebookArtifacts artifacts={artifacts} />
+        </Show>
+      </box>
+    )
+  }
+
+  // #224：artifact 列表渲染
+  function NotebookArtifacts(artifactProps: { artifacts: Record<string, unknown>[] }) {
+    return (
+      <box flexDirection="column">
+        <For each={artifactProps.artifacts}>{(artifact) => <text fg={theme.text}>{artifactLine(artifact)}</text>}</For>
+      </box>
+    )
+  }
+
+  // #224：路径格式化——用 pathFormatter 统一显示
+  function notebookTarget(value?: string) {
+    return value ? pathFormatter.format(value) : "notebook"
+  }
+
+  // #224：env 状态颜色——成功绿/失败红/其他默认
+  function statusColor(status: string) {
+    if (["configured", "selected", "completed", "saved"].includes(status)) return theme.success
+    if (["failed", "needs-selection"].includes(status)) return theme.error
+    return theme.text
+  }
+
+  // #224：cell 执行状态颜色——失败红/跳过灰/其他默认
+  function execColor(exec?: string) {
+    if (exec?.includes("failed") || exec?.includes("error")) return theme.error
+    if (exec?.includes("skipped") || exec?.includes("not-run")) return theme.textMuted
+    return theme.text
+  }
+}
+
+function notebookMetadata(metadata: Record<string, unknown>) {
+  const value = metadata.vscodeNotebook
+  // #224：非对象或数组的 vscodeNotebook 值不合法，返回 undefined 回退到 generic 渲染
+  if (!value || typeof value !== "object" || Array.isArray(value)) return undefined
+  return value as Record<string, unknown>
+}
+
+// #224：notebook edit 操作标签——insert/delete/edit 三种，与 editType 枚举对齐
+function notebookEditLabel(operation?: string) {
+  if (operation === "insert") return "Notebook insert"
+  if (operation === "delete") return "Notebook delete"
+  return "Notebook edit"
+}
+
+// #224：非 edit 的 notebook 工具标签——从工具名派生可读名称
+// #224：vscode_notebook_run → "Notebook run"，vscode_notebook_source → "Notebook source"
+function notebookToolLabel(tool: string) {
+  return tool.replace(/^vscode_notebook_/, "Notebook ").replaceAll("_", " ")
+}
+
+// #224：diff 路径标签——path + cellLabel + language 组成虚拟文件名供语法高亮
+function notebookDiffPath(metadata: Record<string, unknown>) {
+  return [stringValue(metadata.path), stringValue(metadata.cellLabel), stringValue(metadata.language) ? `.${stringValue(metadata.language)}` : undefined]
+    .filter((item): item is string => Boolean(item))
+    .join("")
+}
+
+// #224：edit 完成态摘要行——cell 数变化 + dirty 状态 + editType
+function summaryLine(metadata: Record<string, unknown>) {
+  const before = numberValue(metadata.cellCountBefore)
+  const after = numberValue(metadata.cellCountAfter)
+  const count = before === undefined || after === undefined ? undefined : `${before} -> ${after} cells`
+  return [count, `dirty=${String(booleanValue(metadata.dirty) ?? "unknown")}`, stringValue(metadata.editType)].filter(Boolean).join(" · ")
+}
+
+// #224：cell 单行摘要——label/id/kind/lang/lines/exec/outs/first 七段拼接
+function cellLine(cell: Record<string, unknown>) {
+  return [
+    cellLabel(cell),
+    stringValue(cell.id),
+    [stringValue(cell.kind), stringValue(cell.lang)].filter(Boolean).join("/"),
+    numberValue(cell.lines) === undefined ? undefined : `${numberValue(cell.lines)} lines`,
+    stringValue(cell.exec),
+    arrayStrings(cell.existing_outs).length ? `outs=${arrayStrings(cell.existing_outs).join(",")}` : undefined,
+    stringValue(cell.first) ? `first=${JSON.stringify(stringValue(cell.first))}` : undefined,
+  ].filter(Boolean).join("  ")
+}
+
+// #224：cell 标签——有 index 显示 cN，否则用 id 或默认 'cell'
+function cellLabel(cell: Record<string, unknown>) {
+  const index = numberValue(cell.i)
+  return index === undefined ? stringValue(cell.id) ?? "cell" : `c${index}`
+}
+
+// #224：从 run cells 中收集所有 artifacts 供 summary 展示
+function artifactRows(cells: Record<string, unknown>[]) {
+  return cells.flatMap((cell) => arrayRecords(cell.artifacts))
+}
+
+// #224：artifact 单行摘要——mime/bytes/path/preview 四段
+function artifactLine(artifact: Record<string, unknown>) {
+  return [stringValue(artifact.mime), bytesText(numberValue(artifact.bytes)), stringValue(artifact.artifactPath), stringValue(artifact.preview)].filter(Boolean).join("  ")
+}
+
+// #224：env 状态从 saved 布尔值派生——save 操作没有显式 status 字段
+function envStatus(metadata: Record<string, unknown>) {
+  if (booleanValue(metadata.saved) !== undefined) return booleanValue(metadata.saved) ? "saved" : "save not confirmed"
+}
+
+// #224：notebook 内部用的 previewText——截断时用 '…' 省略号行
+function previewText(input: string, maxLines: number) {
+  const lines = input.split("\n")
+  return lines.length > maxLines ? [...lines.slice(0, maxLines), "…"].join("\n") : input
+}
+
+// #224：bytes 可读化——小于 1KB 显示 B，否则显示 KB
+function bytesText(value?: number) {
+  if (value === undefined) return undefined
+  if (value < 1024) return `${value} B`
+  return `${(value / 1024).toFixed(1)} KB`
+}
+
+// #224：类型安全守卫——非对象返回空 Record，避免后续访问报错
+function recordValue(value: unknown): Record<string, unknown> {
+  if (!value || typeof value !== "object" || Array.isArray(value)) return {}
+  return value as Record<string, unknown>
+}
+
+// #224：类型安全守卫——非数组返回空数组
+function arrayRecords(value: unknown) {
+  return Array.isArray(value) ? value.flatMap((item) => (item && typeof item === "object" && !Array.isArray(item) ? [item as Record<string, unknown>] : [])) : []
+}
+
+// #224：类型安全守卫——过滤出 string 元素
+function arrayStrings(value: unknown) {
+  return Array.isArray(value) ? value.filter((item): item is string => typeof item === "string") : []
+}
+
+// #224：类型安全守卫——string 提取，非 string 返回 undefined
+function stringValue(value: unknown) {
+  return typeof value === "string" ? value : undefined
+}
+
+// #224：类型安全守卫——有限 number 提取
+function numberValue(value: unknown) {
+  return typeof value === "number" && Number.isFinite(value) ? value : undefined
+}
+
+// #224：类型安全守卫——boolean 提取
+function booleanValue(value: unknown) {
+  return typeof value === "boolean" ? value : undefined
+}
diff --git a/packages/tui/src/routes/session/pending-tool-input.ts b/packages/tui/src/routes/session/pending-tool-input.ts
index afbae2c431..fd3f1b6df3 100644
--- a/packages/tui/src/routes/session/pending-tool-input.ts
+++ b/packages/tui/src/routes/session/pending-tool-input.ts
@@ -2,6 +2,10 @@
 export type PendingToolInputStats = {
   // #187：filePath 可空（多文件 patch 时用 fileCount）
   filePath?: string
+  // #224：notebook edit 的稳定 cell 定位键，pending 阶段从 cellId 参数流式提取
+  cellId?: string
+  // #224：notebook edit 操作类型（insert/edit/delete）或 env 操作（configure/restart/save）
+  operation?: string
   // #187：apply_patch 多文件计数
   fileCount: number
   // #187：新增行（write/content 或 newString 或 +diff）
@@ -37,6 +41,8 @@ export function createPendingToolInputParser(tool: string) {
   if (tool === "apply_patch") return createApplyPatchInputParser()
   if (tool === "edit") return createEditInputParser()
   if (tool === "write") return createWriteInputParser()
+  // #224：notebook edit 的 pending 统计需要 filePath/cellId/editType + oldCode/newCode 行数
+  if (tool === "vscode_notebook_edit") return createNotebookEditInputParser()
   // #187：bash 等无 raw 进度 UI 的工具返回空 parser
   return {
     push(_input: string) {},
@@ -44,6 +50,39 @@ export function createPendingToolInputParser(tool: string) {
   }
 }
 
+  // #224：notebook edit 的 pending 统计从流式参数提取 filePath/cellId/editType +
+// oldCode/newCode 行数。bridge 的 sourceProp 会用 "\n" 拼接 string[] 数组项，
+// streaming 统计也必须补上这个隐式换行，否则 pending 阶段的 +N/-N 会低估。
+// #224：与 createEditInputParser 不同——notebook edit 还需要 cellId 和 editType
+function createNotebookEditInputParser() {
+  let filePath = ""
+  let cellId = ""
+  let operation = ""
+  const oldCode = createLineCounter()
+  const newCode = createLineCounter()
+  const scanner = createJsonStringFieldScanner(["filePath", "cellId", "editType", "oldCode", "newCode"], (field, value) => {
+    if (field === "filePath") filePath += value
+    if (field === "cellId") cellId += value
+    if (field === "editType") operation += value
+    if (field === "oldCode") oldCode.push(value)
+    if (field === "newCode") newCode.push(value)
+  })
+  return {
+    push: scanner.push,
+    stats: () => {
+      // #224：notebook edit 的 pending 统计只能来自模型正在流式生成的参数，不能在渲染线程
+      // 读取 VS Code 或 notebook 文件；full-cell replace/delete 的真实删除行数要等完成后的 diff。
+      const stats = pendingStats(filePath, filePath ? 1 : 0, newCode.count(), oldCode.count())
+      if (!stats && !cellId && !operation) return undefined
+      return {
+        ...(stats ?? { fileCount: filePath ? 1 : 0, added: 0, removed: 0, filePath: filePath || undefined }),
+        cellId: cellId || undefined,
+        operation: operation || undefined,
+      }
+    },
+  }
+}
+
 // #187：edit 扫 filePath/oldString/newString，added=new 行 removed=old 行
 function createEditInputParser() {
   let filePath = ""
@@ -131,6 +170,10 @@ function createJsonStringFieldScanner(fields: readonly string[], onValue: (field
   let expectingValueFor = ""
   // #187：当前正在读的 value 字段名；空表示在 key 相位
   let activeField = ""
+  // #224：数组字段状态——oldCode/newCode 可以是 string[]，需要跟踪数组边界
+  // #224：arrayField 记录当前正在遍历的数组字段名，空表示不在数组内
+  let arrayField = ""
+  let arrayFieldHasValue = false
   let key = ""
   // #187：是否位于 JSON 字符串 token 内部
   let inString = false
@@ -188,6 +231,11 @@ function decodeEscaped(char: string) {
           if (char === '"') {
             inString = false
             if (!activeField) pendingKey = key
+            // #224：数组内多 string 项时，每个新 string 前补一个隐式换行，
+            // #224：对齐 bridge sourceProp 用 "\n" 拼接 string[] 的行为
+            // #224：arrayFieldHasValue 确保只在第二个及之后的数组项前补换行
+            if (activeField && arrayField && arrayFieldHasValue) onValue(activeField, "\n")
+            if (activeField && arrayField) arrayFieldHasValue = true
             activeField = ""
             key = ""
             continue
@@ -198,18 +246,31 @@ function decodeEscaped(char: string) {
 
         if (char === '"') {
           inString = true
-          activeField = wanted.has(expectingValueFor) ? expectingValueFor : ""
+          // #224：数组内 string 项也激活 wanted 字段；数组外仍用 expectingValueFor
+          // #224：这样 oldCode/newCode 的 string[] 元素也能被 lineCounter 正确计数
+          activeField = wanted.has(expectingValueFor) ? expectingValueFor : arrayField
           expectingValueFor = ""
           key = ""
           continue
         }
-        if (char === ":" && pendingKey) {
-          expectingValueFor = pendingKey
-          pendingKey = ""
+        // #224：进入数组字段——oldCode/newCode 可以是 string[]
+        // #224：遇到 [ 后切换到数组模式，后续逗号不再重置 pendingKey
+        if (expectingValueFor && char === "[" && wanted.has(expectingValueFor)) {
+          arrayField = expectingValueFor
+          arrayFieldHasValue = false
+          expectingValueFor = ""
+          continue
+        }
+        // #224：退出数组字段——遇到 ] 恢复到普通字段模式
+        if (arrayField && char === "]") {
+          arrayField = ""
+          arrayFieldHasValue = false
           continue
         }
         if (expectingValueFor && !/\s/.test(char)) expectingValueFor = ""
-        if (char === "," || char === "{" || char === "}") pendingKey = ""
+        // #224：数组内不重置 pendingKey——逗号是数组项分隔符不是字段分隔符
+        // #224：这确保 string[] 的后续元素仍能被正确识别为同一字段的值
+        if (!arrayField && (char === "," || char === "{" || char === "}")) pendingKey = ""
       }
     },
   }
diff --git a/sdks/vscode/src/notebook/edit.ts b/sdks/vscode/src/notebook/edit.ts
index dac6c9a7fa..ad6d2393e9 100644
--- a/sdks/vscode/src/notebook/edit.ts
+++ b/sdks/vscode/src/notebook/edit.ts
@@ -108,6 +108,10 @@ return compactEditResult(notebook, {
     shiftedCell,
     shiftedOldIdx,
     sourcePreview: deletedPreview,
+    // #224：delete 的 before/after——deletedPreview 是删除前源码，after 为空
+    // #224：notebookResult 用 before=deletedPreview, after="" 生成全删除 diff
+    beforeSource: deletedPreview,
+    afterSource: "",
   })
 }
 
@@ -148,7 +152,9 @@ async function handleInsert(notebook: vscode.NotebookDocument, input: Record<str
   // notebook 结构编辑：等待 notebook.version 推进
   const applied = await applyNotebookEditAndWait(notebook, edit)
   const shiftedCell = shiftedOldIdx !== undefined ? notebook.cellAt(shiftedOldIdx + 1) : undefined
-  return compactEditResult(notebook, { applied, editType: "insert", opIndex: String(insertIndex + 1), beforeCount, afterCount: notebook.cellCount, anchorCell, shiftedCell, shiftedOldIdx, kind: cellTypeLabel(kind), language: lang, sourcePreview: source })
+  // #224：insert 的 beforeSource 为空（插入前无 cell），afterSource 是新 cell 源码
+  // #224：affectedCellIndex=insertIndex 让 notebookResult 能标记插入位置的 cN 标签
+  return compactEditResult(notebook, { applied, editType: "insert", opIndex: String(insertIndex + 1), beforeCount, afterCount: notebook.cellCount, anchorCell, affectedCellIndex: insertIndex, shiftedCell, shiftedOldIdx, kind: cellTypeLabel(kind), language: lang, sourcePreview: source, beforeSource: "", afterSource: source })
 }
 
 // ---------------------------------------------------------------------------
@@ -199,6 +205,9 @@ async function handleEdit(notebook: vscode.NotebookDocument, input: Record<strin
     const sourceRaw = sourceProp(input, "newCode", documentEol(targetCell.document))
   if (sourceRaw === undefined) throw new Error("newCode is required for edit")
   const source = targetCell.kind === vscode.NotebookCellKind.Code ? stripCodeFence(sourceRaw) : sourceRaw
+  // #224：记录编辑前源码，供 notebookResult 生成 diff
+  // #224：getText 获取编辑前完整 cell 源码
+  const beforeSource = targetCell.document.getText()
   const edit = new vscode.WorkspaceEdit()
   edit.replace(targetCell.document.uri, fullDocumentRange(targetCell.document), source)
   // 整 cell 源码替换：与当前文档不同才等文本版本
@@ -213,6 +222,10 @@ async function handleEdit(notebook: vscode.NotebookDocument, input: Record<strin
     kind: cellTypeLabel(targetCell.kind),
     language: targetCell.document.languageId,
     sourcePreview: source,
+    // #224：before/after 源码供 opencode TUI 生成 diff card
+    // #224：before 是编辑前完整 cell 文本，after 是编辑后完整 cell 文本
+    beforeSource,
+    afterSource: source,
   })
 }
 
@@ -236,7 +249,9 @@ async function handleTypeChange(
   let newSource: string
   let contextSummary: string | undefined
 
   const sourceRaw = sourceProp(input, "newCode", documentEol(targetCell.document))
+  // #224：类型变更前源码——beforeSource 用于 diff 生成
+  const beforeSource = targetCell.document.getText()
 
   if (oldCode) {
     // 类型变更路径：oldCode 可选匹配，再以新 kind 重建 cell
@@ -273,6 +288,10 @@ async function handleTypeChange(
     oldLang,
     sourcePreview: newSource,
     contextSummary,
+    // #224：类型变更前后的完整源码
+    // #224：beforeSource 是变更前源码，afterSource 是重建后的新 cell 源码
+    beforeSource,
+    afterSource: newSource,
   })
 }
 
@@ -289,7 +308,9 @@ async function handleTypeChange(
 
 // string oldCode/newCode 主路径：替代 #78 的 range 编辑
 // applyEdit 失败时 applied=false 仍返回 compact，禁止吞掉失败状态
 async function handleStringEdit(notebook: vscode.NotebookDocument, targetCell: vscode.NotebookCell, oldCode: string, newCode: string, beforeCount: number) {
+  // #224：字符串编辑前源码——beforeSource 用于 diff 生成
+  const beforeSource = targetCell.document.getText()
   const result = matchAndReplace(targetCell, oldCode, newCode)
   const contextSummary = buildContext(notebook, targetCell, result, newCode)
 
@@ -309,6 +330,10 @@ async function handleStringEdit(notebook: vscode.NotebookDocument, targetCell: v
     language: targetCell.document.languageId,
     sourcePreview: newCode,
     contextSummary,
+    // #224：字符串替换前后的完整源码
+    // #224：result.source 是 matchAndReplace 后的完整替换结果
+    beforeSource,
+    afterSource: result.source,
   })
 }
 
@@ -561,6 +586,9 @@ function compactEditResult(
     shiftedOldIdx?: number
     sourcePreview?: string
     contextSummary?: string
+    // #224：before/after 源码供 opencode TUI 生成 diff card
+    beforeSource?: string
+    afterSource?: string
   },
 ) {
   const lines: Array<string | undefined> = [
@@ -612,10 +640,18 @@ async function handleTypeChange(
       cellCountBefore: info.beforeCount,
       cellCountAfter: info.afterCount,
       anchorCellIndex: info.anchorCell?.index,
+      // #224：deletedCellIndex 供 notebookResult 计算删除操作的 cell 标签
+      // #224：beforeSource 是删除前源码，afterSource 为空——diff 显示全删除
+      deletedCellIndex: info.deletedCellIndex,
       affectedCellIndex: info.affectedCellIndex,
       dirty: notebook.isDirty,
       kind: info.kind,
       language: info.language,
+      // #224：opencode TUI 只从 data 派生展示 metadata，不改变 bridge 给模型的 summary。
+      // before/after 保持 cell 级源文本，确保 notebook edit 完成态能渲染真实 diff，
+      // pending 态仍只能显示参数行数估算，不能在 UI 线程重新读 VS Code 文档。
+      beforeSource: info.beforeSource,
+      afterSource: info.afterSource,
     },
   }
 }
