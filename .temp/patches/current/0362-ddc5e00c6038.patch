From ddc5e00c6038ca2a9dbe55913c67ec7ec7e40989 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Sat, 4 Jul 2026 13:35:18 +0800
Subject: [PATCH] =?UTF-8?q?fix(provider):=20connect=20provider=20=E4=B8=8D?=
 =?UTF-8?q?=E5=86=8D=E6=9D=80=E8=BF=90=E8=A1=8C=E4=B8=AD=E4=BC=9A=E8=AF=9D?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

之前在 TUI/Web app 中连接或断开 provider 时，服务端会调
disposeAllInstances 销毁所有实例，导致其他正在运行的会话
（含不同 provider/model）被 SessionRunState finalizer 强制取消。

改为在 authSet/authRemove/OAuth callback 三个服务端 handler 中
调用 refreshProviderCaches：仅用 ScopedCache.invalidateAll 刷新
Provider.Service 缓存（不触碰 SessionRunState），发 global.disposed
通知客户端刷新 UI。运行中会话继续用旧密钥跑完，下一条新消息惰性
重建缓存并获取新密钥。configUpdate 路径保留旧行为（已知限制）。


diff --git a/packages/app/src/components/dialog-connect-provider.tsx b/packages/app/src/components/dialog-connect-provider.tsx
index 6499642b3..c96999da0 100644
--- a/packages/app/src/components/dialog-connect-provider.tsx
+++ b/packages/app/src/components/dialog-connect-provider.tsx
@@ -498,7 +498,8 @@ function ProviderConnection(props: {
   })
 
   async function complete() {
-    await serverSDK().client.global.dispose()
+    // #362：不再调 global.dispose()——服务端 authSet/callback handler 已刷新 Provider 缓存。
+    // global.disposed 事件会触发 bootstrap.refetch() 使 UI 获取最新 provider 列表。
     dialog.close()
     showToast({
       variant: "success",
diff --git a/packages/app/src/components/settings-providers.tsx b/packages/app/src/components/settings-providers.tsx
index bcd30edbc..51625b214 100644
--- a/packages/app/src/components/settings-providers.tsx
+++ b/packages/app/src/components/settings-providers.tsx
@@ -128,7 +128,8 @@ const SettingsProvidersContent: Component<{ onBack?: () => void }> = (props) =>
     await serverSDK()
       .client.auth.remove({ providerID })
       .then(async () => {
-        await serverSDK().client.global.dispose()
+        // #362：不再调 global.dispose()——服务端 authRemove handler 已刷新 Provider 缓存。
+        // global.disposed 事件会触发 UI 刷新。
         showToast({
           variant: "success",
           icon: "circle-check",
diff --git a/packages/opencode/src/provider/provider.ts b/packages/opencode/src/provider/provider.ts
index cc5ab75ac..7f3179e99 100644
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
@@ -18,7 +18,7 @@ import { iife } from "@/util/iife"
 import { Global } from "@opencode-ai/core/global"
 import path from "path"
 import { pathToFileURL } from "url"
-import { Effect, Layer, Context, Schema, Types } from "effect"
+import { Effect, Layer, Context, Schema, Types, ScopedCache } from "effect"
 import { EffectBridge } from "@/effect/bridge"
 import { InstanceState } from "@/effect/instance-state"
 import { EffectPromise } from "@/effect/promise"
@@ -1328,6 +1328,9 @@ export interface Interface {
   ) => Effect.Effect<{ providerID: ProviderV2.ID; modelID: string } | undefined>
   readonly getSmallModel: (providerID: ProviderV2.ID) => Effect.Effect<Model | undefined>
   readonly defaultModel: () => Effect.Effect<{ providerID: ProviderV2.ID; modelID: ModelV2.ID }, DefaultModelError>
+  // #362：刷新所有目录的 Provider 缓存条目，不触发 SessionRunState 的 finalizer。
+  // auth 变更后调用，使下一次 getLanguage/getProvider 惰性重建并读取新密钥。
+  readonly invalidateAll: () => Effect.Effect<void>
 }
 
 interface State {
@@ -1970,6 +1973,12 @@ for (const [id, provider] of configProviders) {
 
     const list = Effect.fn("Provider.list")(() => InstanceState.use(state, (s) => s.providers))
 
+    // #362：仅刷新 Provider.Service 的 ScopedCache（所有目录），不触碰 SessionRunState
+    // 等其他服务的缓存——这是 "auth 变更不杀会话" 的结构性保证。
+    const invalidateAll = Effect.fn("Provider.invalidateAll")(() =>
+      ScopedCache.invalidateAll(state.cache),
+    )
+
     async function resolveSDK(model: Model, s: State, envs: Record<string, string | undefined>) {
       try {
         const provider = s.providers[model.providerID]
@@ -2337,7 +2346,7 @@ for (const [id, provider] of configProviders) {
       }
     })
 
-    return Service.of({ list, getProvider, getModel, getLanguage, closest, getSmallModel, defaultModel })
+    return Service.of({ list, getProvider, getModel, getLanguage, closest, getSmallModel, defaultModel, invalidateAll })
   }),
 )
 
diff --git a/packages/opencode/src/server/global-lifecycle.ts b/packages/opencode/src/server/global-lifecycle.ts
index 12b7687bf..76e208345 100644
--- a/packages/opencode/src/server/global-lifecycle.ts
+++ b/packages/opencode/src/server/global-lifecycle.ts
@@ -1,5 +1,6 @@
 import { GlobalBus } from "@/bus/global"
 import { InstanceStore } from "@/project/instance-store"
+import { Provider } from "@/provider/provider"
 import { Effect } from "effect"
 import { Event } from "./event"
 
@@ -25,4 +26,16 @@ export const disposeAllInstancesAndEmitGlobalDisposed = Effect.fn("Server.dispos
   },
 )
 
+// #362：auth 变更后刷新所有实例的 Provider 缓存（不销毁实例、不杀会话）。
+// 仅失效 Provider.Service 的 ScopedCache，不触碰 SessionRunState 等其他服务。
+// 发 global.disposed 通知客户端刷新 provider 列表（空 properties，非 DaemonStop）。
+// 失败仅告警不阻断——auth 已写入文件，下次访问会惰性重建。
+export const refreshProviderCaches = Effect.fn("Server.refreshProviderCaches")(function* () {
+  const provider = yield* Provider.Service
+  yield* provider.invalidateAll().pipe(
+    Effect.catchCause((cause) => Effect.logWarning("provider cache refresh failed", { cause })),
+  )
+  yield* emitGlobalDisposed
+})
+
 export * as GlobalLifecycle from "./global-lifecycle"
diff --git a/packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts b/packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts
index 9974b19b4..adb8e2180 100644
--- a/packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts
+++ b/packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts
@@ -1,4 +1,5 @@
 import { Auth } from "@/auth"
+import { refreshProviderCaches } from "@/server/global-lifecycle"
 
 import { Effect } from "effect"
 import { HttpApiBuilder } from "effect/unstable/httpapi"
@@ -15,6 +16,8 @@ export const controlHandlers = HttpApiBuilder.group(RootHttpApi, "control", (han
       payload: Auth.Info
     }) {
       yield* auth.set(ctx.params.providerID, ctx.payload).pipe(Effect.orDie)
+      // #362：auth 写入后刷新 Provider 缓存（不杀会话），使下次请求获取新密钥
+      yield* refreshProviderCaches()
       return true
     })
 
@@ -22,6 +25,8 @@ export const controlHandlers = HttpApiBuilder.group(RootHttpApi, "control", (han
       params: { providerID: ProviderV2.ID }
     }) {
       yield* auth.remove(ctx.params.providerID).pipe(Effect.orDie)
+      // #362：auth 移除后同样刷新 Provider 缓存，使该 provider 不再出现在 connected 列表
+      yield* refreshProviderCaches()
       return true
     })
 
diff --git a/packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts b/packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts
index e1377b6f7..40acd5a6e 100644
--- a/packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts
+++ b/packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts
@@ -2,7 +2,7 @@ import { ProviderAuth } from "@/provider/auth"
 import { Config } from "@/config/config"
 import { ModelsDev } from "@opencode-ai/core/models-dev"
 import { Provider } from "@/provider/provider"
-
+import { refreshProviderCaches } from "@/server/global-lifecycle"
 import { mapValues } from "remeda"
 import { Effect, Schema } from "effect"
 import { HttpServerRequest, HttpServerResponse } from "effect/unstable/http"
@@ -101,6 +101,8 @@ export const providerHandlers = HttpApiBuilder.group(InstanceHttpApi, "provider"
           code: ctx.payload.code,
         }),
       )
+      // #362：OAuth 回调内部已写入 auth，刷新 Provider 缓存使新凭据立即生效
+      yield* refreshProviderCaches()
       return true
     })
 
diff --git a/packages/opencode/test/fake/provider.ts b/packages/opencode/test/fake/provider.ts
index 1dbfa6fa7..113fe8c7c 100644
--- a/packages/opencode/test/fake/provider.ts
+++ b/packages/opencode/test/fake/provider.ts
@@ -74,6 +74,8 @@ export namespace ProviderTest {
           defaultModel: Effect.fn("TestProvider.defaultModel")(() =>
             Effect.succeed({ providerID: row.id, modelID: mdl.id }),
           ),
+          // #362：测试 fake 也需要实现 invalidateAll，保持 Interface 完整
+          invalidateAll: Effect.fn("TestProvider.invalidateAll")(() => Effect.void),
           ...override,
         }),
       ),
diff --git a/packages/opencode/test/provider/provider.test.ts b/packages/opencode/test/provider/provider.test.ts
index af12b6010..a0e6425a5 100644
--- a/packages/opencode/test/provider/provider.test.ts
+++ b/packages/opencode/test/provider/provider.test.ts
@@ -2259,3 +2259,46 @@ it.instance(
   },
 )
 
+// #362：invalidateAll 的核心行为：刷新 Provider.Service 缓存后，
+// 下次 list() 会重新读取 env/auth，反映最新的连接状态。
+// 这验证了 "auth 变更后不销毁实例、仅刷 provider 缓存" 的关键路径。
+it.instance("invalidateAll 后 provider.list 反映最新的 env 状态", () =>
+  Effect.gen(function* () {
+    // 设置 env → provider 初始化后包含 anthropic
+    yield* set("ANTHROPIC_API_KEY", "test-api-key")
+    const before = yield* list
+    expect(before[ProviderV2.ID.anthropic]).toBeDefined()
+
+    // 移除 env，但缓存仍持有旧数据（未刷新）
+    yield* remove("ANTHROPIC_API_KEY")
+    const stale = yield* list
+    expect(stale[ProviderV2.ID.anthropic]).toBeDefined()
+
+    // 刷新缓存后，下次 list 重新读取 env → anthropic 不再存在
+    yield* Provider.use.invalidateAll()
+    const after = yield* list
+    expect(after[ProviderV2.ID.anthropic]).toBeUndefined()
+  }),
+)
+
+// #362：invalidateAll 不能破坏后续的 getLanguage 调用——
+// 缓存被清空后，下次 getLanguage 应惰性重建并正常返回。
+// 这验证了 invalidateAll 是安全的：不产生半态或损坏的缓存。
+it.instance("invalidateAll 后 getLanguage 仍能正常重建缓存", () =>
+  Effect.gen(function* () {
+    yield* set("ANTHROPIC_API_KEY", "test-api-key")
+    const provider = yield* Provider.Service
+    const model = yield* provider.getModel(ProviderV2.ID.anthropic, ModelV2.ID.make("claude-sonnet-4-6"))
+    // 首次调用 → 初始化缓存
+    const lang1 = yield* provider.getLanguage(model)
+    expect(lang1).toBeDefined()
+
+    // 刷新缓存 → 清空
+    yield* Provider.use.invalidateAll()
+
+    // 再次调用 → 惰性重建，应正常返回
+    const model2 = yield* provider.getModel(ProviderV2.ID.anthropic, ModelV2.ID.make("claude-sonnet-4-6"))
+    const lang2 = yield* provider.getLanguage(model2)
+    expect(lang2).toBeDefined()
+  }),
+)
diff --git a/packages/tui/src/component/dialog-provider.tsx b/packages/tui/src/component/dialog-provider.tsx
index 0fd51e3c1..d45384f30 100644
--- a/packages/tui/src/component/dialog-provider.tsx
+++ b/packages/tui/src/component/dialog-provider.tsx
@@ -278,7 +278,8 @@ function AutoMethod(props: AutoMethodProps) {
       dialog.clear()
       return
     }
-    await sdk.client.instance.dispose()
+    // #362：不再调 instance.dispose()——服务端 authSet/callback handler 已刷新 Provider 缓存。
+    // 仅靠 bootstrap() 刷新 TUI 本地数据，运行中的会话不受影响。
     await sync.bootstrap()
     dialog.replace(() => <DialogModel providerID={props.providerID} />)
   })
@@ -329,7 +330,7 @@ function CodeMethod(props: CodeMethodProps) {
           code: value,
         })
         if (!error) {
-          await sdk.client.instance.dispose()
+          // #362：不再调 instance.dispose()——服务端 callback handler 已刷新 Provider 缓存
           await sync.bootstrap()
           dialog.replace(() => <DialogModel providerID={props.providerID} />)
           return
@@ -402,7 +403,7 @@ function ApiMethod(props: ApiMethodProps) {
             ...(props.metadata ? { metadata: props.metadata } : {}),
           },
         })
-        await sdk.client.instance.dispose()
+        // #362：不再调 instance.dispose()——服务端 authSet handler 已刷新 Provider 缓存
         await sync.bootstrap()
         if (props.custom && !sync.data.provider_next.all.some((provider) => provider.id === props.providerID)) {
           toast.show({
diff --git a/packages/tui/src/context/sync.tsx b/packages/tui/src/context/sync.tsx
index d89cbcabb..8a4231da6 100644
--- a/packages/tui/src/context/sync.tsx
+++ b/packages/tui/src/context/sync.tsx
@@ -461,10 +461,15 @@ export const {
       switch (event.type) {
         case "global.disposed":
           // #273：仅响应 daemon 主动关闭，其他崩溃走重连
-          if ((event.properties as { reason?: string }).reason !== "daemon-stop") break
-          daemonStopSeen = true
-          // #273：直接调用 exit 函数，原因作为参数传递
-          void exit("opencode daemon stopped.")
+          if ((event.properties as { reason?: string }).reason === "daemon-stop") {
+            daemonStopSeen = true
+            // #273：直接调用 exit 函数，原因作为参数传递
+            void exit("opencode daemon stopped.")
+          } else {
+            // #362：非 daemon-stop 的 global.disposed 来自 auth 变更后的 Provider 缓存刷新。
+            // 不销毁实例，仅刷新 TUI 本地数据以获取最新 provider 列表。
+            void bootstrap()
+          }
           break
         case "server.instance.disposed":
           // #273：daemon stop 已设置退出标志，不再重连
