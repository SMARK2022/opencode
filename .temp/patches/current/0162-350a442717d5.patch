From 350a442717d5a4b99cc56ea6bc5a4f4b29cf9be9 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Mon, 18 May 2026 13:06:30 +0800
Subject: [PATCH] Refactor tests and add new functionality

- Update `defaultAgent` test to reflect changes in expected behavior when build is disabled.
- Introduce new tests for `diffLineStats` to validate line counting logic in TUI diff display.

diff --git a/packages/tui/src/context/sync.tsx b/packages/tui/src/context/sync.tsx
index 1111111111..2222222222 100644
--- a/packages/tui/src/context/sync.tsx
+++ b/packages/tui/src/context/sync.tsx
@@ -370,7 +370,8 @@
             }),
           )
           const updated = store.message[event.properties.info.sessionID]
-          if (updated.length > 100) {
+          // #162：本地缓存消息数阈值与 200 窗口对齐
+          if (updated.length > 200) {
             const oldest = updated[0]
             batch(() => {
               setStore(
@@ -660,7 +661,8 @@
             // #153：force-sync 必须并拉取 session_status，否则 stale-busy 无法清 busy 位
             const [session, messages, todo, diff, status] = await Promise.all([
               sdk.client.session.get({ sessionID }, { throwOnError: true }),
-              sdk.client.session.messages({ sessionID, limit: 100 }),
+              // #162：会话消息拉取窗口扩至 200，降低长会话过早截断
+              sdk.client.session.messages({ sessionID, limit: 200 }),
               sdk.client.session.todo({ sessionID }),
               sdk.client.session.diff({ sessionID }),
               sdk.client.session.status({ workspace: project.workspace.current() }),
@@ -682,8 +684,10 @@
                     (message) => tracker.messages.has(message.id) && !infos.some((item) => item.id === message.id),
                   ),
                 )
-                const removed = infos.slice(0, -100)
-                const visible = infos.slice(-100)
+                // #162：裁掉窗口外旧消息，保留最近 200
+                const removed = infos.slice(0, -200)
+                // #162：可见消息切片与 hydrate limit 一致
+                const visible = infos.slice(-200)
                 const visibleIDs = new Set(visible.map((message) => message.id))
                 for (const message of messages.data ?? []) {
                   if (!visibleIDs.has(message.info.id)) {
diff --git a/packages/tui/test/cli/cmd/tui/sync-live-hydration.test.tsx b/packages/tui/test/cli/cmd/tui/sync-live-hydration.test.tsx
--- a/packages/tui/test/cli/cmd/tui/sync-live-hydration.test.tsx
+++ b/packages/tui/test/cli/cmd/tui/sync-live-hydration.test.tsx
@@ -179,3 +179,4 @@
-test("live messages merged during hydration retain the 100 message window", async () => {
+// #162：窗口扩大后仍必须真正越界，验证实时合并与最旧 parts 的淘汰。
+test("live messages merged during hydration retain the 200 message window", async () => {
   await using tmp = await tmpdir()
   await Bun.write(`${tmp.path}/kv.json`, "{}")
@@ -204,5 +205,5 @@
     resolveMessages(
       json(
-        Array.from({ length: 100 }, (_, index) => {
+        Array.from({ length: 200 }, (_, index) => {
           const id = `msg_${String(index).padStart(3, "0")}`
           return {
@@ -215,6 +216,6 @@
     await hydrate
 
-    expect(sync.data.message[sessionID]).toHaveLength(100)
+    expect(sync.data.message[sessionID]).toHaveLength(200)
     expect(sync.data.message[sessionID].at(-1)?.id).toBe(live.id)
     expect(sync.data.message[sessionID].some((message) => message.id === "msg_000")).toBe(false)
     expect(sync.data.part.msg_000).toBeUndefined()
diff --git a/packages/tui/test/runtime.test.tsx b/packages/tui/test/runtime.test.tsx
--- a/packages/tui/test/runtime.test.tsx
+++ b/packages/tui/test/runtime.test.tsx
@@ -1,3 +1,4 @@
 import { expect, test } from "bun:test"
+import path from "node:path"
 import { testRender } from "@opentui/solid"
 import { abbreviateHome } from "../src/runtime"
@@ -6,5 +7,6 @@
 test("abbreviates paths within home boundaries", () => {
   expect(abbreviateHome("/home/test", "/home/test")).toBe("~")
-  expect(abbreviateHome("/home/test/project", "/home/test")).toBe("~/project")
+  // 缩写使用本机路径分隔符，Windows 不能用 POSIX 字面量判定回归。
+  expect(abbreviateHome("/home/test/project", "/home/test")).toBe(path.join("~", "project"))
   expect(abbreviateHome("/home/tester/project", "/home/test")).toBe("/home/tester/project")
   expect(abbreviateHome("/tmp/project", "/home/test")).toBe("/tmp/project")
--
2.39.0

