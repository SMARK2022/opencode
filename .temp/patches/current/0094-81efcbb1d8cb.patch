From 81efcbb1d8cbe426e69801803e5aa33180485ca1 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Thu, 7 May 2026 01:09:51 +0800
Subject: [PATCH] =?UTF-8?q?feat(notebook):=20=E4=BC=98=E5=8C=96=E5=8D=95?=
 =?UTF-8?q?=E5=85=83=E6=BA=90=E4=BB=A3=E7=A0=81=E6=B8=B2=E6=9F=93=E9=80=BB?=
 =?UTF-8?q?=E8=BE=91=EF=BC=8C=E8=B0=83=E6=95=B4=E5=AD=97=E8=8A=82=E9=99=90?=

diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
index 82a1c4bc83..f6cb6dda02 100644
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -1893,9 +1893,11 @@ function GenericTool(props: ToolProps) {
   const ctx = use()
   const output = createMemo(() => props.output?.trim() ?? "")
   const [expanded, setExpanded] = createSignal(false)
-  const maxLines = 3
-  const maxChars = createMemo(() => maxLines * Math.max(20, ctx.width - 6))
-  const collapsed = createMemo(() => collapseToolOutput(output(), maxLines, maxChars()))
+  // SMARK #94：预览 10 行；仅当总行数 >20 才折叠
+  const maxLines = 10
+  const threshold = 20
+  const maxChars = createMemo(() => threshold * Math.max(20, ctx.width - 6))
+  const collapsed = createMemo(() => collapseToolOutput(output(), maxLines, maxChars(), threshold))
   const limited = createMemo(() => {
     if (expanded() || !collapsed().overflow) return output()
     return collapsed().output
@@ -2168,9 +2170,17 @@ function Shell(props: ToolProps) {
     return stripAnsi(text?.trim() ?? "")
   })
   const [expanded, setExpanded] = createSignal(false)
+  // SMARK #94：运行中或上下文输出禁用折叠；空闲时 preview 10 / threshold 20
   const maxLines = 10
-  const maxChars = createMemo(() => maxLines * Math.max(20, ctx.width - 6))
-  const collapsed = createMemo(() => collapseToolOutput(output(), maxLines, maxChars()))
+  const threshold = 20
+  const maxChars = createMemo(() => threshold * Math.max(20, ctx.width - 6))
+  const collapsed = createMemo(() => {
+    if (isRunning() || showContextOutput()) {
+      // 与 SMARK maxLines=0 等价：不折叠，全量展示流式/上下文输出
+      return { output: output(), overflow: false }
+    }
+    return collapseToolOutput(output(), maxLines, maxChars(), threshold)
+  })
   const limited = createMemo(() => {
     if (expanded() || !collapsed().overflow) return output()
     return collapsed().output
@@ -2242,9 +2252,11 @@ function Write(props: ToolProps) {
   const code = createMemo(() => {
     return stringValue(props.input.content) ?? ""
   })
-  const maxLines = 20
-  const maxChars = createMemo(() => maxLines * Math.max(20, ctx.width - 6))
-  const collapsed = createMemo(() => collapseToolOutput(code(), maxLines, maxChars()))
+  // SMARK #94 Write：预览 10，阈值 20（对齐 BlockTool 契约）
+  const maxLines = 10
+  const threshold = 20
+  const maxChars = createMemo(() => threshold * Math.max(20, ctx.width - 6))
+  const collapsed = createMemo(() => collapseToolOutput(code(), maxLines, maxChars(), threshold))
   const limited = createMemo(() => {
     if (expanded() || !collapsed().overflow) return code()
     return collapsed().output
@@ -2279,7 +2291,8 @@ function Write(props: ToolProps) {
           onClick={diffOverflow() ? () => setExpanded((prev) => !prev) : undefined}
         >
           {/* diff 高度限制：折叠时最多显示 15 行，展开时显示全部 */}
-          <box paddingLeft={1} maxHeight={expanded() ? undefined : 15}>
+          <box paddingLeft={1} // SMARK #94：Edit 折叠预览 10 行；overflow 仍用 total>20
+          maxHeight={expanded() ? undefined : 10}>
             <diff
               diff={diff()!}
               syncScroll={true}
@@ -2624,6 +2637,7 @@ function Edit(props: ToolProps) {
     return { added, removed, total: lines.length }
   })
   // 超过 20 行时启用展开/收缩，避免长 diff 占用过多屏幕
+  // SMARK #94 threshold=20：仅当 diff 总行数>20 才折叠
   const overflow = createMemo(() => stats().total > 20)
 
   return (
@@ -2635,7 +2649,8 @@ function Edit(props: ToolProps) {
           onClick={overflow() ? () => setExpanded((prev) => !prev) : undefined}  // 点击展开/收缩 diff
         >
           {/* 使用 maxHeight 限制初始显示高度，展开时移除限制 */}
-          <box paddingLeft={1} maxHeight={expanded() ? undefined : 15}>
+          <box paddingLeft={1} // SMARK #94：Edit 折叠预览 10 行；overflow 仍用 total>20
+          maxHeight={expanded() ? undefined : 10}>
             <diff
               // 同步滚动：diff 视图与代码视图同步滚动，提升编辑体验
               syncScroll={true}
@@ -2718,6 +2733,7 @@ function ApplyPatch(props: ToolProps) {
             // patch 内容展开/收缩：多文件 patch 超过 20 行时收缩，避免长 patch 占满终端
             const [patchExpanded, setPatchExpanded] = createSignal(false)
             const patchLines = createMemo(() => (file.patch || "").split("\n"))
+            // SMARK #94 threshold=20：patch 行数>20 才折叠
             const patchOverflow = createMemo(() => patchLines().length > 20)
             return (
             <BlockTool
@@ -2734,7 +2750,8 @@ function ApplyPatch(props: ToolProps) {
                 }
               >
                 {/* 内联 diff 组件：移除独立 Diff 函数，直接使用 maxHeight 控制折叠 */}
-                    <box paddingLeft={1} maxHeight={patchExpanded() ? undefined : 15}>
+                    <box paddingLeft={1} // SMARK #94：ApplyPatch 折叠预览 10 行；overflow 仍用 lines>20
+                    maxHeight={patchExpanded() ? undefined : 10}>
                       <diff
                         syncScroll={true}
                         diff={file.patch || ""}
diff --git a/packages/tui/src/util/collapse-tool-output.ts b/packages/tui/src/util/collapse-tool-output.ts
index 4b4c8d87b9..18e78a33ff 100644
--- a/packages/tui/src/util/collapse-tool-output.ts
+++ b/packages/tui/src/util/collapse-tool-output.ts
@@ -1,10 +1,18 @@
-export function collapseToolOutput(output: string, maxLines: number, maxChars: number) {
+// threshold：仅当总行数超过阈值才折叠；maxLines 为折叠后预览行数
+export function collapseToolOutput(
+  output: string,
+  maxLines: number,
+  maxChars: number,
+  threshold: number = maxLines,
+) {
   const lines = output.split("\n")
-  if (lines.length <= maxLines && Array.from(output).length <= maxChars) {
+  // 未超 threshold 行且未超字符预算：全量展示（SMARK 10/20 契约）
+  if (lines.length <= threshold && Array.from(output).length <= maxChars) {
     return { output, overflow: false }
   }
 
-  const preview = lines.slice(0, maxLines).join("\n")
+  // 折叠预览用 maxLines 行；字符预算仍保护窄终端
+  const preview = lines.slice(0, Math.max(0, maxLines)).join("\n")
   if (Array.from(preview).length > maxChars) {
     return {
       output:
@@ -15,5 +23,9 @@ export function collapseToolOutput(output: string, maxLines: number, maxChars: n
     }
   }
 
-  return { output: [...lines.slice(0, maxLines), "…"].join("\n"), overflow: true }
+  return {
+    output: preview + (lines.length > maxLines ? "\n…" : ""),
+    overflow: true,
+  }
 }
+
diff --git a/sdks/vscode/src/notebook/source.ts b/sdks/vscode/src/notebook/source.ts
index e6786da5cd..3995caad51 100644
--- a/sdks/vscode/src/notebook/source.ts
+++ b/sdks/vscode/src/notebook/source.ts
@@ -9,7 +9,6 @@
  *   B: <source line last>
  *
  * Cell headers do NOT consume line numbers. Line ranges in headers are
- // 无 cellId 时返回全本；有 cellId 时只导出目标 cell 虚拟切片
  * 1-based inclusive. Target a cell with `cellId` (Copilot-style #VSC-xxxxxxxx).
  */
 import * as vscode from "vscode"
@@ -26,10 +25,7 @@ import { resolveNotebook } from "./resolve"
  * Bridge endpoint handler: returns notebook source code with global line numbering.
  * Primary input: `cellId` (#VSC-xxxxxxxx). Output capped at 24 KB.
  */
-// 获取 notebook 源码：返回所有 cell 的代码、类型和虚拟行号范围
-// 获取 notebook 源码：返回所有 cell 的代码和虚拟行号范围
-// 虚拟源码视图：按 computeVirtualRanges 输出带 range 的 header
-// 虚拟源码视图：header 带 range，正文行号与 computeVirtualRanges 对齐
+// #94：renderedLines 与 bytesCut 分离；header 不占行预算
 export async function notebookSource(input: Record<string, unknown>) {
   const filePath = stringProp(input, "filePath")
   if (!filePath) throw new Error("filePath is required")
@@ -37,7 +33,6 @@ export async function notebookSource(input: Record<string, unknown>) {
   // filePath 必填：禁止 active-notebook 静默回退
   const notebook = await resolveNotebook(filePath)
   // 默认 limit 400：与 read 工具 DEFAULT_READ_LIMIT 对齐，压低上下文
-  // 默认 limit 400：与 read 工具 DEFAULT_READ_LIMIT 对齐压低上下文
   const limit = Math.max(1, numberProp(input, "limit") ?? 400)
   const offset = Math.max(1, numberProp(input, "offset") ?? 1)
 
@@ -75,14 +70,18 @@ export async function notebookSource(input: Record<string, unknown>) {
   globalStart = Math.max(1, Math.min(globalStart, totalLines > 0 ? totalLines : 1))
 
   // Render cells one by one, emitting headers + in-range source lines
+  // 24KB 字节预算；满额后设 bytesCut 并 break 外层
   const maxBytes = 24 * 1024
   let bytes = 0
-  let rendered = 0
-  let cut = false
+  // renderedLines：仅计数带全局行号的源码行，header 不计
+  let renderedLines = 0
+  // bytesCut：仅字节预算截断时为 true，与行限分离
+  let bytesCut = false
+  let lineLimited = false
   let lastRenderedLine = 0
   const outputCells: string[] = []
 
-  for (const cell of notebook.getCells()) {
+  outer: for (const cell of notebook.getCells()) {
     const cellRange = ranges.get(cell.index)
     if (!cellRange) continue
 
@@ -94,20 +93,21 @@ export async function notebookSource(input: Record<string, unknown>) {
       continue
     }
 
-    // Render header
-    if (rendered >= limit) {
-      cut = true
+    // 行预算已满：禁止再发 header，避免无编号源码的悬空 header
+    if (renderedLines >= limit) {
+      lineLimited = true
       break
     }
+
+    // header 不消耗 renderedLines 行预算，但仍计入字节预算
     const headerLine = buildHeader(cell, cellRange)
     const hdrBytes = Buffer.byteLength(headerLine, "utf8") + 4 // "--: "
     if (bytes + hdrBytes > maxBytes && outputCells.length > 0) {
-      cut = true
+      bytesCut = true
       break
     }
     outputCells.push(headerLine)
     bytes += hdrBytes
-    rendered++
 
     // Render source lines
     // lineAt：避免整 cell getText+split 的内存与 EOL 误差
@@ -118,30 +118,36 @@ export async function notebookSource(input: Record<string, unknown>) {
       if (globalLineNum > globalEnd) {
         break
       }
-      if (rendered >= limit) {
-        cut = true
-        break
+      // 行数达限只停止追加；bytesCut 仅由字节预算触发
+      if (renderedLines >= limit) {
+        lineLimited = true
+        break outer
       }
       const sourceLine = cell.document.lineAt(i).text
       // 不再截断单行：字节预算只截断总输出
       const outLine = `${globalLineNum}: ${sourceLine}`
       const outBytes = Buffer.byteLength(outLine, "utf8")
       if (bytes + outBytes > maxBytes && outputCells.length > 0) {
-        cut = true
-        break
+        // 仅字节预算设 bytesCut
+        bytesCut = true
+        break outer
       }
       outputCells.push(outLine)
       bytes += outBytes
-      rendered++
+      renderedLines++
       lastRenderedLine = globalLineNum
     }
 
-    if (cut) break
-
+    // cell 结束后：行/字节任一截断则退出外层，避免下一 cell 再开 header
+    if (bytesCut || lineLimited || renderedLines >= limit) {
+      if (renderedLines >= limit) lineLimited = true
+      break
+    }
   }
 
-  if (!cut) lastRenderedLine = Math.max(lastRenderedLine, Math.min(globalEnd, totalLines))
-  const more = lastRenderedLine < globalEnd
+  if (!bytesCut && !lineLimited) lastRenderedLine = Math.max(lastRenderedLine, Math.min(globalEnd, totalLines))
+  // more：窗口内仍有未返回行，且不是因为 24KB 截断
+  const more = !bytesCut && lastRenderedLine < globalEnd
 
   let output = warning ? `${warning}\n\n` : ""
   output +=
@@ -150,9 +156,10 @@ export async function notebookSource(input: Record<string, unknown>) {
     ) + "\n"
   output += outputCells.join("\n")
 
-  if (cut) {
+  // 页脚仅在 bytesCut 时提示 24KB；行限走 more 分页文案
+  if (bytesCut) {
     output += `\n\n(Output capped at 24 KB. Showing lines ${globalStart}-${lastRenderedLine}. Use offset=${lastRenderedLine + 1} to continue.)`
-  } else if (more) {
+  } else if (more || lineLimited) {
     output += `\n\n(Showing lines ${globalStart}-${lastRenderedLine} of ${totalLines}. Use offset=${lastRenderedLine + 1} to continue.)`
   }
   output += "\n</content>"
@@ -166,9 +173,12 @@ export async function notebookSource(input: Record<string, unknown>) {
       cellId: cellId ?? (targetCellIndex !== undefined ? copilotLikeCellId(notebook.cellAt(targetCellIndex)) : undefined),
       globalStart,
       limit,
-      returned: outputCells.length,
+      // returned 使用源码行计数，不含 header
+      returned: renderedLines,
       totalLines,
-      truncated: cut || more,
+      truncated: bytesCut || more || lineLimited,
+      bytesCut,
+      lineLimited,
     },
   }
 }
