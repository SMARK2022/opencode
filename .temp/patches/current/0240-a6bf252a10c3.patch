From a6bf252a10c38e0af49f0f34c64a436ee7838f4f Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Wed, 27 May 2026 18:11:34 +0800
Subject: [PATCH] =?UTF-8?q?feat(logging):=20=E4=BC=98=E5=8C=96=E6=97=A5?=
 =?UTF-8?q?=E5=BF=97=E8=AE=B0=E5=BD=95=EF=BC=8C=E8=B0=83=E6=95=B4=E5=8F=91?=
 =?UTF-8?q?=E5=B8=83=E6=97=A5=E5=BF=97=E7=BA=A7=E5=88=AB=E4=B8=BADEBUG?=
 =?UTF-8?q?=EF=BC=8C=E6=B7=BB=E5=8A=A0fetch=E8=AF=B7=E6=B1=82=E7=9A=84?=
 =?UTF-8?q?=E8=AF=A6=E7=BB=86=E6=97=B6=E9=97=B4=E8=B7=9F=E8=B8=AA?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
diff --git a/packages/opencode/src/provider/provider.ts b/packages/opencode/src/provider/provider.ts
--- a/packages/opencode/src/provider/provider.ts
+++ b/packages/opencode/src/provider/provider.ts
@@ -82,17 +82,70 @@ export function requestUserAgent(options: Readonly<Record<string, unknown>>) {
   return headerUserAgent ?? (typeof options["header-ua"] === "string" ? options["header-ua"] : undefined)
 }
 
-function wrapSSE(res: Response, ms: number, ctl: AbortController) {
+// 进程内关联 id：只串联同一次 daemon log 内的 fetch.start/response 与 SSE 里程碑，不跨运行
+let providerFetchRequestID = 0
+
+type FetchTiming = {
+  requestID: number
+  providerID: string
+  modelID: string
+  sessionID?: string
+  startedAt: number
+  log: (fields: Record<string, unknown>) => void
+}
+
+function timing(input: FetchTiming, phase: string, extra?: Record<string, unknown>) {
+  // phase 字符串是稳定契约：区分停滞发生在响应头前、原始 SSE 块前还是 AI SDK 语义事件前
+  // 原生回调必须复用所属 Provider 的 logger；裸 runSync 会把诊断写进 CLI stdout。
+  input.log({
+    phase,
+    requestID: input.requestID,
+    providerID: input.providerID,
+    modelID: input.modelID,
+    sessionID: input.sessionID,
+    elapsedMs: Date.now() - input.startedAt,
+    ...extra,
+  })
+}
+
+function errorFields(error: unknown) {
+  if (error instanceof Error) {
+    const code = "code" in error && typeof error.code === "string" ? error.code : undefined
+    return { errorName: error.name, errorMessage: error.message, errorCode: code }
+  }
+  return { errorMessage: String(error) }
+}
+
+function bodyBytes(body: unknown) {
+  // 只度量已缓冲的 body；流与表单不读取——诊断不得消费或复制 provider 请求
+  if (typeof body === "string") return new TextEncoder().encode(body).byteLength
+  if (body instanceof Uint8Array) return body.byteLength
+  return undefined
+}
+
+function wrapSSE(res: Response, ms: number, ctl: AbortController, timingInput: FetchTiming) {
   if (typeof ms !== "number" || ms <= 0) return res
   if (!res.body) return res
   if (!res.headers.get("content-type")?.includes("text/event-stream")) return res
 
   const reader = res.body.getReader()
+  let chunkCount = 0
+  let bytes = 0
+  let maxGapMs = 0
+  let lastChunkAt = Date.now()
   const body = new ReadableStream<Uint8Array>({
     async pull(ctrl) {
       const part = await new Promise<Awaited<ReturnType<typeof reader.read>>>((resolve, reject) => {
         const id = setTimeout(() => {
           const err = Object.assign(new ProviderError.ResponseStreamError("SSE read timed out"), { code: "SSE_READ_TIMEOUT" })
+          // 记录原始 body 层停滞计数：区分「HTTP/SSE body 缺失」与「心跳未变成模型输出」
+          timing(timingInput, "sse.timeout", {
+            chunkTimeoutMs: ms,
+            idleMs: Date.now() - lastChunkAt,
+            chunkCount,
+            bytes,
+            maxGapMs,
+          })
           ctl.abort(err)
           void reader.cancel(err)
           reject(err)
@@ -105,16 +158,29 @@ function wrapSSE(res: Response, ms: number, ctl: AbortController) {
           },
           (err) => {
             clearTimeout(id)
+            const fields = errorFields(err)
+            // SSE_READ_TIMEOUT 已在超时分支记录，这里只补记其它读取错误，避免双条
+            if (fields.errorCode !== "SSE_READ_TIMEOUT") {
+              timing(timingInput, "sse.error", { chunkCount, bytes, maxGapMs, ...fields })
+            }
             reject(err)
           },
         )
       })
 
       if (part.done) {
+        timing(timingInput, "sse.end", { chunkCount, bytes, maxGapMs })
         ctrl.close()
         return
       }
 
+      const now = Date.now()
+      const gapMs = now - lastChunkAt
+      lastChunkAt = now
+      chunkCount++
+      bytes += part.value.byteLength
+      maxGapMs = Math.max(maxGapMs, gapMs)
+      if (chunkCount === 1) timing(timingInput, "sse.first_chunk", { gapMs, bytes: part.value.byteLength })
       ctrl.enqueue(part.value)
     },
     async cancel(reason) {
@@ -1880,3 +1946,3 @@
-    async function resolveSDK(model: Model, s: State, envs: Record<string, string | undefined>) {
+    async function resolveSDK(model: Model, s: State, envs: Record<string, string | undefined>, bridge: EffectBridge.Shape) {
       try {
         const provider = s.providers[model.providerID]
@@ -1992,6 +2058,24 @@ for (const [id, provider] of configProviders) {
 
           // #83：customFetch 仅鉴权透传；默认走 NetworkProxy.fetch 主路由
           // #83：provider 请求 purpose=provider，区别于基础设施和 npm
+          const timingHeaders = new Headers(opts.headers)
+          const timingInput = {
+            requestID: ++providerFetchRequestID,
+            providerID: model.providerID,
+            modelID: model.id,
+            sessionID: timingHeaders.get("x-session-affinity") ?? timingHeaders.get("x-opencode-session") ?? undefined,
+            startedAt: Date.now(),
+            // 保留 Effect 运行环境的 logger 与级别，计时回调不自行创建全局运行时。
+            log: (fields: Record<string, unknown>) =>
+              bridge.fork(Effect.logInfo("timing", fields).pipe(Effect.annotateLogs({ service: "provider.fetch" }))),
+          }
+          // 诊断刻意只保留 provider 本地的传输里程碑与请求大小：
+          // 不记录 body 内容、鉴权头、prompt、工具输出或原始 chunk
+          timing(timingInput, "fetch.start", {
+            method: opts.method,
+            chunkTimeoutMs: typeof chunkTimeout === "number" && chunkTimeout > 0 ? chunkTimeout : undefined,
+            bodyBytes: bodyBytes(opts.body),
+          })
           const res = await (
             customFetch
               ? NetworkProxy.fetchWithRoute(customFetch, input, {
@@ -2006,11 +2090,23 @@ for (const [id, provider] of configProviders) {
                   // @ts-ignore see here: https://github.com/oven-sh/bun/issues/16682
                   timeout: false,
                 })
-          ).finally(() => headerTimeoutCtl?.clear())
+          )
+            .catch((error) => {
+              timing(timingInput, "fetch.error", errorFields(error))
+              throw error
+            })
+            .finally(() => headerTimeoutCtl?.clear())
+
+          const contentType = res.headers.get("content-type") ?? undefined
+          timing(timingInput, "fetch.response", {
+            status: res.status,
+            contentType,
+            isSSE: contentType?.includes("text/event-stream") ?? false,
+          })
 
 
           if (!chunkAbortCtl) return res
-          return wrapSSE(res, chunkTimeout, chunkAbortCtl)
+          return wrapSSE(res, chunkTimeout, chunkAbortCtl, timingInput)
         }
 
         const bundledLoader = BUNDLED_PROVIDERS[model.api.npm]
@@ -2099,8 +2195,10 @@
       if (s.models.has(key)) return s.models.get(key)!
 
+      // 在 Effect 主路径捕获实例与观测环境，再交给 SDK 的异步 fetch/SSE 回调。
+      const bridge = yield* EffectBridge.make()
       const provider = s.providers[model.providerID]
       return yield* EffectPromise.refineRejection(
         async () => {
-          const sdk = await resolveSDK(model, s, envs)
+          const sdk = await resolveSDK(model, s, envs, bridge)
           const language = s.modelLoaders[model.providerID]
             ? await s.modelLoaders[model.providerID](
diff --git a/packages/opencode/test/session/llm.test.ts b/packages/opencode/test/session/llm.test.ts
--- a/packages/opencode/test/session/llm.test.ts
+++ b/packages/opencode/test/session/llm.test.ts
@@ -1,1 +1,2 @@
+import { Logger } from "effect"
 import { PermissionV1 } from "@opencode-ai/core/v1/permission"
@@ -749,6 +750,40 @@
     status: 200,
     headers: { "Content-Type": "text/event-stream" },
   })
 }
 
+function createStalledEventResponse() {
+  const encoder = new TextEncoder()
+  return new Response(
+    new ReadableStream<Uint8Array>({
+      start(controller) {
+        controller.enqueue(encoder.encode(": heartbeat\n\n"))
+      },
+    }),
+    {
+      status: 200,
+      headers: { "Content-Type": "text/event-stream" },
+    },
+  )
+}
+
+function withProviderLogs<A, E, R>(fn: (logs: Record<string, unknown>[]) => Effect.Effect<A, E, R>) {
+  return Effect.suspend(() => {
+    // 每次执行独立收集，不替换全局 console，也不依赖文件 logger 的批量刷盘时序。
+    const logs: Record<string, unknown>[] = []
+    const logger = Logger.make(({ message }) => {
+      for (const value of Array.isArray(message) ? message : [message]) {
+        // 捕获真实结构化字段；timing 与错误日志分别在各自断言中筛选。
+        if (value && typeof value === "object" && !Array.isArray(value)) logs.push(value)
+      }
+    })
+    // 整个测试 Effect 都带此 logger，SDK 构建时捕获的 bridge 才能继承它。
+    return fn(logs).pipe(Effect.provideService(Logger.CurrentLoggers, new Set([logger])))
+  })
+}
+
+function providerFetchEvents(lines: Record<string, unknown>[]) {
+  return lines.filter((line) => "phase" in line && "requestID" in line).map((line) => Object.fromEntries(Object.entries(line).map(([key, value]) => [key, String(value)])))
+}
+
 describe("session.llm.stream", () => {
@@ -827,5 +862,74 @@
       },
     )
   }
 
+  // #240：从真实 Effect logger 检查 fetch/SSE 顺序，CLI stdout 不承担诊断传输。
+  it.instance(
+    "logs provider response and first raw SSE chunk when chunk timeout is enabled",
+    () =>
+      withProviderLogs((logs) => Effect.gen(function* () {
+        const request = waitRequest(
+          "/chat/completions",
+          new Response(createChatStream("Hello"), {
+            status: 200,
+            headers: { "Content-Type": "text/event-stream" },
+          }),
+        )
+
+        const resolved = yield* Provider.use.getModel(ProviderV2.ID.make("timed-openai-compatible"), ModelV2.ID.make("gpt-test"))
+        const sessionID = SessionID.make("session-provider-fetch-timing")
+        const agent = {
+          name: "test",
+          mode: "primary",
+          options: {},
+          permission: [{ permission: "*", pattern: "*", action: "allow" }],
+        } satisfies Agent.Info
+        const user = {
+          id: MessageID.make("msg_user-provider-fetch-timing"),
+          sessionID,
+          role: "user",
+          time: { created: Date.now() },
+          agent: agent.name,
+          model: { providerID: ProviderV2.ID.make("timed-openai-compatible"), modelID: resolved.id },
+        } satisfies SessionV1.User
+
+        yield* drain({
+          user,
+          sessionID,
+          model: resolved,
+          agent,
+          system: ["You are a helpful assistant."],
+          messages: [{ role: "user", content: "Hello" }],
+          tools: {},
+        })
+        yield* Effect.promise(() => request)
+
+        const events = providerFetchEvents(logs)
+        expect(events.map((event) => event.phase)).toEqual(["fetch.start", "fetch.response", "sse.first_chunk", "sse.end"])
+        expect(events.find((event) => event.phase === "fetch.response")?.status).toBe("200")
+        expect(events.find((event) => event.phase === "fetch.response")?.isSSE).toBe("true")
+        expect(Number(events.find((event) => event.phase === "sse.first_chunk")?.bytes)).toBeGreaterThan(0)
+        expect(Number(events.find((event) => event.phase === "sse.end")?.chunkCount)).toBeGreaterThan(0)
+      })),
+    {
+      config: () => ({
+        enabled_providers: ["timed-openai-compatible"],
+        provider: {
+          "timed-openai-compatible": {
+            npm: "@ai-sdk/openai-compatible",
+            name: "Timed Provider",
+            options: { apiKey: "test-key", baseURL: `${state.server!.url.origin}/v1`, chunkTimeout: 1_000 },
+            models: {
+              "gpt-test": {
+                name: "GPT Test",
+                limit: { context: 400000, input: 272000, output: 128000 },
+                modalities: { input: ["text"], output: ["text"] },
+              },
+            },
+          },
+        },
+      }),
+    },
+  )
+
   // #169：openai 别名 provider 上配置 UA 必须无条件到达线上（验证 #169 去掉 startsWith 守卫）
