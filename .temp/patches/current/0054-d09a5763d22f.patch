diff --git a/packages/opencode/test/agent/agent.test.ts b/packages/opencode/test/agent/agent.test.ts
--- a/packages/opencode/test/agent/agent.test.ts
+++ b/packages/opencode/test/agent/agent.test.ts
@@ -725,12 +725,12 @@
 )
 
 it.instance(
-  "defaultAgent returns plan when build is disabled and default_agent not set",
+  "defaultAgent returns interactive when build is disabled and default_agent not set",
   () =>
     Effect.gen(function* () {
       const agent = yield* load((svc) => svc.defaultAgent())
-      // build is disabled, so it should return plan (next primary agent)
-      expect(agent).toBe("plan")
+      // build 禁用后，interactive 是插入顺序中的下一个 primary agent
+      expect(agent).toBe("interactive")
     }),
   {
     config: {
@@ -747,7 +747,10 @@
   {
     config: {
       agent: {
+        // 禁用所有 primary agent 以触发 "no primary visible agent found" 错误
         build: { disable: true },
+        interactive: { disable: true },
+        review: { disable: true },
         plan: { disable: true },
       },
     },

diff --git a/packages/opencode/test/installation/installation.test.ts b/packages/opencode/test/installation/installation.test.ts
--- a/packages/opencode/test/installation/installation.test.ts
+++ b/packages/opencode/test/installation/installation.test.ts
@@ -96,7 +96,8 @@
       Effect.gen(function* () {
         const result = yield* Installation.use.latest("npm")
         expect(result).toBe("1.5.0")
-        expect(npmCalls).toContain(`https://registry.npmjs.org/opencode-ai/${InstallationChannel}`)
+        // 支持自定义 registry（如 npmmirror.com），不硬编码 npmjs.org
+    expect(npmCalls.some((url) => url.endsWith(`/opencode-ai/${InstallationChannel}`))).toBe(true)
       }),
     )
 
@@ -110,7 +111,8 @@
       Effect.gen(function* () {
         const result = yield* Installation.use.latest("bun")
         expect(result).toBe("1.6.0")
-        expect(bunCalls).toContain(`https://registry.npmjs.org/opencode-ai/${InstallationChannel}`)
+        // 支持自定义 registry（如 npmmirror.com），不硬编码 npmjs.org
+    expect(bunCalls.some((url) => url.endsWith(`/opencode-ai/${InstallationChannel}`))).toBe(true)
       }),
     )
 
@@ -124,7 +126,8 @@
       Effect.gen(function* () {
         const result = yield* Installation.use.latest("pnpm")
         expect(result).toBe("1.7.0")
-        expect(pnpmCalls).toContain(`https://registry.npmjs.org/opencode-ai/${InstallationChannel}`)
+        // 支持自定义 registry（如 npmmirror.com），不硬编码 npmjs.org
+    expect(pnpmCalls.some((url) => url.endsWith(`/opencode-ai/${InstallationChannel}`))).toBe(true)
       }),
     )
 

