From f0a47a1448660231bb59c615f204a6010304e2bc Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Tue, 19 May 2026 13:55:28 +0800
Subject: [PATCH] feat: attachment token residual (#175 tip)

diff --git a/packages/core/src/util/attachment-token.ts b/packages/core/src/util/attachment-token.ts
new file mode 100644
new file mode 100644
--- /dev/null
+++ b/packages/core/src/util/attachment-token.ts
@@ -0,0 +1,218 @@
+// #175：媒体 token 与文本密度隔离；daemon 与 TUI 必须共用
+import { Token } from "./token"
+
+export type AttachmentInput = {
+  type?: "file" | "media" | "image"
+  mime?: string
+  mediaType?: string
+  url?: string
+  data?: string
+  filename?: string
+}
+
+export type AttachmentTokenEstimate = {
+  tokens: number
+  rawChars: number
+  textChars: number
+  count: number
+  imageTokens: number
+  pdfTokens: number
+  otherTokens: number
+}
+
+export type SanitizedTextEstimate = {
+  value: unknown
+  text: string
+  textChars: number
+  rawChars: number
+  attachments: AttachmentTokenEstimate
+}
+
+type ParsedPayload = {
+  mime: string
+  payload: string
+}
+
+type PayloadInfo = {
+  payload: string
+  rawChars: number
+  mime?: string
+}
+
+// #175：累加单位元，全字段为 0
+function emptyEstimate(): AttachmentTokenEstimate {
+  // #175：empty 返回全 0，作为 reduce 单位元
+  return { tokens: 0, rawChars: 0, textChars: 0, count: 0, imageTokens: 0, pdfTokens: 0, otherTokens: 0 }
+}
+
+// #175：分项相加，保留 image/pdf/other
+function addEstimate(left: AttachmentTokenEstimate, right: AttachmentTokenEstimate): AttachmentTokenEstimate {
+  return {
+    // #175：tokens 与分项同步累加，禁止只加 tokens
+    tokens: left.tokens + right.tokens,
+    rawChars: left.rawChars + right.rawChars,
+    textChars: left.textChars + right.textChars,
+    count: left.count + right.count,
+    imageTokens: left.imageTokens + right.imageTokens,
+    pdfTokens: left.pdfTokens + right.pdfTokens,
+    otherTokens: left.otherTokens + right.otherTokens,
+  }
+}
+
+// #175：排除数组，只从普通对象抽附件
+function isRecord(value: unknown): value is Record<string, unknown> {
+  return typeof value === "object" && value !== null && !Array.isArray(value)
+}
+
+// #175：仅解析 data: URL；缺逗号则无效
+export function parseDataUrl(url: string): ParsedPayload | undefined {
+  // #175：非 data: 直接拒绝，交给 isRemoteReference 路径
+  if (!url.startsWith("data:")) return undefined
+  const comma = url.indexOf(",")
+  if (comma < 0) return undefined
+  const semicolon = url.indexOf(";")
+  return {
+    mime: url.slice("data:".length, semicolon >= 0 && semicolon < comma ? semicolon : comma),
+    payload: url.slice(comma + 1),
+  }
+}
+
+// #175：优先 url 字段，其次 data
+function payloadInfo(input: AttachmentInput): PayloadInfo | undefined {
+  if (input.url) {
+    const parsed = parseDataUrl(input.url)
+    if (parsed) return { payload: parsed.payload, rawChars: input.url.length, mime: parsed.mime }
+  }
+  if (!input.data) return undefined
+  const parsed = parseDataUrl(input.data)
+  if (parsed) return { payload: parsed.payload, rawChars: input.data.length, mime: parsed.mime }
+  if (input.type === "media" || (input.type === "file" && !isRemoteReference(input.data))) {
+    return { payload: input.data, rawChars: input.data.length }
+  }
+  return undefined
+}
+
+// #175：// 或 scheme: 视为远程，不计 token
+function isRemoteReference(value: string) {
+  return value.startsWith("//") || /^[a-z][a-z0-9+.-]*:/i.test(value)
+}
+
+// #175：占位计入 textChars，替代 raw base64
+function placeholder(input: { mime: string; filename?: string }) {
+  return `[Attached ${input.mime}: ${input.filename ?? "file"}]`
+}
+
+// #175：单附件入口，按 mime 选密度
+export function estimateAttachment(input: AttachmentInput): AttachmentTokenEstimate {
+  const payload = payloadInfo(input)
+  if (!payload?.payload) return emptyEstimate()
+
+  // #175：mime 优先显式字段，其次 payload 解析
+  const mime = input.mime ?? input.mediaType ?? payload.mime ?? "application/octet-stream"
+  const text = placeholder({ mime, filename: input.filename })
+// #175：image 除数 750，结果夹紧到 1..1600
+  const tokens = mime.startsWith("image/")
+    ? Math.max(1, Math.min(1600, Math.round(payload.payload.length / 750)))
+// #175：pdf 除数 1100
+    : mime === "application/pdf"
+      ? Math.max(1, Math.round(payload.payload.length / 1100))
+// #175：其它 MIME 回退 Token.estimate
+      : Math.max(1, Token.estimate(payload.payload))
+
+  return {
+    tokens,
+    rawChars: payload.rawChars,
+    textChars: text.length,
+    count: 1,
+// #175：image 除数 750，结果夹紧到 1..1600
+    imageTokens: mime.startsWith("image/") ? tokens : 0,
+// #175：pdf 除数 1100
+    pdfTokens: mime === "application/pdf" ? tokens : 0,
+    otherTokens: !mime.startsWith("image/") && mime !== "application/pdf" ? tokens : 0,
+  }
+}
+
+// #175：递归消毒并累计 media
+function sanitize(value: unknown): { value: unknown; attachments: AttachmentTokenEstimate } {
+  // #175：数组元素各自 sanitize 再合并 attachments
+  if (Array.isArray(value)) {
+    return value.reduce(
+      (acc, item) => {
+        const next = sanitize(item)
+        return { value: [...acc.value, next.value], attachments: addEstimate(acc.attachments, next.attachments) }
+      },
+      { value: [] as unknown[], attachments: emptyEstimate() },
+    )
+  }
+
+  if (!isRecord(value)) return { value, attachments: emptyEstimate() }
+
+  const attachment = attachmentFromRecord(value)
+  if (attachment) {
+    const estimate = estimateAttachment(attachment)
+    if (estimate.count > 0) return { value: sanitizeAttachmentRecord(value, attachment), attachments: estimate }
+  }
+
+  return Object.entries(value).reduce(
+    (acc, [key, item]) => {
+      const next = sanitize(item)
+      return {
+        value: { ...acc.value, [key]: next.value },
+        attachments: addEstimate(acc.attachments, next.attachments),
+      }
+    },
+    { value: {} as Record<string, unknown>, attachments: emptyEstimate() },
+  )
+}
+
+// #175：识别 file/media/image 三种形态
+function attachmentFromRecord(value: Record<string, unknown>): AttachmentInput | undefined {
+  // #175：file 形态读 url/mime/filename
+  if (value.type === "file") {
+    return {
+      type: "file",
+      mime: typeof value.mime === "string" ? value.mime : undefined,
+      mediaType: typeof value.mediaType === "string" ? value.mediaType : undefined,
+      url: typeof value.url === "string" ? value.url : undefined,
+      data: typeof value.data === "string" ? value.data : undefined,
+      filename: typeof value.filename === "string" ? value.filename : undefined,
+    }
+  }
+  // #175：media 形态可能把数据放在 data
+  if (value.type === "media") {
+    return {
+      type: "media",
+      mediaType: typeof value.mediaType === "string" ? value.mediaType : undefined,
+      data: typeof value.data === "string" ? value.data : undefined,
+      filename: typeof value.filename === "string" ? value.filename : undefined,
+    }
+  }
+  if (value.type === "image" && typeof value.image === "string") return { type: "image", url: value.image, mime: parseDataUrl(value.image)?.mime }
+  return undefined
+}
+
+// #175：只替换 data 载荷为占位字符串
+function sanitizeAttachmentRecord(value: Record<string, unknown>, input: AttachmentInput) {
+  const mime = input.mime ?? input.mediaType ?? parseDataUrl(input.url ?? input.data ?? "")?.mime ?? "attachment"
+  const text = placeholder({ mime, filename: input.filename })
+  if (typeof value.url === "string" && value.url.startsWith("data:")) return { ...value, url: text }
+  if (typeof value.data === "string") return { ...value, data: text }
+  if (typeof value.image === "string" && value.image.startsWith("data:")) return { ...value, image: text }
+  return value
+}
+
+// #175：返回消毒 text、raw 线长与 media 累计
+export function sanitizeModelMessagesForTokenEstimate(messages: unknown): SanitizedTextEstimate {
+  const result = sanitize(messages)
+  const text = JSON.stringify(result.value)
+  return {
+    value: result.value,
+    text,
+    textChars: text.length,
+// #175：rawChars 用未消毒 JSON 对齐 wire
+    rawChars: JSON.stringify(messages).length,
+    attachments: result.attachments,
+  }
+}
+
+export * as AttachmentToken from "./attachment-token"
diff --git a/packages/schema/src/v1/session.ts b/packages/schema/src/v1/session.ts
--- a/packages/schema/src/v1/session.ts
+++ b/packages/schema/src/v1/session.ts
@@ -256,6 +256,19 @@ export const InputBreakdownSchema = Schema.Struct({
     attachments: NonNegativeInt,
     total: NonNegativeInt,
   }),
+  // #175：media 与 messages 分列，避免 base64 污染文本密度
+  // #175：缺省 media 时 TUI 回退旧 attachments 字符路径
+  media: Schema.optional(
+    Schema.Struct({
+      rawChars: NonNegativeInt,
+      textChars: NonNegativeInt,
+      tokens: NonNegativeInt,
+      count: NonNegativeInt,
+      imageTokens: NonNegativeInt,
+      pdfTokens: NonNegativeInt,
+      otherTokens: NonNegativeInt,
+    }),
+  ),
 })
 
 export const StepStartPart = Schema.Struct({
@@ -281,6 +294,18 @@ export const StepStartPart = Schema.Struct({
         attachments: NonNegativeInt,
         total: NonNegativeInt,
       }),
+      // #175：step 快照同步携带 media
+      media: Schema.optional(
+        Schema.Struct({
+          rawChars: NonNegativeInt,
+          textChars: NonNegativeInt,
+          tokens: NonNegativeInt,
+          count: NonNegativeInt,
+          imageTokens: NonNegativeInt,
+          pdfTokens: NonNegativeInt,
+          otherTokens: NonNegativeInt,
+        }),
+      ),
     }),
   ),
 }).annotate({ identifier: "StepStartPart" })
@@ -320,6 +345,18 @@ export const StepFinishPart = Schema.Struct({
         attachments: NonNegativeInt,
         total: NonNegativeInt,
       }),
+      // #175：step 快照同步携带 media
+      media: Schema.optional(
+        Schema.Struct({
+          rawChars: NonNegativeInt,
+          textChars: NonNegativeInt,
+          tokens: NonNegativeInt,
+          count: NonNegativeInt,
+          imageTokens: NonNegativeInt,
+          pdfTokens: NonNegativeInt,
+          otherTokens: NonNegativeInt,
+        }),
+      ),
     }),
   ),
 }).annotate({ identifier: "StepFinishPart" })
diff --git a/packages/opencode/src/session/prompt.ts b/packages/opencode/src/session/prompt.ts
--- a/packages/opencode/src/session/prompt.ts
+++ b/packages/opencode/src/session/prompt.ts
@@ -43,6 +43,7 @@ import { FSUtil } from "@opencode-ai/core/fs-util"
 import { Truncate } from "@/tool/truncate"
 import { Image } from "@/image/image"
 import { decodeDataUrl } from "@/util/data-url"
+import { AttachmentToken } from "@opencode-ai/core/util/attachment-token"
 import { Process } from "@/util/process"
 import { Cause, Effect, Exit, Latch, Layer, Option, Scope, Context, Schema, Types , Semaphore} from "effect"
 import { InstanceState } from "@/effect/instance-state"
@@ -1557,7 +1558,12 @@ const layer = Layer.effect(
             const modelMsgs = yield* MessageV2.toModelMessagesEffect(requestMsgs, model)
             // 提前组装 messages 复用：token 估算与 handle.process 共用同一份，保证口径一致
             const messages = [...modelMsgs, ...(isLastStep ? [{ role: "assistant" as const, content: MAX_STEPS_PROMPT }] : [])]
-            const messagesText = JSON.stringify(messages)
+            // #175：消毒后文本用于估计；raw 单独保留线长
+            // #175：messagesText 不得再含 base64
+            const messageEstimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate(messages)
+            const messagesText = messageEstimate.text
+            // #175：raw 仅用于 inputChars，不参与 Token.estimate 文本路径
+            const rawMessagesText = JSON.stringify(messages)
             // 合并 agent 与 session 权限，仅估算实际发送的工具定义
             const disabledTools = Permission.disabled(Object.keys(tools), Permission.merge(agent.permission, session.permission ?? []))
             const toolsText = Object.entries(tools)
@@ -1574,7 +1580,7 @@ const layer = Layer.effect(
               })
               .join("\n")
             // 三段字符总量近似请求体，除以 chars-per-token 得 token 估算
-            const inputChars = [systemText, messagesText, toolsText].join("\n").length
+            const inputChars = [systemText, rawMessagesText, toolsText].join("\n").length // #175：inputChars 用 raw
             // 输入字符细分：遍历实际请求消息（requestMsgs）计算每类字符数
             let msgUserChars = 0, msgAssistantChars = 0, msgReasoningChars = 0
             let msgToolInputChars = 0, msgToolOutputChars = 0, msgAttachmentChars = 0
@@ -1603,6 +1609,8 @@ const layer = Layer.effect(
               skills: skillsText.length,
               tools: toolsText.length,
               messages: { userText: msgUserChars, assistantText: msgAssistantChars, reasoning: msgReasoningChars, toolInput: msgToolInputChars, toolOutput: msgToolOutputChars, attachments: msgAttachmentChars, total: msgUserChars + msgAssistantChars + msgReasoningChars + msgToolInputChars + msgToolOutputChars + msgAttachmentChars },
+              // #175：count>0 才写 media，兼容无附件会话
+              ...(messageEstimate.attachments.count > 0 ? { media: messageEstimate.attachments } : {}),
             } as SessionV1.StepFinishPart["inputBreakdown"]
             // #140：输入侧比率校准（历史 step-finish inputChars/tokens），夹紧到 [3,4]
             // #140：history 校准样本来自 step-finish.inputChars
@@ -1620,16 +1628,21 @@ const layer = Layer.effect(
               if (typeof chars === "number" && chars > 0 && tokens > 0) {
                 // 附件不参与输入侧 chars/token 密度校准
                 const bd = (sf as any).inputBreakdown
-                historyInputChars += Math.max(0, chars - (bd?.messages?.attachments ?? 0))
+                // #175：history 与 live 同一 raw→text 密度公式
+                historyInputChars += bd?.media
+                  ? Math.max(0, chars - bd.media.rawChars + bd.media.textChars)
+                  : Math.max(0, chars - (bd?.messages?.attachments ?? 0))
                 historyInputTokens += tokens
               }
             }
             // #140：样本不足时回退 Token.estimate；有样本则夹紧比值
+            // #175：文本估计后加 media.tokens，只加一次
             const estimatedInput =
-              historyInputTokens > 0 && historyInputChars > 500
+              (historyInputTokens > 0 && historyInputChars > 500
                 ? Math.round(inputChars / clampCharsPerToken(historyInputChars / historyInputTokens))
-                : tokenEstimate([systemText, messagesText, toolsText].join("\n"))
+                : tokenEstimate([systemText, messagesText, toolsText].join("\n"))) + messageEstimate.attachments.tokens
             // #140：用即将发送的 estimatedInput 做 overflow，而非 lastFinished 历史巨量
+            // #175：overflow 使用含 media 的 estimatedInput
             if (
               yield* compaction.isOverflow({
                 tokens: { input: estimatedInput, output: 0, reasoning: 0, cache: { read: 0, write: 0 } },
diff --git a/packages/tui/src/util/token-accounting.ts b/packages/tui/src/util/token-accounting.ts
--- a/packages/tui/src/util/token-accounting.ts
+++ b/packages/tui/src/util/token-accounting.ts
@@ -1,3 +1,4 @@
+// #175：TUI 固定侧分配 media.tokens，不抢 system/user 文本份额
 // #122：统一 token 统计入口（step / request / session + breakdown 分配）
 /**
  * token-accounting.ts — 统一 token 统计入口
@@ -14,6 +15,7 @@
  * 这不是"估计"，是按字符占比精确分配已确认 token 总量。
  */
 
+import { AttachmentToken } from "@opencode-ai/core/util/attachment-token"
 import { estimateDataUrlInputTokens } from "./token-estimate"
 
 // #122：无历史校准时默认约 4 字符/token
@@ -167,8 +169,11 @@ export function tokenAccounting(
       if (chars && chars >= 100) {
         const tok = p.tokens.input + p.tokens.cache.read + p.tokens.cache.write
         if (tok > 0) {
-          const bd = p.inputBreakdown as { messages?: { attachments?: number } } | undefined
-          ratioChars += Math.max(0, chars - (bd?.messages?.attachments ?? 0))
+          const bd = p.inputBreakdown as { messages?: { attachments?: number }; media?: { rawChars: number; textChars: number } } | undefined
+          // #175：校准剔除 media.raw，加回 text 占位
+          ratioChars += bd?.media
+            ? Math.max(0, chars - bd.media.rawChars + bd.media.textChars)
+            : Math.max(0, chars - (bd?.messages?.attachments ?? 0))
           ratioTokens += tok
         }
       }
@@ -272,7 +277,8 @@ export function tokenAccounting(
       pendingToolResultChars += (p.state.output?.length ?? 0)
       for (const att of (p.state.attachments ?? []) as Array<{ url: string; mime: string }>) {
         // #122：附件 data URL 估算并入待计入 input
-        pendingAttachTokens += estimateDataUrlInputTokens(att.url, att.mime)
+        // #175：pending 附件与 daemon 同口径
+        pendingAttachTokens += AttachmentToken.estimateAttachment({ url: att.url, mime: att.mime, type: "file" }).tokens
       }
     }
     if (p.state?.status === "error") {
@@ -314,6 +320,7 @@ export function tokenAccounting(
     const bd = breakdownSrc.inputBreakdown as {
       system: number; instructions: number; skills: number; tools: number
       messages: { userText: number; assistantText: number; reasoning: number; toolInput: number; toolOutput: number; attachments: number; total: number }
+      media?: { rawChars: number; textChars: number; tokens: number; count: number; imageTokens: number; pdfTokens: number; otherTokens: number }
     }
     // #127：仅 step-finish 且 step 已确认才走 provider 精确 output
   const isConfirmed = (breakdownSrc as any).type === "step-finish" && stepConfirmed
@@ -322,16 +329,25 @@ export function tokenAccounting(
       ? stepSF.tokens.input + stepSF.tokens.cache.read + stepSF.tokens.cache.write
       : (Number((breakdownSrc as any).inputTokens) || Math.round((Number((breakdownSrc as any).inputChars) || 0) / inputRatio))
     const denom = Number((breakdownSrc as any).inputChars)
-
-    // #124/#127：按字符占比分配 input 分类
-    const alloc = (chars: number) => denom > 0 ? Math.round((chars / denom) * confirmedInput) : 0
+    // #175：先固定切走 media.tokens
+    const media = (bd as any).media as { rawChars: number; textChars: number; tokens: number } | undefined
+    const attachmentTokens = media?.tokens
+    // #175：无 media 时保持旧全量分配
+    // #175：textAllocInput 在无 media 时等于 confirmedInput
+    const textAllocInput = attachmentTokens == null ? confirmedInput : Math.max(0, confirmedInput - attachmentTokens)
+    // #175：textDenom = denom - raw + text，至少为 1
+    // #175：全媒体请求 textDenom 保底 1
+    const textDenom = media ? Math.max(1, denom - media.rawChars + media.textChars) : denom
+    const alloc = (chars: number) => textDenom > 0 ? Math.round((chars / textDenom) * textAllocInput) : 0
     const system = alloc(bd.system)
     const instructions = alloc(bd.instructions)
     const skills = alloc(bd.skills)
     const tools = alloc(bd.tools)
     const userMessages = alloc(bd.messages.userText)
     const toolResults = alloc(bd.messages.toolOutput)
-    const attachments = alloc(bd.messages.attachments)
+    // #175：有 media.tokens 则 attachments 用固定值
+    // #175：attachments UI 槽位优先固定 media.tokens
+    const attachments = attachmentTokens ?? alloc(bd.messages.attachments)
     // #127：历史 assistant/reasoning/tool-call 属于 input context，不是当前 step 新输出
     // #127：历史 assistant 文本计入 input context
     const inputAssistantText = alloc(bd.messages.assistantText)
diff --git a/packages/tui/src/util/token-estimate.ts b/packages/tui/src/util/token-estimate.ts
--- a/packages/tui/src/util/token-estimate.ts
+++ b/packages/tui/src/util/token-estimate.ts
@@ -1,3 +1,4 @@
+import { AttachmentToken } from "@opencode-ai/core/util/attachment-token"
 import type { AssistantMessage } from "@opencode-ai/sdk/v2"
 import { estimate } from "@opencode-ai/core/util/token"
 
@@ -14,16 +15,8 @@ export type UsageInfo = {
 // 图片按每 750 字符约 1 token 估算（Anthropic 视觉编码），上限 1600 token
 // PDF 按每 1100 字符约 1 token 估算；其他类型回退到通用文本估算
 export function estimateDataUrlInputTokens(url: string, mime: string) {
-  if (!url.startsWith("data:")) return 0
-  const comma = url.indexOf(",")
-  if (comma < 0) return 0
-  const payloadLength = url.length - comma - 1
-  if (payloadLength <= 0) return 0
-  // 图片 token 随分辨率增长，但 Anthropic 对单图有 1600 token 上限
-  if (mime.startsWith("image/")) return Math.max(1, Math.min(1600, Math.round(payloadLength / 750)))
-  // PDF 按 base64 负载密度估算，每 1100 字符约 1 token
-  if (mime === "application/pdf") return Math.max(1, Math.round(payloadLength / 1100))
-  return Math.max(1, estimate(url.slice(comma + 1)))
+  // #175：委托 core，禁止 TUI 本地 /750 分叉
+  return AttachmentToken.estimateAttachment({ url, mime, type: "file" }).tokens
 }
 
 // 默认 chars-per-token 比率：4 字符约等于 1 token（适用于英文文本）
@@ -53,8 +46,9 @@ export function charsPerTokenFromHistory(
       // 样本不足 100 字符忽略，避免早期噪声主导密度
       if (typeof inputChars !== "number" || inputChars < 100 || inputTokens <= 0) continue
       // 附件字符不参与文本密度校准：视觉/base64 与自然语言密度不同
-      const bd = p.inputBreakdown as { messages?: { attachments?: number } } | undefined
-      totalChars += Math.max(0, inputChars - (bd?.messages?.attachments ?? 0))
+      const bd = p.inputBreakdown as { messages?: { attachments?: number }; media?: { rawChars: number; textChars: number } } | undefined
+      // #175：history 与 live 同一 media 回写
+      totalChars += Math.max(0, inputChars - (bd?.media ? bd.media.rawChars - bd.media.textChars : (bd?.messages?.attachments ?? 0)))
       totalTokens += inputTokens
     }
   }
diff --git a/packages/tui/test/util/token-accounting.test.ts b/packages/tui/test/util/token-accounting.test.ts
--- a/packages/tui/test/util/token-accounting.test.ts
+++ b/packages/tui/test/util/token-accounting.test.ts
@@ -250,4 +250,23 @@ describe("tokenAccounting", () => {
     expect(acc.breakdown!.assistantText).toBe(35)
     expect(acc.breakdown!.toolCalls).toBe(16)
   })
+  // #175：media.tokens=10 固定；system/user 经 textDenom 为 242
+  test("breakdown allocates media tokens outside text character ratio", () => {
+    const msgs = [userMsg("1"), assistantMsg("2", "1", undefined, 10)]
+    const getParts = (_id: string) => [{
+      ...stepFinish("sf", { input: 500, output: 200, reasoning: 0, cache: { read: 0, write: 0 } }, 9_500),
+      inputBreakdown: {
+        system: 1_000, instructions: 0, skills: 0, tools: 0,
+        messages: { userText: 1_000, assistantText: 0, reasoning: 0, toolInput: 0, toolOutput: 0, attachments: 7_500, total: 8_500 },
+        media: { rawChars: 7_500, textChars: 25, tokens: 10, count: 1, imageTokens: 10, pdfTokens: 0, otherTokens: 0 },
+      },
+    }]
+    const acc = tokenAccounting(msgs as any, getParts)
+    // #175：固定侧 10，不按 7500 字符分配
+    expect(acc.breakdown?.attachments).toBe(10)
+    // #175：textDenom 算法回归锁：system 份额 242
+    expect(acc.breakdown?.system).toBe(242)
+    expect(acc.breakdown?.userMessages).toBe(242)
+  })
+
 })
diff --git a/packages/opencode/test/session/prompt.test.ts b/packages/opencode/test/session/prompt.test.ts
--- a/packages/opencode/test/session/prompt.test.ts
+++ b/packages/opencode/test/session/prompt.test.ts
@@ -2867,3 +2867,61 @@ noLLMServer.instance(
     }),
   30_000,
 )
+
+// #175：大图 preflight media.tokens=1600 且 inputTokens<90000
+it.instance(
+  "preflight estimates image attachments as media tokens",
+  () =>
+    Effect.gen(function* () {
+      const { llm } = yield* useServerConfig((url) => {
+        const base = providerCfg(url)
+        return {
+          ...base,
+          compaction: { reserved: 1_000 },
+          provider: {
+            test: {
+              ...base.provider.test,
+              models: {
+                "test-model": {
+                  ...base.provider.test.models["test-model"],
+                  attachment: true,
+                  limit: { context: 100_000, output: 10_000 },
+                },
+              },
+            },
+          },
+        }
+      })
+      const prompt = yield* SessionPrompt.Service
+      const sessions = yield* Session.Service
+      const chat = yield* sessions.create({
+        title: "Pinned",
+        permission: [{ permission: "*", pattern: "*", action: "allow" }],
+      })
+      const payload = Buffer.from(
+        yield* Effect.promise(() =>
+          Bun.file(path.join(import.meta.dir, "../tool/fixtures/large-image.png")).arrayBuffer(),
+        ),
+      ).toString("base64")
+      yield* prompt.prompt({
+        sessionID: chat.id,
+        agent: "build",
+        noReply: true,
+        parts: [
+          { type: "text", text: "describe this image" },
+          { type: "file", mime: "image/png", filename: "large-image.png", url: `data:image/png;base64,${payload}` },
+        ],
+      })
+      yield* llm.text("image accepted", { usage: { input: 1_700, output: 1 } })
+      const result = yield* prompt.loop({ sessionID: chat.id })
+      expect(result.info.role).toBe("assistant")
+      if (result.info.role !== "assistant") return
+      expect(result.parts.some((p) => p.type === "text" && p.text === "image accepted")).toBe(true)
+      // #175：若仍按 base64/4，inputTokens 会远超 90k
+      expect(result.info.inputTokens).toBeLessThan(90_000)
+      // #175：large-image 夹紧上限 1600
+      expect(result.info.inputBreakdown?.media?.tokens).toBe(1_600)
+      expect(yield* llm.calls).toBe(1)
+    }),
+  { git: true },
+)
diff --git a/packages/opencode/test/util/attachment-token.test.ts b/packages/opencode/test/util/attachment-token.test.ts
new file mode 100644
new file mode 100644
--- /dev/null
+++ b/packages/opencode/test/util/attachment-token.test.ts
@@ -0,0 +1,41 @@
+// #175：单测锁定消毒、远程跳过与分项 token
+import { describe, expect, test } from "bun:test"
+import { AttachmentToken } from "@opencode-ai/core/util/attachment-token"
+
+describe("AttachmentToken", () => {
+  test("estimates image data URLs separately from sanitized text", () => {
+    const payload = "a".repeat(7500)
+    const estimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate([
+      {
+        role: "user",
+        content: [
+          { type: "text", text: "look at this" },
+          { type: "file", mediaType: "image/png", url: `data:image/png;base64,${payload}`, filename: "chart.png" },
+          { type: "file", url: `data:application/pdf;base64,${payload}` },
+        ],
+      },
+    ])
+
+    expect(estimate.attachments.tokens).toBe(17)
+    expect(estimate.attachments.rawChars).toBe(`data:image/png;base64,${payload}`.length + `data:application/pdf;base64,${payload}`.length)
+    expect(estimate.text).not.toContain(payload)
+    expect(estimate.text).toContain("[Attached image/png: chart.png]")
+    expect(estimate.text).toContain("[Attached application/pdf: file]")
+  })
+
+  test("does not treat remote file URLs as uploaded media payloads", () => {
+    const estimate = AttachmentToken.sanitizeModelMessagesForTokenEstimate([
+      {
+        role: "user",
+        content: [
+          { type: "file", mediaType: "image/png", data: "https://example.com/image.png" },
+          { type: "file", mediaType: "image/png", data: "//example.com/image.png" },
+        ],
+      },
+    ])
+
+    expect(estimate.attachments.tokens).toBe(0)
+    expect(estimate.text).toContain("https://example.com/image.png")
+    expect(estimate.text).toContain("//example.com/image.png")
+  })
+})
