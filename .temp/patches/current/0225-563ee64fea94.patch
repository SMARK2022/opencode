From 563ee64fea946ae9212f87899521f643904a4fb9 Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Mon, 25 May 2026 15:49:17 +0800
Subject: [PATCH] fix: 统一使用小写的 "auto" 替代 "Auto" 以保持一致性

diff --git a/packages/opencode/src/agent/agent.ts b/packages/opencode/src/agent/agent.ts
index 2055531..1feaa36 100644
--- a/packages/opencode/src/agent/agent.ts
+++ b/packages/opencode/src/agent/agent.ts
@@ -85,10 +85,11 @@ type State = Omit<Interface, "generate">
 
 // #200：保留代理名集合——permission-reviewer 的安全不变量不可被用户配置放宽
 const RESERVED_AGENT_NAMES = new Set(["permission-reviewer"])
-// #201：显式选择的主 Agent 集合——Auto 可见且可经 default_agent 选择，但它会把
+// #201：显式选择的主 Agent 集合——auto 可见且可经 default_agent 选择，但它会把
 // #201：shell 权限切到 auto 分支。为保持"只有显式选择才启用 auto"的不变量，未配置
 // #201：default_agent 时的隐式回退跳过此处列出的名称，避免禁用其他主 Agent 后意外进入 auto 路由
-const EXPLICIT_ONLY_PRIMARY_AGENT_NAMES = new Set(["Auto"])
+// #225：agent key 统一小写 auto——与 metadata.agent 比较及 default_agent 配置保持大小写一致
+const EXPLICIT_ONLY_PRIMARY_AGENT_NAMES = new Set(["auto"])
 
 export class Service extends Context.Service<Service, Interface>()("@opencode/Agent") {}
 
@@ -184,11 +185,12 @@ const layer = Layer.effect(
             mode: "primary",
             native: true,
           },
-          // #203：Auto 复用 build 权限形状，但只把 shell 命令接入 auto。edit
+          // #203：auto 复用 build 权限形状，但只把 shell 命令接入 auto。edit
           // #203：等非 shell 写入仍沿用 build 的 allow，避免本轮 shell sandbox 修复
           // #203：扩大到新的工具审批面
-          Auto: {
-            name: "Auto",
+          // #225：原生 auto agent 的 key/name 均用小写，避免大小写敏感比较错配
+          auto: {
+            name: "auto",
             description: "Build-like agent that routes shell commands through auto permission review.",
             options: {},
             permission: Permission.merge(
@@ -412,7 +414,7 @@ const layer = Layer.effect(
 
         const list = Effect.fnUntraced(function* () {
           const cfg = yield* config.get()
-          // #201：Agent 列表 localeCompare 排序——Auto 是大写可见 Agent，显式使用 localeCompare
+          // #201：Agent 列表 localeCompare 排序——auto 是可见 Agent，显式使用 localeCompare
           // #201：语义而非码点排序，避免影响 mixed-case 自定义 Agent 展示顺序；默认 Agent 仍居首
           const defaultName = cfg.default_agent ?? "build"
           return Object.values(agents).toSorted((a, b) => {
@@ -431,8 +433,9 @@ const layer = Layer.effect(
             if (agent.hidden === true) throw new Error(`default agent "${c.default_agent}" is hidden`)
             return agent
           }
-          // #201：Auto 隐式默认排除——保持历史回退语义：无显式 default_agent 时不因新增可见
-          // #201：Auto 主 Agent 就自动启用 auto shell 路由；Auto 仍可经 default_agent 精确选择
+          // #225：隐式默认排除集合用小写 auto key，与 agents 对象的键保持一致
+          // #201：auto 隐式默认排除——保持历史回退语义：无显式 default_agent 时不因新增可见
+          // #201：auto 主 Agent 就自动启用 auto shell 路由；auto 仍可经 default_agent 精确选择
           const visible = Object.entries(agents).find(
             ([name, agent]) =>
               agent.mode !== "subagent" && agent.hidden !== true && !EXPLICIT_ONLY_PRIMARY_AGENT_NAMES.has(name),
diff --git a/packages/opencode/src/permission/auto.ts b/packages/opencode/src/permission/auto.ts
index 80ca94a..9d54b3e 100644
--- a/packages/opencode/src/permission/auto.ts
+++ b/packages/opencode/src/permission/auto.ts
@@ -68,7 +68,7 @@ export function evaluate(
     const isShellExternalDirectory = input.permission === "external_directory" && input.metadata.action_kind === "shell"
     // #210：tool-origin external_directory 携带 action_kind/tool/operation 证据——仅凭路径不构成授权上下文
     const isToolExternalDirectory = input.permission === "external_directory" && hasToolExternalDirectoryEvidence(input.metadata)
-    // #203：四级预审路由——safe 直接允许；开发期 shell general 也直接允许，避免 Auto shell
+    // #203：四级预审路由——safe 直接允许；开发期 shell general 也直接允许，避免 auto shell
     // #203：回到人工确认；非 shell general 和 cautious 进入 reviewer/user fallback；dangerous 直接拒绝
     // #203：strict 是用户显式配置的例外，保留原本"低风险也审"的能力
     if (precheck.level === "dangerous") return { action: "deny", reason: precheck.reason, source: "precheck" } satisfies Decision
@@ -88,18 +88,19 @@ export function evaluate(
       return { action: "allow", reason: precheck.reason, source: "precheck" } satisfies Decision
     }
     if (precheck.level === "general" && isShell && !input.strict) {
-      // #203：开发测试期要求 Auto shell 不弹人工确认：未命中 dangerous/cautious 的命令先放行；
+      // #203：开发测试期要求 auto shell 不弹人工确认：未命中 dangerous/cautious 的命令先放行；
       // #203：非 shell general 继续走 reviewer
       return { action: "allow", reason: precheck.reason, source: "precheck" } satisfies Decision
     }
 
     // #203：cautious 是 review 边界——确定性 precheck 发现可见风险但非立即禁止，reviewer 必须显式返回 allow/deny
-    // #203：原生 Auto 不能在 reviewer 缺失时降级回可点击的用户 ask；fail closed 防止敏感 shell 输出因点 Allow 暴露
+    // #203：原生 auto 不能在 reviewer 缺失时降级回可点击的用户 ask；fail closed 防止敏感 shell 输出因点 Allow 暴露
     if (!reviewer) {
-      // #203：显式 user reviewer 配置强于原生 Auto fail-closed 默认——保留小型/自定义运行时
+      // #203：显式 user reviewer 配置强于原生 auto fail-closed 默认——保留小型/自定义运行时
       // #203：无 reviewer service 但有意保留用户审批作为 review 边界的场景
       if (input.reviewerDisabled) return { action: "ask", reason: precheck.reason, source: "reviewer_unavailable" } satisfies Decision
-      if (input.metadata.agent === "Auto") {
+      // #225：metadata.agent 与 agent key 同为小写 auto——大小写敏感比较在此对齐
+      if (input.metadata.agent === "auto") {
         return { action: "deny", reason: `auto reviewer unavailable: ${precheck.reason}`, source: "reviewer" } satisfies Decision
       }
       return { action: "ask", reason: precheck.reason, source: "reviewer_unavailable" } satisfies Decision
diff --git a/packages/opencode/src/permission/reviewer/service.ts b/packages/opencode/src/permission/reviewer/service.ts
index 9fe118d..de1ac55 100644
--- a/packages/opencode/src/permission/reviewer/service.ts
+++ b/packages/opencode/src/permission/reviewer/service.ts
@@ -83,7 +83,7 @@ export const layer = Layer.effect(
       Effect.gen(function* () {
         const cfg = yield* config.get()
         const permission = cfg.permission
-        // #203：reviewerEnabled 统一判定——显式 user 禁用，显式 auto_review 启用，无配置时原生 Auto 隐式启用
+        // #203：reviewerEnabled 统一判定——显式 user 禁用，显式 auto_review 启用，无配置时原生 auto 隐式启用
         if (!reviewerEnabled(permission, input.metadata)) return yield* new ReviewerDisabled()
         const autoReview = permission?.auto_review
 
@@ -654,11 +654,12 @@ function isReviewerError(error: unknown): error is Error {
 }
 
 // #203：reviewer 启用判定——显式配置优先：user 保留人工审批，auto_review 对所有 auto 规则启用。
-// #203：无全局配置时原生 Auto agent 隐式启用 reviewer；否则选 Auto 只会把 cautious 降级成普通 ask
+// #203：无全局配置时原生 auto agent 隐式启用 reviewer；否则选 auto 只会把 cautious 降级成普通 ask
 function reviewerEnabled(permission: ConfigPermissionV1.Info | undefined, metadata: Readonly<Record<string, unknown>>) {
   if (permission?.approvals_reviewer === "user") return false
   if (permission?.approvals_reviewer === "auto_review") return true
-  return metadata.agent === "Auto"
+  // #225：reviewer 隐式启用判定与小写 agent key 对齐
+  return metadata.agent === "auto"
 }
 
 // #198：提取错误消息——provider/model 错误在 reason 中暴露用于诊断，但 AutoDeniedError 仍阻止鼓励绕过策略的重试建议
diff --git a/packages/opencode/test/agent/agent.test.ts b/packages/opencode/test/agent/agent.test.ts
index ba711ff..4e3ae96 100644
--- a/packages/opencode/test/agent/agent.test.ts
+++ b/packages/opencode/test/agent/agent.test.ts
@@ -49,8 +49,9 @@ it.instance("returns default native agents when no config", () =>
     const agents = yield* load((svc) => svc.list())
     const names = agents.map((a) => a.name)
     expect(names).toContain("build")
-    // #201：Auto 必须像其他原生主 Agent 一样可见，用户才能经 default_agent 显式进入 auto shell 路由
-    expect(names).toContain("Auto")
+    // #201：auto 必须像其他原生主 Agent 一样可见，用户才能经 default_agent 显式进入 auto shell 路由
+    // #225：断言改为小写 auto，与 agent key 一致
+    expect(names).toContain("auto")
     // interactive 仍保留：旧 review 迁移目标
     expect(names).toContain("interactive")
     expect(names).toContain("plan")
@@ -77,7 +78,7 @@ it.instance("build agent has correct default properties", () =>
   }),
 )
 
-// #201：interactive 审批回归——新增 Auto Agent 不能改变 interactive 既有用户审批门禁
+// #201：interactive 审批回归——新增 auto Agent 不能改变 interactive 既有用户审批门禁
 // #201：覆盖 shell 与 notebook 执行权限，防止新增 agent 时误把 ask 改成 auto/allow
 it.instance("interactive agent keeps existing ask gates for command execution", () =>
   Effect.gen(function* () {
@@ -89,16 +90,17 @@ it.instance("interactive agent keeps existing ask gates for command execution",
   }),
 )
 
-// #201：Auto 行为级测试——只固定公开权限结果，不检查内部规则顺序或 helper 名
+// #201：auto 行为级测试——只固定公开权限结果，不检查内部规则顺序或 helper 名
 // #203：后续可重构 auto 分支实现，但必须维持 "build-like + shell:auto" 的用户契约
-it.instance("Auto agent routes shell through auto while preserving build-like permissions", () =>
+it.instance("auto agent routes shell through auto while preserving build-like permissions", () =>
   Effect.gen(function* () {
-    const auto = yield* load((svc) => svc.get("Auto"))
+    // #225：get 用小写 agent key
+    const auto = yield* load((svc) => svc.get("auto"))
     expect(auto).toBeDefined()
     expect(auto?.mode).toBe("primary")
     expect(auto?.native).toBe(true)
 
-    // #203：Auto 开发期只接管 shell；edit 仍继承 build 的 allow，避免把 task、edit
+    // #203：auto 开发期只接管 shell；edit 仍继承 build 的 allow，避免把 task、edit
     // #203：或 notebook 执行也引入 auto 分支从而扩大本次切入面
     expect(evalPerm(auto, "bash")).toBe("auto")
     expect(evalPerm(auto, "external_directory")).toBe("auto")
@@ -108,7 +110,7 @@ it.instance("Auto agent routes shell through auto while preserving build-like pe
     expect(evalPerm(auto, "vscode_notebook_env")).toBe("allow")
     expect(evalPerm(auto, "vscode_notebook_edit")).toBe("allow")
 
-    // #201：这些边界继承自 build 的默认安全策略，不因 Auto 的 shell auto 路由而放宽
+    // #201：这些边界继承自 build 的默认安全策略，不因 auto 的 shell auto 路由而放宽
     expect(evalPerm(auto, "question")).toBe("allow")
     expect(evalPerm(auto, "plan_enter")).toBe("allow")
     expect(evalPerm(auto, "repo_clone")).toBe("allow")
@@ -731,18 +733,19 @@ it.instance(
   },
 )
 
-// #201：Auto default_agent 选择测试——Auto 是显式可选主 Agent，验证用户可经 default_agent 选择它
+// #201：auto default_agent 选择测试——auto 是显式可选主 Agent，验证用户可经 default_agent 选择它
 // #201：而无配置默认值仍保持 build（上面的 EXPLICIT_ONLY 排除保证）
+// #225：default_agent 配置与断言统一小写 auto
 it.instance(
-  "defaultAgent can select the Auto shell-auto agent",
+  "defaultAgent can select the auto shell-auto agent",
   () =>
     Effect.gen(function* () {
       const agent = yield* load((svc) => svc.defaultAgent())
-      expect(agent).toBe("Auto")
+      expect(agent).toBe("auto")
     }),
   {
     config: {
-      default_agent: "Auto",
+      default_agent: "auto",
     },
   },
 )
@@ -828,7 +831,7 @@ it.instance("decide agent denies tool permissions", () =>
 )
 
 it.instance(
-  "defaultAgent throws when only explicit Auto primary agent remains",
+  "defaultAgent throws when only explicit auto primary agent remains",
   () => expectDefaultAgentError("no primary visible agent found"),
   {
     config: {
@@ -839,7 +842,7 @@ it.instance(
         // #159：disable decide 而非已移除的 review
         decide: { disable: true },
         plan: { disable: true },
-        // #201：Auto 在这里故意保持启用。它可见且可经 default_agent 选择，但不能
+        // #201：auto 在这里故意保持启用。它可见且可经 default_agent 选择，但不能
         // #201：仅因为标准主 Agent 被禁用就成为无配置 fallback（EXPLICIT_ONLY 排除）
       },
     },
diff --git a/packages/opencode/test/permission/auto.test.ts b/packages/opencode/test/permission/auto.test.ts
index d980acd..4641097 100644
--- a/packages/opencode/test/permission/auto.test.ts
+++ b/packages/opencode/test/permission/auto.test.ts
@@ -331,7 +331,7 @@ describe("permission auto routing", () => {
     expect(called).toBe(false)
   })
 
-  // #203：开发期 shell general 直接 allow——Auto shell 不弹人工确认；未命中 dangerous/cautious 的命令先放行
+  // #203：开发期 shell general 直接 allow——auto shell 不弹人工确认；未命中 dangerous/cautious 的命令先放行
   test("allows shell general decisions during development without reviewer load", async () => {
     let called = false
     await expect(
@@ -357,27 +357,28 @@ describe("permission auto routing", () => {
     await expect(bash("git add .")).resolves.toMatchObject({ action: "ask", source: "reviewer_unavailable" })
   })
 
-  // #203：原生 Auto reviewer 缺失时 fail closed——cautious 不降级为可点击 ask，防止点 Allow 暴露敏感 shell 输出
-  test("fails closed for native Auto when reviewer is not wired", async () => {
+  // #225：测试 metadata.agent 用小写 auto，与 agent key 对齐
+  // #203：原生 auto reviewer 缺失时 fail closed——cautious 不降级为可点击 ask，防止点 Allow 暴露敏感 shell 输出
+  test("fails closed for native auto when reviewer is not wired", async () => {
     await expect(
       Effect.runPromise(
         PermissionAuto.evaluate({
           permission: "bash",
           patterns: ["git add ."],
-          metadata: { action_kind: "shell", command: "git add .", agent: "Auto" },
+          metadata: { action_kind: "shell", command: "git add .", agent: "auto" },
         }),
       ),
     ).resolves.toMatchObject({ action: "deny", source: "reviewer" })
   })
 
-  // #203：显式 user reviewer 配置强于原生 Auto fail-closed——保留用户审批作为 review 边界
-  test("honors explicit user reviewer configuration for native Auto", async () => {
+  // #203：显式 user reviewer 配置强于原生 auto fail-closed——保留用户审批作为 review 边界
+  test("honors explicit user reviewer configuration for native auto", async () => {
     await expect(
       Effect.runPromise(
         PermissionAuto.evaluate({
           permission: "bash",
           patterns: ["git add ."],
-          metadata: { action_kind: "shell", command: "git add .", agent: "Auto" },
+          metadata: { action_kind: "shell", command: "git add .", agent: "auto" },
           reviewerDisabled: true,
         }),
       ),
diff --git a/packages/opencode/test/permission/reviewer-prompt.test.ts b/packages/opencode/test/permission/reviewer-prompt.test.ts
index 118b5c4..caccfd5 100644
--- a/packages/opencode/test/permission/reviewer-prompt.test.ts
+++ b/packages/opencode/test/permission/reviewer-prompt.test.ts
@@ -15,7 +15,8 @@ const userInfo = (id: string): User => ({
   sessionID,
   role: "user",
   time: { created: 0 },
-  agent: "Auto",
+  // #225：测试消息 agent 字段用小写 auto
+  agent: "auto",
   model: { providerID: ProviderV2.ID.make("test"), modelID: ModelV2.ID.make("model") },
 })
 // #206：构造助手消息——包含 parentID/path/cost/tokens 等 Assistant 必填字段
@@ -28,7 +29,7 @@ const assistantInfo = (id: string, parentID = "msg_user_0"): Assistant => ({
   modelID: ModelV2.ID.make("model"),
   providerID: ProviderV2.ID.make("test"),
   mode: "build",
-  agent: "Auto",
+  agent: "auto",
   path: { cwd: "/repo", root: "/repo" },
   cost: 0,
   tokens: { input: 0, output: 0, reasoning: 0, cache: { read: 0, write: 0 } },
diff --git a/packages/opencode/test/session/prompt.test.ts b/packages/opencode/test/session/prompt.test.ts
index a7bcf64..bb46680 100644
--- a/packages/opencode/test/session/prompt.test.ts
+++ b/packages/opencode/test/session/prompt.test.ts
@@ -1297,7 +1297,8 @@ it.instance(
           sessionID: chat.id,
           permission: "bash",
           patterns: [command],
-          metadata: { command, agent: "Auto" },
+          // #225：测试 metadata.agent 用小写 auto
+          metadata: { command, agent: "auto" },
           always: ["*"],
           ruleset: [{ permission: "bash", pattern: "*", action: "auto" }],
         })
@@ -1392,7 +1393,7 @@ it.instance(
         sessionID: chat.id,
         permission: "bash",
         patterns: [command],
-        metadata: { command, agent: "Auto" },
+        metadata: { command, agent: "auto" },
         always: ["*"],
         ruleset: [{ permission: "bash", pattern: "*", action: "auto" }],
       })
