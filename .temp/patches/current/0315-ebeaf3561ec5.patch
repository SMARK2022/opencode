diff --git a/packages/opencode/test/session/prompt.test.ts b/packages/opencode/test/session/prompt.test.ts
--- a/packages/opencode/test/session/prompt.test.ts
+++ b/packages/opencode/test/session/prompt.test.ts
@@ -95,6 +95,9 @@ function withSh<A, E, R>(fx: () => Effect.Effect<A, E, R>) {
   )
 }
 
+// #315：短会话用例在 Windows CI 上冷启动开销大；win32 放宽到 15s，其他平台保持 3s 快速反馈。
+const shortSessionTimeout = process.platform === "win32" ? 15_000 : 3_000
+
 function shortShellDelayCommand() {
   // Windows 的默认 shell 会把 `sleep 0.2` 解析成 PowerShell 命令，CI 冷启动时足以吃掉 3s 用例预算。
   // 这里使用测试进程已依赖的 PATH 内 `bun` 执行短延迟脚本，避免 PowerShell 对引号路径需要 `&` 的差异。
@@ -2154,7 +2157,7 @@ it.instance(
       yield* Fiber.await(fiber)
       expect((yield* status.get(chat.id)).type).toBe("idle")
     }),
-  3_000,
+  shortSessionTimeout,
 )
 
 // Cancel semantics
@@ -2363,7 +2366,7 @@ raceNoLLMServer.instance(
       }
     }),
   { config: cfg },
-  3_000,
+  shortSessionTimeout,
 )
 
 noLLMServer.instance(
diff --git a/packages/opencode/test/tool/shell.test.ts b/packages/opencode/test/tool/shell.test.ts
--- a/packages/opencode/test/tool/shell.test.ts
+++ b/packages/opencode/test/tool/shell.test.ts
@@ -156,9 +156,12 @@ const withShell = <A, E, R>(item: { label: string; shell: string }, self: Effect
 const each = (
   name: string,
   fn: (item: { label: string; shell: string }) => Effect.Effect<void, unknown, ShellTestServices>,
+  // #315：basic 用例贯穿 shell+fixture 完整链路，Windows CI 需要显式更大预算；
+  // 其余用例不传 timeout，保持 it.live 默认行为不变。
+  timeout?: number,
 ) => {
   for (const item of shells) {
-    it.live(`${name} [${item.label}]`, () => withShell(item, fn(item)))
+    it.live(`${name} [${item.label}]`, () => withShell(item, fn(item)), timeout)
   }
 }
 
@@ -221,7 +224,7 @@ describe("tool.shell", () => {
         expect(result.metadata.output).toContain("test")
       }),
     ),
-  )
+   60_000)
 
   it.live("falls back from terminal-only configured shell", () =>
     Effect.gen(function* () {
diff --git a/packages/opencode/test/cli/help/help-snapshots.test.ts b/packages/opencode/test/cli/help/help-snapshots.test.ts
--- a/packages/opencode/test/cli/help/help-snapshots.test.ts
+++ b/packages/opencode/test/cli/help/help-snapshots.test.ts
@@ -88,7 +88,8 @@
 // Fixed wrap width so a developer's terminal doesn't affect snapshots.
 // yargs honors COLUMNS; CI runners typically default to 80 which produces
 // different wraps from a 200-col local terminal.
-const SNAPSHOT_ENV = { COLUMNS: "120" }
+// 快照约定英文；LC_ALL 优先于宿主的其他语言变量，不改变真实 CLI 的本地化行为。
+const SNAPSHOT_ENV = { COLUMNS: "120", LC_ALL: "en_US.UTF-8" }
 
 describe("opencode CLI help-text snapshots", () => {
   // Single test, parallel spawns. Each command's help fires under
diff --git a/packages/opencode/test/cli/run/run-process.test.ts b/packages/opencode/test/cli/run/run-process.test.ts
--- a/packages/opencode/test/cli/run/run-process.test.ts
+++ b/packages/opencode/test/cli/run/run-process.test.ts
@@ -264,6 +264,7 @@
         expect(explicitlyDenied.stdout).toContain("continued after explicit denial")
         expect(yield* Effect.promise(() => Bun.file(`${home}/explicitly-denied`).exists())).toBe(false)
       }),
-    60_000,
+    // 三次冷启动各自仍受 30s 限制；Windows 总预算不能先截断第三次权限断言。
+    process.platform === "win32" ? 120_000 : 60_000,
   )
 
