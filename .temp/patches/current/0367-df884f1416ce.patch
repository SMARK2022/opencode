From df884f1416ce81225f85c6bcc7e8164349a07bac Mon Sep 17 00:00:00 2001
From: SMARK2022 <3222301373@qq.com>
Date: Sat, 4 Jul 2026 17:49:22 +0800
Subject: [PATCH] =?UTF-8?q?feat(session):=20=E7=A7=BB=E9=99=A4=20goal=20?=
 =?UTF-8?q?=E5=AE=9E=E9=AA=8C=E6=80=A7=E5=BC=80=E5=85=B3=EF=BC=8C=E6=B7=BB?=
 =?UTF-8?q?=E5=8A=A0=20/goal=20TUI=20=E5=91=BD=E4=BB=A4?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

将 goal_max_turns 从 experimental 提升到顶层 config 字段，goal 功能不再
需要 experimental.goals 开关，始终可用。prompt loop 续跑检查和 registry
工具注册均移除条件判断。

新增 /goal TUI 命令（dialog-goal.tsx）：无 goal 时弹出 DialogPrompt 输入
objective，有 goal 时弹出 DialogSelect 选择 Edit/Pause/Resume/Clear。
SDK 调用使用 as any 绕过未重新生成的类型（与 sync.tsx 一致）。

修复续跑消息渲染：移除独立的 goal 渲染块（双 Show 双 id 导致堆叠时间距
累积），改为在 text memo 中统一提取 objective 并反转义 XML 实体，走正常
用户消息渲染路径。
diff --git a/packages/core/src/v1/config/config.ts b/packages/core/src/v1/config/config.ts
index b1d3ae727..774d1682c 100644
--- a/packages/core/src/v1/config/config.ts
+++ b/packages/core/src/v1/config/config.ts
@@ -173,6 +173,11 @@ export const Info = Schema.Struct({
       }),
     }),
   ),
+  // [local-smark] goal 续跑上限：0 = 禁用续跑，默认 10。
+  // 0367 起从 experimental 提升为顶层字段，goal 功能始终可用
+  goal_max_turns: Schema.optional(NonNegativeInt).annotate({
+    description: "Maximum auto-continue turns per goal (default: 10, 0 to disable)",
+  }),
   experimental: Schema.optional(
     Schema.Struct({
       disable_paste_summary: Schema.optional(Schema.Boolean),
@@ -186,14 +191,6 @@ export const Info = Schema.Struct({
       continue_loop_on_deny: Schema.optional(Schema.Boolean).annotate({
         description: "Continue the agent loop when a tool call is denied",
       }),
-      // #364：goal 功能开关：启用后持久化会话目标与自动续跑可用
-      goals: Schema.optional(Schema.Boolean).annotate({
-        description: "Enable persistent session goals with auto-continue",
-      }),
-      // #364：单个 goal 最多自动续跑轮数，防止无限循环
-      goal_max_turns: Schema.optional(NonNegativeInt).annotate({
-        description: "Maximum auto-continue turns per goal (default: 10)",
-      }),
       mcp_timeout: Schema.optional(PositiveInt).annotate({
         description: "Timeout in milliseconds for model context protocol (MCP) requests",
       }),
diff --git a/packages/opencode/src/session/prompt.ts b/packages/opencode/src/session/prompt.ts
index 54c4307a3..6a140d227 100644
--- a/packages/opencode/src/session/prompt.ts
+++ b/packages/opencode/src/session/prompt.ts
@@ -1490,9 +1490,10 @@ const layer = Layer.effect(
             // 使用独立 goalTurns 计数器防止无限循环（step++ 在 continue 后跳过）。
             // 仅对主 session（无 parentID）和非 decide agent 生效。
             const cfg = yield* config.get()
-            const maxGoalTurns = cfg.experimental?.goal_max_turns ?? 10
+            // [local-smark] goal 续跑不再需要 experimental 开关，始终可用；
+            // goal_max_turns 已提升为顶层 config 字段（0367）
+            const maxGoalTurns = cfg.goal_max_turns ?? 10
             if (
-              cfg.experimental?.goals === true &&
               maxGoalTurns > 0 &&
               !session.parentID &&
               goalTurns < maxGoalTurns
diff --git a/packages/opencode/src/tool/registry.ts b/packages/opencode/src/tool/registry.ts
index 8c766dc76..fe5e0bd09 100644
--- a/packages/opencode/src/tool/registry.ts
+++ b/packages/opencode/src/tool/registry.ts
@@ -114,7 +114,7 @@ const layer = Layer.effect(
     const greptool = yield* GrepTool
     const patchtool = yield* ApplyPatchTool
     const skilltool = yield* SkillTool
-    // #364：goal 工具：仅当 config.experimental.goals 启用时可见
+    // [local-smark] goal 工具：始终注册（0367 起无 experimental 开关）
     const goaltool = yield* GoalTool
     const agent = yield* Agent.Service
     const codeMode = flags.experimentalCodeMode ? yield* Effect.promise(() => import("./code-mode")) : undefined
@@ -205,9 +205,6 @@ const layer = Layer.effect(
           }
         }
 
-        const cfg = yield* config.get()
-        // #364：goal 工具仅在 experimental.goals 启用时可见
-        const goalsEnabled = cfg.experimental?.goals === true
         const questionEnabled = ["app", "cli", "desktop"].includes(flags.client) || flags.enableQuestionTool
 
         const tool = yield* Effect.all({
@@ -253,8 +250,8 @@ const layer = Layer.effect(
             tool.patch,
             tool.permission_review_decision,
             ...(tool.execute ? [tool.execute] : []),
-            // #364：goal 工具：仅在 experimental.goals 启用时暴露给模型
-            ...(goalsEnabled ? [tool.goal] : []),
+            // [local-smark] goal 工具：始终暴露给模型，不再需要 experimental 开关
+            tool.goal,
             ...(flags.experimentalLspTool ? [tool.lsp] : []),
             ...(flags.experimentalPlanMode && flags.client === "cli" ? [tool.plan] : []),
           ],
diff --git a/packages/opencode/test/session/goal.test.ts b/packages/opencode/test/session/goal.test.ts
index 1efc0a23a..e4094e07c 100644
--- a/packages/opencode/test/session/goal.test.ts
+++ b/packages/opencode/test/session/goal.test.ts
@@ -10,10 +10,14 @@ import { InstanceStore } from "@/project/instance-store"
 import { InstanceBootstrap } from "@/project/bootstrap"
 import { EventV2Bridge } from "@/event-v2-bridge"
 import { SessionProjector } from "@opencode-ai/core/session/projector"
+import { Config } from "@/config/config"
+import { TestConfig } from "../fixture/config"
 import { testEffect } from "../lib/effect"
 
 // #364：测试 layer 组装：SessionGoal 依赖 EventV2Bridge + Database，
 // Session 依赖 InstanceStore 等，使用与 session.test.ts 相同的模式。
+// [local-smark] Config 层供 goal_max_turns 顶层读取测试；TestConfig 提供
+// 空配置桩（get 返回 {}），goal_max_turns 缺省回退到 10 的断言因此成立。
 const it = testEffect(
   AppNodeBuilder.build(
     LayerNode.group([
@@ -23,6 +27,7 @@ const it = testEffect(
       SessionProjector.node,
       CrossSpawnSpawner.node,
       InstanceStore.node,
+      Config.node,
     ]),
     [
       [RuntimeFlags.node, RuntimeFlags.layer({ experimentalWorkspaces: false })],
@@ -30,6 +35,7 @@ const it = testEffect(
         InstanceBootstrap.node,
         Layer.succeed(InstanceBootstrap.Service, InstanceBootstrap.Service.of({ run: Effect.void })),
       ],
+      [Config.node, TestConfig.layer()],
     ],
   ),
 )
@@ -396,3 +402,20 @@ it.instance(
     }),
   { git: true },
 )
+
+// [local-smark] 验证 goal_max_turns 从顶层 config 读取，不在 experimental 下
+// goal 功能不再需要 experimental.goals 开关，始终可用
+it.instance(
+  "goal_max_turns is readable from top-level config without experimental gate",
+  () =>
+    Effect.gen(function* () {
+      const config = yield* Config.Service
+      const cfg = yield* config.get()
+      // goal_max_turns 在顶层可读，默认 10（TestConfig 空桩下回退到默认值）
+      const maxTurns = (cfg as any).goal_max_turns ?? 10
+      expect(maxTurns).toBe(10)
+      // experimental.goals 不再存在，goal 功能始终可用
+      expect((cfg as any).experimental?.goals).toBeUndefined()
+    }),
+  { git: true },
+)
diff --git a/packages/tui/src/app.tsx b/packages/tui/src/app.tsx
index 44dadf590..a5417dbac 100644
--- a/packages/tui/src/app.tsx
+++ b/packages/tui/src/app.tsx
@@ -49,6 +49,8 @@ import { DialogThemeList } from "./component/dialog-theme-list"
 import { DialogHelp } from "./ui/dialog-help"
 import { DialogAgent } from "./component/dialog-agent"
 import { DialogSessionList } from "./component/dialog-session-list"
+// [local-smark] goal dialog：无 goal 时设置框，有 goal 时管理菜单
+import { DialogGoal, DialogGoalMenu } from "./component/dialog-goal"
 import { DialogWorkspaceList } from "./component/dialog-workspace-list"
 import { DialogConsoleOrg } from "./component/dialog-console-org"
 import { ThemeProvider, useTheme } from "./context/theme"
@@ -644,6 +646,26 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
           dialog.replace(() => <DialogSessionList />)
         },
       },
+      // [local-smark] /goal 命令：无 goal 时弹出设置框，有 goal 时弹出管理菜单。
+      // goal 功能 0367 起始终可用（不再需要 experimental.goals 开关）
+      {
+        name: "goal.manage",
+        title: "Set or manage goal",
+        slashName: "goal",
+        run: () => {
+          if (route.data.type !== "session") return
+          // 类型守卫后提取 sessionID，避免 union 类型推断问题
+          const sessionID = route.data.sessionID
+          const goal = sync.data.session_goal[sessionID]
+          if (goal) {
+            dialog.replace(() => <DialogGoalMenu sessionID={sessionID} />)
+          } else {
+            dialog.replace(() => <DialogGoal sessionID={sessionID} />)
+          }
+        },
+        category: "Session",
+        suggested: route.data.type === "session",
+      },
       {
         name: "session.new",
         title: "New session",
diff --git a/packages/tui/src/component/dialog-goal.tsx b/packages/tui/src/component/dialog-goal.tsx
new file mode 100644
index 000000000..f3b93ff25
--- /dev/null
+++ b/packages/tui/src/component/dialog-goal.tsx
@@ -0,0 +1,142 @@
+import { DialogPrompt } from "../ui/dialog-prompt"
+import { DialogSelect } from "../ui/dialog-select"
+import { useDialog } from "../ui/dialog"
+import { useSync } from "../context/sync"
+import { useSDK } from "../context/sdk"
+import { useToast } from "../ui/toast"
+import { createMemo } from "solid-js"
+
+// [local-smark] goal 管理 dialog
+// 无 goal 时 → DialogPrompt 输入 objective 直接创建
+// 有 goal 时 → DialogSelect 选择 Edit/Pause/Resume/Clear
+// Edit 选中后用 dialog.replace 切换到 DialogPrompt（无返回导航，可接受）
+// 0367 起 goal 不再依赖 experimental.goals 开关，命令始终注册
+
+interface DialogGoalProps {
+  sessionID: string
+}
+
+// 无 goal 时显示：输入 objective 创建新 goal
+export function DialogGoal(props: DialogGoalProps) {
+  const dialog = useDialog()
+  const sdk = useSDK()
+  const toast = useToast()
+
+  return (
+    <DialogPrompt
+      title="Set Goal"
+      placeholder="Enter your objective..."
+      onConfirm={(value) => {
+        // 空 objective 不允许创建
+        if (!value.trim()) {
+          toast.show({ message: "Objective must not be empty", variant: "error" })
+          return
+        }
+        // SDK 未重新生成，用 as any 绕过方法不存在检查
+        // (与 sync.tsx 中 goal 读取的 as any 绕过策略一致)
+        void (sdk.client.session as any)
+          .goalSet({ sessionID: props.sessionID, objective: value })
+          .then(() => dialog.clear())
+          .catch(() => toast.show({ message: "Failed to set goal", variant: "error" }))
+      }}
+      onCancel={() => dialog.clear()}
+    />
+  )
+}
+
+// 有 goal 时显示：管理菜单
+export function DialogGoalMenu(props: DialogGoalProps) {
+  const dialog = useDialog()
+  const sync = useSync()
+  const sdk = useSDK()
+  const toast = useToast()
+
+  const goal = createMemo(() => sync.data.session_goal[props.sessionID])
+
+  // 调用 goalSet API 更新 status，出错时 toast 提示
+  const callSet = (input: { status?: string }) => {
+    void (sdk.client.session as any)
+      .goalSet({ sessionID: props.sessionID, ...input })
+      .then(() => dialog.clear())
+      .catch(() => toast.show({ message: "Failed to update goal", variant: "error" }))
+  }
+
+  // 调用 goalClear API 删除 goal
+  const callClear = () => {
+    void (sdk.client.session as any)
+      .goalClear({ sessionID: props.sessionID })
+      .then(() => dialog.clear())
+      .catch(() => toast.show({ message: "Failed to clear goal", variant: "error" }))
+  }
+
+  return (
+    <DialogSelect
+      title="Manage Goal"
+      options={[
+        {
+          title: "Edit objective",
+          // 显示当前 objective 作为描述
+          description: goal()?.objective,
+          value: "edit",
+          // 选中后替换 dialog 为 DialogPrompt 编辑模式
+          onSelect: () => {
+            dialog.replace(() => <DialogGoalEdit sessionID={props.sessionID} />)
+          },
+        },
+        // active 状态下显示 Pause 选项
+        ...(goal()?.status === "active"
+          ? [{
+              title: "Pause",
+              description: "Pause the active goal",
+              value: "pause",
+              onSelect: () => callSet({ status: "paused" }),
+            }]
+          : []),
+        // paused/blocked 状态下显示 Resume 选项
+        ...(goal()?.status === "paused" || goal()?.status === "blocked"
+          ? [{
+              title: "Resume",
+              description: "Resume the goal to active",
+              value: "resume",
+              onSelect: () => callSet({ status: "active" }),
+            }]
+          : []),
+        {
+          title: "Clear",
+          description: "Remove the goal",
+          value: "clear",
+          onSelect: () => callClear(),
+        },
+      ]}
+    />
+  )
+}
+
+// 编辑 objective 的 dialog：预填充当前 objective
+function DialogGoalEdit(props: DialogGoalProps) {
+  const dialog = useDialog()
+  const sync = useSync()
+  const sdk = useSDK()
+  const toast = useToast()
+
+  const goal = createMemo(() => sync.data.session_goal[props.sessionID])
+
+  return (
+    <DialogPrompt
+      title="Edit Goal Objective"
+      // 预填充当前 objective
+      value={goal()?.objective}
+      onConfirm={(value) => {
+        if (!value.trim()) {
+          toast.show({ message: "Objective must not be empty", variant: "error" })
+          return
+        }
+        void (sdk.client.session as any)
+          .goalSet({ sessionID: props.sessionID, objective: value })
+          .then(() => dialog.clear())
+          .catch(() => toast.show({ message: "Failed to update goal", variant: "error" }))
+      }}
+      onCancel={() => dialog.clear()}
+    />
+  )
+}
\ No newline at end of file
diff --git a/packages/tui/src/routes/session/index.tsx b/packages/tui/src/routes/session/index.tsx
index 0d12889d7..88564d72c 100644
--- a/packages/tui/src/routes/session/index.tsx
+++ b/packages/tui/src/routes/session/index.tsx
@@ -1571,26 +1571,22 @@ function UserMessage(props: {
 }) {
   const ctx = use()
   const local = useLocal()
+  // [local-smark] text memo：包含 synthetic 消息的 objective 提取
+  // synthetic goal 续跑消息不显示原始 XML prompt，只提取 objective 作为用户消息文本
   const text = createMemo(() => {
     const texts = props.parts
       .map((x) => {
-        if (x.type === "text" && !x.synthetic) {
-          return x.text
-        }
-        return null
-      })
-      .filter(Boolean)
-    return texts.join("\n\n")
-  })
-  // #364：goal 续跑 prompt：synthetic 消息也渲染，
-  // 但用 "Goal continuation" 标签区分，让用户知道这一轮是 goal 驱动的
-  const goalText = createMemo(() => {
-    const texts = props.parts
-      .map((x) => {
-        if (x.type === "text" && x.synthetic) {
-          return x.text
-        }
-        return null
+        if (x.type !== "text") return null
+        // 非 synthetic 文本直接使用
+        if (!x.synthetic) return x.text
+        // synthetic 文本从 <objective> 标签提取摘要并反转义 XML 实体，
+        // 走正常用户消息渲染路径，避免独立的 goal 渲染块堆积间距
+        const m = x.text.match(/<objective>([\s\S]*?)<\/objective>/)
+        if (!m) return null
+        return m[1].trim()
+          .replace(/&amp;/g, "&")
+          .replace(/&lt;/g, "<")
+          .replace(/&gt;/g, ">")
       })
       .filter(Boolean)
     return texts.join("\n\n")
@@ -1607,33 +1603,6 @@ function UserMessage(props: {
 
   return (
     <>
-      {/* #364：goal 续跑 prompt 渲染：synthetic 消息用特殊样式显示， */}
-      {/* 让用户知道这是 goal 驱动的续跑而非用户输入 */}
-      <Show when={goalText()}>
-        <box
-          id={props.message.id}
-          border={["left"]}
-          borderColor={theme.textMuted}
-          customBorderChars={SplitBorder.customBorderChars}
-          marginTop={props.index === 0 ? 0 : 1}
-          flexShrink={0}
-        >
-          <box
-            paddingTop={1}
-            paddingBottom={1}
-            paddingLeft={2}
-            backgroundColor={theme.backgroundPanel}
-            flexShrink={0}
-          >
-            <text fg={theme.textMuted}>
-              <b>↻ Goal continuation</b>
-            </text>
-            <text fg={theme.textMuted} wrapMode="word">
-              {goalText()}
-            </text>
-          </box>
-        </box>
-      </Show>
       <Show when={text()}>
         <box
           id={props.message.id}
