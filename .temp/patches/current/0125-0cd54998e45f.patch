From 0cd54998e45fcf5ae9f0cbea5875077df72fe974 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Mon, 11 May 2026 12:37:37 +0800
Subject: [PATCH] =?UTF-8?q?feat(prompt):=20=E9=87=8D=E6=9E=84=E6=8F=90?=
 =?UTF-8?q?=E7=A4=BA=E6=A1=86=EF=BC=8C=E6=B7=BB=E5=8A=A0=E6=B5=81=E5=BC=8F?=
 =?UTF-8?q?=E5=A4=84=E7=90=86=E6=9C=9F=E9=97=B4=E7=9A=84=E6=97=B6=E9=97=B4?=

diff --git a/packages/tui/src/context/editor.ts b/packages/tui/src/context/editor.ts
index ecca8c2daa..2f6a05d77a 100644
--- a/packages/tui/src/context/editor.ts
+++ b/packages/tui/src/context/editor.ts
@@ -8,6 +8,7 @@ import { isRecord } from "../util/record"
 import { useTuiPaths } from "./runtime"
 import { createSimpleContext } from "./helper"
 import { editorIntegration } from "../editor"
+import { fileURLToPath } from "url"
 
 const MCP_PROTOCOL_VERSION = "2025-11-25"
 
@@ -427,6 +428,36 @@ export function editorSelectionKey(selection: EditorSelection | undefined) {
 
 // 从 opencode IDE registry 读取最近 active notebook/text 文件（按 workspace 路径打分）
 // 读 ~/.local/state/opencode/ide 注册表 JSON，按 workspace 路径最长前缀打分
+
+// #125：bridge registry URI → 本地路径（file/git；notebook-cell 忽略）
+function bridgeUriToPath(value?: string) {
+  if (!value) return
+  // #125：file:// 用 fileURLToPath 解 percent-encoding
+  if (value.startsWith("file://")) {
+    try {
+      return fileURLToPath(value)
+    } catch {
+      return decodeURIComponent(new URL(value).pathname)
+    }
+  }
+  if (value.startsWith("vscode-notebook-cell:")) return
+  // #125：git: 从 query JSON.path 取真实路径
+  if (value.startsWith("git:")) {
+    const uri = new URL(value)
+    const search = uri.search ? decodeURIComponent(uri.search.slice(1)) : undefined
+    let parsed: unknown
+    try {
+      parsed = search ? JSON.parse(search) : undefined
+    } catch {
+      parsed = undefined
+    }
+    if (parsed && typeof parsed === "object" && typeof (parsed as any).path === "string") return (parsed as any).path
+    const pathname = decodeURIComponent(uri.pathname)
+    return /^\/[a-z]:/i.test(pathname) ? pathname.slice(1) : pathname
+  }
+  return value
+}
+
 function resolveBridgeRegistryActiveFile(activeDirectory: string): EditorSelection | undefined {
   const dir = path.join(os.homedir(), ".local", "state", "opencode", "ide")
   let entries: string[]
@@ -461,7 +492,8 @@ function resolveBridgeRegistryActiveFile(activeDirectory: string): EditorSelecti
 
   const active = best.entry.active as Record<string, string> | undefined
   // notebook 优先于 textEditor：笔记本会话更依赖路径绑定
-  const filePath = active?.notebook || active?.textEditor
+  // #125：notebook/textEditor 可能是 URI，需解码为路径
+  const filePath = bridgeUriToPath(active?.notebook) ?? bridgeUriToPath(active?.textEditor)
   if (!filePath) return
 
   return { filePath, source: "bridge", ranges: [] as EditorSelection["ranges"] }
@@ -476,7 +508,8 @@ function bridgeRegistryWorkspaceScore(entry: Record<string, unknown>, activeDire
     0,
     ...folders.map((folder) => {
       const fsPath = typeof folder.fsPath === "string" ? folder.fsPath : ""
-      const uri = typeof folder.uri === "string" ? folder.uri : ""
+      // #125：workspace folder.uri 同样可能是 file://
+      const uri = typeof folder.uri === "string" ? bridgeUriToPath(folder.uri) ?? folder.uri : ""
       return Math.max(
         fsPath ? pathContainsLength(fsPath, activeDirectory) : 0,
         uri ? pathContainsLength(uri, activeDirectory) : 0,
diff --git a/packages/tui/src/routes/session/context-usage.tsx b/packages/tui/src/routes/session/context-usage.tsx
index af5963986e..4aa51ab106 100644
--- a/packages/tui/src/routes/session/context-usage.tsx
+++ b/packages/tui/src/routes/session/context-usage.tsx
@@ -1,9 +1,10 @@
-import { createMemo, createResource, For, Show } from "solid-js"
+import { createEffect, createMemo, createResource, For, Show } from "solid-js"
 import { useKeyboard, useRenderer, useTerminalDimensions } from "@opentui/solid"
 import { useDialog } from "../../ui/dialog"
 import { useLocal } from "../../context/local"
 import { useProject } from "../../context/project"
 import { useSync } from "../../context/sync"
+import { createThrottledSignal } from "../../util/signal"
 import { useTheme } from "../../context/theme"
 import { TextAttributes } from "@opentui/core"
 import { Locale } from "../../util/locale"
@@ -257,7 +258,7 @@ export function ContextUsagePanel(props: { sessionID: string; onClose: () => voi
   useRenderer()
 
   // 收集计算输入：消息、部件、提供者、配置、代理、模型、VCS、路径、终端宽度
-  const input = createMemo(() => {
+  const inputRaw = createMemo(() => {
     // 展开拷贝消息数组：避免直接引用 sync 内部状态，防止后续 mutation 影响响应式追踪
     const messages = [...(sync.data.message[props.sessionID] ?? [])]
     // 按 messageID 分组部件列表
@@ -278,7 +279,13 @@ export function ContextUsagePanel(props: { sessionID: string; onClose: () => voi
 
   // 异步计算上下文数据，Solid 自动追踪 input 变化并重新计算
   // createResource 自动追踪 input memo 变化并异步重算，加载中 data() 为 undefined
-  const [data] = createResource(() => computeContextData(input()))
+  // #125：节流 resource 源，避免流式 delta 饿死 computeContextData
+  const [input, setInput] = createThrottledSignal(inputRaw(), 500)
+  createEffect(() => setInput(inputRaw()))
+
+
+  // #125：双参 createResource 让 input 成为响应式 source
+  const [data] = createResource(input, computeContextData)
 
   // 键盘处理：escape 或 Ctrl+C 关闭面板；对话框打开时不拦截按键
   useKeyboard((evt) => {
diff --git a/packages/tui/src/util/context-usage.ts b/packages/tui/src/util/context-usage.ts
index 1464dadbc7..9efe05cdc4 100644
--- a/packages/tui/src/util/context-usage.ts
+++ b/packages/tui/src/util/context-usage.ts
@@ -10,6 +10,7 @@ import matter from "gray-matter"
 import type { Agent, Config, Message, Part, Provider, VcsInfo } from "@opencode-ai/sdk/v2"
 import { Flag } from "@opencode-ai/core/flag/flag"
 import { estimateDataUrlInputTokens, charsPerTokenFromHistory } from "./token-estimate"
+import { tokenAccounting } from "./token-accounting"
 // 嵌入 catalog：TUI 无法 import opencode Schema，生成期抽取 description/parameters
 import EMBEDDED_TOOL_CATALOG from "./tool-definition-catalog.json"
 
@@ -1133,11 +1134,25 @@ export function contextGrid(categories: ContextCategory[], contextLimit: number,
   // 缓冲区优先分配尾部格子：保证压缩缓冲区在网格末尾可视化，与自由空间区分
   const tailCount = tail ? countFor(tail, total).count : 0
   const bodyLimit = total - tailCount
-  for (const category of body) {
-    const { exact, count } = countFor(category, bodyLimit - cells.length)
-    for (let i = 0; i < count; i++) {
+  // #125：先算各 body 分类格子数，余数并入 Free space 原位
+  const counts = body.map((_) => 0)
+  let remaining = bodyLimit
+  for (let ci = 0; ci < body.length; ci++) {
+    const count = countFor(body[ci], remaining).count
+    counts[ci] = count
+    remaining -= count
+  }
+  const freeIdx = body.findIndex((c) => c.name === "Free space")
+  // #125：舍入余数给 Free space，避免尾部追加打乱顺序
+  if (freeIdx >= 0 && remaining > 0) counts[freeIdx] += remaining
+
+  for (let ci = 0; ci < body.length; ci++) {
+    const category = body[ci]
+    const count = counts[ci]
+    const exact = contextLimit > 0 ? (category.tokens / contextLimit) * total : 0
+    for (let k = 0; k < count; k++) {
       // 格子饱满度 = 比例余量，用于选择填充字符
-      const fullness = Math.max(0, Math.min(1, exact - i))
+      const fullness = Math.max(0, Math.min(1, exact - k))
       cells.push({
         symbol: category.name === "Free space" ? "⛶" : category.name === "Model reserve" ? "⛝" : fullness >= 0.7 ? "⛁" : "⛀",
         categoryName: category.name,
@@ -1145,19 +1160,8 @@ export function contextGrid(categories: ContextCategory[], contextLimit: number,
         fullness,
       })
     }
-    if (cells.length >= bodyLimit) break
   }
 
-  // 剩余空间填充自由空间格子
-  const free = categories.find((item) => item.name === "Free space")
-  while (cells.length < bodyLimit) {
-    cells.push({
-      symbol: "⛶",
-      categoryName: "Free space",
-      color: free?.color ?? "textMuted",
-      fullness: 0,
-    })
-  }
   // 尾部填充压缩缓冲区格子
   if (tail) {
     const exact = contextLimit > 0 ? (tail.tokens / contextLimit) * total : 0
@@ -1210,20 +1214,36 @@ export async function computeContextData(input: ComputeContextDataInput): Promis
   const msg = messageTokens(compacted, ratio)
   // #99：usage 用 raw 全量消息 + ratio 估流式增量
   const usage = usageTotals(raw, ratio)
-  const instructions = await gatherInstructionFiles(input)
+  // #125：step 未确认时跳过指令/技能/工具定义 FS 扫描
+  const acc = tokenAccounting(
+    input.messages,
+    (id) => input.parts[id] ?? [],
+    maxTokens || undefined,
+  )
+  const stepConfirmed = acc.step.confirmed
+  const instructions = stepConfirmed ? await gatherInstructionFiles(input) : []
   let instructionDetails = instructions.map((item) => ({ path: item.path, tokens: estimate(item.content, ratio) }))
   const messageText = msg.text.join("\n")
   // 加载的指令路径去重：已在消息文本中出现的跳过
   const loadedInstructions = [] as Array<{ path: string; tokens: number }>
-  for (const filepath of msg.loadedInstructionPaths) {
+  if (stepConfirmed) for (const filepath of msg.loadedInstructionPaths) {
     if (messageText.includes(`Instructions from: ${filepath}`)) continue
     const content = await readText(filepath)
     if (content) loadedInstructions.push({ path: filepath, tokens: estimate(`Instructions from: ${filepath}\n${content}`, ratio) })
   }
 
-  const skills = await gatherSkills(input)
+  // #125：未确认时 skills/toolDefs 为空，分类总量仍可由 bd 路径填充
+  const skills = stepConfirmed ? await gatherSkills(input) : []
   const skillListing = skillsSection(skillInfo(skills))
-  let toolDefs = await toolDefinitionTokens(input, compacted, skills, lastUser, { modelID: modelInfo.modelID, providerID: modelInfo.providerID }, ratio)
+  // #125：磁盘扫描仍需 stepConfirmed；嵌入 catalog 的 toolDefs 始终可组装（避免历史 tool call 塌缩）
+  let toolDefs = await toolDefinitionTokens(
+    input,
+    compacted,
+    skills,
+    lastUser,
+    { modelID: modelInfo.modelID, providerID: modelInfo.providerID },
+    ratio,
+  )
   // 系统提示词 = 基础提示 + 环境信息 + 用户自定义 system 覆盖
   const systemText = [
     baseSystemPrompt(input, lastUser, modelInfo.model),
diff --git a/packages/tui/src/component/prompt/index.tsx b/packages/tui/src/component/prompt/index.tsx
index d80b61a63b..9f0362dc88 100644
--- a/packages/tui/src/component/prompt/index.tsx
+++ b/packages/tui/src/component/prompt/index.tsx
@@ -1752,13 +1752,25 @@ export function Prompt(props: PromptProps) {
                   <Switch>
                     <Match when={usage()}>
                       {(item) => (
-                        <text fg={theme.textMuted} wrapMode="none">
-                          {/* 使用脉冲信号控制箭头颜色，token 增加时短暂高亮 */}
-                          <span style={{ fg: usageFlow().input ? theme.text : theme.textMuted }}>↑</span> {Locale.number(item().input)}{showCumulative() ? `(${Locale.number(item().totalInput)})` : ""} ·{" "}
-                          <span style={{ fg: usageFlow().output ? theme.text : theme.textMuted }}>↓</span> {Locale.number(item().output)}{showCumulative() ? `(${Locale.number(item().totalOutput)})` : ""}
-                          {item().context ? ` · ${item().context}` : ""}
-                          {item().cost ? ` · ${item().cost}` : ""}
-                        </text>
+                        <Show
+                          when={showCumulative()}
+                          fallback={
+                            // #125：窄屏合并 ↑↓，只保留 context/cost
+                            <text fg={theme.textMuted} wrapMode="none">
+                              <span style={{ fg: usageFlow().input || usageFlow().output ? theme.text : theme.textMuted }}>↑↓</span>
+                              {item().context ? ` ${item().context}` : ""}
+                              {item().cost ? ` · ${item().cost}` : ""}
+                            </text>
+                          }
+                        >
+                          <text fg={theme.textMuted} wrapMode="none">
+                            {/* 宽屏：分向箭头 + 可选累计括号 */}
+                            <span style={{ fg: usageFlow().input ? theme.text : theme.textMuted }}>↑</span> {Locale.number(item().input)}({Locale.number(item().totalInput)}) ·{" "}
+                            <span style={{ fg: usageFlow().output ? theme.text : theme.textMuted }}>↓</span> {Locale.number(item().output)}({Locale.number(item().totalOutput)})
+                            {item().context ? ` · ${item().context}` : ""}
+                            {item().cost ? ` · ${item().cost}` : ""}
+                          </text>
+                        </Show>
                       )}
                     </Match>
                     <Match when={true}>
