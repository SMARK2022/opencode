diff --git a/packages/tui/src/component/dialog-session-list.tsx b/packages/tui/src/component/dialog-session-list.tsx
--- a/packages/tui/src/component/dialog-session-list.tsx
+++ b/packages/tui/src/component/dialog-session-list.tsx
@@ -26,7 +26,9 @@ export function createDialogSessionListQuery(input: { search?: string; filter: S
   const search = input.search?.trim()
   return {
     roots: true,
-    limit: search ? 30 : 100,
+    // #306：与 sync 侧一致使用 90 天窗口，避免全历史检索拖慢预览；搜索态保持窄结果集。
+        start: Date.now() - 90 * 24 * 60 * 60 * 1000,
+        limit: 100,
     ...(search ? { search } : {}),
     ...input.filter,
   }
@@ -50,7 +52,8 @@ const SESSION_LIST_PREVIEW_PAGE_SIZE = 16
 // #120：最多扫描消息条数，避免全历史
 const SESSION_LIST_PREVIEW_MESSAGE_SCAN_LIMIT = 200
 // #120：同时预加载预览的会话数上限
-const SESSION_LIST_PREVIEW_SESSION_LIMIT = 50
+// #306：预览上限从 50 提到 400，预览覆盖更完整，避免滚动时再触发补拉。
+const SESSION_LIST_PREVIEW_SESSION_LIMIT = 400
 // #120：会话预览并发度
 const SESSION_LIST_PREVIEW_CONCURRENCY = 6
 


diff --git a/packages/tui/src/context/sync.tsx b/packages/tui/src/context/sync.tsx
--- a/packages/tui/src/context/sync.tsx
+++ b/packages/tui/src/context/sync.tsx
@@ -296,7 +296,7 @@ export const {
 
     function listSessions() {
       return sdk.client.session
-        .list({ start: Date.now() - 30 * 24 * 60 * 60 * 1000, ...sessionListQuery() })
+        .list({ start: Date.now() - 90 * 24 * 60 * 60 * 1000, limit: 1200, ...sessionListQuery() })
         .then((x) => (x.data ?? []).toSorted((a, b) => a.id.localeCompare(b.id)))
     }
 

diff --git a/packages/tui/test/component/dialog-session-list.test.ts b/packages/tui/test/component/dialog-session-list.test.ts
--- a/packages/tui/test/component/dialog-session-list.test.ts
+++ b/packages/tui/test/component/dialog-session-list.test.ts
@@ -3,8 +3,10 @@ import { createDialogSessionListQuery, loadDialogSessionList } from "../../src/c
 
 describe("dialog session list", () => {
   test("requests root sessions for the default browse list", () => {
+    // #306：query 新增 start 时间窗口，断言用 expect.any(Number) 保持时间无关。
     expect(createDialogSessionListQuery({ filter: { path: "packages/tui" } })).toEqual({
       roots: true,
+      start: expect.any(Number),
       limit: 100,
       path: "packages/tui",
     })
@@ -13,6 +15,7 @@ describe("dialog session list", () => {
   test("requests root sessions for search results", () => {
     expect(createDialogSessionListQuery({ search: " deploy ", filter: { scope: "project" } })).toEqual({
       roots: true,
+      start: expect.any(Number),
       limit: 30,
       search: "deploy",
       scope: "project",

