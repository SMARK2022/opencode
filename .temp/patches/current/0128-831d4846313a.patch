From 831d4846313a13d78c1e1ca913bf9c66c7437586 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Tue, 12 May 2026 05:39:27 +0800
Subject: [PATCH] =?UTF-8?q?feat(text-decoding):=20=E6=B7=BB=E5=8A=A0?=
 =?UTF-8?q?=E5=AF=B9=E5=A4=9A=E7=A7=8D=E6=96=87=E6=9C=AC=E7=BC=96=E7=A0=81?=
 =?UTF-8?q?=E7=9A=84=E6=94=AF=E6=8C=81=EF=BC=8C=E5=8C=85=E6=8B=AC=E8=87=AA?=

diff --git a/packages/opencode/src/util/text-decoding.ts b/packages/opencode/src/util/text-decoding.ts
index b1e14aba7f..0461a02427 100644
--- a/packages/opencode/src/util/text-decoding.ts
+++ b/packages/opencode/src/util/text-decoding.ts
@@ -1,21 +1,23 @@
-// #126：自动文本解码支持的编码集合
 export type TextEncoding = "utf-8" | "utf-16le" | "utf-16be" | "gb18030"
+// #128：auto 或显式锁定编码
+export type TextEncodingMode = "auto" | TextEncoding
 
 type Options = {
-  fallback?: TextEncoding
+  encoding?: TextEncodingMode
 }
 
-const SAMPLE_BYTES = 64
+const MAX_SEGMENT_BYTES = 8192
 const UTF16_NULL_RATIO = 0.2
+const EMPTY: Uint8Array<ArrayBuffer> = new Uint8Array(0)
 
-// Windows legacy console output is usually GBK-compatible; gb18030 is the widest native decoder label.
-// #126：无 BOM/高位特征时回落编码（Windows 默认 gb18030）
-function fallback(options?: Options): TextEncoding {
-  return options?.fallback ?? (process.platform === "win32" ? "gb18030" : "utf-8")
+// Return explicit encoding if set, undefined for auto mode.
+// #128：显式 encoding 非 auto 时锁定整段流
+function fixed(options?: Options): TextEncoding | undefined {
+  const encoding = options?.encoding
+  return encoding && encoding !== "auto" ? encoding : undefined
 }
 
 // BOMs are authoritative and avoid all heuristic guessing.
-// #126：优先读 BOM 判定编码
 function bom(bytes: Uint8Array): TextEncoding | undefined {
   if (bytes[0] === 0xef && bytes[1] === 0xbb && bytes[2] === 0xbf) return "utf-8"
   if (bytes[0] === 0xff && bytes[1] === 0xfe) return "utf-16le"
@@ -23,8 +25,7 @@ function bom(bytes: Uint8Array): TextEncoding | undefined {
 }
 
 // UTF-16 text without a BOM often exposes NUL bytes on every ASCII code unit.
-// #126：无 BOM 的 UTF-16 常有周期性 NUL 字节
-function utf16ByNulls(bytes: Uint8Array): TextEncoding | undefined {
+function utf16ByNulls(bytes: Uint8Array): "utf-16le" | "utf-16be" | undefined {
   const sample = bytes.subarray(0, Math.min(bytes.length, 4096))
   let even = 0
   let odd = 0
@@ -33,107 +34,224 @@ function utf16ByNulls(bytes: Uint8Array): TextEncoding | undefined {
     if (i % 2 === 0) even++
     else odd++
   }
-  // #126：偶数位 NUL 比例偏高 → UTF-16BE 倾向
   const evenSlots = Math.ceil(sample.length / 2)
-  // #126：奇数位 NUL 比例偏高 → UTF-16LE 倾向
   const oddSlots = Math.floor(sample.length / 2)
-  // #126：奇数位主导且远强于偶数位
   if (odd >= 2 && odd / oddSlots > UTF16_NULL_RATIO && odd > even * 3) return "utf-16le"
-  // #126：偶数位主导且远强于奇数位
   if (even >= 2 && even / evenSlots > UTF16_NULL_RATIO && even > odd * 3) return "utf-16be"
 }
 
-// Strict UTF-8 differentiates real UTF-8 from Windows codepage bytes without replacement characters.
-// #126：fatal UTF-8 解码区分真 UTF-8 与 codepage 字节
-function isUtf8(bytes: Uint8Array, stream = false) {
+// #128：BOM 与 NUL 模式识别 Unicode
+function unicode(bytes: Uint8Array): TextEncoding | undefined {
+  return bom(bytes) ?? utf16ByNulls(bytes)
+}
+
+// Strict UTF-8 check: valid bytes pass, anything invalid fails without replacement.
+// #128：fatal UTF-8 校验，失败则不认 UTF-8
+function isUtf8(bytes: Uint8Array) {
   try {
-    new TextDecoder("utf-8", { fatal: true }).decode(bytes, { stream })
+    new TextDecoder("utf-8", { fatal: true }).decode(bytes)
     return true
   } catch {
     return false
   }
 }
 
-// #126：是否含非 ASCII 高位字节
-function hasHighBit(bytes: Uint8Array) {
-  return bytes.some((byte) => byte >= 0x80)
+// Returns the number of leading ASCII bytes (0x01–0x7f) safe to emit without committing an encoding.
+// #128：可安全透传的前缀 ASCII 长度
+function asciiPrefixLength(bytes: Uint8Array) {
+  let i = 0
+  while (i < bytes.length && bytes[i] > 0 && bytes[i] < 0x80) i++
+  return i
 }
 
-// #126：纯 ASCII 可安全按 UTF-8 透传而不提交编码
-function isAsciiText(bytes: Uint8Array) {
-  return bytes.every((byte) => byte > 0 && byte < 0x80)
+// #128：按换行切段，避免一段编码污染下一段
+function findLineEnd(bytes: Uint8Array) {
+  const index = bytes.indexOf(0x0a)
+  return index === -1 ? undefined : index + 1
 }
 
-// #126：合并缓冲块供探测
-function concat(chunks: Uint8Array[], size: number) {
-  const out = new Uint8Array(size)
-  let offset = 0
-  for (const chunk of chunks) {
-    out.set(chunk, offset)
-    offset += chunk.length
+// #128：UTF-16 换行边界（含 0x0a 00 / 00 0x0a）
+function findUtf16LineEnd(bytes: Uint8Array, encoding: "utf-16le" | "utf-16be") {
+  for (let i = 0; i + 1 < bytes.length; i += 2) {
+    if (encoding === "utf-16le" && bytes[i] === 0x0a && bytes[i + 1] === 0x00) return i + 2
+    if (encoding === "utf-16be" && bytes[i] === 0x00 && bytes[i + 1] === 0x0a) return i + 2
   }
-  return out
 }
 
-// #126：综合 BOM/NUL/UTF-8/高位字节选择编码
-function choose(bytes: Uint8Array, options: Options | undefined, final: boolean): TextEncoding | undefined {
-  if (bytes.length === 0) return "utf-8"
-  const certain = bom(bytes) ?? utf16ByNulls(bytes)
-  if (certain) return certain
-  if (isUtf8(bytes, !final)) return !hasHighBit(bytes) || final || bytes.length >= SAMPLE_BYTES ? "utf-8" : undefined
-  return final || bytes.length >= SAMPLE_BYTES ? fallback(options) : undefined
+// #128：UTF-16 按 2 字节对齐截断
+function evenLength(length: number) {
+  return length - (length % 2)
 }
 
-// #126：一次性探测最终编码
-// #126：探测失败时回落 fallback（Windows 常用 gb18030）
-export function detectTextEncoding(bytes: Uint8Array, options?: Options): TextEncoding {
-  return choose(bytes, options, true) ?? "utf-8"
+// Known binary magic bytes that should never be decoded as text.
+// #128：已知二进制魔数，禁止当文本解码
+// #128：二进制魔数命中时勿当文本
+function hasMagic(bytes: Uint8Array) {
+  if (bytes[0] === 0x1f && bytes[1] === 0x8b) return true
+  if (bytes[0] === 0xac && bytes[1] === 0xed && bytes[2] === 0x00 && bytes[3] === 0x05) return true
+  if (bytes[0] === 0x50 && bytes[1] === 0x4b && bytes[2] === 0x03 && bytes[3] === 0x04) return true
+  if (bytes[0] === 0x89 && bytes[1] === 0x50 && bytes[2] === 0x4e && bytes[3] === 0x47) return true
+  if (bytes[0] === 0xca && bytes[1] === 0xfe && bytes[2] === 0xba && bytes[3] === 0xbe) return true
+  if (bytes[0] === 0x25 && bytes[1] === 0x50 && bytes[2] === 0x44 && bytes[3] === 0x46) return true
+  return false
 }
 
-// #126：整段解码并返回文本与选用编码
-export function decodeText(bytes: Uint8Array, options?: Options) {
-  const encoding = detectTextEncoding(bytes, options)
+// #128：允许的控制字节（TAB/LF/CR）
+function isTextControl(byte: number) {
+  return byte === 0x08 || byte === 0x09 || byte === 0x0a || byte === 0x0c || byte === 0x0d || byte === 0x1b
+}
+
+// Binary-looking data must not trigger GB18030 fallback because that pollutes later UTF-8.
+// #128：控制字节密度启发式判定二进制
+// #128：控制字节过多时按二进制处理
+function looksBinary(bytes: Uint8Array) {
+  if (bytes.length === 0) return false
+  if (hasMagic(bytes)) return true
+  if (bytes.some((byte) => byte === 0)) return true
+  let controls = 0
+  for (const byte of bytes) {
+    if (byte < 0x20 && !isTextControl(byte)) controls++
+  }
+  return controls >= 4 || controls / bytes.length > 0.05
+}
+
+// #128：固定编码解码并返回 {text,encoding}
+function decodeWith(bytes: Uint8Array, encoding: TextEncoding) {
   return {
     encoding,
     text: new TextDecoder(encoding).decode(bytes),
   }
 }
 
-// #126：流式解码器：先缓冲再提交编码，避免把 ASCII 前缀锁死
+// Decode a single segment: UTF-16 preferred, then UTF-8, then GB18030 legacy fallback, then UTF-8 replacement.
+// #128：单段自动探测：unicode → utf-8 → gb18030 → utf-8
+function decodeAutoSegment(bytes: Uint8Array) {
+  const detected = unicode(bytes)
+  if (detected) return decodeWith(bytes, detected)
+  if (isUtf8(bytes)) return decodeWith(bytes, "utf-8")
+  if (!looksBinary(bytes)) return decodeWith(bytes, "gb18030")
+  return decodeWith(bytes, "utf-8")
+}
+
+// #128：对外导出：显式模式或 auto 探测结果
+export function detectTextEncoding(bytes: Uint8Array, options?: Options): TextEncoding {
+  const forced = fixed(options)
+  if (forced) return forced
+  return decodeAutoSegment(bytes).encoding
+}
+
+// #128：一次性解码整段字节
+export function decodeText(bytes: Uint8Array, options?: Options) {
+  const forced = fixed(options)
+  if (forced) return decodeWith(bytes, forced)
+  return decodeAutoSegment(bytes)
+}
+
+// Streaming decoder that segments output so one encoding segment does not pollute later ones.
+// #128：分段流式解码，一段一种编码，避免污染后续输出
 export function createAutoTextDecoder(options?: Options) {
-  let encoding: TextEncoding | undefined
-  // #126：编码确定前 decoder 为空
-  let decoder: TextDecoder | undefined
-  let size = 0
-  const pending: Uint8Array[] = []
-
-  const flush = (final: boolean) => {
-    if (decoder) return decoder.decode(undefined, { stream: !final })
-    const bytes = concat(pending, size)
-    encoding = choose(bytes, options, final)
-    if (!encoding) return ""
-    decoder = new TextDecoder(encoding)
-    pending.length = 0
-    size = 0
-    return decoder.decode(bytes, { stream: !final })
+  const forced = fixed(options)
+  if (forced) {
+    const decoder = new TextDecoder(forced)
+    return {
+      write(chunk: Uint8Array) {
+        return decoder.decode(chunk, { stream: true })
+      },
+      end() {
+        return decoder.decode()
+      },
+      encoding() {
+        return forced
+      },
+    }
+  }
+
+  let pending: Uint8Array<ArrayBuffer> = EMPTY
+  let lastEncoding: TextEncoding | undefined
+
+  // #128：切出 pending 前缀并解码，记录 lastEncoding
+  const emit = (length: number, encoding?: TextEncoding) => {
+    const segment = pending.subarray(0, length) as Uint8Array<ArrayBuffer>
+    pending = pending.subarray(length) as Uint8Array<ArrayBuffer>
+    const decoded = encoding ? decodeWith(segment, encoding) : decodeAutoSegment(segment)
+    lastEncoding = decoded.encoding
+    return decoded.text
+  }
+
+  // Drain one UTF-16 segment (line, max-size, or final) using a locked encoding for this emission only.
+  // #128：UTF-16 流按行/上限/final 分段 drain
+  const drainUtf16 = (encoding: "utf-16le" | "utf-16be", final: boolean) => {
+    const lineEnd = findUtf16LineEnd(pending, encoding)
+    if (lineEnd) return emit(lineEnd, encoding)
+    if (pending.length >= MAX_SEGMENT_BYTES) {
+      const length = evenLength(Math.min(pending.length, MAX_SEGMENT_BYTES))
+      return length > 0 ? emit(length, encoding) : ""
+    }
+    if (final) {
+      const length = evenLength(pending.length)
+      return length > 0 ? emit(length, encoding) : ""
+    }
+    return ""
+  }
+
+  const drain = (final: boolean) => {
+    let output = ""
+    while (pending.length > 0) {
+      // UTF-16 detection: emit as much as possible at UTF-16 line boundaries.
+      const uni = unicode(pending)
+      if (uni === "utf-16le" || uni === "utf-16be") {
+        const next = drainUtf16(uni, final)
+        if (!next) break
+        output += next
+        continue
+      }
+      // ASCII prefix is safe in both UTF-8 and GB18030 — emit immediately.
+      const ascii = asciiPrefixLength(pending)
+      if (ascii > 0) {
+        output += emit(ascii, "utf-8")
+        continue
+      }
+      // Emit at newline boundaries — most CLI output is line-oriented.
+      const lineEnd = findLineEnd(pending)
+      if (lineEnd) {
+        output += emit(lineEnd)
+        continue
+      }
+      // Avoid buffering indefinitely. Emit a segment when it grows too large.
+      if (pending.length >= MAX_SEGMENT_BYTES) {
+        output += emit(MAX_SEGMENT_BYTES)
+        continue
+      }
+      // Flush everything on end().
+      if (final) {
+        output += emit(pending.length)
+        continue
+      }
+      break
+    }
+    return output
   }
 
   return {
     write(chunk: Uint8Array) {
-      if (decoder) return decoder.decode(chunk, { stream: true })
-      // #126：ASCII 在 UTF-8 与 codepage 中相同，可暂不提交 decoder
-      // ASCII bytes are identical in UTF-8 and Windows codepages, so emit them without committing the decoder.
-      // #126：纯 ASCII 先 UTF-8 透传，不锁死最终编码
-    if (pending.length === 0 && isAsciiText(chunk)) return new TextDecoder("utf-8").decode(chunk)
-      pending.push(chunk)
-      size += chunk.length
-      return flush(false)
+      pending = appendBuffer(pending, chunk)
+      return drain(false)
     },
     end() {
-      return flush(true)
+      return drain(true)
     },
     encoding() {
-      return encoding
+      return lastEncoding
     },
   }
 }
+
+// #128：合并 pending 与新 chunk
+function appendBuffer(left: Uint8Array, right: Uint8Array): Uint8Array<ArrayBuffer> {
+  if (left.length === 0) return right as Uint8Array<ArrayBuffer>
+  if (right.length === 0) return left as Uint8Array<ArrayBuffer>
+  const out = new Uint8Array(left.length + right.length) as Uint8Array<ArrayBuffer>
+  out.set(left, 0)
+  out.set(right, left.length)
+  return out
+}
diff --git a/packages/core/src/config/tool-output.ts b/packages/core/src/config/tool-output.ts
index f722fa301f..5058c11b2e 100644
--- a/packages/core/src/config/tool-output.ts
+++ b/packages/core/src/config/tool-output.ts
@@ -7,4 +7,8 @@ export class Info extends Schema.Class<Info>("ConfigV2.ToolOutput")({
   max_lines: PositiveInt.pipe(Schema.optional),
   max_bytes: PositiveInt.pipe(Schema.optional),
   bash_compression: Schema.Boolean.pipe(Schema.optional),
+  // #128：shell 输出文本编码（auto 探测或显式锁定）
+  shell_encoding: Schema.Literals(["auto", "utf-8", "gb18030", "utf-16le", "utf-16be"]).pipe(Schema.optional).annotate({
+    description: "Shell stdout/stderr text encoding. auto detects; set explicitly to lock the decoder for the whole stream.",
+  }),
 }) {}
diff --git a/packages/core/src/v1/config/config.ts b/packages/core/src/v1/config/config.ts
index 5089d42cf2..974c6d4715 100644
--- a/packages/core/src/v1/config/config.ts
+++ b/packages/core/src/v1/config/config.ts
@@ -142,6 +142,12 @@ export const Info = Schema.Struct({
       bash_compression: Schema.optional(Schema.Boolean).annotate({
         description: "Enable Bash output compression to reduce token usage",
       }),
+      // #128：shell 输出文本编码（auto 探测或显式锁定）
+      shell_encoding: Schema.optional(
+        Schema.Literals(["auto", "utf-8", "gb18030", "utf-16le", "utf-16be"]),
+      ).annotate({
+        description: "Shell stdout/stderr text encoding. auto detects; set explicitly to lock the decoder for the whole stream.",
+      }),
     }),
   ).annotate({
     description:
diff --git a/packages/opencode/src/tool/shell.ts b/packages/opencode/src/tool/shell.ts
index c7d1c15e4f..96775a61af 100644
--- a/packages/opencode/src/tool/shell.ts
+++ b/packages/opencode/src/tool/shell.ts
@@ -22,7 +22,7 @@ import { ChildProcess } from "effect/unstable/process"
 import { ChildProcessSpawner } from "effect/unstable/process/ChildProcessSpawner"
 import { ShellPrompt, type Parameters } from "./shell/prompt"
 import { BashArity } from "@/permission/arity"
-import { createAutoTextDecoder } from "@/util/text-decoding"
+import { createAutoTextDecoder, type TextEncodingMode } from "@/util/text-decoding"
 // #126：自动检测 UTF-8/UTF-16/GB18030 的流式解码
 
 // Git 写操作的子命令集合：这些命令会修改仓库状态，需要用户明确授权
@@ -359,6 +359,18 @@ function psUtf8(command: string) {
 
 // 将命令编码为 PowerShell -EncodedCommand 格式（UTF-16LE base64）
 // PowerShell 的 -Command 参数会丢失非 ASCII 字符，-EncodedCommand 解决此问题
+// #128：config.tool_output.shell_encoding，默认 auto
+function shellOutputEncoding(config?: {
+  tool_output?: {
+    shell_encoding?: TextEncodingMode
+    bash_compression?: boolean
+    max_lines?: number
+    max_bytes?: number
+  }
+}): TextEncodingMode {
+  return config?.tool_output?.shell_encoding ?? "auto"
+}
+
 function psEncoded(command: string) {
   return Buffer.from(psUtf8(command), "utf16le").toString("base64")
 }
@@ -612,6 +624,8 @@ export const ShellTool = Tool.define(
         env: NodeJS.ProcessEnv
         timeout: number
         compressOutput: boolean
+      // #128：输出文本编码策略
+      encoding: TextEncodingMode
       },
       ctx: Tool.Context,
     ) {
@@ -669,7 +683,8 @@ export const ShellTool = Tool.define(
 
           // #126：按字节喂给自动解码器，避免 UTF-16 被 decodeText 当 UTF-8 破坏
           // #126：按字节喂解码器，解决 PowerShell/重定向 UTF-16 输出
-          const decoder = createAutoTextDecoder()
+          // #128：按 input.encoding 锁定或自动探测
+          const decoder = createAutoTextDecoder({ encoding: input.encoding })
           // #126：字符串 chunk 写入环形缓冲与截断/落盘逻辑
           const onChunk = (chunk: string) => {
             if (!chunk) return Effect.void
@@ -854,6 +869,8 @@ export const ShellTool = Tool.define(
                 }),
               )
 
+              // #128：读取 shell_encoding 配置
+              const encoding = shellOutputEncoding(cfg)
               return yield* run(
                 {
                   shell,
@@ -862,6 +879,7 @@ export const ShellTool = Tool.define(
                   env: yield* shellEnv(ctx, cwd),
                   timeout,
                   compressOutput,
+                  encoding,
                 },
                 ctx,
               )
diff --git a/packages/opencode/test/util/text-decoding.test.ts b/packages/opencode/test/util/text-decoding.test.ts
index 0f66869a0f..de3bc73e94 100644
--- a/packages/opencode/test/util/text-decoding.test.ts
+++ b/packages/opencode/test/util/text-decoding.test.ts
@@ -1,20 +1,21 @@
 import { describe, expect, test } from "bun:test"
 import { createAutoTextDecoder, decodeText, detectTextEncoding } from "@/util/text-decoding"
-// #126：编码检测与流式解码单测
+// #126/#128：编码检测、分段流式解码与显式锁定
 
 const bytes = (items: number[]) => new Uint8Array(items)
+const utf8 = (text: string) => new TextEncoder().encode(text)
 
 describe("text-decoding", () => {
   // #126：中文 UTF-8 往返
   test("detects UTF-8", () => {
-    const input = new TextEncoder().encode("默认分发")
+    const input = utf8("默认分发")
     expect(detectTextEncoding(input)).toBe("utf-8")
     expect(decodeText(input).text).toBe("默认分发")
   })
 
   // #126：BOM 优先于内容探测
   test("detects UTF-8 BOM", () => {
-    const input = bytes([0xef, 0xbb, 0xbf, ...new TextEncoder().encode("hello")])
+    const input = bytes([0xef, 0xbb, 0xbf, ...utf8("hello")])
     expect(detectTextEncoding(input)).toBe("utf-8")
     expect(decodeText(input).text).toBe("hello")
   })
@@ -33,14 +34,27 @@ describe("text-decoding", () => {
     expect(decodeText(input).text).toContain("默认分发")
   })
 
-  test("falls back to gb18030 for Windows codepage bytes", () => {
+  // #128：legacy Windows 字节在 auto 下走 gb18030
+  test("falls back to gb18030 for legacy Windows codepage bytes", () => {
     const input = bytes([0xd6, 0xd0, 0xce, 0xc4])
-    expect(decodeText(input, { fallback: "gb18030" }).text).toBe("中文")
+    expect(decodeText(input).text).toBe("中文")
+  })
+
+  // #128：显式 utf-8 锁定，不回落 legacy
+  test("honors explicit utf-8 without legacy fallback", () => {
+    const input = bytes([0xd6, 0xd0, 0xce, 0xc4])
+    expect(decodeText(input, { encoding: "utf-8" }).text).toContain("\uFFFD")
+  })
+
+  // #128：显式 gb18030
+  test("honors explicit gb18030", () => {
+    const input = bytes([0xd6, 0xd0, 0xce, 0xc4])
+    expect(decodeText(input, { encoding: "gb18030" }).text).toBe("中文")
   })
 
   // #126：多字节 UTF-8 跨 chunk 不能截断
   test("streams UTF-8 across split multibyte chunks", () => {
-    const input = new TextEncoder().encode("默认分发")
+    const input = utf8("默认分发")
     const decoder = createAutoTextDecoder()
     const output = decoder.write(input.subarray(0, 2)) + decoder.write(input.subarray(2)) + decoder.end()
     expect(output).toBe("默认分发")
@@ -56,10 +70,64 @@ describe("text-decoding", () => {
     expect(decoder.encoding()).toBe("utf-16le")
   })
 
+  // #128：ASCII 前缀不锁定编码，后续可切到 gb18030
   test("does not lock ASCII prefixes before Windows codepage bytes", () => {
-    const decoder = createAutoTextDecoder({ fallback: "gb18030" })
-    const output = decoder.write(new TextEncoder().encode("prefix ")) + decoder.write(bytes([0xd6, 0xd0, 0xce, 0xc4])) + decoder.end()
-    expect(output).toBe("prefix 中文")
+    const decoder = createAutoTextDecoder()
+    const output =
+      decoder.write(utf8("prefix ")) +
+      decoder.write(bytes([0xd6, 0xd0, 0xce, 0xc4, 0x0a])) +
+      decoder.end()
+    expect(output).toContain("prefix ")
+    expect(output).toContain("中文")
+  })
+
+  // #128：GB18030 行后接 UTF-16LE 行，分段探测不互相污染
+  test("segments mixed encodings across lines", () => {
+    const decoder = createAutoTextDecoder()
+    const gb = bytes([0xd6, 0xd0, 0xce, 0xc4, 0x0a])
+    const u16 = Buffer.from("默认分发: Ubuntu\r\n", "utf16le")
+    const output = decoder.write(gb) + decoder.write(new Uint8Array(u16)) + decoder.end()
+    expect(output).toContain("中文")
+    expect(output).toContain("Ubuntu")
+  })
+
+  // #128：显式 gb18030 锁定整段流
+  test("explicit gb18030 decoder locks entire stream", () => {
+    const decoder = createAutoTextDecoder({ encoding: "gb18030" })
+    const output =
+      decoder.write(bytes([0xd6, 0xd0, 0xce, 0xc4])) +
+      decoder.write(utf8(" — text")) +
+      decoder.end()
+    expect(output).toContain("中文")
     expect(decoder.encoding()).toBe("gb18030")
   })
+
+  // #128：显式 utf-8 流不回落
+  test("explicit utf-8 decoder never falls back to legacy", () => {
+    const decoder = createAutoTextDecoder({ encoding: "utf-8" })
+    const output = decoder.write(bytes([0xd6, 0xd0, 0xce, 0xc4])) + decoder.end()
+    expect(decoder.encoding()).toBe("utf-8")
+    expect(output).toContain("\uFFFD")
+  })
+  // #128：UTF-8 盒线/标点不得被当成 legacy 破坏
+  // #128：盒线字符必须走 UTF-8
+  test("keeps UTF-8 box drawing and punctuation", () => {
+    const input = new TextEncoder().encode("┌─ TARGET OUTPUT ──\n— delivered\n")
+    expect(decodeText(input).text).toContain("TARGET OUTPUT")
+    expect(decodeText(input).text).toContain("— delivered")
+    expect(detectTextEncoding(input)).toBe("utf-8")
+  })
+
+  // #128：二进制前缀不得把后续 UTF-8 锁进 gb18030
+  // #128：gzip 魔数后的 UTF-8 不得锁进 gb18030
+  test("does not let binary prefixes lock later UTF-8 into GB18030", () => {
+    const decoder = createAutoTextDecoder()
+    const output =
+      decoder.write(new Uint8Array([0x1f, 0x8b, 0x08, 0x00, 0xff, 0xfe, 0xfd])) +
+      decoder.write(new TextEncoder().encode("\n┌─ TARGET OUTPUT ──\n")) +
+      decoder.end()
+    expect(output).toContain("TARGET OUTPUT")
+    expect(decoder.encoding()).not.toBe("gb18030")
+  })
+
 })
diff --git a/packages/opencode/test/tool/shell.test.ts b/packages/opencode/test/tool/shell.test.ts
index 53ea900e40..4a60311f27 100644
--- a/packages/opencode/test/tool/shell.test.ts
+++ b/packages/opencode/test/tool/shell.test.ts
@@ -1270,3 +1270,62 @@ describe("tool.shell text decoding", () => {
   )
 })
 
+// #128：Windows PowerShell 多编码 shell 输出（与 tip it.live 风格对齐）
+describe("tool.shell windows multi-encoding", () => {
+  if (process.platform !== "win32") return
+
+  for (const item of ps) {
+    it.live(
+      `decodes UTF-8 box drawing output from Windows commands [${item.label}]`,
+      () =>
+        withShell(
+          item,
+          Effect.gen(function* () {
+            const tmp = yield* tmpdirScoped()
+            yield* runIn(
+              tmp,
+              Effect.gen(function* () {
+                const bash = yield* initBash()
+                // #128：PS 写 UTF-8 盒线字符，验证 shell_encoding auto
+                const result = yield* bash.execute(
+                  {
+                    command: `Write-Output "┌─ TARGET OUTPUT ──"; Write-Output "— delivered"`,
+                  },
+                  ctx,
+                )
+                expect(result.output).toContain("TARGET OUTPUT")
+                expect(result.output).toContain("delivered")
+                expect(result.output).not.toContain("\uFFFD")
+              }),
+            )
+          }),
+        ),
+    )
+
+    it.live(
+      `decodes GB18030 legacy shell output from Windows commands [${item.label}]`,
+      () =>
+        withShell(
+          item,
+          Effect.gen(function* () {
+            const tmp = yield* tmpdirScoped()
+            yield* runIn(
+              tmp,
+              Effect.gen(function* () {
+                const bash = yield* initBash()
+                // #128：模拟 OpenStandardOutput 写入 GBK 字节
+                const result = yield* bash.execute(
+                  {
+                    command: `$bytes = [byte[]](0xD6,0xD0,0xCE,0xC4); [Console]::OpenStandardOutput().Write($bytes,0,$bytes.Length)`,
+                  },
+                  ctx,
+                )
+                expect(result.output).toContain("中文")
+              }),
+            )
+          }),
+        ),
+    )
+  }
+})
+
