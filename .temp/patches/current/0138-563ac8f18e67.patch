From 563ac8f18e6779d0f5850d6a79ffa815ad552127 Mon Sep 17 00:00:00 2001
From: SMARK <3222301373@qq.com>
Date: Wed, 13 May 2026 21:03:18 +0800
Subject: [PATCH] =?UTF-8?q?feat(ripgrep):=20=E6=B7=BB=E5=8A=A0=E6=9C=80?=
 =?UTF-8?q?=E5=A4=A7=E6=96=87=E4=BB=B6=E5=92=8C=E7=BB=93=E6=9E=9C=E9=99=90?=
 =?UTF-8?q?=E5=88=B6=EF=BC=8C=E4=BC=98=E5=8C=96=E6=90=9C=E7=B4=A2=E5=8A=9F?=

diff --git a/packages/core/src/ripgrep.ts b/packages/core/src/ripgrep.ts
index ac8ea52d93..483dbaa7e8 100644
--- a/packages/core/src/ripgrep.ts
+++ b/packages/core/src/ripgrep.ts
@@ -18,6 +18,11 @@ import { RipgrepBinary } from "./ripgrep/binary"
 const ERROR_BYTES = 8 * 1024
 const MAX_RECORD_BYTES = 64 * 1024
 const MAX_SUBMATCHES = 100
+// #138：候选文件上限
+export const MAX_SEARCH_FILES = 5_000
+// #138：默认匹配结果上限
+export const MAX_SEARCH_RESULTS = 1_000
+
 
 const RawMatch = Schema.Struct({
   type: Schema.Literal("match"),
@@ -48,6 +53,12 @@ export class InvalidPatternError extends Schema.TaggedErrorClass<InvalidPatternE
   message: Schema.String,
 }) {}
 
+// #138：候选文件过多
+export class SearchTooBroadError extends Schema.TaggedErrorClass<SearchTooBroadError>()("Ripgrep.SearchTooBroadError", {
+  message: Schema.String,
+  maxFiles: PositiveInt,
+}) {}
+
 export interface FindInput {
   readonly cwd: string
   readonly pattern: string
@@ -73,13 +84,15 @@ export interface GrepInput {
   readonly file?: string
   readonly include?: string
   readonly limit: number
+  // #138：false 关闭候选上限
+  readonly maxFiles?: number | false
   readonly signal?: AbortSignal
 }
 
 export interface Interface {
   readonly find: (input: FindInput) => Effect.Effect<readonly Entry[], Error>
   readonly glob: (input: GlobInput) => Effect.Effect<readonly Entry[], Error>
-  readonly grep: (input: GrepInput) => Effect.Effect<readonly Match[], Error | InvalidPatternError>
+  readonly grep: (input: GrepInput) => Effect.Effect<readonly Match[], Error | InvalidPatternError | SearchTooBroadError>
 }
 
 export class Service extends Context.Service<Service, Interface>()("@opencode/v2/Ripgrep") {}
@@ -128,7 +141,11 @@ const layer = Layer.effect(
             Effect.map((chunk) => [...chunk]),
           )
           const truncated = rows.length > input.limit
-          if (truncated) return { items: rows.slice(0, input.limit), truncated, partial: false }
+          if (truncated) {
+            // #138：达到结果上限后主动 kill，避免超大仓库继续全量扫描
+            yield* handle.kill().pipe(Effect.ignore)
+            return { items: rows.slice(0, input.limit), truncated, partial: false }
+          }
 
           const code = yield* handle.exitCode
           const stderr = yield* Fiber.join(stderrFiber)
@@ -216,8 +233,46 @@ const layer = Layer.effect(
           Effect.catchTag("Ripgrep.InvalidPatternError", (cause) => Effect.fail(failure(cause.message, cause))),
         ),
       grep: (input) =>
-        run<RawMatchData>({
+        Effect.gen(function* () {
+          // #138：先限制候选文件数
+          const maxFiles = input.maxFiles === false ? undefined : (input.maxFiles ?? MAX_SEARCH_FILES)
+          if (maxFiles !== undefined) {
+            // #138：显式单文件目标不走目录候选计数
+            if (input.file) {
+              // single file target is always within bound
+            } else {
+              // #138：--files 预扫候选，超过 maxFiles 则拒绝
+              const candidates = yield* run<string>({
+                cwd: input.cwd,
+                limit: maxFiles + 1,
+                signal: input.signal,
+                args: [
+                  "--no-config",
+                  "--files",
+                  "--hidden",
+                  ...(input.include ? [`--glob=${input.include}`] : []),
+                  "--glob=!**/.git/**",
+                  ".",
+                ],
+                parse: (line) => Effect.succeed(line),
+              }).pipe(
+                Effect.map((r) => r.items),
+                Effect.catchTag("Ripgrep.InvalidPatternError", (cause) => Effect.fail(failure(cause.message, cause))),
+              )
+              // #138：候选过多：抛 SearchTooBroadError 迫使收窄 scope
+              if (candidates.length > maxFiles) {
+                return yield* new SearchTooBroadError({
+                  message: `Search scope is too broad: more than ${maxFiles} candidate files.`,
+                  maxFiles,
+                })
+              }
+            }
+          }
+          // #138：结果条数默认 MAX_SEARCH_RESULTS
+          const limit = input.limit && input.limit > 0 ? input.limit : MAX_SEARCH_RESULTS
+          return yield* run<RawMatchData>({
           ...input,
+          limit,
           args: [
             "--no-config",
             "--json",
@@ -273,7 +328,8 @@ const layer = Layer.effect(
               })
             }),
           ),
-        ),
+          )
+        }),
     })
   }),
 )
diff --git a/packages/opencode/src/tool/grep.ts b/packages/opencode/src/tool/grep.ts
index e44b8e89fe..fd82cfc282 100644
--- a/packages/opencode/src/tool/grep.ts
+++ b/packages/opencode/src/tool/grep.ts
@@ -3,10 +3,15 @@ import { Effect, Schema } from "effect"
 import { InstanceState } from "@/effect/instance-state"
 import { FSUtil } from "@opencode-ai/core/fs-util"
 import { Ripgrep } from "@opencode-ai/core/ripgrep"
+// #138：grep 结果上限与过宽候选处理
 import { assertExternalDirectoryEffect } from "./external-directory"
 import DESCRIPTION from "./grep.txt"
 import * as Tool from "./tool"
 
+// #138：工具输出匹配上限（有界搜索）
+// #138：工具默认 64 条匹配上限（与 source 一致）
+const RESULT_LIMIT = 64
+
 export const Parameters = Schema.Struct({
   pattern: Schema.String.annotate({ description: "The regex pattern to search for in file contents" }),
   path: Schema.optional(Schema.String).annotate({
@@ -60,12 +65,30 @@ export const GrepTool = Tool.define(
           const search = FSUtil.resolve(requested)
           const info = yield* fs.stat(search).pipe(Effect.catch(() => Effect.succeed(undefined)))
           const cwd = info?.type === "Directory" ? search : path.dirname(search)
-          const result = yield* ripgrep.grep({
-            cwd,
-            pattern: params.pattern,
-            include: params.include,
-            limit: 100,
-          })
+          const result = yield* ripgrep
+            .grep({
+              cwd,
+              pattern: params.pattern,
+              include: params.include,
+              // #138：多取 1 条以检测截断
+              limit: RESULT_LIMIT + 1,
+            })
+            .pipe(
+              Effect.catchTag("Ripgrep.SearchTooBroadError", (err) =>
+                Effect.succeed({ tooBroad: err.maxFiles as number }),
+              ),
+            )
+          // #138：过宽候选转用户可读输出，不抛到工具崩溃
+          if ("tooBroad" in result) {
+            return {
+              title: params.pattern,
+              metadata: { matches: 0, truncated: false },
+              output: [
+                `Search scope is too broad: more than ${result.tooBroad} candidate files.`,
+                "Use a narrower path or include pattern before searching file contents.",
+              ].join("\n"),
+            }
+          }
           if (result.length === 0) return empty
 
           const rows = result.map((item) => ({
@@ -74,14 +97,15 @@ export const GrepTool = Tool.define(
             text: item.text,
           }))
 
-          const limit = 100
-          const truncated = rows.length === limit
-          const final = rows
+          // #138：超出 RESULT_LIMIT 截断展示
+          const truncated = rows.length > RESULT_LIMIT
+          // #138：只返回前 RESULT_LIMIT 条匹配
+          const final = truncated ? rows.slice(0, RESULT_LIMIT) : rows
           if (final.length === 0) return empty
 
-          const total = rows.length
-          const hasMore = truncated || result.length === limit
-          const output = [`Found ${total} matches${hasMore ? " (more matches available)" : ""}`]
+          const output = [
+            `Found ${truncated ? `${RESULT_LIMIT}+` : final.length} matches${truncated ? ` (showing first ${RESULT_LIMIT})` : ""}`,
+          ]
 
           let current = ""
           for (const match of final) {
@@ -95,13 +119,14 @@ export const GrepTool = Tool.define(
 
           if (truncated) {
             output.push("")
-            output.push("(Results truncated. Consider using a more specific path or pattern.)")
+            output.push(`(Results truncated: showing first ${RESULT_LIMIT} matches. Consider using a more specific path or pattern.)`)
           }
 
           return {
             title: params.pattern,
+            // #138：metadata.matches 为展示条数
             metadata: {
-              matches: total,
+              matches: final.length,
               truncated,
             },
             output: output.join("\n"),
diff --git a/packages/opencode/src/tool/grep.txt b/packages/opencode/src/tool/grep.txt
index c053cd2028..a307a3bb06 100644
--- a/packages/opencode/src/tool/grep.txt
+++ b/packages/opencode/src/tool/grep.txt
@@ -1,9 +1,10 @@
 # grep 工具：优先专用内容搜索，避免 shell grep 丢上下文
-- Fast content search tool that works with any codebase size
+- Fast bounded content search tool for targeted codebase queries
 - Searches file contents using regular expressions
 - Supports full regex syntax (eg. "log.*Error", "function\s+\w+", etc.)
 - Filter files by pattern with the include parameter (eg. "*.js", "*.{ts,tsx}")
-- Returns file paths and line numbers with matching lines
+- Returns matching file paths and line numbers, with results capped for responsiveness
 - Use this tool when you need to find files containing specific patterns
+- For large workspaces, pass path and include to narrow the candidate files before searching contents
 - If you need to identify/count the number of matches within files, use the Bash tool with `rg` (ripgrep) directly. Do NOT use `grep`.
 - For open-ended search, use glob/grep/read directly first. Consider Task only when exploration splits into 3+ independent branches and does not block your next local step.
diff --git a/packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts b/packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts
index 6a826022a0..7de9aaaa75 100644
--- a/packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts
+++ b/packages/opencode/src/server/routes/instance/httpapi/handlers/file.ts
@@ -8,7 +8,7 @@ import { AbsolutePath, RelativePath } from "@opencode-ai/core/schema"
 import { Effect, Layer, Option } from "effect"
 import ignore from "ignore"
 import path from "path"
-import { HttpApiBuilder } from "effect/unstable/httpapi"
+import { HttpApiBuilder, HttpApiError } from "effect/unstable/httpapi"
 import { InstanceHttpApi } from "../api"
 
 export const fileHandlers = HttpApiBuilder.group(InstanceHttpApi, "file", (handlers) =>
@@ -27,7 +27,14 @@ export const fileHandlers = HttpApiBuilder.group(InstanceHttpApi, "file", (handl
     const findText = Effect.fn("FileHttpApi.findText")(function* (ctx: { query: { pattern: string } }) {
       return (yield* ripgrep
         .grep({ cwd: (yield* InstanceState.context).directory, pattern: ctx.query.pattern, limit: 10 })
-        .pipe(Effect.orDie)).map((match) => ({
+        // #138：SearchTooBroad→BadRequest；其它失败→InternalServerError（保留可区分通道）
+        .pipe(
+          Effect.mapError((err) =>
+            err instanceof Ripgrep.SearchTooBroadError
+              ? new HttpApiError.BadRequest({})
+              : new HttpApiError.InternalServerError({}),
+          ),
+        )).map((match) => ({
         path: { text: match.entry.path },
         lines: { text: match.text },
         line_number: match.line,
diff --git a/packages/opencode/src/server/routes/instance/httpapi/groups/file.ts b/packages/opencode/src/server/routes/instance/httpapi/groups/file.ts
index 389bf91925..6d9a80ccbc 100644
--- a/packages/opencode/src/server/routes/instance/httpapi/groups/file.ts
+++ b/packages/opencode/src/server/routes/instance/httpapi/groups/file.ts
@@ -2,7 +2,7 @@ import { FileSystem } from "@opencode-ai/core/filesystem"
 import { NonNegativeInt } from "@opencode-ai/core/schema"
 import { LSP } from "@/lsp/lsp"
 import { Schema } from "effect"
-import { HttpApi, HttpApiEndpoint, HttpApiGroup, OpenApi } from "effect/unstable/httpapi"
+import { HttpApi, HttpApiEndpoint, HttpApiError, HttpApiGroup, OpenApi } from "effect/unstable/httpapi"
 import { Authorization } from "../middleware/authorization"
 import { InstanceContextMiddleware } from "../middleware/instance-context"
 import {
@@ -108,6 +108,7 @@ export const FileApi = HttpApi.make("file")
         HttpApiEndpoint.get("findText", FilePaths.findText, {
           query: FindTextQuery,
           success: described(Schema.Array(LegacyMatch), "Matches"),
+          error: [HttpApiError.BadRequest, HttpApiError.InternalServerError],
         }).annotateMerge(
           OpenApi.annotations({
             identifier: "find.text",
diff --git a/packages/core/test/ripgrep.test.ts b/packages/core/test/ripgrep.test.ts
index 3abce1c02d..5b47b8e2c0 100644
--- a/packages/core/test/ripgrep.test.ts
+++ b/packages/core/test/ripgrep.test.ts
@@ -62,4 +62,38 @@ describe("Ripgrep", () => {
       (tmp) => Effect.promise(() => tmp[Symbol.asyncDispose]()),
     ),
   )
+  // #138：limit 截断匹配数
+  // #138：验证 limit 截断
+  it.live("grep limit caps total matches", () =>
+    Effect.acquireUseRelease(
+      Effect.promise(() => tmpdir()),
+      (tmp) =>
+        Effect.gen(function* () {
+          yield* Effect.promise(() => Bun.write(path.join(tmp.path, "one.txt"), "needle\nneedle\nneedle\n"))
+          yield* Effect.promise(() => Bun.write(path.join(tmp.path, "two.txt"), "needle\nneedle\nneedle\n"))
+          const matches = yield* (yield* Ripgrep.Service).grep({ cwd: tmp.path, pattern: "needle", limit: 3 })
+          expect(matches.length).toBe(3)
+        }),
+      (tmp) => Effect.promise(() => tmp[Symbol.asyncDispose]()),
+    ),
+  )
+
+  // #138：maxFiles 过小拒绝目录搜索
+  // #138：验证 maxFiles 拒绝过宽搜索
+  it.live("grep refuses overly broad candidate sets", () =>
+    Effect.acquireUseRelease(
+      Effect.promise(() => tmpdir()),
+      (tmp) =>
+        Effect.gen(function* () {
+          yield* Effect.promise(() => Bun.write(path.join(tmp.path, "one.txt"), "needle\n"))
+          yield* Effect.promise(() => Bun.write(path.join(tmp.path, "two.txt"), "needle\n"))
+          const exit = yield* (yield* Ripgrep.Service)
+            .grep({ cwd: tmp.path, pattern: "needle", limit: 10, maxFiles: 1 })
+            .pipe(Effect.exit)
+          expect(exit._tag).toBe("Failure")
+        }),
+      (tmp) => Effect.promise(() => tmp[Symbol.asyncDispose]()),
+    ),
+  )
+
 })
diff --git a/packages/opencode/test/tool/grep.test.ts b/packages/opencode/test/tool/grep.test.ts
index 81b74aa8ba..a1208bde58 100644
--- a/packages/opencode/test/tool/grep.test.ts
+++ b/packages/opencode/test/tool/grep.test.ts
@@ -145,7 +145,7 @@ describe("tool.grep", () => {
       const grep = yield* info.init()
       const result = yield* grep.execute({ pattern: "needle", path: test.directory, include: "*.txt" }, ctx)
 
-      expect(result.output).toContain("(Results truncated. Consider using a more specific path or pattern.)")
+      expect(result.output).toContain("(Results truncated: showing first 64 matches. Consider using a more specific path or pattern.)")
       expect(result.output).not.toMatch(/showing \d+ of \d+ matches/)
     }),
   )
@@ -218,4 +218,27 @@ describe("tool.grep", () => {
       expect(requests.find((req) => req.permission === "external_directory")).toBeUndefined()
     }),
   )
+  // #138：有界输出截断
+  // #138：工具层 64 条上限
+  it.instance("caps broad match output", () =>
+    Effect.gen(function* () {
+      const test = yield* TestInstance
+      const content = Array.from({ length: 130 }, (_, index) => `needle ${index}`).join("\n")
+      yield* Effect.promise(() => Bun.write(path.join(test.directory, "test.txt"), content))
+      const info = yield* GrepTool
+      const grep = yield* info.init()
+      const result = yield* grep.execute(
+        {
+          pattern: "needle",
+          path: test.directory,
+        },
+        ctx,
+      )
+      expect(result.metadata.matches).toBe(64)
+      expect(result.metadata.truncated).toBe(true)
+      expect(result.output).toContain("Found 64+")
+      expect(result.output).toContain("Results truncated")
+    }),
+  )
+
 })
